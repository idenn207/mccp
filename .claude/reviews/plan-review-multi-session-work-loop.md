# Plan Review Panel — multi-session-work-loop

**Plan**: `.claude/plans/multi-session-work-loop-m7.plan.md` · **Plan version**: `sha256:1a5a193cd8ca56782d23b92f0e4312b0190face24dd99f5f06f298657a755261`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 7 blocking finding(s): test/HIGH, test/FAIL, invariant/CRITICAL, invariant/HIGH

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | Task 2 and Task 3 must ship as one atomic commit to prevent C1 being uncomputable between them; only mechanical enforcement is test failure within Task 2's test file, which requires test execution discipline | Lines 110-111, 241-244: Plan acknowledges this and explicitly documents that co-presence checking happens 'in the test file' (pre-commit axis) not as git-level atomicity. No commit-hook or gate prevents separate landing. |
| architect | MEDIUM | Single mapping constant CLOSURE_FROM_ADJUDICATION in findings-registry.js enforces that ACCEPT_NOW cannot be mapped to closure anywhere in the system | DD7 (line 119-124) claims findings-registry.js will be 'the only location' for the mapping and that ACCEPT_NOW's value will be explicitly null. However, findings-registry.js does not yet exist, and existing plan-codex-runner.js or other adjudication handlers could handle verdicts before calling the registry API, creating a bypass channel. |
| architect | LOW | Design documentation will record the dependency of PROMOTE_MIN_SEVERITY constant on CLAUDE.md §3.14 temporary rule before §3.14's release conditions close it | Line 66 promises this documentation will be created; Task 8 (line 187) shows feedback-loop-design.md as deliverable. However, this documentation doesn't exist at plan submission time, so the dependency goes unrecorded through the milestone's development cycle. Only documented in this plan text itself. |
| test | HIGH | Acceptance criterion (line 370) requires `.claude/state/findings/multi-session-work-loop.jsonl` to be committed to git after `/mccp:plan` runs, but the Validate block (lines 314-315) only checks `git ls-files --error-unmatch` (tracked status), not commit status | Plan Acceptance line 370: '그 파일이 git에 tracked 상태로 커밋된다' vs Validate lines 314-315: `git ls-files --error-unmatch` only confirms staging/tracking, not that `git commit` has been run |
| test | MEDIUM | Task 6 Validate references manual confirmation step ('확인') without an automated test: '`.claude/cache/STATUS.md`에 C1 행의 두 수치가 실재하는지 확인' (line 268). No grep/assertion command follows to mechanically verify this | Plan Task 6 Validate (line 268): '그리고 `node plugins/mccp/scripts/derive/cli.js render` 후 `.claude/cache/STATUS.md`에 C1 행의 두 수치가 실재하는지 확인' — ends with 확인 (human confirmation) with no automated check command |
| test | MEDIUM | Task 2 and Task 3 codependency is asserted as enforced (DD10, line 157: '하나의 커밋으로 착지한다'), but Task 2's test `C1-SOURCE-REGISTERED-COPRESENT` will fail if Task 3 code is absent. The plan does not show a mechanism to prevent split landing or validate post-landing that both are present in the same commit | Plan DD10 (line 156-160) claims 'Task 2와 Task 3은 하나의 커밋으로 착지한다' with reasoning 'Task 2 자신의 test 파일이... Task 2-only 트리에서 그 Task의 test가 붉어진다' — This is a forward-reference test failure, not a commit-level enforcement |
| test | MEDIUM | Task 4 Validate requires tests use real filesystem failure paths, not mocks (line 255: '이 test는 fs mock이 아니라 **실제 쓰기 불가 경로** (tmpdir 하위의 존재하지 않는 디렉토리)로 실패를 만든다'), but the Validate line (line 255) just runs test suite without mechanism to verify implementation follows this architectural requirement | Plan Task 4 Validate (line 255): Specifies 'fs mock이 아니라 실제 쓰기 불가 경로' but Validate command is generic `node --test plugins/mccp/scripts/lib/tests/c1-feedback-loop.test.js`. No code review or static check ensures this requirement is met |
| test | MEDIUM | The Validation block at line 323-324 chains a complex one-liner pipe to verify audit sample matching, but line 287 (Task 8) creates `m7-audit-sample.json`. This creates a temporal ordering where validation uses data that isn't created until Task 8 completes, yet validation runs in the middle of task execution | Plan Validation lines 323-324 execute `node plugins/mccp/scripts/derive/cli.js run --json \| node -e '...JSON.parse(fs.readFileSync("docs/multi-session-work-loop/m7-audit-sample.json"...`. File created in Task 8 (line 287), but Validate block references it assuming it already exists |
| invariant | CRITICAL | Task 2 and Task 3 atomicity is enforced only via pre-commit test advisory, not mechanical gate—if tests are skipped, C1 becomes uncomputable silently without blocking | Plan line 241: 'Task 2는 Task 3과 하나의 커밋으로 착지한다...이 원자성은 산문이 아니라 **이 Task가 만드는 test 파일 안**에서 강제한다: `c1-feedback-loop.test.js`가 `SOURCE_SCANNERS`에 `findings` 키가 실재하는지를 직접 단언하므로...Task 2만 착지한 트리에서는 **Task 2 자신의 test가 붉어진다**'. Pre-commit tests are advisory; git commits succeed even if tests fail locally or are skipped. Rollback path is asymmetric: rolling back Task 2 alone leaves the test still-failing, trapping the tree state. |
| invariant | HIGH | `.gitattributes` merge=union glob pattern is never specified in the plan—only that it must exist. If pattern is misconfigured, validation test passes locally but parallel merges fail silently | Plan line 186 ('레지스트리 glob에 `merge=union`을 넣는 것도 이 Task의 작업') and line 93 ('glob을 여기 명시하는 이유는 미지정 glob이 미래 work_unit 이름에 안 걸려도 아무도 모르기 때문이다') establish that Task 1 owns this, but the actual glob pattern string is never written. Validation line 320 tests with `git check-attr merge -- .claude/state/findings/zzz-future-work-unit.jsonl`, which manually specifies a path, hiding pattern mismatches. The receipt is not anchored to the glob declaration—if `.gitattributes` entry is wrong and committed, later files won't get merge=union behavior without triggering any validation gate. |
| invariant | HIGH | Acceptance criteria require live artifacts (findings registry file) that can only be created by implementation, not plan review—circular dependency between plan approval and implementation | Acceptance line 370: '[라이브 완주 산출물]: 이 사이클 자신의 `/mccp:plan` 패널 실행이 `.claude/state/findings/multi-session-work-loop.jsonl`에 `finding_opened` 이벤트를 남기고, 그 파일이 git에 tracked 상태로 커밋된다'. This plan describes M7 work itself; the acceptance gate requires M7 work to be complete. But this is a plan-review gate (approval gate), not an implementation acceptance gate. The plan cannot be approved until it is implemented, violating the gate's purpose. |
| invariant | HIGH | Task 2 test `C1-SOURCE-REGISTERED-COPRESENT` asserts findings key existence, but this test cannot run until Task 3 code exists—if CI runs Task 2 test in isolation before Task 3 is merged, test fails and blocks Task 2 validation | Plan line 243: '`SOURCE_SCANNERS`에 `findings` 키 실재(`C1-SOURCE-REGISTERED-COPRESENT` — Task 3 미착지 트리에서 이 Task의 test가 붉어지는 pre-commit 장벽)'. The assertion checks for existence of code (`SOURCE_SCANNERS` key registration) that Task 3 creates. Validation line 243 requires test to pass, but test imports `derive/index.js` which won't have `findings` registered until Task 3 runs. Validation execution order matters: if test runs before imports complete, it fails. |
| invariant | MEDIUM | CLOSURE_FROM_ADJUDICATION mapping structure is described only in prose, never specified as concrete object—implementer must reverse-engineer from DD2 table (lines 74-79), risk of misalignment | Plan DD2 (lines 68-82) describes mapping behavior: `ACCEPT_NOW` → `null`, `REJECTED_BY_DESIGN` → `closed{type:'invalidated'}`, etc. But plan never shows the actual constant definition. `C1-EMIT-PLAN-CODEX` assertion (line 255) requires 'ADJUDICATION_VERDICTS 전건을 덮고 `ACCEPT_NOW`의 상이 `null`', which means the map must exist and be correct. Without explicit specification, implementation can diverge from intent. Plan lines 119-123 say 'findings-registry.js의 `CLOSURE_FROM_ADJUDICATION` 상수 하나이며' but don't show its contents. |
| invariant | MEDIUM | Plan hash anchoring depends on References section stability, but PRD may mutate after plan review—anchor becomes stale without mechanical detection | Plan line 384-385: 'Stamped at: 2026-08-17T12:44:08.697Z' and 'Anchor: plan body content is hash-anchored by the plan-codex receipt's plan_hash. Any post-stamp PRD mutation in ## References will mismatch on the next /mccp:plan validate.' But this plan references the PRD (line 383: 'Source PRD: .claude/prds/multi-session-work-loop.prd.md'), and the PRD is marked mutable (it's tracked in git, its Evidence section is explicitly 2026-08-09 updated as 'GROUND 실측'). If PRD mutates after this plan review but before implementation, plan_hash and PRD content drift silently. There is no **receipt** anchoring the plan to this specific PRD version. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified all citation targets (msw-events.js:36 allowlist pattern, decision.js:71 slug derivation, intent-context.js ADJUDICATION_VERDICTS, gitattributes current state). Checked if `.claude/state/findings/` is gitignored — it is not. Verified plan correctly identifies Task 1 responsibility for .gitattributes `merge=union` update. Examined Task 2-3 co-presence enforcement (line 241) — it relies on test failure, not atomic transaction. Traced DD2's CLOSURE_FROM_ADJUDICATION claim — module doesn't exist yet, existing call paths unchecked. Examined DD1 mirroring claim to §3.14 — global rule exists but is temporary; project-level documentation of dependency is deferred to Task 8. No contradictions found between plan sections. Atomicity enforcement and mapping singleton are sound designs with documented limitations." |
| security | pass | **Attack Surface — Injection of unvalidated Codex output into system context**: Examined the plan's proposed finding injection into `state-injector.js` (Task 5). Verified that: (1) `cited_path` normalization via `normalizeReceiptCwd` (write.js:52-58) safely catches path traversal (`../` prefix check) and absolute paths, replacing them with `<outside-repo>` placeholder; (2) sanitization pipeline correctly applies security checks (mixed-script, directive detection) BEFORE escaping, not after, matching the existing `intent-context.js` order (lines 410-416); (3) escaping is correct (backslash-first, per line 425) preventing double-encoding; (4) truncation safely handles dangling backslashes (line 437-440); (5) registry writes are gated via Task 7's `APPROVED_REGISTRY_WRITERS` coverage validation to single entry point `findings-registry.js`. **Privilege Escalation — Codex findings elevated from PR body to injected system context without user vetting**: Acknowledged as R11 (HIGH) on plan line 358. Plan proposes limiting to CRITICAL+HIGH only (DD1, constant), wrapping in backticks (line 261), exclusion of directive-form findings (line 261), and assertion `C1-PROMOTE-SANITIZED` to verify sanitization (Assertion Roster line 222). Found that T sanitization does claim to reuse `intent-context.js` functions as described but actual implementation code (state-injector.js modifications) is not shown — however, this is normal for a plan document; implementation verification happens at assertion time. Commitment is present (Files to Change line 175). **Path Traversal via `cited_path`**: Verified that malicious paths (e.g., `/src/../../../etc/passwd`) are caught by normalization logic which converts relative paths containing `../` to `<outside-repo>` placeholder before registry storage. **Registry-only write enforcement**: Examined coverage gate design (Task 7 line 272-280). Found that only registered writer `findings-registry.js` can modify registry files; three emit points (plan-review/cli.js, plan-codex-runner.js, santa/seal.js) call `appendFindings()` which provides single normalization choke point. Static lint validates no direct writes; test ID `C1-COVERAGE-REGISTRY-WRITER` asserts this. **Sanitization test assertions**: Found test IDs in Assertion Roster (line 222) for `C1-PROMOTE-SANITIZED` which should verify directive exclusion, entity handling, and escape truncation, though specific test implementations are not shown in plan document (expected — tests are written during Task 5 execution). Attacked following scenarios without finding bypass: - Homoglyph injection via mixed-script detection - Directive injection via `looksDirective` check   - Entity encoding bypass via bounded single-decode - Path traversal via backslash-newline injection (escaped as literal `\\n`) - Double-escaping via incorrect order (pre-check validates order matches intent-context.js) - Registry bypass via direct filesystem write (gated by coverage validator) |
| test | fail | Attacked plan's verification strategy by: (1) Reading all Validate lines and cross-checking them against actual test files and code paths referenced; (2) Verifying that paths like `plugins/mccp/scripts/derive/index.js:28` for `SOURCE_SCANNERS` actually exist and match plan's Producer Preflight claim (they do — `findings` key is absent as claimed); (3) Checking whether acceptance criteria match validation commands (commit status vs. tracked status mismatch found); (4) Examining test requirements vs. validate lines for specificity gaps (Task 4 mock requirement unverced, Task 6 manual confirmation found); (5) Tracing temporal dependencies between tasks and validation — Task 8 artifact used before it exists. Affirmed: core falsifiability through test assertions, source wiring checks, and emission points are testable; most forward references are properly documented. |
| invariant | fail | Attacked atomicity guarantee for Task 2-3 co-landing: traced git commit model vs test advisory distinction; confirmed pre-commit tests don't block commits. Examined merge=union contract enforcement: found validation test manually specifies paths, obscuring glob pattern errors. Traced acceptance criteria: circular dependency between plan-review gate and implementation artifacts confirmed. Audited assertion coverage for Task 2-3 decoupling: test references code that doesn't exist until Task 3, creating import-order vulnerability. Searched for CLOSURE_FROM_ADJUDICATION explicit definition in plan: found only prose description in DD2 table, no object literal or concrete spec. Examined plan-PRD anchoring: found stamp signature but no receipt to bind plan hash to PRD version; PRD is mutable (tracked git file with post-review evidence edits documented). |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 566196,
  "halt_stage": "5.2e",
  "granted": 4,
  "reviewed_plan_hash": "sha256:1a5a193cd8ca56782d23b92f0e4312b0190face24dd99f5f06f298657a755261",
  "plan_path": ".claude/plans/multi-session-work-loop-m7.plan.md",
  "recorded_at": "2026-08-17T16:24:29.193Z"
}
```
