# Plan Review Panel — leadtime-observability-m3

**Plan**: `.claude/plans/leadtime-observability-m3.plan.md` · **Plan version**: `sha256:098efcc57aeb3c529290f237d768a826f54e639745c5b271b8ac98f9dd22b8e7`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 5 blocking finding(s): architect/HIGH, architect/FAIL, test/HIGH, test/HIGH — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | The new `renderer/sections/leadtime-line.js` section is wired into a slot no composer reads, so the module that owns DD3's 4-way ladder is off the actual render path — and the line that is really rendered must be produced a second time inside `status-grid.js` (md) and inside `renderHeroPanel` (html). The plan therefore has two/three producers of the same surface while claiming a single seam, and the DD3 test (`renderer/tests/leadtime-line.test.js`) will assert the ladder on output that is never emitted. | Plan Task 4.2: "`renderer/index.js` — `safeSection('leadtime-line', …)` + `sections` 배열 확장(기존 인덱스 순서 뒤에 append — 기존 구조분해 소비처 무손상)". But both composers destructure exactly 10 positions: `markdown.js:8` `const [grid, pipeline, fanout, activeSessions, timeline, questions, risks, milestoneHistory, multiSession, mswMetrics] = sections;` and `html.js:1230` identical — an appended 11th element is unread. The md insertion point the plan chose is inside `status-grid.js:253` (`const md = [summaryLine, '', widgetsMd, '', nextActionMd(nextAction)].join('\\n')`), whose function signature is `renderStatusGrid(model, formatUtils, planBody, opts)` (`status-grid.js:100`) — it never receives the section result. The html insertion point is `renderHeroPanel(verdict, grid, projectName, escapeHtml, escapeAttr, formatUtils)` (`html.js:993`, called at `:1365`) — it receives neither `model` nor `sections`. The plan's Files to Change simultaneously pins `markdown.js` as "배치 변경 없음", so no channel from the section to either surface is specified. |
| architect | MEDIUM | Validation 6b asserts every `renderHuman` line is ≤100 columns, but no task changes the line that already violates it, so the plan's own gate cannot pass as written. | Validation 6b: `const over=human.filter(function(l){return l.length>100;}); if(over.length) throw ...`. Task 6b only rewrites the first counter line and the `unmatched[…]` line. The coverage line at `plugins/mccp/scripts/lib/leadtime.js:1119-1123` composes `'  coverage: ' + measurable + '/' + panel_records + ' panel records measurable' + ' — counts below are a LOWER BOUND' + '; panel_span observed X/Y (missing Z)'` — ~114 columns for the live corpus, and it is untouched by any task. |
| architect | LOW | DD3's first ladder row (`model.leadtime`이 없다 — 키 부재 → 렌더 안 함) is unreachable for any model derive produces, because Task 3.2 makes `emptyModel` always declare the key; the real discriminator is `null`, and the plan also asks `validateShape` for a "present-only 객체 검사" which would reject that declared `null` unless null is explicitly allowed. | Plan Task 3.2: "`emptyModel`에 `leadtime: null` 선언, `validateShape`에 present-only 객체 검사 추가". `plugins/mccp/scripts/derive/model.js:15-42` shows `emptyModel` unconditionally sets every top-level key (e.g. `host_version: {...}` at :42), and the cited precedent's present-only check at `:93-100` errors with 'host_version present but not an object' for non-object values. |
| security | MEDIUM | Task 5 mirrors the fixed-name `.tmp` atomic-write helper into a git-TRACKED destination, violating the repo's own tmp-naming rule and leaving a committable orphan on crash / colliding between concurrent writers. | Plan Task 5: "다르면 `.tmp` + `renameSync`" with Mirror `derive/cli.js` line 136-142 (`writeAtomic`), whose implementation is fixed-name: `plugins/mccp/scripts/derive/cli.js:138-142` `const tmp = target + '.tmp'`. That helper's destination is `.claude/cache/` which `.gitignore:142` ignores; DD5 deliberately moves this file OUT of that directory to `.claude/state/leadtime/distribution.json`, which .gitignore does not cover (no `.claude/state/leadtime` entry; `.claude/state/*.tmp` is not ignored either — only `.claude/state/completion-ledger/*.tmp` at .gitignore:71). CLAUDE.md §3.6 states the rule for tracked destinations verbatim: "tmp는 `<target>.<pid>.<rand>.tmp` … tmp 이름이 고정이면 동시 writer가 tmp에서 충돌하므로 pid + nonce가 필수입니다". The sanctioned unique form already exists in the same subsystem: `plugins/mccp/scripts/lib/renderer/trigger.js:195-203` `atomicWriteUnique` → `uniqueTmp(target)`, and it also unlinks the tmp on rename failure — `writeAtomic` does not. Concrete consequence: a crash or a second concurrent `cli.js render` (invocable from `/mccp:dashboard-refresh`, `/mccp:dashboard-audit`, `/mccp:archive-complete`) leaves `.claude/state/leadtime/distribution.json.tmp` — partial JSON, untracked, sitting inside a tracked directory where `/mccp:prp-commit`'s natural-language targeting can sweep it into a commit. |
| security | MEDIUM | DD3's only hide predicate ("키 부재") is unreachable by the plan's own wiring, so the branch that separates "axis never computed" from "measured and blind" does not exist — and the plan's own text already conflates them. | DD3 table row 1: "`model.leadtime`이 없다 (키 부재) \| 한 줄을 **렌더하지 않는다**(`null`)". But Task 3.2 says "`emptyModel`에 `leadtime: null` 선언", so the key is always present, and Task 3.1/DD16 say `scanLeadtime` returns `null` when `leadtimeScan` is off: DD16 "bare derive()(run · validate · perf-budget)는 `model.leadtime = null`로 남아 DD3 1행대로 한 줄이 렌더되지 않는다" — that sentence reads a null VALUE as DD3's key-ABSENT row. Task 4.1 then states "hide 조건은 DD3 표의 첫 행 하나뿐", leaving the section's behavior for `leadtime: null` unspecified; an implementer following DD3 row 4 (`blind` → `미산출` + 사유) would print an explicit absence claim for an axis that was never measured, which is exactly the fabricated-absence the PRD forbids (UI10 "없는 기록을 소급 생성하지 않는다"). No Validation covers it — Validation 3 only asserts the line is PRESENT on the render path, and Task 6's assertion is "`leadtimeScan:false`면 `model.leadtime`이 `null`" (model-level), never that the section hides on null. |
| security | LOW | DD17's publication-boundary claim names a single writer path that is factually narrower than the code: three commands invoke `derive/cli.js render`, so the tracked file is also written by flows the operator did not run for that purpose. | DD17 table: "분포 파일 \| … \| `derive/cli.js#cmdRender` **만** \| 발행물 — 운영자가 `/mccp:dashboard-refresh`로 명시 갱신". Actual callers of that entry point: `plugins/mccp/commands/dashboard-refresh.md:18`, `plugins/mccp/commands/dashboard.md:19`, `plugins/mccp/commands/dashboard-audit.md:90`, and `plugins/mccp/commands/archive-complete.md:97` (Phase 4 RENDER+VERIFY, immediately after a journaled atomic file-moving transaction). All four are human-gated so DD17's 'no ambient hook' invariant survives, but the plan's stated publication semantics ('operator explicitly refreshes') do not — `/mccp:archive-complete` will silently mutate a git-tracked file that its operation journal does not record. |
| test | HIGH | Validation 2d and 6 execute `plugins/mccp/scripts/derive/tests/leadtime-source.test.js`, a test file the plan never creates — no Files to Change row, no Task owns it. It is the *sole* falsifier the plan names for two of its own HIGH risks (sentinel path-leak / DD12 failure path, and `leadtimeScan:false` spawn-free budget), which the plan itself says are unreachable by live probe. Both validate lines will error on a missing path, or be silently dropped, leaving those HIGH risks with zero test coverage. | plan:461 and plan:550 run `plugins/mccp/scripts/derive/tests/leadtime-source.test.js`; Files to Change (plan:136-140) lists only leadtime.test.js, leadtime-surface.test.js, leadtime-distribution.test.js, renderer/tests/leadtime-line.test.js, derive/tests/schema-drift.test.js — Task 6 says "위 Files to Change의 test 5면". plan:599 names Validation 2d as the mitigation for the sentinel-leak HIGH risk; plan:369 states the sentinel branch is unreachable without injection. |
| test | HIGH | DD14's invariant and Validation 2b's negative fixture are mutually inconsistent: the plan's own canonical one-line output contains a value token with NO adjacent coverage (`패널 p50 7.6min`), so `assertCoverageAdjacency` implemented as stated ("커버리지 없는 값 토큰은 존재할 수 없다") reds on the correct implementation, while an implementation loose enough to accept it can also accept the negative fixture unless an unstated exception is hard-coded. Either way the gate does not test the property it claims — the same class of defect (a gate that fails the correct implementation / a no-op falsifier) the plan already absorbed twice. | plan:283 "그 뒤 모든 값 토큰은 자기 커버리지를 괄호로 바로 뒤에 단다. 커버리지 없는 값 토큰은 존재할 수 없다" vs plan:305 canonical text `리드타임 (측정 49/62) · 패널 p50 7.6min · 패널→ship ledger p50 0.38d (11/49) …` (패널 token has no adjacent pair) vs plan:443 negative fixture, which is that same string truncated after `ledger p50 0.38d` and must throw. Task 6 bullet plan:355 restates the unqualified rule. |
| test | MEDIUM | Validation 6b asserts every renderHuman line ≤ 100 columns against the LIVE corpus, while the plan measures the headline at ~99 columns. Field widths grow with the corpus (counts, `427.4min`-class values, 3-digit coverage denominators), so this gate rots on the next gate run — precisely the failure mode the plan's own Validation preamble forbids ("실코퍼스 리터럴 카운트는 쓰지 않는다 … 리터럴은 반드시 붉어진다"). | plan:312 "같은 줄에 붙이면 Task 6b의 100칼럼 상한과 충돌한다(헤드라인만으로 이미 약 99)"; plan:557-562 runs `lt.audit({repoRoot:process.cwd()})` over the live repo and fails if any line `.length>100`; plan:399-401 preamble; PRD:24 "코퍼스는 자기 자신을 늘린다". |
| test | LOW | Validation 3's UI8 assertion (`line.indexOf('ledger')`/`'ship'`) is evaluated against the live corpus only. In the blind/one-anchor states DD3 declares legal, the plan never fixes whether anchor labels still appear, so the placement/UI8 gate is corpus-state-dependent rather than a property test — the same truthiness-vs-key-presence mistake the plan explicitly fixed for Validation 1/4 was not applied to the rendered-line gate. | plan:490-493 rendered-line anchor check; contrast plan:413-416 where the plan deliberately switched to key-presence "truthiness 로 보면 DD3 이 합법이라 선언한 상태에서 실패한다"; DD3 table row plan:174 (한쪽 앵커만 조인됨) and plan:173 (blind). |
| invariant | MEDIUM | Risks 표가 명명한 즉효 롤백 경로가 실재하지 않는다 — `leadtimeScan`은 운영자가 끌 수 있는 토글이 아니라 두 호출부에 박힌 리터럴 `true`이고, 그 롤백을 고정하는 test도 env-contract 등재도 없다. | plan Risks: "커지면 토글을 끄는 것이 즉효 완화" (DD16 행) 대 Task 3.4: "`derive(cwd, {…, leadtimeScan: true})` · `deriveImpl(repoRoot, { worktreeScan: true, leadtimeScan: true })`". plan 전문에 `MCCP_*` 신규 변수 0건이고 Files to Change에 docs/ENVIRONMENT.md·env-contract registry 행이 없다. 즉 완화는 코드 편집이며 receipt-write 경로(trigger.js:292-307 → write.js:1180)가 이미 371ms를 무조건 지불한다. |
| invariant | MEDIUM | HIGH 리스크('실패 sentinel이 절대경로를 git-tracked 파일에 커밋')의 유일한 falsifier가 plan이 만들지 않는 파일 안에 있다. | Validation 2d·6이 `plugins/mccp/scripts/derive/tests/leadtime-source.test.js`를 실행하지만, Files to Change 표(plan :136-140)의 test 5면은 leadtime.test.js · leadtime-surface.test.js · leadtime-distribution.test.js · renderer/tests/leadtime-line.test.js · schema-drift.test.js뿐이고 leadtime-source.test.js CREATE 행이 없다. Task 6도 그 파일을 지목하지 않는다. |
| invariant | MEDIUM | DD17이 '분포 파일을 쓰는 주체'를 `/mccp:dashboard-refresh` 하나로 열거했으나 `cli.js render` 호출자는 넷이며, 그중 `/mccp:archive-complete`는 원자적 파일 이동 트랜잭션이라 rollback이 이 tracked 파일의 dirty를 되돌리지 못한다. | plan DD17 표: "분포 파일 \| … \| `derive/cli.js#cmdRender` **만** \| 발행물 — 운영자가 `/mccp:dashboard-refresh`로 명시 갱신". 실측 grep: plugins/mccp/commands/dashboard.md:19 · dashboard-audit.md:90 · dashboard-refresh.md:18 · archive-complete.md:97 이 모두 `scripts/derive/cli.js render`를 호출한다. archive-complete의 원자성·rollback은 apply.js가 PRD/plan 파일 이동에 대해서만 보장한다(CLAUDE.md §3.11 C2). |
| invariant | MEDIUM | DD3의 hide 술어(키 부재)와 DD16/model.js의 `leadtime: null` 선언이 어긋나, bare derive 모델에서 값이 `null`인 경우가 DD3이 명시적으로 금지한 '렌더 결함(⚠ placeholder)'로 접힐 수 있다 — 미지 입력이 잘못된 방향으로 떨어진다. | DD3 표 1행: "`model.leadtime`이 없다 (키 부재) \| 한 줄을 렌더하지 않는다(`null`)" · Task 4.1: "hide 조건은 DD3 표의 첫 행 하나뿐". 그러나 Task 3.2는 "`emptyModel`에 `leadtime: null` 선언", Task 3.1은 leadtimeScan 거짓일 때 "`null`을 돌려준다" — 즉 키는 항상 존재하고 값만 null이다. `'leadtime' in model` 구현이면 null 역참조 → safeSection ⚠ (renderer/index.js safeSection). Validation 3/3b는 leadtimeScan=true 경로만 돌려 leadtimeScan=false 렌더를 한 번도 검사하지 않는다. |
| invariant | LOW | DD1의 '한 줄 = 파일' 동치는 단일 cmdRender 실행 안에서만 성립하고, DD17이 그 발산을 영구화하는데 Validation 4는 3b가 다른 실행으로 덮어쓴 STATUS.md에 대해 그 동치를 단언한다. | Validation 3b가 `.claude/cache/STATUS.md`를 삭제하고 `trigger.triggerRender`로 재생성한 뒤(plan :510-514), Validation 4가 `formatLeadtimeLine(d).text`(파일 payload)를 그 STATUS.md에서 찾는다(plan :530-533). DD17: "`trigger.js`는 `leadtimeScan`만 켜고 writer는 부르지 않는다" — 두 산출이 서로 다른 코퍼스 시점이면 동치가 깨진다. DD6이 payload에서 모든 시각 필드를 제거했으므로(plan :189) stale 여부를 payload만으로 판별할 앵커도 없다. |
| invariant | LOW | Validation 4b의 `git check-ignore` 게이트는 git 오류(exit 128)를 '통과'로 접는다. | plan :535-537 `git check-ignore -q <path> && { echo FAIL; exit 1; } \|\| echo "4b ok (tracked path)"` — check-ignore는 미무시=1, **오류=128**이며 둘 다 `\|\|` 분기로 떨어져 같은 'ok'를 낸다. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified every structural citation the plan leans on: status-grid.js:239/:253 (summaryLine + grid.md join) and :215 cells label — accurate; markdown.js:57 grid.md passthrough — accurate; trigger.js:229-236/292-297/322-324/return-false paths — accurate; derive/cli.js:136-142/155/195-204 — accurate; leadtime.js fmtMin/fmtDay module-private at :1041-1047 vs module.exports at :1248 (cycle argument holds); by_anchor conditional key at :959-996 (Task 1.3b justified); renderHuman not present in the frozen docs (grep 0 hits, DD15 claim true); .gitignore has no `.claude/state/leadtime/` entry so DD5 holds while `.claude/cache/` at :142 is ignored. I attacked DD1's single-projection claim, DD16/DD17's two-entry-point split, DD12's escaping-by-construction claim, and the derive/render layering — those held. The seam that did not hold is the section-to-composer wiring (positional destructure of exactly 10 in both composers, and a hero builder that receives neither model nor sections). |
| security | pass | Attacked, in order: (1) DD12's \\"escaping is structurally unnecessary\\" — traced `audit()`'s path/record/hash carriers (`leadtime.js:721` `{record: item.e.record}`, `:856` parse_errors, `:660-690` unmatched reasons) and confirmed the reason keys are a closed enum and the projection selects counts only, so the R0 LOW REJECT_YAGNI on escapeHtml holds and Validation 2c/2d actually falsify it; could not land an injection or path-leak scenario on the success path, and H2 already closes the sentinel `err.message` absolute-path leak. (2) DD5's tracked-path claim — read `.gitignore` in full; `.claude/state/leadtime/` is genuinely untouched by every `.claude/state/...` rule, claim verified true. (3) Tamper/partial-state trust on the new tracked artifact — looked for a forged/partial `distribution.json` being believed by a reader; the plan has no reader that trusts it (C7 is out of scope, Validation 4 re-derives from the live projection), so no escalation path exists. (4) Ambient-write surface — enumerated every `triggerRender` caller (`receipt/write.js:1180`, `dispatch-envelope.js:250`, `dispatch-watcher.js:122`, `hooks/render-trigger-session-start.js:25`, `dashboard-server.js:391`) and confirmed DD17 + Validation 3b's before/after comparison genuinely closes the ambient tracked-write path; only the narrower command-set claim failed (LOW above). (5) TOCTOU on concurrent receipt writes — already declared as a pre-existing property with a degraded surface, no new consequence. What did land: the fixed-name `.tmp` in a tracked directory (repo's own §3.6 rule and `atomicWriteUnique` precedent contradict the cited mirror) and the unreachable DD3 hide branch conflating null with key-absence. |
| test | fail | Attacked: (1) every Validate line's path realism against Files to Change — found `derive/tests/leadtime-source.test.js` referenced twice but never created; (2) verified the two strongest code-anchored Validation claims against source — `status-grid.js:214-239` really does build `◐ 진행 중 N` into summaryLine (Validation 3 anchor is correct) and `trigger.js:229-236,344,348` really honors `debounceMs` and returns true only on success (Validation 3b's fix is sound), so I could not refute those; (3) cross-checked DD14's stated invariant against the plan's own canonical output string and Validation 2b's negative fixture — contradictory; (4) checked corpus-growth rot on 6b's 100-column and 3's anchor greps; (5) checked DD1/DD6/DD17 claims for falsifiers and found Validation 3b/4/5 do carry real two-way falsifiers (return-value based, before/after distribution compare) — no defect found there. |
| invariant | pass | 공격한 축: (1) Validation 3b의 `triggerRender` 반환 계약 — trigger.js:229-354를 읽어 debounce/lock/render/write 실패가 전부 false임을 확인, 초안 결함은 실제로 닫혔다. (2) DD5의 tracked 주장 — .gitignore에서 `.claude/state/leadtime/`에 닿는 패턴 0건 확인, 참. (3) DD2의 '강등이 분포를 바꾸지 않는다' — leadtime.js:649-652에서 git 증인이 unmatched 분류에만 쓰임을 확인, 참. (4) DD7 배치 앵커 — status-grid.js:214-253에서 `◐ 진행 중` summaryLine과 `:253` join 인용이 정확함을 확인. (5) DD17의 'ambient hook이 tracked 파일을 만지는 경로 0개' — hook 축은 참이나 command 호출자 열거가 불완전(위 finding 3). (6) leadtimeScan=false / model.leadtime=null 미지 입력의 낙하 방향(finding 4). (7) Validation 8·9의 base-범위 diff 게이트, Validation 5의 반환값 기반 판정, DD12 sentinel 열거형 — 모두 fail-closed로 정직하게 서 있어 반증 실패. HIGH/CRITICAL은 찾지 못했다. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 336783,
  "halt_stage": null,
  "backlog_appended": 5,
  "backlog_skipped_nonblocking": 13,
  "granted": 4,
  "reviewed_plan_hash": "sha256:098efcc57aeb3c529290f237d768a826f54e639745c5b271b8ac98f9dd22b8e7",
  "plan_path": ".claude/plans/leadtime-observability-m3.plan.md",
  "recorded_at": "2026-09-02T09:02:38.870Z"
}
```
