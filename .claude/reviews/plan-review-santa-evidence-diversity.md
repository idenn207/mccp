# Plan Review Panel — santa-evidence-diversity

**Plan**: `.claude/plans/santa-evidence-diversity-m2.plan.md` · **Plan version**: `sha256:f1bc85930291667225838816b22b57d1a3cdaf5a5162dc5968f50248beeebb4f`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 10 blocking finding(s): architect/HIGH, architect/FAIL, security/HIGH, security/FAIL — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | Plan Task 3 does not specify how the consistency rubric row is materialized into the file passed to blind reviewer via `--rubric-file`, creating ambiguity about whether DD5's invariant (consistency row must appear when mode=enforce) is actually enforced | Task 3 Step 2 says 'add directive to append consistency row' (prose), Step 3 says 'write full rubric to temp file' (unimplemented), but shows no code to (a) read the Step 2 prose, (b) extract/append the consistency row, or (c) materialize either into a file. The `lanes` CLI already supports `--rubric-file` (cli.js:960-970), but the plan leaves the mechanism for writing that file completely unspecified. DD5 mandates: 'rubric 행은 상수이고 스코프와 같은 스위치를 탄다' — yet Task 3 Step 1 code extracts `CONSISTENCY_RUBRIC_ROW` into a variable but never shows what Step 3 does with it. Acceptance criteria (line 374) verify paths appear in prompt but not that consistency row appears, leaving UI4/DD5 verification gap. |
| architect | MEDIUM | Plan Task 3 Step 1 shows partial code (scope-always call + error exit) but omits the variable extraction and stdout parsing needed to set `SCOPE_PATHS_JSON`, `$CONSISTENCY_RUBRIC_ROW`, and diagnostic variables (`added`, `pairs`, `unresolved`, `truncated`). This creates ambiguity about whether M2 DD2's invariant ('Step 1 is still sole producer of SCOPE_PATHS_JSON') is preserved or broken by scope-always integration | Plan Task 3 Step 1 code block (lines 248-262) ends after `fi` with comment '그 다음 `paths`를 꺼내 `SCOPE_PATHS_JSON`을 **교체**하고' (then extract paths and replace SCOPE_PATHS_JSON), but the actual bash for `echo "$ALWAYS_JSON" \| node -e 'process.stdout.write(...)'` to extract `paths` and set the variable is not shown. Task 3 mirrors M1 lane block pattern (lines 275) implying the structure is known, but without executable code it's unclear whether the step 1 scope-setter will actually be the only producer or whether the CLI is implicitly taking over scope decisions. M1 DD11 penalty: 'CLI가 스코프를 정하기 시작하면 결정 지점이 둘이 된다' — plan boundary is stated but code is incomplete. |
| security | HIGH | Plan specifies contradictory error handling for non-existent Source PRD files: DD4 requires graceful degradation (drop to unresolved[], do not block round), but Task 2's universal `assertContained` check will throw PATH_ESCAPES_GATE on non-existent paths, causing command exit 2 | DD4 (plan:142-146): 'drop non-existent items, dropping itself does not block the round'; Task 2 (plan:231-234): 'all paths through assertContained → all failures exit 2'; path-containment.js:31-36 throws on fs.realpathSync failure |
| security | MEDIUM | Plan does not specify mechanism to distinguish file-not-found errors from path-traversal errors at the containment boundary, yet both scenarios cause fs.realpathSync to throw the same PATH_ESCAPES_GATE error code | path-containment.js throws identical error code (PATH_ESCAPES_GATE) for all realpathSync failures (line 34); plan Task 2 line 232-234 provides no catch/recovery logic |
| test | HIGH | M2 success metric is measured via regression fixture test that verifies #125 is caught in round 1 | PRD Scope section line 42: 'diff 밖 포착 \| #125 회귀 시나리오(계획⟷PRD 불일치)가 라운드 1에 NAUGHTY \| 재현 fixture 회귀 test'. Plan Task 4 line 290-294 explicitly disclaims: 'This test proves scope, not capture. Whether the reviewer catches the inconsistency is LLM behavior that cannot be asserted in shell code.' |
| test | HIGH | Acceptance criteria verify that the primary success metric is achieved | Plan Acceptance line 374 specifies minimum condition: 'this plan and its Source PRD appear in added and both paths are in the blind prompt'. This verifies scope only, not the actual inconsistency detection that is the PRD success metric. Plan Task 4 line 292-294 explicitly states the test cannot verify actual capture, only scope inclusion. |
| test | MEDIUM | Task 3 provides complete bash code to extract JSON values from scope-always output | Task 3 lines 264-269 describe extracting paths, added, pairs, unresolved, truncated, and rubricRow but provide no actual bash commands. The code block (lines 248-262) ends after calling scope-always CLI and checking exit code. The plan says 'そ の 다음 paths를 꺼내...' (next extract paths) but no jq or node -e syntax shown. Existing code at santa-loop.md line 165-174 uses node -e JSON parsing, but plan doesn't show this implementation detail. |
| test | MEDIUM | Validation section 4 automatically verifies required flag rejection | Line 350: 'node plugins/mccp/scripts/lib/santa/cli.js scope-always --decision x ; echo "expect 2, got $?"' — This echoes the exit code but contains no actual assertion (no [ ] or test command). Manual interpretation required; not automated pass/fail. Contrast with other validation commands (1-3) that run test suites with assertions. |
| invariant | HIGH | Step 1 of santa-loop.md has no specified code path to validate scope-always JSON output contains required fields before use in downstream steps | Plan Task 3, lines 264-269 say Step 1 should extract `paths`, `added`, `pairs`, `unresolved`, `truncated`, and `CONSISTENCY_RUBRIC_ROW` from JSON and validate `paths` is an array. But no code is shown or required. Plan references M1 pattern (HAS_ASSIGNMENT check, santa-loop.md:165-171) as model but does NOT mandate that Step 1 implement an equivalent validation. If extraction of any field fails silently (missing field, JSON parse error in the node -e command), downstream variables become undefined. Step 2 would append an undefined `$CONSISTENCY_RUBRIC_ROW`, degrading the rubric silently. This is fail-open drift because the reviewer receives a broken rubric but the gate does not exit. |
| invariant | HIGH | scope-always JSON output schema is incompletely specified between Task 1 and Task 2 | Task 1 specifies function outputs: `mergeScope` returns `{paths, added, truncated}` (line 200). Task 2 specifies CLI output format for off mode includes 7 keys: `{mode, paths, added, pairs, unresolved, rubricRow, truncated}` (line 226). But Task 1 never documents where `pairs` and `unresolved` come from as return values, nor specifies that any module should export these. Task 2 does NOT specify the output format for enforce mode (only off mode shown). This gap means the implementation could omit fields, and Step 1's extraction would silently fail (fields missing), with no branch to catch it. |
| invariant | MEDIUM | Plan permits scope-always to exit 0 with incomplete JSON, violating the stated invariant that JSON is complete or exit code is non-zero | Task 2 line 233-234 says '실패는 전부 typed error → exit 2. **부분 JSON을 stdout에 내지 않는다** — `out()`은 전 검증 통과 후 1회'. This is the correct requirement: validate all, then output complete JSON, or exit 2 if any validation fails. But the plan does not mandate that scope-always.js enforce this pattern. If a developer implements mergeScope to return incomplete output (e.g., missing `unresolved` field), or cmdScopeAlways calls `out()` before all validations complete, the plan has no gate to prevent it. Task 4 test only checks that the 7 keys exist in normal case (line 288) but does not test edge cases where functions might return incomplete values. |
| invariant | MEDIUM | TMPDIR_SANTA move from Step 3 to Step 1 risks silent path escapes if not synchronized with file operations between Step 1 and Step 3 | Current santa-loop.md defines TMPDIR_SANTA in Step 3 lane block (line 143). Plan Task 3 moves definition to Step 1 (line 249) and says to update Step 3 comment (line 246). Plan does NOT explicitly require removing the duplicate definition from Step 3. If Step 3 keeps its own `TMPDIR_SANTA=` re-definition, the variable is redundantly set twice but works. However, more critically, the plan writes diff scope to `$TMPDIR_SANTA/scope-diff.json` in new Step 1 (line 251), then reads it in Step 1's scope-always call (line 254). But Step 3 also uses `TMPDIR_SANTA` to write `lane-paths-$ROUND.json` (current line 148, after proposed move). If the variable is undefined at Step 1 execution time, `printf` would write to an empty path, failing at containment check. |
| invariant | MEDIUM | Plan does not specify what happens to rubric row in Step 2 if Step 1's extraction of CONSISTENCY_RUBRIC_ROW fails or returns empty | Plan Task 3 says Step 1 extracts CONSISTENCY_RUBRIC_ROW from JSON (line 266), and Step 2 appends it verbatim (lines 270-271). No check is specified between extraction and use. If the extraction yields undefined or empty string (because field is missing, JSON parse fails, or node command errors), `$CONSISTENCY_RUBRIC_ROW` is undefined. Step 2's `echo '... $CONSISTENCY_RUBRIC_ROW'` would append an invalid line like `... undefined` or `... ` (empty). The rubric would be malformed, and the blind reviewer would not receive the consistency check instruction. |
| invariant | MEDIUM | Plan does not mandate or test the `--rubric-file` passing mechanism in Step 3 lane block | Plan Task 3 step 3 (lines 272-274) says to write rubric to file and pass it to `lanes --rubric-file`, claiming this is the 'only path' for blind reviewer to receive consistency rubric. But existing santa-loop.md already uses `--rubric-file` (line 961 in cli.js checks if flag is provided). Plan does not specify: (1) what format the file should be (markdown? plain text?), (2) whether the file should contain only the consistency row or the full rubric, (3) how `lanes` consumes it or embeds it in blind prompt. If `lanes` does not actually use this flag to inject the rubric into blind prompt, the consistency check never reaches the blind reviewer. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified plan's citations of patterns (lanes.js lines 1-100 for pure-oracle, dev/sources/plans.js:18-30 for Source PRD parsing, santa-loop-cap.test.js fixture helpers) — all cited patterns exist and match claims. Traced the proposed integration points: scope-always oracle output schema, lanes CLI rubric handling (cli.js:960-992 does accept and pass rubric to buildBlindPrompt), current santa-loop.md Step structure. Checked M1 DD11 citation for scope ownership boundary — correctly cited and M2 correctly claims to preserve it. However, found that (1) Task 3 Step 1 code is incomplete — variable extraction shown in prose but not in bash, and (2) Task 3 Step 2-3 rubric materialization mechanism is entirely unspecified — plan describes what should happen (write rubric file with consistency row) but shows neither the Step 3 code nor the Step 2 prose that would be needed to make it work. These are not minor gaps: they are the load-bearing implementation details for DD5 (fixed rubric constant) and the acceptance test criterion. Without them, implementer faces ambiguous design with two possible (contradictory) interpretations for how rubric reaches blind reviewer." |
| security | fail | Examined path-containment.js assertContained implementation; checked DD4 against Task 2 specifications; verified Task 4 test expectations; traced error propagation from fs.realpathSync through cli.js error handling; confirmed no specification for distinguishing non-existent-file vs traversal-attempt in the error path. Searched for prior handling patterns in existing codebase — found readJsonStringArray (mirror for Task 2) throws on any realpathSync failure with no graceful degradation for non-existent inputs, but that function's use case (validating mandatory input file) differs from this use case (filtering optional referenced files)." |
| test | fail | Checked: (1) PRD success metrics vs plan acceptance criteria — found misalignment where PRD claims LLM will catch inconsistency but plan only tests that paths are in scope, and plan explicitly disclaims LLM behavior is untestable. (2) Task 3 implementation spec — found bash code for JSON extraction is described but not provided, forcing implementer to infer node -e pattern from existing lanes code. (3) Validation commands — found validation #4 lacks assertions, only prints output for manual review. (4) Module list assertions in santa-loop-cap.test.js — confirmed scope-always.js will need addition to three lists per plan spec. (5) Existing file patterns — confirmed SOURCE_PRD regexes exist in plans.js, CLI dispatch pattern exists in cli.js, santa-loop.md structure exists. (6) Test specifications in Task 4 — confirmed response JSON schema is specified with 7 keys (mode, paths, added, pairs, unresolved, rubricRow, truncated). (7) Risk section — confirmed plan acknowledges gap (Risk 5) and documents it as PRD Open Question per DD7.</refutationAttempted> |
| invariant | fail | Traced fail-closed gates through Step 1 and Step 2 in proposed santa-loop.md changes. Checked that scope-always output schema is fully specified in both Task 1 function returns and Task 2 CLI output. Verified that JSON extraction validation code is shown or required before downstream use. Examined TMPDIR_SANTA move synchronization. Checked that rubric row extraction and use is bracketed by validation. Confirmed that --rubric-file mechanism is implemented and tested. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 702658,
  "halt_stage": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:f1bc85930291667225838816b22b57d1a3cdaf5a5162dc5968f50248beeebb4f",
  "plan_path": ".claude/plans/santa-evidence-diversity-m2.plan.md",
  "recorded_at": "2026-08-19T01:30:57.266Z"
}
```
