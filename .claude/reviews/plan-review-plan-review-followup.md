# Plan Review Panel — plan-review-followup

**Plan**: `.claude/plans/plan-review-followup.plan.md` · **Plan version**: `sha256:0b8cadd28cb729f3837de3942f251e4152d5e9bec573066aef3f5f8ddf587169`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 3 blocking finding(s): architect/FAIL, test/CRITICAL, test/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | Budget gate is dead code due to missing `minRemaining` field in emit-workflow-args payload. The workflow expects this field (plan-review.js:132, 155) but cli.js (lines 262-277) never populates it, causing the budget check to default to 0 and never trigger. | cli.js:262-277 payload object construction has no `minRemaining` key; plan-review.js:132 reads `input.minRemaining` with default 0; plan-review.js:155 condition `budget.remaining() < 0` is structurally unreachable. Mirrors plan-fanout/budget.js which correctly emits `minRemaining` (lines 156, 161, 198). |
| architect | MEDIUM | Seam boundary incompleteness: cli.js and workflows/plan-review.js have a broken contract. The pattern correctly mirrors plan-fanout's budget architecture but incompletely—payload emission (cli.js) is incomplete relative to workflow consumption (plan-review.js), leaving the budget protection non-functional. | plan-fanout/budget.js line 156: `minRemaining = minPerAgent * FLEET_SIZE` is correctly returned in output; plan-fanout integration test would fail if this field were missing (lines 266-279 pure oracle relies on it). plan-review lacks equivalent integration. |
| architect | LOW | Axis B claim about '8 seam defects' is unsubstantiated. The plan states 'command body ↔ oracle seam 결함 8건이 통과했다' but does not enumerate what these 8 defects are, making it impossible to verify whether a live-path acceptance test would catch them. | diverse-agent-review-m1.plan.md Acceptance section (lines 328-347) shows no enumeration of 8 specific seam defects; santa-loop review results in M1 plan show findings across multiple categories (HIGH/MEDIUM/LOW) but not specifically labeled as '8 seam defects'; the followup plan provides no citation. |
| test | CRITICAL | Task 1 validation will prove minRemaining is emitted correctly | Task 1 says 'Validate: node --test plan-review-cli-emit.test.js' but the existing test file (which exists at plugins/mccp/scripts/lib/tests/plan-review-cli-emit.test.js) contains zero assertions about minRemaining, budget, or remaining values. Tests present: fleetKeys presence (line 84), granted capping (line 95), quorum validation (line 107), rolesMin clamping (line 142), reviewedPlanHash binding (line 152). No minRemaining test. The plan lists this file as 'UPDATE' but doesn't specify what test assertions to add. |
| test | MEDIUM | Task 2 validation proves the budget skip behavior returns actual remaining values | Task 2 acceptance criterion: 'budget skip 반환이 실측 remaining을 포함한다'. Task 2 validation: 'node --check plugins/mccp/scripts/workflows/plan-review.js' (line 40, 51). node --check only validates syntax, not runtime behavior. There is no test that executes the budget.total && budget.remaining() < minRemaining branch or verifies the skip return object structure. This is a behavioral test gap. |
| test | MEDIUM | Task 3 validation proves the dry-run acceptance item is functional | Task 3 acceptance criterion: 'diverse-agent-review-m1.plan.md Acceptance에 dry-run 항목이 있다'. Task 3 validation: 'node plugins/mccp/scripts/lib/plan-review/cli.js l1 --plan .claude/plans/diverse-agent-review-m1.plan.md' (line 45). The L1 check validates plan markdown syntax and internal consistency (path references, table structure), not whether a specific item exists in the Acceptance section, nor whether that item actually works. This repeats the defect vector that allowed 8 integration issues to pass unit testing—no end-to-end pathway validation. |
| test | MEDIUM | Validation block covers all acceptance criteria | Plan has 4 acceptance criteria (lines 64-67): (1) minRemaining exists and is proportional to fleet.length, (2) budget.total unset keeps existing behavior, (3) budget skip includes remaining values, (4) dry-run item added. Validation block (lines 49-52) has only 2 commands: generic test glob and syntax check. No specific test for criteria (1), (2), (4). Criteria (3) is only syntax-checked. The plan's Acceptance and Validation sections are misaligned. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Axis A: Verified the missing `minRemaining` field exists in payload code (lines 262-277 of cli.js) and that plan-review.js expects and defaults it to 0 (lines 132, 155). Confirmed plan-fanout implements this correctly. Axis B: Examined M1 plan's Acceptance criteria (lines 328-347) and found no live-path execution requirement—the claim is structurally correct but the specific '8 seam defects' are never enumerated in any cited document, making the evidence incomplete. |
| security | pass | Verified the core claims: (1) cli.js payload omits minRemaining field (line 262-277), plan-review.js expects it (line 132), gate never fires because minRemaining defaults to 0. (2) Budget validation pattern: plan references parseRolesMin but that parses "MofN" strings; the numeric validator should be parseFanoutMinPerAgent which properly checks Number.isFinite() && n > 0. (3) Checked for untrusted input paths: MCCP_PLAN_REVIEW_BUDGET is env-var, operator-controlled, not attacker-influenced. (4) No injection surfaces: minRemaining is used only in numeric comparison (line 155). (5) No TOCTOU issues: budget is static per review run. (6) No privilege escalation: adding a resource gate does not weaken existing security. (7) Axis B (live path acceptance test) is testing/quality improvement, not a security-relevant defect. The plan's only weakness is that it cites the wrong validation function as a pattern reference, but it explicitly asks for validation (non-normal values → default + warn), so a careful implementer will add proper bounds checks. No actual security defect found in the design itself. |
| test | fail | I read the plan-review-followup.plan.md and checked: (a) whether minRemaining exists in the current emit-workflow-args payload (cli.js lines 262-277 — NOT present); (b) whether the test file plan-review-cli-emit.test.js contains any minRemaining assertions (full file read — ZERO matches); (c) whether Task 2's `node --check` can validate behavioral acceptance criteria (--check is syntax-only, no execution); (d) whether Task 3's L1 check validates the dry-run item exists or works (L1 checks markdown syntax/internal consistency, not item presence or execution). I also checked if other test files cover plan-review budget behavior (Grep found budget tests in orchestration/round-budget/design-critique files, but none for plan-review.js budget gate). The core claim in Axis A (minRemaining missing from payload) is correct and evidenced. The gaps in Task-level validation are structural, not content issues." |
| invariant | pass | Axis A budget gate mechanism (deadness of condition, payload emission, default handling, backward compatibility). Axis B acceptance criteria sufficiency (scope of M1 tests vs proposed M2 enforcement). Invariant erosion vectors (fail-closed gates, receipt anchoring, rollback safety). Pattern mirroring to plan-fanout precedent. I attacked the logic of the condition `budget.total && budget.remaining() < minRemaining` with minRemaining unset; backward compatibility if budget.total is unset; evidence quality for claimed "8 seam defects"; and whether document-only fixes are sufficient for debt closure. All attacks withstood. The plan's two axes are well-founded, the fixes follow established patterns, and the risks are explicitly mitigated. No HIGH/CRITICAL findings. |
