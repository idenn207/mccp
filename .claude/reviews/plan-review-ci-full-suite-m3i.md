# Plan Review Panel — ci-full-suite-m3i

**Plan**: `.claude/plans/ci-full-suite-m3i.plan.md` · **Plan version**: `sha256:5a87cc3d3507df0baa87d521c46a0aa3b9261001ce569291e3116dbf0249902a`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 8 blocking finding(s): architect/HIGH, architect/FAIL, security/HIGH, security/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | DD2의 머지 판정 축인 `unexplained === 0`은 게이트가 그것을 평가하는 지점에서 **구조적으로 항상 참**이다 — 즉 이 계획의 중심 차단 조건이 실제 producer 출력에 대해 아무것도 막지 못한다. (a) `enumerate.js:111-132`가 `included ∪ excluded = tracked`를 구성으로 보장하므로 세 번째 버킷은 애초에 공집합이다. (b) `run.js:138-140`의 `deriveAttribution`이 `perFileCount === filesTotal`일 때만 `attribution:'complete'`이고 `run.js:205`의 `ok = measuredAll && attr.ok`이므로, `ok===true`는 `per_file` 원소 수 = `files_total` = \|tracked − excluded\|를 함의한다. (c) DD3의 0단계가 `measurement.ok === true`를 **2단계보다 먼저** 요구하고(plan L318), Task 2b (g2)가 `measured − tracked ≠ ∅`까지 0단계에서 막으므로, 커버리지 오라클이 도달하는 모든 measurement에서 measured = included → `unexplained = ∅`. 따라서 Task 2 Validate 분기 (3)·(5)와 DD2가 열거한 `**/*.test.js` 전면 격리 시나리오(분기 4)는 **합성 fixture로만 성립하는 상태**이고, 실제 차단력은 전부 stage 0의 attribution 불변식·격리 파일 수 상한·digest 앵커링·삭제 래칫에서 온다. 이는 이 계획 자신이 `fully_skipped`를 철회하며 세운 기준(plan L283-286: "합성 fixture로만 단언되는 필드를 스키마에 올리면 실제 CI에서 그 값이 영원히 0이어도 test는 항상 green이다")을 같은 문서의 headline 조건이 위반하는 것이다. | scripts/test-suite/enumerate.js:111-132 (`included`/`excluded`가 같은 tracked 목록의 분할) · scripts/test-suite/run.js:138-140,205,566,577 (`ok`가 `perFileCount === filesTotal`를 요구, `files_total = files.length` = included 수) · plan L318 (stage 0 `measurement.ok === true`) · plan L211 "머지 판정은 `coverage_pct == 100`이 아니라 `unexplained == 0`이다" · plan L1302-1303 Acceptance 2 "게이트 green은 `unexplained == 0`을 말한다" · plan L283-286 (`fully_skipped` 철회 사유) |
| architect | LOW | Task 0이 dispatch 대상 ref로 `ci-full-suite-m3`을 지목하지만 그런 브랜치는 존재하지 않는다(현재 브랜치는 `ci-full-suite-m2`이고 `m3`~`m3i`는 decision slug이지 브랜치가 아니다 — 계획 자신이 L128-130에서 slug 재발행을 설명한다). Task 0은 Task 1·9의 선행이므로 첫 단계가 실행 불가 인자를 들고 있다. | plan L597-598 "`gh workflow run test-suite-baseline.yml --ref ci-full-suite-m3`" 대 git status: `Current branch: ci-full-suite-m2` |
| security | HIGH | The wiring-cut oracle asserts argument *values* for only 2 of the 4 gate arguments. `--base-ref`'s value is unasserted, so changing the judgment line's one token to `--base-ref HEAD` (the exact value this plan's own Validation 4b uses at line 1107) makes `base_set` equal `head_set`, so `missing = base_set − head_set` is permanently empty and DD9's canonical deletion ratchet is silently vacuous — while every unit test, the oracle (token presence only), and CI stay green. This is the same one-token-opens-the-gate shape the plan itself classified CRITICAL for `**/*.test.js` (L250-256), and the plan's R9 absorption explicitly rejected the very deferral rationale ("정본 경로가 바뀌면 무관하게 붉어진다") that it still applies to `--base-ref`. The residual static floor cannot backstop it: the plan concedes its slack is unbounded (L772-775). `--measurement`'s value is unasserted for the same reason and admits the analogous redirect to a canned/stale measurement file (only additionally constrained by the tracked-set and digest checks). | plan L941-948 (assertion 1: "`--exclude-from`과 `--floor-from`의 인자 값이 각각 …이다" — values asserted for exactly those two, `--base-ref`/`--measurement` listed only as tokens); fixture list L996-1002 includes "`--exclude-from`의 값만 다른 파일로 바꾼 YAML" but no base-ref/measurement value-change fixture; L727-735 ratchet semantics; L1107 `--base-ref HEAD`; L772-775 floor slack is unbounded |
| security | LOW | The judgment line and the base-fetch step interpolate `${{ github.base_ref \|\| 'main' }}` directly into `run:` shell text. base_ref is not fork-attacker-controlled (it names a branch in the base repo), so this is not a fork-PR escalation, but a branch name containing shell metacharacters yields command injection inside a merge-blocking workflow, and the plan asserts nothing about quoting. The mirrored precedent has the same pattern, so this is inherited rather than introduced. | plan L846-851 judgment line and L817 fetch step; precedent `.github/workflows/version-declaration-gate.yml:60,68` |
| test | HIGH | The plan's single load-bearing enforcement claim ('이 workflow는 red를 만들고 그 red가 체크를 붉게 한다') has no falsifier for the most direct over-permissive failure: `continue-on-error` on the gate step/job. Task 5 declares '`continue-on-error` 없음' (plan L805) but the wiring-cut oracle's assertion set (plan L941-965: 판정 줄 토큰·인자 값 · fork 방어 넷 · fetch-depth/base fetch · 앞 두 단계 존재 · `paths` 부재) omits it, and the negative-fixture list (f) at L996-1002 enumerates 14 mutated YAMLs without one carrying `continue-on-error: true`. A gate step with `continue-on-error: true` makes `gate.js` exit non-zero while the check reports success — every unit test, the coverage oracle, and the wiring-cut oracle stay green. This is exactly the over-permissive direction the plan asserts it closes for `paths` ('없으면 이 milestone의 최대 주장(전 PR 강제)이 과대허용 방향으로 반증 불가', L963-965), so the omission is asymmetric with the plan's own rule. The risk is concrete rather than hypothetical: the sibling file this same milestone edits carries `continue-on-error: true` twice (.github/workflows/test-suite-baseline.yml:88, :107) and is the nearest copy template. | .claude/plans/ci-full-suite-m3i.plan.md:805 ('`continue-on-error` 없음') vs :941-965 and :996-1002 (assertion/fixture lists, no continue-on-error); .github/workflows/test-suite-baseline.yml:88,:107 |
| test | MEDIUM | Axis D 절단 B의 수용 증거(`reasons`에 `deleted_without_allowance` 포함)가 얻어질 수 있는지가 미정의다. Task 3은 floor를 '이 시점의 tracked 개수'로 두므로(plan L753), tracked test 파일 1개를 삭제하면 `tracked < floor`(`below_floor`)와 삭제 래칫(`deleted_without_allowance`)이 **동시에** 2단계에서 성립한다. 그런데 plan은 `judge`가 stage 2에서 사유를 누적하는지 첫 실패에서 단락하는지 어디에도 못박지 않는다. 단락 구현이면 (e4)와 Validation 4b의 단언은 래칫이 살아 있어도 만족되지 않아 거짓 red이고, 반대로 구현자가 통과시키려 floor를 낮추면 그 축의 backstop이 약해진다. 더 나쁜 방향으로는, 삭제 래칫이 죽어 있어도 `below_floor`가 게이트를 막으므로 'B가 red를 만들었다'는 관측 자체는 여전히 성립한다 — 판별자를 `reasons` 코드로 옮긴 R9 흡수의 취지가 이 겹침에서는 명세 공백 때문에 성립하지 않는다. | .claude/plans/ci-full-suite-m3i.plan.md:753 (floor = 이 시점 tracked 개수) · :988-993 ((e4) reasons 단언) · :1109-1111 (Validation 4b) · :682-685 (reasons 열거 — 누적/단락 미정의) |
| test | LOW | `coverage.js` CLI와 `gate.js`가 '같은 헬퍼를 공유하는 두 번째 진입점이지 두 번째 구현이 아니다'라는 계약은 산문뿐이고, plan 자신이 그것을 어길 때 붉어질 검사가 0건임을 명시한다(짝 단언이 gate 경로에만 있다). 즉 구현자가 CLI에 별도 계산을 넣어도 Validation 검사 2가 게이트와 다른 수치를 조용히 보고할 수 있고 이를 잡을 test가 없다. | .claude/plans/ci-full-suite-m3i.plan.md:636-639 ('중복 구현은 금지다. 두 구현이 갈라지면 짝 단언이 gate 경로에만 걸려 있어 붉어질 검사가 없다') |
| invariant | HIGH | 축 D 오라클의 인자 *값* 단언이 비대칭이다 — `--measurement`와 `--base-ref`는 토큰 존재만 단언되고 값이 단언되지 않아, 워크플로 한 토큰의 값만 바꾸면 게이트가 형태를 유지한 채 완전히 열린다. 특히 `--base-ref`를 `origin/${{ github.head_ref }}`(또는 `HEAD`)로 돌리면 `base_set = head_set`이 되어 DD9 삭제 래칫이 항상 참으로 접히고, `--measurement`를 커밋된 손수 만든 JSON으로 돌리면 0~3단계 전부가 그 파일을 판정한다. 두 경우 모두 `wiring-cut.test.js`의 단언 1(토큰 존재)·2(fetch-depth·base fetch 단계 존재)·3(paths 부재)이 전부 만족되고 모든 단위 test가 green이다. | plan L941-948: 단언 대상은 `gate.js · --measurement · --exclude-from · --floor-from · --base-ref` 다섯 토큰인데 값 단언은 `--exclude-from`/`--floor-from` 둘뿐 — '값을 단언하지 않으면 DD7의 통제를 지키는 기계가 0건이고, 같은 PR이 인자를 리뷰 표면이 좁은 다른 파일로 돌리면 DD2 3조건과 DD9 래칫이 함께 무력화된 채 모든 test가 green이다'라는 근거가 나머지 둘에 동일하게 적용되는데 적용되지 않았다. 판정 줄 자체도 `--measurement <measurement.json>`(plan L847)로 정본 경로를 pin하지 않는다. 삭제 래칫이 base 해소에 전적으로 의존한다는 사실은 plan L727-735·L506-518. |
| invariant | MEDIUM | 게이트 기계 자체가 고장 났을 때의 복구 경로가 계획 어디에도 없다 — 전 PR·paths 필터 없음·fail-closed 조합이라 `gate.js`/`coverage.js`/floor 파일의 결함은 즉시 저장소 전체를 머지 불가로 만들고, 그것을 고치는 PR 역시 같은 게이트를 통과해야 한다. 계획은 이 상태를 두 번 위험으로 인지(‘전 PR 영구 red … 유일한 출구는 게이트 완화’)하면서 그 출구를 금지만 하고 승인된 대체 경로(관리자 우회 절차·kill switch·runbook 항목)를 지정하지 않았다. | plan L810-812·L84 '해소에 실패하면 게이트가 전 PR에서 영구 red가 되고 그 상태의 유일한 출구는 이 계획이 금지한 게이트 완화다'; 그러나 Task 8(L1010-1019)의 런북은 'branch protection 수동 1회 절차와 drift 진단 사용법'만 요구하고 UI6이 정책 논의를 배제해 우회/롤백 절차가 어느 산출물에도 배정되지 않았다. Acceptance(L1291-1316)에도 롤백 왕복 항목이 없다. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | 인용 검증: `run.js:10-12/:213-214/:179-183/:205/:407-410/:566-577/:600`, `enumerate.js:49-76/:111-132`, `reporter.mjs:125-153`(per_file `file` 키가 repo-relative posix인지), `redact.js:245-270`, `version-declaration-gate.yml:21-24/:57-60/:68`(fetch-depth·base fetch·`\|\| 'main'` 미러 주장 — 정확), `test-suite-baseline.yml:11-17/:33-38`(DD1의 \\"pull_request 사유 소멸\\" 주장 — 정확), `test-suite.test.js:696-720`(Task 4의 `childEnv` 회귀 논증 — 정확, CLI spawn 경로 없음). 공격 시도: (1) `--base-ref HEAD`를 쓰는 Validation 4b가 `git rm` index 반영으로 실제로 `missing≠∅`를 내는지 — 성립. (2) `exclusions_digest` 앵커링이 전수 실행 step에서 `--exclude-from`이 빠지는 배선 누락을 덮는지 — digest 불일치로 fail-closed, 덮인다. (3) 러너가 CI에서 codex를 부르는지 — `FORCED_POLICY_ENV`(run.js:440,458)가 기본 비활성, 문제 없음. (4) `coverage.js`가 두 번째 차단 경로가 되는지 — 계획이 헬퍼 공유로 닫음. (5) 버킷 완전성이 실제 불변식을 담는지 — **여기서 반증 성공**(위 HIGH). (6) merge-ref 체크아웃에서 base 집합 대조가 뒤집히는지 — merge ref가 base 파일을 포함하므로 추가 무감·삭제 반응이 성립, 결함 없음. |
| security | fail | Attacked, and could NOT land: (1) fork-PR arbitrary code execution — DD4 pins `permissions: contents: read`, `persist-credentials: false`, no `pull_request_target`, SHA pin, and Task 7 assertion 1b mechanically asserts all four; baseline's own credential-leak rationale (test-suite-baseline.yml:60-67) is carried forward. (2) Absolute-path/cwd leakage into a tracked artifact — the durable-receipt precedent does not apply; artifacts are untracked and pass redact.js; the widened exposure *frequency* from dropping `paths` is honestly recorded (plan L369-373). (3) exclusions_digest anchoring — verified `enumerate.js:84-90` digests normalized `(pattern, reason)` only, so DD7's added `ticket` field cannot desynchronize the anchor, and a run with no `--exclude-from` fails the digest check fail-closed. (4) `per_file: null` and `Number(r.exit_code) \|\| 0` fold citations — both verified verbatim at run.js:179-183 and :213-214, so stage 0 is correctly motivated. (5) Codex spawn in CI — `FORCED_POLICY_ENV`/`childEnv` (run.js:440-460) forces `MCCP_CODEX_DISABLED=1` unless `--allow-codex`, which Task 4 blocks at flag parsing under `GITHUB_ACTIONS=true`. (6) Stale/forged measurement generally — bounded by `unexplained` (tracked−measured) and the new `measurement_tree_mismatch` (measured−tracked), and residual forgery is explicitly recorded as unclosed in DD4a with a defensible reason. (7) `persist-credentials:false` breaking the base fetch — checkout@v4 `fetch-depth: 0` fetches all refs authenticated before stripping the header, so `origin/<base>` exists. What did land is the asymmetry in (item 1 above): the plan closed the argument-value axis for two arguments and left the other two, including the one whose vacuous value its own Validation script demonstrates. |
| test | fail | Read the full plan (all 1321 lines) and PRD, then attacked: (1) source-citation accuracy — verified run.js:10-12/:179-183/:213-214/:489-505 say what DD3/DD9 claim (they do; `exit_code: null` + `Number()\|\|0` fold and `per_file: ok ? perFile : null` are exact); (2) whether each Task's Validate line names a command exercising what it changes — Task 1's Linux-only reds cannot be verified by its local `node --test` line but the plan says so explicitly and defers to CI, so not a finding; (3) the over-permissive direction of the gate — found the `continue-on-error` hole in the oracle/fixture set (reported); (4) axis-D B's evidence obtainability under floor/ratchet overlap (reported); (5) Validation shell logic in checks 3, 4, 4b (the `&&`/`\|\|`/`exit 1` construction and `--base-ref HEAD` with `git rm` semantics are correct); (6) whether the coverage denominator claims are pinned by pair assertions ((12) and gate (i)) — they are; (7) whether existing tests encode the behaviour being changed — checked scripts/tests/test-suite.test.js L701/L720 `childEnv` collision, which the plan already absorbed by moving the guard to flag parsing and adding a `GITHUB_ACTIONS=true` local run. Found nothing further. |
| invariant | fail | plan 전문과 PRD를 읽고 `scripts/test-suite/run.js`의 인용 근거(:10-12·:179-189·:210-216·:265-330·:489-538·:576-604·:724)를 실제로 대조했다 — `ok`/`exit_code`/`per_file: null`/`exclusions_digest`/`foldChunks`의 0-접힘 서술은 전부 소스와 일치했다. 열어보려 시도한 게이트: (1) stage 0 우회(spawn 실패 measurement) — 닫혀 있음, (2) `**/*.test.js` 단일 glob — `max_excluded_files` 등가 단언으로 닫힘, (3) 격리 없이 돈 측정 + 큰 격리 목록 짝짓기 — digest 앵커로 닫힘, (4) 삭제 1건 + 추가 1건 상쇄 — 집합 차로 닫힘, (5) 격리 전부 덮어 tracked 0 — 빈 tracked fail-closed로 닫힘, (6) run.js를 `--exclude-from` 없이 돌려 digest 회피 — 불일치 차단, (7) 4b의 `--base-ref HEAD` + `git rm` 조합이 missing을 실제로 비우는지 — 비우지 않음. 열린 것은 오라클의 인자 *값* 비대칭(위 HIGH)과 게이트 자체 고장 시 롤백 경로 부재(MEDIUM)뿐이다. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 265213,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:5a87cc3d3507df0baa87d521c46a0aa3b9261001ce569291e3116dbf0249902a",
  "plan_path": ".claude/plans/ci-full-suite-m3i.plan.md",
  "recorded_at": "2026-09-04T03:17:32.900Z"
}
```
