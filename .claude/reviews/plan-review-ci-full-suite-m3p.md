# Plan Review Panel — ci-full-suite-m3p

**Plan**: `.claude/plans/ci-full-suite-m3p.plan.md` · **Plan version**: `sha256:17145c7c2957da848b3eedfb9daf7d39a9db2ff4e93d1aefb406d59098d3cacb`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 7 blocking finding(s): architect/HIGH, architect/FAIL, test/HIGH, test/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | DD9의 두 래칫이 시간축에서 다시 서로의 출구를 지운다 — `tracked_basis`의 유지 규칙이 없어, 정식 등재된 삭제가 두 사이클 반복되면 저장소 전체가 `below_floor`로 영구 red가 되고 유일한 출구는 이 계획이 '기계로 확인되지 않는다'고 스스로 표시한 `tracked_basis` 하향 편집이다. R14/R15가 닫았다고 주장한 '여백'은 단 1회분만 존재한다. | plan:1027-1035 `tracked_basis`는 '작성 시점의 tracked 개수를 그대로 적는다'(불변 상수) ∧ floor `tracked = tracked_basis − (max_allowed_deletions + 1)` · plan:1079-1083 `max_allowed_deletions`는 `allow_deletions.length`와 **등가**로 pin · plan:722-724 '머지 뒤 그 항목은 정리한다 … 정리하지 않으면 목록이 단조 증가한다'. 세 규칙을 함께 적용하면: 1차 삭제 PR(mad=1 → floor=basis−2, tracked=basis−1) 통과 → 정리 PR(mad=0 → floor=basis−1, tracked=basis−1) 통과 → 2차 삭제 PR(mad=1 → floor=basis−2, tracked=basis−2) 통과 → **2차 정리 후 floor=basis−1 > tracked=basis−2 → 이후 모든 PR이 stage 2 `below_floor`**. plan:1093-1097이 유일한 해소책(`tracked_basis` 하향)을 '어떤 단언도 붉어지지 않고 backstop이 조용히 약해지며 막는 것은 리뷰뿐'이라고 명시한다. plan:731-737이 금지한 '실제 출구가 floor 하향뿐이 되는 상태'와 정확히 같은 형태다. |
| architect | MEDIUM | 삭제 래칫의 base 집합이 **움직이는 tip**(`origin/$BASE_REF`)인데 head 집합은 그 시점에 고정된 merge ref의 index라, base가 앞서 나간 뒤 재실행되는 PR에서 base-only 신규 파일이 `missing`으로 들어와 아무것도 지우지 않은 PR이 `deleted_without_allowance`로 막힌다. '집합 차는 추가에 무감하다'는 주장은 head 쪽 추가에만 참이다. | plan:706-718 `base_set`을 `git ls-tree -r <base>`(=`origin/$BASE_REF`, plan:1256-1264의 판정 줄)로 세우고 `missing = base_set − head_set`이 비지 않으면 차단 · plan:717-718 '집합 차는 추가에 무감하고 삭제에만 반응한다'. 그러나 `actions/checkout`이 체크아웃하는 것은 이벤트 payload의 merge SHA이고(plan:1196-1203이 그 사실을 스스로 인용) `origin/<base>`는 판정 시점에 fetch되는 tip이므로(plan:1204 `git fetch --no-tags origin "$BASE_REF"`), 두 입력의 기준 커밋이 다르다. merge-base 또는 payload base SHA가 아니라 tip을 쓰는 선택에 대한 논증이 계획 어디에도 없다. |
| security | LOW | gate.js is a NEW artifact-producing surface (gate.json uploaded on every PR) but the plan imposes no redaction requirement on its output, unlike measurement.json. Its six fail-closed branches ("git 호출 실패", "--measurement 부재/판독 불가", "--base-ref 해소 실패") naturally surface spawn/git error text, which carries absolute cwd paths — the exact class redact.js exists to stop, and the class this repo already paid for once (CLAUDE.md §3.12 sanctioned cwd re-seal). | plan:921 "판정 단계는 `--json`으로 이 출력을 `gate.json`에 쓰고 artifact로 올린다." vs plan:558 which scopes the redaction claim only to the runner output ("그 내용은 `scripts/test-suite/redact.js`를 이미 통과한 산출이다"); fail-closed six enumerated at plan:946-953 with no redaction constraint; the plan's own prior-round note at plan:1783 says redact.js is the "Good pattern for any M3 artifact/log surface that might carry PR-controlled data". |
| security | MEDIUM | Accepted-but-real residual: removing the `paths` filter plus unconditional `if: always()` upload means every PR publishes measurement.json even when the gate blocks at stage 3 for `redaction_ok === false` — i.e. the detected leak is still published, at a frequency the plan itself says is now broader. The plan discloses this and states the trade-off, so it is a residual rather than an omission; recording it so the next reader does not treat stage-3 blocking as leak prevention. | plan:672-678 "`redaction_ok` 차단은 **발행을 막지 못한다** … `if: always()`이므로 게이트가 3단계에서 차단해도 유출본은 **조건 없이** 공개 artifact가 된다 … 이 milestone은 증거를 택했다"; frequency widening at plan:560-564; that redaction_ok=false is observed in practice, not hypothetical, at plan:1584. |
| test | HIGH | The workflow-shape oracle closes only one of at least four isomorphic over-permissive neutralisation paths. Task 7 assertion 1c asserts `continue-on-error` absence (absorbed at R10 precisely because "판정 단계에 `continue-on-error: true` 한 줄이면 gate.js가 비영점으로 죽어도 체크는 success를 보고하고 ... 전부 green이다", plan:1435-1442), but nothing in assertions 1/1b/1c/2/2b/3/4 or the negative-fixture list (plan:1532-1543) asserts that the judgment step is unconditional. A step-level `if:` on the gate step, a job-level `if:`, or narrowing `on.pull_request.branches`/`types:` each neutralise the merge gate while every listed assertion stays green — the plan's central claim ("전 PR 강제", assertion 3 at plan:1474) remains unfalsifiable in exactly the over-permissive direction it says it is closing. The plan even proves it thinks about `if:` only in the evidence-preserving direction (`if: always()` on upload, plan:1150, 1465-1466), never in the gate-disabling direction. | .claude/plans/ci-full-suite-m3p.plan.md:1435-1442 (continue-on-error assertion + rationale) vs :1400-1490 (full assertion list) and :1532-1543 (negative fixtures) — no `if:` / `branches:` / `types:` fixture or assertion anywhere; grep for `if:` in the plan returns only `if: always()` upload occurrences (lines 203,222,558,673,1150,1152,1182,1466). |
| test | LOW | DD7 claims all three exclusion controls are machine-pinned, but the exclusion *entry-count* cap has no declared home. `.github/test-suite-floor.json` is specified with exactly five constants (tracked_basis · tracked · max_excluded_files · max_allowed_deletions · allow_deletions) and gate.js's fail-closed key enumeration is fixed to that same five, so the entry-count cap lives in an unnamed in-module constant; Task 3's Validate line ("상한 상수와 파일 항목 수 일치 단언") does not say which constant or which file, leaving the equivalence claim without a checkable target. | .claude/plans/ci-full-suite-m3p.plan.md:781 (floor.json = 다섯 상수) and :946-951 (fail-closed 키 열거 다섯) vs :634-638 ("기계적 상한 셋 … 그 등가를 Task 3의 단언이 기계로 확인한다") and :1073-1075 Validate wording. |
| invariant | HIGH | 삭제 래칫의 base 입력에 대칭 fail-closed가 없다 — head가 빈 집합이면 차단하는데 base가 빈 집합이면 래칫이 조용히 항상 통과한다. 계획이 '실제 차단력 둘' 중 하나로 지목한 축이 unknown 입력에서 관대한 방향으로 접힌다. | plan L946-953 fail-closed 여섯: 'git 호출 실패 · 빈 tracked 목록 · --measurement 부재/판독 불가 · --floor-from … · --exclude-from … · --base-ref가 주어졌는데 merge base를 해소하지 못함' — head에는 '빈 목록'이 열거되는데 base에는 '해소 실패'만 있다. L706-710이 base_set을 `git ls-tree -r <base> --name-only`로 만드는데, ref가 해소되면서도 출력이 비는 구성(잘못된/얕은 ref, 트리 없음)에서 exit 0 + 빈 stdout이면 `missing = base_set − head_set = ∅`이 되어 stage 2 래칫이 무조건 통과한다. Task 2b (k)의 다섯 분기(L1000-1008)와 (j)의 여섯(L985-987) 어디에도 'base_set이 비면 차단'을 재는 단언이 없다. |
| invariant | HIGH | floor 도출식과 `allow_deletions` 정리 의무가 결합해, 두 번째 정상 삭제 이후 저장소 전체가 `below_floor`로 영구 red가 된다 — 그리고 유일한 출구는 계획이 '기계로 확인되지 않는다'고 적은 `tracked_basis` 하향이다. | Task 3 L1030 `tracked` floor = `tracked_basis − (max_allowed_deletions + 1)` + L1084 도출 등가 단언(순수 JSON, 트리 미판독) · DD9 L722-724 '머지 뒤 그 항목은 정리한다 … 정리하지 않으면 목록이 단조 증가한다'. 산술: basis B에서 면제 1건(D=1) → floor=B−2, 삭제 후 tracked=B−1 통과 → 머지 후 정리(D=0) → floor=B−1 (경계). 두 번째 삭제 사이클도 같은 절차를 밟으면 tracked=B−2, 정리 후 floor=B−1 → `tracked < floor`로 **모든 PR이 red**. `tracked_basis`의 갱신 규칙은 계획 어디에도 없고, L1093-1097은 그 값을 낮추는 편집을 '어떤 단언도 붉어지지 않는' 리뷰 의존 약화로만 기술한다. 즉 계획이 반복적으로 CRITICAL로 닫았다고 적은 '유일한 출구가 게이트 완화'인 상태가 삭제 축에 남는다. |
| invariant | MEDIUM | `gate.js`가 blocked(비영점)일 때도 `--json` 산출을 실제로 방출한다는 요구가 어디에도 단언되지 않았다 — 축 D 증거(red run의 gate.json)와 Validation 4b가 전부 그 미명시 동작에 매달려 있다. | Acceptance 3-A/3-B(L1879-1889)와 Task 7 (e2)(e4)는 red run의 gate 출력(stage·reasons)을 수용 증거로 요구하고 Task 5(L1152-1158)는 `if: always()` 업로드로 그것을 건진다고 적는다. Validation 4b(L1676-1682)는 `gate.js … --json > /tmp/m3-del.json; DEL_RC=$?` 뒤에 `require('/tmp/m3-del.json').reasons`를 읽는다. 그러나 Task 2b의 gate 분기 (a)~(n) 중 (l)(L996-999)만 `--json`이 coverage를 싣는 것을 재고, **blocked 분기에서 JSON이 stdout에 실제로 기록됨**을 재는 단언은 0건이다. 진단 메시지를 stderr로 내고 비영점으로 죽는 구현은 (m)의 종료코드 단언까지 전부 만족하면서 4b를 파손 JSON으로 hard-fail시키고 CI red run의 증거를 0으로 만든다 — 계획이 R11·R12·R13·R14에서 네 번 흡수했다고 적은 '증거 요건에 CI producer가 없다'의 다섯 번째 형태다. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | 인용 검증: `run.js:10-12`·`:179-183`·`:205-219`(ok/exit_code 분리, `per_file: ok ? perFile : null`)와 `version-declaration-gate.yml:21-24`·`:56-60`·`:62-65`(fetch-depth 사유·base fetch 단계·가드보다 앞선 판별력 test)를 열어 계획의 주장대로임을 확인했다. 공격한 축: (1) `gate.js`가 'DD3 판정 순서의 유일한 소비처'라는 계약 — `judge({measurement, coverage, deletions})` 서명과 fail-closed 여섯, 2단계 내 누적 분기 (n)까지 배선돼 있어 우회 지점을 못 찾았다. (2) 절단 A/B가 판정보다 앞선 자기 test 단계에 걸리는지 — 선택 규칙 (e5)(e5a)와 순수 JSON 도출 단언이 트리 의존을 `max_excluded_files` 등가 하나로 줄여 성립한다. (3) `tracked` 채널 이중 소유(`inputs.js` 대 `run.js/enumerate.js listTrackedFiles`) — 열거 sanity 검사 1이 약하게나마 덮고 계획이 그 약함을 명시한다. (4) 오라클 스캔 범위(판정 줄 안 + `env:` 블록) 비대칭 — 규칙('단언은 값이 정의된 자리를 본다')이 일관되고 음성 fixture가 그 형태를 덮는다. 실제로 깨진 것은 시간축의 floor 유지 규칙(위 HIGH)과 base 기준 커밋 선택(MEDIUM) 둘이다. |
| security | pass | Attacked: (1) fork-PR threat model — DD4's five defenses (permissions contents:read, persist-credentials:false, no pull_request_target, SHA pin, no secrets) are each pinned by negative YAML fixtures at plan:1539-1543, so a silent weakening edit goes red; could not land a bypass. (2) Shell/expression injection — verified the plan replaced the false \\"quoting is defense\\" claim with env indirection (plan:1263-1269) and that the assertion now follows the value into the step's `env:` block (plan:1416-1420), closing the `BASE_REF: ${{ github.head_ref }}` swap; `github.base_ref` is base-repo controlled, so no attacker-controlled value reaches the fetch/--base-ref argument. (3) Ratchet forgery — tried the `**/*.test.js` single-glob exclusion, count-offset deletion (add 1 / delete 1), `--base-ref HEAD`, `--measurement` pointed at a hand-written JSON, missing `--floor-from`/`--exclude-from`, and floor-key-absent defaults; each is explicitly closed (plan:439-447, 712-718, 946-953, 1532-1538). (4) Codex spawn in CI — confirmed scripts/test-suite/run.js:440,458 forces MCCP_CODEX_DISABLED=1 by default and Task 4 puts the CI guard at the flag-parse point, outside the two tests that call childEnv directly (run.js:655-663, plan:1123-1133). (5) Path traversal via exclusions patterns — patterns are globs matched against a tracked path set, never used for fs reads. (6) Gate forgery (same-tree judge) — real, but explicitly recorded as not-closed with a stated reason in DD4a (plan:659-684), which matches the code (gate.js does not compare git_sha to HEAD). Found no HIGH/CRITICAL; the two items above are the honest residuals. |
| test | fail | Verified load-bearing source citations rather than trusting the plan: enumerate.js exports `enumerateTests`/`exclusionsDigest` used by the Validation 4b inline synthesizer (enumerate.js:84-143); run.js:725 `return result.ok ? 0 : 1`; run.js:728-747 confirms `main`/`parseArgv` are NOT exported, so Task 4's spawnSync seam claim holds; `failing` really is emitted by run.js:215/:590 and reporter.mjs:203, so Acceptance 3-A's evidence has a producer. Checked the arithmetic of Validation 4b (git rm → base_set−head_set non-empty; floor = basis−(D+1) so a 1-file cut stays above floor) and of Task 2 branch (7) — both satisfiable. Checked whether cut A/B producers survive the pre-judgment self-test step (covered by (e5a)/(e5) selection rules and the pure-JSON floor identity). Checked whether the deliberately-unasserted step *order* reopens R11's circularity — it does not, since the self-test step's own exit code reddens the job regardless of position. Attacked Task 1's Validate (local single-file green cannot falsify a Linux-only fix) but Task 5's Validate delegates adjudication to CI, so it is covered. The one gap I could evidence is the asymmetric workflow-shape oracle: `continue-on-error` is asserted absent, but conditional execution (`if:`) and trigger narrowing (`branches:`/`types:`) are not, and they are the same over-permissive class. |
| invariant | fail | DD3 판정 순서(0~3단계)를 run.js 실측으로 대조했다 — `foldChunks`의 `Number(r.exit_code) \|\| 0`(run.js:179-183)·`per_file: ok ? perFile : null`(:213-214)·`exclusions_digest`(:580)·`files_total`(:577) 인용은 전부 참이었고 stage 0 신설은 실제 구멍을 닫는다. `enumerate.js`의 `exclusionsDigest`/`enumerateTests` export도 확인해 Validation 4b의 인라인 스크립트가 실행 가능함을 검증했다(export 부재로 인한 정적 파손 없음). 다음 경로들을 열려고 시도했으나 열리지 않았다: (1) `--base-ref HEAD`로 base=head 붕괴 — staged `git rm`은 ls-tree HEAD를 바꾸지 않아 4b가 성립한다. (2) stale base로 인한 위양성 — `pull_request`는 merge ref를 체크아웃하므로 base의 신규 파일이 head에 포함된다. (3) `--allow-codex` CI 가드의 자기파괴 — 파싱 지점 이동이 test L701·L720의 사거리를 실제로 벗어난다. (4) 축 A/B 절단의 자기 test 결합 — (e5)(e5a) 선택 규칙과 순수 JSON 도출 단언이 트리 의존을 실제로 제거한다. (5) `continue-on-error`·`paths`·secrets·SHA pin의 오라클 사거리 — Task 7 (1b)(1c)(3)(f)가 음성 fixture까지 덮는다. 열린 것은 위 셋이다: base 입력의 비대칭 fail-open, floor 도출식의 시간축 wedge, blocked 경로의 JSON producer 미단언. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 192981,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:17145c7c2957da848b3eedfb9daf7d39a9db2ff4e93d1aefb406d59098d3cacb",
  "plan_path": ".claude/plans/ci-full-suite-m3p.plan.md",
  "recorded_at": "2026-09-04T04:22:44.459Z"
}
```
