# Plan Review Panel — codex-intent-context

**Plan**: `.claude/plans/codex-intent-context-m2.plan.md` · **Plan version**: `sha256:febac86e22eb089e70561debd72894e7c617ce78235f29a8509291de8c57ab32`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 7 blocking finding(s): test/HIGH, test/HIGH, test/HIGH, test/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| test | HIGH | Task 2 adds 7 new HTML block test cases to catch backlog bugs (CDATA/DOCTYPE/PHP/comments/type-7-tags/false-block-recovery/unclosed-comments) | plan.md Task 2 describes in prose: '(a) backlog 실측 3케이스 재현 — `<![CDATA[` · `<!DOCTYPE html>` · `<?php` 안의 `INTENT:`가 `unclaimed`이고, fence 안 `<!--` 뒤의 진짜 주장이 `claimed`' ... (7 cases total). However, searching `plugins/mccp/scripts/lib/tests/intent-claims.test.js` for 'CDATA', 'DOCTYPE', 'php' returns NO MATCHES. The Validation section line 229 says 'node --test plugins/mccp/scripts/lib/tests/intent-claims.test.js' but makes no assertion that these 7 cases exist in that file. The test code is described in natural language but NOT provided as concrete assert() statements with fixtures. |
| test | MEDIUM | Task 4 test will verify projection contains only whitelist keys and blocks plan_path | plan.md Task 4 Acceptance item: 'projection이 whitelist 등가 — 입력에 `plan_path`·`zz_future`를 넣어도 `Object.keys(projection)`이 `ARBITER_PROJECTION_KEYS`와 정확히 일치'. However, the plan defines `ARBITER_PROJECTION_KEYS` only conceptually: '통과 키는 정확히 `review_payload_digest` · `intent_items` · `adjudication_path`...' but never shows the actual constant or its location in Task 3 (`intent-arbiter.js`). The test is written to a spec that is not yet codified in code. The constant could be wrong, missing keys, or misconfigured until actually written and committed. |
| test | MEDIUM | Task 9 e2e test scenario 3 verifies wx atomic write EEXIST handling: 강등 쓰기가 `wx`로 `EEXIST`를 받으면 재-probe가 유효를 확인해 강등이 취소된다 | plan.md Task 9 acceptance: 'e2e 시나리오 3이 `EEXIST` → 재-probe → **강등 취소** → 최종 봉인 `'subagent'`를 관측(DD5 7번)'. However, the plan does not specify HOW the test creates the `EEXIST` condition. It does not show: (a) whether the test pre-creates the adjudication file, (b) uses fs mocking/stubbing, (c) spawns concurrent writers, or (d) uses other race-condition tactics. Without implementation strategy, the test is specified but not implementable. The mechanism to trigger `EEXIST` reliably in a unit test is left unspecified. |
| test | HIGH | Task 11 adds 6 new regex lint assertions to plan-command-marker-states.test.js verifying arbiter code paths exist | plan.md Task 11 specifies 6 regex patterns: '/--arbiter-mode/', '/Task\\(mccp:intent-arbiter/', '/arbiter_degraded/', '/parseAdjudicationFile/', '/wx/', '/unknown-task-failure/'. Searching `plugins/mccp/scripts/lib/tests/plan-command-marker-states.test.js` for any of these patterns returns NO MATCHES. These 6 assertions do not yet exist in the test file. Additionally, plan.md Task 11 Validate **itself admits these regexes cannot verify correctness**: '정규식은 **키워드 존재만** 본다 — 주석이나 문자열 안의 `arbiter_degraded`도 매칭되고'. A regex that matches keywords in comments/strings is not a valid test of the feature working correctly. The plan defers actual behavior validation to Task 9 e2e, but does not guarantee both layers run together in CI. |
| test | MEDIUM | Task 10 schema validation verifies intent_arbiter/intent_arbiter_degraded_reason pairing rule via grep | plan.md Validation section (line 254): 'grep -n "intent_arbiter_degraded_reason" plugins/mccp/scripts/receipt/schema.js'. This grep only checks if the field NAME appears in the schema file. It does NOT verify the pairing LOGIC: '`intent_arbiter !== 'author'`인데 사유가 non-null이면 reject'. A simple grep for a field name will pass even if the validation logic is missing or wrong. The test does not actually validate that the schema enforces the rule. |
| test | HIGH | Task 9 Acceptance: e2e scenario 2 verifies degradation probe detects malformed JSON without reaching 30-minute timeout | plan.md Acceptance (line 287): 'e2e 시나리오 2가 파손 JSON을 넣어 강등이 실제로 발동하고 **타임아웃에 도달하지 않음**을 관측'. The plan mentions using `--adjudication-timeout-ms` to lower timeout for testing, but does not show the actual test code that uses this flag. A test claiming to verify "does not timeout" without showing timeout-control implementation could accidentally hang for 30 minutes if the probe logic is broken. The test strategy is sound but implementation is not shown. |
| invariant | CRITICAL | Probe specification gap — how Bash detects invalid adjudication JSON is unspecified, creating fail-open risk where malformed JSON silently bypasses degradation | Plan DD5 step 4-5 (lines 109-110) requires `parseAdjudicationFile` probe to detect broken JSON before runner parses it, else runner times out for 30 minutes. But plan never specifies: (a) what the probe outputs/exits with, (b) how Bash invokes it from plan.md (node -e? exit code?), (c) how Bash captures/checks the result, (d) what happens if probe command itself fails. Task 11 line 213 says to call `node -e` and 'read ok' but provides no implementation detail. Acceptance criterion line 287 requires e2e test scenario 2 to 'observe timeout is not reached' when given broken JSON, but the production code path for the probe is completely unspecified — test harness can inject degradation manually but production wouldn't. This creates a specification-to-implementation gap where the acceptance test could pass while the production path remains fail-open. |
| invariant | HIGH | Mode environment variable sharing between command and runner is not fail-closed against future env-based fallbacks | Plan DD5 line 106 claims 'runner는 이 축의 env를 읽지 않는다' (runner doesn't read the mode env). But this is documented only by comment; no code mechanism enforces it. The plan mirrors this against plan-codex-runner.js:701 'parseArgs로 받는 것만 존재' (only things received by parseArgs exist) but there is no enforcement that a future version of runner won't add a fallback like `parseArgs.arbiterMode \|\| env.MCCP_INTENT_ARBITER`. If runner later adds such a fallback and command passes `--arbiter-mode subagent` but env has `MCCP_INTENT_ARBITER=author`, runner would seal `intent_arbiter='author'` while command believes it ran in `subagent` mode. This violates DD5's entire channel design. The plan should either (a) explicitly forbid runner from reading env on this axis via code, or (b) accept the risk that env-based fallback would violate the seal. |
| invariant | MEDIUM | Degradation-write `wx` retry race condition is specified but not formally validated by test | Plan DD5 line 116 specifies: write degradation with `wx` (exclusive), and if `EEXIST` received, re-probe to decide whether to (a) cancel degradation (arbiter won) or (b) overwrite with reason 'replaced-invalid-arbiter-output'. Acceptance criterion line 288 says e2e scenario 3 must observe this. But Task 9 (the e2e task, line 195) does not explicitly call this scenario '3 - race' by name; it says 'scenario 2~4' but only lists 4 scenarios total (success, degradation/parson, race, incomplete). Line 200 describes the race scenario but doesn't specify that re-probe must be called TWICE (once before write, once after EEXIST) to match DD5 specification. The implementation could call re-probe only once before writing (old behavior) and still pass a test that doesn't explicitly validate the retry path. |
| invariant | MEDIUM | Plan receipt fields (intent_arbiter, intent_arbiter_degraded_reason) are not in makeSkeleton yet Task 10 claims present-only behavior depends on this | Plan line 102 says 'Two new fields are receipt_hash binding targets with no carve-out' and claims present-only status (like M1.5 fields). But makeSkeleton is the function that defines which fields appear in a fresh skeleton receipt. The plan claims present-only semantics but does not explicitly require makeSkeleton modification in Task 10. Line 64 in Files to Change lists `schema.js` UPDATE but not a modification to the makeSkeleton function itself. M1.5 precedent (completion-ledger fields, cost-state fields) shows present-only fields are omitted from makeSkeleton so stale receipts never accidentally inherit them. Without this, old receipt paths touching these fields could accidentally get default values instead of present-only null, violating the anchoring contract that receipt_hash changes when fields change. |
| invariant | MEDIUM | Acceptance criterion line 294 (mutation-check) is test-level only; it does not validate that the 6 regex patterns actually guard against the mutations they claim | Acceptance line 294 says lint regex patterns must 'fail on git show HEAD:' old code. But regex matching is not semantic validation. Example: regex `/parseAdjudicationFile/` would match 'parseAdjudicationFile' anywhere in plan.md including in comments, examples, or stub code. It does not validate that parseAdjudicationFile is actually *called* in the probe path or that its return value is actually *checked*. Plan acknowledges this on line 217: 'lint은 회귀 방지이지 정합성 증명이 아니며, 강등 축의 실제 동작은 Task 9의 e2e 시나리오 2~4가 담당한다' (lint is regression only, not correctness proof; e2e owns actual behavior). But this acknowledgment reveals that Critical Finding #1 (probe spec gap) is NOT covered by either the lint OR the e2e: e2e can inject the degradation manually in test harness, lint can't validate the Bash invocation exists. The intersection of these two defenses is empty. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified tool-restriction claim against review-architect.md; verified plan_path-in-awaiting claim at runner.js:406; checked degradation channel logic (probe→write→`wx`→reprobe sequence); verified parseArgs sole-configuration claim at runner.js:701/734; verified whitelist pattern mirrors perspectives.js:24 data-in-prompt structure; examined Adversarial Review Record (R1-R4) for unresolved HIGH/CRITICAL findings and found all claimed as addressed; checked pattern-to-mirror citations against actual codebase. Found no load-bearing architectural contradictions. Specification gap exists but does not violate architecture — Task dispatch mechanism omitted from plan but should not affect boundary enforcement if implemented per standard patterns." |
| security | pass | Attacked the plan via five axes: (1) whitelist projection mechanism for data leakage to arbiter — verified Task 3(b) defines literal whitelist, Task 4(b) tests exact equality excluding plan_path, Task 9(b) e2e verifies actual projection output has no paths; (2) tool restriction to prevent file access — Task 5 and Task 4(f) enforce [Write] only in agent frontmatter, arbiter has no Read; (3) hash integrity and carve-outs — confirmed new intent_arbiter fields are hash-protected (not in carve-out list like briefing_* and ledger_write_skipped); (4) degradation channel and race conditions — DD5 7 specifies wx open with retry-probe to prevent late arbiter output from being overwritten, test 9 scenarios verify behavior; (5) environment bypass paths — MCCP_INTENT_ARBITER documented as audited mode, no hidden escapes. Found no evidence of exploitable trust boundary violation, data leakage path, or escalation vector. Plan's threat model (single-trust user) is clearly stated and documented as acceptable scope exclusion (DD10, DD4). |
| test | fail | Attacked: (1) Task 2 test case completeness — searched test file for backlog case names (CDATA, DOCTYPE, php), found ZERO matches; (2) Task 4 projection whitelist — verified `ARBITER_PROJECTION_KEYS` constant not shown in plan's Task 3 code; (3) Task 9 e2e scenario 3 — found no implementation detail for creating EEXIST race condition; (4) Task 11 regex lints — grepped test file for all 6 regex patterns, found ZERO matches, and noted plan's own admission that regexes cannot verify correctness; (5) Task 10 schema grep — showed this only checks field name existence, not rule logic; (6) Task 9 timeout acceptance — found timeout-lowering mechanism mentioned but test code not shown. Examined: plan.md full text, intent-claims.test.js, plan-codex-runner.test.js, plan-command-marker-states.test.js, PRD, Acceptance checklist. |
| invariant | fail | Attacked the invariant lens directly: (1) traced the degradation probe path from DD5 requirement through Task 11 command implementation to Task 9 e2e validation — found specification gap at the Bash invocation layer where the plan requires a probe to work but doesn't specify its interface or how Bash captures its result; (2) examined the mode environment variable channel to verify runner cannot read the mode from env independently — found no code-level enforcement, only documented comment; (3) verified receipt hash carve-out policy against M1.5 precedent and traced makeSkeleton to check present-only field initialization — found schema modification but no explicit makeSkeleton change; (4) read DD5 degradation specification closely including the retry/race condition and confirmed it's partially validated; (5) examined Acceptance criteria to find gaps between lint-level validation (regex only) and behavioral validation (e2e test with manual injection in harness). Did NOT find a defect in whitelist projection design (Task 4 specifies exact equality test), arbiter tool restriction (task 4 explicitly validates `['Write']`), or hash inclusion (Task 10 explicitly forbids carve-out). |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 347898,
  "halt_stage": "5.2e",
  "granted": 4,
  "reviewed_plan_hash": "sha256:febac86e22eb089e70561debd72894e7c617ce78235f29a8509291de8c57ab32",
  "plan_path": ".claude/plans/codex-intent-context-m2.plan.md",
  "recorded_at": "2026-08-13T23:25:03.792Z"
}
```
