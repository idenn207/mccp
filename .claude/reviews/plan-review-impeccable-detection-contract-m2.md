# Plan Review Panel — impeccable-detection-contract-m2

**Plan**: `.claude/plans/impeccable-detection-contract-m2.plan.md` · **Plan version**: `sha256:e9775b74edd3c51fc16fd7ba0e6cfef441af872241f0c247ed7a9bb1fc1142a5`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 5 blocking finding(s): test/CRITICAL, test/HIGH, test/FAIL, invariant/HIGH — MCCP_REVIEW_SINGLE_PASS=deadline_pressure 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| test | CRITICAL | Task 4 — Phase 3 of `/mccp:setup` will be skipped when `checkImpeccable().available === true`, preventing duplicate setup questions for already-installed users | Plan's Validation section (lines 315-342) contains only static literal checking (`setup-command-body.test.js` checks for required/prohibited strings) and manual Task 9 observation. There is NO unit test or integration test that would EXECUTE Phase 3 logic and verify it actually skips when the condition is true. Static text checks cannot catch behavioral errors (e.g., if someone later changes Phase 3 to always run despite condition changes). |
| test | HIGH | Task 3 — `.impeccable/design.json` should be ignored and `.impeccable/config.json` should be committed because design.json is a GENERATED byproduct while config.json is the shared committed config | Plan Task 3 (lines 152-160) claims this is based on '공식 `/docs/config`를 근거로' (official /docs/config) but Task 0 (lines 80-102) only collects ambient installation environment info (CLI available, marketplace key). Task 0's Validate section (lines 100-101) performs no verification against official impeccable documentation. Task 3's Validate (line 173) only tests that the gitignore rule works with `git check-ignore`, not that the polarity is correct. |
| test | MEDIUM | Task 7 will create `session-start-dep-check.test.js` that tests SessionStart banner behavior in available and missing environments | Task 7 spec (lines 269-272) states: 'hook을 실제로 spawn해 두 환경에서 배너 유무를 확인한다' (spawn hook and check banner presence in two environments). This specification is ambiguous — it does not explicitly state the assertion: 'when available=true, banner must NOT mention impeccable'. Without explicit assertion specification in the task description, the implementer could write a test that merely checks if the banner exists, not whether it has the correct conditional behavior. |
| invariant | HIGH | Task 0 measurement result is not mechanically transmitted into Phase 3 implementation. Plan describes 'if Task 0 confirms CLI works then Phase 3 recommends it, else downgrade to slash' but no code mechanism enforces this dependency. | Plan Task 0 Validate says notes will record yes/no; Task 4 Step 4 says Phase 3 will check that result. But notes are text written during validation, Phase 3 code is markdown written during Task 4 — no mechanism shown to pass measured result from notes into command body. If implementer doesn't understand the connection or Task 4 runs before Task 0 validates, Phase 3 could recommend a CLI form never confirmed to work. |
| invariant | MEDIUM | Delayed-require of impeccable-detect inside checkImpeccable() is claimed but unproven. If implementation accidentally requires at top-level despite plan language, the circular dependency with dep-check.js (which already requires impeccable-detect at line 52) will partially load one module and create silent failures. | Plan Task 1 Action says 'require() 안에서 require (top-level 금지)' but implementation doesn't exist yet. Validation Task 7 runs tests that would catch this, but test files don't exist until Task 7 executes. No static code assert prevents top-level require in dep-check.js checkImpeccable(). CLAUDE.md §3.4 forbids assumption-comments, so the Patterns table mirroring auto-chain.js is example not proof. |
| invariant | MEDIUM | sessionStart.js hook has no try/catch specifically around checkAll() call. If checkImpeccable() throws despite sentinel design, entire hook crashes. | session-start.js:1067 `const result = depCheck.checkAll();` sits inside a try block wrapping state-injector (lines 1064+), not specifically guarding the dep-check call. If resolveImpeccable() throws (against design intent), exception propagates to hook termination. Sentinel objects require defensive try/catch at call site; compare codex-invoke.js lines 35-89 sentinel guards around spawn/readFile operations. |
| invariant | MEDIUM | Task 0 measurement must precede Task 4 Phase 3 rewriting but plan does not explicitly enforce this ordering. If Task 4 runs before Task 0 validation, Phase 3 code is written without confirmed measurement. | Plan Task 0 Action measures whether 'claude plugin marketplace list' works; Task 0 Validate records yes/no in notes; Task 4 Step 4 says 'if (b) is negative then Phase 3 escalates to slash'. But no dependency gate prevents Task 4 from executing with stale/missing Task 0 result. Task numbering implies sequence but text does not make dependency explicit. |
| invariant | MEDIUM | Design.json will remain tracked in repo per UI7 while ignore rule removes its exception. Provisioner reports as pollution every run, creating merge friction if users manually untrack to silence warning. | Plan Task 3 keeps .impeccable/design.json tracked; changes ignore rule from '!design.json' to '!config.json' (line 158-159). Task 9 expects provision --dry-run shows 'pollution:null' (uncomputed). But real provision will report design.json as pollution (tracked but ignored). setup.md Phase 5 line 244 forbids auto-untrack per existing contract. If user runs setup and manually untracked the file to fix pollution report, their diff shows 'deleted: .impeccable/design.json', and team members pulling it lose the file. Plan documents this as 'normal' but documentation of hazard ≠ prevention. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified circular dependency fix soundness (deferred require pattern correctly breaks cycle created by M2 Task 1); confirmed oracle consolidation (checkImpeccable delegates to resolveImpeccable without aliasing); checked scope boundaries (M2 wires three new consumer sites — dep-check/SessionStart/setup — while gate caller rewiring correctly deferred to M3); spot-tested pattern citations (auto-chain.js:109, impeccable-detect.js:445, gitignore-provision.js:104, plan-review-command-body.test.js) against actual code — all accurate; examined test coverage for circular load-order regression and three consumer sites; verified no unhandled cases (env override, multi-source channels, shadowing, stale paths all handled); validated acceptance criteria are specific/measurable with audit trail separation; confirmed abstraction field naming (available vs installed) reflects different questions; checked current state vs M2 design — no pre-existing circular reference to break, Task 1 creates it correctly. Attacked: boundary leaks, abstraction honesty, citation accuracy, scope creep, test gaps, edge cases. No defects found. |
| security | pass | Circular dependency chain: impeccable-detect requires dep-check (top-level); plan adds checkImpeccable to dep-check with delayed require of impeccable-detect. Verified delayed require prevents partial-export issue and tests validate both load orders. Absolute path leakage via normalizeSourcePath fallback (line 170): fallback only occurs if both repo and home normalization fail AND path.relative doesn't throw; pre-existing from M1; plan improves by passing correct repoRoot. Trust boundary (repoRoot): traced from git rev-parse or process.cwd() through session-start to resolveImpeccable; properly validated with path.relative and symlink rejection. Gitignore rule change (design.json→config.json): correctness concern (claim lacks direct evidence), not security risk. Receipt field injection (impeccable_*): fields are non-sensitive (version, paths, names); strict superset contract preserved. No CRITICAL findings. |
| test | fail | Examined plan's Task 0–10 claims against their Validate sections. Verified: (1) existence of referenced test files (dep-check.test.js exists, setup-command-body.test.js does not yet), (2) test coverage gaps by searching for execution tests vs. static checks, (3) whether Validate commands actually test the behavior claimed by each Task. Checked circular dependency test (well-specified via node -e commands in Validation), checkAll() superset return structure (explicit in Validate line 128), and gitignore polarity's evidentiary basis (Task 0 collects only environment state, not official source verification). Found that setup Phase 3 skip logic has no automated behavioral test — only static literal checking and manual Task 9 observation. Found that gitignore polarity reversal claims to be based on official documentation but Task 0's Validate does not verify against that source. Found session-start test spec is vague about assertion expectations. |
| invariant | fail | Analyzed circular module dependency break (delayed-require in checkImpeccable); traced Task 0 measurement → Task 4 Phase 3 code path to check if proof is mechanically transmitted (found implicit ordering, no code gate); verified receipt schema changes preserve existing 4 keys as strict superset; checked SessionStart hook error handling (no try/catch around oracle call); investigated pollution report and file tracking hazard; examined test sequencing (tests in Validation block don't exist until Task 7 CREATE)" |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 756525,
  "halt_stage": null,
  "backlog_appended": 3,
  "backlog_skipped_nonblocking": 5,
  "granted": 4,
  "reviewed_plan_hash": "sha256:e9775b74edd3c51fc16fd7ba0e6cfef441af872241f0c247ed7a9bb1fc1142a5",
  "plan_path": ".claude/plans/impeccable-detection-contract-m2.plan.md",
  "recorded_at": "2026-08-22T12:25:17.027Z"
}
```
