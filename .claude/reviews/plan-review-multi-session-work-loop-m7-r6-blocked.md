# Plan Review Panel — multi-session-work-loop

**Plan**: `.claude/plans/multi-session-work-loop-m7.plan.md` · **Plan version**: `sha256:eb71d6cf71b5781fc83813ad9285bd9347cd89747ad7286f48d5c11e9ce384f2`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 7 blocking finding(s): security/FAIL, test/HIGH, test/FAIL, invariant/CRITICAL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| security | MEDIUM | Task 7 coverage gate mechanism defends registry chokepoint against direct writes with absolute paths | Plan §Task 1 states 'Task 7 coverage gate의 승인 writer 레지스트리가 정적으로 거부한다' (Task 7 gate mechanically rejects). But Task 7's actual validation focuses on 'finding 표면에 쓰는 코드' (surface writers, `.claude/reviews/`), not registry (`.claude/state/findings/`). Validation test `C1-COVERAGE-STATIC` is described as 'unregistered writer injection fails' without specifying whether this covers registry direct writes. Task 1's chokepoint test `C1-REGISTRY-PATH-NORMALIZED` only validates that `appendFinding` normalizes paths, not that only `appendFinding` can write to registry. |
| test | HIGH | DD2's critical `CLOSURE_FROM_ADJUDICATION` mapping that prevents C1 gaming is testable | Task 4 claims test `C1-EMIT-PLAN-CODEX` will verify the mapping. However: (1) The module `findings-registry.js` being created doesn't exist to show the map structure; (2) No test code is shown that would verify `ACCEPT_NOW` maps to `null` vs `closed{type:'fixed'}`; (3) A review of `plugins/mccp/scripts/lib/tests/plan-codex-runner.test.js` shows no existing tests for finding emission logic. Without seeing the test code, there is no proof that a violation (e.g., changing ACCEPT_NOW to close as 'fixed') would be caught. |
| test | MEDIUM | The plan fully specifies the 26-item assertion manifest that will mechanically verify test coverage | Task 8's Validate says '`assertion-manifest-check.js` 영점 exit + manifest 26항목 전건 실재' (26 items' preconditions). However: (1) The plan names only ~25 assertions across Tasks 1-7's Validate sections (C1-REGISTRY-APPEND through C1-ACCEPTANCE-PROMOTED); (2) The actual `m7-assertion-manifest.json` file structure is never shown; (3) The plan does not provide the complete list that Task 8 should create. This makes the acceptance criterion procedurally unverifiable at plan review time—we cannot confirm the manifest is complete until Task 8 is implemented. |
| test | MEDIUM | Task 2 and Task 3 co-dependence (both landing in one commit) is mechanically enforced to prevent C1 uncomputable state | Plan line 206 states Task 2 and 3 'must land in one commit'. The enforcement mechanism is Task 7's `c1-coverage-gate.js` co-presence check (line 236): '계약과 선언이 한쪽만 있는 트리가 실패'. However: (1) Task 7 creates `c1-coverage-gate.js` which doesn't exist yet; (2) No existing test proves this check is implemented; (3) The test `c1-coverage-gate.test.js` doesn't exist and will be created by Task 7. Without proof the co-presence check exists, the atomic co-dependence claim is unverifiable. If Task 2 alone lands without Task 3, the tree would have `type_separation` contract requirement but no `findings` source—and there is no proof this condition would be caught. |
| test | MEDIUM | DD3's finding ID matching (using `finding_id` and fallback `matchKey`) will correctly identify same findings across rounds with conservative error direction | Plan DD3 (line 83-87) claims error direction is 'conservative' (分母만 증가 — denominator only increases). However, the plan doesn't show test code that would verify: (1) A finding with changed text doesn't match via `finding_id` hash but DOES match via `matchKey`; (2) The secondary key matching correctly refuses to match when multiple candidates exist (line 137: '매칭을 관대하게 만드는 변경은 보수성 확인을 선행 조건으로 문서화'). Without test code showing these edge cases, the conservative direction claim is a design assertion, not a tested property. |
| invariant | CRITICAL | Undetectable tail-end append failures violate fail-closed gate invariant: C1 metric can be silently corrupt when session-end writes fail continuously. | Plan DD8 (line 131): '어떤 work_unit의 **마지막** N개 append가 연속 실패하고 그 뒤로 성공한 write가 하나도 없으면 드러날 구멍이 없다'. Plan admits these undetectable failures are 'seq 설계의 한계' (design limit, not bug), making them irrecoverable. Plan lines 122-123: 'fail-open만 계약하면... 분모가 줄어든 C1이 아무 표시 없이 산출된다' — the gate returns a computed value even though the underlying data is corrupt. This violates the principle that 'HALT vs degrade: for a gate, silently degrading is worse than stopping.' |
| invariant | HIGH | Task 2 and Task 3 atomic landing is a social contract, not mechanically enforced. If they land in separate commits, C1 becomes invalid, but only post-commit detection catches this. | Plan line 206: '이 Task는 Task 3과 하나의 커밋으로 착지한다' and 'Task 2와 Task 3은 하나의 커밋으로 착지한다' — stated as requirement, not enforced. Plan line 142-143 acknowledges the coverage gate detects this 'in tree state', meaning after commits are already in: '분할 착지가 일어난 순간의 트리에서 붉어진다'. No pre-commit barrier exists; the gate runs in Task 7 (post-commit). If Task 2 lands without Task 3, computeC1 breaks because it tries to check `findings.type_separation` on a non-existent source — the tree enters an invalid state until Task 3 lands. |
| invariant | HIGH | Acceptance criteria are manual spot checks, not code gates. The plan can pass PR review even if these are not verified, violating the principle that gate decisions must be mechanical. | Plan Acceptance section (lines 317-327) lists manual verification tasks: line 317 '게이트/경로를 실제로 1회 완주하고 산출물을 확인 (단위 test 통과 ≠ 경로 작동)' — this is human verification after code is written. Line 320-321 'C1이 `computed`로 뒤집힌다... 코드 존재는 판정 근거가 아니다' requires actually running `/mccp:plan` to generate the registry file, but there is no code gate that blocks the PR if this doesn't happen. Line 325 requires 'm7-after.json이 DD8의 미탐지 꼬리를 명시 기록한다' — documentation of undetectability, not prevention. These checks exist outside the PR gate and can be skipped. |
| invariant | MEDIUM | Secondary finding matching key (DD9) lack rigorous specification and test coverage validation. Buggy matching logic can silently overcount C1 closures. | Plan lines 133-137 introduce `(perspective, cited_path)` as 2° key but don't specify matching algorithm. Plan line 136-137: 'matchKey 매칭은 **더 많은 재발을 인식**하므로 분모를 줄이는 방향... 오차 방향이 보수적임을 설계로 고정' assumes correctness without enforcement. Risks table (line 293): 'DD3 비재발 판정의 id 매칭 실패' is HIGH likelihood. The mitigation 'Validate` line 201 mentions 'C1-ID-SECONDARY-KEY' test but plan provides no details on test comprehensiveness or whether it prevents overmatch bugs. |
| invariant | MEDIUM | Merge driver validation (merge=union for parallel branching safety) is manual-only. If glob pattern is wrong, parallel worktrees lose data silently. | Plan Task 1 (line 199) requires '.gitattributes \| UPDATE \| 레지스트리 glob에 `merge=union`' but does not specify the exact glob pattern. Plan Validate section (lines 270-272) uses manual `git check-attr merge` command with no automation enforcement. If the glob pattern (e.g., `.claude/state/findings/*.jsonl`) doesn't match future work_unit names, merge conflicts cause data loss. The fact that `.gitattributes` currently (confirmed by Read) contains only EOL rules confirms no merge driver pattern exists yet — Task 1 must add it correctly on first attempt, with no code-level guard. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Attacked the plan across seven dimensions: (1) **Pattern citations** — verified Patterns to Mirror actually exist at cited locations (msw-events.js allowlist+cap+malformed, handoff-items.js per-source degraded fail-open); (2) **Architectural claims** — checked CLAUDE.md §3.14 exists and matches HIGH/CRITICAL threshold claim, verified `.gitignore` genuinely does NOT ignore `.claude/state/findings/` (intent to git-track), confirmed `record.js` has zero fs imports; (3) **Code-to-design mapping** — verified ADJUDICATION_VERDICTS enum exists at intent-context.js:69 with 4 values as cited; (4) **Real defect identification** — confirmed the current bug at msw-metrics/index.js:632 where `(deferred + downgraded + rejected) > 0` check incorrectly flags all-fixed work_units as invalid; plan correctly proposes migrating to `type_separation` contract check; (5) **Boundary/coupling protection** — verified single chokepoint for path normalization (appendFinding does all normalization before write, not spread across 3 emit sites), Task 7 static coverage gate blocks unapproved emit writers, Task 2·3 co-landing enforced by Task 7 co-presence check; (6) **Registry durability** — confirmed `.gitattributes` lacks `merge=union` declaration and plan assigns it as Task 1 responsibility with Task 7 validation (`git check-attr` not file-grep); (7) **Invariant preservation** — verified `finding_id` hash excludes `cited_path` so normalization consistency doesn't affect dedup, reader uses last-write-wins to handle duplicate findings from merge=union, work_unit sharding avoids typical collision cases with M3 overlap prevention as safety backstop. Acknowledged limitations documented: DD8 untraceable tail loss (last N appends if no subsequent writes), conservative error direction on id-matching fallback, require explicit type_separation declaration. All three task-interdependencies properly sequenced (Task 2 contract before Task 3 registration, Task 7 gate validates both present). Test ownership matrix clear with no circular test dependencies. Acceptance conditions concrete and verifiable (C1.status must be 'computed' not merely code-present). |
| security | fail | Attempted to verify the claim that Task 7 mechanically defends the registry chokepoint by: (1) reading the plan's threat model and design decisions (DD4) explicitly naming cwd-leak risk; (2) tracing the three emit points (plan-review, codex-runner, santa/seal) that call `appendFinding`; (3) verifying Task 7's static lint and runtime falsifier pattern against the B2 precedent (which checks `.claude/receipts/` with explicit APPROVED_WRITERS list); (4) checking whether Task 7's validation would catch direct registry writes with absolute paths. Gap found: Task 7 description checks surface writers, not registry writers. No validation step described as checking the registry file itself for path normalization violations or detecting fs-level registry writes. The plan claims mechanical enforcement ('정적으로 거부한다') but validation doesn't show the mechanism. Practical risk is low (code review gate + limited to approved modules) but claim is unsupported by described validation. |
| test | fail | Checked whether load-bearing test claims are falsifiable: (1) Examined if `CLOSURE_FROM_ADJUDICATION` mapping test exists in `plan-codex-runner.test.js`—no tests found for finding emission logic. (2) Verified whether `m7-assertion-manifest.json` is specified in plan—plan names ~25 assertions but never shows the complete 26-item manifest structure or provides proof these tests would fail if violated. (3) Confirmed Task 7 `c1-coverage-gate.js` doesn't exist yet and has no test proof of co-presence enforcement. (4) Read existing test suite `plugins/mccp/scripts/lib/tests/msw-metrics.test.js` and found test at line 416 exists but lacks proof of ACCEPT_NOW mapping verification. (5) Verified `findings-registry.js` creation is new code with no pre-existing implementation to validate against. Plan makes high-confidence assertions about test coverage but defers the actual test specifications to implementation time, violating falsifiability at review stage. |
| invariant | fail | I attacked the plan's fail-closed invariants by testing: (1) whether undetectable emit failures can silently corrupt C1 (yes, admitted by DD8 line 131); (2) whether Task 2+3 atomicity is mechanically enforced pre-commit (no, only post-commit coverage gate in Task 7); (3) whether acceptance criteria are code-gated or manual (manual spot checks, plan can pass PR without verification); (4) whether secondary key matching is rigorously tested (test mentioned but not detailed); (5) whether merge=union pattern is validated before commit (manual validation only, pattern not specified in plan). I traced the emit-failure and atomicity paths through Task 1, Task 2/3, Task 7, and Validation sections, finding that each depends on manual enforcement or post-incident detection rather than structural barriers. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 537115,
  "halt_stage": "5.2e",
  "granted": 4,
  "reviewed_plan_hash": "sha256:eb71d6cf71b5781fc83813ad9285bd9347cd89747ad7286f48d5c11e9ce384f2",
  "plan_path": ".claude/plans/multi-session-work-loop-m7.plan.md",
  "recorded_at": "2026-08-17T14:21:31.883Z"
}
```
