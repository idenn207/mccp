# Plan Review Panel — review-record-linkage-m3

**Plan**: `.claude/plans/review-record-linkage-m3.plan.md` · **Plan version**: `sha256:0b32a1d5d47f5810db50eac2bc6ab7ed74fea37308196b5f6d51bb458b4e463f`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 9 blocking finding(s): architect/HIGH, architect/FAIL, security/HIGH, security/FAIL — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | 플랜이 요구하는 '공유 정규화 헬퍼'가 살 곳이 Files to Change 어디에도 없다 — 선언된 파일 집합으로는 Task 2/7/9의 계약을 만족할 수 없고, 유일한 in-set 경로는 계층 역전(lib/plan-review → scripts/receipt/write)이다. Task 7의 결속은 fail-closed라 두 벌 정규화가 생기면 정상 ship 거절로 나타난다(플랜 자신이 '두 벌이면 규칙이 갈라진다'고 적음). | plan Task 2/7: "정규화 함수는 재구현하지 않고 `write.js`가 쓰는 것과 같은 헬퍼를 공유한다"; plan Task 9(:201): "`record.js`와 `write.js`가 **같은 정규화 헬퍼**를 부른다는 것". 그러나 write.js:95-99 `relativeToRepo`는 모듈 로컬이고 write.js:1448-1456 `module.exports`에 없다. record.js는 현재 `require(` 0건(grep 결과 무매치)이며, 플랜의 '변경하지 않는 파일'이 linkage-defs.js(자연스러운 헬퍼 거처)를 명시적으로 동결한다(plan:99). Files to Change에 공유 유틸 모듈 CREATE 항목이 없다. CLAUDE.md §1.2에 따라 표에 없는 신규 파일은 dedupe planned matcher도 빗나간다. |
| architect | MEDIUM | Task 6(b)의 back-patch 호출이 대상 레코드 경로(`--record`)를 어디서 얻는지 지정하지 않는다 — 두 후보(방금 봉인된 receipt의 `meta.review_record_path` vs `record.js#reviewRecordPath(slug)` 로컬 파생)가 의미상 다르고, 후자는 이 플랜이 금지한 파일명 관례 부활이다. 앵커 무스탬프(부트스트랩 포함) 시 `--record`가 아예 없는 경우의 동작도 미정의라, Task 7의 진입 predicate 확장('이번에 back-patch한 레코드')이 무엇을 가리키는지 결정되지 않는다. | plan Task 6(b): "`FINALIZE_RECEIPT_HASH` 캡처 직후 `link-receipt`를 호출한다" — 인자 원천 언급 없음. Task 4는 시그니처만 정의(`link-receipt --record <repo-relative> --receipt-hash <hex>`). Task 5는 무스탬프 분기 7종을 정의하므로 `meta.review_record_path` 부재가 정상 경로이고, 그때 Task 7의 "이번에 back-patch한 레코드 1건"(:175)은 지시 대상이 없다. 대비: Task 5는 "파일명 fallback을 만들지 않는다"(:159). |
| security | HIGH | The decision-binding the plan declares for back-patch (record.measurement.plan_path == SHIP_PLAN_PATH) has no implementation site and no test. The only binding actually specified at the write path is directory containment, so a mispointed meta.review_record_path mutates ANOTHER decision's git-tracked review record on disk before any guard runs — the guard (3.0) can only refuse the commit, not undo the write. | Plan Task 4 (line 122) specifies only realpath containment under `.claude/reviews/`, and its Validate (line 124) lists exactly `../` · 절대경로 · 드라이브 문자 · `.claude/reviews/` 밖 · 심볼릭 이탈 · 비-hex 해시 — no decision binding. Task 6(b) (line 164) calls `link-receipt` with no binding. Task 7's binding sentence (line 180: "back-patch 대상 레코드의 measurement.plan_path가 이 ship의 SHIP_PLAN_PATH와 같은지 확인한다") names no component, and Task 7's Validate (a)-(f) (line 184) and Task 9's static assertion list (line 199) contain no assertion for it. The plan itself states the consequence at line 180: "남의 리뷰 레코드가 이 ship의 증거로 커밋된다". |
| security | MEDIUM | Widening the evidence-commit OUTSIDE-HALT from the exact receipt directory to include a review record erodes the E6 fail-closed guarantee, and no Validate item pins that an UNRELATED extra staged path (e.g. a second `.claude/reviews/*.md`, or completion-ledger) still HALTs. A prefix-based `grep -v '^\\.claude/reviews/'` implementation satisfies every stated test while opening the corpus. | `plugins/mccp/commands/pr.md:1208` is `OUTSIDE=$(git diff --cached --name-only \| grep -v '^\\.claude/receipts/mccp-pr-codex/' ...)` whose comment says "refuse to commit if ANYTHING outside the ship-receipt path is staged (esp. completion-ledger/ — E6)". Plan Task 7 item 1 (line 175) widens `:1208` to "receipt 디렉토리 ∪ 이번에 back-patch한 레코드 1건", but Task 7 Validate (line 184) items (a)-(f) contain no negative test for an unrelated extra staged path. |
| security | MEDIUM | A new terminal-ship bypass toggle is introduced without registering it in the canonical env contract, and the plan's own Validation block runs no lint that would catch it — an audited escape that is undiscoverable in the operator index ships silently. | Plan line 383: "신규 토글 `MCCP_PR_SKIP_LINK_EVIDENCE`가 `env-contract/registry.js`에 미등재 → `lint.js` L1이 붉어지는데 Validation에 lint 실행이 없다 \| **미흡수 — backlog 이연**". The Validation block (plan lines 213-240) indeed contains no `env-contract/lint.js` invocation, while CLAUDE.md §4 makes the registry canonical for every operational toggle. |
| test | HIGH | Task 8 축 3(해시를 실제로 비교한다)의 over-permissive 방향을 보는 test가 어디에도 없다 — stale `receipt_hash`를 담은 레코드가 `bidirectional`로 계수되는 경로를 열어 둔 채 green이 된다 | plan Task 8 Validate는 라이브 test 4종만 열거한다: "(i) 커밋된 레코드가 있는 fixture에서 bidirectional이 계수되고 (ii) 같은 레코드가 작업 트리에만 있고 커밋되지 않은 fixture에서는 계수되지 않으며 (iii) 자격 없는 ship이 분모에서 빠지고 (iv) 분모 0이면 denominator: null". 축 3이 닫겠다고 선언한 실패(plan: "이전 ship의 stale 해시가 남은 레코드가 새 receipt와 짝지어져 bidirectional:true로 계수되는 경로가 실재")에 대응하는 단언이 그 4종에 없고, `linkage-defs.js`는 불변(UI4)이라 기존 `linkage-defs.test.js`도 그것을 덮지 못한다(`linkage-defs.js:186`은 비어있지 않은 문자열 판정). 해시 대조 코드를 통째로 빼도 Validate 전항이 통과한다 |
| test | HIGH | Plan의 Validation 3번 명령이 이 워크트리에서 실행 불가라, `--frozen-only` 결정성·바이트 불변 acceptance가 기계로 확인될 수단을 갖지 못한다 | plan Validation: `node ... --frozen-only --json > .git/mccp/tmp/m3-frozen-a.json`. plan 자신이 R3 축 W로 이를 인정한다 — "Validation 3번이 `.git/mccp/tmp/`로 리다이렉트하는데 linked worktree에서 `.git`은 **파일**이라 실행 불가" → "**미흡수 — backlog 이연**". 그런데 Acceptance는 "`--frozen-only` 2회 실행 바이트 동일"을 그대로 요구한다(plan Acceptance 3번째 체크박스). 현재 cwd가 linked worktree(`.worktrees/c1-review-record-linkage`)이므로 acceptance의 유일한 검증 명령이 그 환경에서 돌지 않는다 |
| test | MEDIUM | Task 2가 R4 MEDIUM 흡수로 추가한 `measurement.plan_path` 정규화에 동작 test가 없다 — 정적 "같은 헬퍼를 부른다" 단언은 산출값이 정규화됐음을 증명하지 못한다 | Task 2 Validate는 "`corpus.parseRecord`가 새 키를 그대로 통과시키고 `kind==='record'`가 유지되는지"뿐이고, Files to Change의 `plan-review-record.test.js` 사유도 "`measurement.receipt_hash` 키 추가 회귀"뿐이다. 정규화는 Task 9의 정적 단언("`record.js`와 `write.js`가 **같은 정규화 헬퍼**를 부른다")에만 걸려 있는데, 헬퍼 호출 존재는 그 반환값이 실제로 `measurement.plan_path`에 실렸음을 함의하지 않는다. Task 7의 결속은 fail-closed(plan: "표기 불일치는 무스탬프가 아니라 **정상 ship 거절**")이므로 이 미검증 축의 실패 방향은 ship 차단이다 |
| test | MEDIUM | Task 6(c)가 신설하는 진단 HALT(`[ -f "$SHIP_PLAN_PATH" ]` 실패)에 어떤 test도 없고, 동시에 R12에 따라 이 브랜치의 라이브 완주가 바로 그 HALT에 걸리므로 Task 6의 Validate("Task 9의 정적 배선 test + 라이브 완주") 양축이 모두 이 축을 반증하지 못한다 | Task 6 Validate 전문: "Task 9의 정적 배선 test + 라이브 완주". Task 9는 "(ii) `SHIP_PLAN_PATH` 파생과 `[ -f ]` 확인이 실재하며"라는 **존재** 단언만 두고 HALT 동작을 실행하지 않는다. R12는 "`${PR_PLAN_PATH:-.claude/plans/${DECISION_SLUG}.plan.md}` → `review-record-linkage.plan.md`인데 디스크에 없다"를 **높음(이 브랜치에서 확정)**으로 적는다 — 즉 라이브 완주는 양성 경로를 통과하지 못한다 |
| test | MEDIUM | 기존 test가 `post_baseline.ships/records`의 작업-트리 의미를 핀으로 박고 있는데, Task 8 축 1은 라이브 파티션의 읽기 원천을 `HEAD` 트리로 바꾸면서 이 기존 단언을 어떻게 처리할지 명시하지 않는다 — 기존 test가 그대로 통과하면 축 1이 미적용이어도 green이고, 깨지면 계약 변경이 계획에 없다 | `plugins/mccp/scripts/lib/tests/linkage-audit.test.js:279-290`은 **커밋하지 않은** 파일을 작업 트리에 쓴 뒤 `assert.equal(full.json.post_baseline.ships, 1); assert.equal(full.json.post_baseline.records, 1);`를 단언한다. plan Task 8은 "작업 트리 카운트는 **지우지 않고** 별도 진단 필드로 남긴다"고만 적어 `post_baseline.ships`가 HEAD 기준인지 작업 트리 기준인지 결정하지 않으며, Validate 4종에도 이 기존 단언의 운명이 없다 |
| invariant | HIGH | 신설 라이브 파티션(Task 8 축 1)에 blind/degraded 상태 사다리가 없어, HEAD 트리 판독이 통째로 실패해도 '정상적으로 링크 0건'과 구별되지 않는다. 그리고 이 사이클 Acceptance의 세 항목((a) 파티션 존재 (b) denominator=null (c) 두 카운트 병기)은 판독이 완전히 실패한 실행에서도 전부 참이 되므로, 아무것도 반증하지 못한다. 이는 이 저장소가 동결 파티션에서 이미 겪고 고친 결함과 같은 형태다. | linkage-audit.js:461-468 `if (tree === null) { result.baseline.state='degraded'; ... result.baseline.scope_unknown = true; }` + :455-459 주석 "코퍼스를 통째로 못 본 실행이 state:\\"ok\\" + 전 필드 0 인 블록을 내보냈다". 반면 plan 전문에서 `degraded`/`blind`/`scope_unknown`은 라이브 파티션과 관련해 0회 등장(plan 검색 결과 284행의 evidence-audit 언급 1건뿐)이고, plan:188-190은 "`:511-520`의 tree 판독 경로를 재사용하고 ref만 다르다"만 적는다. plan:282의 acceptance (a)(b)(c)는 판독 실패 시에도 만족된다. Task 7(plan:182)이 `MCCP_PR_SKIP_LINK_EVIDENCE` 우회의 유일한 책임 장치로 "감사가 그대로 보고한다"를 드는데, 그 보고가 조용히 0으로 접히면 우회와 정상이 구별되지 않는다. |
| invariant | MEDIUM | Phase 3.0(evidence commit)이 "이번에 back-patch한 레코드"의 경로를 어디서 얻는지 plan이 지정하지 않는다. 진입 predicate(항목 0)·stage 집합(항목 1)·guard stdin 생산자(항목 2)가 전부 그 값을 요구하는데, 2.5.7과 3.0은 서로 다른 펜스 블록이라 셸 변수가 살아남지 않는다. 그 결과 구현은 (i) 값이 비어 predicate가 영원히 거짓 → 링크 절반이 히스토리에 미도달·무HALT(=plan이 R2로 지목한 지배적 실패 모드가 그대로 재현)이거나 (ii) `.claude/reviews/plan-review-*.md` 글로브로 떨어져 남의 레코드를 stage하는 두 방향으로 갈린다. plan은 같은 셸-상태 문제를 plan.md 쪽에서는 명시적으로 아티팩트 carrier로 닫았는데(Task 6(a)), pr.md 쪽에서는 닫지 않았다. | plan:174-176 "predicate를 'receipt 디렉토리 dirty ∪ 이번에 back-patch한 레코드 dirty'로 넓힌다" / ":1197-1198 ... 레코드가 guard에 도달조차 못 해" — 어느 문장도 경로의 출처를 지정하지 않는다. 대비: plan:164 Task 6(a) "5.2 진입 시 plan 경로를 `$REVIEW_DIR/plan-path` 아티팩트로 한 번 기록하고 … (§5.2 불변식 (i): 셸 상태는 블록을 넘지 못하고)". pr.md 자신도 :1006-1013 "Each fenced block may run as its own shell, so an inherited slug can arrive empty"로 같은 성질을 실측 기록한다. |
| invariant | MEDIUM | Task 5가 상류 plan receipt의 `meta.review_record_path`를 "그대로" ship receipt에 전달하는데, 상류 receipt는 working-tree only이며 어떤 hash/schema 검증도 받지 않는다(evidence-stage-guard는 `mccp-pr-codex`만 검사). 그 값이 Task 1(a)의 형태 규칙(`.claude/reviews/` 접두 + isRepoRelativePath)을 위반하면 ship receipt가 schema-invalid가 되어 terminal ship이 fail-shut한다 — 그 분기는 Task 5의 무스탬프 열거(부재·파손·null·열거 밖·앵커 0/≥2·legacy)에 없고, 선언된 퇴로 `MCCP_PR_SKIP_LINK_EVIDENCE`는 stage만 건너뛸 뿐 전달을 막지 않는다. plan은 같은 위험을 `meta.plan_path`에 대해서는 R14로 인식해 규칙을 좁혔으면서(계측 필드가 ship 차단 조건을 넓혀서는 안 된다) 전달 축에는 그 논거를 적용하지 않았다. | plan:155 "`review_record_path`: 상류 receipt의 `meta.review_record_path`를 **그대로** 전달" + plan:104 규칙 (a) "경로가 `linkage-defs.isRepoRelativePath`를 만족하고 `.claude/reviews/`로 시작할 것" + plan:258 R14 "계측 필드가 terminal ship의 차단 조건을 넓히지 않는 것이 이 Risk의 acceptance". 상류 receipt가 손으로 쓰일 수 있는 경로는 CLAUDE.md §3.3 복구 옵션 4(`/mccp:receipt-write`)이며, evidence-stage-guard.js:87-88은 `gate_id !== 'mccp-pr-codex'`를 거부하므로 plan receipt는 그 무결성 검사를 전혀 받지 않는다. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Task 5 경로 앵커(`meta.plan_path` ↔ `SHIP_PLAN_PATH`)를 write.js:411-442로 직접 검증 — `planPath`가 로컬 변수이고 receipt 필드가 아니라는 주장, `relativeToRepo`가 :439 design_doc_hash에 이미 쓰인다는 주장, POSIX 접기(:95-99) 모두 실측대로였다. R14 blast radius 주장(pr.md:916 PR 제목 허용 · 2.5.7 placeholder 선언 :1003)과 2.5.8/2.5.9의 `${PR_PLAN_PATH:-…}` 선례(:1017-1023)도 인용대로였고, \\"존재하지 않는 경로는 이미 stale로 막힌다\\"는 주장은 :1022 주석이 직접 뒷받침한다. schema.js가 lib/*를 require하는 계층(:3-8, :52-56)이라 linkage-defs require는 신규 위반이 아님을 확인했고, linkage-defs의 `isRepoRelativePath`(:165)·`classifyLink`(:177)·record.js의 `plan_path`(:314)·`reviewRecordPath`(:75) 인용도 전부 실재했다. 순환 의존(write→plan-review) 가능성도 grep으로 없음을 확인. 남은 두 결함은 위 findings. |
| security | fail | Verified the plan's load-bearing citations against source: `linkage-defs.js:165-175` isRepoRelativePath (rejects `..`, drive letters, UNC, whitespace — plan's characterization is accurate and its \\"denylist, not a safety gate\\" caveat matches the file's own comment at :160-164); `record.js:69-77` sanitizeSlug/reviewRecordPath (Task 6(a)'s SoT argument holds); `evidence-stage-guard.js:51,75-77,95-98,133-136,154` (all four line claims accurate, including the pure 2-arg signature that motivates the new anchor channel); `pr.md:1189/1197/1208` (predicate, pathspec filter, OUTSIDE HALT all as described). Hostile scenarios I tried and could NOT land: (1) path traversal via `meta.review_record_path` into the back-patch write — Task 4's realpath containment plus the audit-side \\"do not open the sealed path, look it up in the already-scanned corpus map\\" (Task 8 axis 2) closes both read and write surfaces; (2) absolute-path/cwd leak into a tracked artifact on the new `meta.plan_path` field — Task 1 normalizes via `relativeToRepo` + POSIX and stamps only for an existing file under repo root, and Validate (iv) pins the Windows case, so the §3.12 cwd-leak precedent is not reopened; (3) forging `plan_review_expected=false` to shrink the metric-2 denominator — `linkage-defs.js:127-139` requires a paired non-empty reason and Task 5 refuses to synthesize `false` from absence, so an unexplained exclusion folds to `undecidable`; (4) escalation via the untracked, hash-unverified upstream plan receipt feeding a tracked ship receipt — real, but `receipt/dedupe.js` already grants a strictly stronger privilege (skipping PR-Codex) off the same unverified artifacts, so this is pre-existing residual risk, not introduced here; (5) `MCCP_PR_SKIP_LINK_EVIDENCE` rewriting the sealed record — it does not: the honest `review_record_path` stays sealed and Task 8 axis 1's HEAD-tree read makes the bypass show up as a degraded metric rather than a forged 100%. |
| test | fail | plan 전문과 PRD를 읽고, 각 Task의 Validate 줄이 그 Task가 바꾸는 표면을 실제로 실행하는지 대조했다. 디스크에서 `linkage-audit.test.js`(:255-316, :349-358, :395-410)와 `plan-review-record.test.js`(:110-141)를 열어 (a) 기존 단언이 변경될 동작을 핀으로 박고 있는지, (b) 새 주장(해시 대조·정규화·HALT·라이브 파티션 원천)이 어떤 단언으로 반증 가능한지 확인했다. 반증 실패해 finding으로 올리지 않은 것들: Task 1 (ii) over-permissive 거부 test와 (iii) 플래그 미주입 단언은 실제로 위험 방향을 덮는다; Task 4 containment Validate는 `../`·절대경로·심볼릭·비-hex를 열거해 충분하다; Task 5의 7분기 음성 Validate와 Task 9의 음성 e2e 2건 + \\"stamp 줄을 지우면 red\\" 단계는 앵커의 반증 가능성을 실제로 세운다; Acceptance가 `bidirectional >= 1`을 이 사이클에서 뺀 것은 도달 불가 체크박스 제거라 정당하다; Files to Change의 test 파일 경로는 전부 실재하거나 CREATE로 선언돼 있다. |
| invariant | fail | 공격한 축: (1) hash 커버리지 — hash.js:198-225를 읽어 신규 5필드가 carve-out 밖(=서명 대상)이고 present-only라 과거 hash가 불변임을 확인, 반증 실패. (2) 앵커 — Task 5의 경로 앵커가 evidence-stage-guard.js:95-98과 동형이라는 주장 검증, 성립. 0건/≥2건/legacy 무스탬프 규칙이 실제로 fail-closed 방향인지 확인, 성립. (3) 2.5.7의 새 `[ -f ]` HALT가 신규 차단인지 — pr.md:1079·1097-1115 및 write.js:428(`planAwareMarkdownHash` ENOENT throw)로 대조해 plan의 \\"이미 존재하는 HALT를 앞당길 뿐\\" 주장이 참임을 확인, 반증 실패. (4) 우회(`MCCP_PR_SKIP_LINK_EVIDENCE`)가 receipt 봉인 시점 이후 편집을 요구하는지 — 2.5.7 봉인으로 옮긴 것이 옳음을 확인. (5) back-patch 실패 fail-open이 지표를 부풀리는지 — Task 8 축 3의 해시 대조가 stale을 잡으므로 반증 실패. (6) frozenOnly 화이트리스트(linkage-audit.js:494)로 신규 필드가 동결 블록에 새는지 확인, 새지 않음. 남은 3건은 위 findings이며, 그중 라이브 파티션의 실패 상태 부재(HIGH)는 \\"모든 것이 잘못된 경로\\"를 추적한 결과다 — HEAD 판독 실패 → 라이브 파티션 0 → acceptance 3항목 모두 green. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 710100,
  "halt_stage": null,
  "backlog_appended": 9,
  "backlog_skipped_nonblocking": 8,
  "granted": 4,
  "reviewed_plan_hash": "sha256:0b32a1d5d47f5810db50eac2bc6ab7ed74fea37308196b5f6d51bb458b4e463f",
  "plan_path": ".claude/plans/review-record-linkage-m3.plan.md",
  "recorded_at": "2026-09-02T08:51:20.782Z"
}
```
