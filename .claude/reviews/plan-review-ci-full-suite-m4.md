# Plan Review Panel — ci-full-suite-m4

**Plan**: `.claude/plans/ci-full-suite-m4.plan.md` · **Plan version**: `sha256:ddd23e04d8e6e5f302e1f4cb6587965081d44717b799e25ae49989883e8c9374`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 2 blocking finding(s): invariant/HIGH, invariant/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | Task 1 이 `--pick-delete` 에 부과한 선택 술어("삭제해도 나머지가 green 인 첫 원소")는 그 helper 가 계산할 수 없는 명제다 — 결정적 순수 선택기라고 부르면서 전수 스위트 실행을 요구하는 조건을 얹었고, 어떻게 판정하는지가 계획 어디에도 없다. 오늘 결과가 우연히 참이라 해도(E5c `failing:[]`) 그 술어를 강제하는 코드가 없으면 `--pick-delete` 는 그저 정렬 첫 원소를 내며, 계획은 그것을 stage 2 도달의 전제로 삼는다. | plan `:269-271` "후보는 \\"삭제해도 나머지가 green\\" 인 파일이어야 한다 — 정렬 후 첫 원소가 아니라 그 조건을 만족하는 첫 원소다" 대 `:248` "적격 집합 정렬 후 첫 원소"; 판정 수단 미기재. 실제 CLI 는 `scripts/test-suite/wiring-cut.js:174-192` 의 위치 인자 파서뿐이고 스위트를 부르는 경로가 없다. |
| architect | LOW | Task 2 는 "그 계약을 소비하는 곳을 전부 이름으로 적는다"고 선언하면서 `diffChecks` 반환의 `extra` 소비처를 빠뜨렸다. 처방이 `extra`(=`required \\ declared`)를 `unrelated` 로 대체하는데 CLI 는 여전히 `record.extra` 를 JSON 계약으로 내보낸다. | plan `:348-353` 은 `:137`·`:142`·`:148`·`:159` 만 열거한다. 그러나 `scripts/ci-required-checks.js:151` 이 `extra: verdict.extra` 를 record 에 싣고 `:100-103` 이 그 값을 만든다 — 의미가 바뀌는데 열거에 없다. |
| security | MEDIUM | Task 4가 baseline 수용 조건에서 `redaction_ok === true`를 제거하고 그 대체물로 세운 '게시 안전 불변식'(모든 redaction hit이 `$.failing[i].error` 안에 있다)이, 같은 계획이 backlog H10으로 이연한 fail-open 위에 서 있다. `scripts/test-suite/redact.js:322`의 `if (hits.length >= MAX_HITS) return;`는 `truncated`를 세우지 않고 조용히 반환하므로, hit이 20건을 넘는 leg에서는 21번째 이후의 hit이 배열에 없고 `redaction_scan_truncated`도 false다. 그러면 `## Validation` 오라클의 두 방어(:766 `redaction_scan_truncated===true` throw, :777 hit 위치 검사)가 **둘 다 통과**하면서 failing[].error 밖의 유출을 못 본다. 즉 계획은 유출 탐지 조건을 빼고 그 자리에 같은 결함을 가진 검사를 넣었으며, 그 결함을 인지(H10)하고도 이 의존을 연결하지 않았다. | plan :731-738·:770-778(수용 조건에서 redaction_ok 제거 + 위치 불변식으로 대체) 대 `scripts/test-suite/redact.js:322` (`if (hits.length >= MAX_HITS) return;` — truncated 미설정, MAX_HITS=20은 :175) 및 plan :1000 (H10 '상시. hit 이 20건을 넘는 run 이 나오면 즉시 발현') |
| security | LOW | Task 1이 test seam으로 추가하는 `--workflow <path>` 플래그가 규칙 (a)의 fail-closed 극성을 우회 가능하게 만든다. 거부는 파싱 결과가 **0건**일 때만(`selftest_list_unreadable`) 일어나므로, 자기 test step이 없지만 다른 `*.test.js` 리터럴을 가진 아무 workflow 파일을 `--workflow`로 지정하면 목록이 비지 않아 (a)가 발동하지 않고, `scripts/tests/wiring-cut.test.js`는 tracked `*.test.js`이자 비-격리라 (b)(c)도 통과해 `git rm` 대상이 된다 — 가드가 막으려던 바로 그 파일이다. | plan :256-261 (`--workflow <path>` 플래그를 추가하고 `applyDelete` 가 그 경로를 받도록 한다) + plan :276-278 (거부 극성은 '파싱 결과가 0건이면'에 한정) + `scripts/test-suite/wiring-cut.js:109-118` (applyDelete 는 target 검증 없이 `git rm`) |
| test | MEDIUM | Task 2 (the plan's largest code change — 판독 채널 교체 · declared_unresolved · 3분화 판별자 · diffChecks 시그니처 확장) has no gate-level assertion that its behaviour landed. `## Validation` executes `scripts/tests/ci-required-checks.test.js` as-is (plan:630-632), and that file passes today unchanged; the only executable assertion of the new code, `node scripts/ci-required-checks.js` → exit 0, sits inside the `if [ "$prot" = "true" ]` branch (plan:857-859) which E16 (plan:169, admin:false) declares unreachable this milestone. An implementation that skips Task 2 entirely yields a green Validation. Contrast Task 1, where Validation greps the new reason code (plan:667), and Task 4/5, where the document is re-derived from artifacts — the plan mechanised this class everywhere except its biggest code Task. | plan .claude/plans/ci-full-suite-m4.plan.md:630-632, :857-859, :169; scripts/tests/ci-required-checks.test.js:20 (imports only parseJobNames/diffChecks — readRequiredChecks은 무-test) |
| test | MEDIUM | The OQ3 oracle drops `redaction_ok === true` and substitutes a publication-safety invariant ("every hit is inside `failing[].error`", plan:777), but that substitute is fail-open exactly where it matters: `redact.js#scanResidual` returns after `if (hits.length >= MAX_HITS) return;` **without** setting `truncated`, so hit #21 onward disappears silently and `redaction_scan_truncated` stays false — defeating the oracle's own truncation guard at plan:766. The plan itself records this as backlog H10 (plan:1000) yet still leans on the invariant as the replacement acceptance condition. | scripts/test-suite/redact.js:322 (`if (hits.length >= MAX_HITS) return;` — `truncated` unset) vs plan:766 and plan:777; backlog row plan:1000 |
| test | LOW | The Validation guard check cannot distinguish the two polarities Task 1 defines: `grep -qE 'selftest_target\|selftest_list_unreadable'` accepts either code, so an implementation whose workflow parsing yields 0 entries (blanket refuse) satisfies the judge without rule (a) existing at all — the exact fail-open the plan built the parse for (plan:272-280). The distinguishing evidence lives only in the unit test's fourth branch, which the judge does not verify was added. | plan:667 (`grep -qE 'selftest_target\|selftest_list_unreadable'`) vs plan:272-280 and plan:283-287 |
| invariant | HIGH | The plan consumes the capped review resource through a launch path that is structurally not recorded, making the round ledger a gate that no longer stops anything. It launches the same four `mccp:review-*` roles directly with `Agent` explicitly *because* that path does not reach the counting chokepoint, and it does this repeatedly (the body cites R2, R3 and R4 shadow findings — 12+ unreserved reviewer launches). | Plan `.claude/plans/ci-full-suite-m4.plan.md:1077-1082`: "원장 예산이 2/3라 남은 게이트 라운드는 하나뿐이고 … 같은 네 역할(`mccp:review-*`)을 `Agent` 로 직접 띄워 **원장을 소모하지 않고** 반증을 받았다. 라운드 계수는 `plan-review/cli.js emit-workflow-args` 가 하므로(`:586` `recordPanelRound`) 이 경로는 원장에 기록되지 않는다". Confirmed at plugins/mccp/scripts/lib/plan-review/cli.js:518-528 (BLOCK on cap) and :586 (recordPanelRound) — enforcement and accounting both live only on that path. CLAUDE.md §3.16 states the cap is what makes the 1-round rule mechanical rather than a matter of the executor's diligence; this plan restores it to prose and sets the precedent in writing. |
| invariant | MEDIUM | The same milestone also re-keys the decision slug to obtain fresh cap budget after the existing slug was exhausted, which is the cap-evasion direction the repo's own backlog already flags. | Plan :199-203 "취한 조치: `mccp-plan-codex__ci-full-suite-m4`로 봉인(cap=2)" after E15a recorded 3 rounds on `mccp-plan-codex__ci-full-suite`; the plan itself records at :207-209 that this is "backlog R24 invariant 행이 지적한 '새 슬러그 재발행이 캡을 우회한다'"의 same keying defect. The plan books it as backlog H7 but still takes the budget in this cycle. |
| invariant | MEDIUM | Task 1's guard defines fail-closed polarity for rule (a)'s input failure but leaves rule (b)'s input failure undefined, and the closed reason enum has no code for it — an unreadable/invalid `.github/test-suite-exclusions.json` most plausibly yields an empty quarantine set, which re-admits exactly the target class the guard exists to reject (E10b: excluded 6→5 kills `Enumerate sanity` before judgment, so no `gate.json`). | Plan :249-255 declares exactly four codes — `selftest_target`(a) · `quarantined_target`(b) · `not_tracked_test`(c) · `selftest_list_unreadable`(a's zero-parse) — with no code for an unreadable quarantine list, while :272-280 spends a full paragraph arguing (a)'s zero-parse must be a rejection. `## Validation` only greps `selftest_target\|selftest_list_unreadable` (:667), so no branch of the plan would catch the (b) fail-open. |
| invariant | MEDIUM | The new `--workflow <path>` flag makes rule (a)'s input caller-supplied, so the self-test guard can be disabled by one flag, and only the zero-parse case is pinned by a test. | Plan :256-261 adds `--workflow <path>` to `applyDelete` (today `const WORKFLOW = '.github/workflows/test-suite.yml'` is a module constant — scripts/test-suite/wiring-cut.js:32). A synthetic workflow that names *some other* file parses non-zero, so `selftest_list_unreadable` does not fire and rule (a) simply matches nothing; the plan's fourth test branch (:285-287) covers only the empty-parse case, not the non-empty-but-wrong-list case. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | 인용을 직접 열어 대조했다: `scripts/ci-required-checks.js:85-121`(diffChecks/readRequiredChecks 라인·admin 전용 엔드포인트), `scripts/tests/ci-required-checks.test.js:20,26-49,66-69`(import 목록·고정 단언), `scripts/tests/test-suite-coverage.test.js:722-728,753-761,969-982`(MAX_EXCLUSION_ENTRIES pin·floor 순수 산술·죽은 선언 스캔의 3-소스 한정), `.github/workflows/test-suite.yml:75-76,145-155,168-169,178-189`(job 이름·Enumerate sanity 단언·판정 선행 step), `scripts/test-suite/wiring-cut.js:32,109-118,174-192`(WORKFLOW 모듈 상수·applyDelete arity — 계획의 seam 부재 주장이 참임을 확인), `scripts/test-suite/exclusions.js`(MAX_EXCLUSION_ENTRIES/MAX_PATTERN_WILDCARDS export 실재), `scripts/test-suite/enumerate.js:140-144`(globToRegExp export). 오라클이 의존하는 데이터 형태도 검증했다 — `reporter.mjs:125-194` 의 per_file `{file,tests,fail}` 존재, `redact.js:245-286` 이 두 플랫폼 모두 forward-slash repo-relative 키를 낸다는 점(윈도우 leg 과 리눅스 leg 의 집합 비교가 구분자 때문에 조용히 어긋나는 시나리오를 노렸으나 반증됐다), `run.js:219` 의 `per_file: ok ? perFile : null`(오라클의 검사 순서 주장이 참). 추가로 축 D PR 의 base 미지정이 protection(main 한정)과 어긋나 Acceptance 6 을 도달 불가로 만드는지 공격했으나, `gh pr create` 기본 base 가 main 이고 계획이 head 브랜치만 결속하므로 증거 있는 결함으로 세울 수 없었다. 나머지는 위 두 건 외에 반증 실패. |
| security | pass | 공격한 것: (1) tracked 산출물 유출 — `docs/ci-full-suite/axis-d-*-gate.json` 커밋이 절대경로/호스트 정보를 실어 §3.12 cwd-leak 선례를 재개하는지. `gate.js:181-196,240` 경로는 repo-relative 파일명과 사유 코드만 싣고 coverage는 stage1에서 null이라 유출 경로를 찾지 못했다. baseline 원본 대신 축약본만 커밋하는 결정(plan :223)도 `failing[].error` 채널을 실제로 끊는다. (2) `## Validation`의 셸 주입 — `rd()`가 문서에서 읽는 값이 `gh`/`git`에 도달하는 경로 전부를 봤고, run id·PR 번호·baseline run은 `numeric()`으로 막히며 `enforce-admins`는 `case`, 나머지는 문자열 비교/길이 검사라 주입면이 닫혀 있다. `eval "$(gh …)"` 제거 근거(plan :683-688)도 실재하는 위협을 정확히 기술한다. (3) 부분 상태 신뢰 — Task 2의 `{protected, contexts}` 3분화가 rulesets-only 보호·보호 있음+체크 없음·API 실패 각각에서 약한 필드로 접히는지 확인했으나 전부 fail-closed(`protection_unreadable`/`missing` 발화)로 떨어졌고, 빈 `declared`의 공허참도 `declared_unresolved`로 이미 닫혀 있다. (4) 절단 B의 경로 traversal/pathspec 남용 — 규칙 (c)의 tracked 목록 멤버십이 wildcard pathspec을 걸러내므로 임의 파일 삭제로는 도달하지 못했다. (5) 축 D 증거의 위조 — 기존 run 번호 재사용은 `headBranch`·`workflowName`·`createdAt` 결속으로 실제로 닫혀 있다. 남은 둘(위 findings)만 evidence를 확보했고, HIGH 이상은 찾지 못했다. |
| test | pass | Read the plan in full (1128 lines) and the PRD, then verified its load-bearing citations against source rather than trusting them: scripts/ci-required-checks.js:85-121 (diffChecks/readRequiredChecks shape, :137/:142/:148/:159 consumers, execFileSync gh seam, export list — all as claimed); scripts/tests/ci-required-checks.test.js:26-31/33-41/43-49/62-69 (the assertions the plan says will change — they exist and are correctly named, and no test pins readRequiredChecks, so the plan's \\"unfalsifiable channel\\" claim is true); scripts/tests/wiring-cut.test.js in full (no existing assertion pins applyDelete's unguarded behaviour, so no test encodes the bug); scripts/tests/test-suite-coverage.test.js:722-729/753-761/969-982 (MAX_EXCLUSION_ENTRIES pin, pure-JSON floor arithmetic, dead-reason scan limited to gate/coverage/inputs — plan's rationale for module-owned reason codes holds); scripts/test-suite/exclusions.js:31-122 (no literal reason strings pinned, so Task 5's reason rewrite is safe); .github/workflows/test-suite.yml:145-189 and test-suite-baseline.yml (E10b's Enumerate-sanity kill path, self-test step is literal-parseable, :72 comment claim accurate); run.js:200-224/570-611 and reporter.mjs:128-204 (per_file {file,tests,fail}, platform=process.platform, ok independent of redaction — the OQ3 oracle's inputs all exist). Also attacked the L1 CITATION_RE retraction (l1-check.js:71 does carry `\\\\.?`, so the retraction is correct) and the axis-D run-binding assertions (headBranch/workflowName/createdAt genuinely exclude the pre-existing runs). Found no HIGH; the three items above are the falsifiability gaps I could evidence. |
| invariant | fail | Read the plan in full and the PRD. Verified cited baselines rather than trusting the plan: gate.js:44-60 (closed reason enum incl. `deleted_without_allowance`), gate.js:189-197 (stage-1 immediate return on non-zero exit), wiring-cut.js:32,109-118 (no target guard in applyDelete, module-constant WORKFLOW), test-suite.yml:145-155 (`Enumerate sanity` equality assertion), :168-169 (judgment-preceding self-tests), :186-218 (`if: always()` artifact upload — so red-run gate.json does exist, refuting a suspected missing-artifact hole), plan-review/cli.js:518-528,586 (cap chokepoint + recordPanelRound). Traced unknown-input paths: unprotected-main else branch (fail-closed to explicit `unmet` + ≥30-char blocker — no hole found); `need()`/`rd()` empty values under `set -e` (assignment propagates status — no hole); run-id binding (headBranch/workflowName/createdAt binding closes the pre-existing-run reuse hole); `--pick-delete` picking a suite-reddening file (falls into `a.stage===b.stage` failure — fail-closed); Q read from `origin/main` rather than the edited file (self-justification closed); `unmeasured`/`novel` set handling (both fail-closed); tracked-copy `cmp` assertions for the 90-day artifact expiry. Found no defect in those. The four findings above are what survived. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 396699,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:ddd23e04d8e6e5f302e1f4cb6587965081d44717b799e25ae49989883e8c9374",
  "plan_path": ".claude/plans/ci-full-suite-m4.plan.md",
  "receipt_hash": null,
  "recorded_at": "2026-09-08T07:24:08.615Z",
  "rounds": 3
}
```
