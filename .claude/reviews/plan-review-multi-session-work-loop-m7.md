# Plan Review Panel — multi-session-work-loop-m7

**Plan**: `.claude/plans/multi-session-work-loop-m7.plan.md` · **Plan version**: `sha256:f6bfde5a006196cbfc6034459a4e9c40f714e7f85c94679a9a65687f167441a8`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 3/3 responses · 3 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 7 blocking finding(s): security/HIGH, security/HIGH, security/HIGH, security/FAIL — MCCP_REVIEW_SINGLE_PASS=deadline_pressure 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| security | HIGH | Task 5 references four sanitizer functions from intent-context.js, but those functions are currently NOT exported from module.exports, making the mitigation pathway impossible unless Task 5 explicitly adds them. Plan acknowledges this is required but places critical trust in it being done. | Plan line 264 states: '현재 그 목록에는 `decodeBoundedEntities`만 있어 나머지는 import 불가이고'. Line 265: 'export 없이는 이 Mirror가 실행 불가능한 문장으로 남는다'. Actual intent-context.js module.exports (line 892-926) contains only decodeBoundedEntities; escapeReferenceText, trimDanglingEscape, anyTokenMixedScript, looksDirective are defined but NOT exported. Task 5 Validate (line 265) says it will assert the import, but if the export is omitted, assertion failure is the ONLY guard. Plan acknowledges (line 365) '그 호출이 성립하려면 **네 함수의 export가 먼저 열려야 하며**' but offers no mandatory assertion at commit time. |
| security | HIGH | Reviewer-supplied `cited_path` text is injected into next session's system prompt via state-injector after minimal sanitization. Even with sanitization, this creates an untrusted external input → system prompt boundary. The plan acknowledges this but frames risk as future escalation, not present-cycle, deferring full risk modeling. | Plan line 150-151: '**남는 비용을 "노이즈"라고 부르면 틀린다 — 승격 표면의 독자는 사람이 아니라 다음 세션의 모델이다.**' and '미검증 외부 텍스트가 프롬프트 표면에 도달하는 경계이며'. Plan acknowledges (line 364, in Risks) '**승격 표면이 미검증 리뷰어 텍스트를 다음 세션 프롬프트에 싣는다**' but mitigation relies entirely on sanitizer implementation (escapeReferenceText, trimDanglingEscape) being correct. No multi-layer defense (e.g., quoted presentation, model-level parsing markers). |
| security | HIGH | Plan conditionally requires Task 7 coverage gate to guard BOTH `.claude/reviews/` AND `.claude/state/findings/` surfaces with separate APPROVED_WRITERS lists. If `.claude/state/findings/` coverage is omitted, the normalization chokepoint (Task 1 line 237) can be bypassed by direct writes, leaking absolute paths into git-tracked registry. | Plan line 237: '그 레지스트리는 finding 표면(`.claude/reviews/`)과 레지스트리 경로(`.claude/state/findings/`) 두 표면을 함께 덮으며, 후자를 빼면 이 문장은 근거 없는 주장이 된다(그 누락이 R6 security 지적의 내용이다)'. Task 7 line 277-280 defines APPROVED_SURFACE_WRITERS for .claude/reviews/ AND APPROVED_REGISTRY_WRITERS for .claude/state/findings/, but the actual code does not exist (CREATE directive). Plan says Task 7 must enforce BOTH (line 284 `C1-COVERAGE-REGISTRY-WRITER`) but provides no code specification. Acceptance criterion line 379 only checks `c1-coverage-gate.js --json` exits 0; it does not mandate WHAT that script validates. |
| security | MEDIUM | Escape/sanitization logic (entity decode→escape→trim-dangling-backslash) is implemented as new code in Task 5, not reusing proven functions. If implementation has off-by-one or precedence error, injection is possible. Plan's defense (line 264) depends on assert tests running, but tests are future code and cannot be verified now. | Plan line 264: 'sanitizer는 새로 쓰지 않고 §3.13이 같은 문제로 이미 배송한 `intent-context.js` 함수를 호출한다' and '네 함수를 `module.exports`에 추가한다'. This is the only mitigation for injection. escapeReferenceText (intent-context.js line 423) uses specific order (backslash FIRST, then <>, `, etc.). Line 437 `trimDanglingEscape` checks for odd trailing backslashes. If reimplemented in Task 5, any divergence is a bypass. Plan trusts export + test assertion only. |
| security | MEDIUM | Concurrent worktree registry writes rely on `seq` deduplication for detection of lost appends, not prevention. Plan acknowledges (line 236) concurrent worktrees CAN generate duplicate `seq` values. Detection only triggers if BOTH writers complete; if both hang or second writer never completes after first, silence is permanent. | Plan line 236: '**`seq` 생성에 락을 걸지 않는 것은 의도다.** writer는 `O_APPEND` 단일 write에만 의존하고(부분 착지 없음) 파일 끝에서 `seq`를 계산하므로, 두 프로세스가 같은 work_unit을 동시에 잡으면 같은 번호를 낼 수 있다'. Mitigation is detection via 'max(seq)와 고유 `seq` 개수의 불일치' which requires BOTH writers to succeed or THIRD reader to run. M3 claim TTL (line 236) handles 'only' concurrent claims, not concurrent writes. |
| security | MEDIUM | Plan references security findings (R6, R11, R13) by number in inline risk comments, but no public review document or findings list is provided. R6 and R13 are acknowledged HIGH but no cross-reference to original review, making accountability non-falsifiable. | Plan line 237: '그 누락이 R6 security 지적의 내용이다'. Line 364-365: '(R11 security HIGH 흡수)' and '(R13 security HIGH 흡수)'. No .claude/reviews/ entry exists for mccp-plan-codex/multi-session-work-loop-m7*. Plan header references 'R11', 'R13' but these do not appear in Risks table. Inline citations make findings non-auditable. |
| invariant | CRITICAL | Critical unmitigated dependency: Plan declares C1 will be 'computed' (acceptance L374) but this depends entirely on M8 (pending, undefined) being completed. M8 appears in 6+ plan references (lines 156, 160, 166) but is not listed in plan Risks section (L357-364) | PRD line 160: M8 status is 'pending' \| Line 156-166 plan explicitly states 'M8가 producer를 고쳐' with no M7 fallback \| Acceptance L374: C1 `computed`로 뒤집힌다 requires producer \| PRD line 193: M8 신설 'producer가 실제로 배선된다' — producer currently absent \| Plan Risks (L354-364): No mention of M8 blocking C1 ship |
| invariant | HIGH | Task 2-3 atomic landing is enforced only by social contract (local test), not by gate: Plan claims (L111) 'Task 2와 Task 3은 하나의 커밋으로 착지한다' but only mechanism is 'Task 2 자신의 test가 붉어진다' — this is pre-commit, not gated | Plan DD10 (L158-162): enforcement is 'Task 7의 coverage gate가 정적 co-presence 검사' but Task 7 is AFTER Tasks 2-3 land \| Plan L244-245: 'pre-commit 축' only if developer runs `node --test` \| No pre-commit hook or PR gate blocks split landing \| Only social enforcement: test goes red if run locally |
| invariant | MEDIUM | Task 4 assertion on ADJUDICATION_VERDICTS exhaustiveness is untested premise: Plan claims (DD7, L121-123) 'C1-EMIT-PLAN-CODEX'는 맵이 ADJUDICATION_VERDICTS 전건을 덮는다 as validation but test doesn't exist yet — only promised in Task 4 Validate | Plan L121: '@C1-EMIT-PLAN-CODEX' assertion promised \| Task 4 Validate (L256): `node --test plugins/mccp/scripts/lib/tests/c1-feedback-loop.test.js` mentioned but file doesn't exist \| intent-context.js L69-74 has 4 enum values: ACCEPT_NOW, DEFER_TO_BACKLOG, REJECT_YAGNI, REJECTED_BY_DESIGN \| No code yet validates mapping is exhaustive — only plan claims it |
| invariant | MEDIUM | `.gitattributes` merge=union enforcement is test-only, not gated: Plan Task 1 owns `.gitattributes` update (L189) for findings registry merges, but validation only checks it (L238: git check-attr), doesn't block PR if missing | Plan Task 1 Validate L238: 'test 본문이 execFileSync('git', ['check-attr'...]) 로 git에게 직접 묻는다' \| This is a test assertion, not a pre-merge gate \| If Task 1 forgotten, .gitattributes change silently doesn't land \| Subsequent parallel branch merges will lose entries without detection (git silently picks one branch) |
| invariant | LOW | Sanitizer function export (Task 5) is documented but DD9 (L154) says exports must happen for Task 5 to call them, creating circular dependency if Task 5 is skipped | Plan L154: 'Task 5는 네 함수를 module.exports에 추가한다' \| intent-context.js L890-926: escapeReferenceText, trimDanglingEscape, anyTokenMixedScript, looksDirective are defined but NOT in module.exports \| DD9 L154: 'Task 5는...호출해 승계한다' implies Task 5 needs exports to succeed \| But no gate prevents Task 5 skip; test passes only if Task 5 adds exports |
| invariant | MEDIUM | Receipt hash integrity assumes batch-safe atomic append via fs.writeSync but doesn't specify or test the format invariant: Plan DD8 (L138-140) claims atomic batch write detects loss via seq and malformed detection, but doesn't verify write atomicity or batch format structure | Plan L138: 'N줄을 한 번의 write로' \| Assumes O_APPEND single fs.writeSync is fully atomic \| Plan L237: 'each 줄이 batch_expected: N을 싣는다' but doesn't show JSON structure \| Plan L252: 'reader가 seq 구멍을 유실로 판정' depends on write being atomic — if partial write succeeds (line 1 OK, line 2 broken), seq check works but plan doesn't verify this invariant with a test |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| security | fail | I examined the plan's security model across six axes: (1) Trust boundaries — who writes to shared state and what inputs cross session boundaries. Checked: Task 5 sanitizer export status and found four required functions currently not exported; (2) Data leakage paths — absolute paths into git-tracked registry via normalization bypass. Checked: Task 7 coverage gate design and found structure is sound but actual code absent (CREATE); (3) Untrusted input injection — reviewer finding text into next session prompt. Checked: sanitization claim against actual intent-context.js functions and verified they exist but are not exported; (4) Escape/sanitization logic correctness — checked escapeReferenceText and trimDanglingEscape implementation in intent-context.js; order is correct but reuse is conditioned on export; (5) Concurrent write safety — checked seq-based detection vs lock-free append. Plan uses detection not prevention; covers common case (M3 claim TTL) but not worst-case (both writers hang/orphan); (6) Accountability of referenced reviews — checked for R6, R11, R13 findings and found no linked evidence. Attacked whether normalization chokepoint actually holds with multi-surface coverage gate — plan's design separates APPROVED_SURFACE_WRITERS and APPROVED_REGISTRY_WRITERS correctly, but implementation is future code. Could not identify route from actual input (absolute path in cited_path) to actual consequence (leaked into registry) that doesn't depend on future code correctness or test execution. Sanitization export status IS falsifiable — the functions genuinely exist but are not exported today, making Task 5's requirement real. |
| test | pass | Verified the plan's key claims: (1) Checked the PRD Evidence section's claims about current code state — confirmed that `SOURCE_SCANNERS` at `plugins/mccp/scripts/derive/index.js:28` has no `findings` entry (line 19 claim verified). (2) Verified DD5's claim about buggy `typeIntegrity` check at `plugins/mccp/scripts/lib/msw-metrics/index.js:632` — confirmed it incorrectly requires `(deferred + downgraded + rejected) > 0` which fails when all findings are fixed. (3) Confirmed `.gitignore` does not list `.claude/state/findings/` so registry will be git-tracked as claimed (line 91). (4) Verified existing C1 test at line 416 of msw-metrics.test.js doesn't declare `type_separation` field, matching plan's claim. (5) Checked that all test files referenced in Validate sections (findings-registry.test.js, c1-feedback-loop.test.js, c1-coverage-gate.test.js) don't currently exist, confirming they're to be created. (6) Verified Assertion Roster count (32 total assertions across Tasks 1-8) matches the sum of per-task assertions. (7) Confirmed Task 2 test includes `C1-SOURCE-REGISTERED-COPRESENT` assertion to force co-landing with Task 3 — this pre-commit barrier is documented. (8) Validated that Files to Change table is comprehensive and includes all necessary updates (`.gitattributes`, existing test fixtures, module.exports additions for intent-context.js). (9) Checked that Validation bash commands are valid and executable. (10) Verified that each Design Decision (DD1-DD10) has corresponding test assertions. No untestable claims found. |
| invariant | fail | Traced fail-closed gate integrity: (1) Acceptance criterion (C1 computed) depends on M8 producer which is pending and untracked in Risks — fail-open drift if M8 never ships. (2) Tasks 2-3 atomic landing enforced only by local test, not by pre-commit gate — developer can split without gate catching it. (3) ADJUDICATION_VERDICTS exhaustiveness assertion is promised but test file doesn't exist yet. (4) .gitattributes merge=union is validated in test only, not gated — can be forgotten silently. (5) Sanitizer exports (Task 5) assume Task 5 completes, no bypass detection. (6) Atomic append mechanism described but batch format invariant not specified or tested. Found 1 CRITICAL (M8 dependency), 2 HIGH (Tasks 2-3 split, attestation untested), 3 MEDIUM (gitattributes gate, format invariant, sanitizer export)." |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 3,
    "required": 3,
    "roles": 3,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 458072,
  "halt_stage": null,
  "backlog_appended": 6,
  "backlog_skipped_nonblocking": 7,
  "granted": 4,
  "reviewed_plan_hash": "sha256:f6bfde5a006196cbfc6034459a4e9c40f714e7f85c94679a9a65687f167441a8",
  "plan_path": ".claude/plans/multi-session-work-loop-m7.plan.md",
  "recorded_at": "2026-08-21T01:42:15.498Z"
}
```
