# Plan Review Panel — ci-full-suite-m3u

**Plan**: `.claude/plans/ci-full-suite-m3u.plan.md` · **Plan version**: `sha256:ebed85b989dd1a76e66893ea4bc526f17353c5dc58cfed96fd2cfc8d9ac6a514`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 3 blocking finding(s): invariant/HIGH, invariant/HIGH, invariant/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | `reasons`를 "닫힌 코드 열거"이자 유일 판별자로 선언했는데, fail-closed 여덟 중 여섯의 코드 이름이 계획 어디에도 없다. 열거되지 않은 집합은 닫힌 집합이 아니고, (j)가 요구하는 "그 입력 코드" 단언은 구현자가 즉석에서 지은 문자열을 상대로 쓰이므로 drift 앵커가 성립하지 않는다. | plan L1068-1071은 `suite_red`·`unexplained`·`excluded_over_cap`·`digest_mismatch`·`below_floor`·`deleted_without_allowance`·`redaction`·`measurement_invalid`·`measurement_tree_mismatch` 아홉과 "fail-closed 여덟의 입력 코드(`base_set_empty` · `floor_entry_shape` 포함)"만 적는다. L1093-1117의 여덟 열거(git 실패 · 빈 tracked · `--measurement` 부재/판독불가 · `--floor-from` 부재/판독불가/키부재 · `--exclude-from` 부재/판독불가/검증실패 · merge base 해소 실패 · `base_set_empty` · allow_deletions shape)에는 나머지 여섯의 코드 이름이 없고, L1145-1156 (j)는 "`--json` 산출의 `reasons`에 그 입력 코드가 실린다"를 단언하게 한다. |
| architect | MEDIUM | `timeout-minutes: 60`의 **위치**가 미지정이라 단언 2c(값 pin)가 보호를 담보하지 못한다. step 하나(예: checkout)에 `timeout-minutes: 60`을 두면 단언은 green이고 전수 실행 step은 GitHub 기본 360분을 유지해, R19가 근거로 든 "required check 영구 pending"이 그대로 열린다. 값 pin의 논증("존재만 재면 보호 0인 값이 통과한다")이 scope 축에는 적용되지 않았다. | plan L1739-1741 단언 2c: "`timeout-minutes`가 리터럴 `60`이고 · `concurrency`가 … 이고" — job/step 어느 수준인지 명시 없음. 미러 원본은 job 수준이다: `.github/workflows/test-suite-baseline.yml:44-47` (`jobs.measure` 아래 `timeout-minutes: 180`). 계획은 그 파일을 근거로 인용하면서(plan L1355-1356) 수준은 옮겨 적지 않는다. |
| architect | LOW | 격리 파일의 shape가 두 갈래로 갈린다 — Task 3은 배열로 못박는데 `## Validation`의 오라클 코드는 `raw.exclusions \|\| raw`로 객체 래핑도 받아들인다. 러너/검증기 경로(`normalizeExclusions`)는 배열만 받으므로, 객체 래핑 구현은 Validation 스크립트만 통과하고 소비 경로에서 throw한다. | plan L1204: "`.github/test-suite-exclusions.json`을 `{pattern, reason, ticket}` 배열로 만든다". plan L1971·L1972·L2004의 `node -e`는 `const ex=raw.exclusions\|\|raw;`를 쓴다. `scripts/test-suite/enumerate.js:55-59` `normalizeExclusions`는 `Array.isArray` 아니면 TypeError. |
| invariant | HIGH | R20이 닫은 '스위트를 한 번도 돌리지 않고 커밋된 measurement.json으로 게이트를 green으로 만든다' 경로가 **단계 재배치**로 그대로 다시 열린다. R20의 처방은 `if:` 클래스 단언(단언 1d)이었는데, 그것은 producer를 *끄는* 것만 막고 producer보다 **먼저 판정하는 것**은 막지 못한다. 그리고 이 계획은 순서 단언을 명시적으로 포기한다. | plan L1716-1717: "단계 **순서**는 단언하지 않는다 — 순서 단언은 무관한 편집에 붉어지고, 이 축이 막으려는 것은 순서 뒤바뀜이 아니라 **소실**이다." 그런데 plan L1674-1682은 같은 오라클이 막아야 할 것을 이렇게 적는다: "판정 줄이 pin한 경로는 **repo-relative**라 PR이 같은 이름의 파일을 커밋해 두면 게이트의 0~3단계 전부가 **그 커밋된 파일**을 판정한다. 손으로 만든 green measurement … 는 … **전부 만족**하므로, 스위트가 한 번도 돌지 않은 채 required check가 green이고 단언 1·1b·1c·1d·2·2b·2c·3·3b가 전부 green이다." 판정 step을 전수 실행 step **앞**으로 옮기면 (a) 다섯 step이 전부 존재하고(단언 2b 만족) (b) 어디에도 `if:`가 없고(1d 만족) (c) 네 인자 값이 리터럴 그대로이며(단언 1 만족) (d) 판정은 커밋된 `measurement.json`을 읽는다. 뒤이어 도는 전수 실행 step은 스위트가 red여도 exit 0으로 끝난다 — `scripts/test-suite/run.js:725` `return result.ok ? 0 : 1;`(`ok`는 측정 성립이지 green이 아니다: `:10-12`). 즉 체크 전체가 green이다. `measurement.json`은 `.gitignore`에 없다(grep: `.gitignore` 149행의 `.claude/cache/`만 존재, `measurement.json`/`gate.json` 항목 0건)이라 커밋이 가능하다. 형제 무력화 벡터(`continue-on-error`·`if:`·트리거 좁힘·인자 값 치환·checkout `ref:`)는 전부 단언을 받았는데 이 축만 자발적으로 사거리 밖에 남았다. |
| invariant | HIGH | 판정 줄의 '네 인자 값 리터럴 pin'은 **중복 플래그**에 무감하다 — 리터럴 쌍을 그대로 둔 채 뒤에 같은 플래그를 다른 값으로 한 번 더 붙이면 last-wins 파서가 그 값을 쓰고 오라클은 green이다. R10이 값 치환에 대해 닫았다고 적은 경로가 형태만 바꿔 남는다. | parser 관례가 last-wins임이 실측된다 — `scripts/test-suite/run.js:613-631`의 `parseArgv`는 `flags[a.slice(2)] = next`로 **덮어쓴다**(중복 거부 분기 0건). plan Task 2b(L1090)는 gate CLI가 같은 네 플래그를 받는다고만 적고 `## Files to Change`(L920)·Mirror(L1120-1121)가 `run.js`/`enumerate.js` 관례를 미러하라고 지시하므로 같은 last-wins가 자연스러운 구현이다. 오라클 단언은 plan L1623-1627 "그 `run:` 줄 **안에** … 네 인자의 값이 전부 리터럴로 일치한다"로 **존재**만 재고 유일성·중복 부재를 재지 않으며, 음성 fixture 목록(L1804-1817)에도 '같은 플래그를 두 번 쓴 YAML'이 없다. 따라서 `--floor-from .github/test-suite-floor.json --floor-from bogus.json`(또는 `--base-ref`를 `HEAD`로 재지정)은 모든 단언을 통과하면서 DD9 래칫을 무력화한다 — plan L1649-1652가 바로 그 결과(`base_set = head_set`이면 래칫이 항상 참으로 접힘)를 스스로 적고 있다. |
| invariant | MEDIUM | `## Validation` 4·4a·4b의 `git status --porcelain` 공집합 요구가 이 계획이 정상으로 선언한 상태(12개 신규 파일을 만드는 구현 중)에서 구성상 만족 불가이며, 형제 검사들이 받은 완화/전제 note가 이 셋에만 없다 — '만족 불가'와 '복원이 실패했다'를 구분할 수 없다. | plan L1979 `test -z "$(git status --porcelain)" \|\| { echo "FAIL: --revert-red가 트리에 흔적을 남겼다"; exit 1; }` 와 L2014의 동형 줄은 트리 전체의 청결을 요구하는데, `## Files to Change`(L915-944)는 이 milestone이 CREATE하는 파일만 12개다. 커밋 전에 Validation을 돌리면 이 줄들이 **복원과 무관한 이유로** FAIL하고 그 메시지는 복원 실패를 주장한다. 대조적으로 검사 5(L2023) · 7(L2036-2037) · 7b(L2052)는 각각 note 분기를 갖고, 계획 자신이 이 실패 형태를 R9·R15·R19·R21에 걸쳐 네 번 흡수했다고 적는다(L2019-2022). 선행조건(커밋된 트리에서만 돈다)이 어디에도 적혀 있지 않다. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | 인용 검증: `scripts/test-suite/run.js:213-214`(per_file null) · `:489-505`/`:514`(exit_code null) · `:580`(exclusions_digest) · `:666-679`(exclude-from이 --list보다 앞) · `:685-686`(flags.from) · `:725`(ok?0:1) · `:426-458`(childEnv가 MCCP_CODEX_DISABLED=1 강제) · `enumerate.js:55-147`(normalizeExclusions·exclusionsDigest·enumerateTests 실재, ticket은 digest에 미포함) · `test-suite-baseline.yml:44-47` — 전부 계획이 주장한 대로였다. 산술/집합 공격: DD9 도출식 `tracked = tracked_basis − (max_allowed_deletions+1)`을 초기(면제 0)·정상 삭제 1건·정리 후 재기준 세 사이클에 돌려 `below_floor` 고착을 찾으려 했으나 재기준 규칙이 닫는다. Validation 4a/4b의 `--base-ref HEAD`가 R10이 지목한 \\"base_set=head_set 접힘\\"을 재현하는지 검사했으나, base는 `ls-tree`(커밋), head는 `ls-files`(index)라 `git rm`/`git add`가 실제로 집합 차를 만든다 — 접히지 않는다. 자기 test 단계가 판정보다 앞이라 절단 A·B가 producer를 죽이는 경로를 다시 열려 했으나, 트리 판독 단언이 `max_excluded_files` 하나로 줄고 (e5)·(e5a) 선택 규칙이 그것을 덮는다. `coverage.js`↔`gate.js` 이중 소유, `inputs.js`↔`exclusions.js` 위임, `judge`의 `deletions` 인자, `run.js` 재배선의 소비 경로 위치도 공격했으나 전부 배선이 명시돼 있었다. CI 전수 실행 줄에 `MCCP_CODEX_DISABLED=1`이 없는 것을 결함으로 잡으려 했으나 `run.js`가 기본으로 강제한다(L440·L458). 남은 셋만 evidence가 섰고 HIGH는 없다. |
| security | pass | 공격한 축과 결과. (1) fork-PR 신뢰 경계: `paths` 필터 없는 `pull_request` 게이트가 PR이 추가한 임의 `*.test.js`를 러너에서 실행하는 경로 — DD4(plan L654-669)와 Task 5(L1336-1338) + Task 7 오라클(L1687-1693)이 `permissions: contents: read` · `persist-credentials: false` · `pull_request_target` 미사용 · `actions/*` SHA pin · `secrets.` 미참조 다섯을 값까지 단언한다. 자격증명·시크릿이 PR 통제 프로세스에 도달하는 구체 경로를 만들지 못했다. (2) 셸 주입: `--base-ref "origin/$BASE_REF"` + `env: BASE_REF: ${{ github.base_ref \|\| 'main' }}`(L1479-1485). `github.base_ref`는 base 저장소에 실재하는 브랜치명이라 공격자가 값을 만들 수 없고, `${{ }}` 직접 보간 금지가 두 지점이 아니라 클래스 단언 3b(L1726-1732)로 걸려 있다. `env:`/`with:`/`if:`를 대상에서 뺀 것도 정확하다(셸이 텍스트로 받지 않는다). (3) 내구 산출물로의 절대경로 유출(이 저장소의 cwd-leak 선례): 새로 발행되는 `gate.json`이 `redact.js` 계약 밖이라는 축은 이미 흡수돼 `message`가 `redactText` 통과로 못박혀 있고(L1059-1064), 나머지 필드(`reasons`·`coverage.tracked`·`missing`)는 전부 `git ls-files`/`ls-tree`의 repo-relative 출력이라 절대경로 채널이 아니다. Task 9의 git-tracked 컨테이너 병합은 `validateElement`의 `redaction_ok !== true` 차단 + 2차 `scanResidual`이 실제로 존재함을 소스에서 확인했다(`scripts/test-suite/run.js:321-336` — 계획의 인용이 정확하다). (4) 부분 상태 신뢰/약한 필드로의 fallback: floor 다섯 키·`allow_deletions` 원소 shape·`--exclude-from` 위임 검증이 전부 fail-closed 여덟으로 열거되고 `?? Infinity`/`?? []` 방어적 기본값 경로가 분기 (j)의 키별 다섯 분기로 붉어진다(L1093-1117, L1145-1156). (5) 권한 상승형 우회: `allow_deletions`에 glob 단일 항목으로 삭제 래칫 전체를 여는 경로는 분기 (k) 여섯째(L1189-1193)가, 전면 격리 `**/*.test.js`는 분기 (13)과 `max_excluded_files`가, `base_set` 공집합 관대화는 `base_set_empty`(L817-822)가 닫는다. `--measurement`/`--base-ref` 값 치환과 producer step `if:` 로 끄는 우회도 리터럴 pin + `if:` 클래스 단언으로 닫혀 있다. (6) 게이트 위조(판정 모듈이 PR 통제 트리 안): 닫히지 않았으나 DD4a(L764-789)가 사실관계·통제 지점(코드 리뷰/branch protection)·`redaction_ok` 차단이 발행을 막지 못한다는 교환까지 명시 기록한다 — 은폐된 잔여가 아니다. 어느 축에서도 실제 입력 → 실제 결과로 이어지는 결함을 세우지 못했다. |
| test | pass | I attacked five axes and could not evidence a defect. 1. Citation truth (load-bearing quotes): verified every source claim the plan hangs judgment on — `scripts/test-suite/run.js:179-183` (`Number(r.exit_code)\|\|0` folding null→0), `:205-219` (`per_file: ok ? perFile : null` with the exact comment the plan quotes), `:725` (`return result.ok ? 0 : 1`), `:666-679` (`--list` prints post-exclusion `enumerated.included`), `:685-686` (`flags.from ? readJsonFile : runOnce` — Task 9's `--from` argument), `:728-747` (`main`/`parseArgv` genuinely absent from `module.exports`, so Task 4's spawn-seam requirement is real). All accurate; none overstated. 2. Path realism of Validate/Validation commands: `scripts/version-declaration-guard.js` exists; `.gitignore:149` really contains `.claude/cache/`; `enumerate.js:140-143` really exports `enumerateTests`/`exclusionsDigest`/`normalizeExclusions` with the exact `{trackedFiles, exclusions}` → `{included, excluded}` shape the Validation `node -e` blocks call. I traced checks 4a and 4b through gate stages by hand (planted file is in index but not HEAD ⇒ `missing = ∅` ⇒ reaches stage 1; `git rm` target is in HEAD but not index ⇒ `missing = {DEL}` ⇒ stage 2 `deleted_without_allowance`; synthetic measurement built after the cut so `measured − tracked = ∅`): both are satisfiable, not vacuous, and each asserts a reason code rather than a shared stage number. 3. Tests that pin the bug / regressions from the Task 3 rewire: grepped `scripts/tests/test-suite.test.js` for exclusion usage (`:55,:60,:64,:75,:92-98,:456`). Every one calls `enumerateTests`/`exclusionsDigest` directly or runs `--list` with no `--exclude-from`, so routing run.js's `--exclude-from` through a ticket-requiring `exclusions.js` breaks none of them; and `normalizeExclusions` (`:55-79`) ignores extra keys, so the `{pattern, reason, ticket}` file loads. No existing assertion encodes the behaviour being changed. 4. Over-permissive direction: checked that the wrongly-approved direction is covered rather than only the over-strict one — branch (4) (`**/*.test.js` single entry), (13) (`per_file: null` not folded to 0), (g) (`ok:false ∧ exit_code:0`), (k) sixth branch (glob in `allow_deletions`), (m)/(o) (blocked reaching exit code and stdout JSON), and negative YAML fixtures for `continue-on-error`, step/job `if:`, checkout `ref:`, narrowing trigger keys, `${{` in `run:`, argument-value substitution. I looked specifically for a permissive path with no fixture and did not find one. 5. Claim-without-falsifier: took the plan's two strongest claims — "gate.js is the sole consumer of DD3 ordering" and "axis D proves cuts turn the check red". The first is pinned by branches (a)/(b)/(c)/(d)/(n) plus workflow oracle 1/2b; the second is explicitly split into what the local synthetic checks do and do not measure (4a's own comment concedes the planted-red→suite-red proposition's only producer is the CI run in Acceptance 3-A), which is disclosure rather than an untested assertion. Task 1's Linux-only/node20-only repairs cannot be falsified by its local Windows Validate line, but the plan names the falsifier (post-Task-5 CI, pinned to node 20, and Acceptance item 1 requires that run green), so it is a deferred — not absent — oracle. |
| invariant | fail | 공격한 축: (1) DD3 4단계 판정의 미지 입력 — `ok:false`/`exit_code:null`/`per_file:null`/키 부재를 `scripts/test-suite/run.js:213-214,489-505,725`와 대조했고 stage 0·(f)·(g)·(13)이 전부 fail-closed로 덮는 것을 확인해 반증 실패. (2) fail-closed 여덟 열거와 `reasons` 닫힌 코드 — Task 2b Action(L1093-1117)과 분기 (j)(L1145-1168)의 개수·항목이 일치함을 확인, R18이 지적한 비대칭이 실제로 닫혀 있었다. (3) DD9 floor 산술 — `tracked = tracked_basis − (max_allowed_deletions+1)`을 정상 삭제 2사이클·재기준·절단 B 트리에 대해 손으로 돌려 R14/R15/R17이 만든 자기모순이 실제로 해소됐음을 확인(영구 red 재현 실패). (4) `--base-ref HEAD`(4a·4b) — `merge-base HEAD HEAD` + `ls-tree`(커밋) 대 `ls-files`(index) 비대칭 덕에 `git rm` 삭제가 실제로 `missing`에 잡히므로 래칫이 접히지 않음을 확인. (5) 격리 축의 한 줄 glob — `enumerate.js:27-50` `globToRegExp`와 `:104-132` 이분 구성을 읽어 분기 (4)·(13)이 실제로 덮는 것을 확인. (6) `exclusionsDigest` 존재·정규화 범위 확인(`enumerate.js:84-90,140-147`) — 계획의 인용이 정확. (7) CI에서 codex가 발화하는지 — `run.js:440,458,655-664`가 `MCCP_CODEX_DISABLED=1`을 기본 강제하고 Task 4 가드가 `--allow-codex`를 CI에서 죽이므로 반증 실패. 남은 것은 위 셋이며, 처음 둘은 workflow를 통제하는 PR이 **모든 단언을 green으로 유지한 채** 게이트를 무력화하는 경로다(계획이 이미 in-scope로 취급해 R10·R17·R20에서 닫은 형제 벡터와 같은 위협모델이므로 DD4a 면책 대상이 아니다). |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 308623,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:ebed85b989dd1a76e66893ea4bc526f17353c5dc58cfed96fd2cfc8d9ac6a514",
  "plan_path": ".claude/plans/ci-full-suite-m3u.plan.md",
  "recorded_at": "2026-09-04T05:04:19.894Z"
}
```
