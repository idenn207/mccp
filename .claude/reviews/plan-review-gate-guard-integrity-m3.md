# Plan Review Panel — gate-guard-integrity-m3

> R1~R3 기록은 형제 파일 [`plan-review-gate-guard-integrity.md`](plan-review-gate-guard-integrity.md)에 있다. 이 파일은 `derive-decision`이 산출한 정식 slug(`gate-guard-integrity-m3`)를 따른다.

## R5 (2026-08-16) — **`divergent`** · 차단

**Plan version**: `sha256:f334a115d5d9bf654a6f336d5b0eec38e77e7089c0cafd0e00905d13569847e2` (R4 흡수 반영본)
**Verdict**: `divergent` via `multi-agent` · `review_proof` **null**
**Quorum**: 4/3 응답 · 4 roles — **blocking 11건으로 미충족**
**Layers**: L1 `converged`(violations 0 — 단 1차 시도는 `C6_UNRESOLVED_CITATION` 2건으로 red였고 축약 경로 인용을 full 경로로 고쳐 통과) · L2 `divergent` · L3 not fired
**Elapsed**: 265s / 219252 subagent tokens / 4 agents

Round verdicts: architect **pass**(0) · security **pass**(0) · test **fail**(6) · invariant **fail**(6).

**R4 대비**: architect `fail` → **`pass`** · security `pass` 유지 · test `fail` 유지 · invariant `fail` 유지. architect는 R4가 지적한 "좁히는 기계 미명시"가 해소됐음을 직접 확인했다 — refutationAttempted 항목 7이 pr.md 파일 단위 스코프가 plan.md의 정당한 placeholder(`:95` `:2319`) 오탐을 막는다고 적었다.

**수렴 가능성에 관한 기계적 사실**: `quorum.js:200`은 `passed: reasons.length === 0`이고 blocking finding이 1건이라도 있으면 reason이 추가된다. 즉 quorum은 득표제가 아니라 **blocking-zero제**이며 `required`/`rolesMin`은 승인 표수가 아니라 참여 하한이다. 4인 중 2인이 pass여도 나머지의 HIGH 1건이 전체를 차단한다.

### Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| test | CRITICAL | Task 5 adds a test named 'prose line between heading and table' to detect C3 defect (parsePlanFiles not handling prose between heading and table) | Validation line 250 greps for this test name: `grep -c 'prose line between heading and table' plugins/mccp/scripts/receipt/tests/dedupe.test.js`. Searching the actual file (dedupe.test.js) with pattern 'prose\|freeze\|between heading' yields no matches. The test does not exist. |
| test | HIGH | C3 negative cases are specified and can be verified by test name grep | Plan line 95 claims to UPDATE dedupe.test.js with 'C3 부정 케이스 — 프로즈 줄 존재 시 파싱 성공 + 표 부재 시 여전히 fail-closed' but provides no implementation detail. Validation line 250 assumes this test will exist. The test is not currently in the file and the plan does not specify what it should look like. |
| test | HIGH | validate-callsite-lint.test.js will be updated to check that --plan values in pr.md are shell variables, not placeholders | Plan line 98 lists this file as UPDATE with reason 'close the gap that lint's own comment admits'. But the current test file (validate-callsite-lint.test.js line 15-16) explicitly states 'The flag values may be variables, placeholders, or literals — only flag presence is asserted.' The plan does not show what new test assertion should be added to verify --plan values are variables. |
| test | HIGH | Validation command at line 234-241 will verify all three pr.md validate callsites have shell variable --plan arguments | The Node.js one-liner at line 234-241 checks for `--plan` starting with `"$`. But this is NOT integrated into the validate-callsite-lint.test.js file. The test file update (line 98) and Validation command (line 234-241) appear to be parallel approaches rather than the same mechanism. The A/B test at line 244-247 tries to verify 'updated' test will fail on unfixed code, but the test file hasn't been shown to have the update. |
| test | HIGH | A/B test for C6 (line 244-247) verifies the fix by showing the test fails on stashed (unfixed) pr.md | The A/B test stashes pr.md and runs 'validate-callsite-lint.test.js', expecting FAIL. However, the current test file only checks for flag PRESENCE (line 15-16), not flag VALUES. Even with the placeholder `<plan path>` still in line 883, the test would still PASS because the `--plan` flag exists. The test needs to be updated first to check values, which would make the A/B test coherent, but that update is not shown in the plan. |
| test | MEDIUM | C4 test will verify per_run output includes failing field (per_run[].failing exists) | Validation line 262 checks `if(!Array.isArray(j.per_run)\|\|!("failing" in j.per_run[0]))`. But current code (suite-determinism.js:209) maps `{pass, fail, skipped}` and drops `failing`. After Task 6 fix, this should work, but the Validation assumes the fix has been applied. This is acceptable for a post-execution validation, but highlights that the test is ONLY valid after fixes are done. |
| invariant | HIGH | Task 10 (cleanup/archive) has no mechanical enforcement gate, making M3 'complete' status depend on unforced human action | Plan lines 176-181 (Task 10): '**강제 수단이 없다는 사실을 명시한다** (L2 architect HIGH): 머지 후 사람이 이 두 명령을 실행하도록 **기계적으로 강제하는 게이트는 없다.**' This admission contradicts PRD's investigation of gates that 'look like they work while no longer stopping anything.' The plan claims completion but provides no mechanism to verify cleanup actually occurs, making the M3 completion row itself potentially stale indefinitely. |
| invariant | HIGH | Acceptance criteria (lines 309-311) are unfalsifiable until after implementation because they grep a report file that doesn't exist yet | Plan Acceptance section lines 309-311 requires: 'grep -c "강제하는 게이트는 없다" .claude/PRPs/reports/gate-guard-integrity-m3-report.md ≥ 1' and similar grep checks on non-existent m3-report.md. These files only exist after implementation is complete. The implementer controls whether these strings appear in their own report, making independent verification impossible before/during the gate. |
| invariant | CRITICAL | C6 reveals M1's fix for G2 (terminal gate staleness) was incomplete and partially undocumented | Plan line 61: 'M1 리포트가 2.5.9 수정만 기록하고 2.5.8을 언급하지 않는다' — M1 supposedly fixed G2 at pr.md:943 (2.5.9) but left pr.md:883 (2.5.8) with literal `<plan path>` placeholder. Line 62-63 explains this is fail-open: 'LLM이 깨진 bash를 내는 대신 `--plan` 줄을 통째로 빠뜨릴 때' causes staleness check to skip silently. The M1 milestone marked as 'complete' but the gate it claims to fix was only partially restored. |
| invariant | CRITICAL | M1 and M2 both claim 'complete' status while explicitly failing to meet their stated outcome metrics | PRD lines 75-77 (M2): '**Outcome 문장의 전반부("전수 실행 결과가 실행마다 동일")는 달성되지 않았고** ... `complete`는 지표 충족이 아니라 **운영자의 명시적 수용 판정**'. Line 99-101 (M1): 'Codex 한도가 소진 ... **모델 다양성은 이 축에서도 획득되지 않았다**' but still marked complete. Plan relies on these 'complete' milestones being trustworthy foundations but both explicitly failed their metrics. |
| invariant | MEDIUM | Plan Task 0 (re-verify issues still exist) is meant to happen during implementation, not before approval, creating a skip predicate for gate approval | Plan line 115: 'A1~A4 · B1~B5 · C1~C6의 실측 근거 명령을 그대로 재실행해 **착수 시점에도 성립함**을 확인한다.' This gate is designed to run **during** implementation (line 116 'Mirror' references M1 report pattern). The plan approval thus skips the verification that the claimed issues (A1-C6) still actually exist, delegating it to Task 0 which happens post-approval. |
| invariant | MEDIUM | Plan section D (lines 65-72) explicitly documents unfixed gaps but does not provide rollback safety for their unresolved state | Plan D section headers: 'OQ5 — M2 유입 비결정 2건의 메커니즘' marked 'UI5가 계측까지로 제한' and 'cross-model 미확증' marked as not acquired but not explicitly blocked. Section D items are deferred without mechanical markers preventing downstream gates from treating them as resolved. |

### Refutation attempted

| Perspective | Verdict | 공격 축 |
|---|---|---|
| architect | pass | Verified all major structural claims: (1) C6 placeholder inconsistency — confirmed line 883 has `--plan <plan path>` vs lines 238/943 with real variables; staleness conditional on --plan per validate-cmd.js:363. (2) Validate callsite count — grep confirmed exactly 3 in pr.md (235/880/940), matching gating loci. (3) Precedent patterns — verified fixture tmpdir established in a3-instruction-cost.test.js:31 and variable derivation in pr.md:224/939. (4) M1 OQ closure — confirmed .claude/plans/gate-guard-integrity.plan.md lines 34-100 address OQ1-OQ3. (5) Evidence binding — plan's hash_bound validation uses established audit tool. (6) Trade-off documentation — Task 10 manual execution is explicit with observability fallbacks. (7) Lint boundaries — pr.md file-only scope avoids false positives in plan.md's legitimate placeholders (lines 95, 2319). (8) Cross-dependency handling — A/B testing isolation (15 runs) for C3/C4 addresses OQ5 flakiness. No undefended assumptions or abstraction leaks found. |
| security | pass | **Attacked:** Trust boundaries, data leakage, staleness validation bypass, evidence integrity, and trust escalation.  **Specific scenarios tested:**  1. **C2 archive delegation (data loss via incomplete archiving)**: Verified that removing manual `mv` from `prp-implement.md:1469-1470` and delegating to `/mccp:archive-complete` creates only operational delay, not data loss. Plan stays in active `.claude/plans/` until post-merge archive runs. Timing is correct: plan available for PR flow (2.5.8/2.5.9) before archive-complete removes it. No loss vector found.  2. **C6 placeholder-to-variable fix (silent guard bypass)**: Confirmed line 883 `--plan <plan path>` is a genuine placeholder that could fail silently if LLM omits the line (staleness check skipped via `validate-cmd.js:363` conditional). Verified DECISION_SLUG is in scope at 2.5.8 (derived at 2.5.7). Confirmed fix matches 2.5.9 pattern (line 939 uses `${PR_PLAN_PATH:-.claude/plans/${DECISION_SLUG}.plan.md}`). Proposed variable `CHAIN_PLAN_PATH` follows same scope/availability rules. Grep validation expects 3 occurrences post-fix (currently 2), A/B test non-emptiness confirmed. No escalation or bypass vector created by this change.  3. **A1 evidence ledger commit (audit trail tampering)**: Verified that committing untracked `.claude/state/completion-ledger/gate-guard-integrity-m2__*.json` doesn't leak secrets (contains only metadata: decision_id, verdict, timestamp). Receipt hash is deterministic digest of receipt contents, not reversible. Validation requires `state !== inconsistent` AND `hash_bound === comparable` post-commit (§3.12 no-rehash invariant preserved). No trust boundary crossed by this archival action.  4. **C1 fixture leakage (repo-tree test artifacts)**: Confirmed `msw-events.test.js:13` and `toggle-snapshot.test.js:61` use repo-relative paths (`.test-msw-events/`, `.test-toggle-snapshot/`) with fixed sessionIds, not git-ignored. Move to `os.tmpdir()` with dynamic sessionIds eliminates risk. `.gitignore` currently lacks protection (verified no patterns). Fix scope appropriate.  5. **PR_PLAN_PATH variable trust**: Verified that Phase 2 DISCOVER sets `PR_PLAN_PATH` deterministically from artifact discovery (not user input). Used as fallback in both 2.5.8 and 2.5.9 via parameter expansion `${PR_PLAN_PATH:-.claude/...}`. If unset, defaults to `DECISION_SLUG`-derived path. Staleness check defends against stale/corrupt plan. No user-controllable escalation path created.  6. **Guard test isolation (MCCP_CODEX_DISABLED leakage)**: PRD OQ3 decision confirmed ("기존 receipt 즉시 stale 0건"). Staleness check is fail-closed by design (gate skipped when planPath absent). Plan doesn't claim to fix stale-test isolation itself, delegates to OQ5 and backlog per scope. Test environment assumption documented.  7. **Cross-gate dedupe consistency**: Verified that removing Phase 5 archive from `prp-implement` doesn't break dedupe logic at PR gate. Dedupe depends on plan text and diff, not file location. PR flow (2.5.8/2.5.9) reads plan from active directory before archive. No bypass path created.  **Negative findings:** All three identified defects (C2, C6, C1) are correctly scoped, risks accurately characterized, and mitigations are sound. Evidence durability commitment (A1) preserves §3.12 invariants. No new attack surface, trust boundary violation, or escalation introduced by this plan's changes. |
| test | fail | **Claims attacked:**  1. **C3 falsifiability** — Searched for the test name the plan claims will exist ("prose line between heading and table"). The test does not exist in dedupe.test.js. Plan does not provide implementation of what this test should be.  2. **C6 test update specification** — Read validate-callsite-lint.test.js. Confirmed it only checks flag presence (line 15-16 comment), not flag values. Plan claims to update it but does not show what the update should look like.  3. **C6 Validation command coherence** — The one-liner at line 234-241 checks `--plan "$..."` format, but this is separate from the test file update. The A/B test assumes the test has been updated to check values, but the test file shows no such update specification.  4. **Test file update vs Validation mismatch** — Compared Validation commands with Files to Change list. C3 expects grep for test name; C6 expects both a test file update AND a separate Node.js one-liner check. The relationship is unclear.  5. **Pre-execution state** — Confirmed C6 defect exists (line 883 has placeholder), but the A/B test would not work with current test file because test only checks flag presence, not values.  **Suites read:** - validate-callsite-lint.test.js (full file, 182 lines) - dedupe.test.js test list (grep output) - suite-determinism.js (lines 190-230) - suite-determinism.test.js (lines 1-98) - pr.md (lines 200-945, validate callsites) |
| invariant | fail | Attacked the plan through invariant lens: (1) searched for fail-open drift in Task 10 cleanup enforcement — found no mechanical gate, only human-intended action; (2) examined evidence anchoring in Task 1 — confirmed ship receipts are tracked but ledger entries are untracked, hash_bound mechanics appear sound but depends on prior milestone being trustworthy; (3) traced skip predicates in Task 0 verification — found verification gates enforcement was deferred to implementation not approval; (4) checked rollback reality in C6 staleness fix — found M1 only partially fixed the gate and left 2.5.8 with placeholder; (5) reviewed M1/M2 completion claims — found both explicitly failed outcome metrics but marked complete anyway; (6) examined HALT vs degrade in Acceptance criteria — found criteria are unfalsifiable before implementation because they require grepping non-existent report files; (7) analyzed how anchoring works for M3 'complete' status given no enforcement gate for Task 10. Fundamental issue: the plan is investigating whether gates work, but relies on prior 'complete' milestones that admittedly failed their metrics, and proposes cleanup with no enforcement, making M3's own completion status potentially stale indefinitely." |

---
## R4 (2026-08-16) — **`divergent`** · 차단

**Plan**: `.claude/plans/gate-guard-integrity-m3.plan.md` · **Plan version**: `sha256:e9a434c8377c0e1ed66be874a5756c9ce09aecd8876103864c16f12de1c720a8`
**Verdict**: `divergent` via `multi-agent` · `review_proof` **null**(수렴 증명 없음)
**Quorum**: 4/3 응답 · 4 distinct roles (of 4) — 응답은 충족, **판정은 미충족**
**Layers**: L1 `converged`(violations 0) · L2 `divergent` · L3 not fired (`fires.l3=false`)
**Elapsed**: 220s / 109848 subagent tokens / 4 agents

**Reason**: L2 quorum not satisfied: 9 blocking finding(s): architect/HIGH, architect/FAIL, test/HIGH, test/HIGH

Round verdicts: architect **fail**(3) · security **pass**(0) · test **fail**(4) · invariant **fail**(4).

blocking finding 추이: R1 10 → R2 5 → **R3 0 → R4 9**. R3는 findings 0으로 승인했으나 receipt write 전에 실행이 죽었고, 재진입한 R4가 아래를 찾았다.

### Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | Task 4 proposes to add a 'static regression test' to validate-callsite-lint.test.js that checks --plan values don't start with '<' and do start with '"$', but does not specify how the test will be scoped to only the 3 game-critical callsites (2.5.7 precheck, 2.5.8 chain-check, 2.5.9 ship-gate), creating risk of false positives on legitimate placeholders in other commands like plan.md:419 | Plan lines 138-142: Task 4 action says '정적 회귀를 건다 — ship/chain을 게이팅하는 validate callsite의 `--plan` 값이 `<`로 시작하지 않고 `"$`로 시작함을 단언한다' but does not specify the mechanism for narrowing to the 3 callsites. Line 140 acknowledges the need for scope-limiting ('단언 대상은 **결과에 게이팅이 걸린 곳**...셋이다. `plugins/mccp/commands/plan.md:419`·`plugins/mccp/commands/pr.md:806` 같은...placeholder는 그대로 둔다'), but the action block does not explain HOW this narrowing will be implemented. Line 265 (Risks) restates the risk but mitigation says 'lint는 전 `commands/*.md`를 스캔하므로 범위를 넓히면 광범위 red가 되고', indicating the existing test scans all commands. |
| architect | HIGH | The plan assumes the lint test modification will pass the validation command (line 227) after changes, but if the implementation adds a strict pattern check across all validate calls in all commands, it will fail on legitimate placeholders like plan.md:419, contradicting the scope limitation stated in line 140 | Plan lines 225-227 show validation running 'node --test plugins/mccp/scripts/lint/tests/validate-callsite-lint.test.js' and expecting it to pass after changes. However, the existing test (lint file line 104-143 that I read) scans 'every validate call in command bodies' across all files via listCommandMarkdown(COMMANDS_DIR). If Task 4 adds a check enforcing `--plan` pattern (no `<`, yes `"$`), it would reject legitimate placeholders in plan.md:419 (grep output confirmed: 'node ${CLAUDE_PLUGIN_ROOT}/scripts/lib/plan-review/cli.js l1 --plan "<plan path>"'). This would cause the test to fail in validation, not pass. |
| architect | MEDIUM | Plan section on scope limitation (line 140) states the assertion is limited to 3 callsites but does not document the boundary mechanism (hardcoded line ranges, file-specific scoping, semantic markers in code, or filtering by command name), leaving implementation ambiguous | Plan lines 140 and 265: Both acknowledge the need to limit to 3 specific callsites in pr.md. Line 265 risk says 'lint는 전 `commands/*.md`를 스캔하므로 범위를 넓히면 광범위 red가 되고' (lint scans all commands, so expanding scope = broad red). But Task 4 action block (line 138) does not specify HOW to implement this boundary. The test file I examined (validate-callsite-lint.test.js) has functions that parse all commands (listCommandMarkdown, findValidateCallsites) with no built-in scoping. No alternative approach is documented in the plan. |
| test | HIGH | Task 4 Validation test will verify that the scope is correctly implemented (line 225: grep should return 0 for '--plan <' in pr.md) | Line 225 of plan says: `grep -n -- "--plan <" plugins/mccp/commands/pr.md # 0건 이어야 함`. But current pr.md has `--plan <` at line 419 (dedupe callsite) and line 883 (validate callsite). Task 4 scope (line 140) explicitly states: 'placeholder는 그대로 둔다' for non-gating contexts. Line 419 is a dedupe call (non-validate, non-gating). So after implementing only line 883 per the scope, line 419 will still have `--plan <`, causing the grep test to return 1 match, not 0. This contradicts the stated scope - either the scope needs to expand to line 419, or the validation test is incorrectly written. |
| test | MEDIUM | The grep test at line 226 expects 3 occurrences of '--plan "$' (2.5.7/2.5.8/2.5.9) | Line 226 says: `grep -c -- '--plan "\$' plugins/mccp/commands/pr.md # 3 (2.5.7/2.5.8/2.5.9)`. Currently there are only 2 occurrences (lines 238 and 943). After fixing line 883 as planned, there will be 3. This validation is correctly written and will pass. However, combined with the previous finding about line 225 failing, the full validation block is internally inconsistent. |
| test | MEDIUM | The plan does not explicitly state whether pr.md:419 (dedupe callsite) is within or outside Task 4's scope | Task 4's scope examples at line 140 mention 'plan.md:419' and 'pr.md:806' as non-gating contexts to leave unchanged, but do not explicitly mention pr.md:419. The dedupe command at pr.md:419 is neither a validate callsite nor explicitly listed as 'intentionally unchanged'. This ambiguity directly causes the contradiction between the scope and the validation test. |
| test | HIGH | Validation test [C6] cannot be run as a falsifiable check until the contradiction is resolved | Lines 224-231 describe the validation for C6. Line 225's grep test will fail after correct implementation of the scope as written (only fixing line 883), making it impossible to verify whether the implementation succeeded. The plan states at line 179 that Validation blocks are 'Task가 만들 산출물을 grep으로 확인하므로' post-implementation checks, but this particular check fails in both branches: (a) follow scope and test fails, (b) follow test and over-scope the change. |
| invariant | HIGH | Acceptance criterion (line 287) contradicts stated Risks mitigation (line 263) for flaky tests | Line 263: 'Task 5가 건드리는 함수의 테스트가 OQ5의 flake 당사자라... 전수 결과로 판정하지 않는다' (do NOT judge by full-suite results due to flakiness). Line 287: '전수 실행에서 `fail` 비증가 + `pass` 비감소' (DO judge by full-suite results). Validation block (line 256) includes: 'node --test --test-reporter=tap "plugins/mccp/scripts/**/*.test.js"' which is a full-suite run. The plan explicitly states the risk is High, yet the Acceptance criterion directly violates the mitigation by requiring full-suite judgment. |
| invariant | HIGH | Task 10 Acceptance criterion (lines 288-289) are forward-looking assertions that cannot be validated at plan-review time, breaking anchoring to reviewed artifact | Lines 288-289 require the report to contain specific strings: 'grep -c "강제하는 게이트는 없다" .claude/PRPs/reports/gate-guard-integrity-m3-report.md' and 'grep -c "OQ5의 근본 원인을 주장하지 않는다"'. The report file does not exist at plan-review time; it is generated during implementation. The Acceptance criteria thus reference an artifact that the plan's author has not yet written. This violates the invariant principle that verdicts must be sealed against the thing they claim to describe—here, the acceptance criteria are sealed against a promise about a future document, not against the current plan document. |
| invariant | HIGH | Task 10 (post-merge archive) has no mechanical enforcement gate, creating fail-open risk for status rot | Plan line 173-175: '기계적으로 강제하는 게이트는 없다. 있는 척하지 않는다... 미실행의 결과는 데이터 손실이 아니라 **아카이브 지연**이며'. The plan acknowledges no gate enforces Task 10 after PR merge. If Task 10 is never executed, M3's PRD and plan files remain in the active state (`.claude/prds/`, `.claude/plans/`) indefinitely, even after M3 is marked `complete`. This creates **status divergence**: the ledger says 'complete' but the files remain 'active'. The proposed mitigation (three-way detection: scan oracle, backlog row, report item) is observational, not preventive. Per CLAUDE.md §3.11 C2, PRD archive is required when 'PRD 전체 완료 시에만', but the plan creates no gate to prevent shipping M3-complete while files remain unarchived. |
| invariant | MEDIUM | Validation block Acceptance criterion (line 256) references test outcomes that have acknowledged non-deterministic failures, making the acceptance criterion itself flaky | The plan's own Open Questions (lines 109-110) state: 'M2가 유입시킨 비결정 2건... 각 ≈10%/run'. Lines 237-239 (Validation) show: 'for i in $(seq 1 15); do node --test plugins/mccp/scripts/receipt/tests/dedupe.test.js >/dev/null 2>&1 || echo "FLAKE run $i"; done', indicating even the plan's author expects flakes. Yet line 287 (Acceptance) requires '전수 실행에서 `fail` 비증가 + `pass` 비감소' with no mention of retries or isolation. If the full test suite fails due to random flakiness (not due to plan defects), the Acceptance criterion will fail, blocking the plan's approval even if it is correct. |

### Refutation attempted

| Perspective | Verdict | 공격 축 |
|---|---|---|
| architect | fail | I verified the plan's architectural choices by examining: (1) DECISION_SLUG scope - confirmed it is derived in 2.5.7 before 2.5.8, validating the assumption in Task 4 line 138. (2) The placeholder issue at pr.md:883 - confirmed the placeholder exists and needs replacement with real variable per pr.md line 939 pattern. (3) Fixture pattern consistency - confirmed a3-fixture.test.js line 31 uses os.tmpdir() mkdtemp pattern matching Task 2 proposed approach. (4) C2 archive boundary issue - confirmed prp-implement.md lines 1469-1470 move files prematurely, creating self-blocking risk with guard 2 staleness check. (5) The critical issue: I traced Task 4's lint test narrowing by reading validate-callsite-lint.test.js (lines 104-143 showing scan of ALL commands), confirming the mechanism for scope limitation is absent. The plan states need for scope (line 140, line 265 risk) but the action block (line 138) provides no implementation detail for how to narrow a test that scans all commands to check only 3 specific callsites in one file. |
| security | pass | Traced attack surface along trust boundaries: (1) path normalization to repo-relative before storage; (2) fixture isolation from repo-tracked directories; (3) staleness check gate repair via derived variable vs. placeholder in scope where DECISION_SLUG is proven available; (4) ledger-receipt binding verification via evidence-audit against hash_bound=comparable; (5) archive deferral avoiding self-blocking with acknowledged procedural coupling; (6) absence of secret/path leakage in new changes; (7) no new bypass paths or override escapes. Cross-model review finding coverage (C6, B5) via L2 reflection panel despite Codex disabled. Validation criteria mechanically verifiable via grep. No defects found on any axis. |
| test | fail | Attacked Task 4's Validation section ([C6] block, lines 224-231) by: 1. Extracted all `--plan <` and `--plan "$` patterns from pr.md using grep to verify the validation assertions 2. Found pr.md currently has `--plan <` at lines 419 (dedupe) and 883 (validate)  3. Checked the stated scope (line 140) which says non-gating placeholders should remain unchanged 4. Identified that line 419 is a dedupe callsite (non-validate) and thus not in the 3-callsite scope 5. Verified this contradicts line 225's expectation of 0 matches for `--plan <` 6. Examined plan.md and pr.md for all `--plan <` patterns to understand scope boundaries 7. Confirmed lines 238, 943 have `--plan "$` (validate callsites that should be variable-based per scope) 8. Found that Task 4 explicitly limits scope to "결과에 게이팅이 걸린 곳" (3 validate callsites) yet validation test expects zero `--plan <` across entire pr.md file  The plan makes load-bearing claims about guard restoration but the test that should validate those claims contains an internal contradiction with the stated scope. |
| invariant | fail | Attacked the plan's invariants across five axes: (1) **Fail-open drift**: checked Task 10's enforcement story and found no gate preventing post-merge archive from being skipped, with only detection (not prevention) offered. (2) **Anchoring**: examined the Acceptance criteria and found forward-looking assertions (lines 288-289) that reference a future report file not yet written, violating the principle that verdicts seal against artifacts reviewed at decision time. (3) **Direction of defaults**: traced what happens if Task 10 is never done and found the plan's claim of 'archive delay only' masks actual status divergence (ledger says complete, files remain active). (4) **HALT vs degrade**: confirmed that failure of Task 10 results in silent status rot, not a halt. (5) **Digest coverage**: checked whether new validation includes both the Risks mitigation guidance and the Acceptance criteria, and found direct contradiction between line 263 ('전수 결과로 판정하지 않는다') and line 287 ('전수 실행에서 fail 비증가'), with the Validation block including full-suite runs that the Risks section says not to judge by. |
