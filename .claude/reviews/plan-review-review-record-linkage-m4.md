# Plan Review Panel — review-record-linkage-m4

**Plan**: `.claude/plans/review-record-linkage-m4.plan.md` · **Plan version**: `sha256:b39fca3d9a4045748b9f2692b334dc5f4ff1fb58db2647fc570af0dfdb2a73b4`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 12 blocking finding(s): architect/HIGH, architect/HIGH, architect/FAIL, security/HIGH — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | Task 8의 acceptance는 구조적으로 달성 불가다. 원장은 `<gate>__<decision>.json`으로 키잉되고 이 사이클의 라운드는 R8대로 `mccp-plan-codex__review-record-linkage-m4`에 기록되는데, Task 8은 `--slug review-record-linkage-m4-postimpl`로 record를 돌린다. Task 4가 지정한 주 키 `('mccp-plan-codex', slug)`로 읽으면 그 원장 파일은 존재하지 않고 `ledger.count`는 0을 돌려주므로 `measurement.rounds`는 0이 된다. 그러면 Acceptance의 'rounds가 정수 ≥ 1'과 '`--check-round-structure`가 exit 0'이 동시에 실패한다. | plan Task 8:241 (`record --slug review-record-linkage-m4-postimpl`) vs plan R8:296 (봉인·record 모두 `review-record-linkage-m4`) vs ledger.js:106-121 (`ledgerBasename(gateId, decisionId)`) 및 ledger.js:187-198 (`read`가 파일 부재 시 `emptyState` → `count`가 **0**) |
| architect | HIGH | 강제 locus의 범위가 지표의 범위와 어긋난다. `--check-round-structure`는 '라이브 파티션에 absent가 1건이라도 있으면 비영점'인데, linkage-audit의 라이브 파티션은 '경계 트리 밖의 HEAD 레코드 전부'이지 '착지 후 발행분'이 아니다. plan 자신이 오늘 라이브 68건 전건 0이라고 적었고 UI1이 소급 주입을 금지하므로, 그 68건은 영구히 `absent`로 남아 종료코드는 Task 8 착지 뒤에도 절대 0이 되지 않는다. Validation 주석('Task 8 착지 후에만 통과한다')과 Acceptance 항목이 둘 다 거짓이 된다. | linkage-audit.js:589-644 (post_baseline은 `live.reviews.records` 전건을 파싱해 집계 — 착지 시점 필터 없음) vs plan:16 ('라이브 파티션(HEAD, 레코드 68건)도 0이다'), plan:136-138, plan Validation:274-276, Acceptance:313 |
| architect | MEDIUM | '판독 불가·모듈 부재는 null이고 절대 0이 아니다'라는 미러 주장이 실제 지배적 실패 케이스를 덮지 못한다. `write.js`의 null-not-zero 규율은 `count()`가 **throw**했을 때만 발동하는데, 원장 파일 부재(= 이 실행이 원장에 등록된 적 없음 · 키 어긋남)는 throw가 아니라 깨끗한 정수 0으로 접힌다. 따라서 '측정된 0회'와 '애초에 세어진 적 없음'이 measurement에서 구분되지 않고, DD5의 `not_applicable` 자격(rounds===0 ∧ pre-dispatch halt)도 그 구분 불가능한 0 위에 선다. | ledger.js:187-198 (`readFileSync` 실패 → `raw=null` → `emptyState` → count 0; throw는 오직 손상 JSON) vs plan Task 2:180-185 및 Patterns:57 (`write.js:59-96` null-not-zero를 미러라고 주장) |
| architect | MEDIUM | DD3이 봉인 대조를 'gate 일치 + decision 불일치'로만 좁혔으나, 원장을 실제로 쓰는 주체는 `--slug`가 아니라 봉인이 정한 키다. 즉 리더의 주 키와 라이터의 키가 서로 다른 소유자를 갖는 이중 소유 상태이고, 어긋남은 degradation 한 줄로 표면화되지만 값은 조용히 0이 되어 D1 판정을 뒤집는다. '값의 원천이 같은 원장'이라는 Summary의 주장은 slug == 봉인 decisionId일 때만 참이다. | cli.js:286-292 및 :328,:342-343,:348-356 (`resolveRoundBudget`/`recordPanelRound`가 `state.gateId`/`state.decisionId` — 봉인 유래 — 로만 기록) vs plan DD2:103-108 · DD3:116-121 · Summary:11-13 |
| security | HIGH | DD5/R5의 핵심 전제가 거짓이다 — `not_applicable` 자격의 두 '기계 사실' 중 `halt_stage`는 봉인값이 아니라 검증 없는 CLI 자기신고다. 저자(또는 명령 본문)가 `--halt-stage 5.2b`를 넘기고 원장이 비어 rounds===0이면 그 레코드는 `not_applicable`이 되어 `linkage-audit --check-round-structure`의 `absent` 집계와 비영점 종료를 합법적으로 빠져나간다. DD4가 '진짜로 막는' 유일한 강제 지점이 caller-controlled 문자열 하나로 면제된다. | plan:148-151 '`not_applicable`은 … `halt_stage`가 `PRE_DISPATCH_HALT_STAGES`에 속할 때. 둘 다 봉인된 값이고 저자가 쓰는 값이 아니다' vs plugins/mccp/scripts/lib/plan-review/cli.js:1211 `haltStage: (args['halt-stage'] && args['halt-stage'] !== true) ? args['halt-stage'] : null` (임의 문자열, enum 검증 0) + record.js:266-267 (trim만 함). DD2가 `--rounds` 플래그를 거부한 근거('측정을 자기신고로 바꾼다', plan:106-108)가 이 축에서는 이미 열려 있다. |
| security | MEDIUM | DD3이 '봉인이 판독 가능하고 gate 일치인데 decision만 다를 때'로 degradation을 좁혀, 상류가 이 축에서 명시적으로 봉인하는 '봉인 부재 = 이 실행은 등록된 적 없고 강제가 돌지 않았으므로 옆의 count는 authoritative하지 않다'는 페어링을 레코드 층에서 잃는다. 봉인 부재/만료/판독불가 상태에서 임의 `--slug` 원장의 수가 아무 표시 없이 git-tracked 레코드에 봉인된다. | plan:116-121 (봉인 mismatch만 표면화) + DD6 plan:160-164 (`rounds_source`류 보조 필드 거부) vs plugins/mccp/scripts/receipt/write.js:52-58 'no usable seal means this run was never enrolled, which means enforcement did not run, which means the count beside it is not authoritative. That pairing is how a degraded run stays auditable in the receipt instead of only in stderr' — write.js는 `meta.round_cap: null`로 그 페어링을 남긴다. |
| security | LOW | 레코드 파일 정체성과 원장 키가 서로 다른 정규화를 쓴다 — 파일 경로는 `sanitizeSlug`(다대일 축약)를, Task 4의 원장 조회는 raw `args.slug`를 쓴다. 두 다른 raw slug가 같은 레코드 파일로 접히면서 서로 다른 원장을 읽을 수 있고, DD3의 대조는 봉인 decisionId 대 raw slug만 비교하므로 이 어긋남을 보지 못한다. | plugins/mccp/scripts/lib/plan-review/cli.js:1191 (`const slug = args.slug …` raw) 와 :1230 `reviewRecordPath(slug)` → record.js:77-85 `sanitizeSlug`(`[^A-Za-z0-9._-]`→`-`, `-{2,}`→`-`, 120자 절단) vs review-rounds/ledger.js:75-81 `assertKeyComponent`(SLUG_RE 원문 대조). plan Task 4는 두 값을 같은 것으로 다룬다(plan:198-203). |
| test | HIGH | Acceptance criterion '--check-round-structure가 exit 0' is unachievable as specified, so the plan's headline acceptance test can only pass by deviating from the plan. Task 5 defines the check as nonzero when the LIVE partition contains ≥1 `absent`; DD5 states past records lack the `rounds` key entirely and are therefore `absent`; the plan itself measures the live partition (HEAD, 68 records) at 0 present; and UI1 forbids retrofitting. Task 8 adds exactly one new record, leaving ~68 `absent` records → the check stays nonzero forever. | plan L214 '라이브 파티션에 `absent`가 1건 이상이면 비영점으로 끝나고' vs plan L313 acceptance '`--check-round-structure`가 exit 0이다'; plan L16 '라이브 파티션(HEAD, 레코드 68건)도 0이다'; plan L153-154 '과거 레코드에는 `rounds` 키 자체가 없어 전건 `absent`'; live partition = linkage-audit.js:645 `result.post_baseline` (working-tree/HEAD corpus, linkage-audit.js:42,574) |
| test | HIGH | Task 8, the sole 'real live value' acceptance (UI4), cannot produce `rounds ≥ 1` under the plan's own design. The ledger is keyed `<gateId>__<decisionId>` where decisionId is the `--slug` (DD2 injects `decisionId: slug`), but Task 8 deliberately runs with a DIFFERENT slug (`review-record-linkage-m4-postimpl`). No ledger file exists for that key, so `ledger.count` returns 0 → measurement.rounds = 0 → `classifyRoundStructure` = `absent` (halt_stage is null, not a PRE_DISPATCH stage) → degradation, acceptance L312 fails. The very risk-mitigation slug split (R1) breaks the acceptance test. | plan L241 '--slug review-record-linkage-m4-postimpl'; plan L199 `count({ gateId:'mccp-plan-codex', decisionId: slug, ... })`; ledger key construction plugins/mccp/scripts/lib/review-rounds/ledger.js:106-121 `ledgerBasename(gateId, decisionId)`; missing file → count 0 at ledger.js:192-198 (read catch → emptyState → rounds [] → 0); plan L312 acceptance 'rounds가 정수 ≥ 1' |
| test | HIGH | Task 4's Validate assertion ('원장 부재 시 `null`') contradicts the module it delegates to and, if implemented literally, would conflate 'ledger absent' with 'unreadable'. `ledger.count` does NOT throw for a missing ledger — it returns 0. Task 2's mapping only yields `null` for injection absence or `{available:false}` (which Task 4 only sets on a caught failure). So the specified test either cannot pass, or forces a special case that erases the plan's own 0-vs-null distinction (a missing ledger + pre-dispatch halt_stage would then be mislabeled `not_applicable` per DD5). | plan L207 'Validate: … 원장 부재 시 `null`인지' vs plugins/mccp/scripts/lib/review-rounds/ledger.js:187-198 (missing file → raw=null → emptyState → count 0, no throw); plan L184-185 maps `{available:true,count:0}` → `rounds` 0; plan L148-149 'not_applicable은 … `rounds === 0` 이고 `halt_stage`가 PRE_DISPATCH_HALT_STAGES에 속할 때' |
| test | MEDIUM | Task 7(a)'s static test does not test the failure it claims to guard. It asserts plan.md's 5.-1 seal and 5.2h record use the same `derive-decision` derivation, saying divergence would make DD3's key mismatch permanent — but the actual, plan-acknowledged divergence in this cycle comes from an explicit `--slug` override (R1 mitigation), which the static assertion cannot observe. The over-permissive direction (body identical, runtime slugs diverged) passes the test. | plan L230-232 'plan.md의 5.-1 seal과 5.2h record가 같은 derive-decision 파생을 쓴다는 단언을 더한다(둘이 갈라지면 DD3의 키 어긋남이 상시화된다)' vs plan L112-114 '저자가 슬러그를 명시 override하면(R1 완화가 요구하는 바로 그 행위) 둘이 갈라져' |
| test | LOW | Task 6's Validate is a manual, unrecorded mutation experiment rather than an executable check — nothing in the ## Validation block exercises it, so a future edit that weakens the key-set assertion leaves no failing command. | plan L227 'Validate: 키를 하나 지우면 test가 실패하는 것을 실제로 확인한다(일시 편집 후 되돌림)'; ## Validation block L258-283 contains no corresponding command |
| invariant | HIGH | DD5's `not_applicable` exemption rests on a caller-supplied CLI flag, not a sealed value — the plan's stated justification is factually false, and the exemption is a self-report skip predicate that voids the only enforcement (DD4's `--check-round-structure`). | Plan line 149: "둘 다 봉인된 값이고 저자가 쓰는 값이 아니다" (both are sealed values, not values the author writes). But `halt_stage` originates from a free CLI argument: `plugins/mccp/scripts/lib/plan-review/cli.js:1211` — `haltStage: (args['halt-stage'] && args['halt-stage'] !== true) ? args['halt-stage'] : null`, and `record.js:266-267` merely trims the string. Any shell caller running `cli.js record --slug X --halt-stage 5.2b` in a repo with no ledger gets rounds=0 + pre-dispatch stage → `not_applicable` → exempt from `absent` → `--check-round-structure` exits 0. This is the exact self-report channel DD2 (plan:103-108) refuses for `--rounds`. |
| invariant | HIGH | The claimed null-not-zero invariant does not hold for the dominant failure mode: an absent or unreadable ledger file yields count 0, not null, so "no measurement" is sealed as "measured zero rounds". | `ledger.js:187-198` — `read()` wraps `fs.readFileSync` in try/catch and sets `raw = null` on ANY read error (ENOENT, EACCES); `parseState(null,…)` returns `emptyState` with `rounds: []` (`:164-165`, `:153-159`), so `count()` returns 0. Only JSON corruption throws (`:170`). Plan Task 2 (lines 184-186) enumerates only `주입 부재 / {available:false} / {available:true,count:0} / {available:true,count:3}` and maps count 0 → `rounds: 0`, so no test distinguishes 'ledger absent' from 'zero rounds ran'. Plan line 181 asserts "판독 불가와 모듈 부재는 `null`이고 절대 0이 아니다" — false for the missing/unreadable-file path. |
| invariant | MEDIUM | DD3's key-divergence observation is blind on the exact branch it was written for: when the seal has been overwritten by a different gate (or is unreadable), no degradation is emitted at all, so the unverifiable-key case is silently indistinguishable from a verified match. | Plan lines 118-120 / Task 4 (lines 200-203): degradation is pushed only when `seal.readCap` gives `reason==='ok'` AND `gateId==='mccp-plan-codex'` AND `decisionId !== slug`. The plan itself states the seal is per-git-dir and "`/mccp:pr`이 나중에 `mccp-pr-codex`로 덮어쓰므로" (line 117) — i.e. the gate-mismatch case is expected, and in that case the record emits nothing while `rounds` may have come from an unrelated ledger key. R2 is listed as likelihood '높음' with this cycle itself in that condition (plan:290). |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | plan이 인용한 5개 지점을 직접 열어 대조했다: `linkage-defs.js`(hasRoundStructure가 정말 measurement.rounds 정수 ≥1인지, 헤더가 M4 write-time 소비를 예약했는지 — 둘 다 참), `write.js:59-96`(readRoundLedgerState의 null-not-zero 규율 — 참이나 throw 경로에 한정), `plan-review/cli.js:276-361`(원장 키의 실제 소유자 — 봉인이며 `--slug`가 아니다), `review-rounds/ledger.js`(파일 부재의 반환값 — 0), `linkage-audit.js:560-644`(라이브 파티션의 정의 — HEAD 트리 전건, 착지 시점 필터 없음). 모듈 경계 주장(record.js는 순수 유지, corpus.js 미개변, linkage-defs 불변)과 DD5의 동결 baseline 불변 논거는 공격했으나 반증하지 못했다 — 과거 레코드에 rounds 키가 없어 전건 absent라는 계산은 맞다. 깨진 것은 경계가 아니라 **키 소유권과 강제 범위**다. |
| security | fail | 공격한 것: (1) `--slug`/`--repo-root`를 통한 원장 경로 주입 — ledger.js:75-81의 SLUG_RE와 assertContained가 traversal을 닫고, `--repo-root`는 원장 읽기와 레코드 쓰기 위치를 동시에 옮기므로 키 분리 위조가 성립하지 않아 기각. (2) `.claude/reviews/` git-tracked 산출물로의 절대경로 유출(§3.12 cwd 선례 재개방) — Task 4가 원장 판독 실패를 stderr 한 줄로 접고 degradation에는 봉인 decision id만 싣는다고 명시해 statePath 포함 에러 문자열이 레코드로 흐르는 경로를 찾지 못함(record.js:416-421 degradation은 verbatim 기록되므로 구현이 이 약속을 어기면 재개방된다는 점만 남음). (3) receipt hash 표면 변경 — plan:84대로 write.js/schema.js 무변경이라 no-rehash 저촉 없음, 확인. (4) never-throw 우회로 계측이 게이트를 죽이는 경로 — ledger.count의 throw를 cmdRecord가 삼키고 exit 0을 유지하는 설계라 기각. 남은 것이 위 3건이고, 그중 첫 번째(halt_stage 자기신고)가 이 마일스톤의 유일한 강제 지점을 면제시키는 실질 우회다. |
| test | fail | Read the M4 plan and PRD in full, then checked each load-bearing claim against source: review-rounds/ledger.js (key model, missing-file → 0 not throw, count contract), receipt/write.js:59-96 readRoundLedgerState (the cited mirror), record.js:305-342 (measurement key set — Task 6's literal list does match today's keys, so no finding there), linkage-audit.js pre_baseline/post_baseline partitioning. Attacked: (1) whether Task 8's acceptance value is producible → it is not, slug/ledger key mismatch; (2) whether the exit-0 acceptance for --check-round-structure is reachable given 68 legacy live records and UI1 → it is not; (3) whether Task 4's null-on-absent assertion matches ledger behaviour → it does not; (4) whether Task 7's static assertion covers the divergence it names → it does not. I also checked the measurement key-set contract and the frozen-baseline byte-invariance validation and found no defect in those. |
| invariant | fail | Traced (1) the provenance of both conjuncts of DD5's `not_applicable` exemption — halt_stage proved to be a free `--halt-stage` CLI arg (cli.js:1211, record.js:266), refuting the plan's 'sealed value' claim and opening a self-report skip; (2) the null-vs-0 direction of the ledger read on unknown input — `ledger.read()` swallows fs errors to `raw=null` → `count()`=0 (ledger.js:187-198, 153-165), so absent/permission-denied ledgers seal as measured zero, and no Task-2 Validate case covers it; (3) DD3's seal cross-check branches, finding the gate-mismatch and unreadable-seal branches emit no signal; (4) whether `hasRoundStructure`/frozen-baseline claims hold — linkage-defs.js:54-59 is unchanged and past records lack `rounds`, so the 0/42 frozen invariant and R5 counter-evidence stand (no finding); (5) the receipt/no-rehash surface — plan touches no receipt schema, §3.12 not eroded (no finding). |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 360957,
  "halt_stage": null,
  "backlog_appended": 12,
  "backlog_skipped_nonblocking": 7,
  "granted": 4,
  "reviewed_plan_hash": "sha256:b39fca3d9a4045748b9f2692b334dc5f4ff1fb58db2647fc570af0dfdb2a73b4",
  "plan_path": ".claude/plans/review-record-linkage-m4.plan.md",
  "recorded_at": "2026-09-03T09:02:32.941Z"
}
```
