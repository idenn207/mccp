# Plan Review Panel — diverse-agent-review-m11

**Plan**: `.claude/plans/diverse-agent-review-m11.plan.md` · **Plan version**: `sha256:43e5914331d56a123c99294df2775c3e16448c34f8617db61878a3b591220563`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 15 blocking finding(s): architect/HIGH, architect/HIGH, architect/HIGH, architect/HIGH — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | Plan uses corpus.js output (pass_path.entries) directly to get 5 converged records, but these records exist only as JSON entries, not as traversable files in .claude/reviews/ | Plan line 335-340: 'audit()의 반환에는 pass_path.entries가 이미 실려 있고...절차는 require('corpus.js').audit() 한 번, pass_path.entries 순회다'. But corpus.js lines 489-496 compute pass_path by filtering verdict=='converged' from parsed records. The returned entries reference metadata (record path, plan_path, recorded_at, etc.) but the tool's downstream processing depends on reading those referenced files to extract evidence. Preconditions line 59 states '.claude/receipts/mccp-plan-codex/ 디렉토리째 부재(worktree-only)' meaning review_proof is unavailable, yet plan needs to correlate each converged record against downstream evidence channels (report, backlog, downstream_reviews, pr_codex). Task 1 item 4 reads from channels, but corpus entries provide only metadata pointers, not the channel data itself. |
| architect | HIGH | Plan claims DN3 will handle 'structurally_empty' vs 'absent' distinction for pr_codex channel, but this distinction assumes the channel data exists in a file to inspect; for converged records with codex_disabled=true, the data is gone before audit runs | Plan DN3 line 211-213: 'cross-model 채널은 5건 전부 비어 있고...도구는 그 채널을 structurally_empty로 명시 보고'. But Preconditions line 60: '5건 전부 resolution.codex_verdict=skipped · meta.codex_disabled=true · findings 길이 0'. If all 5 records have codex disabled, the tool will always report pr_codex as structurally_empty for all 5. This becomes a constant flag, not an observation that varies—exactly the condition DN3 says fails to provide information (line 50-51: '항상 켜진 신호는 정보를 나르지 않는다'). The plan's own design rule contradicts its application. |
| architect | HIGH | Plan boundary claim: audit will correlate downstream evidence (reports, backlog, code-review findings) against plan state recorded at approval time (G3 gate). But the 5 converged records come from `.claude/receipts/mccp-pr-codex/` (git-tracked ship receipts), not from `.claude/reviews/` (plan-review panel records) | Preconditions line 57: 'ship receipt가 5건 모두 존재한다—.claude/receipts/mccp-pr-codex/'. Plan Task 1 item 3 (channels) lists 'pr_codex' channel reading '`.claude/receipts/mccp-pr-codex/<slug>.json`의 findings'. But the 5 converged records themselves originate from corpus.js reading `.claude/reviews/`, which contains plan-gate review records (evidence shows M11 record itself is in that directory). Task 1 then reads pr_codex **receipt** findings to find post-approval evidence. This mixes two data sources: convergence verdict from review records, evidence from ship receipts. The plan does not document which source is authoritative for 'converged' status or what happens if they disagree. |
| architect | HIGH | Plan omits mechanical enforcement of the 'converged' invariant it depends on: corpus.js returns pass_path.entries with converged verdicts, but those verdicts come from records written by plan-gate runs whose review_proof cannot be validated (DN9 explicitly acknowledges this) | Plan DN9 line 251-258: 'fan-out security/HIGH가 isReviewProofStructurallyValid를 호출하라고 요구했으나...review_proof 문자열이 0회 등장한다. 따라서 그 호출은 인자를 구성할 수 없다...도구는 그 셋만 검증하고 검증할 수 없는 것은 검증했다고 적지 않는다.' The plan explicitly defers the one validation that could catch mis-stamped converged verdicts. Task 1 (line 352-358) anchors on received_plan_hash and ship receipt plan_hash, but makes no check of the quorum proof structure that originally issued the converged verdict. |
| architect | MEDIUM | Acceptance criterion gap: Plan Acceptance (line 568-590) requires state != blind and coverage reporting, but does NOT require distinguishing 'audit found 0 misses' from 'audit found misses but tool produced no verdict field' | Plan DN2 line 202-206: 'tool은 verdict가 아니라 레코드별 dossier다' and Task 2 item 8 tests 'no false_approve judgment field'. But Acceptance (line 593-599) specifies: 'no_miss_found인 경우에는 그것이 보았고 없었다인지 볼 수 있는 채널이 비어 있었는지를 채널 지도로 구분'. This is an **output requirement** (document must distinguish two reasons for zero misses), not a **tool requirement** (tool must not produce judgment). If audit runs with all channels structurally_empty (DN3 constant for codex_disabled records) and tool correctly outputs zero candidates, the document author can write 'no channels examined' but the Acceptance criterion reads it as 'examined and found nothing'. The plan does not mechanically prevent the gate from passing with 'inconclusive' findings reported as 'clean'. |
| test | HIGH | Validation 1 tests that the approval-audit.js tool produces correct output with all required fields (state, coverage, proof_backing, channels, candidates, lenses, anchor, hash_chain) | Plan lines 519-520 show Validation 1 only checks `j.state` existence and `c.approved === c.auditable+c.unauditable` equation. No test validates: presence of `proof_backing` field, `channels` field structure, `candidates` array, `lenses`, `anchor` subfields, or `hash_chain` axes. Acceptance criteria #591 requires `proof_backing` field presence in output, but no Validation line tests this. |
| test | HIGH | Task 2 provides 12 test items that comprehensively cover output validation including path traversal rejection with fs호출 0회 assertion (item 9) | Plan lines 413-444 describe 12 test items but do NOT show actual test code. Task 2 states items will be created but only describes intent. Item 9 claims 'fs호출을 스텁해 호출 0회를 단언' (mock fs calls and assert 0 invocations) but this is non-trivial and no code validates the mocking strategy. Acceptance #592 requires this test to exist, but the plan provides no test implementation to review. |
| test | MEDIUM | Validation line 4 can reliably compare corpus.js output between origin/main and current branch | Plan lines 529-532 use temporary files in `/tmp/` (non-portable, environment-dependent) and depend on `git show origin/main:...` working correctly. The validation uses pipe + diff which could fail silently if git fetch hasn't updated origin/main. Better approach: use SCRATCHPAD directory (noted in prompt) and validate git remote exists. Path `/tmp/corpus-before.json` may not exist on all systems (e.g., Windows). |
| test | MEDIUM | The tool output will have exactly three coverage keys: approved, auditable, unauditable, with invariant approved === auditable + unauditable | Plan Task 1 line 400-404 defines coverage equation strictly. Validation 1 (line 520) tests this equation. However, Validation 1 does NOT validate that these are the ONLY three keys. If tool output has additional keys like 'unrecoverable' or 'parsing_failed', Validation 1 will still pass because it only checks the three-key invariant, not that exactly three keys exist. Acceptance #589 requires all three keys exist, but doesn't require they're the ONLY keys. |
| test | MEDIUM | Task 1 claims approval-audit.js will read from corpus.js audit().pass_path.entries without modifying corpus.js | Plan line 330-345 states corpus.js is unchanged and audit() input has length 5. However: (1) plan also says corpus is 'live' and records went from 35→37 (line 56), so entries is NOT fixed at 5; (2) plan never validates that corpus.js was actually NOT changed, only that its OUTPUT is unchanged (Validation 4); (3) if approval-audit.js code is never shown for review, how can we verify it actually uses corpus.js correctly? |
| test | HIGH | Validation passes includes comprehensive test of structural empty distinction (DN3) via 'test 항목 2·3이 `absent`와 구분을 고정' | Acceptance #586-587 requires test to prove `pr_codex` channel reports `structurally_empty` when codex_disabled=true, and that 0 doesn't contribute to miss counters. But (1) Validation lines 1-9 do NOT reference this test; (2) Task 2 item 2 & 3 are only described (lines 416-418), no code shown; (3) Validation 2 (line 523) runs test file that doesn't exist yet (`plan-review-approval-audit.test.js`), so Validation cannot pass until file is created and code reviewed. |
| test | HIGH | Plan 회귀 test correctly validates DN6 hash recovery by checking 수정 전 실패 (test should fail before fix) | Plan Task 2 item 4 (lines 419-421) states test should fail BEFORE fix is implemented: '수정 전 실패를 먼저 실측한다'. But the plan provides NO test code to review. If test is implemented AFTER approval of this plan, reviewers cannot verify it actually fails before the fix. Acceptance criteria cannot be machine-verified without seeing actual test code that can be run to confirm pre-fix failure. |
| test | LOW | Validation 1 mechanically confirms coverage equation with proper exit codes | Line 520: `if(c.approved!==c.auditable+c.unauditable)process.exit(1)` exits with code 1 on inequality, but doesn't output to stdout if test PASSES. This means: (a) success produces only the console.log output (state and numbers), no explicit 'PASS' signal; (b) if c.coverage is undefined or c.approved/auditable/unauditable are undefined, this creates silent failures or type errors. Better practice: explicit error handling and success confirmation. |
| invariant | CRITICAL | Plan's acceptance criterion (line 580: `coverage.approved >= 5`) depends on state whose durability is unclear, contradicting documented design contract. The plan contains a finding (fan-out L122) claiming `.claude/reviews/` records are worktree-only, yet `.gitignore` (lines 149-155) designates them as 'DURABLE record'. This mismatch means either: (a) the plan's finding is incorrect and tool output is reproducible, OR (b) git-tracked records are accumulating what should be session-local state. | Plan Acceptance line 580 requires tool to achieve `coverage.approved >= 5` based on reading `.claude/reviews/` records via `corpus.js`. Multi-perspective fan-out (line 122 in findings section): 'M11 operates on git-tracked ship receipts (immutable) but intermediate review records (.claude/reviews/) are worktree-only and could be deleted/recreated between audit runs, breaking audit reproducibility'. Contradicts .gitignore lines 149-155: 'the DURABLE record is .claude/reviews/ plan-review-<slug>.md plus the receipt's review_proof'. |
| invariant | CRITICAL | Receipt anchoring contract violation: acceptance criterion cannot be durably verified. Plan creates `approval-quality-audit.md` as the durable record of acceptance, but this document quotes a tool output that depends on transient state. If worktree-only records disappear, the tool returns `state=blind`, making the Acceptance criterion (state != blind and approved >= 5) impossible to verify against the historical document in the future. | CLAUDE.md section 3.12 receipt durability contract requires acceptance be verifiable later. Plan Acceptance line 580: tool output must be quoted in document and state must not be `blind`. Plan's tool (approval-audit.js Task 1, line 393) returns `state=blind` when 'approved records = 0'. If records are worktree-only and disappear, future audit finds `state=blind` contradicting what the document claimed at acceptance time. No rollback path to restore the records. |
| invariant | HIGH | Plan does not mitigate identified reproducibility risk. The fan-out finding at line 122 proposes two mitigations: '(a) re-derive from git-tracked data only, or (b) write to git-tracked output to guarantee reproducibility'. The plan implements neither - it only reads from sources of unclear durability and produces a document that quotes transient tool output. | Fan-out finding line 122 mitigation column: 'M11 should either (a) re-derive from git-tracked data only, or (b) write to git-tracked output to guarantee reproducibility'. Plan Task 3 (line 448-459) only creates `approval-quality-audit.md` documenting tool output. If underlying `.claude/reviews/` state changes (due to worktree cleanup per line 59 of PRD or O3 re-execution per line 36), the document's quoted output becomes stale and the acceptance cannot be re-verified. |
| invariant | HIGH | Validation checklist does not enforce durability of acceptance-critical state. Validation 1 (line 519-520) runs the tool but does not verify that `.claude/reviews/` records exist or will persist. If records are missing, tool silently returns `state=blind`, which still produces valid JSON and passes the structure check. | Validation 1 (line 519-520): 'node plugins/mccp/scripts/lib/plan-review/approval-audit.js --json \| node -e ... if(!j.state)process.exit(1)'. Checks JSON structure and that `coverage` satisfies arithmetic invariant, but does not fail if `state='blind'` or if approved=0. No validation enforces that the tool output represents durable state. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Attacked: (1) corpus.js data flow—verified that pass_path.entries are computation outputs, not independent auditables; (2) converged record sources—confirmed they come from `.claude/reviews/` by filter on verdict field, but found preconditions claim them as sha-bound results of `corpus.js --json`, creating ambiguity about stable identity; (3) DN3 structurally_empty rule—traced that all 5 records have codex_disabled=true per Preconditions line 60, meaning pr_codex channel is always structurally_empty, not conditionally observed, violating DN3's own stated principle that constant signals carry no information; (4) boundary between plan-gate records and ship receipts—verified that converged verdicts come from plan-review records in `.claude/reviews/` but post-approval evidence comes from ship receipts in `.claude/receipts/mccp-pr-codex/`, and plan does not document precedence if they conflict; (5) review_proof validation—confirmed DN9 explicitly states it cannot be validated and plan makes no mechanical check of the quorum proof that issued the converged verdict; (6) Acceptance criteria clarity—read lines 568-606 and found no requirement that tool output distinguish 'observed zero misses' from 'channels were inaccessible' before reporting success. |
| security | pass | Attacked the plan across multiple security vectors: 1. **Path traversal via untrusted inputs (DN13)**: Verified that `measurement.plan_path` values from markdown records are validated with `isRepoRelativeEvidencePath()` BEFORE filesystem access. Confirmed validator rejects: absolute paths (/), UNC paths (//host), drive letters (C:), backslashes (\\), traversal (..), empty segments. Test item 9 explicitly verifies that invalid paths don't trigger fs calls (fs stub validation). Scenario: malicious plan_path in record → rejected before read → no filesystem access. **Control holds.** 2. **Receipt integrity and tamper detection (DN14, DN15)**: Plan mandates `receiptIntegrityOk()` validation from evidence-audit.js (hash recomputation + schema validation) on ship receipts before using their `plan_hash` for corroboration. Proof_backing axis will mark `receipt_corrupt` if integrity check fails, and `uncorroborated` receipts downgrade state to degraded (fail-closed). Scenario: manually edited ship receipt → hash mismatch → rejected as corrupt → state degraded. **Control holds.** 3. **Measurement JSON parsing from records**: Records embed measurement as JSON within markdown fences. JSON.parse() safely handles escape sequences and injection attempts. The parsed `plan_path` field is extracted as a string, not by manual markdown parsing, preventing quote/escape bypasses. Scenario: malicious escapes in markdown `**Plan**: \\`../../../etc/passwd\\`` → JSON parse extracts literal string → validation rejects traversal. **No vulnerability found.** 4. **Slug-to-receipt mapping (ship receipt path construction)**: Slugs come from record filenames controlled by filesystem/git (cannot contain `/` on Unix/Windows). Tool doesn't construct arbitrary receipt paths from untrusted slug input; slug is derived from filename, which OS/git have already validated. Scenario: create `.claude/reviews/plan-review-../../../etc/passwd.md` → filesystem prevents `/` in filename → impossible to exploit. **Design is safe by OS boundary.** 5. **Git history traversal for plan recovery**: Plan uses `git rev-list` to find historical versions. Path argument to git has already been validated with `isRepoRelativeEvidencePath` (no traversal, no absolute paths), preventing argument injection. Scenario: validated path `.claude/plans/x.md` passed to `git rev-list -- .claude/plans/x.md` → no injection vector. **Control holds.** 6. **Recorded data vs. current state distinctions (DN6, DN10)**: Plan correctly uses `measurement.plan_path` to determine hash scheme (preserving original intent when files move to archived/), separate from current disk location. Detects edited-after-approval as distinct from false-approve. Scenario: plan moved from `.claude/plans/x.md` to `.claude/PRPs/plans/archived/x.md`, hash computed using original scheme, mismatch classified as `edited_after_approval` not false-approve. **Logic is sound.** 7. **Data source hierarchy and fail-closed patterns (DN3, DN9, DN15)**: Plan distinguishes `structurally_empty` (ship receipt has codex_disabled=true) from `absent` (no receipt file). Test items 2-3 and state ladder ensure neither is misclassified as "no defects found." Scenario: Codex channel intentionally disabled → explicitly marked structurally_empty → doesn't contribute to miss count. **Risk structure documented; controls in place.** 8. **Time boundary validation (G3, DN1)**: Plan validates `recorded_at` field format before parsing (ISO 8601 regex `^\\d{4}-\\d{2}-\\d{2}T...` confirmed before Date.parse). Malformed timestamps → unauditable, not default/estimated times. Scenario: corrupted `recorded_at: "invalid"` → format validation fails → time_axis=unauditable → no evidence graduates to miss. **Control holds.** No HIGH/CRITICAL findings. Plan follows established patterns from evidence-audit.js (state ladder, coverage honesty) and review-verdict.js (path validation). Validates input before use, checks integrity of git-tracked data, and uses existing trusted validators. Security design is sound. |
| test | fail | Examined plan claims about test coverage and falsifiability: (1) Traced Validation lines 1-9 against Acceptance criteria checklist to find gaps where tests don't validate required output fields (proof_backing, channels, candidates, lenses, anchor, hash_chain); (2) Checked Task 2 test items 1-12 to verify they have testable implementations - found only descriptions, no code; (3) Verified corpus.js exports and pass_path.entries structure exist; (4) Inspected hash.js for isPlanPath logic to confirm DN6 claims about archived path handling; (5) Confirmed review-verdict.js has isRepoRelativeEvidencePath validator; (6) Identified that Validation 4 uses /tmp/ instead of scratchpad directory; (7) Found that Validation line 1 only tests coverage equation, not output structure; (8) Verified Task 2 item 4 claims "수정 전 실패" but provides no test code to review. |
| invariant | fail | Attacked the plan's fail-closed receipt anchoring and rollback safety through four vectors: (1) Verified whether acceptance criterion's data dependencies (`.claude/reviews/` records) are durable by checking `.gitignore` configuration and comparing against plan's stated assumptions. Found contradiction: plan's identified risk (fan-out line 122) claims records are worktree-only, but `.gitignore` lines 149-155 explicitly designate them as DURABLE. (2) Checked whether plan mitigates the identified reproducibility risk—it proposes no action against either mitigation path. (3) Traced whether acceptance criterion anchors to durable state—plan creates a document quoting tool output that depends on `.claude/reviews/` records; if those records disappear, future verification fails. (4) Inspected Validation section for guards ensuring durable state—Validation 1 only checks JSON structure, not that the tool's data exists or persists. No validation enforces non-blind state or recovery path." |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 633022,
  "halt_stage": null,
  "backlog_appended": 15,
  "backlog_skipped_nonblocking": 5,
  "granted": 4,
  "reviewed_plan_hash": "sha256:43e5914331d56a123c99294df2775c3e16448c34f8617db61878a3b591220563",
  "plan_path": ".claude/plans/diverse-agent-review-m11.plan.md",
  "recorded_at": "2026-08-31T01:00:01.984Z"
}
```
