# Plan Review Panel — multi-session-work-loop-m5

**Plan**: `.claude/plans/multi-session-work-loop-m5.plan.md` · **Plan version**: `sha256:e28a0806d80cec0838e95634fc045b2c3e0bba1e0e6f9fe713c38f1c7f31a295`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · malformed 0
**Layers**: L1 `converged` · L2 `divergent` · L3 not fired (`MCCP_PLAN_REVIEW=multi-agent`)
**Reason**: L2 quorum not satisfied: 7 blocking finding(s): test/CRITICAL, test/HIGH, test/HIGH, test/FAIL

> This is **round 10**. Rounds 1–9 are owned by
> [plan-review-multi-session-work-loop.md](plan-review-multi-session-work-loop.md) (the slug the earlier
> sessions used); this file carries the canonical `derive-decision` slug and every round from 10 on.
> The plan body was **frozen** for this round (`reviewed_plan_hash` bind, /mccp:plan 5.2 invariant ii),
> so no absorption was written into the plan — the record lives here only.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| test | CRITICAL | Task 0's Validate command can execute successfully without prerequisites | Line 329 Task 0 Validate: `node plugins/mccp/scripts/lib/state-journal/single-writer-lint.js --assertions --json` calls tool that line 202 shows Task 7 CREATE. Execution order unclear — if Task Validates run sequentially, this fails. |
| test | HIGH | Plan fixes the issue where Task 0 Validate reads non-existent files | Line 328 claims 'Task 0가 `m5-assertion-manifest.json`을 CREATE한다' fixing prior version where Task created nothing. But Task 0 Validate also requires `single-writer-lint.js` (created Task 7) and M5-specific test files (created Tasks 1-8) that don't exist at Task 0 run time. |
| test | HIGH | G3 test validates M5's STATE.md rendering is correct | Line 349 end: 'R8 지적대로 2·2b만으로는 `next_chunk` 같은 *선재 divergence* 필드의 정확성까지 답하지 못하며, 그 축은 3의 pin이 "격차가 변하지 않았다"만 보증한다 — 정확성 판정은 **잔여**로 남는다'. Test only verifies both old and new paths produce same output; never asserts that output is actually correct. |
| test | MEDIUM | Three call sites of handoff-items functions actually use resolveHandoffRoot properly | Line 199 creates new `resolveHandoffRoot(ctx)` export. Lines 392-394 Task 8 table specifies calls should pass result. But plan never requires test to verify these sites actually CALL the function — only that they accept arguments. A no-op wrapper passes all lint checks. |
| test | MEDIUM | G1 guarantee is verified by test that catches degraded mode issues | Line 353 Task 3 Validate 6 claims 'stickiness는 3회 이상 호출로 단언'. But plan does not specify whether degraded flag (`.degraded` marker file) persists across node process boundaries. A test within single node process cannot verify file-based stickiness survives process restart. |
| invariant | HIGH | G5 guarantees A4 conversion to 'computed' status, but fulfillment is not mechanically blocked at ship time. Plan allows PR creation without confirmed verification. | §G5의 조건성 states verification requires deployment + new session + observation. But the plan does not explicitly state that `/mccp:pr` fails if Validation-SHIP-1/2 fail. The acceptance (L622-623) offers a manual checkbox between 'fulfilled' and 'unconfirmed', with no mechanical enforcement that prevents false claims. The plan says SHIP checks 'before PR creation' but uses advisory framing ('PR 생성 직전 1회') not blocking framing ('PR 생성이 차단된다'). Additionally, GROUND documents A4 producer is currently at 0 artifacts (L148: `*.handoff-items.json = 0건`), so even if verification code exists, the runtime effect is unproven until `claude plugin update` + session cycle. |
| invariant | HIGH | Validation-SHIP gate structure does not explicitly enforce fail-closed semantics for PR creation. | Validation-SHIP section (L513-517) separates SHIP checks from implement loop because they 'always fail in implement loop' and avoid 'soft gate learning'. But the plan never states that PR is mechanically blocked if SHIP-1 cache check fails or SHIP-2 inconclusive case occurs. Shell script (L520-527) shows exit code logic, but no `/mccp:pr` integration statement. Contrast with other fail-closed gates (e.g., receipt hash validation in durable-evidence-substrate M3) which explicitly block. The plan leaves SHIP-2 judgment at 3-state advisory reporting without confirming PR-gate integration. |
| invariant | MEDIUM | Degraded mode activates silently when journal append fails. User has no automatic notification unless they manually run 'journal verify'. | DD6.1 describes degraded mode as 'sticky' and 'reverts to direct write path'. The plan states (L253-262) that markers and stderr warnings exist, but these are after-the-fact diagnostics. During a degraded session, STATE.md is written directly without automatic user-visible signal. Only 'journal verify' or SessionStart marker detection (which happens at next session) surfaces the degraded state. This violates the invariant principle that fail-open paths must be loud. Plan acknowledges this as 잔여 5 (known limitation) but treats it as acceptable rather than a gate failure. |
| invariant | MEDIUM | STATE.md projection write uses advisory lock that can fail silently, creating window for concurrent writer inconsistency. | 잔여 4: 'degraded 구간의 중간 이력은 복원되지 않는다' and 'lock failure시 경고만 남기고 그대로 쓴다'. While journal append is atomic (O_APPEND) and recovery is possible through re-projection, two concurrent `update()` calls could proceed with lock failures, each reading different checkpoint states and writing different projections. The plan acknowledges this is advisory (L85: '락은 여전히 advisory다') and eventual consistency is achieved on next update, but no mechanical guarantee prevents the window. This is documented as acceptable, but represents fail-open drift in the lock layer. |
| invariant | MEDIUM | Known parser divergence on 'next_chunk' block-scalar field is documented but only pinned via test fixture, not resolved. Renders as different values for two independent readers. | L135: 'state-writer.readState → "line one\n…"' vs 'state-injector.readState → "|"'. Plan states (L135): '선재 · 잠복이며 M5 범위 밖이다' and 'G3의 단언을 "두 파서 상호 동등"이 아니라 "파서별 M5 전후 동등"으로 재정의하고, 이 divergence를 fixture로 pin한다'. G3 주의 (L33) explicitly states the two parsers are 'M5 전후 파싱 동등성' but not 'parserally mutual parity'. If M5 or later changes render the block-scalar format, the divergence could expand silently. Test fixture pinning only prevents growth, not resolution. No receipt hash explicitly covers `next_chunk`, so field could be mutable post-hash. |
| invariant | MEDIUM | Plan asserts CL-5 (cwd-relative path bug) is fixed in Task 8, but implementation depends on caller passing resolveHandoffRoot result correctly. If `projectRoot` is empty, fallback uses `discoverRepoRoot` walk-up which can skip if running outside repo, leaving wrong paths silently. | L398: 'projectRoot는 빈 문자열일 수 있다' and skip path exists. While plan adds 3-state judgment in SHIP-2 with '.handoff-root-unresolved' marker, this marker itself can fail to write (L405-406). Plan response is to emit msw-event as backup channel, but both channels being unwritten means SHIP-2 falls to 'inconclusive' case, which forbids marking G5 complete. However, audit trail is still incomplete—silent skip with no marker/event is possible if both writes fail. This is treated as 'handled by SHIP-3 state logic' but that logic can also fail to signal. |
| invariant | LOW | G1 guarantee ('no loss without loud surface') is weakened by implicit assumption that downstream tasks (completion-ledger reading, genesis seeding) succeed. If DD11 ledger read fails during bootstrap, lost tombstones are silent until next journal verify. | L301: 'ledger 읽기 실패면 loud warn + tombstone 0으로 진행한다(부트스트랩을 막으면 클론이 곧 파이프라인 정지가 된다)'. The plan explicitly says 'loud warn' but then chooses not to block bootstrap ('진행한다'). This creates asymmetry: bootstrap proceeds with incomplete security (G2 cannot guarantee replay defense without tombstones), but the loudness is a warning not a gate. G1 claims 'no silent loss' but this path loses replay defense silently at bootstrap time. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Examined G1/G3/G5 invariant boundaries; verified critical citations (A4 producer absence, CL-5 4th recurrence, parser divergence, mergeState reuse); traced journal-store↔project I/O/purity separation; reviewed degraded-mode stickiness semantics; confirmed State-consumer call-site sealing via lint; checked evidence-claim↔journal-seq axis separation. All major boundaries found explicitly defined with documented residual risks (잔여 4-9). Degraded mode violation of G1 is honestly claimed as intentional exclusion. No structural defect found that plan does not already acknowledge. |
| security | pass | Attacked trust boundaries and data handling across five axes: (1) Journal append/read atomicity — verified O_APPEND single-buffer model and malformed-line handling prevent interleaving and silent corruption. (2) State projection purity — confirmed pure reduce function with no I/O, mechanical enforcement via lint detecting `fs`/`child_process` imports. (3) CL-5 path traversal (4th recurrence) — verified all 3 call sites are fixed with explicit repoRoot passing, lint checks for `process.cwd()` literals and 1-hop aliases, runtime test enforces `cwd ≠ repoRoot`. Remaining gap (empty projectRoot → walk-up fail → skip) is documented and fail-closed via SHIP-2 3-state detection. (4) Completion-ledger seeding for genesis — acceptable under single-operator threat model, entries validated with corrupt-skip, single-user repo assumption holds. (5) State-injector parser divergence on `next_chunk` field — pre-existing, explicitly pinned by test, doesn't introduce new leakage (injector doesn't consume that field). All material risks acknowledged in plan §잔여 and L2 rounds 1-9; security reviewer approved by round 8. Cannot construct exploit path with documented design. |
| test | fail | I attacked the plan's core test-falsifiability claims by: (1) Tracing Task 0's Validate command to its tool dependency, finding it requires code from Task 7; (2) Checking whether the plan's claim to fix a previous circular-dependency issue actually holds by examining what Task 0 Validate requires; (3) Reading the G3 test design and finding explicit admission that correctness is NOT tested; (4) Verifying that CL-5 function call sites create a new export but the test doesn't verify they use it; (5) Reading the acceptance criteria for G1 degraded mode and finding process-boundary stickiness isn't actually tested. I examined existing code (handoff-items.js, session-start.js, session-end.js, state-writer.js) to verify CL-5 bug locations. I read the plan's Task/Files to Change matrix and execution model. I found no test files exist yet (expected), confirming this is indeed a plan.</anutationAttempted> |
| invariant | fail | Attacked G1 (no-loss guarantee) through degraded mode exception and bootstrap ledger failure paths. Attacked G5 (A4 computed conversion) through conditional deployment verification that lacks explicit PR-blocking semantics. Attacked gate fail-open drift by checking Validation-SHIP integration (plan says 'before PR creation' but never says 'PR blocked if fails'). Attacked receipt anchoring through parser divergence (two readers give different values, pinned only by test fixture, not resolved). Attacked rollback safety through advisory lock and skip markers that can both fail to write. Traced one full 'everything wrong' path: journal append fails → degraded marker write fails → SHIP-2 inconclusive case → no explicit PR block → accepts false G5 claim → A4 metrics unproven at ship time. Reviewed all 6 major guarantees (G1-G3, G4-G5) and 9 documented 잔여 (known limitations) for fail-closed vs fail-open gaps. |

---

# Round 11 — multi-session-work-loop-m5

**Plan version reviewed**: `sha256:105a68449b2bbc05fef9f0ae61b729faf5918bf8b7c05caac57bd00813c1f672`
**Panel outcome**: `divergent` — quorum 3 미충족 (pass: security)
**Layers**: L1 `converged` · L2 `divergent` · L3 not fired
**Coverage**: 4/4 · malformed 0 · spent 59538 tokens

> **이 verdict는 위 hash에만 유효하다.** 라운드 종료 후 architect CRITICAL(E1 — 세션 정체성
> 취득 경로 부재)을 흡수해 plan을 편집했으므로, `decide`가 DD13 bind로 `unavailable`을 반환했다
> (*plan changed after L2 read it*). **현재 plan 버전은 L2 검토를 받지 않은 상태**이며, 이는 은폐
> 대상이 아니라 게이트가 설계대로 작동한 결과다. plan §잔여 8이 이 상태를 명시한다.

> **패널은 여기서 종료됐다** — 11라운드 동안 3명 동시 pass가 없었고(최선 2), round 11의 기각
> 사유 다수가 *흡수 전 상태 참조* · *plan 미완독*이었다. 원인 축은 리뷰어 등급
> (`CLAUDE_CODE_SUBAGENT_MODEL: haiku` + `effort:low`)이며 diverse-agent-review PRD 소관이다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | CRITICAL | Plan specifies journal records must contain `session_id` and `prev_session_id` fields (Task 1, line 369) but does NOT specify the mechanism by which `update()` acquires these values to populate them | Task 1 (line 369) lists required record schema fields: `session_id` · `prev_session_id` · others. DD5 (line 249) states `update(repoRoot, patch)` signature is immutable to avoid changing 8 call sites. Plan contains zero explanation of where `update()` obtains session identity. Without this, G5 guarantee (line 29) that 'A4 분자가 경계 스코프로 파생' is architecturally impossible — `prev_session_id` cannot exist if producer doesn't receive it. |
| architect | CRITICAL | Guarantee G1 claims 'all state transformations append to journal without loss' (line 25) but the mechanism for populating load-bearing fields (`session_id`, `prev_session_id`) in journal records is undefined | Line 25 G1: '정상 모드의 모든 상태 변형이 손실 없이 append된다'. But without specified source for session identity in records, the journal cannot actually maintain the `prev_session_id` links that G2 (line 26: 닫힌 작업 단위는...저널이 유실된 뒤에도) depends on. The plan cites M3's evidence-claim.js as prerequisite (line 156-157) but that module provides claim_epoch (a UUID with no ordering), not the monotonic session identity needed for this design. |
| architect | HIGH | The plan identifies CL-5 (4th occurrence of cwd-relative path bug) in handoff-items calls but the proposed fix via `resolveHandoffRoot()` helper is incomplete — plan does not specify how that helper distinguishes between 'could not find repo root' vs 'repo root is legitimately missing' for the A4 calculation | Line 436 describes skip scenario: '`discoverRepoRoot`가 40단계 walk-up으로도 `.claude/`를 못 찾는다...repo 밖에서 hook이 돌았다는 뜻이고, 그곳에는 쓸 대상 자체가 없다'. Lines 440-441 describe marker + msw-event channels, and lines 599-605 (Validation-SHIP-2) show 3-state judgment logic. However, the plan does not mechanically specify how the journal writer will distinguish: (a) CL-5 bug still unfixed (marker present) vs (b) repo boundary crossed legitimately (marker absent but no session_end event). These two produce identical external state but different A4 judgments. |
| test | HIGH | G5: A4 numerator is computed from journal boundaries, and ship-time measurement confirms the computed transition | Plan §G5의 조건성 (L35-43) states explicitly that G5 verification requires 3 steps OUTSIDE plan execution: `plugin.json` bump → `claude plugin update` → new session boot. The plan says 'UI9: 코드 존재는 근거가 아니다' (code existence is not the basis). Validation §5 (L496-497) tests `derive/cli.js run --json` output, which measures post-deployment behavior, not pre-deployment code. Task 8 creates `a4-boundary-restore.test.js` which only tests the oracle logic, NOT the runtime hook firing. The plan explicitly states if the 3 runtime steps aren't done, G5 is 'underachieved' and `measurement-instrumentation.md` A4 status stays `forward-only`. **No unit test in the Validation section would catch if the runtime hook never fires at runtime.** |
| test | HIGH | CL-5 4th recurrence (cwd-relative path resolution) is fixed by passing explicit context to handoff-items functions in 3 locations | Files to Change (L214-215) specify UPDATE to `session-end.js` and `session-start.js` with fixes at 3 locations (L430-432). Detailed fixes: L430 `enumerateUnfinishedItems(process.cwd())` → `enumerateUnfinishedItems(repoRoot)`, L431 add `{ stateDir: ... }` option, L432 add options to `restoreAndMatch`. However, no CREATE is listed for a test that verifies these hook functions actually receive the correct repoRoot/stateDir context at runtime. The journal-store.test.js (L217) checks 'worktree path crossing = 0' but tests the journal layer's own path handling, not whether hooks pass correct arguments. **No unit test explicitly verifies the 3 hook call sites receive the corrected context.** |
| test | HIGH | G2: Closed work units don't resurrect via delayed/crashed session replay, enforced by tombstone prioritization and post-tombstone record exclusion from projection | Files to Change (L220) specifies `state-journal-replay.test.js` should test '크래시·재개 재생 시나리오 — 닫힌 작업 부활 불가' (crash-restart replay scenarios — closed units don't resurrect). The mechanism is described in DD3-DD4 and DD11: tombstones mark closed work units with `+∞` high-water, causing subsequent records to be classified `admit-post-tombstone`, which are excluded from projection (L300, L313). However, the plan's Validation section (L477) only says 'node --test plugins/mccp/scripts/lib/tests/state-journal-replay.test.js' without specifying the critical assertion: **that post-tombstone records are actually excluded from the projection and therefore don't change the projected state.** If the `project()` function has a bug and includes `admit-post-tombstone` records, this defect would not be caught by a test that lacks this explicit assertion. |
| test | MEDIUM | The assertion manifest file is created and used to verify all planned test assertions exist before implementation | Files to Change (L224) says `m5-assertion-manifest.json` should be CREATE with 'Task 0 단언 매니페스트 — 각 Task의 Validate 단언을 기계 판독 목록으로 고정. **Task 0의 Validate가 이 파일을 읽으므로 없으면 검증 자체가 실행 불가** (R8 test CRITICAL 흡수).' Task 0 Validate (L348-364) checks manifest self-consistency but explicitly states 'test 파일과의 대조는 여기서 하지 않는다' (doesn't verify against test files at Task 0). Validation §11 is referenced as the 'unique enforcement point'. But the plan text doesn't show where Validation §11 is defined or how it enforces manifest-to-test-file synchronization. **The critical enforcement mechanism (Task 7 lint in Validation §11) is not visible in the plan excerpts, making it impossible to verify this claim.** |
| invariant | CRITICAL | Plan lacks receipt from dual-review gate, creating unanchored approval window | L2 Review section (line 689-705): all 10 review rounds divergent, no receipt issued. Line 708 states 'receipt는 작성되지 않았다' (receipt not written). Line 77 declares plan received no L2 approval. CLAUDE.md §1.4 (gate-design.md) specifies receipt chain as mechanical enforcement. Without receipt, plan verification is unanchored. |
| invariant | CRITICAL | G5 metric verification gates are advisory, not mechanical, allowing unverified milestone ship | Line 89-95 (잔여 11): 'SHIP-1·SHIP-2는 비영점 exit로 신호할 뿐 `/mccp:pr`을 차단하지 않는다' — SHIP checks only signal but do not block PR gate. Line 551-555 (Validation-SHIP) admits SHIP checks run outside implement loop and before `/mccp:pr`, making them advisory. Acceptance (line 660-661) allows unmet G5 to ship with non-canonical PRD status, but this is documentary not mechanical. |
| invariant | CRITICAL | A4 metric may ship as 'computed' without confirmation when producer hasn't run in production | GROUND section (line 162-168): '`*.handoff-items.json` = 0건' (zero artifacts in production across main + 6 worktrees). Producer has never run. Two root causes: CL-5 path bug + computation contamination. G5 condition (line 39-43) requires `claude plugin update` + new session boot to verify, but this happens AFTER plan execution. If skipped, A4 status reverts to `forward-only` per line 43, but SHIP-2 can still output 'computed' if checker doesn't run. |
| invariant | HIGH | Plan claims no receipt, yet conditional on PR-Codex cross-review that may be disabled | Line 77 (잔여 8): 'cross-model 검증은 `/mccp:pr`의 PR-Codex가 담당한다 — **단 조건부다**: `MCCP_CODEX_DISABLED=1`이 user-level `~/.claude/settings.json`에 상시로 켜져 있고... 그 상태로 ship하면 PR-Codex는 발화하지 않고 `codex_verdict='skipped'`가 봉인된다.' When MCCP_CODEX_DISABLED=1, PR-Codex skips. No plan receipt + PR-Codex skip = zero dual-review enforcement. |
| invariant | HIGH | Degraded mode marker can fail to write, leaving undetectable corrupted journal state | DD6.1 (line 272-276): When journal append fails, `writeDegradedMarker` must be called. But line 284-285 shows `writeDegradedMarker` returns `{ok:false}` not throw. Line 290-292 shows next session's `journal verify` is 2nd line of defense. If marker write also fails AND next session never runs `journal verify`, corruption is undetected. Plan admits (line 278): 'R6 invariant CRITICAL 흡수' — marker failure path was unfixed in initial design. |
| invariant | HIGH | CL-5 path bug (4th recurrence) may be incompletely fixed, leaving one call site unpatched | Line 163-165 names 3 bug sites: session-end.js:382, session-start.js:748, enumerateUnfinishedItems(process.cwd()). Files to Change (214-215) updates session-end.js and session-start.js but line 213 says handoff-items.js exports `resolveHandoffRoot` without saying where it's called. L2 R10 D4 (line 782-783) escalates: 'Task 8이 새 export를 만들면서 호출부가 안 쓰는 경우를 (a)인자 전달·(b)`process.cwd()` 아님 어느 쪽도 못 잡는다.' Lint axis 3 checks for literal `enumerateUnfinishedItems(process.cwd())` but could miss it via alias (line 79-81). |
| invariant | HIGH | Task 0 circular dependency: manifest must exist before Task 0 Validate can read it | L2 R10 D1 CRITICAL (line 779-781): 'Task 0의 Validate가 `single-writer-lint.js --assertions`를 호출하는데 그 파일은 Task 7이 CREATE한다' — circular. Mitigation splits responsibility: Task 0 does manifest self-consistency (no external tools), Task 7 does `--assertions` lint. Acceptance (line 650) requires both Validate §10 lint exit 0 AND §11 absent=0. But if Task 0 Validate doesn't call §11's lint (because lint doesn't exist yet), who guarantees all test assertions are present before implementation? |
| invariant | MEDIUM | Plan cannot verify G5 completion within execution window, deferring to post-deployment measurement that user may skip | Line 37-44: G5 verification requires 3 post-execution steps (plugin.json bump, claude plugin update, new session). But accept criteria (line 660-661) allow two outcomes: G5 met (SHIP-1·SHIP-2 exit 0) OR G5 unmet (A4 stays `forward-only`). Line 43-44: 'G5는 그때 **미달**로 계상한다... 이 처리를 사후에 협상하지 않도록 여기서 미리 고정한다.' Unmet G5 ships with deliberate `forward-only` status, satisfying large-cohort constraint via documentation not verification. |
| invariant | MEDIUM | Single-writer lint cannot catch path violations via aliasing or indirect function calls | L2 R10 D4 (line 782-783) and L2 R7 A3 (line 734-735): lint axis 3 extended to '1-hop 지역 별칭' (1-hop local alias) but 'Task 7 axis 5 신설' (NEW axis 5) acknowledges limit: '임의 깊이·오바인딩을 **명시 잔여 9**로 등재' (arbitrary depth/rebinding in residual 9). Plan cannot mechanically guarantee all writers use `resolveHandoffRoot`. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Searched for specification of: (1) how `update()` function acquires session_id/prev_session_id despite invariant signature; (2) where records get session identity fields; (3) source of monotonic session ordering (found only M3's UUID epoch which has no ordering); (4) handoff-items.js CL-5 fix completeness and how a4-boundary-restore.js will detect producer success vs repo-boundary legitimacy. Verified handoff-items.js calls at lines 382, 748 confirming path bug (cwd-relative instead of repo-root). Confirmed plan claims record schema (Task 1:369) and A4 derivation (line 423) both depend on prev_session_id but plan nowhere specifies how that field is populated. This is a boundary specification gap, not a validation issue. |
| security | pass | Attacked: (1) CL-5 path resolution recurrence — verified fixes use existing `repoRoot` context already present in both hooks, no new path handling introduced. (2) Advisory locks enabling race conditions — documented as residual 4 with acknowledged last-writer-wins. (3) Degraded mode state divergence — documented as residual 5 with explicit marker and recovery contract. (4) Static lint coverage gaps — documented as residual 9 with CLI-based enforcement layer separate from tests. (5) Cryptographic integrity — documented as residual 6, single-operator threat model acknowledged. (6) Journal schema injection — design uses allowlist + admission enum filtering, limits attack surface. (7) Recovery toggle misconfiguration — contract explicit (lines 323-330), marker takes precedence (line 328), journal verify catches divergence (line 407). (8) Path leakage in working-tree journal — plan specifies repo-root relative paths, working-tree only, gitignore addition confirmed. Found no undocumented trust boundary violations, data leakage paths, or attack surfaces. All major residual risks explicitly listed and mitigated. |
| test | fail | Attacked: (1) G5 and runtime measurement falsifiability by reading §G5의 조건성 (L35-43) and UI9 definition; (2) CL-5 fix coverage by matching Files to Change (L214-215, L430-432) against test file listings (L217-220) and finding no explicit hook-context verification test; (3) G2 replay defense oracle by reading DD3-DD4-DD11 (mechanism design) and cross-checking with Validation section assertions for explicit post-tombstone exclusion verification; (4) assertion manifest enforcement by following references from L224 through Task 0 and Validation sections. All searches performed on complete plan text with Grep context queries. |
| invariant | fail | Attacked: (1) receipt anchoring — no plan receipt exists, L2 rounds all divergent, PR-Codex optionally skipped (2) fail-open gates — SHIP-1/2 advisory, VALIDATION-SHIP outside implement loop, G5 unmet paths ship (3) skip predicates — A4 metric unverified but can ship with `computed` status if SHIP-2 ignored (4) degraded mode — marker write failure path partially defended by verify 2nd line only (5) path resolution bug CL-5 — claimed 3-site fix but only 2 explicit in Files to Change, lint cannot detect aliases (6) circular task dependency — Task 0 needs manifest that Task 0 creates, test file existence guaranteed only after Task 7 (7) G5 verification window — requires post-deployment steps outside plan execution, defaults to `forward-only` if skipped (8) integrity of metric producer — handoff-items producer in production=0, depends on cache update that user must perform. |
