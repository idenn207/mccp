# Plan Review Panel — ci-full-suite-m2

**Plan**: `.claude/plans/ci-full-suite-m2.plan.md` · **Plan version**: `sha256:8a930d3536f01a47b3b8457c3c64a07a486dda2e6182b135448d8202060af7fe`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 12 blocking finding(s): architect/HIGH, architect/FAIL, security/HIGH, security/FAIL — MCCP_REVIEW_SINGLE_PASS=deadline_pressure 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | Task 1의 갈래 H 격리 기제(childEnv에 MCCP_ROUND_LEDGER=observe 주입)는 구조적으로 불변식을 담지 못한다. seal.js의 판정은 **봉인 우선 · env fallback**이라, 봉인이 존재하면(=Task 1 Validate가 요구하는 바로 그 상태) 자식 env의 observe는 판정에 도달하지 않는다. 즉 Task 1의 Action 2와 Task 1의 Validate는 서로 만족 불가다. 더 나아가 plan이 예비한 퇴로('도달하지 않으면 그 seam이 결함이고 그것을 수리 대상으로 삼는다')는 seal.js가 명시적으로 M3이 막으려는 실패 계열이라고 적어 둔 우회를 test 편의를 위해 도입하자는 것이다 — 격리 경계는 env가 아니라 gitDir/cwd에 있고(codex-invoke.js:227-229,296-299가 gitDir로 봉인을 읽는다), plan의 Files to Change에는 그 경계를 다루는 항목이 없다. | plugins/mccp/scripts/lib/review-rounds/seal.js:208-213 '**모드는 봉인 우선이고 env는 fallback이다** … 반대로 env 우선이면 실행 중 `observe`를 심는 것이 곧 조용한 우회이고, 그것이 M3이 막으려는 실패 계열 자체다'; :241-244 canEnforce = r.mode === 'enforce' (봉인 r 기준, env 무시); plan Task 1 Action 2 + Validate 'seal이 **존재하는 상태에서** … 갈래 H 4파일이 green' |
| architect | MEDIUM | Task 1이 childEnv에 MCCP_CODEX_DISABLED=1을 기본 강제하면, 실제 companion을 부르는 유일한 smoke test가 전수 러너 경로에서 **항상 skip**으로 접힌다. 그러면 M2가 만드는 'green'과 M3의 '커버리지 100%'가 그 파일에 대해서는 '실행됐다'만 주장하고 '단언이 돌았다'는 주장하지 못한다 — PRD 축 D가 겨냥한 바로 그 허위 커버리지 형태다. plan은 이 대가를 Risks에서 다루지 않고 '명시 env 객체를 넘기므로 영향 없음'만 적는다(그 test는 process.env를 읽는다). | plugins/mccp/scripts/lib/tests/codex-companion-smoke.test.js:97-99 `const pre = classify({ env: process.env, … }); … t.skip('smoke skipped: ' + pre.reason)`; codex-reachability.js:23-27 'env.MCCP_CODEX_DISABLED === "1" 이면 invokeResult 가 무엇이든 {reachable:false}'; plan Risks 행 'MCCP_CODEX_DISABLED=1 강제가 … 명시 env 객체를 넘기므로 process.env 강제에 영향받지 않는다' |
| architect | MEDIUM | Task 7의 Mirror('원문을 지우지 않고 취소선 + 정정을 병기')를 `## Delivery Milestones` 표의 **status 셀**에 적용하면 그 행이 archive-complete의 C4 엄격 파싱에서 `non-canonical`이 되어, PRD가 나중에 완료돼도 영구히 archivable이 되지 못한다. Task 7의 Validate('원시 행 수 = complete + dropped 등식이 깨지지 않음')는 이 실패를 **탐지할 수 없다** — in-progress PRD에서는 등식이 어차피 거짓이라 non-canonical 여부와 무관하게 같은 결과가 나온다. | plugins/mccp/scripts/lib/archive-complete/scan.js:101-102 '빈 셀·비정규 텍스트("complete (verify) · gated")는 모두 non-canonical', :159 `if (c.rawRowCount === c.complete + c.dropped)`; plan Task 7 Mirror + Validate |
| architect | MEDIUM | Task 4-1은 codex-reachability의 분류를 14→15로 늘린다고만 적고, 같은 모듈이 갖는 **두 번째 enum인 `kind`(KINDS 5종 + REASON 5종)**에 `round-cap-reached`를 어디에 넣을지 정하지 않는다. 기존 kind 중 어디에 넣어도 축 혼동이다 — CLAUDE.md §3.3이 `disabled`와 `round-cap-reached`를 '서로 다른 축'이라 못박았는데 env-policy/transport/not-installed 어느 것도 '예산 소진'이 아니다. 새 kind를 만들면 KINDS·REASON·소비처가 함께 바뀌는데 Files to Change에는 그 확장이 명시되지 않는다. | plugins/mccp/scripts/lib/codex-reachability.js:31-63 (CLASSIFICATION_KIND 14키 + KINDS 5종 + REASON 5종); plan Task 4 '`KNOWN_CLASSIFICATIONS` / `CLASSIFICATION_KIND`에 `round-cap-reached`를 더하고, test의 하드코딩 `14`를 `15`로 올린다' — kind 값 미지정 |
| security | HIGH | Task 1 step 2 has the suite runner inject MCCP_ROUND_LEDGER='observe' into every spawned test child, which re-creates the `off` kill switch that DD7 deliberately refused and violates the repo's declared invariant that this variable is operator policy nothing may assign. The whole suite would then execute with round-cap enforcement globally non-blocking, so any regression that removes cap enforcement passes green — and no existing test catches it, because the only test guarding this variable scans gate command bodies, not the runner. | Plan Task 1 (.claude/plans/ci-full-suite-m2.plan.md:201): "`MCCP_ROUND_LEDGER`는 이미 존재하는 손잡이이고 값은 `enforce\|observe`다 … `observe`를 `childEnv`에 실은 뒤". Counter-evidence: plugins/mccp/scripts/lib/review-rounds/seal.js:51-54 "`off`가 없다 (DD7). 끄는 것은 M3 이전 동작을 요청하는 것이고 그것이 결함 자체다."; plugins/mccp/scripts/lib/tests/round-cap-command-body.test.js:209-212 asserts "must never assign MCCP_ROUND_LEDGER — it is operator policy, not gate state" but only over commands/*.md bodies, so scripts/test-suite/run.js childEnv is outside its scan. |
| security | MEDIUM | The plan edits childEnv — the exact chokepoint that forwards the entire host/CI environment to every spawned test child — but adds only MCCP_* policy neutralization and no secret boundary, while Task 5/6 newly institutionalizes committing CI-produced run elements into a git-tracked container. Redaction covers only filesystem-path shapes and merge trusts the producer's own redaction_ok, so a failure string echoing an env value (e.g. a token present in a pull_request-triggered fork run) flows into a committed file with redaction_ok=true. | scripts/test-suite/run.js:423-427 childEnv() = Object.assign({}, process.env, …) stripping only NODE_TEST_* ; scripts/test-suite/redact.js:163-168 RESIDUAL_PATTERNS = win-drive-abs/posix-home/posix-tmp/file-url only (no credential class); scripts/test-suite/run.js:321-326 + :328-336 merge blocks on producer-sealed redaction_ok plus a path-only secondary scan; .github/workflows/test-suite-baseline.yml:35-38 triggers on pull_request paths 'scripts/**'; plan Files-to-Change row `.claude/_meta/data/2026-09-01-suite-baseline.json \| UPDATE` and Acceptance (plan:316) requiring >=3 merged CI elements in that tracked file. |
| security | MEDIUM | The plan silently drops its own injected security findings. Its Design Critique section asserts exactly one unabsorbed MEDIUM observation, but the fan-out block contains four unaddressed security findings (env passthrough, no credential class in redaction, artifact→tracked-file trust boundary, fork-PR token exposure) that appear nowhere in Tasks, Risks, or a backlog deferral, contrary to CLAUDE.md §3.14's append-with-evidence obligation. | Plan lines 70, 71, 72, 82 (the four [MEDIUM]/[LOW][security] findings) vs plan line 341: "**미흡수 관측 1건 (MEDIUM, §3.14대로 backlog 이연)**" — that single deferral is about list-of-N folding, not any of the four; grep of the plan shows no Task or Risk row referencing env passthrough, credential redaction, or fork-PR token scope. |
| test | HIGH | Task 1의 근거 주장 자체가 거짓이다 — `MCCP_SUITE_REPO_ROOT` 소비처는 0건이 아니라 실재한다. 그 위에 세운 '변수를 제거하거나 소비처를 만든다'는 행동은 제거를 택하면 reporter의 repo-relative 산출(따라서 redaction/attribution 경로)을 조용히 깨뜨리며, 그 회귀를 잡는 Validate 줄이 계획에 없다. | plan.md:203 "`MCCP_SUITE_REPO_ROOT`의 소비처가 **0건**이다(`scripts/test-suite/run.js:424`가 유일 등장)" vs 실측 `scripts/test-suite/reporter.mjs:223`: `const repoRoot = process.env.MCCP_SUITE_REPO_ROOT \|\| process.cwd();` |
| test | HIGH | Files to Change의 `plan-review/l1-check.js` + `plan-review-l1.test.js`(2행)에 대응하는 Task가 없고, `## Validation` 블록 어디에도 `plan-review-l1.test.js`를 돌리는 명령이 없다. 즉 게이트 체커를 완화 방향으로 고치는 변경인데 이 계획의 Validate 경로가 그것을 한 번도 실행하지 않는다(짝 test는 이미 존재함에도). | plan.md:184-185 (Files to Change 2행) · plan.md:191-250 Tasks 0~7에 l1-check 항목 없음 · plan.md:254-293 Validation 명령 목록에 `plan-review-l1.test.js` 부재. 해당 test는 실재한다: plugins/mccp/scripts/lib/tests/plan-review-l1.test.js:265-274 |
| test | HIGH | `plugin.json` patch bump을 Files to Change에 넣었으나 CLAUDE.md §3.7이 요구하는 동기 4면(renderer/html.js page-foot · renderer/markdown.js derived 줄)이 표에 없고, 그 drift를 잡는 검증 수단(`renderer/tests/i18n-surface.test.js` — 기대값을 plugin.json에서 파생)이 Validation 명령에 없다. plugin.json만 올리면 그 test가 red가 되는데 이 계획의 어떤 Validate 줄도 그것을 실행하지 않는다. | plan.md:186-187 (plugin.json·CHANGELOG만 등재) · plan.md:254-293 Validation에 renderer test 부재 · CLAUDE.md §3.7 "`plugin.json`만 bump하고 footer를 빠뜨리면 그 test가 red로 잡는다" |
| test | HIGH | Task 1의 핵심 주장('러너가 자식을 ambient 게이트 상태로부터 격리한다')을 반증할 test가 없다. Validate는 'seal 존재 상태에서 갈래 H 4파일 green'이라는 간접 오라클뿐인데, 이는 `MCCP_CODEX_DISABLED=1` 강제만으로도 green이 될 수 있어 라운드-원장 중화가 실제로 도달했는지와 구분되지 않는다. 계획 자신이 Risks 1행에서 정확히 이 혼동을 위험으로 적고, 완화책으로 같은 구분 불가 오라클을 제시한다. | plan.md:205 Validate · plan.md:299 Risk "`observe`가 소비처에 도달하지 않는데 다른 이유로 green ... Validate 1을 seal이 존재하는 상태에서 돌린다" — 자식 env의 키 유무를 직접 단언하는 수단은 없음(`childEnv`는 scripts/test-suite/run.js:671-688 export 목록에 없다) |
| test | MEDIUM | Validation 3번 줄은 구조적으로 실패할 수 없다 — `node -e '...주석만...' \|\| echo`이므로 성공해도 실패해도 exit 0이고, 아무것도 단언하지 않는다. UI2('러너가 codex 비활성을 강제한다') 배선 확인을 표방하지만 검증력이 0이다. | plan.md:276-277 `node -e 'const r=require("./scripts/test-suite/run.js"); /* childEnv 강제 여부를 export된 seam으로 확인 */' \|\| echo "childEnv seam이 export되지 않았다면 …"` |
| test | MEDIUM | Task 4는 codex-reachability의 '하드코딩 14를 15로'라고 단수로 적지만 실제 pin은 셋(EXPECTED 배열 · KNOWN_CLASSIFICATIONS.length · 주석 헤더 declared.length)이고, 더 중요한 것은 `round-cap-reached`가 어느 `kind` 버킷에 들어가는지를 계획도 어떤 test도 규정하지 않는다는 점이다. KINDS는 닫힌 5종이라 아무 버킷에 넣어도 test는 green이며(reachable만 아니면 됨), 예컨대 `env-policy`에 넣으면 REASON 문구가 'MCCP_CODEX_DISABLED=1'이라 거짓 사유를 보고하는데 그것을 잡는 단언이 없다. | plugins/mccp/scripts/lib/tests/codex-reachability.test.js:41-70(EXPECTED+두 개의 14) · :86(세 번째 14) · plugins/mccp/scripts/lib/codex-reachability.js:54 KINDS · :57-58 REASON['env-policy'] 문구 · plan.md:224 "test의 하드코딩 `14`를 `15`로 올린다" |
| test | MEDIUM | Task 5의 flaky 판정 오라클('3회의 failing 집합이 동일')은 과다-허용 방향을 덮지 못한다 — 세 run이 모두 같은 파일에서 실패해도 '동일'이므로 통과 서술을 만족시키고, red를 flaky가 아닌 '상시 red'로 분류한 뒤 exclusions로 옮기면 Acceptance가 성립한다. 즉 격리 통로의 남용을 기계가 아니라 산문(Risks의 '건수를 문서에 적는다')이 막는다. | plan.md:236 Validate · plan.md:317 Acceptance "비어 있거나 전부 `exclusions`에 사유와 함께 등재" · plan.md:304 Risk 완화가 '건수를 M2 산출 문서에 적는다' |
| invariant | HIGH | Task 1의 갈래 H 중화 기제(`MCCP_ROUND_LEDGER=observe`를 childEnv에 싣기)는 Task 1 Validate가 요구하는 바로 그 상태(seal 존재)에서 구조적으로 무효다. 그리고 plan은 그 무효를 '그 seam이 결함이고 그것을 수리 대상으로 삼는다'고 적어, 의도적 fail-closed 우선순위를 test를 green으로 만들기 위해 뒤집는 무제한 위임을 열어 둔다. | seal.js:208-213 '모드는 봉인 우선이고 env는 fallback이다 … 반대로 env 우선이면 실행 중 `observe`를 심는 것이 곧 조용한 우회이고, 그것이 M3이 막으려는 실패 계열 자체다' + seal.js:241-248 (seal.reason==='ok'이면 반환 mode는 `r.mode`이고 env는 읽지 않음). Plan:201 '`observe`를 `childEnv`에 실은 뒤 … 도달하지 않으면 그 seam이 결함이고 그것을 수리 대상으로 삼는다', Plan:205 'seal이 존재하는 상태에서 … green'. |
| invariant | HIGH | Validate 3번('러너가 codex 정책을 실제로 강제하는가 — UI2 배선 확인')은 실패할 수 없는 스텝이다. `node -e`의 본문이 주석뿐이라 항상 exit 0이고, `\|\|` 분기는 도달 불가하며 어떤 단언도 없다. UI2의 유일한 기계적 검증 지점이 게이트 모양만 남은 게이트다. | Plan:276-277 `node -e 'const r=require("./scripts/test-suite/run.js"); /* childEnv 강제 여부를 export된 seam으로 확인 */' \|\| echo "childEnv seam이 export되지 않았다면 …"` — 실행문 없음, 단언 없음, 비영점 종료 불가. |
| invariant | MEDIUM | Risks 표의 '`MCCP_CODEX_DISABLED=1` 강제가 codex 경로 test의 의미를 바꾼다 → 낮음' 완화 근거가 반증된다. 근거는 '해당 test들이 명시 env 객체를 넘기므로 process.env 강제에 영향받지 않는다'인데, 여러 생산 경로가 caller env가 아니라 `process.env`를 직접 읽는다. 즉 전수 러너의 기본값이 receipt 내용과 bridge/hook 분기를 스위트 전역에서 바꾸며, plan의 Validate는 갈래 H 4파일만 본다. | receipt/write.js:702 및 :719 `envValue.parseBool(process.env, 'MCCP_CODEX_DISABLED')` (meta.codex_disabled + skip_reason을 ambient env로 stamp) · codex-bridge.js:136 `parseBool(process.env, …)` (bridge short-circuit) · hooks/session-start.js:1066 · lib/dep-check.js:163. Plan:300이 반대 주장을 함. |
| invariant | MEDIUM | Task 4가 `round-cap-reached`를 `CLASSIFICATION_KIND`에 더하도록 지시하면서 어느 `kind`에 매핑할지 정하지 않는다. 현행 KINDS 5종 중 정직한 값이 없고(`transport`로 접으면 예산 소진을 전송 실패로 오보), 현재 동작은 이미 fail-closed unreachable + '알 수 없는 분류' 사유라 안전 축의 이득이 없다. 매핑 선택이 미지정인 채 test의 14→15만 올리면 오라클이 거짓 사유를 봉인한다. | codex-reachability.js:54 `KINDS = ['env-policy','not-installed','unauthenticated','transport','reached']` · :110-118 '표를 벗어난 값은 도달 성공으로 읽지 않는다 … unknown classification "…" (fail-closed)'. Plan:224는 'reachable이 아니다'만 정하고 kind를 정하지 않음. |
| invariant | MEDIUM | 핵심 갈래 H의 수용 증거가 기계 검증이 아니라 산문이다 — '통과했다는 기록이 문서에 있다'는 명제는 실제 격리 없이도 참이 될 수 있고, 되돌아갔을 때(회귀) red를 내는 앵커가 없다. | Plan:318 '갈래 H 검증이 **seal이 디스크에 존재하는 상태**에서 통과했다는 기록이 `docs/ci-full-suite/m2-green.md`에 있다.' — 대응하는 자동 단언은 Plan:169의 test-suite.test.js 행 한 줄뿐이고 Validate 블록에 seal 존재를 만들거나 확인하는 스텝이 없다. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | (1) Task 1의 격리 추상화를 소스로 검증 — seal.js resolveEnforcement의 봉인>env 우선순위(:208-213, :241-244)와 codex-invoke의 gitDir 결속(:227-229, :296-299)을 읽어 env 주입이 Validate 조건에서 무효임을 확인. (2) run.js childEnv(:423-427)와 INHERITED_TEST_CONTEXT_KEYS 관용구가 실제로 인용대로인지 확인 — 정확했다. (3) codex-invoke.js:17,19가 round-cap-reached를 15번째로 이미 선언하고 codex-reachability는 14키인지 실측 — plan의 drift 주장은 참이었고, 대신 kind enum 미지정 공백을 찾았다. (4) l1-check.js CITATION_RE(:71)와 그 주석(:66-70)을 열어 dot-path 수정이 실재하는지 확인 — 실재했고 plan 서술과 일치. (5) archive-complete/scan.js의 C3/C4 파싱을 읽어 Task 7 Validate가 자기 위험을 못 잡음을 확인. (6) codex-companion-smoke.test.js의 skip 경로가 process.env를 읽는지 확인해 Risks 표의 반박 논거가 이 파일에는 성립하지 않음을 확인. (7) Files to Change의 경로 full-path 규약·Patterns to Mirror의 goal-detect/santa-loop-cap skip 관용구는 공격했으나 결함을 찾지 못했다. |
| security | fail | Read the plan and PRD in full, then verified each cited module rather than the plan's summary of it. (1) Traced Task 1's env-neutralization proposal into scripts/test-suite/run.js:423-427 and plugins/mccp/scripts/lib/review-rounds/seal.js:49-79 — the plan's chosen handle is the one whose own header states that turning enforcement off is the defect, and round-cap-command-body.test.js:203-212 encodes an assign-never invariant that structurally cannot see the runner; that landed. (2) Traced the fork-PR → full-env → failure-string → artifact → git-tracked-container path through the workflow trigger, childEnv, redact.js RESIDUAL_PATTERNS and validateElement; the secret class genuinely has no coverage and the merge trusts the producer seal, so this reaches a committed artifact. (3) Checked the plan's claim that it fixed l1-check.js CITATION_RE for dot paths — the fix and its rationale comment are already present at l1-check.js:64-71, so the citation is accurate (no finding from my lens). (4) Attacked Task 1's forced MCCP_CODEX_DISABLED='1' as a policy-forgery or receipt-stamping escalation and could not land it: the toggle is honored seal-OR-env toward disabled, --allow-codex is an explicit opt-out per UI2, and no receipt is written by the suite runner. (5) Attacked Task 3's fetch-depth: 0 as widening fork exposure and could not land it — checkout depth does not change token scope or trigger surface, and the plan explicitly leaves triggers/paths/continue-on-error untouched. (6) Attacked mergeIntoContainer/readJsonFile for prototype-pollution or path traversal via --from and found assertNoPollution/sanitize and a size cap already in place (run.js:341-350). |
| test | fail | plan의 인용을 실독으로 대조했다: seal.js:49-54의 MCCP_ROUND_LEDGER 값 열거(참), l1-check.js:71 CITATION_RE의 선행 점(이미 수정됨 — 짝 test도 :265-274에 실재), codex-invoke.js:14-24 헤더가 이미 15종을 선언(참), codex-reachability.test.js의 14 리터럴 위치 3곳과 EXPECTED/kind 구조, run.js:421-425 childEnv와 671-688 export 목록, reporter.mjs:223. 각 Files to Change 행에 대응하는 Task와 Validate 명령을 역매핑해 l1-check 2행과 plugin.json/CHANGELOG 행이 어떤 Validate에도 걸리지 않음을 확인했다. Task 1·5의 오라클이 실패 방향(위장 green·격리 남용)을 덮는지 각각 반례를 구성했다. mask.test.js·meta-research·instruction-contract 축은 Validation 명령이 실제로 그 파일을 돌리므로 공격했으나 결함을 찾지 못했다. |
| invariant | fail | Task 1의 두 중화 축을 실제 소스로 추적: (a) codex-invoke.js:214-239의 `seal OR env`가 단조 disable 방향이라 `MCCP_CODEX_DISABLED=1` 강제는 실효함을 확인(반증 실패), (b) review-rounds/seal.js:208-254의 seal-우선 규칙으로 `MCCP_ROUND_LEDGER=observe` 경로가 seal 존재 시 무효임을 확인(반증 성공). Risks 표의 'process.env 영향 없음' 주장을 write.js/codex-bridge/session-start 실독으로 대조. l1-check.js:71 CITATION_RE의 선행 점 수정이 C6를 느슨하게 하는지 확인 — 존재 검사는 그대로라 완화 아님(반증 실패). Task 4의 codex-reachability 미지 분류 경로가 이미 fail-closed임을 확인. Validation 블록의 각 스텝이 실제로 비영점 종료할 수 있는지 검사해 3번이 항상 exit 0임을 확인. Acceptance 4항목의 앵커 유무 점검. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 2490039,
  "halt_stage": null,
  "backlog_appended": 12,
  "backlog_skipped_nonblocking": 11,
  "granted": 4,
  "reviewed_plan_hash": "sha256:8a930d3536f01a47b3b8457c3c64a07a486dda2e6182b135448d8202060af7fe",
  "plan_path": ".claude/plans/ci-full-suite-m2.plan.md",
  "recorded_at": "2026-09-02T08:57:46.387Z"
}
```
