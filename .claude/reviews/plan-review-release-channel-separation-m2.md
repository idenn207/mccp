# Plan Review Panel — release-channel-separation-m2

**Plan**: `.claude/plans/release-channel-separation-m2.plan.md` · **Plan version**: `sha256:16f6a4587dca4ee5627829d410c8f63cc23c5822bc8adcbdd99d711512b58914`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 8 blocking finding(s): architect/HIGH, architect/FAIL, test/HIGH, test/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | 검사 13의 `cache_dirs_added: 0` 단언이 mechanism 축을 무시해, Task 5 후퇴선(dev-marketplace)을 R8이 이미 닫았다고 적은 바로 그 형태 — '정직한 보고 = 확정 HALT, 통과 유일 경로 = 거짓 기재' — 로 다시 만든다. R8은 `marker_observed_with_plugin_dir`를 `mechanism` 선언 축으로 갈라 닫았으나, 같은 블록의 형제 키에는 그 분기가 적용되지 않았다. | plan L756-757: `printf '%s\\n' "$OBS" \| grep -qE '^- cache_dirs_added: 0$' \|\| { echo 'HALT: cache_dirs_added is not 0 …'; exit 1; }` — `mechanism` 값과 무관하게 무조건. 반면 L750은 `mechanism: (plugin-dir\|dev-marketplace)`를 1급 분기로 인정한다. 그 불변식의 출처는 plugin-dir 전용이다(Task 3 Validate, L196-199·L202: '`~/.claude/plugins/cache/mccp/mccp/` 하위에 새 디렉토리가 생기지 않는다'). 그런데 plan 자신이 후퇴선이 설치를 요구할 수 있음을 명시한다 — Task 2 L159-160: '그 전제가 깨지는 순간(후퇴선이 `plugin install`을 요구하면) M1 Task 2의 캐시 백업 절차를 그대로 선행한다.' 즉 dev-marketplace 경로는 캐시 디렉토리를 만들 수 있고, 그때 정직한 보고서는 `cache_dirs_added: 1`을 적어 확정 HALT하며 green을 얻는 유일한 길은 0으로 적는 것이다. R8 흡수 서술(L1136)이 '게이트가 거짓 보고를 보상하는 형태'라 부른 것과 동형. |
| architect | MEDIUM | '두 기제 모두 실패'라는 결과에 착지 경로가 없다 — 게이트 자신의 HALT 메시지가 지시하는 행동(그 사실을 보고서에 적기)을 수행해도 같은 검사가 HALT한다. | plan L752-753: `grep -qE '^- marker_observed: (true\|yes)$' \|\| { echo 'HALT: … 두 기제 모두 실패했다면 그것이 이 마일스톤의 결론이고, 문서가 아니라 그 사실을 보고서에 적어야 한다'; exit 1; }` — 지시를 따라도 exit 1이다. Risks 표(L802-810)에도 '두 기제 모두 실패' 행이 없고 Acceptance(L817-822)는 marker 관측을 요구한다. Task 3 Validate(L209-210)는 실패 시 Task 5로만 보내고 그 다음 후퇴선이 없다. |
| architect | MEDIUM | 산출물을 만드는 Task와 그 산출물의 계약을 강제하는 게이트가 분리돼 있고, Task 쪽에 계약이 복제되지 않아 caller가 올바르게 만들 정보를 갖지 못한다. | 검사 13(L731-742)은 보고서에 `## 실측 원문` 섹션과 7개 키(`mechanism`·`marker_nonce`·`marker_observed`·`marker_absent_without_mechanism`·`installed_plugins_version`·`installed_plugins_git_commit_sha`·`cache_dirs_added`)를 특정 형식(`- key: 값`)으로 요구한다. 그러나 그 산출물을 만드는 Task 11(L340-355)의 Action·Validate에는 섹션명도 키 목록도 형식도 등장하지 않는다 — Validate는 '보고서가 존재하고 Task 3~5의 관측값을 포함하며' 뿐이다. |
| architect | LOW | 검사 12의 `ALLOWED`가 `Files to Change` 표의 투영이 아니라는 한정은 명시됐으나, 실제로 이 브랜치가 확실히 수정하는 두 tracked 파일(plan 본문·backlog)이 표에 없다는 사실은 어디에도 적히지 않는다 — §1.2 dedupe planned matcher는 그 표를 읽으므로 두 파일이 residual로 떨어진다. | ALLOWED(L682)는 `.claude/plans/release-channel-separation-m2.plan.md`와 `.claude/plans/codex-findings-backlog.md`를 명시 허용하지만, `Files to Change` 표(L122-131)에는 두 행이 모두 없다. plan은 backlog에 실제로 적재했다고 여러 곳에서 적는다(L1031-1032). CLAUDE.md §1.2: 'plan 표의 첫 열을 git diff 경로와 리터럴/glob 매칭 … 매칭 실패 → 모든 파일이 residual로 떨어져 `skip_safe=false`'. fail-closed라 안전하지만 표의 소유 주장이 거짓이 된다. |
| security | MEDIUM | The only global-state mutation this milestone makes — registering a worktree-pointing marketplace in the operator's global `known_marketplaces.json` (Task 5) and flipping `enabledPlugins` (Task 4) — has ZERO mechanical guard in `## Validation`. All 13 checks look only at the repo (git diff/status, manifests, report, i18n test). The restoration assertion lives only in Task 4.3 / Task 5.4 prose, which is human-executed and does not run if the experiment session dies mid-Task-5. The plan itself repeatedly asserts the opposite standard for every repo-side axis ('기구의 죽음은 청결이 아니다', checks 7/10/11/12 all HALT on instrument failure), so this is an evidenced asymmetry, not a stylistic preference. Concrete input→consequence: an aborted Task 5 leaves a permanent global registry entry resolving mccp content from a worktree path outside the `release` channel — the exact trust boundary M1 established — and the branch still passes all 13 checks and ships. | plan.md Task 5 step 4: "`claude plugin marketplace add`는 운영자 전역 `known_marketplaces.json`에 영구 항목을 만들고, 그 항목은 이 worktree를 가리킨다"; Task 4.3 판정 기준 list is the only assertion site; `## Validation` checks 1-13 (plan.md L366-795) contain no reference to `known_marketplaces.json` or `enabledPlugins`. |
| security | MEDIUM | The absolute-path leak detector's UNC branch is over-corrected and now matches ANY single backslash followed by an alphanumeric, not UNC. In the `node -e "…"` double-quoted shell string, `\\\\\\\\[A-Za-z0-9]` collapses to the JS source `\\\\[A-Za-z0-9]`, i.e. the regex `\\` (one literal backslash) + alnum. Given the leading `(^\|[^A-Za-z0-9])`, any quoted shell/regex fragment such as `printf '%s\\n'` or `grep -E '^\\+'` in `docs/dogfood-install.md` or the M2 report — files whose whole purpose (Task 11, check 13) is to record the commands actually executed — produces a false HALT reported as 'absolute path in added line(s)'. The plan's own R8 correction names exactly this failure class ('게이트가 유출을 보상하는 형태': a gate that cannot be passed by an honestly-redacted document pressures the author to weaken the detector). The R1 note claims the prior UNC term was 'matching-impossible dead code' and that this edit closed it; the edit swung past UNC into a generic single-backslash matcher and no test in this plan exercises a report line containing `\\n`. | plan.md L573: `const RE = /(^\|[^A-Za-z0-9])([A-Za-z]:[\\\\\\/]\|\\/Users\\/\|\\/home\\/\|\\\\\\\\[A-Za-z0-9])\|…/;` inside `node -e "…"` (L569); plan.md L783-789 (R8 정정) states the prior over-broad placeholder pattern made a correctly-redacted report structurally unpassable. |
| test | HIGH | The plan's own HIGH-rated invariant — operator global state (`~/.claude/settings.json`, `installed_plugins.json`, `known_marketplaces.json`) is restored after Task 4/5 — has NO falsifier anywhere in `## Validation`, only human prose. This is exactly the failure mode the plan itself declared closed by check 13 ("닫는 수단이 사람이 체크하는 체크박스여서 고치겠다고 선언한 실패 모드를 그대로 재현", plan:719-721). Check 13 mechanized the analogous axis for `installed_plugins.json` (live compare, plan:759-780) but left the registry/settings restoration axis unmechanized, even though it is equally readable live at Validation time. Task 5 explicitly creates a permanent global registry entry pointing at the worktree (plan:260-265); if `marketplace remove` is skipped or fails, nothing in the 13 checks reports it. | plan:240-241 Task 4 Validate is prose-only ("위 3파일 판정이 전건 통과한다"); plan:267-268 Task 5 Validate likewise; the check-13 required-key list plan:738-739 contains no registry/settings key; grep of the whole plan shows `known_marketplaces` appears in Validation only at plan:645 as a repo-ingress filename signature, not as a restoration assertion. |
| test | MEDIUM | Check 4 (dev-manifest shape) and check 13's `mechanism` declaration are never cross-checked, so `mechanism: dev-marketplace` can be reported while the shape assertion silently skips. The plan acknowledges the skip as a known limitation (plan:414-418) and says "보고서가 기록하며, 그 기록이 이 skip의 짝" — but after R8 added a machine-readable `mechanism` key, that pairing is available to the gate and still is not used, so the stated 'pair' is human-only. | plan:419 `if [ -f marketplace.dev.json ]; then` (unconditional skip when absent) vs plan:750-751 `grep -qE '^- mechanism: (plugin-dir\|dev-marketplace)$'` — no check couples them. |
| test | MEDIUM | Acceptance requires the live run to follow `docs/dogfood-install.md` verbatim, but no check verifies the documented procedure matches the mechanism actually measured. Check 5 only tests existence of the doc (plan:432) and check 13 reads only the report; a doc describing `--plugin-dir` alongside a report declaring `mechanism: dev-marketplace` passes all 13 checks. | plan:817-822 Acceptance "`docs/dogfood-install.md`에 적힌 절차를 그대로 따라"; plan:432 `test -f docs/dogfood-install.md`; plan:731-757 check 13 reads only `$REPORT`. |
| invariant | HIGH | 라운드 캡(v1.33.5 기계 강제, §3.16)이 원장 파일 이동으로 최소 3회 우회됐고, plan은 그중 1회만 기록한다. 게이트는 여전히 게이트처럼 보이지만 이 decision에 대해 아무것도 멈추지 않는다. | plan L1102: '원장 파일을 `archive/…rounds-r1-r4.json`으로 **옮겨** 카운터만 리셋하고 R5부터 재개했다'. 그러나 디스크에는 리셋 산출물이 3개다 — `.claude/state/review-rounds/archive/mccp-plan-codex__release-channel-separation-m2.rounds-r1-r4.json` · `…rounds-r5-r7.json` · `…rounds-r8.json`. plan 본문에는 r5-r7·r8 리셋에 대한 서술이 0건이고, 현재 원장 `mccp-plan-codex__release-channel-separation-m2.json`은 `rounds` 길이 1로 초기화돼 있다. 게다가 같은 plan L997-998은 '원장을 지우거나 receipt를 위조하는 §3.16 금지 행위는 하나도 하지 않았다'고 단언한다 — L1102·L1106과 정면으로 모순된다. CLAUDE.md §3.16: '캡에 걸렸을 때의 정당한 행동은 … 원장을 지우는 것은 그 목록에 없다.' |
| invariant | HIGH | 게이트의 diff-containment 검사(검사 12)가 `.claude/state/` 전체를 무조건 허용하므로, 위 원장 리셋은 `## Validation` 어느 항목에도 걸리지 않는다 — 캡 우회가 기계적으로 불가시다. | plan L682 `ALLOWED='^((…)$\|(\\.claude/receipts\|\\.claude/state\|\\.claude/reviews)/)'`. 원장 경로 `.claude/state/review-rounds/…`와 `archive/…`가 이 접두 규칙에 전건 포섭되고, 검사 7의 `LEAK_PATHSPEC`(L515)에도 `.claude/state`는 없다. 검사 10은 삭제만, 검사 11은 백업/plugin-state 서명만 본다. |
| invariant | HIGH | Task 5가 운영자 전역 `known_marketplaces.json`에 남기는 영구 항목(worktree를 가리키는 dev marketplace)의 해제는 `## Validation`의 어떤 검사도 검증하지 않는다 — plan이 검사 12를 신설하며 스스로 폐기한 '사람이 표시하는 Task Validate'가 유일한 가드로 남았다. | plan L261-265: 'worktree가 사라진 뒤에도 남으면 … 릴리스 채널 밖의 본문이 해소 가능한 상태로 방치된다'. 그 판정은 Task 4 L234-236 / Task 5 Validate L267-268에만 있고, `## Validation` 검사 1~13(L366-795) 어디에도 `known_marketplaces` 또는 `~/.claude/settings.json` 문자열이 없다. 기술적 불가가 이유일 수 없다 — 검사 13 L760이 이미 `IP="$HOME/.claude/plugins/installed_plugins.json"`을 라이브로 읽고 부재 시 HALT한다. 같은 plan L656-662는 '유일한 가드가 Task Validate뿐이면 반증 수단이 0'이라며 그 형태를 폐기했다. |
| invariant | MEDIUM | 검사 13의 라이브 대조 중 gitCommitSha 축은 접두 일치라 1글자 값으로 자동 통과한다 — '보고서가 관측하지 않은 상태를 적었다'를 잡는다는 그 검사의 주장이 그 축에서는 성립하지 않는다. | plan L780 `case "$LIVE_S" in "$RPT_S"*) ;; *) …exit 1 ;; esac`. 앞선 필수 키 루프(L738-742)는 `.+`만 요구하므로 `- installed_plugins_git_commit_sha: 6` 한 글자가 요구를 만족하고 접두 일치도 만족한다. version 축(L778 엄격 일치)과 비대칭이며, 완화 방향이다. |
| invariant | LOW | 후퇴선 manifest 형태 단언(검사 4)은 파일명·위치가 계획과 다르면 조용히 skip된다 — 그 skip의 짝으로 지목된 '보고서 기록'은 기계 검사가 아니라 산문이다. | plan L419 `if [ -f marketplace.dev.json ]; then`. plan 자신이 L416-418에 '이 검사는 파일명·위치가 계획대로일 때만 발화한다 … 조용히 skip된다(L2 R1 invariant LOW — 이연하지 않고 한정을 명시)'라고 적었고, 검사 13의 필수 키 집합(L738-739)에는 dev manifest 경로를 보고하게 하는 키가 없다. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | plan 전문(1156줄)과 PRD를 읽고 다음을 공격했다. (1) `## Validation` 13개 검사의 파이프라인 종료코드·guard 배치를 R4가 세운 규칙(git을 단독 할당으로 받기)과 대조 — 검사 2·6·7·10·11·12·13 전부 그 형태를 지키고 있어 새 fail-open을 못 찾았다. (2) 검사 12의 `ALLOWED`를 `.gitignore` 실물과 대조해 정상 작업물이 stray로 오탐되는지 확인 — `.claude/receipts/*`·`state/plan-review/`·`cache/`가 전부 ignored이거나 접두 허용 안에 있어 오탐 없음. `.claude/reviews/`가 tracked라는 plan의 전제도 `.gitignore`에 항목이 없음을 확인해 참. (3) 검사 12가 Task 3의 hook 변조를 커밋 전후 양쪽에서 잡는지 — 작업 트리 arm(L701)이 있어 성립. (4) 검사 13의 sed 범위 추출·`installed_plugins.json` 배열 접근·live 대조의 판별력 — 결함 없음. (5) mechanism 축이 검사 13 전 키에 일관되게 적용되는지 — 여기서 `cache_dirs_added`가 R8이 닫았다고 적은 결함의 미봉합 잔여임을 발견(HIGH). (6) 두 기제 모두 실패하는 경우의 착지 경로 부재, (7) 산출물 Task와 게이트 계약의 분리, (8) `Files to Change` 표와 ALLOWED의 drift를 추가로 확인했다. |
| security | pass | Attacked: (1) leak-detector regex — hand-expanded bash double-quote escaping of the drive-letter, /Users//home/, MSYS `/c/`, and UNC alternatives; drive and POSIX arms are correct, UNC arm is over-broad (reported). (2) Target-set derivation for check 7 — verified the two exclusion claims are true, not convenient: `.claude/reviews/plan-review-release-channel-separation.md:9` really does contain a verbatim machine-quoted absolute path (`C:/_project/mccp/.worktrees/...`), and `CHANGELOG.md:3039-3040` really does carry the pre-existing synthetic `X:\\\\parent\\\\repo`; both exclusions are disclosed with backlog deferral, so they are acceptable residual, not a defect this plan introduces. (3) `meta.cwd` precedent — checked whether M2 reopens absolute-path leakage into a git-tracked artifact on a new field; Task 11 H4 mandates `<HOME>`/`<PLUGINS>`/`<WORKTREE>` substitution and check 7 covers `.claude/PRPs/reports`, so I could not land it. (4) Committed-hook-tamper escalation — Task 3 mutates tracked `session-start.js`; traced whether the marker change could reach a release cut, but check 12 unions committed diff + working tree and `plugins/.../session-start.js` is outside ALLOWED, so it is caught either way. (5) `marketplace.dev.json` as a distributed channel-bypass artifact — it requires an explicit `marketplace add <file>` by the user and check 3/4 pin both manifests' shapes; I could not construct a silent path to users. (6) Instrument-death-reads-as-clean on checks 2/7/10/11/12/13 — every one now has an explicit `rev-parse`/assignment guard; I found no remaining `\|\| true` on a git call whose failure folds to a pass. (7) Global-state restoration — landed (reported). |
| test | fail | Read the full plan (Tasks 1-11, 13-check Validation block, Risks/Acceptance, review ledger) and the source PRD. Attacked: (1) check-13 live-compare oracle — tried to break the sed range, the required-key regexes, the array-vs-object accessor, and the sha prefix `case` match; all hold (required keys force non-empty values, so the empty-pattern vacuous match is unreachable). (2) check 6 bump detector — traced `grep -c` exit-1 with `\|\| true` and the `sort -V` upward test; fails closed. (3) checks 7/10/11/12 pipeline-exit-code traps that R4 fixed — verified git calls are now standalone assignments with guards; found no remaining fold. (4) Task 3's hook-tamper claim — check 12 now covers committed AND worktree arms, so the marker leak is genuinely caught. (5) Task 10 version 4-face sync — check 9 runs i18n-surface.test.js, which derives from plugin.json, and check 6 supplies the bump-occurred axis. Found nothing there. What I could not find a falsifier for: the global-state restoration invariant (Task 4/5), the mechanism↔dev-manifest pairing, and the doc↔measured-mechanism agreement — all three are load-bearing claims left to human observation while adjacent axes were mechanized. |
| invariant | fail | 공격한 것: (1) §3.16 라운드 캡 — plan의 Codex Adversarial Review 절 서사(L979-1141)와 `.claude/state/review-rounds/` 실제 파일을 대조해 리셋 횟수 불일치와 자기모순 단언을 확인. (2) `## Validation` 13개 검사 전건을 '기구 사망 = 청결' 경로로 훑음 — 검사 1(ls-remote 빈 출력)·2·7·10·11·12의 rev-parse guard와 파이프라인 종료코드 함정은 R4~R6에서 이미 닫혀 있어 새 fail-open을 찾지 못했다. (3) 검사 6의 bump 판정(BUMP_TOUCHED가 git 실패 시 0으로 접혀도 HALT하므로 fail-closed). (4) 검사 13의 unknown 입력 — CRLF·빈 값·section 부재는 전부 HALT 방향이었고, 접두 일치 축 하나만 완화 방향이었다. (5) rollback reality — Task 2 백업의 복원 단언이 `## Validation`에 존재하는지 grep으로 확인해 부재를 확인했고, 검사 13이 `$HOME`을 읽는다는 사실로 '기술적 불가' 반론을 스스로 반증했다. (6) ALLOWED 접두 규칙이 게이트 자기 산출물 디렉토리를 통째로 허용하는 범위를 원장 경로에 적용해 봄. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 216562,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:16f6a4587dca4ee5627829d410c8f63cc23c5822bc8adcbdd99d711512b58914",
  "plan_path": ".claude/plans/release-channel-separation-m2.plan.md",
  "recorded_at": "2026-09-02T08:18:30.840Z"
}
```
