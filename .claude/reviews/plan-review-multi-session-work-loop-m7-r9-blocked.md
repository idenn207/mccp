# Plan Review Panel — multi-session-work-loop

**Plan**: `.claude/plans/multi-session-work-loop-m7.plan.md` · **Plan version**: `sha256:16a1e40a02ca3bc029fddd2df4fd67fb539e0901184df68cedc006bead247a72`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 9 blocking finding(s): security/CRITICAL, security/HIGH, security/HIGH, security/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| security | CRITICAL | Coverage gate specification is incomplete: Task 7 requires scanning two surfaces (finding surface `.claude/reviews/` AND registry path `.claude/state/findings/`) but only specifies approval logic for the finding surface, leaving registry path enforcement unspecified and unverifiable | Plan line 262-263: 'Task 7는 2축이다. **정적 lint** — finding을 표면화하는 writer의 승인 레지스트리(record.js·plan-codex-runner.js·seal.js)를 두고, 레지스트리 밖에서 finding 표면에 쓰는 코드가 유입되면 실패한다. 덮는 표면은 **둘**이다: finding 표면(`.claude/reviews/`) 그리고 **레지스트리 경로(`.claude/state/findings/`)** — 후자가 없으면 Task 1의 "단일 초크 포인트" 주장이 기계적 근거를 잃는다(승인 writer는 findings-registry.js 하나뿐이고, 그 밖에서 레지스트리 경로에 write하는 코드는 초크 포인트를 우회해 정규화되지 않은 절대경로를 실을 수 있다).' Task 7 section does not specify which files/functions are approved to write to `.claude/state/findings/`, nor which static lint patterns will catch violations of this constraint, unlike the B2 gate example which lists APPROVED_WRITERS and APPROVED_PREFIXES. The plan references B2 pattern as model but task 7 adds no concrete implementation specification for the registry path scanning. |
| security | HIGH | Path normalization trust boundary relies on unenforced API contract: plan claims single chokepoint in `appendFinding()` but does not specify how to prevent three separate emit points (plan-review/cli.js:663, plan-codex-runner.js, santa/seal.js) from bypassing it via direct registry writes | Plan line 225 states 'repo-relative로 정규화해 기록한다' and identifies caller 'emits from three places', but line 225-226: '호출자 책임으로 두면 emit 지점 3곳 중 하나만 빠져도 절대경로가 새고, 그 누락을 잡을 지점이 없다' — acknowledges this gap but proposes Task 7 coverage gate as mitigation. However Task 7's coverage gate specification (line 262+) does not enumerate `.claude/state/findings/` writers or detection patterns for preventing direct writes to registry path, only stating 'that coverage is needed'. Without explicit approval list and lint rules, the chokepoint claim is design-only, not mechanically enforceable. |
| security | HIGH | Seq monotonicity under concurrent access not protected: plan requires 'per-work_unit 단조 seq' (line 126) for gap detection but specifies no synchronization mechanism if M3 claim TTL fails | Plan line 126: 'work_unit별 단조 seq' used for loss detection. M3 claim TTL at 15min (line 61-62 of plan-codex-runner.js). But plan Tasks 1-8 never specify locking, atomic seq generation, or what happens if two sessions acquire same work_unit. If claim TTL lapses or races, concurrent append calls to same `.claude/state/findings/<work_unit>.jsonl` could generate duplicate or out-of-order seq values, breaking the gap-detection invariant that depends on seq monotonicity. |
| security | MEDIUM | `cited_path` field truncation at 256 chars breaks 2-key matching fallback: plan requires `(perspective, cited_path)` as secondary matching key (DD9) but applies per-field 256-char cap from msw-events pattern (line 53), creating silent failure when paths exceed cap | Plan line 144: 'matchKey를 노출' requires 2-key (perspective, cited_path); line 53 mirrors 'per-field 256자 cap' from msw-events.js. If real repository paths exceed 256 chars (e.g., nested node_modules or deep `.claude/` structures), `cited_path` truncation creates indistinguishable records. Two findings with distinct full paths will have identical truncated cited_path, causing 2-key matching to fail and silently falling back to finding_id-only match. This reopens the finding_id collision window that the 2-key design intended to mitigate. |
| security | MEDIUM | `finding_id` collision vulnerability with 64-bit hash insufficient for large-scale accumulation: plan uses sha256[:16] = 64 bits = 2^32 collision birthday bound, risking collisions in thousands of findings across sessions | Plan line 225: 'finding_id는 (work_unit, gate_id, perspective, severity, normalizedClaim)의 sha256 앞 16자'. Bitcoin and NIST recommendations for collision resistance suggest 128 bits minimum for audit contexts. With planned multi-session accumulation and PRD stating 'gateway_verdict' must be measurable long-term, 64-bit hash makes birthday collisions feasible within year-scale operation. Plan states 2-key matching (perspective, cited_path) provides mitigation but provides no guidance on collision handling, partition strategy, or cryptographic guarantee. |
| invariant | HIGH | Task 2-3 동시 착지 강제가 산문에 의존하여 CI/CD 없이 분할 commit 가능 | Plan line 148: 'DD10의 coverage gate가 정적 co-presence 검사를 함께 소유한다' vs Line 232: 'Task 2 test가 붉어지는 pre-commit 장벽' (optional)과 'Task 7이 만들어 그 이후 트리만 본다' — CI/CD gate 없이 Task 2만 commit되면 Task 3과 분리된 상태로 배송 가능. Task 7 gate는 post-ship 수동 명령. |
| invariant | CRITICAL | .gitattributes update 책임이 Task 1 선언과 TDD Ownership 표 사이 미해결 | Plan line 225 (Task 1 Action): '.gitattributes에 merge=union을 넣는 것도 이 Task의 작업이다' vs Line 178 (Files to Change): 담당 Task 없음 / Line 195 (TDD Ownership): .gitattributes 미등록. Plan line 227 Task 1 Validation이 git check-attr merge를 검사하려 하지만 현재 .gitattributes에 merge=union 절이 없음 (확인: worktree의 .gitattributes는 EOL 규칙만 있고 findings glob merge=union 절 부재). Task 1 실패 시 Task 7 gate C1-GATE-MERGE-UNION이 나중에 fail하지만 이미 Task 1은 shipped. |
| invariant | HIGH | Task 7 정적 lint의 기계적 강제 메커니즘이 미지정 — 패턴만 인용 | Plan line 262: 'APPROVED_WRITERS registry를 두고' — b2-coverage-gate.js 선례 인용(line 263). 하지만 M7용 c1-coverage-gate.js 신설 (line 160)이 어떤 APPROVED_WRITERS를 어떻게 강제하는지 코드 수준 상세 없음. 예시: plan-codex-runner.js에 emit 호출을 추가해야 하는데, 그것이 APPROVED_WRITERS에 자동 등록되는가? 수동 허용목록인가? c1-coverage-gate.js가 그 목록을 검사하는가? 현재 코드는 존재하지 않고 (Glob 결과: No files found), plan은 '정적 lint가 실패한다'고 선언하지만 기제를 명시하지 않음. |
| invariant | HIGH | Task 4 emit 실패가 fail-open으로 설계되어 레지스트리 파일 부재 시에도 호출자 exit code 0 | Plan line 337 Risk: 'emit 배선이 게이트를 차단' mitigation — 'fail-open + loud warn을 계약으로 고정하고 C1-EMIT-FAILOPEN이 호출자 exit code 불변을 단언' (line 337). Plan line 247: 'emit throw가 호출자 exit code를 바꾸지 않음'. 결과: `/mccp:plan` 또는 `/mccp:prp-implement`이 registry append 실패해도 호출자는 exit code 0을 받음. Acceptance 체크박스 (line 352-353) 자동화 안 됨 — '파일 부재는 배선 실패이며 test green으로 대체되지 않는다'는 선언이지 기계적 차단이 아님. |
| invariant | MEDIUM | Acceptance gate가 인간 체크박스이므로 건너뛰어도 PR 통과 가능 | Plan §Acceptance (line 346-361): 모든 항목이 checkbox [ ]. Plan의 Validation 섹션 (line 274-316)은 모두 수동 명령. PR gate는 receipt 게이트만 검사하고 Acceptance 항목을 자동 확인하지 않음. 누군가 'C1이 computed로 뒤집힌다' (line 350) 체크박스를 건너뛸 수 있음 — plan이 요구하지만 자동 enforcement 없음. Acceptance line 356: '판정 권한은 이 명령에 있다'는 서술이지만 명령 실행은 강제되지 않음. |
| invariant | MEDIUM | 같은 work_unit의 샤드 dedupe 논리가 last-write-wins 외 미명시 | Plan line 225: reader는 'finding_id로 dedupe하고 종결은 last-write-wins으로 접는다'. 하지만 같은 PRD의 여러 milestone plan (M1~M7)이 각각 다른 registry 파일을 갖는다 해도, 예를 들어 M1과 M2가 동일한 코드 이슈를 발견하면 같은 finding_id (work_unit + gate_id + perspective + severity + normalizedClaim hash)를 갖는다. dedupe가 last-write-wins이면 M2 결과가 M1을 덮는다. 그런데 이것이 의도인가, 버그인가? plan이 명시하지 않음. 특히 'M7이 세션 경계 피드백을 닫는다'는 의도와 충돌 가능 — M1에서 발견한 finding이 M2~M7을 거쳐 M7에서 다시 발견되면 제거 기록이 남지 않을 수 있음. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified plan citations to source code (SOURCE_SCANNERS in derive/index.js:27-51, ADJUDICATION_VERDICTS in intent-context.js:69-74, typeIntegrity check at msw-metrics/index.js:632, plan-review/cli.js I/O boundary at 663-664). Confirmed architectural claims: .claude/state/findings/ is NOT in .gitignore (plan correct that it needs git-tracking per DD4); slugFromPlanPath correctly extracts basename only (line 71 verified), making archived/active path distinction irrelevant; O_APPEND append safety mirrors msw-events.js pattern (lines 1-100 verified as concurrent-write safe). Checked structural invariants: Task 2-3 co-landing is enforced via Task 2's own test file containing C1-SOURCE-REGISTERED-COPRESENT assertion (rows 192, 209-217, 232 consistent); findings can only be emitted from 3 designated points (Task 4 lists them, Task 7 gate covers both .claude/reviews/ and .claude/state/findings/ surfaces). Verified sharding model: work_unit keyed by decision slug from PRD basename (line 71-74 of decision.js), so archive doesn't change slug (line 97 claim validated). Checked failure modes: emit fail-open + seq gap detection + degraded marker gives asymmetric loss coverage matching DD8 (batching prevents "말미 k개", runtime falsifier catches opened-loss, seq catches closed-loss conservatively). No circular dependencies, no abstraction leaks, no boundary violations found. Test specification for derived (not hardcoded) type_separation is named (C1-TYPE-SEPARATION-CONTRACT) but implementation details deferred to Task 2; this is acceptable for a plan-level document. |
| security | fail | Attacked: (1) path normalization chokepoint — checked whether three emit points could bypass appendFinding() normalization by reading plan-review/cli.js, plan-codex-runner.js outline, and santa/seal.js references. Plan relies on "single chokepoint" enforced by coverage gate, but Task 7 doesn't specify registry-path scanning. (2) seq monotonicity — reviewed plan-codex-runner.js 15-min claim TTL and M3 contract, found no explicit locking for seq generation. (3) `cited_path` truncation — traced through msw-events.js 256-char cap pattern and plan's DD9 2-key matching strategy; confirmed plan applies allowlist truncation to all fields including cited_path. (4) finding_id collision — calculated 64-bit birthday bound from plan's sha256[:16] design; confirmed no guidance on collision resolution. (5) coverage gate specification — read B2 gate example (APPROVED_WRITERS registry + lint patterns) and compared to Task 7 spec; found Task 7 names requirement but leaves `.claude/state/findings/` enforcement completely unspecified — no approved writer list, no static lint rules, no runtime validator mentioned for registry path. Scenario not landed: repo write attacker (out of scope per plan threat model), single-operator token leakage via allowlist bypass (mitigated by field filtering in msw-events mirror). |
| test | pass | I systematically examined: 1. **Test file existence and paths**: Verified that new test files (`findings-registry.test.js`, `c1-feedback-loop.test.js`, `c1-coverage-gate.test.js`) will be created by their owning tasks, and that existing test files (msw-metrics.test.js, msw-metrics-acceptance.test.js) exist and will be updated. The Assertion Roster provides a machine-readable contract that the assertion-manifest-check.js tool will mechanically verify post-implementation. 2. **DD5 contract falsifiability**: Confirmed that Task 2's change from `(deferred + downgraded + rejected) > 0` to `type_separation === true` is verifiable through test assertions `C1-TYPE-SEPARATION-CONTRACT` and `C1-TYPE-COLLAPSE-REJECTED`. The plan constrains this change atomically with Task 3 via pre-commit test barrier: Task 2's own test asserts `SOURCE_SCANNERS.findings` exists, so Task 2-only commit will fail red. 3. **.gitattributes merge=union requirement**: Confirmed that `.gitattributes` currently exists but lacks the `merge=union` entry for registry files. Task 1 will add this. Validate commands (lines 301-303) use `git check-attr` (not text grep) to verify the setting actually applies to both existing and future filenames, which correctly tests the glob pattern. 4. **Registry emit validation**: Confirmed that Task 4's finding emit from three sources (plan-review/cli.js, plan-codex-runner.js, santa/seal.js) is validated by Task 7's coverage gate, which does both static lint (APPROVED_WRITERS registry) and runtime falsifier (comparing `.claude/reviews/` surface deltas to registry events). The two-axis design correctly addresses that static analysis cannot see dynamic paths. 5. **Finding ID derivation**: Task 1 owns the normalization logic for `finding_id` (sha256 of normalized claim). The plan acknowledges collision risk but controls it via tuple uniqueness: `(work_unit, gate_id, perspective, severity, normalizedClaim)`. No gap found since finding_id is an internal implementation detail, not a load-bearing assumption. 6. **Backward compatibility**: Existing C1 test fixture at msw-metrics.test.js:416 will be updated with `type_separation: true`, and msw-metrics-acceptance.test.js currently lists C1 in forward-only (line 194). Task 8 will move C1 to claimed-computable. No regression path identified. 7. **Validate command realism**: All paths in Validation section (lines 274-316) either exist (msw-metrics-acceptance.test.js, derive/cli.js subcommands) or will be created by their tasks (test files, assertion manifest, coverage gate). The `git check-attr` and `test -s` commands are real shell operations that will work post-implementation. 8. **Atomic Task 2-3 landing**: Confirmed that the enforcement mechanism is sound: Task 2's test file will assert `SOURCE_SCANNERS['findings']` exists (C1-SOURCE-REGISTERED-COPRESENT). This creates a pre-commit red barrier if Task 2 lands without Task 3, making separate landing detectable. |
| invariant | fail | 균형 검사 과정: (1) Task 간 순환 의존성 — Task 2-3 동시 착지 강제 메커니즘 검토 (pre-commit vs CI/CD). (2) Task ownership 불명 — .gitattributes 책임 추적 (Files to Change / TDD Ownership / 현재 worktree 상태). (3) 정적 gate 기제 — Task 7 c1-coverage-gate.js 존재 여부 및 APPROVED_WRITERS 메커니즘 (Glob: 미존재). (4) emit fail-open — Task 4의 exit code 설정 검토 및 Acceptance 자동화 확인. (5) dedupe 논리 — 같은 work_unit 다중 샤드의 last-write-wins 해석 및 의도 일관성. 모든 검사에서 산문 선언과 기계적 강제 사이의 gap을 특정했고, plan이 존재하지 않는 코드를 인용하거나 선택적 pre-commit 검사에 의존하는 부분을 확인했다. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 296510,
  "halt_stage": "5.2e",
  "granted": 4,
  "reviewed_plan_hash": "sha256:16a1e40a02ca3bc029fddd2df4fd67fb539e0901184df68cedc006bead247a72",
  "plan_path": ".claude/plans/multi-session-work-loop-m7.plan.md",
  "recorded_at": "2026-08-17T15:41:45.606Z"
}
```
