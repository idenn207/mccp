# Codex Findings Backlog (defer-to-later)

Append-only log of Codex findings classified DEFER_TO_BACKLOG by YAGNI triage
(v0.2.9+ gate policy — see [docs/gate-design.md](../../docs/gate-design.md) §Divergent auto-rerun).

Reviewed quarterly OR when a new milestone consciously elects to absorb.

| Date | Severity | Source plan | Finding |
|---|---|---|---|
| 2026-06-19 | HIGH | v1.3.0 cycle retro | STATE.md body 자동 roll 부재. v1.3.0 cycle M1-M4 6건 PR이 STATE.md body를 미갱신 → main이 M0 stale 상태로 25일 잔존. v0.3.6 content-hash skip(state-writer.js:554 HASH_EXCLUDE_FRONTMATTER_KEYS)은 churn 차단 목적으로 정상 작동 — 유지. 후보 axis: `pr.md` Phase 1 VALIDATE에서 frontmatter.last_pr_url vs `gh pr list --base main --state merged --limit 1` 비교 → mismatch 시 stderr WARN(또는 BLOCK). 대안: `/mccp:work` post-merge hook으로 body roll 자동 propose. v1.4.x cycle axis 후보. |
| 2026-06-19 | MEDIUM | .claude/plans/v1-4-0-multi-session-m1-continuity-primitive.plan.md | F4 heartbeat-based active reclaim (Codex Implement R1, partial absorb at M1). M1은 `activeOnly:true`에 24h TTL cutoff 적용 → crash-orphan 최대 24h false-active. M2가 heartbeat (lease/last_seen_at refresh) 기반 정확 reclaim 추가해야 — pid-aware + host-aware tri-state policy (state-writer.js §3.6 PR-phase-lock 패턴 mirror). 후보 location: session-ledger.js#listLedgers의 activeOnly filter 확장 + SessionStart hook이 자신의 ledger heartbeat. **ABSORBED in v1.4.0-m2 (PR #46)** — host-aware tri-state reclaim shipped via M2 session-ledger.js#listLedgers + heartbeat schema v2. row 보존(audit trail). |
| 2026-06-20 | LOW | .claude/plans/v1-4-0-multi-session-m3-friction-zero.plan.md | F1 deferred 부분 — friction-telemetry sidecar offline retention. M3는 in-band cap을 race 안전성 위해 제거(append-only invariant) + retention을 backlog로 이월. 후보 axis: `plugins/mccp/scripts/lib/friction-telemetry.js`에 `cleanupOldEvents({maxAgeMs})` offline tool + cron-style invocation (또는 `/mccp:trace` 진입 시 lazy GC). 운영 관찰: M3 cycle 종료 후 `.claude/state/m3-friction-events.jsonl` 크기 < 5KB 예상 — 즉시성 낮음. v1.5.x cycle 또는 분기별 quarterly review에서 absorb. |
| 2026-06-22 | MEDIUM | .claude/plans/detector-availability-signal-redesign.plan.md | F2 detector available 판정 전 로컬 CLI 버전 체크(v2.1.154+/v2.1.139+) 추가 — 현재 무시(낙관), 실행 시 Claude Code가 차단 |
| 2026-06-25 | MEDIUM | .claude/plans/dashboard-truthfulness-m5-semantics.plan.md | F4 위험 소스 lifecycle scope. Task 2는 rail 위험을 위험 섹션(plan-body active=unmarked)과 통일해 rail↔섹션 일치를 보장하나, 섹션 자체가 완료 plan의 unmarked 위험을 lifecycle 무관하게 집계 → 완료 plan의 historical risk가 live 위험으로 표시될 수 있음. M5는 rail↔섹션 정합(표시값 일관)까지만 닫고, 위험 소스를 current/in-progress plan scope로 한정하거나 completed-plan unmarked rows를 historical/carried-over로 명시 버킷하는 것은 후속 truthfulness axis(M6 후보). 후보 location: sections/risks.js 위험 enumerate에 plan lifecycle filter + status-grid rail 소비. 가드: 완료 plan의 unmarked 위험이 live rail 카운트를 부풀리지 않는 negative test.
| 2026-06-25 | HIGH | .claude/plans/dashboard-truthfulness-m7-next-action.plan.md | F2 deferred (heavy coverage) — ledger 승격 over-claim의 cheap freshness guard(decisionId+basename full-match + plan_file_hash 일치)는 M7 Task 1에서 흡수했으나, 완전한 terminal-gate fanout coverage 검증(모든 terminal 게이트가 ledger에 존재) + commit-ancestry 확인(ledger가 현재 HEAD ancestor commit에서 나왔는지)은 defer. 후보 location: parsers/decision-state.js ledger promotion + isMilestoneClosed coverage 확장. 가드: bundled-PR multi-slug ledger에서 일부 slug만 close됐을 때 나머지 slug over-claim 0 negative test. v1.x dashboard-truthfulness M8+ 또는 ledger commit-wiring 부채와 함께 absorb.
2026-06-25 | MEDIUM | .claude/plans/dashboard-truthfulness-m8-risk-lifecycle.plan.md | F2: unknown-lifecycle risks(~16/23)를 confirmed-active와 분리 — activeKnown/lifecycleUnknown rail breakdown(degraded provenance)
2026-06-29 | MEDIUM | .claude/plans/design-grounding.plan.md | F3-heavy: second fully-validated post-EXECUTE receipt (diff baseline hash + result hash) instead of field-preserving re-stamp — deferred, lightweight re-stamp absorbed in M1
2026-06-29 | MEDIUM | .claude/plans/design-grounding.plan.md | F4-full: make ALL captured machine-checkable signal grounding (not just declared requiredSignals) an enforce-mode block — deferred, requiredSignals subset absorbed in M1
2026-06-29 | HIGH | .claude/plans/design-grounding.plan.md | Implement-Codex F1-residual: full-file/JSX-aware H17 (nested-card) grounding on reconstructed post-change rendered files — added-line buckets cannot support DOM open-tag stack + class= matcher misses JSX className=. Deferred; M1 narrowed GROUNDING_RULE_IDS to ['H15'] (line-local-safe) + H17 stays owned by renderer full-HTML lint path.
2026-07-01 | MEDIUM | .claude/plans/dashboard-readability-m3.plan.md | Codex R1 F2: 비-renderer user-facing verdict emitter(state/fix-task.js:63 "Codex divergent — review concerns", hooks/stop-review-loop.js:357 "Stop-loop: Codex divergent (NR)")를 verdict-label.js helper로 라우팅 통일 — M3는 대시보드 renderer scope 한정, 대시보드 밖 recovery/CLI 표면은 별도 cycle에서 흡수.
2026-07-08 | MEDIUM | .claude/plans/audit-remediation-p5-receipt-hash-tamper.plan.md | **[ABSORBED → v1.22.6 integrity-unification M2 Task 1]** subject_hash mismatch도 stale→'regenerate' 가이드를 받아 tamper 증거 소실 위험(receipt_hash와 동일 잠복 결함) — subject_hash tamper 처리를 receipt-tamper kind로 통일 검토(P5 out-of-scope, Codex R1 F1 파생)
2026-07-08 | MEDIUM | .claude/plans/workflow-orchestration-m2b-nworker-parallel.plan.md | Plan-Codex R1 F4(review 축): 단일 workflow-native merged-diff adversarial review(worker 밖에서 통합 diff 1회 LLM verify) — M2b는 post-merge integrated `node --test` 게이트(F4 test 축)까지 흡수, merged-diff *review*는 dual-review 재설계라 M3(verify 네이티브화)로 이연. 후보 location: workflows/implement-dispatch.js aggregate 스테이지에 adversarial-verify agent() 추가 (research §7 verification-absent 패턴). **ABSORBED in M3** (`workflow-orchestration-m3-verify-native.plan.md` — aggregate adversarial-verify 스테이지, 단일·병렬 양 경로). row 보존(audit trail).
2026-07-08 | MEDIUM | .claude/plans/workflow-orchestration-m3-verify-native.plan.md | Plan-Codex R1 F3 sub-scope: merged-verify verdict(`meta.merged_verify_verdict`)를 PR-chain-check enforcement로 전파(divergent가 /mccp:pr 단계에 survive해 PR 차단) — M3은 runtime verify-decide HALT(commit 전 차단)로 enforcement 확보, PR-phase 전파는 cross-gate dedupe·dual-review 재설계 표면이라 별도 axis로 이연. 후보 location: receipt/dedupe.js + pr.md chain-check에 mccp-implement-verify verdict 소비.
2026-07-08 | HIGH | .claude/plans/audit-remediation-p6-doc-reconciliation.plan.md | Plan-Codex R1 F2: quarantine `releaseLock`(v0.2.8-generic-receipt-quarantine.js:249-254) no-token legacy 경로 — token 미전달 시 ownership 검증 없이 unlink(loud warn). 현 `migrate()`는 항상 token 전달이라 트리거 caller 부재하나, 경로 제거/test-gate hardening 이연(P6는 doc-only로 잔여 리스크 정직 서술만, code hardening out-of-scope).
2026-07-08 | MEDIUM | (pre-existing, discovered during audit-remediation-p6) | renderer verdict-label.test.js:137 "receipt/worktree verdict 필드 새 어휘" FAILS on origin/main baseline (assert receiptKeys.length===3) — 확인: 테스트/verdict-label.js가 main과 diff 0, footer 편집 stash 후에도 실패 → P6 무관 pre-existing. drawer-detail/verdict-label 회귀로 별도 cycle에서 조사.
2026-07-09 | HIGH | .claude/plans/cost-model-subscription-m1.plan.md | M1 subscription fail-open guard: require fresh-context + calibrated 2nd (tool/turn) threshold before bypassing USD gates for expensive consumers (Codex F1 accepted-risk → M2 harness-cost axis)
2026-07-09 | MEDIUM | .claude/plans/cost-model-subscription-m1.plan.md | context-state session sticky-critical flag (Codex F2 defer — out-of-order handled by writer context_ts; sticky-across-recovery is M2)
2026-07-09 | MEDIUM | (pre-existing, discovered during workflow-orchestration-m4) | validate-callsite-lint.test.js:137 FAILS — `plugins/mccp/commands/pr.md:165` PRECHECK `cli.js validate --command mccp:pr --decision "$DECISION_SLUG"`가 `--plan` 누락(v1.3.1 callsite 계약 위반). 확인: pr.md가 origin/main과 diff 0(마지막 변경 601f629 v1.20.3 #86) → M4 무관 pre-existing. M4 Files to Change에 pr.md 없음(dedupe diff⊆Files 불변식상 이 cycle 수정 시 scope 확장) → 별도 cycle에서 pr.md:165에 `--plan "$ARGUMENTS"` 추가.
2026-07-15 | MEDIUM | .claude/plans/workflow-orchestration-live-activation-m3.plan.md | plan-conflict-detector.js#normalizePath가 백틱을 제거하지 않아 parseFilesToChange는 `path/f.js`(백틱 포함)를, isInPlan은 bare diff 경로를 비교 → 영구 미매칭. 따라서 detectFromFileExpansion은 non-empty 파일 목록을 받으면 변경 파일 전부를 "unplanned"로 보고한다. 오늘 latent인 이유는 command body가 git diff --name-only origin/<base>..HEAD를 넘기는데 게이트가 커밋 이전에 돌아 이 값이 항상 빈 값이기 때문(→ 검사가 조용히 미실행). 두 결함 한 축: (a) normalizePath 백틱 stripping, (b) axis-H file-expansion guard가 유일 call site에서 사실상 dead. M3 implement 중 detector를 working tree에 돌리다 표면화(백틱 제거 시 conflict:false로 21개 전부 planned 확인).
2026-07-15 | HIGH | (pre-existing, discovered during workflow-orchestration-live-activation-m3 PR gate) | codex-runner.js가 PR-Codex verdict를 구조적으로 읽지 못한다. codex-invoke.js ok-path envelope는 `{ok, stdout, stderr, classification, blocking, advisory, durationMs}`이고 review 본문은 전부 `.stdout`(free-form JSON 텍스트)에 있는데, codex-runner.js:241은 존재하지 않는 `codexJson.summary || codexJson.conclusion`을 읽어 `codex_summary`가 **항상 공백**이고, filterDesignFindings(codex-result-filter.js:83)는 존재하지 않는 `codexResult.findings`를 읽어 malformed-input 경로로 빠지므로 `codex_actionable_findings`가 **항상 false**다. 결과: (a) PR body `## Codex Adversarial Review`의 합치 결론이 항상 비고, (b) `--codex-actionable-findings` non-approving 경로가 절대 발동 안 해 PR-Codex가 divergent/needs-attention verdict를 조용히 rubber-stamp한다. **실측**: M3 PR에서 codex-runner는 actionable=false를 보고했으나, 동일 diff에 codex-invoke를 직접 돌려 `.stdout`을 파싱하니 `verdict:"needs-attention"` + "No ship" + HIGH finding(confidence 0.92)이었다. plan.md:691 / prp-implement.md:249는 `JSON.parse(...).stdout` → `codex-bridge.parseVerdict`로 올바르게 읽는다 — PR 게이트만 누락. 수정: codex-runner가 `.stdout`을 파싱해 `result.verdict`/`result.summary`/`result.findings`를 추출하고, verdict !== 'converged'(또는 findings 존재) 시 codex_actionable_findings=true로 승격. dedupe의 codex_verdict fail-closed 계약(v1.20.3)과도 정합.
2026-07-15 | RESOLVED (live-activation-m3-pr-codex-absorption, F1) | MEDIUM | .claude/plans/workflow-orchestration-live-activation-m3.plan.md | (Implement-Codex R1 F2, DEFER) codex-runner의 isActionable()은 filtered findings보다 verdict를 먼저 보므로, companion이 **scope-excluded design finding만**을 근거로 non-approve verdict를 내면 codex_findings=[] 인 채 PR이 차단된다 — design-scope 제외/routing 계약과 충돌하고 실패가 불투명(차단 사유가 PR body에 안 보임). 현 동작은 보수적 차단이라 안전 측이고, DESIGN_SCOPE_PREAMBLE이 애초에 design finding 배출을 억제하므로 발생 조건이 좁아 M3에서 이연. 수정 방향: filtered structured review로 actionability를 평가하되 no-findings fail-closed는 보존하고, 전 finding이 scope filter에 drop된 경우를 별도 상태로 표면화(회귀 test 동반). Codex 권고: "Evaluate actionability from the filtered structured review, not the original verdict alone."
2026-07-15 | MEDIUM | (pre-existing, discovered during v1.22.3 M3 PR-gate follow-up) | `--apply-fix-task`는 유령 플래그다 — dispatch는 되지만 소비자가 없다. `state-resumption.js:142`(Row 2: handoff_spawn + unsafe checkpoint + fix-task pending)가 `{command:'/mccp:prp-implement', args:'--apply-fix-task'}`를 반환하고 `resume.md:171`이 `Skill(mccp:prp-implement, args="--apply-fix-task")`로 그대로 forward하는데, `prp-implement.md`에는 이 플래그 파싱이 **전무**하다: L120 `cat "$ARGUMENTS"` → `cat "--apply-fix-task"` → "No such file" → Phase 1 LOAD 에러 경로. ARGUMENTS를 plan path로 가정하는 곳이 전부 동일하게 깨진다(L511/L564/L714 `derive-decision --args`, L765 `--plan "$ARGUMENTS"`, L806 `${ARGUMENTS}.delegations.jsonl`, L807 `hash-markdown`). 즉 `/mccp:resume`의 Row 2 경로는 구조적으로 완주 불가. 실패는 loud(LOAD 에러)라 silent corruption은 아니고 발생 조건도 좁지만(handoff_spawn ∧ unsafe ∧ fix-task pending), fix-task.md가 존재하는 지금 live하다. 수정 방향(택 1): (a) prp-implement에 `--apply-fix-task` 모드 구현 — ARGUMENTS가 이 플래그면 plan 대신 `.claude/state/fix-task.md`를 소스로 삼는 분기, (b) state-resumption Row 2를 실재하는 명령으로 재매핑. `state-resumption.test.js:52`가 dispatch 문자열만 검증하고 수신측 파싱은 검증 안 해 test green인 채 dead — M3에서 교정한 "test stub이 실제 producer가 아니라 구현 가정을 인코딩" 패턴과 동형.
2026-07-15 | RESOLVED (live-activation-m3-pr-codex-absorption, F1 전제 복구) | HIGH | (pre-existing since v0.3.6, discovered during this cycle's implement gate) | codex-result-filter.js가 실제 producer finding을 구조적으로 매칭하지 못해 impeccable 가용 시에도 필터가 **항상 identity**였다. 실제 companion(codex 1.0.4 render.mjs#normalizeReviewFinding)은 finding을 `{severity,title,body,file,line_start,line_end,recommendation}`로 고정 emit하고 codex-invoke.js는 이를 무변형 통과시키는데, findingMatches(codex-result-filter.js:70)는 `finding.category`/`finding.text`만 읽는다 — 둘 다 실 payload에 **부재**. 따라서 design/a11y finding이 하나도 drop되지 않았고 receipt audit 4필드(design_findings_dropped/a11y_routed_to_impeccable/dropped_findings_digest)가 구조적으로 0/false/null이었다. **실측**: codex_design_scope_excluded=true인 receipt 18개(v0.3.6~현재) 전부 dropped=0. 원인 계열은 M3의 `.stdout` blindness와 동일 — 모듈 자체 test가 producer 계약이 아니라 구현 가정({category,text} 손수 fixture)을 인코딩해 suite green인 채 프로덕션 blind. 본 사이클에서 발견 즉시 수정: MATCH_FIELDS에 `title` 추가(back-compat로 category/text 유지) + DIGEST_FIELDS로 digest 해소 + 실 producer 형태 회귀 test 7건 고정. `body`/`recommendation`은 **의도적 미매칭** — false-drop은 in-scope finding을 조용히 제거해 게이트를 약화시키고 false-keep은 fail-closed라, 비대칭 비용이 recall보다 우선. F1(scope-excluded verdict)은 이 수정 없이는 dropped>0이 성립 불가라 dead code였을 것.
2026-07-15 | RESOLVED (live-activation-m3-pr-codex-absorption, F5 4th gate) | HIGH | (pre-existing since v1.20.12 M3, discovered during this cycle) | `implement-dispatch/verify.js#deriveVerdictFromCodex`(aggregate merged-verify 게이트, `MCCP_WORK_MERGED_VERIFY` **default enforce**)가 `codex-bridge.parseVerdict(stdout)`으로 verdict를 파생 → F5와 **동일 계열 blindness**. 실제 producer는 `.stdout`에 구조화 JSON(`.result.verdict`)을 담는데 free-text 키워드 스캔이 이를 산문으로 읽는다. **실측**: 본 사이클 실제 Plan-Codex R1(verdict=`needs-attention`, "No ship", findings 4)에 대해 `converged` 반환 — finding 본문의 "converged로 찍으면 무결성 버그"라는 경고문을 키워드로 오인. 결과: `/mccp:work`의 통합 verify가 commit 직전 "No ship" 리뷰를 고무도장. M3가 PR 게이트에 대해 고쳤다고 주장한 그 결함이 4번째 게이트에 잔존했다(M3 codex-runner 주석이 plan/implement를 "already correct"로 오기한 것과 동형). 수정: `codex-review-payload.deriveGateVerdict`(구조화 우선 → free-text fallback은 divergent만) 소비로 전환. auto-CRITICAL 탐지는 raw text 우선 순서 유지(escalate 방향이라 무해). test fixture도 bare prose(`stdout:"converged"`)에서 실 producer envelope로 교체 — 그 fixture가 존재하지 않는 입력으로 스캔이 동작함을 증명하고 있었다(이번 사이클 반복 패턴). 회귀: 실측 R1 payload → divergent + block=true.
2026-07-16 | MEDIUM | (residual of live-activation-m3-pr-codex-absorption F2, 5th round) | fan-out debt 마커는 "떴다"를 아는 시점(Workflow 반환 후 `FANOUT_ACTUAL_N>0` 확정)에 찍히므로, fan-out이 lease(`MCCP_ORCHESTRATION_RESERVATION_LEASE_MS`, default 10분)를 **초과하는 동안** 다른 dispatch의 write-side prune(`reserveWorkers`/`reconcileReservation`이 만료 pending을 제거하고 persist)이 먼저 실행되면 항목이 이미 사라져 이후 reconcile이 raw에서도 못 찾고(no-op) debt로 고정할 대상도 없다 → 실제로 뜬 agent가 미카운트(over-permissive). Codex가 지목한 경로(reconcile 3회 실패 → pending 잔존 → lease prune)는 debt 마커로 닫혔고 이 잔여는 더 좁다(≥2개 동시 dispatch ∧ fan-out > 10분). 수정 후보: (a) reserve 시점에 debt를 찍고 reconcile이 청소 — 확실히 덮지만 아무것도 안 뜬 경우 유령 예약이 되어 R1 F3가 고친 자기중독이 되살아나므로 fan-out 한정 + 짧은 repair 경로 필요, (b) `reconcileReservation`이 "entry 부재 ∧ actualN>0"을 no-op이 아니라 재가산으로 처리 — 문서화된 idempotency(중복 호출 무해)와 상충하므로 호출 1회성 보장 필요, (c) in-flight 예약 heartbeat(`open[].at` 갱신) — Workflow 호출이 셸을 블록해 현 구조상 불가. 어느 쪽도 명백한 우위가 없어 이연.
2026-07-21 | HIGH | (residual of live-activation-m3-pr-codex-absorption — PR-Codex R1 8th round 최종 리뷰, DEFER) | **[ABSORBED → v1.23.0 integrity-unification M3]** terminal `/mccp:pr`에 non-approving PR-Codex 결과에 대한 mechanical hard-stop이 없다. `pr.md:461`이 스스로 명시한다 — "`codex_actionable_findings` has **no mechanical hard-stop** here (this body only parses it, and validate-cmd does not gate on it)". **실측 재현**: receipt `mccp-pr-codex/live-activation-m3-pr-codex-absorption.json`이 `resolution.codex_verdict='divergent'`(Codex raw = `needs-attention`, "No-ship", HIGH 1 + MEDIUM 1)인데 `cli.js validate --command mccp:pr --decision <slug>` → `{ok:true, blocking:[], missing:[], stale:[]}` exit 0. `codex-runner.exit`도 0. 즉 v1.22.3 M3가 복구한 verdict 파서는 terminal 게이트에서 **audit-only**다. 현재 유효한 차단은 (a) receipt가 `divergent`를 봉인 → cross-gate dedupe fail-closed(`dedupe.js#evaluateForDedupe`)로 **다음** `/mccp:pr`이 PR-Codex를 재실행, (b) LLM/운영자가 판정을 자발적으로 존중하는 것 — 둘 다 이번 호출을 막지는 않는다. 이 설계는 의도적이다(`pr.md:461-466`이 "Do not describe it as 'the PR is blocked'"까지 명시). 수정 방향: `validate-cmd.js`가 `mccp-pr-codex` receipt의 `resolution.codex_verdict !== 'converged'`를 blocking으로 승격 + audited override env(`MCCP_FORCE_PR_WITHOUT_CODEX_CONVERGENCE="<reason>"`, 기존 strict reason validator 재사용). **본 M3 scope 밖으로 이연** — M3는 runaway backstop 축이고 이건 terminal 게이트를 advisory→mechanical로 승격하는 독립 재설계다. 이 축을 즉시 흡수하려는 시도가 8라운드 비수렴 루프(07-15 14:13 ~ 07-17 02:25, 11커밋)의 직접 원인이었다: 강제 메커니즘을 적대적으로 리뷰하면 우회 표면(env opt-out · lock-fail · crash window · session key 불일치 · pre-clamp 경로 · reconcile skip)이 매 라운드 새로 노출된다. 별도 cycle에서 축 단위로 설계할 것.
2026-07-21 | MEDIUM | (residual of live-activation-m3-pr-codex-absorption — PR-Codex R1 8th round 최종 리뷰, DEFER) | fan-out debt 마커가 Workflow 호출 **전에** 찍히므로(`plan.md:289` mark-debt), "Workflow 미가용/미호출"이 명시적 지원 결과인데도 그 경로가 `fanout-result.json`을 쓰지 못하면 reconcile이 의도적으로 skip되고(`plan.md:339`) **0개 launch에 대해 예약이 영구 pin**된다. 세션 cap(`MCCP_ORCHESTRATION_MAX_AGENTS` default 24) headroom을 소모해 이후 실제 fan-out/work dispatch를 억제할 수 있다. 현 동작은 의도적 선택 — `plan.md:339`가 "guessing 0 would under-count a real launch and leave the cap over-permissive"로 보수적 over-count를 명시 채택했다. 잔여 리스크는 2.5.2 직후 artifact write를 **LLM에 의존**한다는 점(셸이 아니라 모델이 쓴다). 이 축은 본 사이클에서 이미 진동했다: `0f44cf8`(pre-launch 경계로 이동 + debt decay) → `eab8a65`(decay 제거 — 실 launch를 under-count) → `14b83de`(actual=0 lock-fail 시 false pin 청소). 반대 방향 수정이 3회 교차한 표면이라 단발 패치가 아니라 재설계 단위로 다뤄야 한다. line 35의 lease-prune residual(2026-07-16, 동일 debt-marker 축)과 **묶어서** 흡수할 것. 수정 후보: Workflow skip/unavailable 경로의 `{"invoked":false}` artifact write를 셸 레벨로 기계화(LLM 의존 제거) → "모르는 상태"와 "0임을 아는 상태"를 구분.
2026-07-21 | HIGH | (pre-existing, discovered while running the PR gate for v1.22.3 M3 / PR #107) | **LLM briefing stamp가 `/mccp:pr` 게이트 전체를 차단한다.** `finalize-receipt.js:269`가 `callReceiptCli(cli, writeFlags, { cwd, timeoutMs: 60000 })`로 receipt CLI를 spawn하는데, CLI 단독 실행은 **0.13초**인데도 매번 60초 `ETIMEDOUT` → **exit 127**. `pr.md` 2.5.7의 `FINALIZE_EXIT != 0` → `[MCCP-GATE-STOP]` 규칙에 걸려 **모든 `/mccp:pr`이 receipt 단계에서 hard-stop**한다. **실측(3/3 재현)**: default 2회 → exit 127 + ETIMEDOUT / `MCCP_BRIEFING=off` 1회 → exit 0 + 정상 JSON(`write_flags_used`에 `--codex-verdict divergent`·`--codex-actionable-findings` 정확 파생). 중요한 비대칭 — **receipt 자체는 매번 정상 기록**된다(`meta.created_at`이 호출마다 전진). 즉 실패 지점은 write가 아니라 **부모의 wait**이고, briefing의 "fail-open" 계약이 receipt *내용*에만 적용되고 **caller의 exit code에는 적용되지 않는다**. 원인 계열: receipt-write 경로의 capped LLM briefing 호출(v1.3.0-m2 `lib/briefing/{cost-guard,invoke,index}.js`)이 spawn된 비대화형 자식에서 LLM 채널 없이 블록. **왜 지금까지 잠복했나**: 이전 세션은 cost-state가 sticky critical($186.92 + hard_ceiling)이라 `MCCP_BRIEFING=auto`의 tier autoDisable이 briefing을 껐다. v1.22.0이 도입한 시간축 decay + 경과로 tier가 green($29.56)이 되면서 briefing이 재활성돼 행이 표면화됐다 — **decay 수정이 만들어낸 후속 결함**이다(고친 축이 다른 축의 은폐를 걷어낸 형태). 수정 방향: (a) briefing invoke에 caller timeout보다 충분히 짧은 자체 timeout + 만료 시 stamp 생략하고 **정상 반환**(exit code 오염 금지), (b) briefing의 fail-open 불변식을 "receipt 내용 + caller exit code" 양쪽으로 확장하고 회귀 test 고정, (c) LLM 채널이 없는 spawn된 자식 컨텍스트를 skip 사유로 승격(`pr-phase.lock` PR_PHASE_LOCKED skip의 형제 축). 우회: `MCCP_BRIEFING=off`(문서화된 §4 토글 — 리뷰를 약화시키지 않고 요약 stamp만 끈다). **게이트 전체를 막는 blocker이므로 별도 patch cycle 우선순위 최상위.**
2026-07-21 | MEDIUM | (pre-existing, discovered while running the PR gate for v1.22.3 M3 / PR #107) | `finalize-receipt.js`가 `--deferred-findings <N>`를 forward하지 않아 PR 게이트 경로의 `meta.deferred_findings_count`가 **항상 0**이다. `receipt/cli.js write`는 이 플래그를 지원하지만(`cli.js:21` usage), `finalize-receipt.js`는 `writeFlags`를 명시적으로 구성하며(L203~L266: gate/decision/plan/quiet + security-force-override / impeccable-skip / impeccable-silent-skip / pr-design-chain-skip만) deferred를 목록에 넣지 않는다 — passthrough도 없다. **실측**: PR #107이 PR-Codex R1 finding 1건을 `DEFER_TO_BACKLOG`로 triage했는데 receipt는 `deferred_findings_count: 0`. canonical audit(backlog 파일 + PR body `Deferred to backlog:` 줄)은 정확하므로 실질 audit 손실은 없으나, **receipt만 읽는 소비자**(derive `sources/receipts.js` → 대시보드, 향후 자동 분석)는 "이연 0건"이라는 거짓을 본다. `pr.md` 2.5.4는 DEFER_TO_BACKLOG 항목을 backlog에 append하라고만 지시하고 receipt stamp는 언급하지 않아 command body 쪽도 계약이 비어 있다. 수정 방향: `finalize-receipt.js`에 `--deferred-findings` 인자를 추가해 writeFlags로 forward + `pr.md` 2.5.7 플래그 구성에 forward 지시 추가 + triage 표의 DEFER 행 수와 stamp 값 일치를 검증하는 회귀 test.
2026-07-21 | HIGH | (pre-existing, discovered while opening the follow-up chore branch for PR #107) | **`derive-decision`의 implement-receipt fallback이 브랜치를 건너뛰어 남의 receipt를 덮어쓸 수 있다.** 신규 브랜치 `chore/gate-briefing-hang-backlog`(main 분기, 커밋 1개, backlog 2행 추가)에서 `cli.js derive-decision --command mccp:pr --args ""`를 돌리면 `live-activation-m3-pr-codex-absorption`을 반환한다 — **PR #107의 slug**다. 경고는 `[mccp:decision] branch slug has no exact receipt and K2 augment missed; fell back to implement-receipt`로 stderr에 나오지만 stdout은 그 slug를 그대로 준다. 이 상태로 `/mccp:pr`을 완주하면 2.5.7이 `.claude/receipts/mccp-pr-codex/live-activation-m3-pr-codex-absorption.json`을 **다른 브랜치의 `head_sha`로 덮어써** #107의 audit anchor가 소실된다(receipt는 gitignored라 복구 불가, 재실행만이 유일 복구). fallback 자체는 "같은 작업의 후속 세션"을 이어주려는 의도지만, **현재 브랜치가 그 implement-receipt를 만든 브랜치인지 검증하지 않는다** — 즉 무관한 브랜치에서도 남의 slug를 상속한다. 실측: 두 브랜치는 공통 커밋이 없고(`main` 분기) diff도 전혀 겹치지 않는데 동일 slug가 나왔다. 수정 방향: (a) fallback한 implement-receipt의 `meta.git_branch`가 현재 브랜치와 다르면 fallback을 **거부**하고 branch-slug로 진행하거나 명시 `--decision`을 요구, (b) `head_sha`가 현재 브랜치의 ancestor인지 확인(무관 브랜치면 미해당), (c) 최소한 stdout이 아니라 **비영점 exit + 명시 요구**로 승격해 조용한 상속을 차단. 가드: 서로소 브랜치 2개에서 같은 slug가 파생되지 않음을 검증하는 회귀 test. 관련 — 본 chore가 `/mccp:pr` 게이트를 돌리지 않고 직접 PR을 연 이유가 이것이다(§3.11의 파일이동 chore 선례와 동일하게 human-gate + git history로 review).
2026-07-22 | CRITICAL | (discovered by Plan-Codex R1 on durable-evidence-substrate.plan.md — DEFER to the separate false-approval-predicate cycle) | **[ABSORBED → v1.22.5 integrity-unification M1 Task 1]** **completion-ledger의 승인 술어가 v1.20.3이 이미 신뢰 불가로 판정한 필드를 본다 — 대조 가능한 엔트리의 30%가 거짓 양성.** `completion-ledger/index.js:95-96`이 `receipt.resolution.converged !== true`로 append 여부를 결정한다. 그런데 `resolution.converged`는 **receipt-write 시 항상 true로 default되던 필드**이고, 정확히 그 이유로 v1.20.3이 cross-gate dedupe 축에서 이를 폐기하고 present-only `resolution.codex_verdict`(enum `converged|divergent|critical|unavailable|skipped`)로 이전했다(CLAUDE.md §1.2 "v1.20.3 무결성 복구"). **ledger 축에는 그 수정이 적용되지 않았다** — dedupe만 고치고 ledger는 남았다. **실측 전수 감사**: ledger `verdict=converged` 엔트리 중 ship receipt와 대조 가능한 10건에서 **3건이 거짓 양성**(`live-activation-m3-pr-codex-absorption` → receipt `codex_verdict='divergent'` + `meta.codex_review_actionable_findings=true` / `workflow-orchestration-m4-parallel-activation` → `skipped` / `worktree-gitdir-tmp-resolve` → `skipped`). 나머지 19건은 ship receipt가 gitignore로 이미 소실돼 **참·거짓 판정 자체가 불가능**하다. 파급: ledger는 git-tracked 내구층이고 `milestone-history`가 완료 타임라인을, `archive-complete`가 status 정정 근거(evidence)를 여기서 끌어온다 — 즉 거짓 승인이 대시보드 완료 판정과 아카이브 결정으로 그대로 전파된다. **순서 불변식**: 이 술어를 고치기 전에 ledger/receipt를 내구화(git 추적)하면 거짓을 영구화할 뿐이다 — 소실보다 나쁜 *거짓 내구 증거*가 된다. 그래서 `durable-evidence-substrate.plan.md`가 이 축 뒤로 blocked 됐다. 수정 방향: (a) append 술어를 `resolution.codex_verdict === 'converged'` ∧ `meta.codex_review_actionable_findings !== true` ∧ 비-advisory/비-skipped로 교체(부재=구 receipt는 fail-closed), (b) 기존 거짓 양성 3건 정정 또는 격리 + 대조 불가 19건에 `unverifiable` 표식, (c) `resolution.converged`를 읽는 잔여 소비처 전수 조사(dedupe·ledger 외 제3의 소비처가 있는지) + 회귀 test. 관련: 같은 뿌리의 terminal 게이트 축(2026-07-21 HIGH — `validate-cmd`가 `codex_verdict!=='converged'`를 blocking으로 승격하지 않음)과 **동일 필드 계열**이므로 묶어서 설계할 것.
2026-07-22 | MEDIUM | (discovered while running the Plan-Codex gate for durable-evidence-substrate) | **[ABSORBED → v1.22.6 integrity-unification M2 Task 3 — verify-and-close: 현 코드는 `.stdout`→`.result.verdict`를 정상 파싱(실측), 실-producer 회귀 fixture로 drift 봉인, 코드 변경 없음]** **verdict SoT 파서가 유효한 구조화 응답을 파싱하지 못한다.** `codex-review-payload.js#parseReviewPayload(stdout)`가 정상 Codex 응답(top-level `result.verdict='needs-attention'` + `findings[3]` 보유, `parseError: null`)에 대해 `null`을 반환했고, `deriveGateVerdict`는 `{verdict:'unavailable', source:'unavailable', rawVerdict:null}`을 냈다. 실제 응답 구조는 `{ok, stdout, stderr, durationMs, classification, blocking, advisory}` 래퍼이고 `stdout`이 다시 `{review, target, threadId, context, codex:{...}, result:{verdict, summary, findings, next_steps}, rawOutput, parseError}` JSON 문자열이다 — 파서가 이 중첩 형태를 인식하지 못한 것으로 보인다. **안전 방향으로 실패했다**(fail-closed: `unavailable`은 승인 불가라 F5 계열 거짓 converged를 재생산하지 않음)는 점은 설계대로다. 그러나 v1.22.3이 free-text 스캔을 fallback으로 강등하고 구조화 payload를 **4게이트 공용 단일 SoT**로 세운 목적이 "정상 응답에서 정확한 verdict를 읽는 것"이므로, SoT가 정상 응답을 못 읽으면 모든 게이트가 상시 `unavailable`로 떨어져 **승인 경로가 구조적으로 닫힌다**(운영자가 advisory 우회를 상습화하게 만드는 압력 — 게이트 무력화의 전형 경로). 실측 재현: `threadId=019f8598-aeff-78a0-8853-2bcf33e186db` 응답, `stdout.length=10828`. 수정 방향: (a) `parseReviewPayload`가 래퍼 `.stdout`을 한 번 더 JSON.parse해 `.result.verdict`에 도달하는 경로를 지원하는지 확인하고 누락이면 추가, (b) 정상 응답 fixture를 회귀 test로 고정(현재 이 형태의 fixture가 없어 파서가 실제 producer 출력과 drift한 채 통과 중일 가능성), (c) `codex-result-filter`가 v0.3.6 이래 실제 producer finding 형태와 어긋나 항상 identity였던 선례와 동일 계열 — **파서·필터가 실제 producer 출력으로 검증되고 있는지** 전수 점검.
2026-07-22 | HIGH | (discovered by cross-session contradiction between durable-evidence-substrate and multi-session-work-loop-m1) | **ledger↔receipt 감사가 대조 대상 부재를 "이상 없음"으로 보고한다 — 부재의 증거를 증거의 부재로 뒤집는다.** 두 세션이 동일한 전수 감사를 돌려 정반대 결론에 도달했다. main worktree: ship receipt 33건 → `comparable=10, false_positive=3, unverifiable=19`. `.worktrees/v1.22.4-multi-session-m1`: ship receipt **0건**(`.claude/receipts/` 디렉토리 자체 부재 — gitignore) → `comparable=0, false_positive=0, unverifiable=28`. 후자가 "거짓 양성 0"으로 보고돼 "결함 없음"으로 해석됐으나, 실제로는 **판정 자체가 불가능한 상태**였다. 최악의 케이스(`live-activation-m3-pr-codex-absorption` — ledger 엔트리도 untracked)는 그 worktree에서 **원리상 관측 불가**다. 파급 3가지: (a) **fresh clone은 항상 이 상태다** — 저장소를 새로 받은 누구도 ledger 거짓 양성을 발견할 수 없다. (b) 내구성 결함이 단순 데이터 손실이 아니라 **술어 결함(2026-07-22 CRITICAL 행)을 탐지할 감사를 무력화하는 2차 결함**임이 확정됐다 — 두 결함이 서로를 은폐한다. (c) `multi-session-work-loop-m1.plan.md`가 completion-ledger를 A1(작업 완주율) **최선 소스**로 지정했는데, 그 소스는 승인 술어가 거짓이고 검증 corpus가 worktree에서 부재하다 — M1 계측 설계의 전제 재검토 필요. 수정 방향: (1) 모든 증거 대조 도구가 `{comparable, ok, false_positive, unverifiable, coverage}`를 반환하고 **`comparable===0`이면 `clean`이 아니라 `blind` 상태 + 비영점 exit**을 내도록 계약화(derive per-source `degraded` 플래그 패턴 미러), (2) 0-coverage에서 clean을 반환하지 않음을 회귀 test로 고정, (3) 대조 불가 엔트리에 `unverifiable` 표식 — 침묵은 "검증됨"으로 오독된다. 흡수 위치: `durable-evidence-substrate.plan.md` Task A1(최우선) · Phase B Task B2. **일반 교훈**: 이 저장소의 감사·필터·파서 계열이 실제 producer 출력 부재/형태와 어긋난 채 조용히 통과한 선례가 이미 3건이다(v0.3.6 `codex-result-filter` 항상 identity · `parseReviewPayload` 정상 응답 미파싱(2026-07-22 MEDIUM) · 본 건). "통과했다"가 "검사했다"를 의미하지 않는 표면을 전수 점검할 것.
2026-07-23 | HIGH | (discovered by PR-Codex R5 on durable-evidence-substrate follow-up — DEFER, 5-round non-convergence 착륙) | **[ABSORBED → v1.22.5 integrity-unification M1 Task 2]** **evidence-stage-guard가 스키마·gate 검증 없이 self-hash만으로 통과시킨다.** `evidence-stage-guard.js#validateContent`가 JSON 파싱 + `receipt_hash` 존재 + `receiptHash` 재계산 일치 + 절대 cwd 부재만 확인하고, receipt 스키마 validator(`receipt/schema.validate`)나 `gate_id==='mccp-pr-codex'`·`phase==='pr'`·파일명 slug↔`decision_id` 일치를 강제하지 않는다. wrong-gate/schema-invalid JSON이 자기 `receipt_hash`를 계산해 Phase 3 evidence-commit을 통과 → 신뢰 불가 blob이 durable ship evidence가 된다. R3/F1이 tamper(hash 불일치)는 닫았으나 valid-hash-but-invalid-schema는 열려 있다. 수정 방향: `receipt/schema.validate`를 staged blob에 실행 + gate_id/phase 강제 + 파일명 decision slug↔`receipt.decision_id` 일치 검증. **동일 tamper/무결성 계열**(R3/F1·아래 R5-F2)과 묶어 read/write 양경로를 receiptHash+schema로 통일할 것.
2026-07-23 | HIGH | (discovered by PR-Codex R5 on durable-evidence-substrate follow-up — DEFER) | **[ABSORBED → v1.22.5 integrity-unification M1 Task 3]** **evidence-audit가 declared hash 일치만으로 hash_bound을 집계, receiptHash 재계산 안 함.** `evidence-audit.js`의 `hash_bound`은 `entry.receipt_hash === receipt.receipt_hash`일 때 증가하되 `receiptHash(receipt)`를 재계산하지 않는다. commit 후 receipt body가 변조/stale인데 `receipt_hash`만 그대로면(편집된 verdict가 ledger verdict와 우연히 일치하면) ok + hash_bound으로 집계될 수 있다. R3/F1이 stage-guard(write 경로)의 tamper를 닫았으나 audit(read 경로)는 여전히 declared hash를 신뢰한다 — 동일 tamper 계열의 read-side 잔여. 수정 방향: ship receipt 읽을 때 스키마 검증 + `receiptHash(receipt)` 재계산, 불일치는 inconsistent/degraded로 처리하고 미검증 receipt는 hash_bound 미집계 + 회귀 test.
2026-07-23 | MEDIUM | (discovered by PR-Codex R5 on durable-evidence-substrate follow-up — DEFER) | **[ABSORBED → v1.22.6 integrity-unification M2 Task 2 — oid→paths[] via `git ls-tree -r` 증강 + per-path allowlist; 플랜의 rev-list 다중경로 가정은 실측 거짓이라 ls-tree로 정정]** **history-leak-scan allowlist가 blob당 임의의 한 경로에만 적용된다.** `history-leak-scan.js#scanRange`가 rev-list 출력을 `oid→first-path`로 dedup한 뒤 그 대표 경로에만 path-specific allowlist를 적용한다. 같은 leaking blob이 default allowlist fixture 경로와 real artifact 경로 양쪽에서 도달 가능하면, git이 fixture 경로를 먼저 보고할 때 real 경로가 평가되지 않아 exact allowlist가 비-allowlist leak을 억제한다. 확률 낮음(동일 blob 내용이 두 경로에 동시 존재해야 함). 수정 방향: oid당 전체 경로 추적 또는 allowlist 판정 전 경로별 스캔 — allowlist 매칭 path+line만 억제하고 같은 blob의 비-allowlist 경로는 보고.
2026-07-22 | LOW | .claude/plans/multi-session-work-loop-m1.plan.md (Implement-Codex R2 next_steps[3], DEFER) | Validation CHECK 6의 `k=$(grep -oE 'revert|fix-type|finding 귀속' … | sort -u | wc -l)`이 매칭 0건이면 `set -o pipefail` + `set -e`로 **조용히 abort**한다 — 명명된 `FAIL: discriminating criteria k < 2` 메시지가 아니라 무언의 exit 1. Codex R2가 직접 확인한 대로 이 경로는 **fail-closed**라 판정 결론은 동일하고(k=0이면 `k >= 2`도 실패), 차이는 진단 가능성뿐이다. 같은 계열 hazard가 CHECK 2c에서는 *성공 시에만 실패*하는 형태로 나타나 milestone을 영구 차단했고(R2 사이클에서 발견·수정), CHECK 6은 그 반대편 — 실패 방향이라 안전하지만 원인 진단이 어렵다. 수정 후보: `grep -c` 또는 `awk`로 count 산출을 명시화해 0건도 정상 반환하게 하고 판정은 `[ "$k" -ge 2 ]`가 단독으로 내리게 한다(CHECK 2c에 적용한 것과 동일한 계약 — 필터는 빈 결과를 정상값으로 돌려주고, 판정은 hard exit이 담당). M1 범위 밖(Validation 블록 품질 축)이라 이연.
2026-07-22 | MEDIUM | (self-found while running the implement design gate for multi-session-work-loop M1) | **impeccable detector의 severity 어휘가 mccp critique oracle의 alias 표에 없어, detector finding을 그대로 먹이면 무조건 fail-closed가 된다.** `scripts/detect.mjs`는 `severity: "advisory"`를 emit하는데(실측: `.claude/cache/status.html` 스캔 → `numbered-section-markers`, `severity:"advisory"`), `design-critique-decide.js#normalizeSeverity`의 `SEVERITY_ALIASES`는 CRITICAL/HIGH/MEDIUM/LOW와 그 별칭(P0-P3, blocker, major, minor 등)만 알고 `advisory`를 모른다 → `UNKNOWN` → `decideCritique`가 실패 항목으로 계상(v1.3.0-m2 Codex R1 F2가 의도한 fail-closed) → `ESCALATE` 후 cap 도달 시 `DIVERGENT_UNRESOLVED` → implement receipt `design_critique_verdict='divergent'` → `/mccp:pr` Phase 1.6 chain-check **BLOCK**. 즉 detector가 advisory 1건만 내도 PR이 막힌다. 현재 잠복인 이유는 oracle의 입력이 detector raw row가 아니라 critique이 합성한 P0-P3 findings(별칭 표에 있음)라는 **관행**에 의존하기 때문이며, 그 관행은 어디에도 강제돼 있지 않다 — `critique.md`는 P0-P3로 태깅하라 하고 detector는 advisory/warning을 내는데 둘을 잇는 계약이 없다. 수정 후보: (a) `SEVERITY_ALIASES`에 detector 어휘(`ADVISORY`→LOW, `WARNING`→MEDIUM 등)를 추가, (b) detector 출력을 oracle에 넣기 전 매핑하는 어댑터를 명시하고 미매핑 어휘는 loud warn, (c) UNKNOWN fail-closed는 유지하되 "미지의 어휘"와 "severity 필드 부재"를 구분해 전자는 명명된 오류로 승격. (a) 단독은 새 어휘가 또 생기면 같은 문제가 재발하므로 (b)와 함께 가야 한다. M1 범위 밖(design-gate 계약 축)이라 이연.
2026-07-23 | HIGH | .claude/plans/multi-session-work-loop-m1.plan.md | PR-Codex R3 F1 — measurement-feasibility §4 소급 프로토콜의 estimand 불일치: §4는 "C1 전용"이라 선언하는데 절차는 finding을 후속 PR·revert 창에 연결하고 임계는 게이트/창/유형별(L234가 C3 불가 증명 목적임을 명시)이다. C1은 "같은 작업 단위 안에서 해소"로 정의되므로 M2가 실행할 C1 전용 절차가 사실상 없다. 이연 근거: 해당 절은 PROVISIONAL 층이고 plan이 M2 진입 전 re-freeze를 의무화한 바로 그 표면이다(§"필수 re-freeze 게이트"). re-freeze 시 §4를 C1의 단위(finding id → 원 작업 단위 → 해소 유형 → 동일 단위 내 폐쇄 여부)로 재작성하고, 창·revert·fix-title 임계는 C3 전용·M2 비실행으로 분리 표기할 것.
2026-07-23 | HIGH | (pre-existing since completion-ledger 도입, self-found during /mccp:code-review of the M1 working tree) | **[ABSORBED → v1.22.5 integrity-unification M1 Task 1 — #111이 이연한 '별도 patch cycle'이 곧 본 M1]** **completion-ledger가 `resolution.converged`를 승인 술어로 써서 Codex가 말한 적 없는 `converged`를 감사 코퍼스에 기록한다** — v1.20.3이 cross-gate dedupe에서 고친 것과 **동일한 결함 클래스**가 ledger에 잔존. `lib/completion-ledger/index.js:96`이 `receipt.resolution.converged !== true`로 gate하고 `:108`이 `meta.advisory`/`meta.skipped`만 보고 verdict를 파생한다. `codex_verdict`는 completion-ledger 모듈 전체에 **0회 등장**(grep 확인). `resolution.converged`는 receipt-write 시 항상 `true`로 default되는 필드라 이 게이트는 사실상 무조건 통과다. **실측**: receipt `mccp-pr-codex/multi-session-work-loop.json`이 `resolution.codex_verdict='divergent'`(receipt_hash `fff11fc2…`)인데 동일 hash를 anchor하는 ledger 항목 2건이 `"verdict":"converged"`로 생성됨. 두 항목은 본 리뷰에서 커밋 전 격리(scratchpad `ledger-quarantine/`)했고 워킹트리에서 제거됨. **영향**: (a) `archive-complete/scan.js:174-178`이 ledger를 **강증거**로 취급하고 `verdict=converged`를 evidence 문자열로 출력 → drift 판정에 거짓 승인 주입(human-gate라 즉시 파괴적이진 않음), (b) `derive/sources/ledger.js`가 `decision_id`+최신 `completed_at`로 dedupe하므로 **더 최신인 거짓 항목이 승자**, (c) ledger 파일은 git-tracked라 durable-evidence-substrate PRD가 지향하는 감사 코퍼스의 첫 오염 사례가 된다. 수정 방향: (a) `index.js:96` gate를 `resolution.codex_verdict === 'converged'`로 전환(부재=구 receipt는 fail-closed로 미기록 — dedupe v1.20.3 계약 미러), (b) `store.js:29` `VALID_VERDICTS`에 `divergent` 추가 여부 결정 — divergent를 "기록하되 정직하게" 할지 "아예 미기록"할지가 설계 분기점이며, ledger 존재 자체가 완료 신호(`index.js:137` 주석)이므로 후자가 계약과 정합, (c) 실 producer receipt(`codex_verdict='divergent'` + `converged:true`) 형태의 회귀 test 고정 — 기존 test가 `converged:true` 손수 fixture로 구현 가정을 인코딩해 suite green인 채 blind인 패턴(codex-result-filter / verify.js 선례와 동형). PR #109 브랜치는 PRD가 "코드 변경 0"을 명시해 scope 밖 — 별도 patch cycle.
2026-07-30 | HIGH | .claude/plans/integrity-unification-m3.plan.md (Implement-Codex R4 F6, DEFER) | **dedupe skip proof가 ship gate에서 독립 재검증되지 않는다.** `pr-ship-gate.js#hasSkipProof`가 `meta.codex_dedupe_at_pr===true`를 `codex_verdict='skipped'`의 충분 증거로 신뢰하지만, runtime ship gate도 `validate --check-ship-verdict`도 dedupe를 재실행하거나 plan/implement receipt가 현재 `codex_verdict='converged'` + residual 비어있음을 재확인하지 않는다. 실제 command flow에선 Phase 2.5.2 `evaluateForDedupe`(v1.20.3 fail-closed)가 dedupe 플래그 세팅 전에 이미 양 게이트 convergence + residual을 검증하므로, F6은 그 upstream 검증을 ship 시점에 재검증하라는 defense-in-depth이고 exploit은 codex-result.json 파일 위조(단일 사용자 위협모델 밖)를 요구한다. R1(unproven-skip)·R3(post-write swap)이 닫은 것과 다른 **pre-write proof gap**. 수정 방향(후속 milestone 규모): (a) ship gate(finalize+validate)가 dedupe skip receipt에 대해 plan-codex+implement-codex receipt를 재read해 `isConvergedVerdict` 재확인(bounded 절반 — dual-review core), (b) residual-files 절반은 dedupe 시점에 검증가능 proof(plan/implement receipt hash·verdict·head/base·residual digest)를 pr-codex receipt meta에 봉인 후 ship gate가 재검증 — plan-path threading 또는 신규 스키마 필드 필요. M3 core(non-approving mechanical hard-stop) 범위 밖 defense-in-depth라 이연.
2026-07-30 | HIGH | .claude/plans/integrity-unification-m3.plan.md (santa-loop R3 Codex GPT-5.4 재-corroboration, DEFER 유지) | **F6(위 항목) 교차모델 재확인.** integrity-unification M3 브랜치를 santa-loop 3라운드 dual-review(Claude Opus + Codex GPT-5.4)로 돌린 결과, R1(override provenance stale-forward bypass)·R2(present-only receipt_hash 안정성)는 흡수했고, R3에서 Codex가 이 F6(`codex_dedupe_at_pr`를 ship gate가 독립 재검증 않음)을 **독립적으로 재발견**했다. Implement-Codex 4라운드 dogfood의 DEFER 판단과 두 번째 cross-model이 일치 — 완전 fix가 sealed verifiable dedupe proof(신규 스키마 필드 또는 plan-path threading)를 요구하는 milestone-규모임을 재확인. 단일 사용자 위협모델에서 exploit은 여전히 codex-result.json 파일 위조를 요구하고 upstream `evaluateForDedupe`(v1.20.3 fail-closed)가 dedupe 플래그 세팅 전 이미 convergence+residual을 검증하므로 M3 core 무결성(non-approving verdict의 mechanical hard-stop)은 온전. DEFER 유지, 후속 milestone. (같은 라운드 F4 HEAD-binding fail-open은 흡수 — ship-gate-head-unverifiable로 fail-closed 전환.)
