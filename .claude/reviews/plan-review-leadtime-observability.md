# Plan Review Panel — leadtime-observability

**Plan**: `.claude/plans/leadtime-observability-m3.plan.md` · **Plan version**: `sha256:2c85504c18d209ebf8f9426c0b8b1a4232a2dc3a072037975512f1880079ef4a`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 11 blocking finding(s): architect/HIGH, architect/HIGH, architect/FAIL, security/HIGH

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | DD1/Task 2의 '단위 어휘는 하나' 재사용 계약이 구조적으로 성립하지 않는다 — 순환 의존 또는 복제 중 하나가 강제된다. Task 2는 `leadtime-surface.js`가 `leadtime.js`의 `fmtMin`/`fmtDay`를 '그대로 재사용'한다고 못박지만 두 함수는 module-private이고 export되지 않는다. 동시에 Task 6b는 `leadtime.js#renderHuman`이 `formatLeadtimeLine`을 부르게 만든다. 즉 leadtime.js -> leadtime-surface.js -> leadtime.js 순환이 생기고, `module.exports`가 파일 **끝**(line 1248)에서 할당되므로 surface 모듈이 top-level require로 받는 것은 미완성 exports(빈 객체)라 `fmtMin`이 undefined로 호출 시점에 죽는다. Files to Change의 leadtime.js 행에도 export 추가가 없다. | plan Task 2: '기존 renderHuman의 헬퍼는 영문 min/d(... line 1041-1047 fmtMin/fmtDay)이고 이 포매터가 그 두 함수를 그대로 재사용한다' + Task 6b 1항 'renderHuman(r)의 첫 줄을 formatLeadtimeLine(...)로 바꾸고'; plugins/mccp/scripts/lib/leadtime.js:1041-1047 (fmtMin/fmtDay 정의) 대 plugins/mccp/scripts/lib/leadtime.js:1248-1275 (module.exports 목록에 fmtMin/fmtDay 없음, 파일 말미 할당) |
| architect | HIGH | Task 1 3b가 '흡수했다'고 주장한 검사 정합이 Validation 스크립트에 실제로 반영되지 않았다. 3b는 'Validation 1과 4가 같은 명제(`ledger_basename` in by_anchor)를 검사하게 된다'고 적었지만 Validation 1은 여전히 truthiness(`sa...ledger_basename && sa...ship_plan_hash`)로 검사한다. DD3 표가 합법이라 선언한 상태(한쪽 앵커만 조인 → 나머지 `null`)에서 Validation 1은 실패한다. 오늘 코퍼스는 양쪽이 non-null이라 이 오류가 게이트에서 드러나지도 않는다. | plan line 380: `if(sa.post_panel_span.by_anchor.ledger_basename && sa.post_panel_span.by_anchor.ship_plan_hash){} else throw new Error('two anchors must both be present keys (UI8)')` 대 plan line 481 (`'ledger_basename' in d.post_panel_span.by_anchor`) · plan Task 1 3b '투영은 두 키를 항상 만들고 부재를 null로 적는다 … 이로써 Validation 1과 4가 같은 명제를 검사하게 된다' · DD3 표 '한쪽 앵커만 조인됨 \| 그 축만 값, 나머지는 미산출' |
| architect | MEDIUM | git-tracked `distribution.json`을 hook 구동 렌더 경로에서 자동 갱신하게 만들면서, 병렬 worktree 간 내용 충돌 축이 설계에 없다. DD6의 content-stability는 '내용이 같을 때'만 write를 막는데, PRD 자신이 코퍼스가 게이트 실행마다 자란다고 못박았으므로 내용은 사실상 매 사이클 바뀐다. trigger.js 경로의 호출자는 receipt/write · dispatch · SessionStart hook · dashboard-server라 각 worktree가 자기 코퍼스 관점으로 같은 tracked 파일을 서로 다르게 덮어쓴다(§3.8 병렬 worktree 관행). Risks 표에는 '렌더마다 갱신' 축만 있고 이 축이 없다. | plan DD6 · DD16 ('그 호출자가 receipt/write.js line 1180 · dispatch-envelope.js line 250 · … hooks/render-trigger-session-start.js line 24') · PRD line 24 '코퍼스는 자기 자신을 늘린다' · plugins/mccp/scripts/lib/renderer/trigger.js:292-343 (렌더+piggyback가 hook 경로에서 무조건 실행) |
| architect | LOW | `derive/sources/leadtime.js` 배치가 그 디렉토리의 계약과 어긋난다. 그 디렉토리는 `SOURCE_SCANNERS`에 등록되어 `model.sources.<name>` count-source를 채우는 스캐너의 자리인데, 이 축은 등록되지 않고 top-level `model.leadtime`을 채운다. plan 자신이 인용한 top-level 필드 선례(`model.metrics`)는 `derive/sources/`가 아니라 `../lib/msw-metrics`에 산다. | plugins/mccp/scripts/derive/index.js:100-117 (SOURCE_SCANNERS 루프가 결과를 `model.sources[name]`에 병합) 대 :126-132 (`model.metrics = computeMetrics(model)`, `require('../lib/msw-metrics')`) · plan Files to Change `plugins/mccp/scripts/derive/sources/leadtime.js \| CREATE` |
| security | HIGH | H2의 유일한 기계적 강제(Validation 2d)는 절대 도달하지 않는 경로를 검사한다 — '실패 sentinel이 절대경로를 git-tracked 파일에 커밋한다'(Risks 표 **높음**)를 실제로 반증할 수 없다. 2d는 `scanLeadtime('/no-such-repo-for-leadtime-probe',{leadtimeScan:true})`가 sentinel을 낼 것을 전제하지만, `audit()`은 존재하지 않는 root에서 throw하지 않는다: `leadtime.js:302` `if (!fs.existsSync(dir)) return src;` · `:305-310` readdir try/catch · `corpus.js:681` `if (!fs.existsSync(dir)) { out.sources.push(src); return; }` · `:684-690` try/catch, 그리고 `readGitTouchedPaths`는 `leadtime.js:427`에서 실패를 `reason:'git-exec-failed'`로 삼킨다. 따라서 그 호출은 정상 summary(=`error_kind` 부재)를 돌려주고 2d는 `sentinel must carry a closed error_kind enum`으로 **항상** 붉는다. 실행자가 그것을 통과시키는 가장 싼 방법은 missing-root를 sentinel로 특수 분기시키는 것인데, 그러면 실제 유출원(`module-load-failed`·`oracle-threw` — Node의 require/fs 에러 메시지가 절대경로를 품는 경로)은 여전히 **한 번도 실행되지 않는다**. 즉 DD5가 git-tracked로 옮긴 파일에 머신 고유 경로가 실릴 수 있는지 여부는 이 사이클에서 검증되지 않는다. | plan Validation 2d (line 422-430) `scanLeadtime('/no-such-repo-for-leadtime-probe',…)` + `if(typeof sentinel.error_kind!=='string') throw …` vs plugins/mccp/scripts/lib/leadtime.js:302,305-310,427 and plugins/mccp/scripts/lib/plan-review/corpus.js:681,684-690,695,703 — audit() never throws on a nonexistent root; plan Risks row 'high': '실패 sentinel이 절대경로를 git-tracked 파일에 커밋한다 … Validation 2d가 sentinel 직렬화를 훑어 강제' |
| security | MEDIUM | DD6의 content-stability는 '모든 커밋이 이 파일을 끌고 다닌다'를 막지 못한다. 파일이 tracked이고(DD5) 쓰기 주체가 운영자가 부르지 않는 ambient 경로(SessionStart hook·`receipt/write.js`·dispatch watcher가 부르는 `trigger.js`, DD16)인데, payload는 코퍼스가 자랄 때마다 바뀐다 — PRD Evidence 마지막 항이 '코퍼스는 자기 자신을 늘린다'(게이트 실행 2건이 그 사이 새 레코드가 됐다)라고 실측으로 못박았다. 따라서 게이트를 한 번 돌 때마다 커버리지 분모가 바뀌어 tracked 파일이 재기록되고, 무관한 브랜치의 작업 트리가 hook 한 번으로 dirty해진다. Validation 5는 '코퍼스가 그대로인 연속 2회 렌더'만 재므로 이 축을 전혀 검사하지 않는다. | plan DD6 (line 186-188) '렌더마다 갱신하면 … 모든 커밋이 이 파일을 끌고 다닌다' + DD16 (line 238) trigger.js 호출자 5곳(`receipt/write.js:1180` · `hooks/render-trigger-session-start.js:24` 등) vs .claude/prds/leadtime-observability.prd.md:24 '코퍼스는 자기 자신을 늘린다'; plan Validation 5 (line 486-491)는 동일 코퍼스 2회 렌더의 mtime만 비교 |
| test | HIGH | Validation 3 — the plan's central acceptance check (leadtime line present in STATUS.md/status.html AND below next-action, DD7) can never pass: both next-action anchor patterns are unmatchable, so the block always throws 'next-action anchor not found'. | plan:447 `const NEXT_ANCHOR=(md.match(/^다음s/m)\|\|md.match(/⏭/)\|\|[])[0];` — the first regex is the literal string '다음s' (escaping lost; the real line is '다음: …' at plugins/mccp/scripts/lib/renderer/sections/status-grid.js:90-94), and grep for '⏭' under plugins/mccp/scripts/lib/renderer returns no files. plan:448 then hard-throws. The design-critique R0 HIGH absorption (plan:595) is therefore backed by a check that cannot execute. |
| test | HIGH | Validation 6b — the only test of DD15/Task 6b (renderHuman leads with the shared one line, ≤100 columns) is not valid JavaScript and cannot run. | plan:506-511: `const human=lt.renderHuman(r).split(/` followed by a raw newline then `/);`, and the throw string at :509-511 also spans raw newlines inside single quotes. Both are SyntaxErrors — `node -e` aborts before any assertion. Task 6b's Validate line (plan:351) names no other command, and Task 6's assertion list (plan:324-340) contains no renderHuman-first-line or column-width assertion. |
| test | MEDIUM | Task 1.3b claims it fixed the truthiness-vs-key-presence asymmetry so that Validation 1 and 4 test the same proposition, but Validation 1 still uses truthiness and would fail on a state DD3/DD13 declare legal (both anchors null). | plan:276 'Validation 1과 4가 같은 명제(`\\'ledger_basename\\' in by_anchor`)를 검사하게 된다' vs plan:380 `if(sa.post_panel_span.by_anchor.ledger_basename && sa.post_panel_span.by_anchor.ship_plan_hash){} else throw new Error('two anchors must both be present keys (UI8)')` — nulls (the DD13-mandated absence encoding, plan:234) make this throw, while Validation 4 (plan:481) correctly uses `in`. |
| test | MEDIUM | Task 6b Validate asserts the `--json` `unmatched` key set is 'frozen by a test', but no such freeze test exists and no Validation command exercises it — the claim that human-output truncation does not leak into JSON is unfalsifiable. | plan:351 '`--json`의 `unmatched` 키 집합 무변경(동결 test로 고정)'. The only frozen literal in plugins/mccp/scripts/lib/tests/leadtime.test.js is CORPUS_FROZEN (:282, asserted :358) which freezes corpus.js output, not leadtime JSON. Task 6's assertion list (plan:324-340) has no such item, and the Validation block has no command for it. |
| test | MEDIUM | Validation 2b's token oracle encodes a format assumption that Task 2 never specifies, so it can fail on correct output (or silently under-count). | plan:409-410 filters `t.split(' · ')` on `/p50/` and requires `vals.length>=3`, but Task 2 (plan:284-286) only says '값 부재 축은 `미산출`로 쓰고' — it does not require an absent axis token to still contain 'p50'. Under a live blind/partial corpus a compliant formatter emitting e.g. 'ledger 미산출 (11/49)' makes the check throw. |
| test | LOW | Validation 3's UI8 anchor-collapse check is document-scoped, not line-scoped, so it passes even if the leadtime line collapses the two anchors. | plan:456 `if(md.indexOf('ledger')===-1\|\|md.indexOf('ship')===-1) throw new Error('anchors collapsed (UI8)')` — searches the whole STATUS.md rather than the extracted leadtime line at index `i`, while the same block already located that line. |
| invariant | HIGH | Validation 3's UI8 anchor-collapse gate cannot fail — it greps the WHOLE STATUS.md for 'ledger'/'ship', two words that already appear throughout the rendered document independently of the new leadtime line. The plan's stated mechanical enforcement of PRD Decision 1 / UI8 ('두 앵커가 각각 보인다') is a no-op gate: an implementation that collapses both anchors into one number, or renders no anchors at all, still passes. | plan Validation 3: `if(md.indexOf('ledger')===-1\|\|md.indexOf('ship')===-1) throw new Error('anchors collapsed (UI8)');` — but the live rendered artifact already matches: Grep of `.claude/cache/STATUS.md` for `ship\|ledger` returns hits at lines 21, 26, 28-29, 32-35, 37-39, 42, 49, 51, 53 (backlog/risks/pipeline sections), with no leadtime line present at all. The check must be scoped to the extracted leadtime line, as the same Validation does for `LEAD='리드타임 ('`. |
| invariant | HIGH | Validation 2d — the sole mechanism the plan cites to close its own HIGH risk '실패 sentinel이 절대경로를 git-tracked 파일에 커밋한다' — never reaches the sentinel/catch path, because leadtime.js swallows missing-directory errors rather than throwing. Pointing scanLeadtime at '/no-such-repo…' yields an ordinary blind summary with no `error_kind`, so the probe either red-fails on a false premise or (once 'fixed') stops exercising the failure path entirely. The DD12 'no path in the projection' invariant remains unverified exactly where it matters. | plan Validation 2d asserts `typeof sentinel.error_kind!=='string'` throws; but `plugins/mccp/scripts/lib/leadtime.js:299-317` — `readJsonDir` wraps `fs.readdirSync` (:306) and JSON.parse (:317) in `catch (_err)` and returns without throwing; same pattern at :367-368, :389-390, :426. Also `plugins/mccp/scripts/lib/renderer/trigger.js:322-329` shows the cited snapshot-writer precedent writes into gitignored `cacheDir`, so no existing precedent covers a tracked-path failure sentinel. |
| invariant | MEDIUM | DD6's content-stability mitigation is structurally void for the very path it protects: the distribution file is git-tracked (DD5) and written from the fail-open receipt-write/auto-refresh render path (DD16), while the PRD states the corpus grows on every gate run — so payload content changes on essentially every gate cycle and the tracked file is dirtied mid-cycle, which is precisely the '모든 커밋이 이 파일을 끌고 다닌다' outcome DD6 claims to prevent. No validation covers the dirty-worktree-after-commit case. | plan DD6: '변경 없으면 기록하지 않는다 … git-tracked 파일을 렌더마다 갱신하면 … 모든 커밋이 이 파일을 끌고 다닌다'; PRD line 24: '코퍼스는 자기 자신을 늘린다 … 이 축의 검증에 리터럴 카운트를 쓰면 안 된다'; `plugins/mccp/scripts/receipt/write.js:1180` calls `require('../lib/renderer/trigger').triggerRender('receipt-write', …)` on every receipt write. Validation 5 only checks two renders back-to-back with an unchanged corpus. |
| invariant | MEDIUM | Validation 1 still contains exactly the truthiness-vs-key-presence defect that Task 1 step 3b claims to have absorbed: it requires both anchors to be truthy, which fails-closed on a state DD3 explicitly declares legal ('한쪽 앵커만 조인됨' → the other is `null`). | plan Validation 1: `if(sa.post_panel_span.by_anchor.ledger_basename && sa.post_panel_span.by_anchor.ship_plan_hash){} else throw new Error('two anchors must both be present keys (UI8)')` vs Task 1 3b: '이로써 Validation 1과 4가 같은 명제(`'ledger_basename' in by_anchor`)를 검사하게 된다' and DD3 row '한쪽 앵커만 조인됨 \| 그 축만 값, 나머지는 미산출'. Validation 4 does use `in`; Validation 1 does not. |
| invariant | MEDIUM | The DD7 hierarchy assertion in Validation 3 cannot resolve its anchor against the actual renderer output — the regex lost its escape (`/^다음s/m` instead of `\\s`) and the `⏭` fallback does not occur in the rendered STATUS.md, where next-action is emitted inline as '→ 다음 …' / '다음: …'. The check therefore deterministically HALTs at 'next-action anchor not found', pressuring the implementer to weaken the very ordering gate the design critique's HIGH finding created. | plan Validation 3: `const NEXT_ANCHOR=(md.match(/^다음s/m)\|\|md.match(/⏭/)\|\|[])[0];`. Live `.claude/cache/STATUS.md:17` renders `… → 다음 codex disabled round invarian…` (no `⏭`, not line-initial); `plugins/mccp/scripts/lib/renderer/sections/status-grid.js:90-94` emits `'다음: '` forms. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | 인용 검증: derive/index.js:100-183(소스 루프·metrics·host_version allowGit:false·mask 순서), derive/cli.js:136-205(writeAtomic·snapshot piggyback), renderer/trigger.js:260-360(두 번째 렌더 진입점·snapshot piggyback·triggerRender export) — DD16·DD2 인용은 실제로 맞았고 production 렌더 진입점이 정확히 둘임을 renderStatus grep으로 확인했다. .gitignore를 훑어 DD5(`.claude/state/leadtime/`이 어느 무시 규칙에도 안 닿음)를 확인했고 반증 실패. leadtime.js:940-1003으로 `by_anchor`가 조건부 키라는 3b 주장, :649-652로 git 증인이 분류에만 쓰여 분포가 강등에 불변이라는 DD2 주장을 확인했고 둘 다 참이었다. `derive/sources/*` 등록 co-presence 게이트가 실재하는지 grep했으나 없었다(그래서 LOW로만 보고). 깨진 것은 셋: 포매터 재사용 계약의 순환/미export, 3b가 흡수했다고 주장한 Validation 1의 truthiness 잔존, tracked 산출물의 병렬 worktree 충돌 축 부재. |
| security | fail | DD12(투영에 경로·레코드명·해시 없음)를 공격했다 — `aggregate`의 `unmatched[k].by_reason[r]`가 `{record: item.e.record}`를 담고 `corpus.js:699`가 `record` 이름을 `src.dir + '/' + name`(즉 `/`와 `.md` 포함)으로 만들므로 순진한 복사는 git-tracked 파일에 레코드 경로를 싣는다. 그러나 Validation 2c의 `/[\\\\/]\|\\.md\\b\|sha256:/` 직렬화 스캔이 라이브 코퍼스(미짝 70건)에서 그것을 실제로 잡으므로 유효한 방어였다 — 기각. `emptySource`의 `dir` 필드(leadtime.js:269)와 `sourceDamaged` 진단 문자열도 같은 스캔에 걸린다. escapeHtml REJECT_YAGNI(R0 LOW)도 공격했으나 오늘 투영에 주입 가능한 자유 문자열이 실재하지 않아 입력→결과 경로를 만들지 못했다 — 보고하지 않는다. `degradations` 원소가 열린 문자열일 가능성도 봤지만 `degradations`는 `leadtime.js`에 0회 등장하는 신규 필드라 값 집합이 전적으로 구현 선택이고 2c가 경로류를 막으므로 현재 근거로는 결함이 아니다. DD5의 gitignore 주장은 `.gitignore`를 직접 확인해 참임을 검증했다(`.claude/state/leadtime/`에 닿는 규칙 없음, `.claude/cache/`는 :142에서 통째 무시). `writeDistribution(root, …)`의 경로 traversal 입력도 찾았으나 root는 repoRoot 상수라 주입원이 없다. 남은 두 건이 위 findings다. |
| test | fail | Read the full plan and PRD, then attacked the Validate lines against real code: verified the next-action anchor text in renderer/sections/status-grid.js:90-94 and grepped '⏭' across plugins/mccp/scripts/lib/renderer (absent) to falsify Validation 3; parsed Validation 6b as JS (raw newlines in a regex literal and string → SyntaxError); cross-checked Task 1.3b's claimed truthiness fix against the actual Validation 1/4 code; grepped leadtime.test.js for a JSON `unmatched` freeze test backing Task 6b's Validate line (only CORPUS_FROZEN exists); probed Validation 2b/2c token-oracle assumptions against Task 2's stated format. I could not refute DD2's cross-git-mode equivalence check, DD6 content-stability (Task 5 return-value test plus Validation 5), DD12's projection-serialization sweep, or the DD16 second-render-entrypoint check (Validation 3b) — those are genuinely falsifiable as written. |
| invariant | fail | Traced each new gate for the unknown/absent input: (1) tried to open the UI8 anchor gate — succeeded, it is document-wide and already satisfied by unrelated STATUS.md text; (2) traced the failure sentinel path against leadtime.js's actual error handling (readJsonDir swallows) and found Validation 2d never reaches the catch branch it claims to prove; (3) checked digest/tracked-artifact coverage: distribution.json is git-tracked and written from the fail-open receipt-write render path with a content-stability argument the PRD's own growing-corpus fact defeats; (4) cross-checked Validation 1 vs Task 1 3b vs DD3 for the null-anchor legal state (truthiness contradiction remains); (5) resolved the DD7 ordering anchor against live renderer output (regex escape lost, fallback absent). I also checked DD16's spawn-free budget claim (leadtimeScan default-off wiring is consistent), the corpus.js/gate-wiring base-range diffs (Validations 8/9 use origin/main...HEAD plus worktree — sound), and DD12's projection serialization regex under shell quoting (correct); found no defect on those axes. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 253908,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:2c85504c18d209ebf8f9426c0b8b1a4232a2dc3a072037975512f1880079ef4a",
  "plan_path": ".claude/plans/leadtime-observability-m3.plan.md",
  "recorded_at": "2026-09-02T07:44:23.428Z"
}
```
