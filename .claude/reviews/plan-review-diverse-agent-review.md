# Plan Review Panel — diverse-agent-review

**Plan**: `.claude/plans/diverse-agent-review-m7.plan.md` · **Plan version**: `sha256:9f4b1361a0880394ad4b0adfdb3eccabe2bb9bc3ccf4a4db1bf8dd240522dbee`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 12 blocking finding(s): architect/HIGH, architect/HIGH, architect/FAIL, security/HIGH

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | The plan's FILES TO CHANGE declares files as CREATE that are produced during implementation Task 2, making the plan irreversibly invalid on re-execution due to L1 C3_CREATE_EXISTS gating. | Lines 90-91 list `.claude/reviews/plan-review-diverse-agent-review-m7-budget.md` and `.claude/PRPs/reports/diverse-agent-review-m7-report.md` as CREATE. L1-check.js:333-336 enforces: 'if (ACTIONS_REQUIRING_ABSENCE.indexOf(row.action) !== -1 && exists) { violations.push(v('C3_CREATE_EXISTS', ...))' for any CREATE file that exists. After Task 2 completes (which creates these files per lines 137-139), any re-execution will run L1 validation, find these files exist, and fail with C3_CREATE_EXISTS. The plan does not document this one-time-only property or provide idempotent cleanup. |
| architect | HIGH | Boundary leak: DN4 (line 76) correctly identifies that observation turns must execute BEFORE outputs are created to avoid L1 C3_CREATE_EXISTS, but the plan's own FILES TO CHANGE violates this principle by listing implementation outputs as CREATE in the very plan that contains them. | DN4 states: 'L1이 CREATE 행을 실존으로 검사하기 때문이다... 따라서 산출물 생성 전에 관측을 끝낸다.' (Observations must complete before output creation because L1 checks CREATE file non-existence). However, FILES TO CHANGE declares creation of files that Task 2 produces (lines 90-91). This creates a self-referential structural violation: the plan prescribes a sequencing rule to avoid L1 blocking, then violates that rule in its own structure. When the plan itself is gate-checked (which includes L1), the rule is satisfied only on the first run. |
| architect | MEDIUM | The plan claims to mirror M6 patterns (line 59-60) including how M6 fixed evidence files, but the plan's implementation differs: M6's `.claude/reviews/plan-review-diverse-agent-review-m6-r4-blocked.md` was a **capture of an existing gate record**, whereas M7's `-m7-budget.md` is a **transformation/copy of `.claude/reviews/plan-review-diverse-agent-review.md`** created during a nested plan execution. The plan does not explain why this difference is compatible with L1 CREATE/existence checking. | Patterns to Mirror table line 59 cites M6's approach to 'slug 공유 덮어쓰기(O3)에서 레코드를 분리'. Task 2 Action step 3 (line 138) says: '레코드를 `.claude/reviews/plan-review-diverse-agent-review-m7-budget.md`로 고정한다' (fix the record to this file). This file is not pre-existing; it is created by copying/transforming the observation gate's output. The plan does not justify why this pattern is safe for CREATE declaration when M6's pattern would also have created a previously-nonexistent file, raising the question of whether M6 actually completed or hit the same boundary leak. |
| security | HIGH | Task 3 Validate block checks the wrong filename for approval proof, making the bidirectional integrity check ineffective | Task 2 specifies file creation at line 138: 'レコードを `.claude/reviews/plan-review-diverse-agent-review-m7-budget.md`로 고정한다' and line 90 Files to Change lists CREATE of `-m7-budget.md`. But Task 3 Validate at line 209 searches for `.claude/reviews/plan-review-diverse-agent-review-m7-gate.md` (the wrong filename with `-gate` instead of `-budget`). This causes line 210 to read: `const approved=fs.existsSync(rec) && ...` where `rec` points to a non-existent file, making `approved` always false and breaking DN8's requirement to 'enforce bidirectional consistency between observation status and row content'. |
| test | CRITICAL | Task 2 requires the operator to run '/mccp:plan' with a budget goal of +200k to observe the budget gate firing; the Validate scripts then verify the observed behavior | Plan line 137: '운영자가 **예산 목표 `+200k`를 실은 새 turn**에서 `/mccp:plan .claude/prds/diverse-agent-review.prd.md`를 실행한다' + Task 2 Validate lines 151-162 which expects to read `layers.l2` containing observed remaining/minRemaining from the live run. However, no part of the plan's text or the `/mccp:plan` command body (plugins/mccp/commands/plan.md) documents or provides a mechanism for how the operator supplies this budget goal to the command. |
| test | HIGH | The Validate scripts in Task 2 check that the workflow's budget gate fires, but these tests depend on a live operator-executed run producing `.claude/reviews/plan-review-diverse-agent-review-m7-budget.md` with specific JSON structure | Task 2 Validate lines 141-180 read from `.claude/reviews/plan-review-diverse-agent-review-m7-budget.md` and parse `layers.l2` field. However, there is no test in the repo that verifies the actual `/mccp:plan` command chain will produce this file with the expected structure. The test in `plan-review-workflow-port.test.js` lines 188-202 exercises the workflow in isolation with a mock budget object, but does not verify end-to-end behavior where the real harness provides the budget to the real workflow. |
| test | HIGH | The plan asserts that `budget.total` is set by 'the operator' with value +200k, but provides no testable definition of what 'the operator' action is (environment variable, flag, configuration, etc.) | Plan line 137 says operator '발행' (issues) a turn with budget goal, and line 81 (DN6) says 'budget 목표를 낮추는 것은 시뮬레이션이 아니다' (lowering budget target is not simulation) — implying a real, user-visible mechanism exists. However, searching the codebase reveals no documented way for a user to set `budget.total` on the `/mccp:plan` command itself. ENVIRONMENT.md references `MCCP_PLAN_REVIEW_BUDGET` (per-reviewer estimate) and `MCCP_PLAN_FANOUT_BUDGET` but not a user-facing budget goal setting mechanism. |
| test | HIGH | Task 1 Capture and Task 2 Validate both assume `.claude/reviews/plan-review-diverse-agent-review.md` exists and contains machine-readable JSON with specific fields | Task 1 lines 106-128 attempt to capture this file; Task 2 lines 152-162 parse its JSON. However, there is no contract in the source code or tests that guarantees this file will exist with the expected schema when `/mccp:plan` runs. The file path is derived from the decision slug (which is stable), but nothing in the workflow or cli.js code is tested to verify it will be produced with `layers.l2` containing the budget skip message. |
| invariant | HIGH | Plan's validation accepts stale data without temporal anchoring — no run-specific identifier proves the measurement JSON is from THIS observation, not a previous run | Task 2 Validate (lines 145-180) reads `.claude/reviews/plan-review-diverse-agent-review-m7-budget.md` and checks that it contains valid JSON with `verdict=='unavailable'` and `halt_stage=='5.2e'`, but contains zero checks that would fail if the file is a copy of a previous observation's record. No timestamp, nonce, run-ID, or mtime-based verification exists. If the budget gate previously fired and left a record, manual copying of that stale file would pass all validations. |
| invariant | MEDIUM | Plan's restoration verification is incomplete — sha256 check passes even if the plan file was never corrupted, defeating the detection of failed restoration | Task 2 Validate (c, lines 173-177) reads the expected sha256 from the measurement file's comment and compares it to the current plan's sha256. This only verifies that the current file matches the pinned value; it does NOT prove that restoration from backup actually occurred. If the plan file never changed (e.g., because the observation didn't run), this check still passes. A complete restoration check would require verifying that the file was corrupted before restoration, then restored, then verified clean — but the plan only checks the final state. |
| invariant | MEDIUM | Code validation via substring matching is fragile and can miss violations — indirect evidence of 0-spawn guarantee is insufficient | Task 2 Validate (lines 165-171) uses `indexOf('reason: \\'budget\\'')` and `indexOf('phase(\\'Refute\\')')` to check code ordering. This substring search can be defeated by: (1) comments containing the same strings, (2) quote style changes (e.g., double quotes), (3) whitespace variations in the source. Moreover, this only verifies that the strings appear in the right ORDER in the file; it does not verify that the budget return is actually executed before agents are spawned in the control flow. A truly unreachable branch could still have the strings in the correct order in the source. |
| invariant | MEDIUM | Manual file operations lack atomicity — plan relies on operator copying files without transaction guards, risking partial/corrupted state | Task 2 action (step 3, lines 138-140) describes operator manually reading `.claude/state/plan-review/l2.json` and `.claude/reviews/plan-review-diverse-agent-review.md` and preserving them to create the fixed record file. If the operator is interrupted during copy, or if the source files are partially written, the resulting `.claude/reviews/plan-review-diverse-agent-review-m7-budget.md` could be corrupted. No atomic write, no validation of copy completeness, no rollback if corruption is detected. This is worse than a fail-closed gate — it's an unmonitored manual operation that could introduce silent data corruption. |
| invariant | MEDIUM | Proof of execution is structural, not empirical — validates that code COULD fire, not that it DID fire in this run | DN5(lines 78-79) acknowledges: '이 셋 중 어느 것도 프로세스 외부에서 spawn 수를 직접 센 것이 아니다' — none of these are direct counts of spawns. The validation checks (a) that l2.json has `skipped:true, reason:'budget'`, (b) that the code order suggests the branch is reachable, and (c) that plan was restored. But a gate that failed to fire would still produce some record (with a different message). The plan provides no proof that THIS run with THIS budget target actually triggered the budget condition. If the budget target was too high, or if the remaining tokens weren't decremented properly, the gate wouldn't fire but the stale record would still pass validation. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified plan citations: confirmed `cli.js:347` produces `minRemaining`, `budget.js:86` formula is `perReviewer × fleetLength`, `plan-review.js:161` gate check is `budget.total && budgetRemaining < minRemaining`, and `l1-check.js:333` enforces C3_CREATE_EXISTS. Examined FILES TO CHANGE table structure and L1 validation logic to identify the boundary leak where CREATE files become invalid on re-execution. Traced the sequencing of L1 checks (gate-time vs observation-time) and identified the self-referential violation where the plan's own structure violates the sequencing rule DN4 prescribes. Compared M6/M7 pattern claims against actual file creation mechanics to expose the abstraction leak. Confirmed the plan does not document non-idempotence or provide cleanup logic." |
| security | fail | Checked Task 2's specification of which filename gets created (line 138: `-m7-budget.md`); verified Files to Change listing (line 90: `-m7-budget.md`); verified Task 2 Validate reads that file correctly (line 147: `const F='.claude/reviews/plan-review-diverse-agent-review-m7-budget.md'`). Compared against Task 3 Validate block (line 209) which constructs `const rec='.claude/reviews/plan-review-diverse-agent-review-m7-gate.md'`. Verified this filename mismatch by grepping the plan document for both strings - found `-m7-budget` in lines 90, 138, 147 and `-m7-gate` only in line 209 (Validate block). Verified the logic impact: line 210 checks `fs.existsSync(rec)` which will always be false since the file created is `-m7-budget.md` not `-m7-gate.md`, causing the approval detection to always fail. Checked all relative file paths in Validate blocks for traversal risks - found none; all use '.claude/' and 'plugins/' prefix patterns. Verified budget gate firing condition logic at workflow line 161 is sound (`if (budget.total && budgetRemaining < minRemaining)`). No evidence of absolute path leakage or injection vulnerabilities in data handling. |
| test | fail | Attacked the plan's core claims by: (1) Reading the workflow source at `plugins/mccp/scripts/workflows/plan-review.js:161` to verify the budget gate condition exists — confirmed ✓. (2) Checking if tests exercise the end-to-end path where operator sets a budget goal and observes it fire — found only unit tests (`plan-review-workflow-port.test.js`) with mock budget objects, no integration test. (3) Searching for the documented mechanism for users to set `budget.total` for the `/mccp:plan` command — found none. (4) Verifying that the Validate scripts depend on artifacts that must be produced by the live run — confirmed that Task 2 Validate lines 151-162 read from `.claude/reviews/plan-review-diverse-agent-review-m7-budget.md` which is never created by automated tests. (5) Reading `/mccp:plan` command body to find where budget goal is injected — found no such mechanism documented or implemented. The plan is entirely dependent on manual human action ("operator issues a turn with budget goal") but never specifies what action that is or how to test it. |
| invariant | fail | I attacked the plan's core claim that the observation provides "증거" (evidence) of a live budget gate firing. I examined: (1) receipt anchoring — whether the copied `.claude/reviews/` file can be distinguished from a stale copy, finding no run-specific identifier; (2) file integrity — whether restoration is actually proven, finding only final-state verification, not before/after proof; (3) code validation robustness — whether substring search can guarantee 0-spawn, finding it fragile to formatting changes and unable to verify actual control flow; (4) atomicity — whether manual copy operations are safe, finding they lack transaction guards; (5) empirical vs structural proof — whether the gate actually fired this run or just could fire theoretically, finding no mechanism to distinguish. The plan's Validate blocks check file existence and content format but not temporal properties, not provenance, not actual execution proof. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 262500,
  "halt_stage": "5.2e",
  "granted": 4,
  "reviewed_plan_hash": "sha256:9f4b1361a0880394ad4b0adfdb3eccabe2bb9bc3ccf4a4db1bf8dd240522dbee",
  "plan_path": ".claude/plans/diverse-agent-review-m7.plan.md",
  "recorded_at": "2026-08-16T03:51:50.164Z"
}
```
