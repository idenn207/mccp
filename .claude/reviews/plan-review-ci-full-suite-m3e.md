# Plan Review Panel — ci-full-suite-m3e

**Plan**: `.claude/plans/ci-full-suite-m3e.plan.md` · **Plan version**: `sha256:319bcd6808c93083c9133ff28f78e0c3aa9868b010803ae1766b805684a4cd70`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 2 blocking finding(s): test/HIGH, test/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | DD2's `max_excluded_files` ratchet is asserted by *equality* against a glob expansion over a moving `tracked` set, so an unrelated PR that adds a `*.test.js` file matching an existing exclusion glob turns the merge-blocking suite permanently red with no attribution path. The gate itself uses `<=` (safe), but Task 3's paired assertion uses `=`, and nothing constrains exclusion patterns to literal paths. | Plan Task 3 Validate (line 592-593): "`max_excluded_files`가 격리 목록의 실제 확장 파일 수와 **같음**"; patterns are globs whose `**` expands across separators — `scripts/test-suite/enumerate.js:33-35` (`if (src[i+1]==='*') out += '.*'`). Contrast the floor axis, which DD9 deliberately makes one-directional (`tracked < floor` only) precisely to avoid this (plan line 402). |
| architect | MEDIUM | The `tracked` channel is declared as a single source of truth but is implemented twice, in two CLIs, with two different git invocations. DD2 corrects itself to say the channel and its three fail-closed conditions belong to `gate.js`, yet Task 2 still assigns the same resolution and the same fail-closed set to `coverage.js`'s CLI. Two owners of one invariant is exactly the shape DD2 spent a round rejecting. | Plan line 161-163 ("`tracked` 해소와 fail-closed 셋 … 은 **`gate.js` CLI층의 계약**") vs Task 2 line 502-509 ("`tracked`는 CLI층이 `git ls-files '*.test.js'`로 해소해 순수층에 넘긴다 … git 호출 실패·빈 목록은 차단" and "`tracked >= floor` … CLI가 **비영점**"). Additionally the runner resolves the same set by a third route — `scripts/test-suite/run.js:407-409` uses `git ls-files -z` plus an `endsWith('.test.js')` filter (`enumerate.js:122`), not the `*.test.js` pathspec. |
| architect | LOW | Task 5's enumeration sanity check 2 ("분할") names an input with no producer: `run.js --list` prints only `enumerated.included`, so the workflow step has no command that emits "격리가 덮는 파일 수". The step as written cannot be implemented from the surfaces the plan declares. | Plan line 626: "`run.js --list --exclude-from …`의 개수 + 격리가 덮는 파일 수 = tracked 개수". `scripts/test-suite/run.js:676-679` prints `enumerated.included` only; `enumerated.excluded` is never surfaced by any CLI flag in that file. |
| architect | LOW | Axis D's contrast-run evidence is stated as impossible-as-written: a `--revert` changes the tree, so the control run cannot be "the same commit". Task 7 (e2) says "같은 브랜치의" while the Action prose and Acceptance 3 say "같은 커밋에서" — the acceptance criterion is unsatisfiable under one reading. | Plan line 702-703 "같은 커밋에서 `--revert`한 대조 run" vs line 709 "대조 run(같은 브랜치의 `--revert` 상태)"; Acceptance item 3 (line 986) repeats "`--revert` 대조 run". |
| security | LOW | DD3의 `per_file` 모양 서술이 인용한 코드와 반대다. 계획은 '방출되는 measurement의 per_file은 null이 아니라 빈 배열'이며 'null만 단언하는 것은 공허한 단언'이라고 적지만, `foldChunks`는 `per_file: ok ? perFile : null`이라 stage-0이 다루는 바로 그 시나리오(chunk spawn 실패 → ok:false)에서 최상위 per_file은 **null**이다. `[]`는 오히려 ok===true ∧ tracked 공집합에서만 나오는 드문 모양이다. Task 2 분기 (13)이 두 모양을 모두 단언하므로 게이트 구멍은 열리지 않지만, 구현자가 계획의 우선순위 서술('[]가 실제 producer 모양')을 따르면 실제 CI 입력 모양을 부차로 취급하게 된다. | plan L256-263 "방출되는 measurement의 `per_file`은 `null`이 아니라 **빈 배열**이다(`:589`)" 대 scripts/test-suite/run.js:213-214 `// ok:false일 때 per_file은 null이지 []가 아니다` / `per_file: ok ? perFile : null` |
| test | HIGH | 축 D(음성 통제)의 실증 명제가 구성상 반증 불가다 — 절단 대상이 같은 Task에서 새로 쓰는 test가 단언하는 바로 그 줄이라, 실험은 '새 단언이 동작한다'만 재고 PRD가 요구한 '기존 스위트가 배선 부재를 잡는가'를 재지 않는다. 그런데도 Task 10이 이 run URL로 Success Metric 4(배선 절단 탐지)를 닫는다. | plan Task 7: "대상은 **이 milestone이 만든 배선**으로 한다 … `test-suite.yml` 판정 줄의 `--exclude-from` 인자 하나" + "실증되는 명제는 정확히 하나다: 절단 → `wiring-cut.test.js` red → 스위트 red → `gate.js` 1단계 차단" — 그 red는 `wiring-cut.test.js`가 그 토큰의 존재를 단언하도록 같은 Task에서 작성되기 때문에 발생한다(Task 7 "단언 대상은 … `gate.js` · `--measurement` · `--exclude-from` · `--floor-from` 전부"). PRD L54/L63은 이 축의 목적을 "커버리지 100%는 'test가 실행됐다'만 말하고 '결함을 잡는다'를 말하지 않는다"의 반증으로 규정하는데, 자기가 단언하는 줄을 지우는 실험은 그 명제에 대해 실패할 수 없다. |
| test | MEDIUM | Task 1(Linux red 6파일 수리/가드)이 만들어낼 수 있는 '전량 skip/가드된 green 파일'을 어떤 Validate도 탐지하지 못하는데, DD2가 그 탐지 필드(`fully_skipped`)를 명시적으로 철회했고 커버리지는 그런 파일에 `executed` 전액을 준다. 즉 이 milestone 자신의 편집이 게이트를 과대허용 방향으로 여는 경로가 test 없이 남는다. | plan DD2: "`fully_skipped`는 이 milestone이 산출하지 않는다 — producer가 없기 때문이다. 전 test가 skip인 파일('실행됐다'는 참이고 '단언이 돌았다'는 거짓)은 실재하는 위험" · Task 1 Validate: "각 파일을 `MCCP_CODEX_DISABLED=1 node --test --test-concurrency=2 <file>`로 단독 green 확인" — green 여부만 보고 단언이 실제로 돌았는지는 보지 않는다. 수리/가드 판정 기준도 산문("단언이 특정 플랫폼에서만 참이면 가드")이며 기계 검사가 없다. |
| test | LOW | Task 5의 '열거 sanity — 정체성' 검사는 실패할 수 없는 검사다. `run.js --list`의 tracked 원천이 이미 `git ls-files`이므로 같은 명령을 자기 자신과 비교하는 셈이고, 게이트가 실제로 쓰는 분모 채널(`gate.js`가 독자적으로 해소하는 `git ls-files '*.test.js'`)은 이 검사의 사거리 밖이다. | plan Task 5: "1. **정체성** — `run.js --list`(플래그 없음) ≡ `git ls-files '*.test.js'`. 이것이 `tracked` 채널의 정본 검증이다" vs `scripts/test-suite/run.js:407-410` (`listTrackedFiles` → `git ls-files -z`) + `scripts/test-suite/enumerate.js:122` (`p.endsWith('.test.js')`). 두 변은 같은 git 출력의 동일 필터라 어긋날 구성이 없다. |
| test | LOW | Task 3의 `max_excluded_files` 등가 단언은 무관한 PR을 붉게 만드는 과대엄격 분기다 — 격리 glob의 확장 결과는 tracked 집합이 늘면 함께 늘므로, 기존 패턴에 걸리는 test 파일이 추가되는 것만으로 상수 편집 없이 test가 red가 된다. | plan Task 3 Validate: "`max_excluded_files`가 격리 목록의 실제 확장 파일 수와 **같음**" (DD9 3번의 floor는 '이하'로 완화돼 있는 것과 대조 — DD9: "floor를 정확히 현재 개수로 두면 test 추가 때마다 붉어지므로 **floor 이하만 금지**"). 같은 논거가 확장 파일 수 등가 단언에는 적용되지 않았다. |
| invariant | MEDIUM | DD3's producer-shape correction is inverted against the source it cites: the plan says the emitted measurement's `per_file` is `[]` (not `null`) and therefore that asserting `null` is a "공허한 단언", but `run.js:214` emits `null` exactly in the `ok:false` spawn-failure case DD3 derives, and `[]` only when `ok===true`. The plan's own methodological rule ("실제 producer가 내지 않는 모양만 단언하는 것은 금지") would, if applied later, drop the *real* shape and keep the synthetic one. | Plan L256-263: "방출되는 measurement의 `per_file`은 `null`이 아니라 **빈 배열**이다(`:589`) … `null`도 같은 방향으로 접지만, 그 모양은 실제 producer가 내지 않으므로 **그것만 단언하는 것은 합성 fixture 위의 공허한 단언**" vs `scripts/test-suite/run.js:213-214`: "// `ok:false`일 때 `per_file`은 `null`이지 `[]`가 아니다 — 모름을 0으로 쓰지 않는다." / `per_file: ok ? perFile : null`. Both shapes are asserted in Task 2 (13), so no gate opens today. |
| invariant | MEDIUM | Task 5's "열거 sanity" step is gate-shaped but cannot stop what it claims to. The "분할" half has no producer: `run.js --list` prints only `enumerated.included` and no surface emits the excluded-file count, and `## Files to Change` scopes the `run.js` UPDATE to two unrelated items — so the excluded count can only be derived as `tracked − included`, making the equation an identity. The "정체성" half compares git's own output to git's own output filtered two ways, so it cannot detect the enumeration gap it nominally guards. | Plan L626: "**분할** — `run.js --list --exclude-from …`의 개수 + 격리가 덮는 파일 수 = tracked 개수" vs `scripts/test-suite/run.js:676-679` (`--list` writes `enumerated.included` only) and plan L447 (run.js UPDATE = exclusions.js rewiring + `--allow-codex` guard only). Identity half: plan L624 vs `run.js:407-409` `listTrackedFiles` = `git ls-files -z` + `.endsWith('.test.js')`. |
| invariant | LOW | gate.js's enumerated fail-closed set omits `--exclude-from` absence and exclusions-file absence. Task 2b names exactly three (git failure, empty tracked, `--floor-from` absence); the direction the parser falls on a missing/unreadable exclusions file is left unstated, and DD7's whole control ("게이트가 그 경로만 읽는다") plus DD2's `max_excluded_files` and digest conditions all depend on that argument being present. | Plan L550: "**fail-closed 셋이 여기 걸린다** — git 호출 실패 · 빈 tracked 목록 · `--floor-from` 부재" — `--exclude-from` is the very token Task 7 (L665) elects as the wiring-cut target, and the plan itself concedes at L678-681 that its removal is observationally inert when the exclusion list lands at 0 items. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified every load-bearing citation rather than trusting it: `run.js:10-12` and `:725` (ok = measurement-succeeded, exit code is `result.ok`), `:179-183` (`Number(r.exit_code)\|\|0` folds null→0, confirming DD3's stage-0 argument), `:489-505` (spawn failure yields `exit_code:null, per_file:null, redaction_ok:true` — the exact combination DD3 stage 0 targets), `:170`/`:189`/`:589` (emitted `per_file` is `[]` not `null` — plan's R5 correction is right), `:577-578`, `:575-576`, `:265-330`/`:321-326` (`validateElement` blocks `redaction_ok!==true`), `:676-679`, `enumerate.js:49-76`, `:84-90`, `globToRegExp`, and the four mirror ranges in `env-contract-drift.yml:1-46`/`:27-29` and `test-suite-baseline.yml:19-25`/`:60-71`. All say what the plan claims. I then attacked the invariants empirically: (a) tried to make `unexplained===0` tautological — it is not, because the denominator comes from git at judgment time while the numerator comes from the measurement, so `--files-from`, a shrunk run step, or a deleted run step all land fail-closed; (b) tried the one-line glob gate-opener the plan calls CRITICAL — `**`→`.*` confirmed, and `max_excluded_files` does close it; (c) tried to find a file that runs but never appears in `per_file` (fully-skipped or import-crashing file) which would produce permanent unexplained-block — `reporter.mjs:31-35` documents that even an import-crash file emits its nesting-0 roll-up, so presence holds; (d) checked the join-key boundary between `per_file` (redacted) and `tracked` (git) — `redact.js:245-269` returns repo-relative POSIX for in-repo paths, so the shapes match and there is no silent key mismatch; (e) checked whether cutting the `--exclude-from` argument leaves a judge alive to report red — it does, and Task 7 (e3) asserts it; (f) checked whether a missing `--exclude-from` on the gate line opens the gate — it fails safe (quarantined files become unexplained); (g) checked DD1's claim that the baseline's `pull_request` trigger's stated rationale is dead — the file's own comment (`:11-17`) matches. I found no HIGH; the four items above are the defects I could actually evidence. |
| security | pass | 인용 검증: DD3 stage-0의 fold 논증(run.js:179-183, 489-505 — spawn 실패가 `ok:false ∧ exit_code:0 ∧ redaction_ok:true`를 낸다)은 코드와 정확히 일치했고, `:725`의 `ok` 종료코드 계약, `:321-326`의 `redaction_ok !== true` merge BLOCK, `enumerate.js:84-90`의 digest 정렬·재계산 가능성, `globToRegExp`의 `**` 경로 구분자 통과(`**/*.test.js` 한 줄로 전 파일 격리 가능)도 전부 실재를 확인했다. 시도한 적대 시나리오: (1) `--files-from`으로 실행 집합을 조용히 축소 — gate가 git에서 tracked를 독립 해소하므로 `unexplained>0`으로 차단됨, (2) run 단계에서만 `--exclude-from` 제거 — `exclusions_digest` 불일치로 차단됨, (3) `--allow-codex` 주입 — Task 4의 `GITHUB_ACTIONS` 가드가 차단, (4) 격리 1줄 + 상한 상수 동반 상향으로 게이트를 열기 / 판정 모듈·workflow 자체를 같은 PR에서 무력화(`pull_request`는 PR head의 workflow로 실행된다) — 둘 다 DD4a가 입력·결과·닫지 않는 이유까지 명시적으로 기록하고 있어 은닉 결함이 아님, (5) git-tracked 컨테이너로의 절대경로/러너 경로 유출(§3.12 선례) — `validateElement`의 producer 봉인 + `RESIDUAL_PATTERNS`의 `posix-home`이 `/home/runner/...`를 잡고 merge가 fail-closed라 경로를 세우지 못했다. `redaction_degraded`가 `redaction_ok`와 `validateElement`에 반영되지 않는 축을 추적했으나 잔여 스캔이 degradation과 독립이라 결과에 도달하지 못했다. 유일하게 반증에 성공한 것은 위 per_file 인용 오류 하나다. |
| test | fail | plan 전문(994줄)과 PRD를 읽고, 인용된 코드가 실제로 그 말을 하는지 대조했다: `run.js:163-193`의 `foldChunks`(`Number(r.exit_code)\|\|0` · `per_file` push 조건), `:566-606`(`files_total = files.length` = 격리 후, `exclusions_digest` 봉인), `:407-410` `listTrackedFiles`, `enumerate.js` 전문(`normalizeExclusions`가 `ticket`을 버림 → digest 앵커가 ticket에 영향받지 않음 확인, `globToRegExp`의 `**` 확장이 DD2가 주장한 게이트 개방 경로를 실제로 만든다는 것 확인). DD3 0~3단계 판정이 Task 2b (a)~(j)로 실제 단언 대상이 되는지, DD9의 `--floor-from` 누락 fail-closed가 gate 분기에 걸리는지, DD2 분모 채널 짝 단언이 coverage 순수층이 아닌 gate 경로에 있는지 — 셋 다 배선돼 있어 반증하지 못했다. 축 D 절단이 게이트를 green으로 만드는 자기모순, `--exclude-from` 누락 시 digest 불일치로의 fail-closed 방향도 확인했고 결함 없음. 남은 넷이 위 findings다. |
| invariant | pass | Traced the full "everything went wrong" path against the cited sources. (1) Verified DD3's 4-stage order against `run.js`: confirmed `ok`/`exit_code` split (:10-12, :725), chunk spawn failure emitting `exit_code:null` + `redaction_ok:true` (:489-505), `Number(r.exit_code)\|\|0` folding null to 0 (:179), and `ok = measuredAll && attr.ok` (:205) — stage 0 does close the "never ran" hole; but found the per_file shape claim inverted against :213-214. (2) Attacked the `exclusions_digest` anchor: `normalizeExclusions` strips `ticket` so the digest covers pattern+reason only, but both sides use the same function and `ticket` is enforced at load, so no hole. (3) Attacked the wiring-cut target: removing `--exclude-from` from the gate line falls closed when the list is non-empty (digest mismatch / unexplained > 0) and is inert when empty — the plan states this honestly. (4) Attacked the DD9 floor ratchet: `floor = 0` satisfies both the gate (`tracked >= floor`) and the specified paired test (`floor <= tracked`), but DD4a already discloses that all ratchet constants are PR-editable and declines to close that axis with a stated reason. (5) Checked artifact-before-judgment, `if: always()`, `continue-on-error` absence, `--allow-codex` CI guard, and the run-step-fails-so-gate-never-runs path — all fail closed or disclosed. (6) Checked the "no approval receipt" preamble against the informational allow-path and §3.16 audited escapes — reason is recorded, chain state is measured. No HIGH/CRITICAL erosion found. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 279795,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:319bcd6808c93083c9133ff28f78e0c3aa9868b010803ae1766b805684a4cd70",
  "plan_path": ".claude/plans/ci-full-suite-m3e.plan.md",
  "recorded_at": "2026-09-04T02:38:31.628Z"
}
```
