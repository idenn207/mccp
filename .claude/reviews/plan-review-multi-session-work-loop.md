# Plan Review Panel — multi-session-work-loop (M5)

**Plan**: `.claude/plans/multi-session-work-loop-m5.plan.md` · **Plan version**: `sha256:63146d1badf7c3d079a6683b85e96c70bdef7615abb388f1525950f16fe93cbb`
**Verdict**: **divergent** via `multi-agent` · **round 9 (최종 — 운영자 지시로 종료)**
**Quorum**: 4/3 responses · 4 roles — **passed: false** (pass 1 / 필요 3)
**Layers**: L1 `converged` · L2 `divergent` · L3 not fired (`multi-agent` 모드)

> **receipt는 작성되지 않았다.** 패널 경로는 `review_proof` 없는 verdict를 봉인하지 못하도록
> 설계돼 있고(fail-closed), divergent에는 proof가 생기지 않는다. **이 부재가 정직한 기록이며 승인을 위조하지 않는다.**

> **종료 사유**: 운영자 지시(2026-08-12) — 미승인 상태로 진행한다. 9라운드 동안 **3명 동시 pass가 한 번도 없었고**,
> 마지막 두 라운드의 실질 소득이 7축 → 2축으로 줄었으며, round 9의 invariant 응답은 이미 기각된 범주로의 회귀였다.

> **리뷰어 등급.** `CLAUDE_CODE_SUBAGENT_MODEL: "haiku"` + `plan-review.js:176`의 `effort:'low'`.
> architect가 round 3·7 두 번 `StructuredOutput` 미호출로 null을 반환했다.

## 라운드별 추이

| Round | architect | security | test | invariant | findings | 비고 |
|---|---|---|---|---|---|---|
| 1 | fail | fail | fail | fail | 19 | |
| 2 | **pass** | fail | fail | fail | 15 | |
| 3 | _(agent 오류)_ | **pass** | **pass** | fail | 5 | StructuredOutput 미호출 |
| 4 | _(세션 한도)_ | fail | _(세션 한도)_ | _(세션 한도)_ | 5 | **환경 실패** — coverage 1/3, 판정 불성립 |
| 5 | fail | fail | fail | fail | 14 | |
| 6 | **pass** | fail | fail | fail | 19 | |
| 7 | _(agent 오류)_ | fail | fail | fail | 13 | StructuredOutput 미호출 · coverage 3/4 |
| 8 | **pass** | **pass** | fail | fail | 11 | coverage 4/4 · pass 2 — 최선 기록 |
| 9 | fail(MEDIUM만) | **pass** | fail | fail | 11 | 최종 — invariant 5건 전부 "코드가 아직 없다" 범주 |

**수렴하지 않았다.** 라운드 2·3·6에서 개별 리뷰어가 pass로 전환했으나 **동시에 3명이 pass한 적은 없다**. plan은 매 라운드 더 엄격해지기만 했으므로(잔여 명시 · lint 축 · 기계 검사 추가) 이 진동은 plan 품질의 하락이 아니라 **동일 입력에 대한 리뷰어 판정 분산**이다 — PRD가 지목한 "판정 위치만 바꿔도 결과가 흔들린다"의 실측 사례이며, diverse-agent-review PRD가 다룰 축이다.

## Findings (round 9 — 최종, 운영자 지시로 종료)

> 입력은 round 8 흡수본 `sha256:63146d1badf7c3d079a6683b85e96c70bdef7615abb388f1525950f16fe93cbb`.
> **security는 2라운드 연속 findings 0으로 pass**. architect는 round 8의 pass에서
> **MEDIUM 3건만으로 fail**로 되돌아갔고, invariant는 **5건 전부가 "코드가 아직 없다"** 범주였다
> — 라운드 2·5에서 이미 기각됐고 패널 자신도 그때 수용했던 범주다.

> **리뷰어 계약의 미정의 구간.** 계약은 *"Any HIGH/CRITICAL finding means verdict=fail"* 과
> *"pass ONLY when you could not find a defect"* 를 함께 적어 **MEDIUM-only의 처리를 규정하지 않는다**.
> architect는 이를 fail로 해소했다. 이 구간이 판정 진동을 키운다 — diverse-agent-review PRD 소관.

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | **MEDIUM** | Files to Change list is incomplete — test file for journal-store.js missing from manifest | Validation section line 442 calls `node --test plugins/mccp/scripts/state/tests/journal-store.test.js` but this file is NOT listed in Files to Change (lines 181-220). Task 2's Validate section explicitly depends on this test file. All other test files created by the plan are listed in Files to Change table (lines 203-208), making this an inconsistency. |
| architect | **MEDIUM** | G3 abstraction relies on parser asymmetry that could break under future changes — no mechanism prevents divergence in consumption paths | Plan acknowledges (Line 135) that state-writer.readState and state-injector.readState diverge on YAML block scalars (`next_chunk: \|` field). state-writer parses as multi-line string, state-injector parses as `"\|"` literal. Plan 'redefines' G3 from 'mutual equivalence' to 'per-parser M5-before/after equivalence' (weakening the invariant). Injection parser doesn't currently consume `next_chunk`, but if future code adds a required field using block-scalar format, injection breaks silently. Test pins this divergence to prevent *widening*, but doesn't prevent future code from depending on the diverging field. |
| architect | **MEDIUM** | Single-writer lint (Task 7 axis 3) cannot catch all CL-5 path failures — implicit aliases and transitive dependencies escape static analysis | Plan states (line 375-376) that lint checks: (a) path argument is passed, (b) it's not literal `process.cwd()`. But then admits (line 417-418): 'Bash 경로 리터럴 검사를 통과하고, `enumerateUnfinishedItems(cwd)`처럼 **잘못된 변수를 바인딩한 경우**는 이름만으로 판정할 수 없다.' Plan's fix: add `cwd ≠ repoRoot` runtime fixture. But if implementer accidentally binds `const wrongRoot = …; enumerateUnfinishedItems(wrongRoot)`, both lint (axis 3 checks it's not `process.cwd()` literal) AND test (assumes wrongRoot was set correctly) would pass. Documented as 'residual 9' but no mechanical gate prevents this — it's human-dependent. |
| test | **HIGH** | Task 0 creates a machine-readable assertion manifest and Task 0's Validate checks that assertions exist as test titles in actual files ('구현 후에는 누락을 실패로 만든다' — after implementation, missing assertions should fail). | Plan §Task 0 (line 325-328): states that `single-writer-lint.js --assertions` 'contrastes si todos los títulos existen' and '구현 후에는 누락을 실패로 만든다'. However, Validation section 10 (line 488) only checks 'node plugins/mccp/scripts/lib/state-journal/single-writer-lint.js --json   # exit 0 필수' with no requirement that all manifest entries are marked as existing. The Validation doesn't check whether the lint reports any assertions as 'absent' vs 'exist', only that exit code is 0. This means incomplete test implementations (manifest entries without corresponding test() titles) would pass validation. |
| test | **MEDIUM** | All new test files mentioned in Validate sections (state-journal-order.test.js, state-journal-projection.test.js, etc.) will exist and contain the specific test() assertions listed in the manifest. | Plan specifies new test files in Tasks 1-8 and lists detailed assertions each should contain (Task 3 Validate lists 7 specific assertions, Task 4 lists 5, etc.). However: (1) None of these test files currently exist (verified by glob search), (2) The manifest file that ties them together doesn't exist yet and is created BY Task 0 AS OUTPUT, creating circular dependency where manifest must reference test titles that haven't been written yet, (3) No mechanism prevents implementer from creating empty test files or files without the required test() titles — they would pass Validation section 3 (node --test finds 0 tests = passes) and Validation section 10 (lint exits 0). The only path to validation success requires actual test implementations matching manifest entries, but this is not mechanically enforced. |
| test | **LOW** | Task 3 Validate assertion 2b ('양쪽 파서의 기대값 단언') ensures that if both state-writer and state-injector parse M5 output, they return correct values for REQUIRED_FRONTMATTER_KEYS and optional fields. | Plan line 348-349 explicitly states that assertion 2 alone is insufficient because 'two parsers could fail identically and still pass (a)=(b)'. Then it claims 2b adds protection by fixing expected values in fixtures. However, the test described at line 348 says both parsers must return 'same expected values', but this only works if the expected values in the fixture are correct. If the fixture sets wrong expected values (e.g., for next_chunk which is documented as diverging at line 135), then both parsers would be tested against the same wrong values and both would pass even if the round-trip is actually broken. The plan acknowledges this gap at line 348: '(R8 지적대로 2·2b만으로는 `next_chunk` 같은 *선재 divergence* 필드의 정확성까지 답하지 못하며'. |
| invariant | **HIGH** | A4 producer will ship artifacts enabling G5 verification - the plan claims 'deployed after M5 implementation + actual measurement' | Plan lines 40, 155, 384: 'G5의 확인에는 plan 실행 밖 3단계가 필요하다'. But session-end.js line 381-382 and session-start.js line 748 still call handoffItems without correct opts - the bug unfixed means writeHandoffItems still defaults to cwd-relative stateDir (line 144 of handoff-items.js). Derive source at handoff-items.js line 29 searches repoRoot-absolute path. These never intersect. Plan admits at line 148: 'A4 producer는 한 번도 아티팩트를 남기지 못했다' |
| invariant | **CRITICAL** | Degraded mode protects STATE.md from unanchored journal write failures (G1) - 'append 실패 → loud degraded 마커 + 투영 경로 우회' | Current state-writer.js lines 614-628 has zero degraded mode logic. withStateLock at lines 563-567 does silent proceed-without-lock for advisory lock failure. No .degraded marker exists. Plan promises Task 3 adds this, but claims made before implementation means validation cannot verify the circuit breaker actually prevents G1 violation. If Task 3 incomplete, silent failures during journal append still cause STATE.md writes to proceed (line 625: writeStateAtomic called regardless) |
| invariant | **CRITICAL** | CL-5 path bug is fixed - 'Task 8의 CL-5 4번째 재발 수정' | Plan correctly identifies M3 & M4 fixes at session-start.js lines 714-719 and 740-742. But session-end.js lines 381-382 still have the pattern: `enumerateUnfinishedItems(process.cwd())` + `writeHandoffItems(sid, unfinished)` with no opts. Session-start.js line 748 identical: `restoreAndMatch(observerSessionId)` with no opts. These match the exact defect pattern cited in the plan. Not yet fixed means A4 denominator remains unreachable |
| invariant | **HIGH** | Skip marker prevents false attribution of missed CL-5 fixes - 'SHIP-2가 3-state로 판정한다' | Plan §DD6 lines 400-416 promises `.handoff-root-unresolved` marker and `handoff_root_unresolved` msw-event. But neither mechanism exists yet. Plan also says skip path exists in resolveHandoffRoot when projectRoot is empty. The skip logic (line 395-397) doesn't have the marker write - only a loud warn + skip. Without the marker, Validation-SHIP-2 cannot distinguish 'CL-5 fixed' vs 'path resolution failed' vs 'hook never ran' |
| invariant | **MEDIUM** | STATE.md consumer code contract is invariant - G3 '호출부 단위로 정의하고, Task 7 lint가 그 분리를 기계 검증한다' | Task 7 axis 2 lint doesn't exist yet. Plan at line 375 promises to check 'git diff origin/main...HEAD'에서 그 줄이 변경되면 실패'. But the machine cannot verify this before code exists. Additionally, handoff-items.js must NOT be modified (Task 8 line 377: 'handoff-items.js가 저널과 통합하지 않는다'), but the lint (axis 4) checking `import` statements doesn't exist. No mechanical enforcement means the invariant is a declaration not a gate |

## Refutation attempted (round 9)

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | `fail` | Verified G1-G5 guarantees against implementation: checked state-writer.update() rewriting to use journal append + projection pathway with atomic rename, confirmed renderState signature preserved. Verified G2 replay defense (order.js admit enum precedence: tombstone > epoch) against DD2 use of session-ledger.created_at rather than inventing new epoch type — sound reuse. Verified G3 parser asymmetry is pre-existing and tested with fixture pin — architecturally necessary compromise not new defect. Verified G4 retention via checkpoint compression with test asserting pre/post equivalence. Verified G5 has explicit fail-open handling (ship verification separate from implement loop). Verified handoff-items independence (axis 4 lint forbids state-journal import). Checked all task Validate sections have concrete pass/fail criteria. Verified ledger-based genesis tombstone seeding with fallback for missing ledger. Searched for cyclic dependencies, subprocess injection, and load-bearing assumptions in narrow environments — found none. Confirmed DD1-DD11 design choices are reasonable and documented. The Files to Change list omission of journal-store.test.js is the only structural gap; the parser asymmetry and path-binding brittleness are documented as known limitations rather than undetected defects." |
| security | `pass` | **Lens applied**: Attack surface (trust boundaries between journal/STATE.md/completion-ledger), data leakage (machine names, absolute paths in durable artifacts), bypass paths (degraded mode escape, marker write failure), tamper surface (incomplete record hashing chain — acknowledged as out-of-scope per single-operator threat model). **Attacked**: 1. CL-5 path bug — confirmed real, plan correctly fixes in Task 8 via `resolveHandoffRoot` + explicit `opts` passing 2. Degraded marker failure window — plan specifies throw + recovery via journal-verify; acceptable under fail-open hook contract 3. Completion-ledger bootstrap tampering — git-tracked, visible in history; acceptable under single-operator model   4. Handoff-items independence (axis 4 of Task 7 lint) — ensured by import check; plan prevents entanglement with journal 5. Record schema for information leakage — fields are `record_id`, `ts`, `session_id`, `session_epoch`, `work_unit`, `seq`, `kind`, `patch`, `prev_session_id`, `superseded_by`, `checkpoint_of`, `content_hash`; no new absolute paths or machine identifiers beyond existing `patch` data 6. O_APPEND atomicity — plan relies on documented precedent (msw-events.js); platform-specific behavior not stated but not a security failure in design 7. State projection atomicity between journal append and STATE.md write — gap permitted under advisory lock model; closure verified by next session's verify via mtime inference 8. Three-channel A4 producer validation (marker + event + session_end) — correctly handles marker write failure via independent session_end proof; no false negatives possible 9. Degraded mode exit via `journal checkpoint --reseed` — correctly specifies this is manual/explicit (no auto-exit); documented in DD6.1 **Could not land**: privilege escalation through env toggles (plan restricts to `MCCP_STATE_JOURNAL` single toggle, 3 values, no self-modification), path traversal via crafted `decision_id` in completion-ledger (requires git push access = outside threat model), silent state loss during recovery (design prevents via multi-channel detection), leakage into git-tracked ship receipt (plan keeps ship-receipt separate from working-tree journal per residual 1). |
| test | `fail` | Attacked three core test falsifiability claims: (1) That the manifest completion check is tested by requiring non-zero exit on missing assertions — found Validation §10 only requires exit 0 with no check for assertion presence status. (2) That new test implementations must match manifest entries — found no mechanical enforcement; empty test files or missing test() titles would pass sections 3 and 10 of Validation. (3) That state-parser round-trip tests catch parser correctness — found the test relies on fixture values that must themselves be correct, acknowledged as insufficient for divergent fields like next_chunk. |
| invariant | `fail` | Attacked G1-G5 guarantees through: (1) verified CL-5 4th occurrence unfixed in current code vs M3/M4 fixes visible (session-end.js line 381-382, session-start.js line 748 call handoffItems without opts while M4 fix 3 lines away passes stateDir); (2) confirmed journal modules don't exist yet (no state-journal/ directory), so degraded marker and write logic unimplemented; (3) verified A4 artifact production still broken (handoff-items derive source scans repoRoot-absolute path, writer still defaults to cwd-relative); (4) confirmed degraded mode branching logic absent from state-writer.update() (lines 614-628 have no degraded check, marker write, or throw path); (5) verified Task 7 lint not yet written (no single-writer-lint.js file), so axis 2 & 4 mechanical enforcement missing. All five gaps prevent post-ship verification of G1-G5." |

<details>
<summary>round 8 findings 전문 (보존)</summary>
## Findings (round 8)

> 입력은 round 7 흡수본 `sha256:e76e8f2ee41370c8ba373f3caadbe6e71f1b035de9c104909bd35503613018dc`.
> **architect·security 모두 findings 0으로 pass**했고 coverage 4/4 — 8라운드 중 처음이다
> (round 3은 2 pass였으나 architect가 오류로 빠져 coverage 3/4였다). quorum 3 미충족으로 verdict는 여전히 `divergent`.

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| test | **CRITICAL** | Task 0 Validate will check that all test assertions from the manifest exist as test titles in test files | Plan line 305-307 requires `m5-assertion-manifest.json` with `{task, id, title}` entries. Plan line 307 Validate line says: '`node plugins/mccp/scripts/lib/state-journal/single-writer-lint.js --assertions --json` — 매니페스트 항목 수 > 0'. However, the manifest file does not exist (confirmed by glob search), is not listed in Files to Change table (lines 186-219 CREATE/UPDATE list), and no task is assigned to create it. The validation command will fail when attempting to read a non-existent file. |
| test | **HIGH** | G5 completion can be validated by checking if plugin.json version exists in ~/.claude/plugins/cache/ | Plan §G5 (lines 36-42) states: 'ship 시점 검증 단계로 명문화한다: PR 생성 전 implementer가 `claude plugin update` + 새 세션 1회를 수행하고 `*.handoff-items.json` ≥1건 · A4 status `computed`를 **실측**한다.' Validation §8 (line 450-458) includes a Node script that checks if the plugin cache directory exists. However, this check cannot run during the PR gate validation—it requires running in the implementer's actual Claude environment after plugin deployment. This is post-deployment verification, not a test falsifiable within the PR gate itself. |
| test | **HIGH** | Task 3 Validate (line 326-328) tests that both state-writer.readState and state-injector.readState produce correct values from M5 output | The plan explicitly admits (line 135): '두 파서는 오늘도 같은 답을 주지 않는다 (실측)' on the `next_chunk` field. Validate point 2 (line 326) claims: '파서별 M5 전후 동등 — 그 산출을 `state-writer.readState`로 읽은 결과가 (a)=(b), `state-injector.readState`로 읽은 결과가 (a)=(b).' This tests only that each parser produces consistent output before/after M5, not that they produce *correct* output. If both parsers produce wrong values in identical ways, the test passes. Point 2b (line 327-328) adds assertions that injector parses M5 output successfully, which helps, but G3's claim requires both consumers receive correct data, not just consistent data. |
| test | **MEDIUM** | CL-5 fourth occurrence fixed by adding three lines to pass explicit arguments (line 370-372) | Plan line 14 claims '**원인은 CL-5의 4번째 재발**' but only names 3 specific instances with line numbers: session-start.js:709-712 (M3), session-start.js:735-741 (M4), session-start.js:748 (current M5). The plan refers to a 4th occurrence but provides evidence for only 3. Additionally, Task 8 Validate (line 399) includes `(e) **fixture가 `cwd ≠ repoRoot`임을 자체 단언**` but line 396 admits: 'test가 `cwd === repoRoot`인 환경에서 돌면 두 경로가 우연히 같은 곳으로 풀려 통과한다.' This means the test can pass even with wrong code if the fixture's `process.chdir` call doesn't actually change the effective working directory due to symlinks, network paths, or case-folding on Windows. |
| test | **MEDIUM** | Task 7 lint will detect all forms of passing wrong CL-5 arguments (lines 354-356) | Plan line 354 describes lint axis 3 checking whether CL-5 functions receive explicit path arguments. Plan line 354 extends detection to '1-hop 지역 별칭'. But then explicitly admits (line 356): '**임의 깊이 별칭·재할당·고차 함수 경유는 여전히 못 잡으며, 그것은 정적 검사의 한계**' and relegates this to '**명시 잔여 9**'. The lint therefore will not catch cases like: `const getRoot = () => process.cwd(); enumerateUnfinishedItems(getRoot())`. No test or lint is claimed to catch such indirect forms, only documented as an unresolved gap. |
| test | **MEDIUM** | G1 ('모든 상태 변형이 손실 없이 append된다') is validated by tests showing append never fails silently | Plan Tasks 2-3 claim journal write failures result in loud stderr + degraded marker. However, plan line 250 describes degraded mode: 'append 실패는 `.claude/state/journal/.degraded` 마커를 남기고, **마커가 존재하는 동안 `update()`는 투영을 하지 않는다** — M5 이전의 직접 경로로 동작한다.' This means on append failure, M5 falls back to direct STATE.md write (line 250). But plan DD6 (line 235-236) claims: 'DD6 — 저널 write 실패는 조용한 직접 쓰기로 폴백하지 않는다' contradicting the degraded mode description. The claim about 'zero data loss' must be qualified: data written via direct path during degraded mode is not in the journal and Validate Task 5 (line 342) compression test assumes journal-backed state. |
| invariant | **CRITICAL** | Degraded mode provides fail-closed protection against journal write failure | Plan lines 235-273 (DD6, DD6.1): Degraded mode allows STATE.md direct writes while journal is offline. The claimed 'blocking' (line 256) only applies to genesis bootstrap (EX_TEMPFAIL), not ongoing writes. Critical gap: if writeDegradedMarker fails in fail-open hook context (line 268), there is no exception thrown, creating a silent failure window where journal append failed AND marker write failed AND STATE.md was written. The incomplete journal is never detected until SHIP-2 runs at PR time. |
| invariant | **CRITICAL** | G5 acceptance gate enforces completion of A4 metric transformation before allowing proceed | Acceptance lines 578-580: G5 has OR gate ('충족' OR '미달'). Validation-SHIP runs only 'PR 생성 직전 1회' (line 475), not in implement loop. SHIP-2 3-state includes 'inconclusive' which says 'do not report G5 as met' but does not specify blocking PR (lines 521-524). Additionally, this plan has no L2 receipt (L2 divergent after 7 rounds, line 624), meaning `/mccp:pr` gate receives no plan-review receipt to anchor approval. |
| invariant | **HIGH** | Single-writer lint provides fail-closed enforcement against unauthorized STATE.md writes | Task 7 line 358: Plan explicitly states lint will NOT be '강제 게이트로 승격' to `/mccp:pr` Phase 1. Validation §10 runs it advisorily during implement (line 467), not as gated blocker. CL-5 recurrence four times attributed to 'lint not in通過 path', yet this fix leaves it advisory only. |
| invariant | **HIGH** | MCCP_STATE_JOURNAL toggle provides rollback to pre-M5 behavior | Line 295, 554: Toggle has three modes (enforce/shadow/off) but plan never specifies: (a) when shadow mode is auto-triggered vs manual, (b) how long it persists, (c) how it blocks writes while active, (d) test assertion that shadow mode actually restores old behavior. 'Recovery switch' is mentioned but invariant not defined. |
| invariant | **MEDIUM** | Journal append operation is protected against data corruption at write time | Plan discusses content_hash verification post-read (DD6.3, lines 280, 345-347) but never specifies write-time integrity protection. O_APPEND borrowed from msw-events provides only single-buffer atomic syscall semantics, not protection against sector corruption, kernel buffer corruption, or cross-process writes. Digest coverage catches post-read corruption only. |

## Refutation attempted (round 8)

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | `pass` | Verified core invariants (G1-G5): (1) update() signature unchanged, callers unmodified; (2) readState() has no replay logic, preserving G3 contract; (3) degraded marker placement is in critical section after lock, enabling sticky mode; (4) journal-store is unidirectional from state-writer; (5) handoff-items boundary correctly maintained (Task 7 Axis 4). Confirmed CL-5 is real and live (session-start.js:748 missing stateDir, session-end.js:381-382 passing process.cwd()). Verified project() signature uses caller-injected base parameter, preserving DD4 purity. Checked retry loop structure, abort semantics (DD6.1 throw-at-marker-write), and genesis bootstrap from completion-ledger (DD11). Single-writer lint targets writeStateAtomic, which is the only public STATE.md write path. Dependency graph is acyclic: callers→update()→journal-store.appendRecord(); journal read only by a4-boundary-restore; no circular coupling. Task 7 lint covers: direct writeStateAtomic calls (Axis 1), stateWriter.update caller-lines (Axis 2), CL-5 enumerateUnfinishedItems/writeHandoffItems/restoreAndMatch signatures with non-cwd arguments (Axis 3), handoff-items avoiding journal import (Axis 4), project.js avoiding I/O imports (Axis 5). Boundary protection through fixture (cwd≠repoRoot) + lint (1-hop alias detection). G3 caveat (two parsers are not mutually equal) is pinned with fixture in validation. Degraded recovery (journal checkpoint --reseed) is explicit operational step, not automatic, preventing silent mode inconsistency. Plugin.json version cache check (Validation §8) mechanically gates G5 "实測" prerequisite. Spot-checked Files to Change align with 10 Tasks." |
| security | `pass` | Examined attack surface along trust boundaries and data handling: (1) CL-5 path bug 4th recurrence — plan fixes 3 locations with resolveHandoffRoot + walk-up fallback + 2-channel skip detection (marker+msw-event+session_end audit); (2) Genesis bootstrap from completion-ledger — handles corruption with warnings, doesn't silently accept; (3) Degraded mode — proper fallback with marker-based drift detection via mtime inference; (4) Single-writer invariant — 5-axis lint including 1-hop alias detection for incomplete fixes; (5) Absolute path leakage — moving to append-only journal reduces surface; cwd-mask handles residual; (6) Content integrity — sha256 hashing + admits adversarial tampering out of scope per single-operator threat model; (7) Completion-ledger tamper resistance — git-tracked with audit trail, temporary in-session deletion before bootstrap could allow replay but mitigated by git history; (8) File permissions — not explicitly specified but msw-events.js precedent applies (0o600 assumed); (9) Transitive dependency violation of project.js purity — lint covers direct imports, test with fs stubs covers transitive. All documented residuals (9 listed) are within threat model scope. Defense-in-depth layering (marker channels, epoch ordering, multiple verification axes, mechanical lint enforcement) prevents silent failures. |
| test | `fail` | Attacked falsifiability of five key guarantees: (1) Task 0 manifest file non-existence—confirmed it doesn't exist and Files to Change doesn't create it; (2) G5 plugin deployment check—examined Validation §8 and found it's post-deployment, not testable in PR gate; (3) G3 parser equivalence—read test spec (Task 3 Validate) and found it only tests consistency, not correctness; (4) CL-5 occurrence count and test robustness—examined claims vs named sites and found only 3 instances named as 4th, and fixture assumptions about path resolution; (5) Task 7 lint coverage—read plan description and found explicit admission of unhandled alias forms. All five findings anchor to concrete text locations in plan and code. |
| invariant | `fail` | Attacked fail-closed drift: degraded mode allows STATE.md direct writes when journal fails; marker write failure in hook goes silent. Attacked skip predicates: G5 has inconclusive SHIP-2 state with no hard block. Attacked rollback reality: MCCP_STATE_JOURNAL modes mentioned but trigger/duration/blocking behavior unspecified. Attacked receipt anchoring: plan has no L2 receipt (divergent), SHIP-2 has no blocking clause for inconclusive, Validation-SHIP only runs at PR time not in implement loop. Attacked HALT vs degrade: degraded mode degrades to direct write rather than halting. Attacked digest coverage: content_hash only post-read, not at write time. |

</details>

<details>
<summary>round 7 findings 전문 (보존)</summary>
## Findings (round 7)

> 이 라운드의 입력은 round 6 흡수 편집이 반영된 plan version `sha256:ef441f9cb8f76f01dd4b5ce36470c6077e16da3f5b21f34fde4123d025d6d17b` 이다
> (round 6은 `sha256:ef654bd5…`). 즉 리뷰어가 본 것은 새 본문이다.

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| security | **HIGH** | Marker write failure in resolveHandoffRoot bypasses the transparency mechanism, violating documented guarantee to distinguish failure modes | L363-364: Plan commits that SHIP-2 reads marker to distinguish causes ('그 마커를 읽어 0건의 원因을 구分한다'). L366-369: SHIP-2 validation checks marker existence via fs.existsSync() to distinguish 'producer not run' vs 'root unresolved'. L357-365: Plan describes resolveHandoffRoot skip behavior but doesn't specify marker write failure handling. No requirement for fail-closed or exception-on-failure. If marker write fails silently, SHIP-2 reports false root cause, violating the documented transparency commitment in L564. |
| test | **HIGH** | G3 — STATE.md render output is byte-identical before/after M5 across two independent parsers (state-writer.readState and state-injector.readState) | Plan §Task 3 Validate and Acceptance (line 309-315) specify: '동일 patch 시퀀스를 (a) M5 이전 경로와 (b) 저널 투영으로 각각 렌더한 결과가 byte-identical'. The test mirrors existing state-writer.test.js. However: (1) the test input source is **undefined** — are patches synthetic or captured from real STATE.md mutations? Synthetic patches won't exercise all code paths in real mergeState. (2) The plan acknowledges (line 131) that two parsers diverge on next_chunk block scalars: 'state-writer.readState는 `"line one\ntask_fingerprint: HIJACKED\nline three"`, state-injector.readState는 `"\|"` 를 반환한다'. The 'divergence pin' fixture ensures this divergence stays stable, but does NOT prove correctness of either parser. If both parsers silently fail on a different edge case (e.g., triple-quoted block scalars), both would remain broken while test passes. G3 claim should require testing against **golden captured STATE.md files from production**, not synthetic test fixtures. |
| test | **CRITICAL** | G5 — A4 metric's numerator will transition to 'computed' status, validated by real measurement after deployment | Plan §G5 conditions (lines 35-45) state: '확인은 배포 후 실측이며 코드 존재는 판정 근거가 아니다(UI9)'. Validation-SHIP §2 (lines 456-478) checks: (1) `*.handoff-items.json ≥1 建` (2) A4 status = 'computed'. But the test does NOT verify: (a) the *values* in A4 (could be computed as 0/0 or negative denominator); (b) the handoff-items file *contents* are correct (only file count); (c) that the hook *runs at all* in the new session (only checks cache directory exists). The plan acknowledges M4 env-snapshot producer '프로덕션에서 산출하지 못했' (line 13-14) due to plugin cache not being deployed. Validation-SHIP §1 (§8, lines 420-428) checks cache directory exists **at plan-review time**, but deployment happens at PR merge time — the version could become stale between review and merge if other PRs ship in between. Line 423 says `if(!require("fs").existsSync(d))` fails the check, but this is **OS/environment-dependent** and doesn't test the hook actually *functions*. |
| test | **HIGH** | CL-5 4th recurrence will be fixed in all 3 call sites (enumerateUnfinishedItems, writeHandoffItems, restoreAndMatch) without partial misses | Plan §Task 8 (line 349-368) lists 3 fixes in a table (lines 351-355). The defense is: (1) Task 7 axis 3 lint checks for `process.cwd()` literal; (2) test fixture creates `cwd ≠ repoRoot` to catch function mismatches. But: (a) The lint only catches **syntactic** `process.cwd()` patterns. It will miss `const f = process.cwd(); enumerateUnfinishedItems(f)` or indirect calls via higher-order functions. (b) Even with the fixture, if the test only exercises 1 or 2 of the 3 functions, a partial fix passes. Line 359 says 'CL-5 수정은 3곳이다' but then states 'read 실패 시 walk-up' — if readers use different resolution logic than writers, the fix is still broken. (c) Task 7 axis 3 validates existence of `opts` but line 367 says 'lint는 환경에 의존하지 않으므로 우연한 통과가 원리적으로 불가능하다' — this is false. Static lint can't verify that the value passed in `opts` is actually the right path if it's computed from a variable. |
| test | **MEDIUM** | Degraded mode will be sticky — once `.degraded` marker exists, all subsequent update() calls skip projection until --reseed | Plan §DD6.1 (lines 243-257) describes degraded mode as 'sticky': '마커가 존재하는 동안 `update()`는 투영을 하지 않는다'. But Acceptance (line 503) just lists 'degraded 모드(DD6.1)' without specifying which test verifies **stickiness across multiple calls**. The Validation steps §1-§7 don't enumerate a specific test that: (1) simulates append failure; (2) verifies marker created; (3) calls update() again; (4) verifies it uses direct path; (5) calls update() a third time; (6) verifies it **still** uses direct path and hasn't switched back. Without this multi-call test, a one-time direct write on failure could masquerade as sticky degraded mode. |
| test | **MEDIUM** | A4 value computed from journal boundary will be plausible and not include self-credit or genesis boundaries | Plan §Task 8 (line 346) claims `a4-boundary-restore.test.js` has 'single assertions: (a) 같은 세션의 sidecar만 있을 때 분자 0(self-credit 불가) (b) genesis 경계만 있을 때 `insufficient`'. Line 370 further adds test clause (d) '분자 ≤ 분모'. But: (1) No test checks that A4 numerator and denominator are **non-negative integers**. A4 could compute as {numerator: -5, denominator: 10} and pass all stated assertions. (2) No test checks denominator > 0 when claiming status='computed' — insufficient genesis case sets status='insufficient', but no test verifies that 'computed' cases have denominator ≥ 1. (3) Validation-SHIP §2 (lines 472-478) only checks status field, not values. These missing tests mean a malformed A4 computation (negative values, zero denominator, etc.) would not be caught. |
| test | **MEDIUM** | Project() function is pure and produces identical results to M5-previous mergeState pipeline | Plan §Task 3 (line 305-307) claims `project(records) → {frontmatter, body}` is 'pure reduce' with no I/O. Line 307 says 'renderState를 재작성하지 않고 **그대로 재사용**'. But the Validate test (line 310, unit 1) only checks byte-identity of the output. It does NOT check: (a) that project() doesn't call I/O functions (readFile, writeFile, network, etc.); (b) that project() produces identical intermediate states during reduction. A test that only checks final output could pass even if project() has side effects (e.g., logging to a file, mutating a global cache). The purity claim is asserted but not mechanically verified in the test suite. |
| invariant | **CRITICAL** | Marker write failure handling is unspecified for the most likely code path: append fails, STATE.md write succeeds, marker write fails | The abort table in DD6.1 (lines 236-242) covers: (1) append success, (2) append success with STATE.md fail, (3) append fails [with marker entry but no mention of marker failure], (4) reject-malformed, (5) append fails AND STATE.md fails AND marker fails→throw. Row 3 (append fails) does not explicitly state what happens if marker write fails after STATE.md succeeds. Line 256 asserts 'marker write failure causes throw' in DD6 rationale, but this is NOT in the explicit abort table specification. The Tasks (1-6) do not detail where marker write failure handling occurs or what causes update() to throw in the row 3 scenario. |
| invariant | **HIGH** | G5 ship-time verification (Validation-SHIP-2) can fail after PR is already created, creating a gate-after-delivery problem | Validation-SHIP-2 (lines 456-478) runs 'PR 생성 직전' but is separated from implement loop. If `*.handoff-items.json` does not exist and marker is present, the error message says 'CL-5 is NOT closed. Do not report G5 as met.' But at ship time, PR has already been created (the validation runs 'PR 생성 직전' which is ambiguous - before or after gh pr create?). Looking at lines 441-445, it clarifies SHIP is run '/mccp:pr 직전' - before PR creation. However, if verification fails, there is no fail-closed gate preventing PR submission - only advisory guidance to 'not report G5 as met'. The plan does not specify what mechanism prevents PR submission if ship verification fails. |
| invariant | **HIGH** | Degraded mode can prevent journal path operations while allowing direct STATE.md writes, creating a mixed-mode window where integrity is unclear | Lines 250-254 state: '.degraded 마커가 존재하는 동안 genesis 부트스트랩은 실행되지 않고 EX_TEMPFAIL로 멈추며' (bootstrap is blocked) but '단 이 거부는 저널 경로에만 적용된다 — STATE.md 직접 경로는 계속 동작하므로 세션이 막히지 않는다' (STATE.md direct path continues). This creates a window where: (1) journal operations are blocked, (2) STATE.md direct writes continue, (3) the SoT (source of truth) is ambiguous. The plan says this is intentional to 'avoid pipeline stop' (잔여 4), but this means degraded state is NOT fail-closed - it's a permissive fallback that continues operations. |
| invariant | **HIGH** | Abort table in DD6.1 (lines 236-242) has only 5 rows but does not cover all state combinations, leaving some scenarios unspecified | The abort table explicitly covers: (1) append+project success, (2) append success + STATE.md write fail, (3) append fails, (4) reject-malformed, (5) append fails + STATE.md fails + marker fails. Missing: (a) append fails + STATE.md succeeds + marker write fails (most likely in quota-exhausted scenarios), (b) append fails + marker check/write during first lock attempt, (c) partial marker write (some bytes written, then failure). The table refers to '아래' for details but line 244-257 does not enumerate all failure modes. Plan assertion (line 256) says 'marker write failure is not swallowed and update() throws' but the abort table does not guarantee this for all paths. |
| invariant | **CRITICAL** | Task descriptions do not specify where marker write failure handling code is located or what condition causes the promised throw | Task 2 (lines 300-303) specifies journal-store.js with appendRecord, readRecords, writeCheckpoint - no marker write. Task 3 (lines 305-315) specifies state-writer.update() rewire but does not detail marker write error handling in the code path. Task 6 (lines 327-334) specifies journal verify CLI with 3 checks but verify is a read-side diagnostic, not write-side error handling. The promised 'marker write failure → throw' is stated in DD6 rationale (line 256) but no Task or File to Change entry shows WHERE this error handling is implemented. Line 189 Files to Change lists 'state-writer.js \| UPDATE' but does not specify the error path. |
| invariant | **MEDIUM** | Plan pledges to fix CL-5 at 3 call sites but one of those (enumerateUnfinishedItems path resolution) may not be exhaustively checked by lint axis 3 | Task 8 line 349-355 specifies 3 locations for CL-5 fix: (1) session-end.js:381 enumerateUnfinishedItems(repoRoot), (2) session-end.js:382 writeHandoffItems with stateDir, (3) session-start.js:748 restoreAndMatch with opts. Line 367 says lint 'axes 3' checks '존재 검사만으로는 부족' and adds condition '(a) 세 함수 모두 경로 인자를 전달하고 (b) 그 인자가 process.cwd() 리터럴이 아니다'. However, the condition checks the ARGUMENT but not whether it's the CORRECT argument. For example, `enumerateUnfinishedItems(cwd)` (wrong variable name) would pass both (a) and (b) checks. The lint catches the `process.cwd()` literal but not wrong variable binding. |

## Refutation attempted (round 7)

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | _(agent 오류 — StructuredOutput 미호출, round 3과 동일)_ | — |
| security | `fail` | Traced the marker-based transparency mechanism from specification (L363-364 commitment) through implementation (L357-365 resolveHandoffRoot design) to validation (L466-469 SHIP-2 check). Verified that the plan makes an explicit commitment to distinguish two failure modes via marker file but does not specify failure handling for marker write itself. Checked whether exception handling is mentioned anywhere - found none. Confirmed that plan is detailed about other failure modes (DD6.1 degraded mode with loud errors and fail-closed requirements) but omits this for marker write. Verified impact: if marker write fails silently, SHIP-2 cannot fulfill its documented role to distinguish root causes, producing false audit trail for G5 acceptance record." |
| test | `fail` | Attacked two primary claims under G-series guarantees and Task validations: (1) G3's byte-identical parser equivalence — found test input source undefined (synthetic vs. real) and divergence-pinning doesn't prove correctness. (2) G5's A4 transition to 'computed' — found Validation-SHIP checks existence, not correctness; environment-dependent cache path; no hook-execution test. Also examined CL-5 3-site fix (found lint only syntactic, misses indirection), degraded mode stickiness (no multi-call test), A4 value plausibility (no range/sign checks), and project() purity (no I/O verification). Reviewed Validation §1-§10 and Acceptance criteria; all new test files don't exist yet (expected for plan), but test specifications lack detail on input generation, assertion coverage for edge cases, and environment/deployment timing assumptions. |
| invariant | `fail` | Attacked DD6.1 abort table completeness by enumerating all state combinations (append success/fail × STATE.md write success/fail × marker write success/fail). Found that the table covers 5 scenarios but the most likely degraded-mode path (append fails, STATE.md succeeds, marker fails) is unspecified. Verified Tasks 1-6 do not detail marker write error handling. Checked G5 ship-time validation gate for fail-closed guarantee and found it runs pre-PR but has no specification of what mechanism actually blocks PR if verification fails. Examined degraded mode gate design and found it is not fail-closed but permissively allows direct STATE.md path. Searched plan text for explicit code path specifications for marker write failure and found the assertion in DD6 rationale but not in abort table or Tasks. Verified lint axis 3 for CL-5 fix checks argument existence but not correctness of bound value. |

</details>

<details>
<summary>round 6 findings 전문 (plan 본문 §L2 Review가 참조 — 보존)</summary>
## Findings (round 6)

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| invariant | **CRITICAL** | G5 verification (Validation §11) is structurally impossible on first run, converting a CRITICAL gate into a soft/optional one | Plan §G5 조건성 (line 35-44): G5 requires 'plugin.json bump → `claude plugin update` → new session boot' but Validation §11 (line 414-427) runs as part of `/mccp:prp-implement` phase which occurs *before* the new session. The validation commands `*.handoff-items.json ≥1` and `A4 status=computed` cannot exist until after deployment + new session. Yet Acceptance (line 459) lists both 'G5 confirmed' and 'G5 unconfirmed' as valid checkbox outcomes in the same list, treating milestone incompleteness as an accepted completion mode rather than a gated failure. |
| security | **HIGH** | Degraded mode entry is fail-closed and prevents STATE.md corruption from incomplete journal | Plan §DD6.1 claims degraded marker is checked immediately after advisory lock acquisition, but state-writer.js:563-567 shows lock failure is advisory with warning-only. If lock fails, the code proceeds outside the lock without checking any marker. The marker write itself (when append fails) could also fail with FS errors, but update() in hooks is fail-open per state/msw-events.js:243-244 pattern — exceptions are caught and warnings logged, not propagated. This creates a window where a failed marker write leaves the journal incomplete without signaling degradation to the next session. |
| security | **HIGH** | G3 STATE.md consumption contract: caller code changes = 0 for projection compatibility | Plan §G3 states 'STATE.md 소비 목적의 코드 변경 0줄' and 'Task 7 lint가 그 분리를 기계 검증한다'. Plan notes that session-start.js:748 and session-end.js:382 are updated for CL-5 (Task 8) but not for STATE.md consumption. However, both files *call* state-writer functions (e.g., state-writer.update via handoffItems), and they are in the '소비처 18곳' list cited in GROUND. The Files to Change table shows both files as UPDATE. If handoff-items module is changed to integrate with the journal (as M5 implies), then these hook files' interaction with state-writer could become state-journal-dependent, violating G3. The lint (Task 7 axis 2) checks only that specific call-site lines don't change, not that the entire call grap |
| test | **HIGH** | Tasks 1-8 contain executable test contracts that will validate G1-G5 guarantees | Lines 272-340 specify detailed assertions (e.g., 'byte-identical output', 'self-credit impossible', 'TTL-independent defense'), but test files don't exist: glob patterns show no `state-journal*.test.js`, `a4-boundary*.test.js`, etc. Validation §1-7 commands (lines 354-382) reference these non-existent tests. The plan assumes these will be implemented exactly as specified but provides no mechanism to validate the specifications before implementation. |
| invariant | **HIGH** | Degraded mode recovery depends on a gate that is not in the enforcement path | DD6.1 (line 224-240) specifies that when journal append fails, a `.degraded` marker is written and subsequent updates bypass the journal. Recovery requires running `journal checkpoint --reseed` which overwrites the corrupted journal with a new genesis based on the corrupted STATE.md. However, nothing guarantees this recovery happens before the next session boots. If a session enters degraded mode and crashes before `journal verify` is run, the next session's genesis bootstrap will inherit the corrupted state. The plan says (line 258) ledger corruption is caught with 'loud warn + tombstone 0 진행' — this is fail-open: corrupted ledger allows silent resumption without full tombstone seeding, vio |
| invariant | **HIGH** | CL-5 path fix (4th re-occurrence) depends on fallible resolution with silent skip fallback | Task 8 (line 321-339) fixes the handoff-items path resolution via `resolveHandoffRoot(ctx)` which: (1) tries `ctx.projectRoot`, (2) falls back to `discoverRepoRoot` walk-up, (3) if both fail: 'loud warn 후 handoff 기록 자체를 skip한다'. But Validation §11 (line 417-420) checks `*.handoff-items.json ≥1`, which will see 0 if resolution failed. The acceptance criterion (line 457) says 'CL-5 수정 3곳 착지' but doesn't gate on resolution success. If `projectRoot` is empty string (acknowledged at line 335: '실측 검증됨') and discover fails, the fix is bypassed silently and handoff artifacts are never created, causing A4 to remain 0 and Validation §11 to fail. |
| invariant | **HIGH** | M2 re-freeze prerequisite is not mechanically gated | PRD Delivery Milestones M2 entry condition (line 154): 'M1의 measurement-feasibility.md가 PROVISIONAL이므로... re-freeze되기 전에는 착수하지 않는다'. The plan assumes M2's design is frozen before M5 starts. But the plan itself does not check this condition — it just references M2 as completed-but-incomplete (line 156: 'complete (인정 조건 미충족)). If M2 never re-freezes (because M1 feasibility remains provisional), M5's foundation is built on unstable schema definitions. The plan has no gate to prevent this drift. |
| architect | **MEDIUM** | `prev_session_id` field assignment not specified in record creation logic | Line 273 lists `prev_session_id` in record schema definition; Line 322 claims Task 8 will use `prev_session_id` boundaries for A4 calculation; no Task (1-10) specifies where or when `prev_session_id` is assigned to new records. Plan defers to implementation without documenting the protocol. |
| architect | **MEDIUM** | Retention eviction algorithm undefined when global limits exceeded | Task 5 (line 301-302) defines checkpoint compression and three constant limits (`세션당 바이트 · 전역 바이트 · 보존 일수`) but Validation §5 (line 303) only checks `상한 초과 시 발화`. Plan does not specify FIFO/LRU/age-based eviction order, which matters because dropped records could break `prev_session_id` boundary references. |
| security | **MEDIUM** | A4 denominator (handoff items) is reliably enumerated and recorded | Plan Task 8 identifies that session-end.js:381 calls `enumerateUnfinishedItems(process.cwd())` without passing stateDir. The fix uses a proposed `resolveHandoffRoot(ctx)` function that falls back to walk-up when projectRoot is empty (observer-sessions.js:99 returns `projectRoot: ''`). However: (1) the function is not implemented in the codebase yet, (2) if walk-up (discoverRepoRoot) fails and returns null, the plan says handoff recording is skipped with 'loud warn', but (3) skipping the write means A4 denominator shrinks silently — the 'loud warn' goes to stderr but the measurement itself becomes corrupted by losing the denominator source. No acceptance test verifies this failure mode doesn' |
| security | **MEDIUM** | Completion-ledger entries reliably seed tombstones in genesis, protecting against replay of closed work units | Plan §DD11 says genesis bootstrap reads git-tracked completion-ledger and seeds tombstones. Validation is described as JSON parse + decision_id existence check. However, the ledger is shared across all worktrees (git-tracked), and entries could be polluted with data from other projects/PRD cycles if they share the same repo root. The validation only checks structure, not whether the decision_id belongs to the current project/context. If corrupted or stale entries exist in the ledger, they become tombstones that block legitimate re-work. The plan mitigates this with corrupt counting, but the corrupt entries that DO parse successfully but have wrong context are admitted without filtering. |
| security | **MEDIUM** | CL-5 path resolution always resolves to correct location when projectRoot is empty | Plan Task 8 proposes `resolveHandoffRoot(ctx)` to handle `projectRoot == ''` case by falling back to walk-up using discoverRepoRoot. But discoverRepoRoot (msw-events.js:192-202) looks for `.claude/` directory and returns null if not found within 40 levels. A hook running from a context where `.claude/` is >40 levels up, or in a directory tree with no `.claude/`, will get null. The plan says 'loud warn 후 skip' but doesn't define what happens to the already-enumerated items. They were enumerated at the wrong cwd, so they're in the wrong place on disk (or not recorded at all), and the next session can't find them. |
| test | **MEDIUM** | Task 7 single-writer lint will catch partial CL-5 fixes, specifically rejecting `process.cwd()` literals in handoff-items calls | Plan specifies (line 315) that axis 3 lint must reject 'process.cwd() 리터럴' but the lint file `single-writer-lint.js` doesn't exist. Validation §10 (line 407) calls this non-existent tool. Without the actual lint code, we cannot verify that the spec's three-condition check (lines 315-316: existence check AND `process.cwd()` != literal) is implementable. If lint implementation is incomplete, CL-5 fixes can remain partial and undetected (tested via fixture `cwd ≠ repoRoot` only, which isn't sufficient if lint is weak). |
| test | **MEDIUM** | G5 (A4 metric transitions to computed) can be validated during implementation | Plan explicitly acknowledges (lines 35-43) that G5 'cannot be verified before ship' and requires `plugin.json` bump → `claude plugin update` → new session run. But M4's identical producer (`*.env-snapshot.json`) is currently 0 in production (GROUND line 140) due to plugin not being deployed. This same deployment gap can recur for M5. Validation §8-§11 attempt to force this via cache check, but that only checks version existence, not actual artifact production. If deployment is skipped, `*.handoff-items.json` will still be 0 and Acceptance §G5 (line 459) correctly forbids `computed` claim, yet the plan cannot catch this until post-deployment. |
| test | **MEDIUM** | Test specification for Task 4 (replay defense) is sufficient to catch all resurrection scenarios | Task 4 (lines 295-298) specifies 5 assertions for crash+resurrection scenario, but test file doesn't exist. The plan describes the scenario abstractly ('세션 A가 작업 잡음 → 크래시 → 세션 B 승계·완료·tombstone → A가 되살아나 지연 레코드 append') but doesn't specify HOW to simulate crashes in the test environment. What if the test environment cannot actually simulate a session crash? What if partial crashes (e.g., hook fires but session doesn't fully terminate) create edge cases the 5 assertions don't cover? These questions cannot be answered without the test code existing. |
| invariant | **MEDIUM** | Acceptance criteria conflate PASS and FAIL paths, making the completion gate ambiguous | Acceptance line 459 lists G5 verification as a single checkbox that accepts two outcomes: '[x] G5(전환 확인) — ... **미확인 시**: ...'. The checkbox treats 'G5 confirmed' and 'G5 unconfirmed + fallback' as alternative valid states of the same criterion. In contrast, other acceptance items (G1-G4) are separate checkboxes for each guarantee. G5 is the only guarantee where incompleteness is pre-accepted as a valid completion path. This violates the principle that a gate should have a single pass condition; bifurcating the acceptance path silences the choice to skip verification. |
| invariant | **MEDIUM** | Journal integrity check (DD6.3) is advisory, not fail-closed on corruption | Task 6 (line 307-309) specifies `journal verify` returns non-zero exit if `content_hash` validation fails or degraded marker is present. But this command is in the tools list, not in the gate path. Validation §2 (line 373) runs `journal verify --json` but the plan does not specify that a non-zero exit from this check blocks the milestone. If journal records are corrupted during M5 implementation and not caught before finalize, the corrupted journal becomes part of the ship receipt. The corrupted records are marked '격리' in the journal but still reside in the durable log (line 248-249), creating a permanent record of data that failed integrity checks but was still admitted. |
| architect | **LOW** | G5 (A4 `computed` status transition) depends on post-deployment execution outside plan's enforced scope | Line 38-40 acknowledges requirement; Line 376-378 and §11 (Line 414-420) add mechanical deployment+artifact checks; but line 39 requires 'new session 1회' to generate handoff artifacts. If that session never runs, A4 stays `forward-only` indefinitely. Plan handles gracefully (§G5 conditional acceptance, line 43-45) but headline claim ('A4를 배송', line 15) remains unfulfilled. |
| test | **LOW** | The plan's baseline for comparing 'new red tests' vs 'pre-existing red tests' is formally specified | Acceptance line 451 requires 'Validation §1~§7 전부 통과 (전체 스위트는 사전 존재 red 목록과 대조해 신규 red 0)'. But no machine-readable 'pre-existing red list' exists in the repository that M5's Validation can reference. The red-test-suite-restore PRD identifies 2 specific failures but doesn't maintain a formal registry. Without such a registry, Validation cannot mechanically determine if a test failure is 'new red' introduced by M5 or 'pre-existing red' that should be ignored. |

## Refutation attempted (round 6)

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | `pass` | Systematically verified architectural claims: (1) G3 consumption contract separation via Task 7 lint—confirmed sound; (2) `prev_session_id` boundary mechanism—found specification gap on assignment protocol; (3) retention policy—found algorithm unspecified; (4) CL-5 handoff-items fix scope and separation from STATE.md sites—confirmed correct; (5) two-parser equivalence and divergence pinning—confirmed sound; (6) G5 conditional acceptance and deployment verification—confirmed mechanically enforced but upstream execution-dependent. Examined coupling between M5 journal layer and M8 metric producers; structure properly decomposes but has documented dependencies. No logical defects found; three gaps are design underspecifications rather than structural failures. Plan's core abstraction (journal→ |
| security | `fail` | Attacked: (1) Degraded mode race condition and fail-loud guarantees — verified advisory lock implementation and found lock failure + marker write failure create windows where journal incompleteness isn't communicated to next session. (2) A4 denominator reliability — traced handoff-items calls in session-end.js:381-382 and session-start.js:748, confirmed they don't pass stateDir, verified walk-up function exists but can return null, found no acceptance test for skip scenario. (3) Completion-ledger cross-contamination — checked observer-sessions.js:99 behavior (empty projectRoot) and completion-ledger/store.js structure (shared across worktrees), identified no project-context filtering in genesis bootstrap. (4) G3 invariant scope creep — read state-writer.js, handoff-items.js, and plan Files |
| test | `fail` | Attacked the plan's test coverage strategy by searching for: (1) test files specified in Tasks 1-8 — found none exist; (2) single-writer-lint.js (Task 7) — does not exist; (3) references to pre-existing CL-5 bugs in hooks — verified they exist (session-end.js:381-382, session-start.js:748) and require the lint to catch partial fixes; (4) G5 assumptions about deployment — confirmed M4's parallel producer (*.env-snapshot.json) is still 0 in production showing same risk recurrence; (5) replay defense test feasibility — examined what a crash simulation would require in test environment; (6) pre-existing red baseline registry — found red-test-suite-restore documents some failures but no formal machine-readable baseline exists for Validation §1-7 to reference. Could not find: executable test cod |
| invariant | `fail` | Attacked the following invariant-erosion paths: (1) fail-open drift in G5 gate — traced the execution order of plugin deployment and Validation §11 and found the mechanical check cannot run on first attempt, confirming the gate is structurally soft; (2) degraded mode recovery path — read DD6.1 table and trace to next-session bootstrap, confirmed recovery is not in the hot path and depends on manual `journal verify` that may not run; (3) CL-5 re-fix path — confirmed it's the 4th occurrence of same bug, examined the fallible `resolveHandoffRoot`, verified silent skip is the default failure mode; (4) M2 prerequisite — checked PRD entry condition and confirmed plan has no gate enforcing re-freeze; (5) acceptance criteria structure — compared G5 against G1-G4 and identified the unique bifurcati |

</details>
