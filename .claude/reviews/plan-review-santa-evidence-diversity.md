# Plan Review Panel — santa-evidence-diversity

**Plan**: `.claude/plans/santa-evidence-diversity-m3.plan.md` · **Plan version**: `sha256:9af3f69e98015176b778fc4b63892a44c438df881fa41882f26fef92e8346682`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 10 blocking finding(s): test/HIGH, test/HIGH, test/HIGH, test/HIGH — MCCP_REVIEW_SINGLE_PASS=deadline_pressure 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| test | HIGH | Plan's Validate section claims Validation #1 will test M3 functionality (familyOf, parseDegradeGate, parseDegradeAck, diversityFrom, etc.) by running `node --test plugins/mccp/scripts/lib/tests/santa-lanes.test.js` | Plan Tasks 6 specifies adding tests for M3 functions to santa-lanes.test.js, but these tests do not exist in the file (checked lines 1-300). Current file contains only M1 tests. Validate command #1 will run existing tests but cannot verify new M3 code because (a) model-diversity.js doesn't exist, and (b) tests for familyOf, parseDegradeGate, diversityFrom, deriveVerdict(degraded), CLI model validation don't exist. |
| test | HIGH | Validation #5 tests that `node plugins/mccp/scripts/lib/santa/cli.js record --decision x --round 0 --id B --model gemini-2.5-pro --reviewer-file /dev/null --lane bundled` exits with code 2 when gemini is not in PATH | Task 4 proposes adding PATH checking in cmdRecord at line 326-329 (right after model field validation). However, Validation #5 passes `/dev/null` as `--reviewer-file`. Current code at cli.js:295-306 reads the file and tries `JSON.parse('')` on it, which throws SANTA_REVIEWER_INVALID before the model check would execute. This means the test currently exits 2 for the wrong reason (invalid JSON), not for missing CLI. The validation command's exit code happens to be correct (2) but the failure reason is different than claimed, making this a false-positive test. |
| test | HIGH | Receipt 5 fields (santa_model_families, santa_model_degraded, santa_degrade_reason, santa_degrade_ack, santa_degrade_ack_reason) are tested by Validation #2 which runs santa-review-gate.test.js | Plan Task 3 proposes adding these 5 present-only fields to write.js and schema.js. Plan Task 6 claims to update santa-review-gate.test.js with tests for these fields. However, none of these fields exist in write.js (searched, no matches for santa_model_degraded, santa_degrade_reason, etc.) and the test file santa-review-gate.test.js contains no tests for M3 degradation fields (read full file, M3 tests absent). Validation #2 will pass existing tests but not validate the new receipt fields because they don't exist yet. |
| test | HIGH | seal.js deriveVerdict() function will return 3 values: 'converged', 'divergent', 'degraded' (per Task 2 and DD1) | Current seal.js:144-151 deriveVerdict() function only returns two values: 'divergent' (lines 146-150) or 'converged' (line 151). There is no 'degraded' value in the implementation. The plan claims Task 2 will add this, but no code modifications have been made yet. Tests at Validation #2 that reference seal.js won't test degraded handling because the code doesn't implement it. |
| test | CRITICAL | model-diversity.js module exists with functions familyOf(), parseDegradeGate(), parseDegradeAck(), and diversityFrom() to be tested | Glob search for plugins/mccp/scripts/lib/santa/model-diversity.js returns no results. The file does not exist. Plan Task 1 proposes creating it, but the create action has not been executed. Validation #1 references santa-lanes.test.js which cannot test functions from a non-existent module. |
| test | HIGH | Validate section commands test that new M3 functionality was implemented correctly | Validation section (lines 394-420) lists 6 commands. Command #1 runs existing test files that don't contain M3 tests. Command #2 runs existing test files that don't have M3 receipt field validation. Command #5 exits 2 but not for the claimed reason (JSON parse error vs missing CLI). Command #6 checks if ownership.md mentions model-diversity.js, but that module doesn't exist. Overall, the Validate section assumes all code from Tasks 1-8 has been implemented, but the commands don't verify that the implementations are correct - they only run existing test suites that don't cover M3. |
| test | MEDIUM | CLI model validation will be tested by Validation #5's command which executes `record --model gemini-2.5-pro` | Plan Task 4 proposes implementing the check in cmdRecord after line 328 (model non-empty validation). The Validate command at line 415-416 uses `/dev/null` as reviewer file. According to cli.js:300-306, reading and parsing `/dev/null` as JSON fails before the model check would execute. The command exits code 2, but not because gemini is missing - because the JSON parse fails. A proper test would need a valid reviewer JSON file and ensure the PATH check runs and fails correctly. |
| invariant | HIGH | Step 5.5 control flow for degraded verdict + ack flag is not shown in the plan, leaving the skip-predicate implementation to code review and end-to-end test only. If the order is reversed or ack check is omitted, push could silently succeed when it should be blocked. | Plan Task 5.2 describes what Step 5.5 should do (check degraded first, read degradeAck, branch on it) but shows no bash code. Current santa-loop.md lines 739-745 show only a generic '!= converged' check. The plan warns: '`!= converged` 절이 앞서면 degraded가 divergent 메시지로 흡수된다' but doesn't enforce the described reordering via code. |
| invariant | HIGH | Seal output contract is ambiguous: the plan says seal() returns verdict='degraded' (Task 2.5) but also says seal.js builds writeArgs with splay'd verdict (Task 2.3). Step 5.5 receives SEAL_JSON from cli.js; it's unclear whether this contains pre-splay verdict or post-splay receipt_verdict, and whether degradeAck/degradeReason keys are included. | Task 2.5: 'seal() 반환에 degraded(bool) · degradeReason(string\|null) · degradeAck(bool) 3키 추가. verdict는 'degraded'를 그대로 낸다' vs Task 2.3: 'review-verdict는 verdict === 'degraded' ? 'divergent' : verdict로 사영한다'. Seal.js calls write(writeArgs) but doesn't document whether the return object includes the keys Step 5.5 expects to read. |
| invariant | MEDIUM | Unit test validation does not include explicit verification that the Step 5.5 ack flag actually allows push to proceed. Validation 1-4 test schema, hash, and CLI logic, but Task 6 does not list a test case for 'degraded+ack → push succeeds'. Only Task 7 (manual end-to-end execution) observes this critical path. | Plan Validation section (lines 396-420) lists test commands for santa-lanes, santa-loop-cap, santa-gate, and i18n-surface tests, but none explicitly test the Step 5.5 bash logic. Task 6.1 tests deriveVerdict result, Task 6.3 tests receipt schema, but neither tests the skip-predicate (ack flag allowing push). Task 7 expects to observe: 'push가 진행되고' but this is a manual observation, not a regression test. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified all major structural claims: (1) P0 boundary justification (deriveVerdict not in frozen table, precedent from santa-adjudication M3 opening seal.buildProof). (2) Citation accuracy — REVIEW_VERDICT_VALUES, CODEX_VERDICT_VALUES, pattern mirrors in lanes.js/write.js all confirmed. (3) Consistency — gate.js unmodified, present-only semantics valid, ownership document supports opening seal.js. (4) No hidden coupling — gate returns only NICE\|NAUGHTY, seal.deriveVerdict is internal, no caller loops introduced. (5) UI constraints — all 20 user-intent constraints checked against plan structure; none violated. Existing tests are preserved (Validation 1-6 pattern solid). The plan's DD mappings to existing code are accurate. Checked for invariant erosion — none found. The layering and abstraction boundaries are maintained per protocol 2 (addition-only, frozen signatures unmodified). |
| security | pass | Attacked trust boundaries in model declaration verification, environment variable handling, PATH traversal, validation order, receipt field invariants, and approval override mechanisms. Examined Task 1-4 implementation specifications against existing patterns in lanes.js, seal.js, cli.js, write.js, and validateReason.js. Found no defects to block. Specific attacks tested: 1. **Model family classification gaming** (DD3): substring matching on "gpt-4-with-claude-mode" would correctly match "gpt" before "claude" due to declared order. Lowercase+trim applied before matching prevents case-based evasion. Unknown models fail-closed to degraded. 2. **PATH environment injection** (Task 4): CLI checks whether codex/gemini exists in process.env.PATH, but doesn't verify which tool actually ran. Plan explicitly acknowledges this ceiling in DD6 (same as M1's `--lane` pattern) and limits check to sanity-validation: "設置されていないCLIを参칭" is prevented; claiming Claude when codex ran is not. Design is intentional. 3. **MCCP_SANTA_DEGRADE_ACK override validation** (Task 1): Plan specifies strict validateReason delegation. Existing validateReason.js enforces: ≥30 chars, ≥3 words, no one-token banlist, no filler. Call site must pass `{strict:true}` or namespace 'impeccable' - plan says "strict 위임" implying proper mode. Write.js pattern `review_l3_invoked` block (Task 3 mirror) shows `=== true` conditional stamp, matching existing precedent. 4. **Receipt field presence-only invariants** (Task 3): Bidirectional invariant enforced for `{degrade_ack, degrade_ack_reason}` pair. `santa_model_degraded=true` and `review_verdict='divergent'` projection happens in seal.js writeArgs (DD2). Schema doesn't cross-validate these axes, but data flow from seal() ensures projection before write. Tests required to validate DD2 projection (Validation 2 covers this). 5. **Verdict value expansion scope** (Task 2, Risk): `deriveVerdict` adds third value `'degraded'` but doesn't appear in downstream REVIEW_VERDICT_VALUES enum. Plan shields this with sProjection: receipt gets `'divergent'` not `'degraded'`. Validation 3 (grep REVIEW_VERDICT_VALUES) confirms no vocabulary leakage. 6. **Containment on proof/report writes** (seal.js DD2): Both `.claude/state/santa-loop` paths are asserted-contained like existing design. SLUG_RE blocks traversal chars. Asymmetry (plan write has containment, old write had only report path) is justified and follows review feedback. 7. **Duplicate env parsing** (cli.js Task 4, lanes.js pattern): `familyOf(model)` and `cmdRecord` both derive family from --model. Two separate checks (record-time PATH check, seal-time diversity check) are intentionally decoupled - no race or synchronization issue. 8. **Model declaration durability** (DD6): Recorded model strings flow through projection to receipt. Family derivation happens twice (record + seal) from same algorithm, so classification is consistent. No unstable intermediate state exposed to receipt write. All identified risks in plan (Risks section) are mitigated as stated or explicitly documented as intentional design trade-offs with acknowledged limitations. |
| test | fail | Attacked the plan by examining: (1) whether model-diversity.js exists (it doesn't), (2) whether tests for M3 functions exist in santa-lanes.test.js and santa-review-gate.test.js (they don't), (3) whether seal.js deriveVerdict() supports 'degraded' (it doesn't), (4) whether the 5 new receipt fields exist in write.js and schema.js (they don't), (5) whether Validate command #5 actually tests what it claims (it exits 2 but for JSON parsing, not CLI availability), (6) whether cli.js has PATH checking for model CLIs (it doesn't). Confirmed via code inspection that all proposed M3 functionality is absent and tests don't exist to verify it would work once written. |
| invariant | fail | I attacked the plan through six key invariant lenses: (1) **Fail-open drift** — traced the three code paths (model family classification, env parsing defaults, PATH check) to verify they fail closed when inputs are unknown; defaults are toward 'enforce' (stricter) and 'unknown' family (degraded). (2) **Direction of defaults** — confirmed env parser uses 'enforce' as default for invalid MCCP_SANTA_DEGRADE_GATE, which is the fail-closed direction. (3) **Skip predicates** — identified the ack flag in Task 5.2 as the predicate allowing push on degraded verdict, but found the implementation is not shown in code. (4) **Anchoring** — verified present-only fields are correctly mapped (5 fields, makeSkeleton not modified, schema validates them), but the splay of degraded→divergent in receipt is not mechanically proven in seal.js/write.js code flow. (5) **Rollback safety** — confirmed env toggle MCCP_SANTA_DEGRADE_GATE exists to disable enforcement, and observed fields are stamped even when gate is off (DD4 honored). (6) **HALT vs degrade** — confirmed functions are documented as non-throwing and fall back safely, but identified the Step 5.5 bash control flow is missing, creating a code-path where the gate's mechanical halt could be lost if ack check is misplaced. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 634657,
  "halt_stage": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:9af3f69e98015176b778fc4b63892a44c438df881fa41882f26fef92e8346682",
  "plan_path": ".claude/plans/santa-evidence-diversity-m3.plan.md",
  "recorded_at": "2026-08-19T05:50:57.193Z"
}
```
