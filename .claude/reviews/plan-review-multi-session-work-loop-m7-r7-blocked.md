# Plan Review Panel — multi-session-work-loop

**Plan**: `.claude/plans/multi-session-work-loop-m7.plan.md` · **Plan version**: `sha256:1d2e9caeb4fd2e8e95d2d8f17e09747d030e068d2dce71662372b27336b02232`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 8 blocking finding(s): test/HIGH, test/HIGH, test/HIGH, test/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| test | MEDIUM | Task 2 adds `type_separation: true` to the existing test fixture at msw-metrics.test.js:416, and the Validate command will catch regression. | Plan line 211 states fixture needs `type_separation: true` added, but the actual fixture (msw-metrics.test.js:416-427) does not declare this field. The Validate command at line 213 just runs `node --test`, which will fail if the field is missing, but doesn't explicitly verify the fixture was updated. The task Action (lines 210-211) mentions adding the field but doesn't specify where/how to add it to the fixture code. |
| test | HIGH | Test file c1-feedback-loop.test.js will pass validation with specific assertions (C1-EMIT-PLAN-REVIEW, C1-EMIT-PLAN-CODEX, C1-EMIT-SANTA, C1-EMIT-FAILOPEN, C1-EMIT-LOSS-VISIBLE) when created. | Plan line 226 lists required assertions by ID but doesn't provide the test code. The Validate command at line 213 only runs `node --test plugins/mccp/scripts/lib/tests/c1-feedback-loop.test.js`, which will pass if ANY tests pass, not necessarily all required assertions. There is no grep or assertion-count verification to ensure all required assertion IDs are present in the file. |
| test | MEDIUM | .gitattributes will have `merge=union` declared for findings registry paths after Task 1 completes. | Current .gitattributes file (lines 1-16) contains only EOL rules, no merge directives. Plan line 204 states Task 1 must add `.claude/state/findings/*.jsonl merge=union` to .gitattributes, but Task 1's Action section (line 204-206) doesn't specify the exact text to add to .gitattributes. The Validate at lines 280-282 tests this via `git check-attr` but has no pre-commit verification that the line was added. |
| test | HIGH | SOURCE_SCANNERS['findings'] will be registered in derive/index.js after Task 3 completes, and Task 2's test will fail if Task 3 is not applied. | Plan line 211 claims Task 2's test `c1-feedback-loop.test.js` will assert `SOURCE_SCANNERS` has 'findings' key (`C1-SOURCE-REGISTERED-COPRESENT`), enforcing atomic Task 2+3 commit. However, the test file doesn't exist yet (CREATE in Task 2), and the plan provides only the assertion name, not the test code. The co-presence check is also claimed for Task 7's gate (line 148), creating dual verification points that could diverge if Tasks 2-3 split or gate logic changes. |
| test | HIGH | The Acceptance criterion at line 331 requires `.claude/state/findings/multi-session-work-loop.jsonl` to exist and be git-tracked after `/mccp:plan` completes. | Line 331 states the file should be created by this plan's own `/mccp:plan` run, but none of the Validate commands (lines 255-296) actually execute `/mccp:plan` to verify this. Lines 276-277 test file existence and tracking, but they run AFTER implementation. The plan provides no Validate command that invokes the actual command path to verify findings are emitted. This is deferred to manual Acceptance criterion (line 328), creating a test-to-code gap: unit tests pass (building blocks work) but end-to-end path is untested. |
| invariant | CRITICAL | Task 2·3 synchronization enforced only by pre-commit test, not by commit-time or gate mechanism—violates fail-closed requirement | Plan §Risks 'Task 2·3 분할 착지를 아무도 관측하지 못함': acknowledges gate runs 'Task 7 이후' (after 6 intervening tasks). Mitigation 'Task 2 자신의 test 파일이 SOURCE_SCANNERS의 findings 키를 직접 단언' is procedural, not mechanical. Plan shows no code for this test file, only describes what it should do (line 212). If Task 2 lands with incomplete/missing assertion or test is bypassed, C1 becomes invalid (type_separation_undeclared), violating acceptance criterion 'C1이 computed로 뒤집힌다' (line 329). No code-level barrier prevents Task 2-only commit to main. |
| invariant | HIGH | Coverage gate post-commit validation cannot prevent state where Task 2 consumer exists but Task 3 producer absent during multi-task window | Plan DD10 states coverage gate 'Task 7이 만들어 그 이후 트리만 본다' (line 148). Tasks 2-6 land between Task 2 commit and Task 7 gate execution. If Task 3 fails to land or is incomplete during Tasks 4-6 execution, C1 is silently invalid (type_separation_undeclared) for multiple task cycles. Acceptance section requires explicit check 'c1-coverage-gate.js --acceptance 영점 exit' (line 335) but this runs at Task 7, not atomically with Task 2. |
| invariant | HIGH | Receipt anchoring to work_unit (decision slug) vulnerable to PRD path changes without detecting registry orphaning | Plan DD4 'work_unit(= decision slug)' bases anchoring on PRD path derivation (line 89). Registry files named `.claude/state/findings/<work_unit>.jsonl`. If PRD is moved (e.g., archived) or path derivation changes, new sessions generate different decision slugs and don't see old findings. Registry is git-tracked but git history doesn't prevent silent orphaning. Plan mentions 'git-tracked 감사 corpus' (line 95) but provides no validation that all work_units from past sessions remain discoverable by future runs of derive. |
| invariant | MEDIUM | Absolute path normalization in findings registry has no runtime enforcement—relies solely on code review of 3 emit points | Plan describes '단일 초크 포인트' in appendFinding (line 95, Task 1 mirror section) but all 3 callers (plan-review cli.js, plan-codex-runner.js, seal.js) must normalize before calling. Coverage gate's static lint (Task 7) checks 'authorized writers' but cannot verify *correct* normalization. Future emit point added by different author could normalize incorrectly or call appendFinding with pre-normalized path, double-escaping it. No runtime type guard or test validates normalization output format. |
| invariant | MEDIUM | .gitattributes merge=union declaration required for safety but Task 1 manually creates entry—late validation (Task 7) allows multi-task window with broken merge semantics | Plan Task 1 validation: '.gitattributes에 .claude/state/findings/*.jsonl merge=union을 넣는 것도 이 Task의 작업이다' (line 204). Current .gitattributes (read at file path) contains only EOL rules, no merge directive. Acceptance explicitly checks 'git check-attr merge' (line 336) but only at Task 7 validation. If Task 1 forgets entry, parallel merges will silently use default (probably 'resolve' or 'binary') and drop one session's appends. No blocking prevention until Task 7. |
| invariant | LOW | Seq hole detection for emit failure visibility cannot catch tail loss—batch of finding_closed at session end with no subsequent emit remains undetected | Plan DD8 acknowledges 'menu 조각이 유실돼도... 그 구간의 C1은 실제보다 낮게 보고된다... 잔여를 닫는다고 주장하지 않으며' (line 136). Fallback detection via runtime falsifier (finding surface delta) is primary but only if `finding_opened` events exist in parallel. If last batch of `finding_closed` events is entirely lost and no later emit from that work_unit runs, loss is structurally invisible—only `seq` holes from *later* writes expose *earlier* losses. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified core structural claims: (1) C1 producer gap correctly identified (SOURCE_SCANNERS lacks findings entry per plugins/mccp/scripts/derive/index.js, findings-file flag unused per grep of command bodies); (2) DD5 typeIntegrity defect correctly cited (current code line 632-633 requires deferred+downgraded+rejected > 0, which is backwards); (3) Registry design sound (single choke point for path normalization via appendFinding, mechanical gate checks both .claude/state/findings/ and .claude/reviews/ surfaces); (4) Boundary preservation (record.js has 0 fs imports, emit at cli.js I/O boundary per line 663); (5) Task 2-3 atomicity mechanically enforced (test c1-feedback-loop.test.js asserts SOURCE_SCANNERS.findings exists); (6) Batch atomicity by API constraint (sequential append prohibited) + seq-based loss detection in reader; (7) Deduplication strategy sound (finding_id primary matching, matchKey secondary with conservative bounds to not flip C1 direction); (8) .gitattributes merge=union coverage correct (Task 1 registers glob pattern, Task 7 gate-checks it via git check-attr on two test filenames). No citation mismatches to existing code, no impossible dependencies, no leaks between abstraction layers detected. |
| security | pass | Attack surface: (1) Path traversal via work_unit parameter — validated by git-tracked path normalization + chokepoint enforcement. (2) Direct registry path writes bypassing `appendFinding()` — mitigated by Task 7 coverage gate checking registry path (`C1-COVERAGE-REGISTRY-WRITER`) + static lint + runtime audit. (3) `.gitattributes` merge attribute misconfiguration — Task 1 Validate includes `C1-MERGE-UNION` test using `git check-attr` on both real and future names; test fails if attribute missing or wrong. Trust boundaries: (1) Seq initialization and concurrent writes — reader detects duplicate seq as degraded, sorted by seq before gap analysis, batch atomicity prevents partial writes. (2) Secondary matching key inflation — restricted to `cited_path`-present + single-candidate cases per DD9, validated by `C1-ID-SECONDARY-KEY`. (3) Path normalization enforcement — single chokepoint in `appendFinding()` per DD4, all 3 emit points must use it (Task 4); Task 7 coverage gate rejects unregistered writers to both surfaces. Data leakage: (1) Absolute paths in git-tracked registry — normalized to repo-relative or `<outside-repo>` placeholder per `normalizeReceiptCwd` pattern; stored in allowlist field `cited_path`. (2) Finding claim text — not stored in registry, only `claim_digest` (16-char sha256 prefix); original claim in PR body/review surface. (3) Degraded markers — logged to `.claude/state/findings/<work_unit>.degraded` for diagnosis; audit trail preserved in seq numbers. Escalation/tampering: (1) Direct fs.write bypassing API — Task 7 coverage gate static lint (axes A/B/C) + runtime filesystem audit before/after diffs. (2) Concurrent writes corrupting seq — batch atomicity + merge=union + sorted reader. (3) Malformed line injection — per-line skip per msw-events pattern; reader detects seq gaps. Validation layer adequacy: Task 1 Validate 10+ assertions including `C1-MERGE-UNION` gate; Task 7 Validate requires both `C1-COVERAGE-STATIC` and `C1-COVERAGE-REGISTRY-WRITER` tests. Plan-level co-presence: Task 2-3 pre-commit test `C1-SOURCE-REGISTERED-COPRESENT` ensures compute contract land together; Task 1 `.gitattributes` test failure gates whole task. No defects found in attack surface, trust boundary design, or data handling specification. |
| test | fail | Read plan (lines 1-382) and PRD (lines 1-239); checked existence of test files referenced in Validate sections (findings-registry.test.js, c1-feedback-loop.test.js, c1-coverage-gate.test.js) — none exist yet; verified SOURCE_SCANNERS in derive/index.js (lines 27-51) — 'findings' key absent; confirmed .gitattributes (lines 1-16) has no merge=union rule; read msw-metrics.test.js fixture at line 416-427 — no type_separation field present; traced Validate command chains for Tasks 1-8 to identify which claim no actual verification, which rely on files not yet created, and which reference test assertion IDs without providing test code or verification that assertions are present in created files.</refutationAttempted> |
| invariant | fail | Attacked: (1) Task 2·3 commit atomicity—traced synchronization requirement through DD2, DD10, Risks section; verified no pre-commit hook or CI gate exists to prevent Task 2-only landing; confirmed test file is described not shown and procedural not mechanical. (2) Receipt anchoring—examined work_unit derivation, path normalization requirements, git-tracked corpus design; checked .gitattributes for merge directive (absent). (3) Fail-closed gates—read coverage gate description (Task 7), confirmed it runs post-commit after 6 intervening tasks; traced intermediate states where invariants can be broken. (4) Seq hole detection for emit failure—read DD8 loss scenarios and degradation marker mechanism; confirmed undetected tail case. (5) Rollback safety—examined append-only contract and normalized path handling; found no recovery mechanism if paths are incorrectly normalized or merge directive is absent." |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 716586,
  "halt_stage": "5.2e",
  "granted": 4,
  "reviewed_plan_hash": "sha256:1d2e9caeb4fd2e8e95d2d8f17e09747d030e068d2dce71662372b27336b02232",
  "plan_path": ".claude/plans/multi-session-work-loop-m7.plan.md",
  "recorded_at": "2026-08-17T15:22:29.785Z"
}
```
