# Plan Review Panel — diverse-agent-review

**Plan**: `.claude/plans/diverse-agent-review-m11.plan.md` · **Plan version**: `sha256:2c5bb64915b73de4ffd2c00f0e10874e3d44ea1e312ac87d37f6238100728025`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 13 blocking finding(s): security/HIGH, security/FAIL, test/CRITICAL, test/CRITICAL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| security | HIGH | Plan identifies HIGH security requirement (receipt integrity validation) but does not explicitly implement or defer it | Fan-out findings line 96 states 'M11 must mirror this before using reviewed_plan_hash as audit evidence' and explicitly cites evidence-audit.js receiptIntegrityOk() as the pattern. However, Task 1 item 2b (proof_backing axis) only describes hash comparison without requiring receipt integrity validation. Task 5 backlog items explicitly defer 'isReviewProofStructurallyValid' validation (line 474-475) but do NOT list receiptIntegrityOk validation, creating a gap between the identified requirement and implementation. |
| security | MEDIUM | Plan relies on loose string matching for backlog findings correlation, creating false-positive risk | Task 1 item 4 describes backlog channel parsing: '느슨 매칭 + 매칭한 부분 문자열을 근거로 동봉하고, 그 부정확성을 산출 문서가 그대로 적는다'. This substring matching could associate unrelated findings with approval audits. For example, a backlog line containing 'plan execution issue' in a generic sense might match when searching for a specific plan. The plan acknowledges this imprecision but does not mitigate it with validation rules or false-positive detection. |
| security | MEDIUM | Date.parse() is lenient and may accept non-ISO timestamps as evidence in G3 temporal trust boundary | Design Notes G3 (line 188) specifies 'G3의 "승인 시각"은 `measurement.recorded_at` 단 하나다'. Task 1 item 2c uses 'ISO.parse' (actually Date.parse() in JavaScript) to parse this timestamp. Date.parse() accepts many formats beyond ISO 8601 (e.g., 'Mon Aug 26 2026', ambiguous date strings). This could allow timestamps in unexpected formats to pass validation while violating the ISO strictness that G3 requires. The plan does not specify strict ISO validation or rejection of ambiguous formats. |
| test | CRITICAL | M11's acceptance criterion ('approval was correct') can be validated by tests | Plan Multi-Perspective Fan-out line 92: 'M11's acceptance criterion lacks a mechanical definition; audit tool cannot validate false-approve without ground truth... No unit test can be pre-written because the criterion is unstated.' Task 2 line 418 promises 'no judgment fields' test but cannot test whether the judgment process itself is correct. |
| test | CRITICAL | The three-gate definition (G1/G2/G3) is fully testable | Plan DN1 lines 182-186 defines G2 as 'defect in reviewer lens': '결함이 그 실행에서 실제로 발화한 관점의 렌즈 안에 있다'. Task 2 item 3 tests 'lenses' are parsed from Refutation table, but no test validates whether a defect was actually WITHIN the scope of those reviewers. A test can verify reviewers exist, not whether they looked at that axis. |
| test | HIGH | The time boundary (recorded_at) is robustly validated | Plan DN1 lines 188-195 state recorded_at is 'THE single' time marker, and implementation must reject absence/parse failure. Task 2 item 12 tests '부재·파싱 불가' but not malformed-yet-valid timestamps (e.g. wrong timezone format, off-by-one year). Date.parse is permissive; the test won't catch if a timestamp parses as wrong date. |
| test | HIGH | Archive path round-trip validation is tested before implementation | Plan Task 2 item 4 requires testing archived paths: 'recorded path is `.claude/plans/...` but file is `.claude/PRPs/plans/archived/...` fixture where anchor recovers.' Plan DN6 lines 235-238 admits this is a gap discovered post-M8. Existing hash.test.js (line 299-315) tests planAwareMarkdownHash but does NOT test the archived-path scenario. The test to catch this doesn't exist until Task 2 item 4 is written. |
| test | HIGH | The 'independent producer' definition (G3) ensures non-self-audit | Plan DN3 lines 197-200 state G3 evidence must be from '다른 생산자·다른 시점'. But Task 2 does not specify how to validate producer independence. The tool reads from 4 channels (report, backlog, downstream_reviews, pr_codex) — there is no test that verifies these are produced by independent actors or at independent times, or that the same actor couldn't have written both approval and evidence. |
| test | MEDIUM | The 'structurally_empty' classification is enforced at runtime | Plan DN3 lines 211-213 state the rule: 'meta.codex_disabled=true' and 'resolution.codex_verdict=skipped' → structurally_empty. Task 2 items 2-3 test fixtures. But if a future schema change drops meta.codex_disabled or if the validator logic drifts, the test won't catch the silent reversion. The rule is checked against fixture data, not against contract invariants. |
| test | MEDIUM | No judgment fields are present in the tool output | Plan DN2 lines 202-206 and Task 2 item 8 require 'no false_approve classification fields'. The test can assert field names don't appear, but cannot catch if judgment logic is embedded in field selection, ordering, or conditionals that don't use explicit judgment field names. |
| test | MEDIUM | Path validation (isRepoRelativeEvidencePath) is applied before filesystem access | Plan DN13 lines 352-354 state validator runs '먼저', then 'reject → filesystem에 닿지 않는다'. Task 2 item 9 requires 'fs calls = 0 for rejected paths'. But the test stub method is filesystem-agnostic — if the implementation code is reordered (reads first, validates after), the test will still pass because it stubs fs after the fact. |
| invariant | CRITICAL | Plan acceptance criteria accept inconclusive or null audit results as complete milestones, enabling fail-open pass without proving the audit answered its core question | Acceptance (line 568-590) requires: coverage.approved >= 5, state != blind, each-miss-judgment-cites-evidence. BUT Acceptance never requires the audit to FIND or NOT FIND false-approves. If audit runs and reports zero defects (structurally empty channels + no post-approval evidence), all criteria pass. PRD asks: 'did panel miss defects?' (yes/no judgment required) but plan accepts 'here is the data' (advisory report) as equivalent. Acceptance line 575-576 says 'each miss judgment cites evidence' — vacuously true if zero misses found, making this criterion permissive about inconclusive results |
| invariant | HIGH | Plan's own receipt (ship receipt from /mccp:plan run) does not anchor to audit findings, breaking traceability of whether milestone delivered what PRD required | Plan Acceptance (line 574) requires dossier committed to git at `docs/diverse-agent-review/approval-quality-audit.md` with audit output embedded. But the receipt produced by this /mccp:plan execution will be a standard ship receipt (`.claude/receipts/mccp-pr-codex/<slug>.json`) which contains NO FIELD recording the audit's state, findings, or corroboration results. Later audit verification requires reading external document, not receipt. Per §3.12 receipt-anchoring mandate, if a decision's proof depends on external artifact, that artifact must be recorded IN the receipt or the receipt is unanchored. The audit findings live outside the receipt's scope. |
| invariant | HIGH | Plan lacks remediation path for false-approve findings, but does not clearly state this limitation in acceptance or PRD outcome, creating asymmetry between 'judge correctness' (PRD question) and 'report only' (plan scope) | PRD line 108 Outcome: 'judge whether approvals were correct'. Plan DN2 line 202: tool produces 'dossiers' not 'verdicts', judgment is separate. Plan DN11 line 268: 'gate를 손대지 않는다' — no gate changes. But Acceptance never clarifies: if audit finds false-approves, what happens? No mitigations are proposed (re-run panel, block approvals, adjust thresholds). The asymmetry: Acceptance criterion (coverage>=5, state!=blind) does not distinguish between 'audit clean' and 'audit found problems' — both exit 0. This enables silent pass with findings that remain unacted. |
| invariant | HIGH | Acceptance criterion 'state != blind' is permissive about data completeness but not about data validity; tool state=degraded (exit 1 soft warning) still passes as acceptable, silencing corroboration mismatches | DN15 line 385-389: degraded state (exit 1) is triggered by 'parse failures >= 1 OR proof_backing not corroborated >= 1'. If ship receipt plan_hash != review record reviewed_plan_hash (meaning approved plan ≠ shipped plan), state becomes degraded, exit 1. Plan Acceptance does not require state=ok (exit 0); it only forbids state=blind (exit 2). So an audit reporting corroboration mismatches (uncorroborated record hashes, indicating reviewed plan differed from shipped plan) passes as acceptable. Per §3.12 no-rehash invariant, if receipts are tampered or versions drift, this corruption should fail-closed, not degrade to warning. |
| invariant | MEDIUM | Plan depends on corpus.js filtering (verdict=='converged') without validating that prior /mccp:plan runs actually converged correctly, creating transitive trust in unvalidated upstream gate | Task 1 line 327-342 corrects initial error: tool uses corpus.js output directly (pass_path.entries) without re-verifying converged verdict. Plan assumes records written by prior /mccp:plan runs are truthful. But plan provides no acceptance criterion validating convergence proof structure or re-checking quorum (DN9 explicitly states review_proof cannot be validated because plan-gate receipts are worktree-only). If prior runs silently mis-stamped verdict as converged when they should have blocked, this audit accepts that false verdict as input. No per-record quorum revalidation in acceptance criteria. |
| invariant | MEDIUM | Plan's design mirror of evidence-audit.js uses same state ladder (blind/degraded/ok) but applies it to a non-binding advisory tool, confusing fail-closed semantics from a binding audit context | evidence-audit.js is a binding gate audit (E1-E4 defects directly block; exit codes 0-4 determine whether approval is valid). Plan's approval-audit.js mirrors the same states but explicitly states (DN2) it produces non-binding dossiers; humans judge. Acceptance accepts state=ok OR state=degraded equally (both pass). This repurposes fail-closed state machinery in a permissive advisory context, breaking the invariant that state-ladder exit codes mean something about blocking. The same state=degraded that would block a receipt validation is silently acceptable as 'audit completed'. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Verified plan's core claims: (1) corpus.js exports + pass_path.entries contract — confirmed all 9 exported items match (line 868-878), `entries` field exists (line 497), no new exports required; (2) hash regime selection — confirmed isPlanPath logic in hash.js:169-175 correctly routes to structural vs. regular hash based on path; (3) evidence-audit.js pattern mirroring — confirmed corpus.js implements state precedence ladder (blind/degraded/ok) and structural-vs-absense distinction (lines 27-42); (4) boundary definitions — checked G1/G2/G3 gates (DN1 lines 183-186) are operationally definable; tool outputs `candidates` with sources for document author to judge; (5) path validation — confirmed `isRepoRelativeEvidencePath` pattern exists and plan applies it (DN13); (6) temporal ordering — confirmed recorded_at field exists in records (record.js:315, corpus.js:506); (7) separation of concerns — tool is read-only LLM-free (approved-audit.js), document author applies judgment and cites evidence (Task 3 line 575-576); (8) Acceptance criteria — reviewed 12 checkpoints (line 568-590), all mechanically verifiable or clearly defined; (9) file changes — confirmed corpus.js listed in Validation 8 as unchanged artifact; (10) single-source-of-truth preservation — confirmed tool uses only existing corpus.js.audit() return, no new reads needed |
| security | fail | Attacked: (1) Receipt integrity validation - searched for explicit receiptIntegrityOk() requirement in Tasks and backlog; found fan-out identifies need but implementation Tasks (1.2b) and backlog (Task 5) do not explicitly require/defer it. (2) Trust boundaries in hash comparisons - verified that plan uses cross-file comparison (ship_plan_hash vs recorded_reviewed_plan_hash) but does not explicitly require validating the receipt before reading from it. (3) Path validation - confirmed plan requires isRepoRelativeEvidencePath validation via DN13 and Task 1 item 1, pre-filesystem. (4) Backlog matching - examined Task 1 item 4 which acknowledges loose substring matching; no explicit false-positive remediation. (5) Timestamp parsing - reviewed G3 temporal contract; plan calls Date.parse which is lenient, not strict ISO. Could not find evidence of actual exploit path from these gaps in the current infrastructure (evidence-audit already validates receipts), but defect exists in this plan's specification vs identified requirements. |
| test | fail | Attacked the plan's core falsifiability: (1) Searched for and read the acceptance criterion definition — found it explicitly stated as undefined (Multi-Perspective Fan-out L92, a CRITICAL finding embedded in the plan's own review). (2) Examined Task 2 test specification (12 items) against each of the three gates (G1/G2/G3 from DN1) — G2 (reviewer lens scope) cannot be falsified by any test in the list. (3) Read hash.test.js line-by-line to verify whether archive-path round-trip coverage exists in the codebase — confirmed it does not; Task 2 item 4 introduces it. (4) Searched for tests of recorded_at parsing robustness — found Date.parse vulnerability uncovered by Task 2 item 12. (5) Checked evidence-audit and corpus test patterns (the models M11 will mirror) for how they handle "absence vs structural zero" — found corpus.test.js has blind/degraded rules, but M11 plan relies on fixtures to test them, not on contract validation. (6) Verified path validation ordering in review-verdict.js (L80-95) — the validator exists and is sound, but Task 2 item 9's fs-stub test cannot prove call order without integration test. |
| invariant | fail | Attacked plan's acceptance criteria (lines 568-590) against PRD requirement (line 108: judge whether approvals were correct). Checked whether acceptance requires finding false-approves: found no such criterion. Verified vacuous-truth risk: zero misses found → criterion 'each miss cites evidence' is vacuously true. Examined receipt anchoring: plan acceptance requires dossier in git but ship receipt has no field recording audit findings. Checked state ladder semantics: degraded state (exit 1) still passes acceptance, silencing corroboration failures. Verified permission asymmetry: audit reporting 'all 5 correct' and 'all 5 false-approves' both satisfy same acceptance. Traced corpus.js dependency: plan assumes converged verdicts from prior runs without revalidation. Confirmed DN2 intent ('tool reports, humans judge') conflicts with acceptance criteria (pass regardless of judgment content). |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 1788043,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:2c5bb64915b73de4ffd2c00f0e10874e3d44ea1e312ac87d37f6238100728025",
  "plan_path": ".claude/plans/diverse-agent-review-m11.plan.md",
  "recorded_at": "2026-08-27T07:43:41.753Z"
}
```
