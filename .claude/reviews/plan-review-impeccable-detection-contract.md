# Plan Review Panel — impeccable-detection-contract

**Plan**: `.claude/plans/impeccable-detection-contract-m4.plan.md` · **Plan version**: `sha256:911ff610cb2c92b57c316764187d73c4dbb111b43a539a95d023e5cac161272c`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 10 blocking finding(s): architect/FAIL, security/CRITICAL, security/HIGH, security/HIGH — MCCP_REVIEW_SINGLE_PASS=scope_too_small 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | Phase filtering implementation will not break backward compatibility for implement gate's routeCommands calls | Plan line 116 specifies `opts.phase` defaults to 'pre', but existing test at plugins/mccp/scripts/lib/tests/impeccable-routing.test.js:42-55 calls `routeCommands({gate:'implement',...})` without phase parameter and asserts length===16 (IMPLEMENT_COUNT). After plan, same call with phase='pre' default returns only 14 entries (pre-phase). Plan line 155 documents implement constants as '14/5/19' but never specifies how existing unphased calls are handled. Additionally, prp-implement.md Phase 3.6.4 (Task 5 line 145) calls `routeCommands` without phase parameter to get finish commands, but if default phase='pre', it returns only pre-phase entries. |
| security | CRITICAL | Plan Task 4 & 5 lack specification for required 'status' field in impeccable_commands_routed entries | Schema.js lines 917-937 require entries to have {command, call_form, status} where status ∈ {invoked, recommended, failed, unknown-skill, skipped}. Plan Task 5 Action 3 says '{command, call_form, status}를 누적한다' but never explains where status value comes from. Routing oracle output (impeccable-routing.js:239-241) produces {command, stage, callForm} — no status field exists. If implementer copies oracle output, schema validation will reject the receipt due to missing status field. |
| security | HIGH | Schema mismatch between routing oracle output and receipt entry requirements creates silent data loss or bypass opportunity | Routing oracle returns {command, stage, callForm} but receipt entries must be {command, call_form, status}. Plan Task 4 Action 3 says 'entry must pass existing schema validation' but doesn't document the field transformation. If implementer naively hardcodes status='invoked' for all finish commands without verification, receipt will claim commands were invoked when they were actually never run. This violates trust boundary between actual execution and receipt claim. |
| security | HIGH | Plan does not specify mechanism to populate required status field from routing decision | Task 5 Action 3 describes calling routeCommands and processing results but provides no algorithm for deriving status values. The five allowed statuses (invoked, recommended, failed, unknown-skill, skipped) are outcome states but Task 5 only discusses call_form processing. Plan assumes status will appear magically from somewhere. No file path or data source is cited. |
| security | MEDIUM | Phase parameter could leak into receipt if not properly stripped, violating schema stability contract | Plan Task 3 adds phase parameter to entry() function and claims it's stripped like signal. Write.js lines 710-715 read impeccable_commands_routed from file. If phase value reaches receipt (either directly or via intermediate data structure), it will fail schema validation which only allows {command, call_form, status} per schema.js line 903-904. No test is specified to verify phase is actually stripped before restamp. |
| test | HIGH | Plan Task 2 changes shape callForm from 'background' to 'recommend', but existing test (a) will fail with this change | Plan line 107 instructs changing entry('shape', 'discovery', 'background', null) to entry('shape', 'discovery', 'recommend', null). Current test at plugins/mccp/scripts/lib/tests/impeccable-routing.test.js:42-55 (test '(a)') explicitly asserts at line 47: `assert.strictEqual(m.shape.callForm, 'background');`. Plan Task 2 validation (line 110) says `node --test plugins/mccp/scripts/lib/tests/impeccable-routing.test.js` would pass, but this test would fail after the code change. Plan Task 6 says it will 'add four tests' but does not state it will update or fix the failing assertion at line 47. |
| test | HIGH | Plan introduces phase filtering that reduces implement catalogue from 16 to 14 pre-phase entries, but existing test length assertions won't be updated | Plan Task 3 introduces phase filtering with default 'pre'. Existing entries clarify/distill are moved from pre-phase to finish-phase, leaving 14 pre-phase entries. Current test at line 45 asserts `assert.strictEqual(r.commands.length, IMPLEMENT_COUNT);` where IMPLEMENT_COUNT=16 (line 23 comment confirms '14→16'). With phase='pre' as default, routeCommands would return 14 entries, not 16. Plan Task 6 says 'implement는 pre 14 / finish 5 / 합 19를 상수로 pin' but does not explicitly state that IMPLEMENT_COUNT constant must be updated from 16 to 14, nor does it state what phase parameter should be passed to maintain backward compatibility. Line 98 (test g) also asserts r.commands.length === IMPLEMENT_COUNT and would also fail. |
| test | HIGH | Plan Task 6 lists only 'adding' new tests but validation commands in Task 2 and 3 will fail due to updated code breaking existing test assertions | Plan Task 2 validation line says: `node --test plugins/mccp/scripts/lib/tests/impeccable-routing.test.js` and Task 3 repeats same validation. Plan Task 6 line 152 says 'impeccable-routing.test.js에 넷을 추가한다' (add four tests), not 'update existing tests'. But existing test (a) at line 42-55 will fail after code changes. Task 6 test 2 says to verify 'callForm === background 인 엔트리가 0건' which would pass after changes, but this doesn't fix the existing assertion that background SHOULD exist. |
| test | MEDIUM | Plan Task 6 metric test 1 may not catch the shape callForm change due to testing negative logic | Plan Task 6 test 1 (line 153) says: 'callForm !== "recommend" 인 명령은 INTERVIEW_REQUIRED_COMMANDS에 속하지 않는다' (commands with non-recommend callForm must not be in INTERVIEW_REQUIRED_COMMANDS). But after Task 2 changes shape to 'recommend', shape will never have callForm !== 'recommend', so this test validates the wrong thing. The test verifies that existing invoke-form commands aren't interviews, but it doesn't verify that shape specifically is recommend. A separate test assertion checking shape.callForm === 'recommend' would be needed, but the plan doesn't mention it. |
| invariant | MEDIUM | Receipt entries appended by restamp lack phase provenance, violating anchoring principle for future audits | Task 4, lines 131-132: restamp appends entries as `{command, call_form, status}` without phase field. Task 3-4 defines finish-phase entries but Task 4 schema says no phase in appended entries. This means auditors cannot determine from receipt alone which entries (e.g., 'clarify') came from which phase, especially if routing logic changes in future PRs. While the finish-phase command list is deterministic (5 named commands), the receipt structure itself under-encodes the relationship. |
| invariant | MEDIUM | Restamp validation flow is not explicit in plan, creating implementation risk for digest recalculation | Task 4, line 131: plan says 'digest를 재계산한다' and references mirroring `restampGroundingVerdict` (`:1064`), but doesn't explain: (a) where `validate()` is called in the new restamp function, (b) whether it uses same hash algorithm as original, (c) what happens if validation fails. The mirror pattern does validate (write.js:1104-1109), but plan leaves critical validation flow as implementation detail, risking missed or incorrect validation. |
| invariant | MEDIUM | Finish-phase routing code path lacks reproducible automated test coverage | Task 8, lines 178-184: finish-phase routing is only testable by adding temporary `scratch-m4-surface.css` UI file to make `FINISH_SURFACE=1`. This workaround means: (1) code path tested only once per cycle, (2) test is not repeatable via CI, (3) future PRs touching UI might uncover bugs not caught by this one-time validation. Plan honestly documents this limitation but does not mitigate it. Three success criteria (line 180-182) all depend on this temporary file existing. |
| invariant | MEDIUM | Restamp operation is fail-open in advisory phase, risking silent under-reporting in receipt | Task 5, line 145: Phase 3.6.4 restamp is 'fail-open + loud stderr' because 'Phase 3.6은 advisory'. While consistent with existing Phase 3.6 behavior, this means if restamp fails (e.g., `routeCommands` crashes, file write fails), the receipt will be incomplete (`meta.impeccable_commands_routed` won't have finish entries) but gate completes normally. Plan says failure is logged in 'Phase 5 REPORT' but this is log output, not machine-checkable receipt field. Incomplete receipt becomes audit liability. |
| invariant | MEDIUM | Phase default='pre' in routeCommands creates silent loss risk for callers that forget to specify phase | Task 3-2, line 116: `opts.phase` defaults to `'pre'`. If any code path calls `routeCommands` without specifying phase (e.g., in future PRs), finish-phase entries silently disappear from routing result. Task 6-3 pins that plan/prd/pr outputs are unchanged, which mitigates this risk for those gates. But no test prevents NEW callers from making this mistake, and the default-to-pre behavior is not documented in code comments per plan. |
| invariant | LOW | Exclusion of 'onboard' from finish-phase routing lacks schema/evidence backing from impeccable 4.1.1 | Task 3-4, line 123-124: plan excludes `onboard` because it '새로 짓는 명령' (creates new surfaces). But plan does not cite impeccable 4.1.1 SKILL.md or command-metadata.json to prove this. Task 1 cites proof for `shape` (file path + line), but Task 3's onboard exclusion rests on assumption. If impeccable's onboard actually modifies existing code (not creates new surfaces), excluding it from finish-phase would be wrong decision. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified plan's architectural claims: (1) entry() function signature and Object.freeze usage mirrors existing pattern at impeccable-routing.js:51 ✓ (2) routeCommands public schema stripping at lines 235-243 supports hiding phase metadata ✓ (3) PLAN_GUIDE and gate tables share structure correctly ✓ (4) schema.js already contains `meta.impeccable_commands_routed` validation at lines 901-937, so 'schema 변경 0' claim is accurate ✓ (5) Phase filtering preserves duplicate-call invariant for pre/finish split ✓ (6) External plugin file citations are acknowledged and Task 1 plans verification ✓ (7) Test structure at Task 6 is sound for new tests (M4 metric, background unreachable, 0-firing stages). However, backward compatibility gap: current test at impeccable-routing.test.js:42 expects 16 entries with default call, but plan's phase='pre' default causes pre-phase-only filtering (14 entries for implement). Plan documents implement will have 14/5/19 counts but never specifies whether the default behavior preserves backward compat or requires phase parameter at existing call sites. This affects tests and prp-implement.md Phase 3.6.4 which calls routeCommands without phase." |
| security | fail | Analyzed the plan's Task 4-5 entry creation pathway: (1) Traced routing oracle output structure (impeccable-routing.js:239-241 returns {command, stage, callForm}) (2) Checked schema validation rules for impeccable_commands_routed (schema.js:917-937 requires {command, call_form, status}) (3) Searched for any status-derivation logic in Task 5 — found none (4) Verified that plan Task 4 Action 3 acknowledges schema pass-through requirement but does not specify how status is populated (5) Checked whether intermediate data structures could auto-populate status — no source identified. (6) Searched for test coverage of status field computation — restamp-routed.test.js mentioned but no implementation details given on how status values are verified. The core gap is structural: routing oracle has {command, stage, callForm}, schema requires {command, call_form, status}, plan provides no bridge. |
| test | fail | Examined plan Tasks 1-8 and cross-referenced against: (1) current impeccable-routing.js entry() function signature and STAGE_ROUTING.implement table (16 entries with shape as 'background'); (2) existing test file impeccable-routing.test.js with specific assertions at lines 23, 42-55, 62, 98, 104; (3) plan's Task 2 code changes (shape background→recommend); (4) plan's Task 3 phase filtering (distill/clarify move to finish-phase, creating 14 pre-phase entries); (5) plan's Task 6 new test requirements (four tests to add); (6) plan's validation commands in Tasks 2, 3, 5 that reference running `node --test plugins/mccp/scripts/lib/tests/impeccable-routing.test.js`. Found three assertions that will fail after code changes but plan does not explicitly instruct updating them: shape.callForm assertion (line 47), commands.length assertions (lines 45, 98 against IMPLEMENT_COUNT=16 which should become 14). Plan does not reconcile the breaking changes with existing tests. |
| invariant | fail | I traced the restamp pattern against existing `restampGroundingVerdict` code to verify validation exists; checked current receipt schema for `impeccable_commands_routed` structure to confirm no schema changes needed; reviewed routing.js entry structure and task 3's phase addition to identify information loss; examined test coverage in impeccable-routing.test.js to check if phase-filtering is tested; looked at accept criteria to check if live test is actually gated on finish-phase entries appearing. I attacked: (1) fail-open drift by confirming restamp is fail-open in advisory phase with incomplete receipt as result, (2) anchoring by verifying recipe entries don't encode which phase they came from, making future audits impossible without external knowledge, (3) testing gaps by confirming finish-path only tested with temporary workaround, (4) phase-filtering defaults by confirming default='pre' could hide finish entries from callers, (5) onboard exclusion reasoning by checking for evidence in plan." |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 947177,
  "halt_stage": null,
  "backlog_appended": 10,
  "backlog_skipped_nonblocking": 9,
  "granted": 4,
  "reviewed_plan_hash": "sha256:911ff610cb2c92b57c316764187d73c4dbb111b43a539a95d023e5cac161272c",
  "plan_path": ".claude/plans/impeccable-detection-contract-m4.plan.md",
  "recorded_at": "2026-08-23T04:09:49.121Z"
}
```
