# Plan Review Panel — release-channel-separation-m3

**Plan**: `.claude/plans/release-channel-separation-m3.plan.md` · **Plan version**: `sha256:3097ebb310e95340820244741eec98e9c07342a9c80871fca640dc9b638ffd39`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 4 blocking finding(s): architect/HIGH, architect/FAIL, test/HIGH, test/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | DD8이 근거로 삼은 '기계가 이미 이 경계를 강제한다'는 명제가 거짓이다. 인용한 env-contract lint의 L8/L10 어디에도 evidence가 `plugins/mccp/` 안이어야 한다는 제약이 없다 — 따라서 `MCCP_RELEASE_CUT`을 registry에 넣는 것은 '구조적으로 만족 불가'가 아니라 그냥 만족 가능하다. 그럼에도 plan은 이 거짓 명제를 산출물(런북)에 '적기만 한다'고 지시한다(내구 문서에 misquoted precedent를 봉인). | plan L183-187: 'registry에 넣으면 env-contract/lint.js L8/L10이 요구하는 런타임 read site가 plugins/mccp/ 안에 없어 구조적으로 만족 불가다 — 즉 기계가 이미 이 경계를 강제하고 있고, M3은 그 사실을 런북에 적기만 한다.' 반증: env-contract/lint.js:536 `readFile(path.join(root, rel))` — evidence 경로는 **repo root** 기준으로 해소되므로 repo-root `scripts/version-declaration-guard.js`도 실재 검사를 통과한다. L10 정방향은 evidence 행 ±2에 이름이 있으면 통과하는데(evidence-name.js:131, EVIDENCE_WINDOW=2), scripts/version-declaration-guard.js:131 `const raw = env.MCCP_RELEASE_CUT;`가 정확히 그 조건을 만족한다. 경로 도메인 제약은 not-consumed 역방향(docs/environment/*.md 앵커 요구, evidence-name.js:110)에만 존재한다. |
| architect | MEDIUM | 검사 7이 DD9의 불변식을 부분적으로만 잰다. DD9는 '이 PRD가 완료되면 PRD와 plan이 archived/로 이동하므로 런북은 그것에 의존해선 안 된다'고 선언했는데, 정규식은 m1·m2 plan **두 파일만** 열거한다 — M3 자신의 plan과 PRD 경로 인용은 그대로 통과하고, 둘 다 §3.11대로 이동한다. 즉 '이것을 기계로 잰다'는 주장이 자기 검사보다 넓다. | plan L189-192: '이 PRD가 완료되면 §3.11대로 PRD와 plan이 archived/로 이동한다 … `## Validation` 검사 7이 이것을 기계로 잰다.' vs plan L392 검사 7: `grep -qE 'release-channel-separation-m[12]\\.plan\\.md' docs/release-channel.md` — `-m3.plan.md`와 `.claude/prds/release-channel-separation.prd.md`는 매칭되지 않는다. §3.11이 PRD 목적지를 `.claude/prds/archived/`로 못박는다. |
| architect | MEDIUM | 검사 14의 backlog 행 형식이 이 저장소의 canonical writer가 내는 형식과 어긋난다. canonical append는 선행 파이프 `\| `로 시작하는데, 검사는 선행 파이프 **없는** 줄만 센다. 규격대로 적재하면 검사가 0을 세어 HALT하고, 검사를 통과시키려면 파서가 '이건 데이터가 아니라 파서를 고친다'며 특례로 받아 주는 비정규 형태를 쓰게 된다 — 게이트가 잘못된 형식을 유도한다. | plan L446: `BACKLOG_ROWS=$(grep -c "^$(date +%Y-%m-%d) \|" .claude/plans/codex-findings-backlog.md \|\| true)`. 반대로 derive/sources/backlog.js:41-43은 'Canonical appends (plan-review/cli.js backlog-append) always emit the leading pipe'라 적고, plan-review/backlog-append.js:170이 `return '\| ' + escapeCell(o.date) + ' \| ' …`로 실제 선행 파이프를 낸다. 파일 자체의 헤더/최신 행도 `\| 2026-08-13 \| MEDIUM \|`(backlog.md:8-10) 형태다. |
| architect | LOW | 검사 1이 원격 가변 상태(`origin/release`)를 리터럴 SHA에 결속해, 이 마일스톤의 게이트가 외부 행위자(운영자의 첫 릴리스 컷)에 의해 붉어질 수 있다. 하필 이 마일스톤이 그 컷 절차를 문서화하는 축이다. | plan L327-328: `[ "$REMOTE_RELEASE" = "647dfecba75eecd9287ee538ca5f7056c7ba71da" ] \|\| { echo "HALT: M3 must not move the release channel…"; exit 1; }` — 검사가 재는 명제는 '이 브랜치가 좌표를 움직였는가'인데 실제로 재는 것은 '원격 release가 특정 커밋에 있는가'다. PRD 결정 3은 컷 시점을 '운영자가 정한다'로 열어 둔다(prd.md:61). |
| security | MEDIUM | Check 9 does not reject what the plan claims it rejects. The plan's Patterns row (line 57) states the invariant as "모든 force-push는 `--force-with-lease=<관측 SHA>` — 인자 없는 lease도, bare `--force`도 쓰지 않는다", and the check-9 comment claims it protects "M1 D3이 흡수한 규율(모든 force-push는 관측 SHA에 결속된 명시 lease)". The regex `git push[^\|]*--force( \|$)` only catches the literal long option. A runbook line `git push -f origin release`, `git push origin +release:release`, or — most likely, because it looks correct — a bare `git push --force-with-lease origin release` with no `=<sha>` all pass the check. The no-arg lease is the exact form the plan bans and the exact form that silently clobbers a concurrent `release` move once the runbook's own earlier `git ls-remote`/fetch step has refreshed the remote-tracking ref. Concrete input→consequence: the runbook text drifts to a no-arg lease during rollback; check 9 stays green; an operator following it force-moves `release` (which DD7 confirms has *no* branch protection: `gh api …/branches/release/protection` → 404) onto an unintended commit that reaches every installed user on the next `claude plugin update`. | .claude/plans/release-channel-separation-m3.plan.md:57 and :406-413 (check 9 regex `git push[^\|]*--force( \|$)`); DD7 at :175-181 confirms no branch protection exists |
| security | MEDIUM | The absolute-path leak detector (check 13) covers only 4 of the 7 git-tracked files this milestone writes. Its pathspec is `-- docs README.md CLAUDE.md .claude/PRPs/reports`, but `## Files to Change` also lands `.claude/prds/release-channel-separation.prd.md`, `.claude/plans/codex-findings-backlog.md`, and `CHANGELOG.md`. Task 8 explicitly appends this cycle's deferred axes and gate MEDIUM/LOWs to the backlog, and this cycle's evidence lives in a worktree (`.worktrees/c0-release-channel-separation/`), so an absolute worktree/HOME path entering a backlog row or a PRD Open-Question answer is a realistic input. The consequence is exactly the repo's documented precedent — an absolute path sealed into a git-tracked artifact — while the check's own comment asserts the invariant is "이 브랜치가 **추가한** 줄에 절대 경로 0건" for the branch, not for four directories. | .claude/plans/release-channel-separation-m3.plan.md:434-443 (`git diff --unified=0 origin/main...HEAD -- docs README.md CLAUDE.md .claude/PRPs/reports`) vs :198-204 (Files to Change lists prd.md, codex-findings-backlog.md, CHANGELOG.md) |
| security | MEDIUM | Check 13 reproduces the instrument-death-reads-as-clean anti-pattern that the plan itself condemns in check 2. `ADDED=$(git diff … \| grep '^+' \| grep -v '^+++' \|\| true)` swallows a `git diff` failure into an empty `ADDED`, after which `printf '%s\\n' "$ADDED" \| grep -qE …` finds nothing and the leak gate reports clean. Check 2's own comment states the rule being violated: "파이프라인의 종료코드는 grep의 것이고, git이 죽으면 grep은 빈 입력에 1을 돌려 `if`가 거짓이 된다 — 즉 **기구 사망이 "무변경"으로 읽힌다**". Check 2 guards this with an explicit `git rev-parse --verify` + separated `git diff` assignment; check 13 has no such guard, so the plan's single leakage detector is the one check that can fail open. | .claude/plans/release-channel-separation-m3.plan.md:330-338 (check 2's stated rule and its guard) vs :439-443 (check 13 with `\|\| true` on the whole pipeline and no instrument-liveness guard) |
| security | LOW | Check 4 (version-declaration guard) can be exempted by an ambient environment variable, and the CHANGELOG number-squatting axis has no second net. `releaseCutReason(env)` returns a reason from `process.env.MCCP_RELEASE_CUT` with no requirement that the caller intended a release cut in this run; if that value survives in the shell from any prior release-cut work, check 4 exits 0 even though this branch squatted a `## [X.Y.Z]` heading. The plugin.json/footer axes are independently caught by check 2's file loop, but CHANGELOG.md is not in that loop, so the number-squatting axis of the guard is the one that becomes unbacked. M3 is the milestone that owns umbrella decision 1, which the plan itself calls "위반은 이중으로 비싸다". | scripts/version-declaration-guard.js:130-141 (`releaseCutReason` reads env unconditionally; format-only validation) and .claude/plans/release-channel-separation-m3.plan.md:339-346 (check 2 file loop omits CHANGELOG.md), :368-374 (check 4 invoked without clearing MCCP_RELEASE_CUT) |
| test | HIGH | The plan names label honesty as this milestone's ONLY failure mode, yet it is the one prose property with no mechanical check — while weaker prose properties (bare --force, plan citations, pointer strings) each got a grep. The asymmetry is unjustified because the assertion is trivially greppable. | Plan Risks L462: '절마다 측정됨/전사됨/미측정 라벨을 붙인다. 컷 절이 "미측정"이라는 사실이 문서의 1급 내용이며, 그것을 숨기는 것이 유일한 실패다' and Acceptance L479-480 '각 절이 측정됨/전사됨/미측정으로 라벨된다'. The Validation block's only runbook-existence check is L377 `test -f docs/release-channel.md`; checks 7/9 (L392, L411) grep the same file for far less load-bearing properties. Preamble L312-314 concedes '라벨이 정직한가 — 은 사람이 본다'. |
| test | MEDIUM | Check 7 does not test DD9's stated invariant. DD9 says the runbook must not depend on files that archiving relocates, but the regex is hard-coded to m1/m2 plan filenames only — it misses this very plan (-m3), and misses the PRD, which §3.11 also relocates (.claude/prds/archived/). It also scans only docs/release-channel.md, not the README/CLAUDE.md pointers added in Tasks 4-5. | Plan L189-192 (DD9) vs Validation check 7, L392: `grep -qE 'release-channel-separation-m[12]\\.plan\\.md' docs/release-channel.md`. CLAUDE.md §3.11: 'PRD: 활성 .claude/prds/ → .claude/prds/archived/'. Files to Change L201 shows the PRD is a live citation target. |
| test | MEDIUM | Check 9 tests only half the discipline it cites. The Patterns to Mirror row commits to 'no argument-less lease AND no bare --force', but the regex catches only bare `--force`; an argument-less `--force-with-lease` (the exact footgun D3 named) and the `git push -f` shorthand both pass silently. | Plan L57: '모든 force-push는 `--force-with-lease=<관측 SHA>` — 인자 없는 lease도, bare `--force`도 쓰지 않는다' vs check 9 L411: `grep -nE 'git push[^\|]*--force( \|$)' docs/release-channel.md` — the comment at L410 explicitly notes `--force-with-lease` is excluded from the pattern, with no compensating assertion that every lease carries a value. |
| test | LOW | Check 14's backlog row pattern does not match the row format used by the file's most recent entries, so Task 8 will either fail the gate or be forced into a format inconsistent with current practice. | Validation check 14, L446: `grep -c "^$(date +%Y-%m-%d) \|" .claude/plans/codex-findings-backlog.md` requires no leading pipe, but .claude/plans/codex-findings-backlog.md:290-306 (the newest block) all begin `\| 2026-08-19 \|`, as does :10. |
| invariant | MEDIUM | 검사 4는 판정을 version-declaration-guard.js에 위임하는데, 그 가드는 ambient `MCCP_RELEASE_CUT` env가 세팅돼 있으면 위반이 있어도 exit 0을 낸다. 계획은 이 env를 unset하거나 부재를 단언하지 않으므로, M3이 스스로 '이 규칙의 소유 마일스톤이라 위반은 이중으로 비싸다'고 적은 축의 게이트가 주변 환경 하나로 조용히 열린다(§3.12의 ambient `codex_disabled`가 proof가 되던 결함과 같은 형태). 런북이 그 값을 쓰라고 가르치는 절차(Task 3-2)를 문서화하므로 운영자 셸에 남을 개연성도 실재한다. | plan:373-374 `node scripts/version-declaration-guard.js --base origin/main \|\| {...}` (exit code만 검사) vs scripts/version-declaration-guard.js:239 `const ok = violations.length === 0 \|\| cutReason !== null;` 및 :258-262 `release cut allowed:` 경로가 exit 0으로 끝남. plan은 `MCCP_RELEASE_CUT` 부재를 어디서도 단언하지 않는다. |
| invariant | MEDIUM | 검사 9는 'lease 없는 강제 이동을 문서가 권하지 않는다'는 규율의 기계 지점이라고 선언하지만, 계획 자신이 금지한 3형태 중 1형태만 잡는다. `git push -f`(단축형)와 **인자 없는 `--force-with-lease`**는 정규식에 걸리지 않는다 — 후자는 `## Patterns to Mirror`가 명시적으로 금지한 형태다. 파괴적 명령을 가르치는 유일 산출물에서 게이트가 게이트처럼 보이면서 금지 대부분을 통과시킨다. | plan:411 `if grep -nE 'git push[^\|]*--force( \|$)' docs/release-channel.md; then` (주석 :410 "`--force-with-lease`는 `--force` 뒤에 `-`가 오므로 이 패턴에 걸리지 않는다") vs plan:57 "모든 force-push는 `--force-with-lease=<관측 SHA>` — **인자 없는 lease도**, bare `--force`도 쓰지 않는다" |
| invariant | LOW | 검사 13은 계획 자신이 검사 2 주석에서 '기구 사망이 무변경으로 읽힌다'며 금지한 파이프라인 패턴을 그대로 쓴다. `git diff \| grep \| grep -v \|\| true`는 종료코드가 마지막 grep의 것이고 `\|\| true`가 그마저 삼키므로, git diff가 실패하면 `$ADDED`가 비어 유출 검사가 '깨끗함'으로 통과한다. | plan:439-441 `ADDED=$(git diff --unified=0 origin/main...HEAD -- docs README.md CLAUDE.md .claude/PRPs/reports \| grep '^+' \| grep -v '^+++' \|\| true)` vs plan:332-334 "`git diff \| grep -q`를 쓰지 않는다. 파이프라인의 종료코드는 grep의 것이고, git이 죽으면 grep은 빈 입력에 1을 돌려 ... 즉 **기구 사망이 '무변경'으로 읽힌다**" |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | DD8의 '기계가 이미 강제한다' 주장을 env-contract/lint.js L8(:525-544)·evidence-name.js 정방향/역방향 코드로 직접 대조해 반증했다(경로 제약 부재 확인). DD9 자기완결성 불변식과 검사 7 정규식의 범위 차이를 §3.11 아카이브 목적지와 대조했다. 검사 14의 backlog 행 형식을 derive/sources/backlog.js 파서 주석과 plan-review/backlog-append.js의 실제 출력, 그리고 backlog.md 현행 행과 3중 대조했다. 그 외 공격했으나 결함을 못 찾은 축: 검사 4(version-declaration-guard 호출 — 오라클 재구현 없이 위임, merge-base 기준 확인), 검사 2·3(파이프 종료코드 함정 회피와 marketplace source shape 단언), 검사 10(PRD 미체크 OQ grep이 실제 `- [ ] **` 4건과 Task 6의 답변 대상 OQ1·2·3·5와 정확히 일치), Files to Change의 '건드리지 않는 것' 목록과 검사 2의 릴리스 표면 열거의 정합, README/CLAUDE.md 포인터-only 경계(§3.7 resident + instruction-contract lint 호출). |
| security | pass | Attacked five axes end to end. (1) Trust boundary of the release channel: confirmed DD7's claim that branch protection is absent and traced whether the plan's lease discipline is machine-backed — check 9's regex is narrower than the invariant it claims to enforce (finding 1). (2) Durable-artifact leakage, the repo's known cwd-leak precedent: compared check 13's pathspec against Files to Change (finding 2) and its shell shape against the plan's own check-2 rule (finding 3). (3) Bypass/escape surfaces: verified `releaseCutReason` in scripts/version-declaration-guard.js:130-141 actually enforces >=30 chars / >=3 words as the plan claims (it does), then looked for an ambient-env bypass and found the CHANGELOG axis unbacked (finding 4, LOW). (4) Tamper/immutability: check 3's `sha`-pin detector was attacked as a partial-state trust problem — it fails closed on every field (`source`, `ref`, `path`, `url`, `sha` presence) and rejects a non-object `source`, so a half-written manifest cannot be read as intact; no defect. Check 1 reads the remote directly rather than a cached ref, and check 10/14 fail closed on a missing file under `set -eu` — I could not make instrument death read as clean there. (5) Escalation via the runbook itself: check 7 (no plan-file citations) and check 8 (every hex coordinate must resolve) do what they claim; I could not construct a hostile coordinate that reaches a user. UI5's temporary-`sha` residue risk is genuinely closed by check 3, and DD7's decision not to enable branch protection is an argued residual with evidence, not a plan defect. No HIGH or CRITICAL landed. |
| test | fail | Attacked: (1) transcription fidelity — grepped the M1 report for 1.33.7→1.33.4, 12s, 647dfec pin; the plan's claimed measured values check out (m1-report.md:104,108,148,124) so that attack failed. (2) Path realism — scripts/version-declaration-guard.js exists at the cited path; README.md:42 contains the exact sentence Task 4 edits; .claude-plugin/marketplace.json shape assertion is coherent. (3) Instrument-death direction of checks 1/2/12 — each already guards the failing-instrument path (ls-remote non-empty, rev-parse verify, diff exit guards), so the 'dead tool reads as clean' attack failed. (4) Over-permissive direction of the FF observation — Task 2 item 2 requires the reverse-direction pair, a genuine positive control; failed to refute. (5) Check 10 OQ pattern matches the PRD's actual `- [ ] **` lines. Findings above are where I did land: the label-honesty claim with no falsifier, check 7's regex not covering DD9's stated set, check 9 testing half its cited discipline, and check 14's row-format mismatch against backlog:290-306. |
| invariant | pass | 검사 1~14를 한 줄씩 unknown-input으로 공격했다. 검사 1·2·12는 ls-remote/rev-parse/명시 guard로 fail-closed임을 확인(instrument death → HALT). 검사 10·14는 파일 부재 시 빈 문자열 → `[ ... ]` 실패 → HALT로 접히는 것을 확인했고, 검사 14의 날짜 행 형식(`^YYYY-MM-DD \|`)이 실제 backlog(254건 매칭)와 일치함을 확인해 '영원히 붉음' 가설을 기각했다. 검사 3(`sha` 잔존 탐지)·검사 5·6·7·8은 모두 부재/실패가 HALT 방향임을 확인. 검사 8의 hex-token 요구가 정상 문서를 붉게 할 수 있으나 fail-closed 방향이라 finding 아님. 검사 4는 guard 소스를 직접 읽어 exemption 경로(:239, :258)가 exit 0임을 확인해 finding 1을 세웠다. 검사 9의 정규식을 금지 3형태와 대조해 finding 2를, 검사 13의 파이프라인을 계획 자신의 검사 2 주석과 대조해 finding 3을 세웠다. DD1(2차 리허설 없음)의 롤백-증거 결핍을 fail-open으로 몰아보려 했으나, 머지 후 리허설이 실제 강등 배포가 된다는 논증(PRD Evidence + M1 보고서 인용)이 성립하고 n=1 한정이 산출물에 명시 요구되므로 반증 실패. 채널 무이동 불변식(검사 1의 리터럴 SHA 핀)과 릴리스 표면 diff 0(검사 2)은 우회 경로를 못 찾았다. HIGH/CRITICAL 근거는 세우지 못했다. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 372307,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:3097ebb310e95340820244741eec98e9c07342a9c80871fca640dc9b638ffd39",
  "plan_path": ".claude/plans/release-channel-separation-m3.plan.md",
  "recorded_at": "2026-09-03T09:01:09.485Z"
}
```
