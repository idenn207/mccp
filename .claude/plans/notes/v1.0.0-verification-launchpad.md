# v1.0.0 Verification Launchpad

> 본 노트는 main worktree의 verification cycle 가이드. 11 worktree × 새 Claude 세션의 진입 명령 SoT.
> Verification 종료 시(W-VERDICT) archive 또는 verdict 파일에 통합.

## Preflight 상태 (2026-06-12 W0 부트스트랩 완료)

- ✅ 11 worktree 모두 디스크 존재 (`git worktree list | grep -c v1.0.0-verify-` → 11)
- ✅ 11 audit 스캐폴드 작성 완료
- ✅ 각 worktree `.claude/settings.local.json` = `{"env": {}}` (defaults 복귀)
- ✅ F1 absorption preflight enforcement 통과 (Codex R1 권장사항 충족)
- ⚠️ main worktree `.claude/settings.json` + `.claude/state/STATE.md` modified — hook 자동 갱신 추정, verification 동작 무관

## Findings 시작 인벤토리 (main 세션 Phase 5에서 라이브 캡처)

| ID | Worktree | Severity | Captured At |
|---|---|---|---|
| Finding A | fallback-ux (row 11m) | HIGH | Phase 5.2 first attempt — disk vs session env divergence |
| Finding B | receipts (첫 row) | HIGH | Phase 5.7 양쪽 모드 fail — validate-cmd derive-decision normalize partial-fix |
| Codex F1 (absorbed) | — | HIGH | manifest 10→11 count mismatch, plan v0.5 attestation으로 흡수 |
| Codex F2 (absorbed) | W2 binding | HIGH | real auth mutation → isolated CODEX_HOME 권장 |
| Codex F3 (absorbed) | W11 binding | MEDIUM | W11 self-scored → raw evidence + predeclared max + second review |

## 각 worktree 진입 명령 (copy-paste)

각 명령은 `cd` + `claude` 새 세션 시작 + 본 plan에서 명시한 첫 prompt. 외부 시스템 변경/인증/verdict 결정 외에는 사용자에게 묻지 않는 autonomy contract 적용.

### W1 — baseline (10 명령어 + work 3분기, 90분)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-baseline
claude
```

First prompt:
```
v1.0.0 verification W1 시작. ../../.claude/plans/v1-0-0-release-verification.plan.md Task W1을 자동 진행하고 .claude/audit/v1.0.0-baseline.md에 결과를 채워줘. 외부 시스템 변경 / 인증 / verdict 결정 외에는 사용자에게 묻지 마. main 세션의 Phase 5 Codex R1 absorption attestation을 binding amendment로 인지.
```

### W2 — codex-backoff (8 classification, 60분, F2 binding 적용)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-codex-backoff
claude
```

First prompt:
```
v1.0.0 verification W2 시작. Task W2의 8 codex classification을 audit/v1.0.0-codex-backoff.md에 채워줘. F2 absorption 적용: not-authenticated case는 isolated CODEX_HOME=<temp> 또는 mock companion으로 시뮬, 실 ~/.codex/auth.json 건드리지 마. 사용자 escalate은 dependency 조작 직전 1회만.
```

### W3 — impeccable (5 row, 45분)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-impeccable
claude
```

First prompt:
```
v1.0.0 verification W3 시작. Task W3의 4 impeccable scenario (5 row 포함) 를 audit/v1.0.0-impeccable.md에 채워줘. design_findings_dropped + a11y_routed_to_impeccable receipt meta 확인 필수.
```

### W4 — receipts (5 손상 시나리오, 60분, Finding B 첫 row 미리 캡처됨)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-receipts
claude
```

First prompt:
```
v1.0.0 verification W4 시작. audit/v1.0.0-receipts.md에는 이미 main 세션의 Finding B (validate-cmd derive-decision normalize partial-fix) 가 첫 row로 캡처됨. Task W4의 5 receipt 손상 시나리오를 추가로 채워줘. 자동 + 수동 복구 outcome 모두 기록.
```

### W5 — handoff (9 cell matrix, 60분)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-handoff
claude
```

First prompt:
```
v1.0.0 verification W5 시작. Task W5의 cost ceiling 3-tier × auto-handoff 3-mode 매트릭스를 audit/v1.0.0-handoff.md에 채워줘. claude.exe ENOENT degrade가 정상 outcome (Layer 2c minimum-spec mode confirmed).
```

### W6 — state-continuity (Session A + B, 45분, 세션 2회)

Session A:

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-state-continuity
claude
```

Session A first prompt:
```
v1.0.0 verification W6 Session A. mid-task state 만들기: synthetic plan 작성 + prp-implement 진입 직전에 종료. PreCompact 강제 trigger 후 audit/v1.0.0-state-continuity.md에 Session A row 작성하고 세션 종료. 종료 시 다음 세션 첫 prompt를 출력.
```

Session B (Session A 종료 후 같은 worktree에서 새 세션):

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-state-continuity
claude
```

Session B first prompt:
```
v1.0.0 verification W6 Session B. STATE.md inject가 새 세션 첫 message에 보이는지 확인하고 audit에 Session B row 작성. resume이 1줄 명령으로 가능했는지 평가. W11 second review pass도 진행 (F3 absorption — audit/v1.0.0-fallback-ux.md 점수 ±1 미만 disagreement 검증).
```

### W7 — docs-sync (4 항목, 30분)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-docs-sync
claude
```

First prompt:
```
v1.0.0 verification W7 시작. Task W7의 문서 분리 4 항목을 audit/v1.0.0-docs-sync.md에 채워줘. 발견된 stale 문서는 결함으로만 기록 (main에 변경 누수 금지).
```

### W8 — dual-reviewer (5-6 row, 90분, rate-limit risk 최고)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-dual-reviewer
claude
```

First prompt:
```
v1.0.0 verification W8 시작. Task W8의 reviewer escalate 3-cap × intentional finding 매트릭스를 audit/v1.0.0-dual-reviewer.md에 채워줘. codex 실 호출 사용. rate-limit hit 시 사용자에게 escalate.
```

### W9 — goal-loop (4 row, 30분)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-goal-loop
claude
```

First prompt:
```
v1.0.0 verification W9 시작. Task W9의 미구현/부분 동작 4 항목을 audit/v1.0.0-goal-loop.md에 (Built/Partial/Absent) 분류로 채워줘.
```

### W10 — env-matrix (22 row, 120분)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-env-matrix
claude
```

First prompt:
```
v1.0.0 verification W10 시작. Task W10의 환경변수 매트릭스를 audit/v1.0.0-env-matrix.md에 채워줘. 각 env × non-default value 1회씩. 매트릭스 row 사이 settings.local.json 복원 자동.
```

### W11 — fallback-ux (12 row + row 11m + raw evidence, 90분, F3 binding)

```powershell
cd c:\_project\my\my-claude-code-plugin\.worktrees\v1.0.0-verify-fallback-ux
claude
```

First prompt:
```
v1.0.0 verification W11 시작. audit/v1.0.0-fallback-ux.md에는 이미 Finding A (disk vs session env divergence, row 11m) 가 캡처됨. Task W11의 12 fallback UX 매트릭스를 추가로 채워줘. F3 absorption 적용: 각 row에 raw stdout/stderr 캡처 + predeclared max acceptable (Type, Next-step) score 명시 + scripted assertion 가능한 경우 적용. W6 Session B가 second review pass 수행 예정.
```

## 권장 실행 순서

| 단계 | Worktrees | 이유 |
|---|---|---|
| **Phase 1 (병렬 가능)** | W1, W3, W4, W7, W9 | codex 의존 적음, rate-limit risk 낮음 |
| **Phase 2 (순차 + 30분 휴식)** | W2 → W5 → W6 → W10 → W11 | dependency 조작 / cost mock / cross-session / env 매트릭스 / fallback UX |
| **Phase 3 (마지막)** | W8 | rate-limit risk 최고, 실 codex 다회 호출 |
| **Phase 4 (main 세션)** | W-VERDICT | 11 audit cherry-pick + verdict 작성 + ship 결정 사용자 위임 |

## 사용자 escalate 인벤토리 (최대 14회, F2 absorption 후)

- 11 worktree 진입 명령 (11회) — 본 노트 copy-paste
- W6 Session A → B 전환 (1회)
- W11 dependency 조작 직전 통합 confirm + 종료 후 복원 verify (2회)

총 14회 = dogfood 7-12h 동안 평균 30-50분당 1회.

## Verification 종료 시 (W-VERDICT, main 세션)

본 노트 archive 후 다음 진행:

```powershell
cd c:\_project\my\my-claude-code-plugin
# 11 audit 파일 main으로 cherry-pick
for /D %w in (.worktrees\v1.0.0-verify-*) do copy "%w\.claude\audit\v1.0.0-*.md" ".claude\audit\"
# verdict 작성
claude
```

W-VERDICT first prompt:
```
v1.0.0 verification W-VERDICT. .claude/audit/v1.0.0-*.md 11개를 통합해 release-verification-verdict.md 작성. 5-tier severity tally + W11 UX rubric 평균 + STOP_RELEASE / CONDITIONAL / GO 권고 + v0.4.0 axis 우선순위 재조정.
```

## 보류 결정 (Verification 종료 시 사용자 응답 후)

- main의 `MCCP_CODEX_DISABLED=1` settings.local.json 복원 — 사용자 결정
- 11 worktree 정리 (audit만 보존 vs 전체 보존) — 사용자 결정
- v1.0.0 ship 최종 결정 — 사용자 결정 (autonomy contract)
- Finding B 패치 우선순위 — verdict에서 권고
