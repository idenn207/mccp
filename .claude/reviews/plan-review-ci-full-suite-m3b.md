# Plan Review Panel — ci-full-suite-m3b

**Plan**: `.claude/plans/ci-full-suite-m3b.plan.md` · **Plan version**: `sha256:58ca81b0f1ff84fe54f4d42ffd0c0af22a563558e381f5aca99d54e09f48fda3`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 12 blocking finding(s): architect/HIGH, architect/FAIL, security/HIGH, security/HIGH

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | DD2 explicitly retracts `fully_skipped` ("이 milestone이 산출하지 않는다 … 그래서 필드를 만들지 않는다"), but Task 2 — the task that actually builds the module — still specifies it in the return contract of `computeCoverage` and instructs reporting it. The module's contract is defined in two places that disagree; an implementer following Task 2 builds exactly the surface DD2 forbids, and no Validation branch asserts its absence, so nothing catches the drift. The plan's own preamble claims `fully_skipped` 철회 was an absorbed R2 finding — the absorption landed in DD2 only. | .claude/plans/ci-full-suite-m3b.plan.md:150 ("**`fully_skipped`는 이 milestone이 산출하지 않는다 — producer가 없기 때문이다.**" … :157 "그래서 필드를 **만들지 않는다**") vs :379-383 ("`{denominator, numerator, excluded, unexplained, fully_skipped, coverage_pct, ok, reasons}`를 낸다" … "`fully_skipped`는 별도 필드로 보고하되 `ok`에 영향을 주지 않는다(DD2)"); preamble :20 lists "`fully_skipped` 철회" as absorbed. Validation branches :385-400 contain no `fully_skipped` assertion in either direction. |
| architect | MEDIUM | Same staleness in the gate flag's semantics: DD2 defines `--assert-accounted` as a three-way conjunction (unexplained===0 ∧ excluded.length <= max_excluded_files ∧ exclusions_digest match), but Task 2's normative flag description states only two conditions (unexplained, floor), dropping the two conditions R2 added as the CRITICAL closure of the "glob 한 줄이 게이트를 연다" path. The Validate branches (4)(9) do cover them, so this is contract drift rather than an open hole — but the authoritative task text an implementer reads specifies a weaker gate than DD2 does. | .claude/plans/ci-full-suite-m3b.plan.md:121-122 ("게이트 플래그는 `--assert-accounted`이고 조건은 **셋의 논리곱**이다 — `unexplained === 0` ∧ `excluded.length <= max_excluded_files` ∧ `exclusions_digest` 일치") vs :381 ("**`--assert-accounted`**(`unexplained > 0` 또는 `tracked < floor`이면 비영점 — 게이트가 부르는 것)") |
| security | HIGH | 머지 차단 게이트가 스위트 red에 대해 fail-open이다 — 어떤 Task도 `exit_code === 0`을 소비하는 단계를 만들지 않고, 러너는 test가 깨져도 exit 0으로 끝난다. 즉 '전 PR 강제'라 선언한 체크가 실패 test를 통과시킨다. | scripts/test-suite/run.js:724-725 `// 측정 도구는 스위트의 red로 죽지 않는다 — ok가 판정이고 exit_code는 데이터다` + `return result.ok ? 0 : 1`, 그리고 run.js:10-12 `ok = 측정이 성립했는가. 스위트 green이 아니다. exit_code가 비영점이어도 ok:true일 수 있고`. 계획 DD3(plan L173)은 `판정 순서를 exit_code === 0 → coverage → redaction_ok로 고정한다`고 선언하지만, 그 순서를 구현하는 소비처가 어디에도 배정되지 않았다 — Task 5의 단계 열거(plan L438-441)는 `격리 목록 검증 → 열거 sanity → 전수 실행 → 커버리지 --assert-accounted ... → redaction_ok`이고, Task 2의 `coverage.js` 조건(plan L381-383, DD2 L121-122)은 `unexplained===0 ∧ excluded<=max ∧ digest 일치`뿐이라 greenness를 전혀 보지 않는다. |
| security | HIGH | DD3의 핵심 논증('게이트가 suite green을 요구하는 순간 redaction_ok 결합이 구조적으로 공허해진다')이 성립하지 않는다 — 게이트가 green을 요구하는 지점이 계획 어디에도 배선되지 않았으므로, 계획이 완화 대신 택했다는 '순서'는 실행되지 않는다. 축 D의 실증 명제('절단 → wiring-cut.test.js red → 전수 게이트 red', plan L212-213, L469)도 같은 미배선 위에 서 있다. | plan L170 `green이면 단언 diff 자체가 존재하지 않으므로 스캔할 실패 텍스트가 없다` — 이 전제의 소비처가 Task 5 단계 열거(plan L438-441)에 없고, run.js:205 `const ok = measuredAll && attr.ok === true`가 실패 test와 무관함. |
| security | LOW | DD4의 '잔여 유출 축을 이 milestone이 넓히지 않는다'는 거짓이다. baseline은 `paths:` 필터로 드물게 돌았지만(PRD L23 '매치가 없으면 workflow를 통째로 건너뛴다'), 신규 workflow는 `paths` 필터 없이 전 PR에서 돌며 `if: always()` artifact를 올린다 — redact.js가 스스로 열거한 미커버 축(UNC·드라이브 상대·얕은 root·POSIX 열거 누락)이 fork PR이 제어하는 test 출력과 함께 매 PR 공개 artifact로 나가는 빈도가 구조적으로 증가한다. | plan L188-189 `artifact 업로드는 유지하되(if: always()) ... 잔여 유출 축은 redact.js 헤더가 열거하고 backlog가 소유한다 — 이 milestone이 넓히지 않는다` vs plan L435 `pull_request에 paths 필터 없음`; scripts/test-suite/redact.js:132-160 `다만 전수는 아니다 ... 이 목록은 exhaustive하지 않다` (UNC·드라이브 상대·얕은 root·퍼센트 디코드 미구현). |
| test | HIGH | Task 2가 DD2가 명시적으로 철회한 `fully_skipped` 필드를 여전히 산출 계약에 넣는다 — 두 진술이 정면 충돌하고, 어느 쪽이 구현돼도 red가 되는 test가 하나도 없다. | plan DD2 L150-159: "`fully_skipped`는 이 milestone이 산출하지 않는다 — producer가 없기 때문이다 … 그래서 필드를 **만들지 않는다**" (그리고 preamble L19-20이 R2 흡수로 "`fully_skipped` 철회"를 열거). 그런데 Task 2 L379가 반환 형태에 `fully_skipped`를 그대로 열거하고 L383이 "`fully_skipped`는 별도 필드로 보고하되 `ok`에 영향을 주지 않는다(DD2)"라 적는다. Task 2 Validate의 9개 분기(L385-400) 어디에도 `fully_skipped`가 없다 — 필드를 만들든 안 만들든 test는 green이다. 이것이 DD2 자신이 금지한 "합성 fixture로만 단언되는 필드"의 반대편, 즉 아무 오라클도 없는 필드다. |
| test | HIGH | DD3(M2가 CRITICAL로 지목한 `redaction_ok` ↔ greenness 결합을 닫는 유일한 기제)이 workflow의 단계 **순서**에만 존재하는데, 그 순서를 검증하는 test가 없다. 순서가 뒤집혀도 모든 단위 test와 CI가 green이다. | plan DD3 L173: "판정 순서를 `exit_code === 0` → coverage → `redaction_ok`로 고정한다". Task 5의 Validate(L449-450)는 "PR을 열어 `test-suite` 체크가 실제로 발화하고 green임을 확인" + job 이름 확인뿐이다. Task 7의 절단 오라클은 `--assert-accounted`·`--floor-from` 두 인자의 존재만 단언한다(L466-468, Validate (a)(f)). 즉 M2 closure L154-158("red 하나가 Acceptance 조건 둘을 깬다 … M3이 이 측정을 merge-gating으로 승격시키기 전에 닫아야 한다")가 요구한 닫힘이 plan 자신이 §3.17 선례로 경계한 "기계는 만들어지고 그것을 부르는 한 줄이 빠진다" 형태로 남는다. |
| test | MEDIUM | 축 D의 CI 증거(Acceptance 3)가 run 상태만 요구하고 red의 **원인**을 절단에 귀속시키는 오라클이 없다 — plan이 스스로 mirror로 채택한 "run 성공은 증거가 아니다(수용 증거는 artifact 내용)" 원칙을 red 방향에서 위반한다. | Task 7 Validate (d) L475: "버리는 PR의 `test-suite` 체크가 red인 run URL이 문서에 기록되고, 그 PR이 **닫힘**". Acceptance 3 L731도 run URL만 요구한다. 그러나 M2 closure L46-47이 미격리 flaky(`post-edit-format-md`)를 기록하고 있어 무관한 red가 같은 체크를 붉게 만들 수 있다. plan 자신이 L228-229에서 "'절단했더니 red가 됐다'가 아니라 '절단했는데 우연히 red가 됐다'이고, 둘은 구분되지 않는다"고 쓰고도, CI 증거 축에는 artifact의 `failing`에 `wiring-cut.test.js`가 있어야 한다는 단언을 두지 않는다. Patterns to Mirror L71이 채택한 `test-suite-baseline.yml:19-25`가 정확히 그 반대를 요구한다. |
| test | MEDIUM | Task 9의 Action이 병합 대상으로 Task 0 artifact를 지목하는데, 그 artifact는 정의상 green 이전 측정이라 `redaction_ok=false`로 `--merge-into`가 거부한다 — Validate 6은 그 조합에서 만족 불가다. | Task 9 L495-497: "Task 0과 5의 Linux artifact를 … 병합한다. `--merge-into`는 `redaction_ok !== true`를 거부하므로 **Task 1이 green을 만든 뒤에만 성립한다**". Task 0(L351-359)은 정의상 Task 1 이전의 red 집합 측정이다. M2 closure L114-117이 Linux red 측정 두 원소의 `redaction_ok`가 **false**임을 실측으로 기록하고, `run.js:321-326`이 그것을 BLOCK한다. Validate(L501) "병합 후 컨테이너 전 원소가 `ok` 이고 … `redaction_ok`"는 Task 0 원소가 들어오면 성립할 수 없고, 들어오지 않으면 Action이 지시한 병합이 수행되지 않은 것이다 — 어느 쪽인지 Validate가 구분하지 못한다. |
| invariant | CRITICAL | 머지 차단 게이트의 핵심 술어인 '스위트가 green인가'가 어떤 기계에도 배선되지 않았다. 이 계획이 쓰는 러너는 red 스위트에 대해 exit 0으로 끝나도록 설계돼 있으므로, 계획대로 구현해도 전 test가 붉은 PR이 test-suite 체크 green으로 머지될 수 있다. | scripts/test-suite/run.js:10-12 — "`ok` = **측정이 성립했는가**. 스위트 green이 아니다. `exit_code`가 비영점이어도 `ok:true`일 수 있고" + run.js:725 `return result.ok ? 0 : 1;` (CLI 종료코드는 `ok`이지 `exit_code`가 아니다). 계획은 DD3(plan L173)에서 "판정 순서를 `exit_code === 0` → coverage → `redaction_ok`로 고정한다"고만 적고, `## Tasks`/`## Validation` 어디에도 `exit_code`를 검사하는 단계·플래그·도구가 없다 — Task 2의 순수 함수 계약 `computeCoverage({tracked, measurement, exclusions, floor})`(plan L378)은 exit_code를 입력으로도 `ok` 산출로도 쓰지 않고, `--assert-accounted`는 버킷 완전성만 잰다. 실패한 test 파일도 nesting-0 이벤트를 내므로 attribution='complete' → `ok:true` → exit 0 (run.js:138-139, 205)이다. |
| invariant | HIGH | DD5 축 D의 실증 논증이 위 (1)에 의존하는데 그 연결 고리가 존재하지 않는다 — "절단 → test red → 게이트 red"의 중간 단계가 성립하지 않으므로, 축 D의 버리는 PR이 red를 냈다 해도 그것이 무엇 때문에 red였는지 계획은 보증하지 못한다. | plan L213 "(`wiring-cut.test.js`가 red → 전수 게이트 red → 체크 red)" 대 run.js:10-12·725 (red test는 러너 종료코드를 바꾸지 않는다). 또한 Task 7의 절단 오라클은 `--assert-accounted`와 `--floor-from` 두 인자만 단언한다(plan L466-468) — exit_code 검사 단계와 `redaction_ok` 검사 단계는 절단 오라클의 대상이 아니므로 workflow에서 그 두 줄을 지워도 어떤 test도 붉어지지 않는다. 이는 DD5가 스스로 인용한 실패 모드("기계는 만들어지고 그것을 부르는 한 줄이 빠진다")를 이 milestone이 재현하는 것이다. |
| invariant | HIGH | 게이트 술어 `--assert-accounted`의 정의가 DD2와 Task 2에서 서로 다르며, Task 2 정의를 따르면 R2가 CRITICAL로 흡수한 두 조건(격리 파일 수 상한 · exclusions_digest 앵커링)이 CLI 종료코드에 도달하지 않는다. computeCoverage가 `ok:false`를 내도 CLI가 exit 0이 될 수 있고, Validation 분기 (4)·(9)는 `ok===false`만 단언하므로 그 괴리를 잡지 못한다. | DD2 plan L121-122: "게이트 플래그는 `--assert-accounted`이고 조건은 **셋의 논리곱**이다 — `unexplained === 0` ∧ `excluded.length <= max_excluded_files` ∧ `exclusions_digest` 일치" 대 Task 2 plan L381: "**`--assert-accounted`**(`unexplained > 0` 또는 `tracked < floor`이면 비영점 — 게이트가 부르는 것)". Validation 분기 (4)(9)는 plan L390-400에서 전부 `ok=false`로만 기술된다. |
| invariant | MEDIUM | `redaction_ok`가 머지를 차단하는지 여부가 어디에도 정해져 있지 않다. DD3은 순서와 메시지만 정하고 차단 여부를 말하지 않으며, 러너의 종료코드는 redaction과 무관하므로 이 축은 명시 단계 없이는 관측조차 되지 않는다. | run.js:568-604 — `out.ok`는 569행에서 확정되고 `out.redaction_ok`는 600행에서 별도로 계산된다(종료코드에 미반영). DD3(plan L173-175)은 "판정 순서를 …로 고정한다"와 "게이트 메시지가 …을 명시한다"만 적고 차단/비차단을 규정하지 않는다. DD4가 매 PR 공개 artifact 업로드를 유지하므로(plan L188) 이 축의 미정의는 유출 방어의 미정의다. |
| invariant | MEDIUM | Task 2가 DD2가 명시적으로 철회한 `fully_skipped` 필드를 여전히 산출하도록 지시한다 — 계획 서문이 R2 흡수 항목으로 "`fully_skipped` 철회"를 열거했음에도 Task가 갱신되지 않았다. | DD2 plan L150-159: "**`fully_skipped`는 이 milestone이 산출하지 않는다 — producer가 없기 때문이다** … 그래서 필드를 **만들지 않는다**." 대 Task 2 plan L379·L383: 반환 객체에 `fully_skipped` 포함 + "`fully_skipped`는 별도 필드로 보고하되 `ok`에 영향을 주지 않는다(DD2)". 서문 L20의 흡수 목록에도 "`fully_skipped` 철회"가 적혀 있다. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Attacked (1) every load-bearing code citation: `enumerate.js:22-50` really does expand `**` to `.*` across separators (DD2's one-line-opens-the-gate claim is true), `normalizeExclusions:55-79` really throws on empty reason and validates only {pattern, reason} (DD7's premise true), `exclusionsDigest:84-90` exists with the quoted comment (DD2 anchoring premise true), `run.js:666-667` really loads `--exclude-from` via `readJsonFile` bypassing any ticket/cap check (DD7's rewiring necessity true), `run.js:263-330 validateElement` matches the Mirror claim. (2) Fail-closed seams: `--floor-from` omission dies, `--exclude-from` omission inflates `unexplained` rather than passing, digest mismatch catches the measurement/list pairing — I could not construct a green-with-suite-erased path through the stated conditions. (3) Layering: `coverage.js` pure-layer/CLI split holds, floor+max_excluded_files placement in one file is justified (gate measures expansion, validator cannot). (4) DD7's \\"게이트가 그 경로만 읽는다\\" — technically the path is a workflow argument and `wiring-cut.test.js` asserts only the two flags, but redirecting it is itself a tracked diff, i.e. the same review control DD7 already admits is the control; not an evidenced defect. What I did find is internal: DD2's R2 retraction of `fully_skipped` and DD2's three-condition gate never propagated into Task 2, so the module contract disagrees with itself at the only site an implementer reads. |
| security | fail | 공격한 것: (1) DD2의 `**/*.test.js` 한 줄 우회 — `enumerate.js:27-50 globToRegExp`가 실제로 `**`를 `.*`로 확장함을 확인해 계획의 서술이 참임을 검증했고, `max_excluded_files` 상한이 그것을 닫는 것도 일관됨. (2) 래칫 3종(`floor` · `max_excluded_files` · `상한 상수`)이 전부 PR head에서 읽히는 tracked 파일이라 PR 작성자가 같은 diff에서 올릴 수 있다는 특권 상승 — 계획이 Risks 표(L707-708)와 DD7·DD9에서 '기계가 아니라 리뷰가 막는다'로 명시 공개하므로 finding으로 올리지 않음. (3) `exclusions_digest` 앵커링 — `enumerate.js:84-90`이 `pattern+reason`만 해싱하고 `ticket`을 뺀다는 비대칭을 찾았으나 단일 CI job 안에서 도달하는 결과가 없어 제외. (4) `--allow-codex` CI 가드(`GITHUB_ACTIONS==='true'`) 우회 — run.js 자체가 PR-controlled이므로 가드는 어차피 defense-in-depth이고 계획도 '라이브 취약점 없음'이라 적음(plan L426-427), 결과 없음. (5) `pull_request_target` 미사용·`contents: read`·SHA pin·secrets 미주입 — 실제로 fork 코드가 토큰/시크릿에 닿는 경로를 만들지 못해 결과에 도달 실패. (6) DD6 job-name drift가 CI로 강제되지 않는 점 — 계획이 UI5 근거로 명시 운영자 진단이라 적어 공개된 잔여 위험. 착지한 것은 게이트가 red를 소비하지 않는 fail-open 경로와 artifact 노출면 확대 주장의 반증뿐이다. |
| test | fail | plan 전문 + PRD + M2 closure를 읽고, Files to Change의 각 소비 모듈(`scripts/test-suite/enumerate.js` 전문, `run.js`의 `validateElement`/`REQUIRED_FIELDS`/`ok` 산출 L141-218·L262-336·L652-747, `scripts/tests/test-suite.test.js`의 exclusions 분기, 컨테이너 `2026-09-01-suite-baseline.json`의 원소 `ok` 값)을 대조했다. 공격한 주장: (1) DD2의 3조건 게이트가 Task 2 Validate 9분기로 실제로 덮이는가 — 덮인다(분기 4·6·8·9). (2) Task 0의 `ok===true` Validate가 red 스위트에서 불가능하지 않은가 — `ok`가 스위트 green이 아니라 측정 완전성이므로 성립, 내 가설을 기각했다. (3) 기존 test가 버그를 고정하고 있는가(DD7 재배선으로 red가 될 test) — `test-suite.test.js` L55-98·L456은 ticket-less 수용을 단언하지 않아 회귀 없음, 기각. (4) `exclusionsDigest`가 `ticket` 확장에도 안정적인가 — `normalizeExclusions`가 `{pattern, reason}`만 남기므로 안정, 기각. (5) `childEnv`가 Task 4 test에서 도달 가능한가 — L729 export로 가능, 기각. 남은 결함 4건은 위에 보고했다. |
| invariant | fail | DD2의 세 조건을 한 줄 glob(`**/*.test.js`)으로 여는 경로를 enumerate.js:27-50 `globToRegExp`와 :98-138 `enumerateTests`로 재추적했고 `max_excluded_files` 조건이 실제로 닫는 것을 확인했다. exclusions_digest 앵커링을 enumerate.js:84-90에서 검증했다(ticket 미포함이나 무해). 오래된 measurement를 현재 트리와 짝짓는 stale 앵커링 공격을 시도했으나 버킷이 tracked 집합 위에서 계산되므로 fail-closed였다 — 기각. Validation 검사 3의 `&& { exit 1; } \|\| true`가 `\|\| true`에 삼켜지는지 확인했으나 `exit`이 셸을 즉시 종료하므로 성립 — 기각. 검사 4의 `--apply` 실패 시 잔여 경로도 CUT_RC=0 → 비영점으로 접혀 fail-closed였다 — 기각. workflow 삭제·paths 필터 재삽입은 GitHub required check가 pending으로 남아 차단되므로 기각. 열린 것은 러너 종료코드 계약(run.js:10-12, 725)이 `ok`(측정 성립)이지 스위트 green이 아니라는 점, 그리고 그 축·`redaction_ok` 축이 절단 오라클(Task 7)의 사거리 밖이라는 점이다. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 232086,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:58ca81b0f1ff84fe54f4d42ffd0c0af22a563558e381f5aca99d54e09f48fda3",
  "plan_path": ".claude/plans/ci-full-suite-m3b.plan.md",
  "recorded_at": "2026-09-04T02:12:31.410Z"
}
```
