# Plan Review Panel — orchestrator-step-wiring-m2

**Plan**: `.claude/plans/orchestrator-step-wiring-m2.plan.md` · **Plan version**: `sha256:61fe03997fe8beade6c171a863bf3aec8f78d6d32e60449c6fff7b889c34c15b`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 8 blocking finding(s): architect/HIGH, architect/FAIL, security/HIGH, security/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | DD2의 work_unit 해소 순서가 자신이 세운 불변식("추정으로 채우지 않고 모르면 키를 비운다")을 담지 못한다. 2순위인 STATE.md `task_fingerprint`는 부재하지 않고 리터럴 sentinel `'unknown'`으로 존재하므로, 아직 fingerprint가 세팅되지 않은 STATE.md에서 recorder는 work_unit=`'unknown'`을 봉인한다 — DD2가 `'default'`를 거부한 바로 그 "A1 corpus와 조인되지 않는 쓰레기 키"다. 게다가 Task 5(5)가 지정한 test는 "둘 다 없을 때 키 부재"를 단언하는데, `task_fingerprint`는 `state-injector.js:23`의 REQUIRED_FRONTMATTER_KEYS라 실제 STATE.md에서는 결코 부재하지 않는다 — 실서비스에서 발생하는 유일한 경로를 test가 구조적으로 못 본다. | plugins/mccp/scripts/state/state-writer.js:120 `task_fingerprint: 'unknown',` (emptyState 기본값) · plugins/mccp/scripts/state/state-injector.js:23 REQUIRED_FRONTMATTER_KEYS에 `task_fingerprint` 포함 · plan DD2 "해소 순서는 `--work-unit` 명시 → STATE.md `task_fingerprint` → `null`" + Task 5 "(5) DD2 — … 둘 다 없을 때 키 부재" |
| architect | MEDIUM | Task 4가 선언한 "worktree 수 상한과 timeout 은 `worktrees.js` 의 값을 그대로 쓴다(새 상수 도입 금지)"는 실행 불가능하다. `SCAN_TIMEOUT_MS`(3000) · `DEFAULT_CAP`(20) · `parseCap`은 전부 module-private이고 `module.exports`에 없다. 따라서 구현은 리터럴 복제(= 금지된 새 상수, drift 발생) 또는 `worktrees.js` export 수정 중 하나를 해야 하는데 후자는 Files to Change에 없다. 재사용을 주장하는 추상화가 실제로는 도달 불가한 값을 가리킨다. | plugins/mccp/scripts/derive/sources/worktrees.js:27-28 (`SCAN_TIMEOUT_MS`/`DEFAULT_CAP` 모듈 지역 상수) · :427-433 `module.exports = { scanWorktrees, parseWorktreePorcelain, deriveWorktreeProgress, isSelfWorktree, normalizeWorktreePath }` · plan Files to Change 표에 `derive/sources/worktrees.js` 없음 |
| architect | MEDIUM | `last-halt`가 cap을 차용하면 UI4가 요구한 "저장소 전체에서 가장 최근 halt 1건"이 조용히 틀린 답이 될 수 있는데, Task 4(4)의 실패 열거에 truncation이 없다. worktrees.js의 cap은 대시보드 나열용이라 절삭돼도 행이 빠질 뿐이지만, 전역 최댓값 선택에서는 절삭이 빈 stdout이 아니라 **다른 worktree의 오래된 halt**를 정답인 양 출력한다. | plugins/mccp/scripts/derive/sources/worktrees.js:354-369 (cap 초과 시 `parsed = parsedAll.slice(0, cap)` + stderr 경고) · plan Task 4 "4. 항목 부재 · `chain_progress` 부재 · JSON 파손 · STATE.md 부재 · git 실패 · 타임아웃은 전부 빈 stdout + exit 0" (truncation 미열거) |
| architect | MEDIUM | DD3의 커버리지 분모(사이트 표)와 plan이 되풀이하는 숫자가 서로 어긋난다. Task 6 표의 `enforcement: shell` 행은 11개(3.preflight · 3.route.fleet · 3.route.single · 3.gate.no-return · 3.gate.args · 3.gate.verdict · 3.wp.no-return · 3.wp.collect · 3.wp.verdict · 3.merge · 3.verify)이고 실측 비영점 exit도 11건인데, Task 7은 "shell 10건"·"12개 halt 지점", Acceptance는 "shell 행 10/10"이라 적는다. Task 8(d)가 양방향 일치를 강제하는 지표의 분모가 plan 자신 안에서 두 값을 갖는다. | plan Task 6 표 13행(prose 2 + shell 11) vs Task 7 제목 "12개 halt 지점 배선" · Task 7 "shell 10건은 각 종료 앞에 한 줄" · Acceptance "사이트 표의 `shell` 행 10/10" · 실측 plugins/mccp/commands/work.md 비영점 exit 11건(:210, :495, :553, :631, :643, :659, :716, :724, :743, :756, :825) |
| architect | LOW | UI5 pin의 앵커 줄 번호가 실제 파일과 어긋난다. plan은 `:224`의 `rm -f` 목록과 `:715`의 `-d "$GITDIR/dispatch-fleet-results"` 검사, `3.wp.no-return`이 `:713-717`이라 적지만 실제는 `rm -f` = :223, `[ ! -d …dispatch-fleet-results ]` = :713, `exit 13` = :716이다. Task 8(f)가 리터럴 pin이라 기능적으로는 견디지만, UI5 경계 논증 전체가 잘못된 좌표 위에 서 있다. | plugins/mccp/commands/work.md:223 `rm -f "$GITDIR/dispatch-fleet-args.json" …` · :713 `if [ -f "$GITDIR/dispatch-workflow-started.json" ] && [ ! -d "$GITDIR/dispatch-fleet-results" ]; then` · plan DD11 "`:713-717` 로 UI5 가 지목한 `:715` 와 같은 if 블록 안" |
| security | HIGH | Validation 7 does not demonstrate fail-open at all, and the recorder it describes has no repo-root containment guard — running `record-halt` from a non-repo cwd silently CREATES `<cwd>/.claude/state/STATE.md` and exits 0 for the ordinary success reason. The plan's own Acceptance line ("UI2 (fail-open): Validation 7 이 기록 불가 상황에서 recorder 의 exit 0 + 비어있지 않은 stderr 를 실증") is therefore false evidence for the milestone's central safety invariant, and the sibling CLI the plan claims to mirror added exactly this guard. | plan Validation 7 spawns the CLI with `{cwd:os.tmpdir()}`; work-orchestrator.js:252-261 `findRepoRoot` returns `cwd` when no `.git` ancestor exists (`return cwd \|\| process.cwd();` :260), and auto-chain.js:249-252 then does `fs.mkdirSync(path.join(repoRoot,'.claude','state'),{recursive:true})` / state-writer writes STATE.md there. Contrast msw-metrics/cli.js:308-314 — "security review S5 — argv를 무검증으로 경로 조립에 넘기지 않는다" + `.claude`/`.git` marker check + refusal. Task 3/DD6 specify no such marker check. |
| security | MEDIUM | The `--reason` value is persisted verbatim (only CR/LF-folded and 200-char capped) into `chain_progress` inside git-tracked STATE.md, with no `mask.scrubAbsPaths`, while the plan requires abs-path scrubbing only for the ephemeral stderr/reader path. This is the same durable-artifact absolute-path leak class that already forced a sanctioned re-seal (§3.12 `meta.cwd`), and the halt messages the wiring will quote already interpolate absolute paths. | plan DD7 ("`--reason` 은 `oneLineExcerpt`(CR/LF 접기 + 200자)로 좁힌다") and Task 4(4) apply `scrubAbsPaths` only to reader stderr; CLAUDE.md §3.2 states STATE.md is git-tracked; work.md:494 halt message body is `"  (Token kept at $GITDIR/dispatch-fleet-reservation.json ..."` where `$GITDIR=$(git rev-parse --git-path mccp/tmp)` resolves to an absolute path inside a worktree. The prior fan-out raised precisely this gap (plan line 461: "mask.scrubAbsPaths ... is not mentioned for chain_progress entries") and no Design Decision answers it. |
| security | LOW | The coverage metric DD3 rests on a denominator the plan itself contradicts: Task 6's table enumerates 11 `enforcement: shell` sites, but Task 7 says "shell 10건"/"12개 halt 지점" and Acceptance says "shell 행 10/10". A bidirectional table↔wiring test written to a 10-site expectation cannot be green against an 11-row table, and the "100% coverage" claim is unverified. | plan Task 6 table rows: 3.preflight, 3.route.fleet, 3.route.single, 3.gate.no-return, 3.gate.args, 3.gate.verdict, 3.wp.no-return, 3.wp.collect, 3.wp.verdict, 3.merge, 3.verify = 11; plan Task 7 "shell 10건은 각 종료 앞에 한 줄" and Acceptance "사이트 표의 `shell` 행 10/10". work.md actually has 11 non-zero exits at :210, :495, :553, :631, :643, :659, :716, :724, :743, :756, :825. |
| test | HIGH | 지표 5(소비 지점 노출 = 배너)는 이 plan 안에서 반증 불가하다. Task 9가 work.md에 넣는 `last-halt` 배너 줄을 강제하는 test가 하나도 없다 — Task 8의 정적 test 6개 단언 (a)~(f)는 전부 `record-halt`(producer 배선)와 UI5 pin만 다루고 `last-halt`를 한 번도 언급하지 않는다. Task 9의 Validate는 '그 블록을 그대로(verbatim) 실행해 출력 확인'이라는 사람 눈 확인이다. 즉 배너 줄이 이후 편집으로 사라져도 red가 되는 것이 없다. 이는 plan 자신의 명제('산문 지시가 아니라 정적 test가 강제하는 배선', Summary L12-13)를 그 자신이 지키지 않는 지점이다. 실측으로도 M1이 넣은 A1 배너를 pin하는 test는 존재하지 않는다 — `plugins/mccp/scripts/` 전체에서 `commands/work.md`를 리터럴로 참조하는 곳은 `command-body/debt.js:44,45`(비결속 메타데이터 line)와 env registry 문자열뿐이라, 선례 역시 미보호다. | plan L253-264 (Task 8 단언 (a)~(f) 전문에 `last-halt` 0회) vs L285-286 "**Validate**: 그 블록을 그대로(verbatim) 실행해 출력 확인" · L402 "- [ ] **지표 5 (소비 지점)**: `/mccp:work` 진입에 halt 줄이 A1 줄과 함께 표시된다" · 실측 plugins/mccp/scripts/lib/command-body/debt.js:44 (scripts/ 전체에서 work.md를 참조하는 유일 지점이며 배너와 무관) |
| test | MEDIUM | 지표 4 커버리지 분자/분모 숫자가 plan 자신의 표와 어긋난다. Task 6 표의 `enforcement: shell` 행은 11개(`3.preflight`·`3.route.fleet`·`3.route.single`·`3.gate.no-return`·`3.gate.args`·`3.gate.verdict`·`3.wp.no-return`·`3.wp.collect`·`3.wp.verdict`·`3.merge`·`3.verify`)인데, plan은 네 곳에서 'shell 10건'/'10개 fenced'/'10/10'/'12개 halt 지점'이라고 적는다. 실측 `work.md`의 비영점 exit도 11건(:210,:495,:553,:631,:643,:659,:716,:724,:743,:756,:825)으로 표와 일치하고 10이 아니다. Acceptance 체크박스가 '10/10'을 요구하므로, 그 숫자를 문자 그대로 만족시키면 shell 사이트 1건이 미배선인 채 acceptance가 green으로 읽힌다(Task 8(d)의 양방향 단언이 red로 잡아 주긴 하나, 그 순간 Acceptance 문구와 test가 서로 모순된다). | plan L217-231 (표: shell 행 11개) vs L139 "10개 fenced halt 배선 + 산문 2건" · L236 "### Task 7: `work.md` — 12개 halt 지점 배선" · L237 "shell 10건은" · L401 "`shell` 행 10/10 이 배선되고"; 실측 grep `exit [1-9]` on plugins/mccp/commands/work.md → 11 hits (:210,:495,:553,:631,:643,:659,:716,:724,:743,:756,:825) |
| test | MEDIUM | Validation 6·8은 구조적으로 실패할 수 없는 '검증'인데 Acceptance가 'Validation 1~9 전부 통과'를 판정 기준으로 삼는다. Validation 6은 `record-halt`/`last-halt`를 부르지만 두 서브커맨드는 DD6/Task 4(4)에 의해 **어떤 실패에도 exit 0 + 빈 stdout**이 계약이라, 프로듀서·리더가 완전히 망가져도 이 줄은 통과한다(출력 대조 단언이 없다). Validation 8은 `console.log`로 개수만 찍고 기대값(10 또는 11)과 비교하지도, 비영점 종료하지도 않는다 — '지표 4 커버리지 실측'이라는 이름표가 붙어 있으나 어떤 회귀도 잡지 못한다. | plan L334-337 (Validation 6: 두 CLI 호출, 출력 단언 없음) · L196-197 "전부 **빈 stdout + exit 0**" · L349-360 (Validation 8: `console.log("nonzero exits="+exits+" record-halt calls="+recs)` 뿐, assert/process.exit 없음) · L399 "- [ ] Validation 1~9 전부 통과" |
| invariant | HIGH | 커버리지 게이트의 분모가 plan 자신의 표와 어긋난다 — 표는 shell halt 사이트를 11건 열거하는데 배선 지시·정적 test·Acceptance(지표 4)는 전부 '10'이라 적는다. 실측 work.md의 비영점 exit도 11건이다. 지표 4가 '10/10 = 100%'로 green이 되면서 한 사이트가 미배선인 상태가 성립할 수 있고, 그 상태를 잡을 기계가 없다(Task 8(d)는 표 ↔ 배선의 양방향 일치만 보므로, 표를 10행으로 줄이면 조용히 통과한다). | plan Task 6 표 :219-231 은 prose 2 + shell 11(3.preflight·3.route.fleet·3.route.single·3.gate.no-return·3.gate.args·3.gate.verdict·3.wp.no-return·3.wp.collect·3.wp.verdict·3.merge·3.verify)을 열거하나, Task 7 제목 '12개 halt 지점 배선'·Task 7 'shell 10건은'·Files to Change '10개 fenced halt 배선'·Acceptance '지표 4 … shell 행 10/10'. 실측: plugins/mccp/commands/work.md 의 비영점 exit 은 :210,:495,:553,:631,:643,:659,:716,:724,:743,:756,:825 로 11건. |
| invariant | MEDIUM | `last-halt` 레코드에 해소(resolved) 개념도 보존 상한도 없어, 한 번 halt가 기록되면 그 줄이 이후 모든 `/mccp:work` 진입에 무기한 표시된다. Task 9가 '조용한 것이 맞다'고 전제한 '평소 미표시'는 최초 halt 이후로는 성립하지 않으며, 배너는 이미 복구된 과거 halt를 '직전 halt'로 계속 주장한다. | plan Task 4 (2) 'status === "halted" 항목을 모으고 ts 기준 전역 최신 1건을 고른다' + Task 9 'halt 없음 → 줄을 아예 내지 않는다' + DD10 '보존 상한을 두지 않는다'. 코드상 chain_progress는 append-only(state-writer.js:730-735 `log.steps.push`)이고 해소 상태를 쓰는 경로가 plan 어디에도 없다. |
| invariant | MEDIUM | Validation 6이 개발 저장소의 git-tracked STATE.md에 실제 halt 레코드(`--reason "validation smoke"`)를 쓰는데 정리 단계가 없다. 합성 레코드가 실제 halt와 구분 불가한 형태로 corpus에 남아 커밋되고, 해소 개념이 없으므로(위 finding) 이후 배너의 '직전 halt'로 고착된다 — 지표 4의 관측 corpus를 계측 스모크가 오염시킨다. | plan Validation 블록 6번: `node plugins/mccp/scripts/lib/work-orchestrator.js record-halt --step implement --site 3.preflight --reason "validation smoke"` 뒤에 되돌리는 단계 없음. chain_progress는 STATE.md frontmatter이고 §3.2대로 git-tracked. |
| invariant | LOW | UI5 pin의 인용 라인이 실제와 어긋난다 — plan이 pin 대상으로 지목한 `:715` 는 주석 줄이고 `-d "$GITDIR/dispatch-fleet-results"` 검사는 `:713` 이다. pin 자체는 리터럴 기반이라 기능상 치명적이지 않으나, UI5 경계를 라인 번호로 서술한 문장이 이미 부정확하고 recorder 삽입이 그 이하 라인을 전부 밀어낸다. | plan Task 8(f) '`:715` 의 `dispatch-fleet-results` 디렉토리 검사를 리터럴로 단언'. 실측 plugins/mccp/commands/work.md:713 `if [ -f "$GITDIR/dispatch-workflow-started.json" ] && [ ! -d "$GITDIR/dispatch-fleet-results" ]; then`, :715 는 `# write fix-task.md; cleanup(resumeFromRunId 재개) 지시`. |
| invariant | LOW | Validation 8('지표 4 커버리지 실측')은 exit/record-halt 개수를 출력만 하고 비교·비영점 종료를 하지 않아 판정 능력이 없다 — 커버리지를 '실측'한다고 적힌 스텝이 어떤 불일치에도 통과한다. | plan Validation 8: `console.log("nonzero exits="+exits+" record-halt calls="+recs);` 뿐이며 단언·process.exit 없음. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | (1) 인용 검증: state-writer.js:719-741 recordChainProgress·:677-687 throw 경로·auto-chain.js:240-254 침묵 catch·:152 chain_progress 미소비 주장 — 전부 실제와 일치(정확). (2) \\"소비자 0건\\" 주장을 grep으로 재확인 — 참. (3) worktrees.js 재사용 주장(파서/argv/scrub/cap/timeout)을 export 목록과 대조 — parseWorktreePorcelain·parseStateMd는 도달 가능하나 cap/timeout은 도달 불가(발견). (4) work.md의 비영점 exit 전수를 열거해 Task 6 표와 대조 — 개수 불일치(발견). (5) DD2 해소 순서를 emptyState 기본값과 대조 — 'unknown' sentinel 누락(발견). (6) DD5/DD6 fail-open 구조(try/catch + 명시 exit + `\|\| true` 배치)와 인라인 `\|\| { …; exit 13; }` 3건(:643/:724/:756)의 배선 가능성 — 결함 못 찾음. (7) DD8 단일 CLI 표면 주장을 work.md의 실제 호출(:90 classify, :204 next-step)로 확인 — 참. (8) DD9(신규 토글 0)·DD10(MAX_LINE_BYTES 무손실 계약) — 결함 못 찾음. (9) UI5 앵커 좌표 대조 — off-by-one/two(발견, LOW). |
| security | fail | Attacked: (1) trust boundary of `record-halt` inputs — enum/regex/oneLineExcerpt in DD7 do block YAML/frontmatter injection (JSON.stringify at state-writer.js:730-739 plus CR/LF folding), and `--site`'s regex permits `..` but the value never reaches a path, so I could not land traversal; (2) the reader's `git worktree list --porcelain` traversal — fixed argv, pinned cwd, timeout, stdio locked, and worktrees.js:336/344 scrub errors, boundary limited to git's own list, so I could not land cross-repo inclusion; (3) `worktree=<basename>` banner field — basename only, no machine/user path exposure; (4) concurrent writers — each worktree has its own STATE.md and writes go through withStateLock, so no cross-worktree clobber; (5) the DD6 throw path claim — verified real at state-writer.js:677-691, and the plan's CLI-level try/catch is the right shape. Landed: no containment guard on the recorder's repo-root resolution combined with a vacuous fail-open validation, unscrubbed reason text reaching a git-tracked artifact, and an internally inconsistent shell-site count. |
| test | fail | 인용 검증: state-writer.js:719-741(recordChainProgress 4필드 push)·:677-691(실제 throw 경로) 확인 — DD6/Task 1 인용 정확. auto-chain.js:240-254 침묵 catch 확인 — Task 2 정확. worktrees.js `parseWorktreePorcelain`/`parseStateMd` 실제 export 확인(:37,:179,:427-429) — Task 4 mirror 유효. 그 다음 각 Task의 Validate 줄이 실제로 그 Task가 바꾸는 것을 돌리는지 대조했고, Task 9(배너)만 자동 test가 없음을 확인(Task 8 단언 6개에 `last-halt` 0회, scripts/ 전체에 work.md 배너 pin 부재). Task 6 표의 shell 행을 세고 work.md의 비영점 exit 11건을 실측해 plan의 '10' 표기와 어긋남을 확인. Validation 6·8이 실패 가능한 단언을 갖는지 확인 — 둘 다 없음. debt.js 라인 이동으로 command-body lint가 깨지는지 공격했으나 헤더(:12-16)가 `line`을 비결속 메타데이터로 명시하고 매칭은 textDigest라 무해 — 지적하지 않았다. DD5/Task 8(c), DD7 입력 좁힘, DD2 work_unit 해소 순서는 Task 5가 각각 단언 항목을 갖고 있어 반증 실패. |
| invariant | fail | work.md의 비영점 exit 전수를 grep으로 세어 plan의 halt 사이트 표·배선 개수·Acceptance 분모와 대조(11 vs 10 불일치 확인). auto-chain.js:240-254 침묵 catch, state-writer.js:677-691 throw 경로, recordChainProgress:719-741 present-only 삽입 지점 인용이 실제 코드와 일치하는지 확인(전부 정확). DD5/Task 8(a)(c)를 :643/:724/:756 인라인 `\|\| { …; exit 13; }` 형태에 대입해 recorder 삽입이 exit를 삼키는 경로가 생기는지 추적(생기지 않음 — 뒤에 명시 exit가 남음). fail-open 축(CLI 전체 try/catch + exit 0)이 게이트 판정을 완화하는 지점이 있는지 확인(recorder는 관측 전용, 게이트 분기 미변경 — 결함 없음). UI5 pin의 라인 인용을 실측 대조(:713 vs :715 어긋남). reader의 미지정 입력(STATE.md 부재·JSON 파손·git 실패)은 전부 빈 stdout+exit 0으로 명시돼 있어 승인으로 끝나는 분기는 없었으나, 해소/보존 축(stale halt 무기한 표시)과 스모크 레코드 정리 부재는 plan에 답이 없었다. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 69861120,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:61fe03997fe8beade6c171a863bf3aec8f78d6d32e60449c6fff7b889c34c15b",
  "plan_path": ".claude/plans/orchestrator-step-wiring-m2.plan.md",
  "recorded_at": "2026-09-03T04:21:29.975Z"
}
```
