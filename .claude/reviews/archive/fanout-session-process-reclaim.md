# Phase 2.5 Multi-Perspective Fan-out — session-process-reclaim (M1+M2 plan)

> 이 기록의 심사 대상은 `.claude/plans/session-process-reclaim.plan.md`다.
> `/mccp:plan` Phase 2.5가 자동 생성했고 verbatim이다. followup plan
> (`session-process-reclaim-followup.plan.md`) 본문에 실려 있었으나, findings의 줄 번호·Task
> 번호·섹션 참조가 전부 위 원본 문서를 가리켜 followup을 읽는 리뷰어가 존재하지 않는 앵커를
> 대조하게 되므로 여기로 귀속을 정정해 옮겼다. 삭제가 아니다.

## Multi-Perspective Fan-out

<!-- Auto-injected by /mccp:plan Phase 2.5 fan-out (read-only). -->

**Coverage**: 4/4 perspectives (architect, security, test, explorer) · spent ~66k.

### Findings (severity-ranked)

- **[MEDIUM][architect]** probeProcess specification is scattered across three locations (D15, Task 2 Action, Task 8) without a clear architectural home; implementer must stitch fragments together — Plan D15 lines 125–162 describe OS logic, Task 2 Action line 383 describes function signature + injection requirement, Task 8 case 9d line 502 tests it as uninjectable. R6 Rejected Findings acknowledges 'prior plan did not list probeProcess in Files to Change' but plan still treats it as embedded in Task 2 rather than standalone infrastructure.
- **[MEDIUM][architect]** Task 2 Validate does not foreground that probeProcess is MANDATORY for D15 verification; narrative buries upgrade in Rejected Findings — Task 2 Action line 383 says 'probeProcess ... is this Task that product'. But the critical R6 security upgrade that makes it non-optional (can't test Critical risk without it) is in Rejected Findings §R6 test CRITICAL, not in the main narrative. Reader sees description but may not grasp non-negotiability.
- **[MEDIUM][architect]** TOCTOU residual acknowledged but relies on timing assumptions (500ms win32 / 1500ms POSIX) that could fail on slow systems or under high OS contention — D11 lines 223–235 explicitly states 'whole global lock without which 0 is not achievable' and 'checked but explicit'. D15 §D15 table lines 152–155 bind tolerance to platform. If real probe latency or spawn-to-probe delta exceeds tolerance, `identity_match` fails silently (register succeeds but reclaim is skipped). Plan correctly documents the residual but the architecture has no mechanism to detect if tolerance is insufficient.
- **[MEDIUM][architect]** Registry growth depends on SessionStart always executing; if sessions are killed (SIGKILL) before SessionEnd hook runs, stale records accumulate unbounded — D14 task 10 lines 555–566 describe SessionStart cleanup as orphan detection only, but architecturally it's the **only** mechanism that purges dead session directories. D9 lines 211–213 show SessionEnd is best-effort and budget-bounded. If administrative kill bypasses hooks repeatedly, `.claude/state/session-processes/<old-sid>/` directories survive and grow indefinitely.
- **[MEDIUM][security]** TOCTOU window between PID liveness check and kill allows recycled PIDs to be claimed by unrelated processes under tight timing conditions — Plan §D11 documents this residual risk: 'kill 直前 再評価' mitigates but doesn't eliminate the window. The sequence is (1) check isPidAlive at line 898+ area, (2) kill at actual kill site in reclaimSession. Between steps, OS could recycle the PID. The 500ms/1500ms identity tolerance (§D15) provides a defense, but a process starting exactly within that window with matching command line fragment could theoretically be killed.
- **[MEDIUM][security]** probeProcess timeout (5s on win32, 2s on POSIX) combined with multiple records could cause SessionEnd hook to exceed its 10s budget, leaving .unreclaimed.json records stranded — Line 80-82 sets PROBE_TIMEOUT_WIN32_MS=5000 and PROBE_TIMEOUT_POSIX_MS=2000. Line 93 sets MAX_BUDGET_MS=9000 (under 10s hook timeout). If multiple records each require probe timeouts stacked sequentially, the hook could timeout before writeFailures are recorded, destroying audit evidence. Lines 1125-1150+ show loop doesn't parallelize probes.
- **[MEDIUM][security]** realpath.native fallback to path.resolve doesn't follow symlinks, creating inconsistent behavior in cross_repo check at line 877-878 — Lines 846-850: 'try { return fs.realpathSync.native(abs) } catch (_) { return abs }'. The fallback path.resolve doesn't follow symlinks. An attacker placing a symlink in the path could cause the fallback to return a different canonical form, potentially bypassing 'cross_repo' ownership check at line 877 where both sides use canonicalPath().
- **[MEDIUM][test]** Task 8 case 9d (실물 probe): POSIX skip condition is incomplete - no specification for timeout vs access-denied behaviors — Plan Task 8 L510-514: 'win32에서는 skip하지 않는다' (hard fail required) but POSIX 'etimes 미지원이면 t.skip' OR 'probe 타임아웃(2s)은 fail'. However, these are presented as alternatives without clarity on what happens when etimes IS supported but probe times out due to system load. PRD Critical scenario (PID reuse by unrelated process) is only testable on win32 if POSIX path can silently skip. No specification of acceptable POSIX timeout rates for CI.
- **[MEDIUM][test]** Task 9(a3) spawn site inventory regex has acknowledged false-negative potential but no baseline verification — Plan Task 9(a3) L541: glob + regex pattern `/\b(spawn|spawnSync|...)\s*\(/` will miss dynamic patterns like `const fn=spawn; fn(...)`. Plan L544 states 'false-negative 율을 0으로 주장하지 않는다' and 'false-negative 축을 닫는다: 그물을 넓히고 전수 등재를 요구'. But Task 9 Validate never shows a **baseline scan of current codebase** to verify pattern coverage. Without proof that pattern catches all 3 existing detached spawns, Task 9(a1) comparison is untested for false negatives.
- **[MEDIUM][test]** Task 1 Validate (0) gitignore precondition runs before any registry state is created, creating an untestable window — Plan Task 1 Action (0) L339-342: '.gitignore 항목이 Task 1보다 먼저 착지해야 한다' with 'git check-ignore -q .claude/state/session-processes/x.json' failing if gitignore is missing. But this assertion runs in test, not in actual implementation order. If test passes (gitignore exists), the **precondition is satisfied**. However, if gitignore is added **after** first registry write in implementation, the test will still pass because the check-ignore happens at test-time, not at register()-time. No runtime guard in register() to verify gitignore coverage.
- **[MEDIUM][test]** Task 11: version bump and CHANGELOG synchronization is process-level concern, not test-level — Plan Task 11 Action L570: 'plugin.json 1.23.7 → 1.24.0' and 'renderer/html.js page-foot + renderer/markdown.js derived 줄 동기' with note 'version 충돌을 다루기 전에' (§3.7 체크리스트 4번). But Task 11 Validate L571 only shows 'node --test plugins/mccp/scripts/lib/renderer/tests/i18n-surface.test.js'. No assertion in test that version string is consistent across 5 surfaces (plugin.json, html.js, markdown.js, footer literal, CHANGELOG). Relies on human checklist, not mechanical validation.
- **[MEDIUM][explorer]** Cross-repo detection relies on `fs.realpathSync.native` — undocumented symlink resolution variant used nowhere else in visible codebase — Plan Task 1 action and D4 repeatedly reference `fs.realpathSync.native` for repo-root normalization to catch symlink-based path escapes. Grep of codebase finds only `fs.realpathSync` (without `.native`) in 0 files, and `.native` variant appears in 0 files. Pattern is security-critical for D4's `cross_repo` verdict but relies on Node.js API not previously used in this repo
- **[MEDIUM][explorer]** PID probing (`probeProcess`) is entirely new platform-specific implementation; not reusable from existing code — Plan Task 2 and D15 require win32 `Get-CimInstance Win32_Process` (3 fields piped as single line) and POSIX `ps -o etimes=,comm=,args=` parsing. No existing PID probe utility in codebase. Grep for 'probeProcess' returns only `session-ledger.test.js` which doesn't implement it. Task 2 designates this as must-export from session-processes.js
- **[MEDIUM][explorer]** Dashboard server's existing reuse detection (`isReusablePid`) does NOT perform PID identity verification; plan adds that requirement for session-processes reclamation — dashboard-server.js:112-120 checks 4 AND conditions (host, alive, repoRoot, statusPath) but **never checks** elapsed time or command-line match. Plan D15/Task 2 require both for PID-reuse defense (CRITICAL per PRD). The two implementations solve different problems (same-session reuse vs cross-session kill safety) — no duplication risk but distinct threat models
- **[MEDIUM][explorer]** Plan's test strategy for 'no accidental kill' (Task 8 case 9d) requires actual OS `probeProcess` call, not mock-injectable; this breaks test-only injection if implemented incorrectly — Plan Task 8 lines 501-514 explicitly rejects injectable mock for case 9d. Lines 503-506 show direct module call `const { probeProcess } = require('../session-processes'); const p = probeProcess(process.pid);` with comment '主入点 없음' (no injection point). Rejected Findings R10 confirms that if hooked through `reclaimSession` call path only, mock would pass test despite broken probe
- **[MEDIUM][explorer]** Gitignore entry must be added BEFORE Task 1 test runs; plan correctly places this at Task 1 start (line 327-342) but marks it non-negotiable via test assertion — Plan Task 1 action (0) states: 'This order is enforced by Validate unit assertion (0)' — `git check-ignore -q .claude/state/session-processes/x.json` must exit 0. If gitignore entry is added AFTER Task 1 runs, intermediate commits could stage untracked registry files. R12 CRITICAL/HIGH finding explicitly enforced this via Validate gate, not prose
- **[LOW][architect]** Concurrent session reuse registration has implicit ordering: session B reads A's `.pid` file to detect reuse, but A's register() and B's reuse register() can interleave without explicit synchronization — D7 line 191 and Task 3 line 405 describe reuse as 'separate record' in B's directory, avoiding lock on A's record. But D3 explicitly states 'no cross-PID unlink in register path' — the window where B reads A's pid file (between A's fs.writeFileSync and A's path validation) is unguarded. Mitigation is file atomicity + separate dirs, but ordering is implicit not mechanical.
- **[LOW][architect]** Idempotency contract for reclaimSession hook re-entry not explicitly stated; second SessionEnd invocation behavior undefined — D9 lines 197–213 describe hook as try/catch but don't guarantee idempotency. If hook is re-invoked after recovery, already-unlinked records won't cause error (unregister is best-effort) but state is not formally idempotent. Plan should state: second reclaim finds records already gone or .unreclaimed.json present, is benign.
- **[LOW][architect]** session_pid:null degradation introduces new pattern (absence = downgrade) not seen in broader codebase; cognitive load for maintainers — Task 1 schema line 352 allows 'integer > 0 **or** null'; evidence-claim.js lines 80–87 perform downgrade. Broader codebase patterns (receipt schema, evidence-lock) treat absence as error, not as valid degraded state. This is justifiable (§D4 pseudocode) but adds a new semantic.
- **[LOW][security]** Win32 probeProcess output parsing doesn't validate field encoding or maximum lengths, creating potential field-shift vulnerability if PowerShell output is corrupted — Line 795-796: output is split on newlines, filtered, then rejoined with spaces without validating that exactly one line was parsed. Lines 740-754 parseWin32ProbeLine() relies on pipe delimiter '|' to separate three fields. If PowerShell's output contains unexpected encoding or malformed delimiters, the parser could misalign fields (e.g., treating command line as execImage). The delimiter choice (pipe is forbidden in NTFS) is safe, but field validation is absent.
- **[LOW][security]** Test coverage gap: no test cases for malicious or special-character-containing command lines in Win32 probeProcess output — Tests 9f-9g in session-processes-reclaim.test.js cover empty fields and normal cases, but lack tests for: (1) command lines with `|` delimiters embedded (e.g., path containing pipe in UNC names, though forbidden in NTFS), (2) extremely long command lines that could trigger regex issues in tokenizeCommandLine, (3) null bytes or control characters that could disrupt parsing. The win32 field splitter uses indexOf() with no bounds checking.
- **[LOW][security]** No explicit validation that execImage field returned from probeProcess is safe for path operations — Line 752: execImage is parsed but not validated for length or content before being returned. Later, line 959 checks isNodeInterpreterImage() but doesn't validate the string length or check for traversal sequences like `..|` that could theoretically appear in corrupted probe output.
- **[LOW][security]** Missing audit trail if probeProcess succeeds but its output parsing is malformed — silent null return looks the same as liveness check failure — Line 821-823: probeProcess returns null on ANY exception, including parsing failures. This conflates 'platform doesn't support probing' with 'probeProcess encountered corrupted output'. The caller treats both as identity_unverifiable (fail-closed), but no distinction is recorded. If a future OS returns unexpected output format, silent failures accumulate.
- **[LOW][test]** Task 2 케이스 7: MCCP_RECLAIM_IDENTITY_TOLERANCE_MS downward rejection test doesn't specify loudness of warning — Plan Task 2 L401-402: test케이스 7 단언은 하향(0·100·-5·abc)을 넣어도 유효 허용치가 플랫폼 기본값 미만으로 내려가지 않음을 단언. Plan L401 says '하향 시도는 loud stderr warn을 남긴다' but test Validate L389 케이스 7은 'MCCP_RECLAIM_IDENTITY_TOLERANCE_MS 하향 거부' 단언 수준으로만 나열. No specific assertion that stderr contains warning or that failure path differs from silent ignore.
- **[LOW][test]** Task 3: session ID mutation mechanism for reuse test is not specified in test structure — Plan Task 3 Validate L408-411: test должен 'ub2e4른 세션 id 아래에서' but references existing test at L165-179 which does NOT show how to change session ID between startServer calls. Plan acknowledges existing test pattern reuse but doesn't show the mechanics of CLAUDE_CODE_SESSION_ID mutation within a single test case. If test relies on env mutation, that's not shown in the Validate section.
- **[LOW][test]** Task 7 exception path stderr capture: assertion doesn't verify error message content, only that stderr is captured — Plan Task 7 Validate L484-485: 단언 (a) 'reclaimSession이 throw해도 run()이 입력을 그대로 반환하고...캡처한 stderr에 threw와 그 오류 메시지가 나타남을 단언한다'. But the test pseudocode (L453-465) shows stub returning specific fields, not throwing. A stub that throws but with empty message could pass 'stderr contains threw' without verifying actual error details propagate.
- **[LOW][test]** Task 1 Validate assertion 5: existing-file check (exec_path) is overspecified for handoff-session kind — Plan Task 1 Validate L377 단언 5: '자기등록 3 호출부 한정. handoff는 이 단언 대상이 아니고 회수 대상도 아니다'. But test pseudocode doesn't show conditional skip for handoff records. If test runs assertion 5 on a handoff record with exec_path='powershell.exe' (not an actual file), the test will fail even though the plan correctly excludes handoff from exec_path validation.
- **[LOW][test]** Task 8 case 11b: real-time budget measurement has no specified tolerance for system load variance — Plan Task 8 L518: '실시간 예산 준수: 각 호출이 200ms 걸리는 killer 12건 + budgetMs:600 → reclaimSession이 실제 벽시계로 예산+여유 안에 반환(단위 test지만 주입 타이머가 아니라 실측)'. No tolerance band specified (e.g., 600±50ms). On slow CI or under load, this test could flake. No specification of what 'real wall-clock' means (Date.now()? process.hrtime()?).
- **[LOW][test]** Task 2: probeProcess timeout handling differs from other error modes but test doesn't distinguish them — Plan Task 2 Validate L383: 'default는 ...§D15 플랫폼 분기, 파싱 실패·빈 출력·비-정수는 null'. But Task 8 케이스 9b specifically tests 'probe가 null(미지원/타임아웃)' → identity_unverifiable. No test validates that **timeout** (slow system) is treated same as **missing capability** (unsupported OS). If implementation treats timeout as temporary-fail vs permanent-unverifiable, test doesn't catch the difference.
- **[LOW][explorer]** Plan correctly identifies and reuses existing session identity utilities (`resolveSessionId`, `resolveSessionPid`, `isPidAlive`) — Plan §Patterns to Mirror lines 32-39 cite `evidence-lock.js:69-95` and `evidence-lock.js:82-91` as source; those functions exist at those exact locations and match the signature used in plan
- **[LOW][explorer]** Owner-only atomic write pattern already established in codebase; plan correctly mirrors it — Plan Task 1 and D8 cite `plan-codex-runner.js:75-79 writePrivate` pattern; `dashboard-server.js:83-85` shows identical tmp+rename atomic pattern; `writeFileSync(tmp, text, {mode: 0o600})` + `renameSync`
- **[LOW][explorer]** Session-keyed directory structure convention already present; plan follows established pattern — .gitignore lines 63, 83, 137 show existing patterns: `.claude/state/hook-trace/`, `.claude/state/evidence-claims/`, `.claude/state/session-ledgers/` all session-keyed; plan proposes `.claude/state/session-processes/<session_id>/<pid>.json` following identical pattern (Task 1, D3)
- **[LOW][explorer]** Plan integrates into SessionEnd hook via existing fail-loud-open pattern; no new hook registration required — CLAUDE.md §3.2 documents fail-loud-open precedent; session-end-marker.js:24-58 shows pattern (emit to stderr, never block marker write). Plan Task 7 D9 places reclamation call at end of `run()` after marker and observer cleanup, matching this pattern exactly
- **[LOW][explorer]** Evidence lock tri-state liveness model is deliberately NOT reused here; plan's shorter lease (5s vs 60s) and different fail policy (fail-closed on missing lock vs fail-open) is intentional — evidence-lock.js:122-129 explains tri-state design rationale for *minute-scale* receipt locks. Plan D4 and evidence-lock.js docs explicitly state this pattern is not applicable to ms-scale file IO (task-level locking). Plan intentionally diverges: §D4 pseudocode `isSiblingLive` uses immediate reclaim on cross-host/dead-pid even within lease
- **[LOW][explorer]** Plan's PID identity axes (path + timestamp + executable image) require precise normalize logic; path separator unification documented but not extracted as utility — Plan D15 axes 1-2 require `normPath(s) = s.replace(/\\/g, '/').toLowerCase()` (win32-specific case-fold) and substring inclusion check. No existing `normPath` utility in codebase; similar patterns exist (e.g., gitignore-provision.js path handling) but not generalized. Plan embeds normalization inline in Task 2, risking future divergence if adopted elsewhere
- **[LOW][explorer]** Terminal `/mccp:pr` ship gate interaction NOT addressed in plan; PR-Codex receipt generation could race with SessionEnd reclamation if Codex runner is still alive — Plan D13 states codex runner (`lifetime:'session'`) WILL be killed at SessionEnd. But PRD/CLAUDE.md §1.2 note and §3.13 show plan-codex-runner has `marker` recovery path and 900s timeout. If PR command spawns new codex runner while Session is ending, timing gaps exist. Plan addresses marker resilience (§3.13) but does NOT discuss interaction with `/mccp:pr` Phase locks or dedupe gates currently active in mccp-pr-codex receipt generation
- **[LOW][explorer]** Evidence-claim.js `sameHolder()` degradation (pidUsable=false → drop session_pid axis) is DIFFERENT from plan's session identity contract; potential confusion when porting patterns — evidence-claim.js:99-107 shows `sameHolder` uses degraded comparison when either side has `session_pid=null`. Plan D4's `isSiblingLive` pseudocode (line 109-113) does NOT degrade on null — it treats null as 'unknown liveness → assume live(true)' for cross-host/unverifiable. This is intentional (fail-closed, avoid false-positive overshadowing) but opposite direction from evidence-claim degradation. Both are correct for their contexts but not interchangeable

### Meta-gaps

- No lifecycle diagram showing registry paths (register → list → reclaim → unregister/unlink transitions) — plan has 11 tasks but no visual summary of data flow  _(architect)_
- probeProcess specification is cross-referenced (D15 + Task 2 + Task 8) but not consolidated into a single section that can be read linearly  _(architect)_
- Idempotency contract for reclaim if hook is re-entered (e.g., SessionEnd crash recovery) is not formally specified  _(architect)_
- No discussion of how platform-specific IDENTITY_TOLERANCE_MS constants integrate with central configuration; users can only override via env, not via .claude/settings.json  _(architect)_
- Architectural dependency: registry cleanup relies on SessionStart running; plan doesn't describe contingency if SessionStart is bypassed (e.g., manual CLI restart)  _(architect)_
- Task 2 does not emphasize that probeProcess is mandatory for testing D15 Critical risk; upgrade from prior plan is buried in Rejected Findings R6  _(architect)_
- Plan does not specify behavior if MCCP_RECLAIM_BUDGET_MS is set to a value that, combined with probe timeouts, would cause SessionEnd hook to block past its 10s timeout. The clamping at MAX_BUDGET_MS=9000 helps but doesn't account for hook cleanup overhead.  _(security)_
- No mention of how the module behaves if execFileSync fails to spawn PowerShell or ps binary (e.g., PATH corrupted, permissions issue). Test coverage for uninstalled dependencies is absent.  _(security)_
- Security hardening for the .unreclaimed.json files themselves is not specified — these audit records could be truncated/deleted by an attacker post-sessionEnd if file permissions are insufficient. The 0o600 mode is correct, but no verification test exists.  _(security)_
- The plan acknowledges symlink escapes via realpath.native but doesn't test the fallback path (path.resolve) separately for POSIX symlink scenarios where realpath fails.  _(security)_
- Environmental isolation: if MCCP_RECLAIM_OUTLIVES=1, but SESSION_ID_RE validation is bypassed via env injection, the registry could write to unintended paths. No test for SESSION_ID from hostile environment sources.  _(security)_
- Cross-platform: plan is tested on Windows 11 (UI5), but POSIX code paths (etimes, /proc/pid/exe, ps command) are not tested in the scope. Locale-dependent issues in ps output formatting are mentioned but not fully hardened.  _(security)_
- No baseline scan showing current spawn site count and pattern match rate - Task 9(a) comparison is untested for coverage gaps  _(test)_
- No specification of acceptable POSIX probe timeout rate in CI environment - Task 8 케이스 9d skip condition is underspecified  _(test)_
- Version string consistency across 5 surfaces (plugin.json, html.js, markdown.js footer, status.md footer, CHANGELOG) has no mechanical test - relies on human checklist (§3.7)  _(test)_
- Concurrent registration atomicity test (Task 1 case 12: '서로 다른 3 pid 동시 등록') doesn't specify concurrency model (threads? async? process.nextTick?) or timing precision required  _(test)_
- POSIX EPERM vs timeout distinction not tested - both lead to identity_unverifiable but represent different system states  _(test)_
- Registry file corruption recovery scenario missing - what if .json parse fails during list() or reclaimSession()?  _(test)_
- Hook timeout budget enforcement not validated at integration level - Task 7 tests stub behavior, not actual SessionEnd hook 10s timeout  _(test)_
- Marker-write-first ordering is verified via spy in Task 7(b) but not validated to actually prevent kill from blocking marker write  _(test)_
- Platform-specific Windows job object vs POSIX signal group handling explicitly out-of-scope (§UI5) but test framework doesn't validate Windows-only behavior  _(test)_
- Registry cleanup policy (ORPHAN_STALE_MS threshold) is hardcoded constant but no test validates configurable vs fixed behavior  _(test)_
- Plan does not address interaction with existing `pr-phase-lock.js` tri-state semantics during SessionEnd; if a PR-phase lock is held while reclamation runs, scheduling/ordering guarantees are unspecified  _(explorer)_
- No mention of Windows vs POSIX behavior differences for signal delivery; plan notes UI5 Windows-first but skips depth on graceful-vs-force semantics (mitigated by D12 'SIGTERM is TerminateProcess on Windows' but not tested)  _(explorer)_
- Cross-session `reuse` role liveness depends on `isPidAlive` of the owner session's `session_pid`; if that session_pid is null (degraded), the reuse becomes invisible to kill-safety checks until TTL expires — interaction between degradation and reuse safety unspecified  _(explorer)_
- Plan refers to `completeness` ledger and receipt 'timestamp' fields but does not cite which receipt/envelope schema version they come from; fields like `proc_started_at_ms` are new and could conflict with future schema migrations  _(explorer)_
- Observer-sessions cleanup (Task 7 calls existing `stopObserverForContext`) happens before reclamation; if observer-sessions module throws, reclamation is skipped entirely — not explicit fail-loud-open guarantee for reclamation specifically  _(explorer)_

### Patterns to mirror

- evidence-lock.js:69-95 — resolveSessionId() / resolveSessionPid() / isPidAlive() (§D4, Patterns to Mirror line 32)  _(architect)_
- evidence-claim.js:74-107 — tri-state session identity {session_id, host, session_pid} + sameHolder() (line 33)  _(architect)_
- plan-codex-runner.js:75-79 — writePrivate pattern: writeFileSync(tmp, text, {mode:0o600}) + renameSync (line 35)  _(architect)_
- session-end-marker.js:24-58 — fail-loud-open marker-first + downstream failures don't block (line 40)  _(architect)_
- dashboard-server.js:68-121 — isReusablePid multi-AND validation (PID + host + repo + path + mode) (line 38)  _(architect)_
- .gitignore:57,67 — session-keyed registry directories (.claude/state/evidence-claims/, .claude/state/session-processes/) with explicit comments re: session-local scope (line 41)  _(architect)_
- plugins/mccp/scripts/receipt/evidence-lock.js:82-91 — `isPidAlive(pid)` pattern with EPERM→alive logic and error handling, used in this plan but should be mirrored for consistency  _(security)_
- plugins/mccp/scripts/state/evidence-claim.js:74-107 — session identity tuple `{session_id, host, session_pid}` and `sameHolder()` predicate, mirrored in plan D4 for ownership checks  _(security)_
- plugins/mccp/scripts/hooks/session-end-trace.js:24-31 — SESSION_ID_RE pattern and explicit `'.'`/`'..'` rejection for path safety, used at line 50 in session-processes.js and must remain synchronized  _(security)_
- plugins/mccp/scripts/lib/observer-sessions.js:172-198 — `process.kill(pid, 'SIGTERM')` pattern without force 2-stage, ESRCH/EPERM handling, and fs cleanup; plan mirrors this (§D12, no force)  _(security)_
- plugins/mccp/scripts/lib/dashboard-server.js:82 — `fs.mkdirSync(dir, {recursive:true})` before write, replicated in plan §D8 as mandatory mkdir before every register() call  _(security)_
- plugins/mccp/scripts/lib/plan-codex-runner.js:75-79 — `writePrivate(tmp, text, {mode:0o600})` + atomic rename pattern, used for all session-processes writes to avoid TOCTTOU on write itself  _(security)_
- plugins/mccp/scripts/receipt/write.js:52 — `normalizeReceiptCwd` pattern of repo-relative/outside-repo/unknown normalization for leak prevention, NOT used here (working-tree only, gitignored), but same philosophy (§D15 footnote that exec_path is absolute, not normalized)  _(security)_
- Multiple lock implementations (pr-phase-lock.js, evidence-lock.js, quarantine.lock) — use fail-closed tri-state for cross-host/degraded session_pid detection (isSiblingLive pseudocode in §D4 mirrors this pattern exactly)  _(security)_
- plugins/mccp/scripts/lib/tests/dashboard-server.test.js:23 - tmpRepo() fixture with fs.realpathSync.native for Windows path handling  _(test)_
- plugins/mccp/scripts/lib/tests/hook-trace.test.js - per-session test fixture and session-keyed directory patterns  _(test)_
- plugins/mccp/scripts/lib/tests/pr-phase-lock.test.js - host-aware tri-state ownership validation table pattern (same-host+alive vs cross-host)  _(test)_
- plugins/mccp/scripts/state/tests/session-spawner.test.js:19-28 - spawn recorder pattern (mock spawnImpl to capture calls)  _(test)_ <!-- 경로 정정: fan-out은 `lib/tests/`로 적었으나 실제 파일은 `state/tests/` 아래다. L1 C6가 잡았다. -->

- plugins/mccp/scripts/lib/tests/plan-command-marker-states.test.js:234-238 - timeout implementation test pattern (actual execution, not just prose)  _(test)_
- plugins/mccp/scripts/migrations/tests/host-aware-reclaim.test.js - full judgment table validation (all rows tested)  _(test)_
- plugins/mccp/scripts/hooks/tests/session-end-trace.test.js:12-17 - tmpRepo fixture with cleanup in try-finally  _(test)_
- plugins/mccp/scripts/lib/pr-phase-lock.js:139-146 - exportable ownership token hash so tests can compute expected values  _(test)_
- plugins/mccp/scripts/lib/tests/evidence-claim.test.js - session identity resolution test pattern reusable for registry context  _(test)_
- Session identity resolution: `evidence-lock.js:69-80` `resolveSessionId/resolveSessionPid` — MUST reuse, not reimplementshare or diverge (three-source env fallback order is canonical)  _(explorer)_
- PID liveness probe: `evidence-lock.js:82-91` `isPidAlive(pid)` via `process.kill(pid, 0)` with EPERM-as-alive semantics — MUST reuse for all three contexts (evidence-claim, dashboard isReusablePid, session-processes reclaimable checks)  _(explorer)_
- Session-keyed directory structure: `.claude/state/{hook-trace,evidence-claims,session-ledgers}/` pattern — mirror as `.claude/state/session-processes/<session_id>/`; mode `0o700` + containment checks (realpath + ancestor) established in evidence-lock.js and task 1  _(explorer)_
- Atomic tmp+rename write: `dashboard-server.js:83-85`, `plan-codex-runner.js writePrivate` — MUST use for all `.json` and `.failed.json` writes; `writeFileSync(tmp, ..., {mode:0o600})` + `renameSync`  _(explorer)_
- Fail-loud-open + marker-first: `session-end-marker.js:24-58` pattern — stderr emit never blocks output; attach reclamation AFTER marker write (not before), emit stderr on failure, do not throw into hook  _(explorer)_
- Directory mode enforcement on creation: `mkdirSync(dir, {recursive:true, mode:0o700})` is NEW required addition — established `0o600` for files but directory mode was missing; Task 1 must add this before any registry write  _(explorer)_
- Liveness-degraded fallback: `evidence-claim.js:74-96` `resolveIdentity` — if CLAUDE_PID missing/dead, downgrade to `{session_id, host}` only; warn loudly, keep working. Do NOT fail-closed on pid unavailability.  _(explorer)_
- Test matrix pattern for multi-way verdicts: `migrations/tests/host-aware-reclaim.test.js` (cited in plan Patterns to Mirror line 42) — full cross-product per row of decision table with independent unit assertions, not prose coverage  _(explorer)_
