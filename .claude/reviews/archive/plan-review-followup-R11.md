# Plan Review Panel — session-process-reclaim-followup

**Plan**: `.claude/plans/session-process-reclaim-followup.plan.md` · **Plan version**: `sha256:26fe6d392de6f9f3f3f5cf0bbfa9a9bbe330079e5bd55857e2681e6da41c3d7a`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 4 blocking finding(s): architect/FAIL, test/FAIL, invariant/CRITICAL, invariant/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | Task 9's escalate_pending clear depends on R3 backlog entry existing, but no atomic mechanism ensures Task 9's backlog writes happen before escalate_pending is cleared | Task 9 (L202-232): 'Action' says 'add to backlog' and separate block (L218-232) does the escalate_pending guard+clear. The guard uses grep to check R3 exists, but the action block and guard are presented as sequential prose, not atomic code. If the implementer manually clears escalate_pending before/instead of adding backlog entry, the guard (`grep -qF`) fires **in a subshell** (L226) whose exit status doesn't abort the parent Task. Comment L233-234 says guard is '혼자서 충분하지 않다' (insufficient alone) and Acceptance must re-check, but this means the Task itself can silently succeed while leaving escalate_pending uncleared if implementer skips the backlog write. |
| architect | MEDIUM | Plan claims 10 new backlog items will be added (Task 9 action), but backlog file currently contains 0 of these keywords, creating order dependency that is not mechanically enforced | Grep of .claude/plans/codex-findings-backlog.md for `canonicalPath\|SIGKILL\|CITATION_RE\|record 슬러그 충돌\|announceReuseRegistration\|register\\(\\)\|unreclaimed.json\|assertSafeSessionId\|probeProcess\|realpath 폴백` returns no matches. Validation 7 (L483-490) will fail immediately because it expects these keywords to exist. Plan says Task 9 should add them (L206-216), but Task 9 is described as manual human action ('아래를 **열거된 그대로** 추가한다'), with no executable code block to guarantee they're written. |
| architect | MEDIUM | Task 4's claim about avoiding non-existent receipt paths (L162: '아직 실재하지 않는 receipt 경로를 가리키지 않는다') is violated by placing the self-reference marker ANCHOR-PENDING(Task 11) in git-tracked files before Task 11 executes, creating forward coupling where Task 4's correctness depends on Task 11's success | Task 4 Action (L161): 'Task 11 Action 3이 receipt 생성 **뒤에** 그 자리표시자를 실제 경로로 치환한다'. Validation 10 (L569-570) checks `ANCHOR-PENDING(Task 11)` exists after Task 4, proving it's a placeholder. But the plan says (L162) 'git-tracked 파일이 Task 11의 성공에 의존하는 forward reference를 갖게' is bad. The mitigation L162 says 'Task 11 Action 3이 receipt 생성 **뒤에** 수행' — but there's no mechanism ensuring this order in a distributed/multi-session workflow. If Task 11 fails, the placeholder remains in git-tracked STATE.md/fix-task-applied.md pointing to a Task that did not complete. |
| architect | LOW | Task 11's version conflict gate (L267-285) has asymmetric error handling: both BRANCH_V and MAIN_V_NOW are read with `\|\|exit 1` guards, but the plan's evidence claim (L264-266 comment) says this fixes an earlier asymmetry that 'previous 판은' (previous version) had. The defect is not demonstrated in current code—it's only claimed based on comments. | Plan L267-271 reads BRANCH_V with full protection (read fails → exit 1, empty → exit 1, non-semver → exit 1). Plan L273-280 reads MAIN_V_NOW with same protection. Comment L264-266 describes a prior asymmetry where '이전 판은 그 변수들을 **[받아오지 않았다]**' — claiming previous version assumed variables were set. But the plan provides no diff, no commit reference, and no actual previous code to compare against. This is a citation to a defect that cannot be verified. |
| architect | LOW | Plan's Files to Change table (L74-92) claims `CLAUDE.md` §3.7 will be updated (Task 10), but the update scope (§3.7 `:325` 'stale 동기 대상 5면') is not cited with a line-number range to the actual file, making verification of the 'drift' claim impossible without reading CLAUDE.md separately | Task 10 action (L245): 'CLAUDE.md:325의 5면 목록에서... 빼고'. But CLAUDE.md is not readable as part of this plan's input evidence. The plan says `:325` is stale, but plan itself does not inline what `:325` currently says. Validation 10 (L248) tries to check this: `grep -c 'i18n-surface.test.js' CLAUDE.md` → exists, but this checks for presence not correctness of the claimed drift. The claim 'test가 `plugin.json`에서 파생하므로 **자동 추종한다**는 사실로 대체한다' (replace with fact that test derives from plugin.json) cannot be verified without reading the actual test file and CLAUDE.md to confirm plugin.json import exists. |
| test | MEDIUM | Task 12 (smoke test) is required to verify the primary hypothesis ('processes are reclaimed at SessionEnd'), but the plan does not provide its implementation code — only a specification of what it should do | Plan line 318-332 (Task 12 section) and files-to-change line 91: Task 12 is specified as 'CREATE' the script `plugins/mccp/scripts/lib/tests/manual/session-process-reclaim-smoke.js` but no code is provided. The 'Action' section (line 322-327) describes the contract/requirements in prose, not executable code. Validation 9 (line 528-545) depends entirely on this task-12-created script existing and working correctly. If Task 12 produces incorrect code, Validation 9 will fail, but the plan itself is not self-contained. |
| test | LOW | The Validate section of Task 12 (line 329-332) requires that 'pidAlive(pid) === false' be checked, but neither the task specification nor existing tests show how to implement this bounded polling correctly | Task 12 line 324 advises '50ms 간격 최대 5초' polling, but this is implementation advice not a testable contract. The existing session-processes-reclaim.test.js tests mock this with injected functions (line 258: recorder() that tracks kills), so real bounded-polling verification with timeouts is not tested until the smoke test runs. |
| test | MEDIUM | No existing test verifies that real processes spawned detached in production code (dashboard-server.js line 700-706 registers 'self', plan-codex-runner.js line 326-332 self-registers when launched detached) are actually reclaimed by SessionEnd hook | Session-processes-reclaim.test.js seeds the registry with mock PIDs (line 101: seed(repo, SID, {pid: 4242})) rather than spawning real processes. Session-end-marker-reclaim.test.js uses a mock reclaimSession function injected at line 73. The integration test in dashboard-server.test.js line 606-652 tests registration and unregister-on-close, but not SessionEnd reclaim. Only the smoke test (Task 12, not yet provided) will verify end-to-end real process reclaim. |
| test | LOW | Task 12's contract lists 5 requirements (lines 323-327) but one is internal to Task 12's implementation ('정리 책임' — the script must kill and cleanup failed processes itself) while Validation 9 (lines 528-545) does not verify this cleanup happens | Line 326: '회수가 실패해 자식이 살아남으면 스크립트가 스스로 kill하고 임시 repo를 지운 뒤 비영점 종료한다' — but Validation 9 only checks that if success path is taken, pidAlive is false. If the smoke test spawns a process and reclaim fails, then the test code itself doesn't clean it up, Validation 9 won't force it to do so (it would just exit 1). The cleanup requirement is asymmetric. |
| invariant | CRITICAL | Task 11's version collision gate is procedural, not mechanical — fail-open when implementer skips the manual pre-condition block | Plan line 254 says '/mccp:pr을 호출하기 전에 아래 블록을 먼저 돌리고' and explicitly states 'Validate는 사후 판정이라 그것만으로는 PR이 이미 만들어진 뒤에 붉어질 수 있다. 게이트는 호출 경로 앞에 있어야 게이트다.' Yet the version gate (lines 267-287) is shown as prose pseudocode, not enforced by the /mccp:pr command itself. If the implementer forgets to run this manual block in their shell, or runs it in a different shell session where BRANCH_V doesn't persist, the /mccp:pr invocation proceeds without collision prevention. Validation 5 (lines 424-425) only detects the collision after the PR is already created, making it a post-hoc check, not a preventive gate. |
| invariant | MEDIUM | Receipt anchor remains pending indefinitely in git-tracked files if Task 11 fails to produce a receipt | Task 4 Action (lines 161-162) leaves 'ANCHOR-PENDING(Task 11)' self-referential placeholders in git-tracked files (.claude/state/STATE.md, .claude/PRPs/reports/session-process-reclaim-report.md, .claude/state/fix-task-applied.md). The plan acknowledges this violates naive receipt anchoring but justifies it as 'correct state representation.' However, if Task 11's /mccp:pr fails to produce a receipt, these references remain in git history permanently as dangling forward references (line 162: 'Validation 10이 판정한다' — but there is no Validation 10 check preventing git-tracked ANCHOR-PENDING in live state). The placeholder is meant to be replaced in Task 11 Action 3 (line 290), but if that step never runs, the git-tracked file contains a broken promise indefinitely. |
| invariant | MEDIUM | Escalate_pending release depends on a guard that the plan acknowledges is insufficient and can be bypassed | Lines 217-235 (Task 9 Action) set a guard: grep -qF 'announceReuseRegistration' ... \|\| exit 1. But line 234 explicitly states: 'guard는 혼자서 충분하지 않다 — 위 블록을 통째로 건너뛰고 손으로 state-writer를 부르면 아무것도 막지 못한다.' The plan relies on Acceptance (line 635) to re-verify the final state: 'Task 9의 guard는 실패 시 조용히 넘어갈 수 있으므로 최종 상태를 Acceptance에서 한 번 더 읽는다.' This means the mechanical guard is fail-open by design, relying entirely on post-execution acceptance verification. An implementer could bypass Task 9 entirely and call state-writer manually to set escalate_pending=false without the R3 backlog entry existing. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Attacked plan structure for atomicity, ordering dependencies, forward references in git-tracked files, and claim verification: 1. **Task 9 escalate_pending atomicity**: Verified that the guard to check R3 backlog entry exists before clearing escalate_pending is a subshell whose failure doesn't abort the parent Task. The plan acknowledges this ('혼자서 충분하지 않다') and defers to Acceptance, but Acceptance only post-hoc checks final state — doesn't prevent mid-execution bypass. 2. **Backlog entries present/absent**: Grepped .claude/plans/codex-findings-backlog.md for all 10 keywords claimed in Validation 7 (L483-490) — found zero matches. These are prerequisites for Validation 7 to pass, yet they're described as Task 9 output, creating circular dependency if Task 9 is manual human action with no code enforcement. 3. **Task 4 forward reference to Task 11**: Confirmed ANCHOR-PENDING(Task 11) marker is placed in git-tracked STATE.md and fix-task-applied.md before Task 11 executes (per Validation 10 L569). The plan's own safety claim (L162) explicitly warns against this pattern, then does it anyway, justified only by "Task 11 will replace it." No mechanical ordering guarantee exists across session boundaries. 4. **Task 11 version gate prior defect claim**: Searched for evidence of the "asymmetry" the plan claims to fix (L264-266). The plan cites no previous version, no commit, no file path—only a comment claiming an earlier `node -e …` call might fail silently. Read both BRANCH_V and MAIN_V_NOW blocks in current plan code; both are protected with exit guards. Cannot evidence that prior version actually had the defect described. 5. **CLAUDE.md §3.7 claim**: Plan Task 10 claims to fix drift at CLAUDE.md:325 where "5면 목록" should become "4면". Plan does not inline the current text of CLAUDE.md:325, making it impossible to verify the cited lines actually say what the plan claims. Validation 10 (L248-249) checks for presence of `i18n-surface.test.js` and manifest reference, but doesn't validate the actual `:325` content against the claim. |
| security | pass | Path normalization mismatch (canonicalPath vs realpathNearest) — found to be fail-closed with consistent application to both sides of comparisons, documented in plan backlog item 1 as LOW. Session ID validation — regex and assertion checks prevent path traversal. Process ownership verification — multi-layered checks (host, repo, session, PID, sibling reuse, identity via start time/path/image) are comprehensive and well-tested. PID reuse detection — checked via full path not basename, with tolerance windows. Evidence lock mechanism — atomic, token-based, lease-managed, fail-closed. Non-existent receipt references — explicitly documented in plan section A6 and corrected in Task 4. All documented issues in sections A1-A6, B1-B6, and 10 backlog items are acknowledged with severity assessment. No new undocumented vulnerabilities discovered. |
| test | fail | - Traced whether existing tests (session-processes.test.js, session-processes-reclaim.test.js, session-end-marker-reclaim.test.js, dashboard-server.test.js) verify end-to-end: process spawn → register → SessionEnd → reclaim. Found they test these in isolation, not end-to-end. - Verified whether Task 12 (smoke test) is implemented in the plan or left as a specification: Found it is specified (line 318-332) but not implemented — code must be written by implementer. - Checked whether Validation 9 has coverage to catch if Task 12's smoke test is wrong: Found it does check pidAlive and JSON structure, but depends on Task 12 being correctly implemented. - Examined whether the plan's main claims (M1·M2 "complete" implementation) have supporting tests that don't rely on Task 12: Found no such test — all end-to-end verification defers to the smoke test. - Reviewed Task 12's contract (lines 322-327) and Validation 9's checks (lines 528-545) for gaps: Found minor asymmetry in cleanup responsibility and edge case handling. |
| invariant | fail | Attacked the following invariant anchors: (1) **Fail-open drift in pre-condition gates**: examined Task 11's version collision gate (lines 254-287) — it is shown as prose pseudocode to be run manually before /mccp:pr, but there is no mechanical enforcement that this runs before the command invokes. Validation 5 (post-hoc check) catches collision only AFTER PR creation. The plan explicitly acknowledges this problem ('게이트는 호출 경로 앞에 있어야 게이트다') but provides no solution in the actual command path. (2) **Receipt anchoring integrity**: verified that Task 4 leaves ANCHOR-PENDING placeholders in git-tracked files that could become permanent dangling references if Task 11 fails. The plan accepts this as 'correct state' but it violates the principle that git-tracked files should reference only verifiable artifacts. (3) **Skip predicates and mechanical vs procedural gates**: examined the escalate_pending release guard in Task 9 — the plan acknowledges it can be bypassed and relies on Acceptance verification rather than mechanical enforcement. This is a two-level gate (procedural + post-hoc) rather than fail-closed. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 317044,
  "halt_stage": "5.2e",
  "granted": 4,
  "reviewed_plan_hash": "sha256:26fe6d392de6f9f3f3f5cf0bbfa9a9bbe330079e5bd55857e2681e6da41c3d7a",
  "plan_path": ".claude/plans/session-process-reclaim-followup.plan.md",
  "recorded_at": "2026-08-16T20:00:49.391Z"
}
```
