# Plan Review Panel — diverse-agent-review

**Plan**: `.claude/plans/diverse-agent-review-m8.plan.md` · **Plan version**: `sha256:766d368f6673bfc3685e40e9477715a082f782ae015e2f4654f62949e69d9de6`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 6 blocking finding(s): architect/HIGH, architect/FAIL, test/HIGH, test/FAIL — MCCP_REVIEW_SINGLE_PASS=deadline_pressure 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | The plan claims corpus.js will compute `binding_axis` to classify blocking reasons (M/K/findings), but the measurement JSON in recorded files omits `rolesMin`, making it impossible to determine if K was the blocking factor without unspecified additional logic | .claude/reviews/plan-review-diverse-agent-review-m6-r4-blocked.md:64-68 shows quorum JSON has `required` and `roles` but NOT `rolesMin`. Plan Task 1 (line 153-154) specifies output of '차단 레코드마다...M·K가 binding이었던 레코드 수' but plan lines 142-163 do not specify how to extract rolesMin from records. quorum.js:189-190 shows the decision rule requires rolesMin to check 'if (roles < rolesMin)'. |
| architect | HIGH | The plan's precondition (line 18-20) relies on an ad-hoc script's findings (0 records with M·K binding), but the tool specification cannot independently reproduce this measurement due to missing historical rolesMin data | Plan line 20-21: '차단 레코드 27건 중 M(응답 수)이나 K(고유 역할)가 binding constraint였던 것은 **0건**이다.' This is based on 'ad-hoc 스크립트' not the tool being specified. DN4 (line 84-88) uses this precondition but Task 1 (line 153-154) doesn't explain how binding_axis will be computed without rolesMin values present in records. |
| architect | MEDIUM | The plan acknowledges K parameter changed at commit 794c4de (DN5, line 90-94) creating two K regimes in the corpus, but doesn't specify how corpus.js will determine which rolesMin value to use when classifying each historical record | Plan line 93-94: '도구는 레코드의 `recorded_at`을 그 커밋 시각과 대조해 이 분할을 출력한다' specifies k_split output but doesn't specify the implementation for binding_axis that depends on knowing K value per record. Specification gap: how does the tool get commit-time metadata to pair with recorded_at for K regime classification? |
| architect | MEDIUM | Pattern mirror claim (line 65) cites `record.js:158-176` as pattern for record parsing, but record.js::findingRows only handles findings extraction, not quorum reason string parsing which the binding_axis output requires | Plan line 65: 'record.js:158-176' cites findingRows as 'quorum reading 대상' but findingRows only processes Findings table (line 157-176), not Measurement.quorum fields. The actual quorum reading is in record.js:119-132 (readQuorum function) which doesn't parse reason strings to classify M vs K vs blocking findings. No pattern is cited for reason string parsing that binding_axis requires. |
| test | HIGH | DN4, DN5, DN7 claims about the real corpus are falsifiable by the planned tests | The plan makes three load-bearing empirical claims: DN4 (M and K parameters never block any of 27 blocked records), DN5 (K=3→K=1 natural experiment shows unchanged approval rate), DN7 (F6 never blocks records alone). Task 2 test (line 167-177) uses hand-built fixtures and tests the parsing/counting logic, but cannot validate whether the REAL corpus of 35 records satisfies these claims. Validation 1 (line 239-240) runs corpus.js on real data but only checks `if(!j.state)process.exit(1)` — it does not assert that `binding_axis.m === 0`, `binding_axis.k === 0`, or `f6.solo_blocks === 0`. No Validation line checks these core outputs against expected values. Task 3 Validate (line 190) only counts markdown headings. Only human inspection of tool output can verify the claims. |
| test | MEDIUM | Task 2 test requirements do not explicitly cover the `k_split` output axis needed for DN5 verification | Task 1 (line 150-160) specifies six output axes for corpus.js, including `k_split` to split records by commit timestamp 794c4de and compare converged rates. Task 2 test requirements (line 167-177) list 7 test items but do not explicitly require testing k_split behavior with fixture records at different timestamps. The natural experiment (DN5) depends on k_split being correct, but if the test omits this, the tool could have a timestamp-parsing bug that goes undetected. Only manual inspection would catch it. |
| test | MEDIUM | Acceptance criterion that 4 judgments are 'anchored to tool output' lacks an automated validation mechanism | Line 289-290 states: '판정 4개(승인 경로 존재 · M/K 손잡이 성격 · 실제 승인 규칙 · F6 기여도)가 전부 도구 출력에 앵커돼 있고, 어느 것도 서술만으로 주장되지 않는다' (all 4 judgments must be anchored to tool output, not asserted by prose alone). However, Task 3 Validate line 190 only runs `grep -c '^## '` to count headings. There is no Validation that checks whether the document's prose actually references the tool output or quotes the values. This acceptance criterion is human-validated only. |
| test | LOW | Validation 1 does not verify acceptance criterion that corpus state must not be 'blind' and records must be ≥30 | Acceptance criterion line 287-288 requires: 'that 출력의 `state`가 `blind`가 아니고 `records`가 30 이상이어야 한다' (state is not blind, records ≥30). Validation 1 (line 239-240) checks `if(!j.state)process.exit(1)` and prints state and records, but does NOT assert the acceptance criteria. It only verifies state field is present. If the real corpus has `state='blind'` or `records < 30`, Validation 1 would print those values but exit 0 anyway. The acceptance check is manual. |
| invariant | HIGH | Validation #7 (gate-wiring integrity check) omits critical decision-affecting files, allowing gate modifications to bypass detection | Plan line 257-265 lists gate files to check: cli.js, quorum.js, decide.js, l1-check.js, record.js, workflows/plan-review.js, commands/plan.md. Glob output shows plugins/mccp/scripts/lib/plan-review/ contains budget.js and l3.js that are NOT in this list. budget.js (lines 1-50) is labeled 'plan-review budget oracle' and implements the budget gate predicate (parsePanelBudget). l3.js (lines 1-50) contains cross-model decision logic (buildL3Record) and is a gatekeeper for Codex approval. The plan claims (UI6, Validation #7) that gate wiring is unchanged, but the mechanical check does not cover all files that could silently open the gate. |
| invariant | MEDIUM | Test coverage gap for record parsing: Task 2 does not specify test cases for records with missing required sections, creating risk of silent undercount | Plan Task 2 'Validate' section specifies 7 test cases but test (1) only checks 'normal' records parse correctly, not that missing sections are caught. Test (6) checks that 'parsing failures surface as degraded' but the corpus tool behavior when a record is missing ## Findings or ## Refutation is undefined. The tool must parse three sections per task 1 description ('parseRecord returns {measurement, findings, refutation}') but acceptance criterion (line 287-288) accepts the tool output if records >= 30, even if some records fail parsing and return degraded state. |
| invariant | MEDIUM | Acceptance criterion for records count is ambiguous: does not explicitly require state='ok', only state != 'blind', allowing degraded state (partial parsing success) to satisfy the criterion | Plan Acceptance line 287: 'そ の 出 力 の `state`が `blind`が あ ら ず`records`が30以上' — specifies state != blind and records >= 30, but does not require state == ok. Plan defines three states: blind (0 records, exit 2), degraded (parse fail, exit 1), ok (success, exit 0). If tool returns state:degraded with records:30 (partial success), acceptance reads as satisfied, but Validation #1 (line 239-240) requires exit 0 (implying state:ok). The ambiguity creates a mismatch between human-readable acceptance and mechanical validation. |
| invariant | LOW | Hard-coded commit hash (794c4de, v1.23.1) for k_split analysis creates maintenance debt: future invocations of the tool will split records by this old commit time even if the commit has been rebased or the analysis context has changed | Plan DN5 line 93: 'recorded_at을 794c4de 시각으로 갈라 두 구간의 converged 빈도'. Task 1 axis 6 (k_split) specifies this commit hash as the split point. This is a one-time natural experiment observation, but the hash is hard-coded into the tool. If run in 2+ years, the commit context may be different (rebased, amended, etc.) and the split will be inconsistent with the original observation. No test guards against hash staleness. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Attacked: (1) The oracle specification for parseRecord — verified the measurement JSON structure in existing records lacks rolesMin field needed to classify K-binding cases. (2) The binding_axis output claim — traced quorum.js decision logic (line 189-190: 'if (roles < rolesMin)') and confirmed it requires rolesMin; verified that recorded measurement blocks do not include this field. (3) The K split awareness — confirmed plan acknowledges K changed at specific commit but doesn't specify how the tool will cross-reference historical record timestamps with commit times to apply correct rolesMin values. (4) The pattern mirror — verified record.js has separate findingRows and readQuorum functions; findingRows only processes Findings table, readQuorum doesn't parse reason strings, no cited pattern handles the reason string parsing required for binding_axis classification. (5) The precondition validity — confirmed the 'binding_axis shows 0 M/K binding' claim comes from ad-hoc script output, not from the specified tool, creating a gap between what the plan assumes will be proven and what the specification can actually compute. |
| security | pass | Attacked trust boundaries: input records are git-tracked immutable; tool is read-only with no configurable behavior or external input. Attacked data leakage: JSON output contains only statistics counts, never exposes evidence field contents or environment variables. Attacked parsing robustness: verified plan requires degraded state (not silent failure) on malformed input; test case 6 covers. Attacked path traversal: tool scans only 2 fixed directory paths (non-recursive). Attacked state machine: verified state ladder (blind/degraded/ok) precedence prevents silent success. Attacked audit surface: tool output is verbatim-quoted in git-tracked document with reproduction command. Attacked escalation: tool cannot modify gate behavior, environment, or produce side effects. Found incomplete specification of markdown un-escape order in parser contract (test case 7 checks column alignment but not value correctness for backslash+pipe combinations), but this is correctness/completeness gap, not security defect—wrong output caught on auditable reproduction; records immutable so no attack path. No evidence of secret leakage, privilege escalation, or tamper surface. |
| test | fail | Attacked the plan's core empirical claims about the real corpus by checking: (1) whether the planned tests (Task 1-2) would catch if the real 35-record corpus violated DN4 (M/K never binding) — found that Task 2 uses fixtures only and Validation 1 doesn't assert the binding_axis values. (2) Whether DN5 (K natural experiment) is adequately tested — found Task 2 test requirements do not explicitly include k_split test cases. (3) Whether the acceptance criteria are mechanically enforced by Validation commands — found that "4 judgments anchored to tool output" and "state ≠ blind, records ≥30" are human-validated only, not automated checks. Read plan lines 142-267, Task 1-3 actions/validates, Acceptance criteria lines 287-295, Validation 1-3, PRD line 40-41 (preconditions met), and record.js (sample record format). Checked existing test patterns in plan-review-quorum.test.js to understand test structure.</refutationAttempted> |
| invariant | fail | Attacked fail-open drift by checking whether new skip predicates are introduced: Validation #7 claims to ensure gate files are unchanged, but the file list is incomplete (missing budget.js, l3.js). Attacked direction of defaults when records are missing/malformed: plan specifies degraded/blind states but acceptance criterion ambiguously allows degraded. Attacked test coverage by tracing which record parsing edge cases are specified: test #1 checks normal records, test #6 checks parse-fail surfacing, but missing-section behavior is undefined. Attacked receipt anchoring by examining whether tool output is mechanically verified: output must be embedded verbatim in documentation with reproduction command, weak anchoring (no hash signature). Attacked rollback reality by checking if gate files are protected: Validation #7 does not verify all decision-affecting files. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 739649,
  "halt_stage": null,
  "backlog_appended": 6,
  "backlog_skipped_nonblocking": 9,
  "granted": 4,
  "reviewed_plan_hash": "sha256:766d368f6673bfc3685e40e9477715a082f782ae015e2f4654f62949e69d9de6",
  "plan_path": ".claude/plans/diverse-agent-review-m8.plan.md",
  "recorded_at": "2026-08-26T02:30:12.370Z"
}
```
