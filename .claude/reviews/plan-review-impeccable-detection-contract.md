# Plan Review Panel — impeccable-detection-contract

**Plan**: `.claude/plans/impeccable-detection-contract-m3.plan.md` · **Plan version**: `sha256:ae3b12ae050fc9fdcab6462d561d46b008b5ea49c546f68b19715d7ce3990f5d`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 14 blocking finding(s): architect/HIGH, architect/CRITICAL, architect/HIGH, architect/FAIL — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | Task 6 specifies re-wiring call sites to use resolved impeccable invocation names, but does not show the Bash code pattern required to extract IMPECCABLE_INVOCATION from the DETECT JSON and output it to stderr | Plan Task 6 (lines 196-222) states: 'detect 블록이 `impeccable_invocation`을 뽑아 `IMPECCABLE_INVOCATION` 변수에 담고, stderr로 리터럴 호출형을 출력한다' (the detect block should extract impeccable_invocation and output the call form to stderr). However, the Action section does not specify: (a) the Bash code pattern for extracting the field from DETECT JSON (mirrors at line 685-692 of plan.md show existing extraction patterns like `SKILL_AVAIL=$(echo "$DETECT" \| node -e 'try{...}catch{...}')` but Task 6 does not show equivalent extraction code for invocation), (b) where in each of the 4 command files (plan.md, prp-implement.md, pr.md, code-review.md) this extraction should be added, (c) the exact stderr format that makes the invocation name visible to the LLM, or (d) how the LLM should incorporate that output into the Skill call. This is a critical boundary between Task 1 (JavaScript oracle modifications) and Task 6 (Bash integration). |
| architect | CRITICAL | Task 6 validation requires updating impeccable-guard.test.js with new assertions, but the plan does not show what those new test assertions should contain | Plan Task 6 (lines 206-222) describes three updates to impeccable-guard.test.js: (i) assert that IMPECCABLE_INVOCATION extraction exists in the 4 canonical commands, (ii) assert that no hardcoded bare Skill(impeccable literal exists in those same 4 commands, (iii) add a paired assertion that 'repo has .claude/skills/impeccable/SKILL.md ↔ command has bare literal' must be the same value. However, the plan provides no example test code showing what these assertions should look like. The current test (line 81-86 at `impeccable-guard.test.js`) only asserts that `Skill(impeccable` appears somewhere. The plan's requirement to distinguish between IMPECCABLE_INVOCATION extraction and bare literal absence is load-bearing for preventing a single-commit violation (lines 226-228 explain why Task 6 and 7 must be atomically committed), yet the test code that enforces this is not specified. |
| architect | HIGH | Task 1's description of the eclipsed field does not explicitly specify that resolveImpeccable() should return eclipsed in the resolutionFields() output, creating ambiguity about the oracle's contract | Task 1 (line 107-126) proposes adding eclipsed array to resolveImpeccable() return and states 'resolutionFields()에 impeccable_eclipsed를 얹고' (add impeccable_eclipsed to resolutionFields). However, the current resolutionFields() function (impeccable-detect.js:445) only returns 6 fields from the resolved object. The Task 1 description does not explicitly show that the code should do: `impeccable_eclipsed: resolved.eclipsed,` in the resolutionFields() return object. Without this explicit specification, it's ambiguous whether the eclipsed field is part of the oracle contract or just an internal detail of resolveImpeccable(). |
| architect | MEDIUM | Plan Validation steps do not verify that command bodies correctly extract and output IMPECCABLE_INVOCATION or that the integration works end-to-end | The Validation section (lines 269-282) lists 9 bash/node commands to run, but none of them verify: (a) that the 4 canonical command files actually contain code to extract and output IMPECCABLE_INVOCATION, (b) that the Bash extraction pattern is syntactically correct and produces valid output, (c) that the stderr output can be parsed by the LLM, or (d) that a live invocation of any command actually emits the invocation name to stderr. The validation only checks unit tests, oracle output, and file existence—not the integration boundary. |
| security | MEDIUM | Plan adds `impeccable_eclipsed` to resolutionFields() receipt output but does not explicitly specify whether the field is present-only (excluded from hash) or included in receipt hash calculation. | Plan Task 1: '`resolutionFields()`에 `impeccable_eclipsed`를 얹고' specifies the field is added to receipt output. CLAUDE.md §3.12 establishes the no-rehash invariant: 'receipt_hash'는 하나의 sanctioned 재봉인 도구를 제외하면 절대 재계산하지 않는다.' CLAUDE.md §3.13 Receipt audit precedent shows present-only fields: '`makeSkeleton` **미포함**(§3.12 tracked ship corpus hash 안정성...)' indicating present-only fields exclude from hash. If eclipsed is NOT marked present-only, new M3 receipts will have different hashes than M1/M2 receipts, violating the no-rehash invariant and breaking receipt validation chains. |
| security | MEDIUM | Plan Task 5 (setup Phase 3.5) directs outputting eclipsed source paths to the user ('모든 소스를 버전·경로와 함께 출력') but does not specify whether these paths are sanitized through safeLabel before display. | Plan Task 2 specifies that dep-check printer output values 'must pass through safeLabel' (CLAUDE.md dep-check.js L34-38 defines SAFE_LABEL_RE as '/^[A-Za-z0-9._+:-]{1,64}$/'); Task 5 Phase 3.5 says setup outputs all sources with versions and paths but includes no safeLabel reference. safeLabel rejects paths containing '/' (forward slash), making any file path fail the regex and become '?'. Either setup output leaks filesystem paths (information disclosure), or output becomes unreadable (breaks user decision-making per UI4). Plan does not specify which. |
| test | HIGH | Task 1's eclipsed field rules (4 conditions) are testable via impeccable-resolve.test.js | Plan lines 120-123 specify 4 test conditions: (i) single winner with eclipsed = non-winners, (ii) shadowed:true → empty eclipsed, (iii) ≤1 source OR available:false → empty eclipsed, (iv) resolutionFields() includes eclipsed. But plugins/mccp/scripts/lib/tests/impeccable-resolve.test.js contains no eclipsed field assertions—the test file exists but lacks the described assertions. An implementer must manually add these 4 conditions to multiple existing test cases (e.g., lines 228-248 'plugin + project' case). No automated check validates completeness of these additions. |
| test | CRITICAL | Guard test's paired assertion catches out-of-order commits (Task 6/7) | Plan lines 212-219 describe a 'paired assertion' (single commit invariant): existence of `.claude/skills/impeccable/SKILL.md` must equal hardcoding of bare `Skill(impeccable, ...)` in commands. This assertion does NOT exist in plugins/mccp/scripts/lib/tests/impeccable-guard.test.js (checked lines 1-130). Plan says this assertion must catch Task 7-without-Task 6 (file deleted, commands still hardcode → red test). But the test code implementing this paired assertion is absent. Validation command 'node --test impeccable-guard.test.js' would run, but if paired assertion is never written, test passes despite broken invariant. |
| test | HIGH | impeccable-cleanup.test.js validates all 6 rejection rules | Plan Task 4 lines 156-170 specify 6 rejection rules (don't delete winner, skip plugin source, path match + no symlinks, require --confirm, use git rm/recursive delete, reject if shadowed:true). Plan says test must verify these via 'bare 소스 2개 fixture에서 `apply --source project`·`--source user` 양쪽 모두 거부' (line 310). Validation references 'node --test plugins/mccp/scripts/lib/tests/impeccable-cleanup.test.js' (line 273). But the file does not exist (verified via glob). An implementer must create this test file with all 6 rules covered. No prior test structure constrains what gets tested—incomplete coverage (e.g., missing rule 3 or 5) could pass validation. |
| test | HIGH | Task 6 updates guard test by replacing hardcoded invocation check with IMPECCABLE_INVOCATION extraction check | Plan lines 206-220 say test line 81-87 ('Skill(impeccable, ...) invocation appears') must be REPLACED (not augmented) with new assertions: (i) IMPECCABLE_INVOCATION extraction exists, (ii) no hardcoded bare literals. Current test at impeccable-guard.test.js:81-87 still contains the old `assert.ok(/Skill\\(impeccable/.test(src))` check. The plan does NOT show the new test code. Validation runs 'node --test impeccable-guard.test.js' but this assumes the test has been correctly refactored. If an implementer keeps the old assertion alongside new ones, test would RED when commands use resolved invocation (correct fail for wrong test). But plan must explicitly state: old assertion is REMOVED, not kept. |
| test | MEDIUM | Node command bodies correctly extract resolved impeccable invocation via IMPECCABLE_INVOCATION variable and pass it to Skill() | Plan Task 6 lines 198-205 direct: detect block extracts `impeccable_invocation`, stores in `IMPECCABLE_INVOCATION`, outputs to stderr. Markdown instructs LLM to 'use the name output by above block'. But: (1) Test does not verify invocation extraction actually occurs in commands (no grep for IMPECCABLE_INVOCATION in command bodies). (2) Test does not verify stderr output line exists. (3) Test does not verify command bodies actually READ the variable and pass it to Skill(). Validation runs guard test, but no test checks these three wiring steps. After implementation, command bodies could omit extraction, stdout, or usage, and tests would not catch it. |
| test | MEDIUM | Task 7 validation confirms only plugin source + empty eclipsed remain after deletion | Plan Task 7 Validate (lines 235-238): 'node plugins/mccp/scripts/lib/impeccable-detect.js resolve --json' should output source:plugin · invocation:impeccable:impeccable · eclipsed:[] and '79 파일 삭제' shown in git status. But Validation command (line 276) only invokes 'node plugins/mccp/scripts/lib/impeccable-detect.js resolve --json'—it does NOT parse or validate the JSON output. The resolve command could output invalid JSON, wrong source, or non-empty eclipsed, and validation passes. Plan describes expected output but Validation command does not enforce it programmatically. |
| test | MEDIUM | Setup Phase 3.5 has correct branches for eclipsed vs shadowed cases | Plan Task 5 lines 176-189 specify two branches: (a) eclipsed.length > 0 shows all sources + approval question + cleanup option, (b) shadowed === true hides Remove option. Validate (line 150) runs guard test; plan says test will assert 'Phase 3.5 앵커와 `impeccable-cleanup.js` 참조'. But current guard test (impeccable-guard.test.js) contains no test checking Phase 3.5 structure. A test must verify Phase 3.5 exists AND contains conditional branch logic. Without this test, implementation could have Phase 3.5 but with wrong branch logic. |
| test | MEDIUM | Task 0 live measurement of skill resolution is machine-checkable | Plan Task 0 lines 78-89 require observation that (a) Skill(impeccable:impeccable) resolves to plugin channel and (b) no tool permission prompt appears. Plan lines 95-98 state: 'Task 0의 (a)는 harness가 skill 이름을 해소하는 사건이라 스크립트가 관측할 수 없고, 그래서 코드 게이트로 만들 수 있는 대상이 아니다'—translation: skill resolution is NOT machine-checkable. Plan concedes this is 'not a forcing mechanism' and relies on Task 7 resolve output + Task 9 receipt. But Task 7 Validate only runs the detect command, not verifying its output. Task 9 Validate only checks note file existence, not receipt content. So Task 0's critical precondition (can the skill actually resolve?) is unguarded by automation. |
| invariant | CRITICAL | Task 8 documentation is incomplete: shadowed:true → empty eclipsed invariant is missing from CLAUDE.md §3.17 update action | Plan line 110-114 specifies Rule (1): 'shadowed:true이면 eclipsed는 빈 배열이다'. Plan line 242-244 Task 8 action lists only two invariants to add to §3.17: (1) eclipsed는 열거만 하고 버전을 비교하지 않는다, (2) 정리는 승자와 plugin 소스를 절대 건드리지 않는다. The shadowed:true rule is omitted from documentation action. This is load-bearing: line 111 states 'Task 4의 거부 규칙 (6)이 그 구분을 코드로 강제한다' — the cleanup oracle (Task 4) depends on understanding that shadowed:true means eclipsed=[], not 'no other sources'. If future maintainers add this invariant only to code without documenting it in §3.17, the gate still operates while the invariant is undocumented, making erosion likely. |
| invariant | HIGH | Task 0 branching decision enforcement is incomplete: plan says Task 0 measurements determine whether Tasks 6/7 proceed, but Validation only checks file existence, not decision logic consistency with acceptance status | Plan line 90-94 states if Task 0 measurements show failure or permission issues, milestone must stay 'in-progress' and Tasks 6/7 must not execute. Plan line 309 (Acceptance) says 'Task 0의 두 측정이 원시 출력과 함께 노트에 기록되고, 판정이 Task 6·7의 진행 여부를 명시한다'. But Validation line 281 only runs 'test -f .claude/notes/impeccable-detection-contract-m3.md' — checks existence only. There is no automated verification that: (a) if note says skip, then milestone status is in-progress, or (b) if tasks were skipped, then conditional acceptance items 316-319 are not checked. This creates fail-open risk: developer could ignore Task 0 skip decision, execute Tasks 6/7 anyway, and pass Validation by just creating the note file. |
| invariant | HIGH | Single-commit invariant enforcement is multi-layer and depends on developer discipline for layers 1-2: git must enforce what tests + PR gates cannot | Plan line 212-219 Task 6 describes 'paired assertion' in test that will red if '.claude/skills/impeccable/SKILL.md exists XOR bare literals hardcoded in command bodies'. Plan line 294 Risk mitigation says 'guard test의 짝 단언이 사본 존재와 bare 하드코딩을 같은 값으로 강제해'. But this test only catches the violation IF: (1) developer runs tests locally before commit, (2) tests actually run during PR gate. Between commit of Task 6 alone and Task 7, the repository is in a state where all gates would call unknown_skill, but this won't be detected until PR gate runs. Intermediate state after Task 6 commit (before Task 7 pushed) can exist on branch with unknown_skill gates. Plan line 317 Acceptance requires 'git log -1 --stat이 재배선(명령 본문)과 사본 제거(79 파일 삭제)를 같은 커밋에 보인다' — this catches it, but only if someone reviews the log before merge. The gate is not mechanically fail-closed at git commit time. |
| invariant | MEDIUM | Task 6 rewriting action lacks concrete specification of what LLM output format is required: HIGH-risk mitigation (stderr vs shell variables) is documented only as 'write the name to stderr' without showing exact pattern | Plan line 201 says 'stderr로 리터럴 호출형을 출력한다' but doesn't specify format. Plan line 297 Risk flags this as 'heights' risk: '셸 변수가 도구 호출을 못 넘어 LLM이 이름을 추정한다 \| 높음 \|'. Mitigation is '변수가 아니라 stderr 출력 줄을 읽게 한다. 산문에 그 이유를 적는다'. So LLM must read stderr instead of shell variable, documented in command prose. But the exact stderr format, when it's output, how LLM should parse it are all implicit. This makes implementation vulnerable to misinterpretation where LLM still tries to use shell variable or misreads the format. |
| invariant | MEDIUM | Task 1 test requirement is ambiguous: specifies test checks 'four branches' but doesn't enumerate which four of the actual six resolveImpeccable() branches | Plan line 122-123 test requirement (iv) says: 'resolutionFields()가 네 분기 전부에서 impeccable_eclipsed를 싣는다'. But resolveImpeccable() has six distinct return branches: (1) env-forced-missing, (2) env-forced-available, (3) bare.length>1 (shadowed), (4) bare.length===1, (5) pluginRows.length>0, (6) not-installed. The plan doesn't specify which four branches should be tested or why four not six. This leaves test coverage ambiguous and vulnerable to incomplete implementation. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | I attacked the plan's structural specification of the Task 1/Task 6 boundary (oracle output → Bash extraction → LLM usage), verified the citations to existing code patterns, examined the test file structure to check if new assertions are properly scoped, traced the eclipsed field through resolutionFields() to verify the contract is complete, and reviewed the Validation section to confirm it covers integration points. I found three structural defects: Task 6 omits the Bash extraction code pattern that implementers must follow; the new guard test assertions are described but not shown, making them unverifiable and potentially incomplete; and Task 1 doesn't explicitly state that eclipsed should be returned by resolutionFields(). |
| security | fail | Attacked: (1) Receipt field hash status — checked whether eclipsed follows M1/M2 present-only precedent. Found no explicit statement in plan despite CLAUDE.md §3.12 no-rehash and §3.13 present-only pattern precedent. (2) Path handling in setup output — verified safeLabel definition (CLAUDE.md + code) and whether Task 5 applies it; found Task 2 requires sanitization but Task 5 silent. (3) Cleanup oracle (Task 4) — verified rules are code-enforced not doc-dependent; symlink detection uses lstat; well-known path check is exact match. Cleanup oracle rules appear sound. (4) Deletion atomicity — git rm for tracked provides atomicity; untracked deletion is user-explicit --confirm. Acceptable. (5) Invocation re-wiring (Task 6) — stderr output channel works within command body; invocation name constrained by SKILL_DIR_NAME_PATTERN regex. Safe. (6) Information disclosure through eclipsed array in commit history — already present in M1's sources field; not new. Could not find evidence that plan violates trust boundaries beyond the two specification gaps identified. |
| test | fail | Checked whether test files exist and contain the specific assertions the plan describes: (1) eclipsed field tests in impeccable-resolve.test.js — found test file exists but no eclipsed assertions present; (2) impeccable-cleanup.test.js — file does not exist; (3) paired assertion in impeccable-guard.test.js checking `.claude/skills/impeccable/SKILL.md` existence vs hardcoded bare literals — not present in current test file. Verified current code state: project-local impeccable copy exists at `.claude/skills/impeccable/` with 79 files; all 8 command files contain hardcoded `Skill(impeccable, ...)` calls. Reviewed Validate commands: several assume test files have been created and updated with specific assertions, but those assertions are only described in plan prose, not shown in test code. Checked whether error paths are tested: Task 4 rule (6) shadowed:true rejection is described but no existing test fixture covers ambiguous-winner case. Identified that guard test's existing assertion (line 81-87) would conflict with new wiring if not replaced; plan says it must be replaced but does not show replacement code. |
| invariant | fail | Attacked the plan through five axes: (1) documentation completeness — checked if all Task 1 rules appear in Task 8 documentation action; found shadowed:true→empty-eclipsed rule missing. (2) Receipt anchoring — checked if Task 0 branching decision is mechanically enforced; found Validation only checks file existence, not decision logic. (3) Fail-closed gates — checked if single-commit invariant is enforced before merge; found test-based enforcement depends on developer running tests + PR gate running tests, not git-time block. (4) Specification precision — checked if Task 6 specifies exact output format for HIGH-risk mitigation; found only general description. (5) Test completeness — checked if Task 1 test specifies branch coverage; found 'four branches' mentioned without enumeration of which branches. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 399805,
  "halt_stage": null,
  "backlog_appended": 14,
  "backlog_skipped_nonblocking": 9,
  "granted": 4,
  "reviewed_plan_hash": "sha256:ae3b12ae050fc9fdcab6462d561d46b008b5ea49c546f68b19715d7ce3990f5d",
  "plan_path": ".claude/plans/impeccable-detection-contract-m3.plan.md",
  "recorded_at": "2026-08-22T14:39:52.286Z"
}
```
