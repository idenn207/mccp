# Plan Review Panel — santa-delta-review

**Plan**: `.claude/plans/santa-delta-review-m2.plan.md` · **Plan version**: `sha256:60931158a59b7498bb1c591921af6c1447f72dc2726d04ab91d77a89dcfd6139`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 16 blocking finding(s): architect/HIGH, architect/FAIL, security/CRITICAL, security/CRITICAL — MCCP_REVIEW_SINGLE_PASS=deadline_pressure 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | Task 2 (Layer 1 unit test with tmpdir) and Task 3 (Layer 2 live review) can share the same fixture repository as required by acceptance criteria | Plan Task 2 (line 117): 'mkdtemp + git init 저장소에 corpus를 심고' creates tmpdir repo; Task 2 Validation (line 121): 'node --test' runs as unit test (standard cleanup); Task 3 Action (line 124): 'Task 2의 fixture 저장소에서' references Task 2's repo for reuse; Acceptance (line 193): '같은 fixture에서' requires SAME fixture for both layers. Unit tests with mkdtemp typically destroy the tmpdir after execution, making Task 3's reference to 'Task 2의 fixture 저장소' reference a non-existent repository. |
| architect | MEDIUM | Task 2 Validation section specifies sufficient verification of pre-registered defect class expectations | Plan Task 2 Validation (line 121): '신규 test는 결정적이어야 하며 반복 실행에 동일 결과' (new test must be deterministic with identical results on repeat). This validates only DETERMINISM, not whether the corpus actually contains all 4 expected defect classes. Plan Risk (line 183) claims 'corpus를 Class C 없이 구성해 flip을 쉽게 만든다' and mitigation 'C가 없는 corpus는 test가 붉어진다', but the Validation section does not show an explicit assertion that enforces presence of Class C or the other expected classes. Acceptance criteria (line 193) require actual execution but don't specify whether Task 2's test asserts corpus structure. |
| security | CRITICAL | Task 3 (Layer 2) does not specify the algorithm for matching Codex findings to corpus defect IDs, creating a critical trust boundary gap that could allow silent false negatives and makes measurement unreproducible | Plan Task 3 states: '반환된 finding을 corpus 결함 id에 대조해 계층별 발견/미발견을 기록한다' (line 124) but does not specify: (a) corpus defect ID format or generation algorithm, (b) Codex finding identification/matching algorithm, (c) behavior on non-match. This is the core trust boundary for Layer 2 measurement. Without this specification, defects could be silently mismatched (finding appears detected when it wasn't), or the measurement could become unreproducible between runs. DD4 (line 91-96) acknowledges that compareCoverage() returns 'unknown' records for unmatched IDs but does not specify what makes an ID matchable or how matching occurs. |
| security | CRITICAL | Plan uses non-deterministic Layer 2 (Codex findings) with a binary decision rule ('1건 차이라도' line 83), but provides no specification for handling variance across runs | Plan states Layer 2 is '비결정적이라 CI 게이트로 만들지 않는다' (line 75) but DD3 (line 83) applies a threshold rule: 'corpus 전체에서 델타의 Layer 2 발견 수가 full 대비 단 1건이라도 적으면 default를 뒤집지 않는다'. If run 1 detects N defects and run 2 detects N+1, which is ground truth? The plan acknowledges non-determinism but does not specify how to reconcile it with a binary threshold decision. This is a measurement validity issue that directly affects whether the final decision (flip/no-flip DELTA_SCOPE_DEFAULT) is defensible. |
| security | HIGH | Corpus defect ID format and generation method is unspecified, creating a trust boundary vulnerability where malformed IDs could enable injection attacks | Task 1 (line 112) defines coverageOf() to return 'per-defect {id, class, inScope, reason}' but does not specify ID format constraints or generation algorithm. If IDs can contain markdown, newlines, or special characters, they could inject structure into audit output (similar to renderScopeLines' newline check at scope-delta.js:327-332). DD5 (line 97-100) notes that closed enums prevent injection risk for defect classes, but makes no such statement about defect IDs themselves. The plan exports DEFECT_CLASSES as a closed enum but not DEFECT_ID_FORMAT or similar constraint. |
| security | HIGH | File contents returned by buildCorpus() are not specified to be sanitized, creating leakage risk into receipt fields or logs | Task 1 (line 112) states buildCorpus() returns '파일 내용 + 결함 manifest를 데이터로' but the plan does not specify: (a) content sanitization rules, (b) what receipt fields can receive file contents, (c) whether absolute paths or sensitive strings should be redacted. The receipt schema does not explicitly exclude file contents from meta fields. Test repos created with mkdtemp + git init could contain sensitive git history. Compare to scope-delta.js:327-332, which explicitly throws on newlines in paths — no equivalent guard is specified for corpus file contents. |
| security | HIGH | Plan creates risk of corpus bias toward delta scope (result can be engineered by choosing corpus that naturally aligns with delta scope boundaries) but specifies no mitigation | Plan acknowledges (line 70) 'corpus를 고르는 사람이 답을 고르게 된다 계층 없이 단일 탐지율 하나를 내면 그 수는' but relies on Task 2's 'Layer 1은 결정적이다' to prevent gaming. However, Layer 1 only verifies that defects are **in-scope for delta**, not that delta **detects** them (line 74 says Layer 1 measures 'containment...리뷰어에게 보일 기회가 있다'). Risks section L1 (line 180) acknowledges corpus representativeness risk as 'High' but mitigation only documents the limitation ('N=1 합성이라고 명시') without specifying corpus composition rules that prevent intentional bias. |
| security | HIGH | Plan's decision rule (DD3, line 83) is vulnerable to false negatives: Class C (path dropped, mathematically outside delta scope) will cause default flip decision to fail if any Class C defect is detected by full scope | Plan predicts (line 87): 'corpus에 Class C가 1건이라도 있고 full이 그것을 잡으면 규칙상 flip은 성립하지 않는다' and states 'corpus 전체에서 델타의 Layer 2 발견 수가 full 대비 단 1건이라도 적으면'. If corpus contains even 1 deliberately-undetectable defect (by delta design), the rule guarantees no flip. Plan claims this is intentional (line 87: '그것은 실패가 아니라 PRD M2 outcome의 후반부'), but it means the default will almost certainly remain 'off' by design, not by evidence. This transforms the measurement from 'prove delta is safe' into 'collect evidence delta is not safe', inverting the burden of proof. |
| security | MEDIUM | Plan permits 'reason' string field in coverageOf() output without specification of format constraints, creating a trust boundary similar to the scope-assertion-patterns vulnerability | Task 1 exports {id, class, inScope, reason} for each defect but does not constrain the 'reason' field format. Plan states (line 95) 'manifest에 없는 id가 오면 그것을 예외가 아니라 `unknown` 레코드로 낸다' — but 'reason' is a free text field that could contain markdown/injection. Compare to scope-delta.js §45-83, which defines SCOPE_ASSERTION_PATTERNS and PRIOR_ROUND_PATTERNS as closed regex lists because free-text reasons can inject structure. No such guard is specified for corpus reason field, yet it will be displayed in output files. |
| test | HIGH | Task 2 will freeze (pin) the pre-registered expectations for defect containment across 4 classes via assertions | Line 118 states '이 test가 동결하는 것' (this test freezes). However, Line 121 Validate section specifies only 'deterministic test with identical results' but does NOT list any assertions about per-class containment (e.g., assert A in both scopes, assert C only in full, etc.). Line 193 Acceptance states 'don't claim completion based on unit test passing alone' — contradicting the freeze claim. Line 183 Risk mitigation assumes the test asserts Class C is missing but doesn't document how. |
| test | MEDIUM | The test will verify the pre-registered expectations are correct by running the corpus + CLI + coverageOf and checking results match expectations | Task 2 description (lines 117-118) says to create a test 'with corpus planted, rev0 commit, fix commit with anchor, call real CLI twice (off/enforce), and compare with coverageOf'. The 'sums pre-registered expectations' list classes A-D and which scope includes them. But Task 2 Validate (line 121) does not specify any assertions about per-class containment. Without explicit assertions, the test could call coverageOf() and ignore its output while still passing (deterministic + identical results on reruns). |
| invariant | CRITICAL | Task 4 (decision rule application) has no mechanical verification that Task 3 (Layer 2 measurement) was actually performed with real reviewers | Plan §Task 4 (line 133): validation is only 'node plugins/mccp/scripts/lib/env-contract/lint.js — 코드 default와 registry가 갈리면 붉어진다'. This checks code-registry synchronization, NOT that (a) `.claude/notes/santa-delta-review-m2.md` exists, (b) contains real measurements from actual reviewer invocations, or (c) measurements indicate no detection loss. Task 3 (lines 123-126) is 'manual' work: '실제 리뷰어 레인이 두 번 발화한 발견 대조표' recorded in notes. But notes are not receipts, have no schema validation, and are outside the evidence durability system (CLAUDE.md §3.12). |
| invariant | CRITICAL | Acceptance criteria are all human checkboxes with no code enforcement. Task 4 can execute and flip DELTA_SCOPE_DEFAULT without triggering any gate that verifies the prerequisite condition (DD3: 'Layer 2 발견 수' measurement exists and shows no loss) | Plan lines 190-196: All 6 acceptance items are unchecked boxes for human review (checkmarks [ ]). Line 193 says '게이트/경로를 실제로 1회 완주하고 산출물을 확인' but this is human confirmation, not a code gate. Contrast with Task 4's actual validation (line 133) which only runs lint. If Layer 2 measurements are missing or fake, no code path detects this before the default is flipped (line 130: '`scope-delta.js`의 `DELTA_SCOPE_DEFAULT`를 `'enforce'`로'). |
| invariant | HIGH | Decision rule (DD3, line 83) is frozen and strict ('1건이라도 적으면 default를 뒤집지 않는다'), but there is no receipt anchoring the measurements to the decision. The flip is a permanent code change without durable audit trail binding it to real measured evidence | Plan §Files to Change (lines 38-55): No receipt file is created for M2. Compare to CLAUDE.md §3.12 (evidence durability contract): 'ship receipt는 감사 대조 corpus라 worktree 삭제 후에도 ledger↔receipt 대조가 성립'. The decision to flip `DELTA_SCOPE_DEFAULT` affects all future santa-loop reviews (fail-open if measurement was skipped), but is unanchored to measurements. Only `.claude/notes/santa-delta-review-m2.md` (non-receipt) records data, and notes are ephemeral, not bound by receipt schema validation or `decision_id` hash. |
| invariant | HIGH | Rollback path is manual and undetectable. If Task 3 is skipped and the default is flipped anyway, there is no mechanical way to detect this violation or automatically revert it when defects are later discovered | PRD line 76: 'M1이 `off`로 배송한 이유는... 그 대가(16~93%p)를 아직 아무도 재지 않았기 때문이다' and 'fixture 비교가 하락 없음을 보이면... default를 뒤집는다'. The flip is contingent on measurement proving no loss (risk: 16-93%p detection rate drop per cited evidence). But Plan §Task 4 has no code to read/validate the measurements. Rollback is manual: a future reviewer would need to find the note file (if still in worktree), check if measurements were real, and manually flip the code back. No automatic detection of 'measurements were skipped'. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified all pattern citations (scope-delta.js:1-18, 44-52, 319-349; santa-delta-instrumentation.test.js:29-73; schema.js:1074-1084) match their claims. Confirmed scope-delta is pure (no fs/git), NO_NARROW enum is closed, schema present-only fields are correctly specified, and scope-always properly restores plan/PRD to preserve UI7. Checked that MCCP_SANTA_DELTA_SCOPE default toggle is used correctly across write.js and seal.js. Analyzed the fixture repository sharing architecture between Task 2 (deterministic unit test with tmpdir) and Task 3 (empirical Layer 2 live reviewer execution). Found Task 2 and Task 3 fixture repo boundary incompatibility: plan requires same physical fixture (Acceptance line 193: 'same fixture') but proposes tmpdir cleanup pattern incompatible with Task 3 reuse claim (line 124: 'Task 2의 fixture 저장소에서'). Verified judgment rule DD3 is locked before measurement. Checked that the two-layer architecture (Layer 1 determinism + Layer 2 empiricism) is sound per DD2." |
| security | fail | Attacked the plan through six security lenses: (1) Trust boundaries for data matching — checked how corpus defect IDs are generated/matched to Codex findings and found no algorithm specified; (2) Measurement validity with non-determinism — examined plan's claim to use a binary rule with non-deterministic Layer 2 and found no reconciliation mechanism; (3) File content leakage — searched for content sanitization rules and found none specified; (4) Corpus bias — looked for compositional constraints and found only documentation of risk, not mitigation; (5) Path/special-character injection — reviewed corpus data structures against existing pattern guards (scope-delta.js) and found reason fields lack equivalent guards; (6) Decision rule resilience — traced through DD3 to verify the flip condition and confirmed it's designed to fail on Class C matches, making flip outcome predictable by corpus construction. Checked existing code (scope-delta.js:365-432, seal.js:92-100) to understand trust boundaries in related modules and found they use closed enums + format validation to prevent injection — plan does not specify equivalent controls for new corpus module. |
| test | fail | Attacked the core claim that Task 2's test will verify containment expectations by examining (1) what Task 2 describes vs what it validates, (2) the pre-registered expectations listed but not tied to test assertions, (3) the Acceptance criterion stating 'test passing alone is not completion' which contradicts 'test freezes expectations', (4) the Risk mitigation that assumes assertions exist but doesn't specify them in Task 2. Checked Validation lines for Task 1-7 to see if any document the containment assertions — none do. Examined existing instrumentation.test.js to understand the test pattern, confirming that tests need explicit assertions to verify behavior, not just determinism. Found no path in the plan where per-class containment verification is specified as a testable assertion. |
| invariant | fail | Attacked the decision rule anchoring and gating mechanisms through these angles: (1) Traced Task 3 (Layer 2 manual measurement) to its outputs (note file, no receipt) and found no schema, no hash binding, no validation gate. (2) Read Task 4's validation (env-contract lint) and confirmed it only checks code-registry sync, never reads or validates the note file. (3) Examined acceptance criteria (lines 190-196) and confirmed all are human checkboxes, no code enforcement. (4) Verified against CLAUDE.md §3.12 evidence durability contract: receipts must be git-tracked and durable; notes are not receipts. (5) Traced the decision rule (DD3, line 83) and confirmed it is frozen before measurement, but the actual measurement proof is unanchored. (6) Checked for skip predicates: Task 4 can execute without proof Task 3 ran. (7) Checked rollback safety: no automatic path to detect or revert if measurements were skipped. All attacks successful; invariant erosion is confirmed at the decision boundary where measurement must gate a permanent code change." |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 471993,
  "halt_stage": null,
  "backlog_appended": 16,
  "backlog_skipped_nonblocking": 3,
  "granted": 4,
  "reviewed_plan_hash": "sha256:60931158a59b7498bb1c591921af6c1447f72dc2726d04ab91d77a89dcfd6139",
  "plan_path": ".claude/plans/santa-delta-review-m2.plan.md",
  "recorded_at": "2026-08-21T04:08:07.193Z"
}
```
