# Plan Review Panel — ci-full-suite-m3v

**Plan**: `.claude/plans/ci-full-suite-m3v.plan.md` · **Plan version**: `sha256:5ec57c353b98ca95719d942388c2838d4163cfd4e2870c6c6c33923110472fb9`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 4 blocking finding(s): test/HIGH, test/FAIL, invariant/HIGH, invariant/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | LOW | 분모 채널의 소유자(gate.js)가 러너가 의도적으로 채택한 NUL 구분 파싱 규율을 물려받지 않는다 — 계획은 `tracked`를 `git ls-files '*.test.js'`로 선언하고 Validation의 인라인 해소도 전부 개행 split이다. run.js는 정확히 그 파싱을 결함으로 주석에 못박아 두었다. | `scripts/test-suite/run.js:404-409` — "흡수행 L-2 — `-z` NUL 구분 파싱. 개행 분리는 개행을 포함한 파일명을 조용히 누락시키고, **누락은 분모를 줄인다**" / `listTrackedFiles`가 `gitLines(['ls-files','-z'])`. 대비: plan DD2 L543 "`tracked`는 **`git ls-files '*.test.js'`** 다"(`-z` 무언급)와 Validation L2088·L2092의 `cp.execSync("git ls-files").split(String.fromCharCode(10))`. 방향은 fail-closed다(측정에는 있고 tracked에 없어 stage 0 `measurement_tree_mismatch`로 막힌다)이므로 과대허용은 아니고, 그래서 LOW다. |
| security | LOW | DD4a의 면책 논거("통제는 diff 가시성과 코드 리뷰다")는 런타임 변조 경로를 포함하지 못한다. 강제 workflow의 단계 순서가 전수 실행(= PR이 작성한 임의 test 코드가 러너에서 실행) → 판정이므로, PR이 추가한 test 파일이 실행 중에 `.github/test-suite-floor.json`·`.github/test-suite-exclusions.json`을 워크스페이스에서 덮어쓰면 게이트는 그 변조된 입력으로 판정한다. DD7·DD9가 래칫의 실효 통제로 든 "상수를 올리는 별도 편집이 diff에 숫자로 남는다"는 이 경로에서 성립하지 않는다(diff에는 숫자가 아니라 test 코드가 남는다). 계획의 위협모델 문장은 여전히 결론(닫지 않는다)에서는 정당하지만, 근거 서술이 배선보다 넓게 적혀 있다. | plan L1385-1386 "단계 순서는 체크아웃 → base ref 확보 → Node 고정 → 격리 목록 검증 → 열거 sanity → 판정자 자기 test → 전수 실행 → 판정 …" 과 DD4a plan L794-797 "래칫 상수 셋(...)과 판정 모듈 자신이 전부 tracked 파일이라 같은 PR에서 함께 수정 가능하다" · DD7 plan L762-764 "늘리려면 상한 상수를 같이 올리는 별도 편집이 필요하고 그 사실이 diff에 숫자로 남는다" |
| test | HIGH | The oracle has no assertion forbidding shell-level exit-code suppression (`\|\| true`, `; exit 0`, `set +e`) in the judgment or judge-self-test `run:` blocks — the single cheapest over-permissive neutralizer left, and it keeps every existing assertion green. Appending `\|\| true` after the pinned judgment line leaves assertion 1 (all four flags + `--json` + `> gate.json` literal, uniqueness), 1c (`continue-on-error` absent), 1d (`if:` absent), 2b/2b2 (step set + order), 2c/2d, 3/3b all satisfied, yet `gate.js` blocking no longer makes the check red. The plan closed exactly this class three times for sibling vectors — `continue-on-error` (R10, plan L1713-1720: "판정 단계에 `continue-on-error: true` 한 줄이면 … 체크는 success를 보고하고 … 전부 green이다"), `if:` (R17/R20, L1721-1740), and `ref:` (2d, L1802-1807) — and the negative-fixture list at L1879-1896 enumerates ~30 YAMLs without one for this. The same gap covers the judge-self-test step, which the plan calls the only device keeping "판정자의 건강이 판정자 하류에 있지 않게" (L1415-1431). | .claude/plans/ci-full-suite-m3v.plan.md:1669-1902 (assertions 1,1b,1c,1d,2,2b,2b2,2c,2d,3,3b,4 and fixture list (f)) — no token `\|\| true` / `set +e` / trailing-command rule; grep for `\|\| true` in the plan returns exactly one hit, line 2027, which is local Validation, not a workflow oracle |
| test | MEDIUM | No branch or Validate line exercises the legitimate-deletion path when the deleted file is covered by an exclusion pattern, and the plan's own procedure makes that path structurally red before judgment. Task 3 pins `max_excluded_files` as an *exact equality* against the tree's expanded exclusion count, and that assertion runs in the judge-self-test step which is *before* judgment. DD9's sanctioned exit for a deletion enumerates only `allow_deletions` + floor rederivation + `tracked_basis` rebase — never `max_excluded_files`. So a properly registered deletion of a quarantined (excluded) test file — precisely the class most likely to be deleted under UI7 — turns the self-test red for an unrelated reason and `gate.json` is never produced. Task 7 (e5) rule 2 patches this only for the axis-D experiment ("다른 파일을 고른다"), i.e. the plan works around the coupling for its own demo instead of testing it; there is no branch showing an allow_deletions-registered excluded-file deletion passing end to end. This is the same "만족 불가와 안 했다를 구분할 수 없다" family the plan absorbed at R9/R15/R19/R21. | .claude/plans/ci-full-suite-m3v.plan.md:1315 ("`max_excluded_files`는 격리 목록의 실제 확장 파일 수와 **정확히 같다**(등가)") vs :863-882 (DD9 deletion exit: allow_deletions + tracked_basis rebase only) and :1872-1878 (Task 7 (e5) rule 2 avoids the collision rather than covering it) |
| invariant | HIGH | The plan's own `## Validation` harness is fail-open: it has no `set -e`, no aggregated exit status, and its final command is `... \|\| echo "note: ..."`, so the block exits 0 even when checks 1, 3, 3b, 5, 6, 7 and 7b all fail. Acceptance item "Validation passes" therefore cannot be falsified by those checks. This is exactly the failure shape the plan absorbed a round at a time for every sub-tool — Task 2 branch (10) "`ok=false`인 모든 분기에서 CLI가 비영점", Task 2b branch (m) "`blocked`가 종료코드에 도달한다", Task 3 "`exclusions.js --check`가 비영점" — and it never applied that discipline to the harness that invokes them. A gate whose verifier always reports success is a gate only in shape. | plan L1983-2141: the ```bash block opens with comments and `mkdir -p .claude/cache` (L1999) — no `set -e` anywhere. Failing steps do not halt: `node --test ... test-suite.test.js` (L2005-2010), `exclusions.js --check` (L2024), `ci-required-checks.js --json \|\| echo "note: ..."` (L2111), `container-check.js` (L2114). The last statement is `node scripts/test-suite/gate.js ... \|\| echo "note: 로컬 게이트 차단 ..."` (L2136-2140), which forces exit 0 for the whole block. Contrast the discipline the plan demands of every callee, e.g. L1049 "(10) **`ok=false`인 모든 분기에서 CLI가 비영점**" and L1211-1217 "(m) **`blocked`가 종료코드에 도달한다**". |
| invariant | MEDIUM | The receipt-chain anchor cited as this plan's justification is measured on a different decision namespace than the plan under review, violating the rule the same paragraph states. The plan asserts chain state is "missing-only" for *this* plan, then discloses the measurement came from slug `ci-full-suite-m3j` while the artifact under review is `ci-full-suite-m3v`. Receipt namespaces are per-decision, so this is an unanchored evidence claim — the identical defect L2 R11 already raised against the `m3a` anchor, recurring 13 slugs later. | plan L413-418: "실측한 chain 상태는 **missing-only**다 … **슬러그마다 다시 재야 한다** — receipt 네임스페이스가 decision별이므로 다른 슬러그의 측정은 이 계획의 근거가 아니다(L2 R11 invariant가 앞선 라운드의 `m3a` 앵커를 지적했다). 위 값은 `ci-full-suite-m3j`에서 실측한 것이고…" Plan file under review: `.claude/plans/ci-full-suite-m3v.plan.md`. |
| invariant | MEDIUM | Validation check 2 hard-aborts the whole block on a first top-to-bottom run while its message claims a SKIP, so checks 3–7 are never reached and "만족 불가" is again indistinguishable from "안 했다" — the ordering form of the failure the plan states it absorbed at R9/R15/R19/R21/R22. The guard's producer (check 7) sits ~100 lines below the consumer, and `.claude/cache/` is gitignored, so the precondition is absent on every clean run. | plan L2016: `test -s .claude/cache/m3-local.json \|\| { echo "SKIP: 검사 2는 검사 7의 measurement를 요구한다 — 7을 먼저 돌려라"; exit 1; }` — label says SKIP, action is `exit 1` terminating the script; producer is at L2120-2122. The plan acknowledges the inversion only in prose at L2129-2131 ("실제 실행 순서는 7 → 2다 — 번호는 읽는 순서이지 실행 순서가 아니며") rather than reordering, and L1991-1998 shows the sibling mitigation-note pattern it applied to checks 4/4a/4b but not here. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | 인용 검증: `run.js:10-12`(ok=측정 성립)·`:725`(`return result.ok ? 0 : 1`)·`:213-214`(`per_file: ok ? perFile : null` + 주석)·`:179-183`(`Number(r.exit_code)\|\|0`로 null→0)·`:407-410`·`:666-672`·`enumerate.js:55-59`(Array 아니면 TypeError)·`:111-132`(included∪excluded 이분 구성)·`:122`(endsWith 필터)를 전부 열어 확인했고 계획의 서술과 일치했다. 미러 원본 `version-declaration-gate.yml`도 열어 L21-24(fetch-depth 근거)·L59-60(base fetch step)·L62-65(\\"가드가 초록인데 그 가드 자체가 고장 나 있으면\\" 판별력 test를 가드 앞 독립 단계에)가 실재함을 확인했다 — 미러 인용이 산문이 아니라 실제 규율이다.\\n\\n구조 공격: (1) DD9의 두 래칫 산술을 두 삭제 사이클에 걸쳐 손으로 돌렸다(basis B → floor=B−(D+1) → 삭제 → 정리 시 재기준). R17이 지목한 영구 `below_floor` 고착이 재기준 규칙으로 실제로 닫히는지 확인했고 닫힌다. (2) merge base 드리프트 — head가 merge ref인데 base가 tip이면 base-only 신규 파일이 `missing`으로 들어오는 경로를 재검했고 `git merge-base HEAD <ref>`로 닫혀 있다. (3) 축 D 절단 A·B의 producer 생존 — 판정보다 앞선 자기 test 단계가 트리를 읽는 단언을 `max_excluded_files` 등가 하나로 줄이고 그것을 (e5a)·(e5) 선택 규칙이 덮는지 확인했다(덮는다). (4) `judge({measurement, coverage, deletions})`가 정말 DD3 순서의 유일 소비처인지 — fail-closed 열한 코드는 judge 호출 *전*이라 CLI가 조립한다는 예외가 명시돼 있고 `stage:0`+`coverage:null` 형태로 구분된다. (5) 한 불변식 두 소유자 — `inputs.js`(구현) 대 `gate.js`(계약), `exclusions.js` 위임 명시를 확인했다. (6) 열거 sanity 검사 2가 동어반복인지 — `max_excluded_files`라는 다른 파일의 독립 2항과 대조하므로 실질이다. (7) 로컬 `--base-ref HEAD`가 래칫을 항상 참으로 접는다는 R10 지적과 4a·4b가 모순되는지 — 4b는 index를 자르므로 HEAD 트리 대 index 차가 실재해 성립한다. (8) `exclusions_digest`가 `ticket`을 덮지 않는 점 — 게이트가 같은 파일을 `exclusions.js`로 로드해 ticket을 강제하므로 실 구멍 없음.\\n\\n찾지 못했다: 구조·경계 축에서 HIGH/CRITICAL로 올릴 만한 결함. 위 LOW 하나만 evidenced로 남긴다." |
| security | pass | 신뢰 경계·유출·에스컬레이션 축으로 다음을 실제로 공격했고 전부 이미 닫혀 있거나 명시 기록된 잔여였다: (1) fork PR 자격증명 — `permissions: contents: read` · `persist-credentials: false` · `pull_request_target` 부재 · SHA pin · `secrets` 미주입 다섯이 전부 오라클 단언 1b에 있음(plan L1741-1751). (2) 셸 주입 — `${{ }}`가 `run:`에 직접 보간되지 않음을 지점이 아니라 **클래스**로 단언(3b, L1794-1801)하고 인용이 방어가 아니라는 정정도 실재(L1525-1531). (3) `--base-ref` 신뢰 — 값이 `github.base_ref`(base repo 소유)에 묶이고 `head_ref`/`HEAD`로의 치환을 `env:` 블록까지 확장된 단언이 막음(L1685-1700), last-wins 파서를 통한 중복 플래그 우회는 오라클 유일성 + fail-closed `duplicate_flag` 두 자물쇠(L1147-1157). (4) 위조된 measurement로 게이트 통과 — `if:` 클래스 단언(1d) · producer→consumer 순서 단언(2b2) · checkout `ref:` 부재 단언(2d) · `continue-on-error` 부재(1c)가 각각 닫음. (5) 부분 상태 신뢰/관대한 기본값 — floor 다섯 키 각각의 부재, `allow_deletions` 원소 shape, `base_set_empty`, `measurement` 판독 불가가 전부 fail-closed 열한 코드로 열거됨(L1095-1099, L1185-1193). (6) glob을 통한 전면 면제(격리·삭제 양축) — 분기 (4)와 여섯째 분기가 음성 통제로 존재. (7) durable artifact 유출 — `gate.json`의 `message`가 `redact.js` 통과로 계약 안에 들어옴(L1080-1085)이고, `if: always()` 업로드가 stage 3 차단에도 발행을 막지 못한다는 사실은 교환으로 명시 기록(DD4a L798-804). (8) CI에서의 codex 발화 — `run.js:440,458`이 `MCCP_CODEX_DISABLED=1`을 강제하고 Task 4가 `--allow-codex`를 플래그 파싱 지점에서 CI 차단. (9) DoS/영구 pending — job 수준 `timeout-minutes: 60` + `concurrency`가 값·수준까지 pin. 유일하게 남은 것은 위 LOW 하나이며 그것도 DD4a가 \\"의도는 막지 않는다\\"로 선언한 범위 안이라 HIGH로 올리지 않았다. |
| test | fail | Read the full plan (Tasks 0-10, Validation, Acceptance) and the PRD, then attacked: (1) citation accuracy — verified scripts/test-suite/run.js:613-631 last-wins parseArgv, :655-662 --allow-codex parse site, and enumerate.js normalizeExclusions/enumerateTests; all plan citations checked out. (2) Tried to show Validation 4a/4b synthetic measurements would die at stage 0 `measurement_tree_mismatch` because they feed the full `git ls-files` output — refuted: enumerateTests filters on `.test.js` itself (enumerate.js:122), so measured ⊆ tracked. (3) Tried to show `--base-ref HEAD` in 4a/4b folds the deletion ratchet to always-true — refuted: `--apply-delete` is `git rm` (index) while base_set comes from the HEAD commit tree, so `missing` is non-empty. (4) Checked whether existing tests pin behaviour the rewiring changes — scripts/tests/test-suite.test.js only exercises enumerate directly (no CLI `--exclude-from` fixture lacking `ticket`), and only L696-720 touch `childEnv`, matching the plan's stated reason for moving the guard to the flag-parse site; no test encodes the defect. (5) Walked each Files-to-Change row against a Validate line; only CHANGELOG.md lacks one (not worth reporting). (6) Checked assertion coverage of the over-permissive direction for the enforcement workflow, which is where the two findings above came from. |
| invariant | fail | Read the full plan (2319 lines) and PRD, then attacked: (1) DD3's four-stage order for unknown/partial measurement inputs — traced chunk-spawn failure (`ok:false ∧ exit_code:0`), `per_file:null` vs `[]`, missing keys; verified against `scripts/test-suite/run.js:165-220` and `:713-726` — the cited semantics are accurate and stage 0 covers it. (2) The nine fail-closed input categories / eleven codes for absent, unreadable, empty, duplicate-flag and shape-violating inputs — could not find a permissive fallback. (3) The DD9 deletion ratchet: tried the add/delete offset (closed by set difference), rename (documented, fail-closed), empty `base_set` (closed by `base_set_empty`), base-tip drift (closed by merge-base), and the floor/`tracked_basis`/`max_allowed_deletions` arithmetic across two consecutive deletion cycles and a rename cycle — the derivation `tracked = tracked_basis − (max_allowed_deletions + 1)` holds and leaves a real exit. (4) Workflow-neutering vectors: `continue-on-error`, step/job `if:`, checkout `ref:`, trigger-narrowing keys, `${{ }}` interpolation, step reordering, duplicate flags, tag pins, secrets, timeout scope — each has an assertion plus a negative fixture; deleting `wiring-cut.test.js` itself is caught by the deletion ratchet and by the self-test run line. (5) Validation checks 4/4a/4b for direction on failure of `--apply*` — all fail-closed. (6) Verified plan citations of `run.js` line numbers and `parseArgv` last-wins behaviour — accurate. What I could not defend is the harness itself (finding 1), the decision-slug anchoring of the chain evidence (finding 2), and the ordering guard (finding 3). |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 285070,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:5ec57c353b98ca95719d942388c2838d4163cfd4e2870c6c6c33923110472fb9",
  "plan_path": ".claude/plans/ci-full-suite-m3v.plan.md",
  "recorded_at": "2026-09-04T05:14:11.481Z"
}
```
