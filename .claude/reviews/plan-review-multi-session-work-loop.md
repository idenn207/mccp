# Plan Review Panel — multi-session-work-loop

**Plan**: `.claude/plans/multi-session-work-loop-m10.plan.md` · **Plan version**: `sha256:ce3d993d70ae9250d4ff1f13b40162091d4c1fc07d83238078e97824134b98d5`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 10 blocking finding(s): architect/HIGH, architect/FAIL, security/HIGH, security/HIGH — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | 승격 억제의 술어가 '해소됐는가'가 아니라 '처분 줄이 있는가'다 — 저장소가 이미 코드로 고정한 resolving/non-resolving 구분을 세션 경계에서 무너뜨린다. `deferred`(또는 `rejected`)로 처분된 still-open CRITICAL/HIGH finding이 SessionStart 승격 목록에서 사라지지만, 레지스트리 state는 여전히 `open`이고 C1도 미해소로 센다. 즉 '고쳐지지 않은 CRITICAL이 다음 세션에 안 보이는' 상태가 gate exit 0과 양립한다 — M7이 없애려던 실패 모드 그 자체다. | plan:175 "승격 억제는 handoff-items.js#enumerateOpenFindings에서 한다 … 처분된 finding_id를 SessionStart 목록에서 내린다" + plan:172 어휘 6종에 `deferred`·`rejected` 포함 + plan:197-198(CRITICAL/HIGH도 `deferred` 허용, MEDIUM/LOW 일괄 `deferred`). 반면 findings-registry.js:48-52 "앞 둘만 해소다 — 이연·강등·기각을 해소로 계상하는 것이 UI5가 금지하는 조작 경로" (`RESOLVING_CLOSURE_TYPES=['fixed','invalidated']`)이고 state/cli.js:513이 실제로 그 술어로 걸러낸다. plan의 억제 규칙에는 이 분리가 없다. PRD:158 M7 Outcome "발견과 해소 사이의 유실이 사라진다". |
| architect | MEDIUM | 처분 어휘가 레지스트리의 기존 종결 enum과 별개의 세 번째 어휘인데 둘 사이 매핑을 선언하지 않는다. 같은 개념(finding의 종결 유형)에 대해 `fixed·obsolete·superseded·duplicate·rejected·deferred`와 `fixed·invalidated·deferred·downgraded·rejected`가 공존하며, 어느 쪽이 '해소'인지의 판정이 두 곳에 따로 살게 된다. | plan:172 어휘 6종 vs findings-registry.js:51-52 `CLOSURE_TYPES`/`RESOLVING_CLOSURE_TYPES`. plan은 `obsolete`/`superseded`/`duplicate`가 registry의 `invalidated`에 대응하는지 아무 데도 적지 않으면서, 그 어휘로 registry-공급 표면(승격 목록)을 제어한다(plan:175). |
| architect | MEDIUM | `item_id = "<source>:<native>"` 합성키 문법에 소유자가 없다. plan은 rowId에 대해서는 "파서 두 벌 금지"를 명시하면서, 정작 문법을 세 계층(lib/msw-metrics 생성 · state/handoff-items 접두 제거 · derive/sources/backlog 접두 필터)이 각자 하드코딩하도록 배치하고 공유 헬퍼를 Files to Change에 두지 않았다. | plan:153-156(문법 정의, `debt-inventory.js` 소유) · plan:175("`item_id`의 `findings:` 접두를 벗기면 그대로 나온다" — handoff-items) · plan:185("`item_id` 접두 `backlog:`" — derive/sources/backlog.js). plan:150 "파서 두 벌 금지"와 같은 원칙이 이 축에는 적용되지 않았고, Files to Change(plan:112-120)에 접두 문법을 export할 모듈 지정이 없다. |
| architect | MEDIUM | gate 축 2가 자기가 인증할 산출물을 만든 모듈의 `--verify` 출력을 그대로 신뢰한다 — 거울로 삼은 m9 gate가 독립 산출(derive model) 위에 자기 술어를 구현한 것과 구조가 다르다. `debt-inventory.js`의 검증이 느슨해지면 gate는 그 사실을 볼 수 없다. | plan:236 "처분 완전성 — `debt-inventory.js --verify` 출력을 읽어 …" vs m9-coverage-gate.js:298-317(`readMetrics` → 자체 `predicates(repoRoot, model)` 평가). plan:186도 unmatched 카운터를 "봉인 파일을 아는 모듈이 센다"며 producer 쪽에 둔다. |
| security | HIGH | 인벤토리 dedupe가 claimDigestOf로 접은 항목이 그대로 억제 키가 되므로, 플랜이 'claimDigestOf를 억제 키로 쓰지 않는다'고 세운 격리가 실제로는 성립하지 않는다. 기계 처분 2(`superseded`, backlog 54행)만으로 다른 work-unit의 열린 CRITICAL finding이 SessionStart 승격에서 조용히 사라질 수 있다. | plan:157 "`claimDigestOf`(findings-registry.js:218)로 A의 Finding 셀을 정규화해 B와 대조. 매칭되면 한 항목으로 접되 … `claimDigestOf`는 claim만 해시하므로 여기(인벤토리 dedupe)에만 쓰고 억제 키로는 쓰지 않는다(R2 security F2 — 그러면 다른 work-unit의 CRITICAL이 함께 사라진다)" 와 plan:175 "매칭 키는 … **`finding_id`**다 — `item_id`의 `findings:` 접두를 벗기면 그대로 나온다"가 결합하면, 접힌 item 1건에 대한 처분이 곧 그 안에 접힌 `finding_id`의 억제가 된다. findings-registry.js:218 `claimDigestOf`는 `normalizeClaim(claim)`만 해시하며 work_unit을 포함하지 않는다(대조: :201-211 `deriveFindingId`는 work_unit·gate·perspective를 포함). plan:195 기계 처분 2가 backlog 산문 표기(ABSORBED/RESOLVED)만 보고 `superseded`를 자동 부여하므로, 사람 판단 없이 이 경로가 발화한다. 플랜은 접힌 항목의 `item_id` 접두가 무엇인지도 명시하지 않는다(plan:153-157). |
| security | HIGH | `deferred` 처분이 CRITICAL finding을 세션 경계 승격에서 영구 제거한다 — M7이 세운 '발견과 해소 사이 유실 0' 불변식을 게이트 없는 CLI 한 줄로 되돌릴 수 있고, 어떤 지표도 이를 감지하지 못한다. | plan:175 "승격 억제는 handoff-items.js#enumerateOpenFindings에서 한다 … 처분된 `finding_id`를 SessionStart 목록에서 내린다"는 처분 6종을 구분하지 않는다(plan:172 어휘 6종에 `deferred` 포함). plan:197 은 CRITICAL/HIGH도 항목마다 `deferred` 가능이라 적고, plan:236 게이트 축 2는 CRITICAL/HIGH `fixed` **≥ 1**만 요구한다. 억제 대상은 `handoff-items.js:133-145`의 promotable 목록이며 그 주석(`:126-129`)이 "미해소 HIGH·CRITICAL finding을 다음 세션의 작업 목록에 올린다"고 선언한 바로 그 표면이다. 플랜의 위험 행(plan:295)이 다루는 것은 '미처분 finding의 과다 억제'뿐이고, 처분 유형별 억제 자격은 어디에도 없다. 억제 회귀 test 3종(plan:180)의 (a)는 오히려 이 동작을 고정한다. 원장은 git-tracked·append-only에 만료가 없고(plan:117), M10 종료 후 `m10-coverage-gate.js`가 다시 돌지 않으므로 `unmatched_dispositions` 검사도 이후 추가되는 줄을 보지 않는다. |
| security | MEDIUM | 처분 원장 CLI가 M10의 자기 완료 축을 게이트 없이 stamp할 수 있다는 사실은 플랜이 정직하게 적었으나, 그 원장이 동시에 **다른 milestone(M7)이 소유한 런타임 승격 표면**을 조작한다는 점은 위협 모델 서술에서 빠졌다. | plan:177 은 "여기서 위조 가능한 것은 M10의 자기 완료 선언뿐"이라 적고 plan:239 도 위협 모델을 M8·M9 gate와 동일하게 한정한다. 그러나 plan:175 가 같은 원장을 `handoff-items.js#enumerateOpenFindings` 입력으로 배선하므로 위조 가능한 것은 완료 선언 하나가 아니라 '다음 세션이 무엇을 보는가'이기도 하다. 두 축의 사후 감지 수단이 다르다 — 전자는 gate exit code, 후자는 (플랜 스스로 plan:295에서 인정하듯) C1이 승격을 보지 않아 감지 수단이 없다. |
| test | HIGH | 기존 test가 Task 4가 깨뜨릴 동작을 정본으로 고정하고 있는데, plan은 그 test를 Files to Change에도 Validation에도 넣지 않았다. Task 4는 `scanBacklog` 반환에 `closed_count`/`open_count`를 추가하지만 `derive/tests/backlog-source.test.js`는 반환 키 집합을 정확히 6개로 단언한다 — 착지 즉시 red가 되고, plan의 Validate 문구는 그 test와 직접 모순된다. | plugins/mccp/scripts/derive/tests/backlog-source.test.js:144-150 — test('the return shape is unchanged so consumers need no edit') → assert.deepEqual(Object.keys(r).sort(), ['count','degraded','error','invalid_count','items','ok']). vs plan Task 4 Validate: "신규 키는 `closed_count:0`·`open_count === count`다" (plan:189). 이 test 파일은 plan의 `## Files to Change`(plan:109-125)에도 `## Validation` #4 목록(plan:266-273)에도 없다. |
| test | MEDIUM | 편집 대상 파일의 자기 suite를 Validate가 돌리지 않는다. Task 4가 편집하는 것은 `plugins/mccp/scripts/derive/sources/backlog.js`인데 Validation #4는 `derive/tests/` 아래를 한 파일도 실행하지 않는다(모두 `lib/tests/`·`hooks/tests/`·`state/tests/`뿐). 그 결과 위 F1의 red가 검증 단계에서 관측되지 않는다. | plan:266-273의 test 목록에 `plugins/mccp/scripts/derive/tests/` 경로가 0건. 반면 해당 소스의 전용 suite는 plugins/mccp/scripts/derive/tests/backlog-source.test.js(:25 `require('../sources/backlog')`)이며, 다른 소비처 suite도 derive/tests/generic-interface.test.js·mccp-fixture.test.js에 있다. |
| test | MEDIUM | plan이 locus 선택의 **기계적 강제 장치**로 지목한 test가 실제로는 그 위반을 탐지하지 못한다. plan은 "원장 읽기를 `findings-registry.js` 안에 넣으면 그 test가 red가 된다"고 단언하지만, 인용된 단언은 `process.env` 문자열 부재와 `isPromotable(finding)` 1-arg 호출의 반환값 두 개뿐이라 파일시스템 IO(예: `discoverRepoRoot()` + 원장 read)는 전혀 잡지 못한다. 즉 이 load-bearing 주장을 반증할 test가 없다. | plugins/mccp/scripts/lib/tests/c1-feedback-loop.test.js:616-625 — `code.indexOf('process.env') === -1` + `isPromotable({severity:'HIGH',state:'open'}) === true` / MEDIUM false. 파일 IO 단언 없음. 대상 함수는 findings-registry.js:790-797로 원장 부재 시 같은 값을 반환하므로 IO를 추가해도 두 단언 모두 green으로 남는다. plan:50-56 및 Patterns 행 plan:98이 이 test를 "locus 선택을 강제한다"로 인용. |
| test | MEDIUM | item identity의 핵심 축인 `rowId`를 plan이 **이미 존재하는 단 하나의 구현**으로 인용하지만 저장소에 존재하지 않는다. Task 4는 `splitRow`·`isRowShaped`·`rowId` export를 지시하는데, 앞 둘만 실재하고 `rowId`는 정의 소유자·생성 Task가 명시되지 않는다. | plan:154 — "`rowId`는 `sha256(date\|severity\|source\|finding)` 앞 16자이고 위 스캐너가 export하는 **단 하나의 구현**이다". 실측: `rowId` grep 전 저장소 2건뿐이며 둘 다 무관(plan 자신 · renderer/tests/pipeline.test.js). plugins/mccp/scripts/derive/sources/backlog.js:103-105의 exports는 `{ scanBacklog }` 단독이고 파일 어디에도 rowId 정의 없음. |
| invariant | HIGH | 승격 억제가 처분 종류를 구분하지 않아, 미수정 상태로 `deferred`된 CRITICAL/HIGH finding이 SessionStart 인계 목록에서 영구히 사라진다 — M7이 세운 "발견과 해소 사이 유실 0" 불변식을 게이트처럼 보이는 채로 끈다. | plan Task 3: "승격 억제는 `handoff-items.js#enumerateOpenFindings`에서 한다 … 처분된 `finding_id`를 SessionStart 목록에서 내린다" (Files to Change 표도 동일: "처분된 `finding_id`를 SessionStart 목록에서 내린다"). 6종 어휘(`fixed`·`obsolete`·`superseded`·`duplicate`·`rejected`·`deferred`) 중 어느 것도 예외로 적히지 않았고, plan은 동시에 "전건 `deferred`로 밀어도 gate는 exit 0"(`## 이 milestone의 술어가 보증하지 않는 것`)을 인정한다. 억제되는 항목은 레지스트리에서 여전히 `state:'open'`이므로(Task 3: "레지스트리 무변경") 어떤 지표도 이 소실을 보지 못하며, plan 스스로 "C1은 승격을 보지 않으므로 그 사고를 감지할 수 없다"고 적는다. 억제 대상 코드 경로는 plugins/mccp/scripts/state/handoff-items.js:133-171 (PROMOTE_MIN_SEVERITY = CRITICAL/HIGH 승격, PRD Delivery Milestones 7행이 소유한 불변식). 명시된 양방향 test 3종((a)처분된 finding 사라짐 (b)미처분 open CRITICAL 유지 (c)파손 시 fail-open) 중 어느 것도 `deferred` CRITICAL을 구분하지 않으므로 이 결함은 green으로 통과한다. |
| invariant | HIGH | 대량 이연에 대한 유일한 기계적 마찰인 `--successor` 검사가 순수 파일 실재 술어라, plan이 Patterns to Mirror에서 스스로 "함정"으로 지목한 "커밋된 정적 파일이라 한 번 착지하면 영구히 참" 형태를 그대로 재도입한다. | plan Task 3: "`deferred`는 **`--successor` 필수**이며 실재하는 경로여야 한다" / Validate: "`deferred`인데 `--successor` 파일이 없으면 exit 비영점". 같은 plan의 Patterns to Mirror 행: "정적 파일 실재 술어의 함정 \| `m9-coverage-gate.js:161-166` \| '정책 문서의 *존재*는 미산출을 설명하지 않는다 … 커밋된 정적 파일이라 한 번 착지하면 영구히 참이고'" (실제 m9-coverage-gate.js:161-171이 그 문장을 담고 있음을 확인). Task 7은 같은 함정을 피하려 `declaration-corrected`에 **내용 대조**(`asserted_text`)를 요구하면서, 이연 축에는 존재 검사만 남겼다. 결과적으로 이미 존재하는 아무 repo 파일(예: `.claude/plans/codex-findings-backlog.md`, 본 plan 파일)을 successor로 지정하면 전건 `deferred`가 통과하며, Risks 표가 그 경로를 Likelihood "높음"으로 인정하면서 완화책으로 바로 이 술어를 든다("`deferred`는 실재하는 successor 필수 + … 대량 이연이 보인다"). |
| invariant | MEDIUM | gate 축 2가 판정 전체를 `debt-inventory.js --verify`에 위임하면서, 그 도구가 실행 불가·비정상 종료·출력 판독 불가일 때 어느 방향으로 접히는지 plan에 없다 — 게이트 기계 자체의 고장이 차단이 아니라 통과로 접힐 여지가 남는다. | plan Task 8 축 2: "`debt-inventory.js --verify` 출력을 읽어 `open:0` · `unmatched_dispositions:0` · evidence/successor 규칙 전건 충족 · CRITICAL/HIGH `fixed` ≥ 1". 거울로 삼은 m9 gate는 입력 부재를 명시적으로 fail-closed 처리한다(m9-coverage-gate.js:296 'PRD unreadable — cannot tell which rows were flipped', :300 'derive model unavailable — predicates cannot be evaluated' 둘 다 `ok:false`). m10 plan은 그 대응 문장을 갖지 않으며, 축 2의 규칙 집합은 gate가 독립 재계산하지 않고 검사 대상 도구의 자기 보고에만 결속된다(축 1만 `inventory_sha256` 재계산을 명시). |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | plan이 인용한 file:line을 전수 대조했다 — computeC1(index.js:729-741, work-unit 귀속 없음: 참) · state/cli.js:496-498 주석(문구 정확) · isPromotable(findings-registry.js:790-797, 1-arg 순수 술어: 참) · normalizeCitedPath(:164, export :817, 절대경로/`..` OUTSIDE_REPO: 참) · deriveFindingId(:201-211) · claimDigestOf(:218) · handoff-items.js:133-145 · c1-feedback-loop.test.js:616-625(process.env 0회 + 1-arg pin: 참) · backlog.js:48-58 fail-soft·:103-105 export(참, 단 `rowId`는 아직 존재하지 않고 Task 4가 신설해야 한다) · m9-coverage-gate.js:161-166·303-317(미-flip `checked:false`: 참) · quorum.js:176-182(CLAUDE.md의 175-181보다 plan 쪽이 정확) · IV2 receipt(converged:true + codex_verdict:'divergent' 실재) · IV1 dispatch-log 파일 실재. \\"단일 초크 포인트\\" 주장도 grep으로 확인했다 — `isPromotable` 실호출자는 handoff-items 하나뿐이고 state-injector는 `enumerateOpenFindings`만 부르므로 억제 locus 선택은 실제로 유효하다. derive model 스키마가 신규 키를 거부하는지도 확인했으나(model.js:55-104) 미지 키 거부가 없어 Task 4의 additive 주장은 성립한다. 봉인/재봉인 자기모순, C1 오염 경로, 번호 갭 파서 위험은 공격했으나 반증하지 못했다. 남은 결함은 억제 술어가 registry가 이미 코드로 세운 resolving/non-resolving 경계를 흡수하지 않는다는 것과, 처분 어휘·합성키 문법의 소유자 부재다. |
| security | fail | 공격한 축: (1) 경로 누출 — `normalizeCitedPath` 재사용 주장을 findings-registry.js:164-184에서 검증했고 절대경로·`..`·repoRoot 부재 경로가 모두 OUTSIDE_REPO로 접히는 것을 확인, evidence/successor 4형식 허용도 정규화를 우회하지 않아 finding 아님. (2) 봉인 위조 — 처분 줄의 `inventory_sha256` 결속 + gate 축 1은 봉인 삭제/재생성 시나리오를 실제로 red로 잡으므로 반증 실패. (3) C1 오염 — `findings-registry.js` 무변경 + `state/cli.js:496-498` 인용이 정확함을 원문에서 확인, 오염 경로 못 찾음. (4) fail-open 억제 — 방향이 안전(과소 억제)이라 finding 아님. (5) backlog 파서 재사용 — splitRow/isRowShaped는 :31-46에 실재하나 `rowId`는 아직 없음(Task 4가 신설). 실제로 착지한 것은 억제 키 축: 플랜이 스스로 세운 claimDigest 격리가 seal-time fold를 통해 무너지는 경로와, 처분 유형(특히 `deferred`)이 억제 자격을 구분하지 않아 live CRITICAL이 영구 침묵하는 경로다. |
| test | fail | 공격한 축: (1) Task별 Validate 라인이 편집 파일의 실제 suite를 부르는지 — derive/sources/backlog.js가 전용 suite 없이 편집되고, 그 suite가 반환 키 집합을 pin하고 있음을 확인(F1·F2). (2) plan이 인용한 test가 실제로 그 주장을 강제하는지 — c1-feedback-loop.test.js:616-625를 직접 읽어 IO 탐지 능력 부재 확인(F3). (3) 인용 실재성 — m9-coverage-gate.js:161-166/303-317, handoff-items.js:133-145, findings-registry.js:164-184/201-221/790-797, state/cli 사이드카 논거, computeC1(index.js:729-741)은 전부 인용대로였고 반증 실패. rowId만 부재(F4). (4) 실패 방향 커버리지 — 승격 억제의 위험 방향(과다 억제)은 Task 3 Validate (b)(c)가 실제로 덮고 있어 반증 실패. (5) \\"C1 불변\\" 단언의 실효성 — 레지스트리 write가 실제로 C1을 움직이므로 유효한 반증 수단임을 확인, 반증 실패. (6) gate 축 4의 flip 전 skip 함정 — Task 9가 2회 실행을 의무화해 이미 닫힘, 반증 실패. |
| invariant | fail | m9-coverage-gate.js 전문을 읽어 plan이 인용한 :161-166 함정 문장·:303-317 미-flip skip·fail-closed 입력 처리(:296,:300)가 실제로 그 내용인지 대조했고(전부 일치), backlog.js:31-105로 splitRow/isRowShaped/module.exports 미노출 주장과 4열 스키마를 검증했으며(일치, 단 `rowId`는 아직 부재하는 신규 함수), handoff-items.js:133-171로 승격 경로와 fail-open 주석을 확인했다. 열어보려 시도한 게이트: (1) 봉인 재작성 거부의 우회(봉인 삭제 후 재봉인 → 처분 줄의 `inventory_sha256` 결속이 축 1에서 red가 되므로 우발 경로는 닫힘, 고의 삭제는 M8·M9와 같은 선언된 위협모델 밖 — finding 아님), (2) 스냅샷 이후 신규 부채(Task 5가 `meta.sealed_at_commit`으로 경계를 명시 — 닫힘), (3) `claimDigestOf`를 억제 키로 쓰는 교차 work-unit 오억제(plan이 `finding_id`로 고정 — 닫힘), (4) 파손 원장 fail-open 방향(억제 미발동 방향이라 보수적 — 닫힘), (5) flip 전/후 2회 실행 요구(m9 `checked:false` 구멍을 실제로 덮음 — 닫힘). 열린 채로 남은 것은 위 세 건이며, 특히 처분 종류를 구분하지 않는 승격 억제와 successor 존재 검사는 plan 자신이 최대 리스크로 지목한 대량 이연 경로를 그대로 통과시킨다. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 1155572,
  "halt_stage": null,
  "backlog_appended": 10,
  "backlog_skipped_nonblocking": 8,
  "granted": 4,
  "reviewed_plan_hash": "sha256:ce3d993d70ae9250d4ff1f13b40162091d4c1fc07d83238078e97824134b98d5",
  "plan_path": ".claude/plans/multi-session-work-loop-m10.plan.md",
  "recorded_at": "2026-08-31T09:08:07.784Z"
}
```
