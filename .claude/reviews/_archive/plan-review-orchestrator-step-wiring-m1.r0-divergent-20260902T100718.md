# Plan Review Panel — orchestrator-step-wiring-m1

**Plan**: `.claude/plans/orchestrator-step-wiring-m1.plan.md` · **Plan version**: `sha256:47a53275c35576ddf53a057ef977bc4d2f2b67d7b8072ff384ae2358f1c97af0`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 10 blocking finding(s): architect/HIGH, architect/FAIL, security/HIGH, security/FAIL — MCCP_REVIEW_SINGLE_PASS=deferred_to_prd_completion 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | Task 5의 분자 필터가 기존 진단 축 `sealed_without_completion`을 조용히 오염시키는 경로를 열어 두고, 계획 어디에도 그 소유권이 정해져 있지 않다. `sealed_without_completion`은 `sealedWorkUnits` 각 항목이 `completedWorkUnits` **Set**에 있는지로 계산되며(`plugins/mccp/scripts/derive/sources/session-activity.js:238-242`), 그 값은 A1 메트릭 객체에 그대로 실린다(`plugins/mccp/scripts/lib/msw-metrics/index.js:149`·`:168`·`:183`). Task 5는 "완주 work_unit이 분모 Set에 없으면 분자에서도 제외"라고만 적고(plan:471-477) 그 제외를 `completedWorkUnits` Set을 잘라서 하는지 카운트만 조정해서 하는지를 고정하지 않는다. Set을 자르는 자연스러운 구현이면 PRD 단위로 걸러진 work_unit의 sealed 이벤트가 전부 '완주 없이 봉인됨'으로 재분류되어 A1 출력에 유령 gap이 실린다 — Task 8(10)이 다른 맥락에서 정확히 '유령 gap'이라 부른 실패다. Task 8 어느 항목도 `sealed_without_completion` 불변을 단언하지 않으므로 어떤 test도 이를 잡지 못한다. R1·R2가 A2와 m8-gate에 대해 지적한 '열거되지 않은 소비처'와 같은 계열이며, 계획은 그 열거가 이제 완결됐다고 주장한다(DD8). | plugins/mccp/scripts/derive/sources/session-activity.js:238-242 (`for (const wu of sealedWorkUnits) { if (!completedWorkUnits.has(wu)) sealedWithout++; }`) · plugins/mccp/scripts/lib/msw-metrics/index.js:183 · plan Task 5 "완주 work_unit이 분모 Set에 없으면 **분자에서도 제외**하고" |
| architect | MEDIUM | reader가 토글을 읽는가에 대해 계획이 서로 모순되는 두 규칙을 적용하며, Task 5b가 DD2가 금지한 바로 그 실패를 설치한다. DD2는 "읽는 쪽은 이 토글을 읽지 않는다 … 토글을 양쪽에 걸면 producer가 공유 위치에 쓴 뒤 토글을 끈 사람이 그 이벤트를 잃는다"(plan:173-174)고 못박는다. 그런데 Task 1은 `resolveEventsDir`의 공유 활성 여부를 `MCCP_MSW_EVENTS_SHARED`가 결정하게 하고(plan:377), Task 5b는 **reader인** `evaluateAcceptance`가 그 오라클을 경유하게 한다(plan:505-507). 결과적으로 DD2가 제시한 유일한 되돌림 수단(토글 off)을 쓰는 순간 m8 게이트는 공유 위치의 `task_started`를 보지 못해 `pre_missing:['task_started']` → `ok:false`가 되고(`m8-coverage-gate.js:178`·`:192`), 이는 Task 5b가 고치려던 '살아 있는 producer를 제거됐다고 보고'와 동일한 오탐이다. Task 8(11)은 토글 on 상태의 `acc.ok===true`만 단언하므로 off 경로를 잡는 test가 없다. | plan DD2:173-174 · plan Task 1:377 · plan Task 5b:505-507 · plugins/mccp/scripts/lib/msw-metrics/m8-coverage-gate.js:178,192 |
| architect | MEDIUM | '공유 위치'를 파생하는 지식의 소유자가 정해지지 않아 최소 두 모듈에 중복된다. 실측하면 `resolveEventsDir`의 reader 호출자는 test뿐이고(repo 전체 grep), 실제 reader인 `session-activity.js`는 경로를 손수 조립한다(`:91` `path.join(repoRoot,'.claude','state','msw-events')`) — `derive/sources/findings.js`도 마찬가지다. Task 2는 "candidates에 공유 위치를 추가"라고만 적고 그 경로를 `commonDirOf`에서 얻는지 별도로 조립하는지, 그리고 DD2대로 토글을 무시해야 하는 reader가 토글을 읽는 오라클을 어떻게 재사용하는지를 정하지 않는다. 반면 Task 5b는 다른 reader에게는 오라클 경유를 강제한다. 즉 계획이 세운 KIND 경계 오라클은 호출자가 우회할 수 있는 '단일 진실 원천'이며, 두 파생이 어긋나면 producer가 쓴 곳을 reader가 보지 않는 CL-5 이전 상태(조용한 0건)로 되돌아간다. | plugins/mccp/scripts/state/msw-events.js:236-243 (오라클) · plugins/mccp/scripts/derive/sources/session-activity.js:91 (손수 조립) · plan Task 2:410-413 대 Task 5b:505-507 |
| architect | LOW | Task 5a의 A2 분모 교체가 `computeA2`의 두 반환 분기 중 하나만 덮는다. `sessions.length`는 computed 분기(`msw-metrics/index.js:223`)와 forward-only 분기(`:237`) 두 곳에 있는데, 계획은 `:202`·`:223`만 인용하고(plan DD8) Task 8(9)의 단언도 `status:'computed'` 경로에만 걸린다. 표본이 0인 저장소(오늘의 실제 상태 — A2는 forward-only다)에서는 오염된 분모가 그대로 보고되며 붉어지는 test가 없다. | plugins/mccp/scripts/lib/msw-metrics/index.js:237 `denominator: sessions.length \|\| null` · plan Task 8(9) "`computeA2`의 `denominator === 2`이며 `status:'computed'`인지 단언" |
| security | HIGH | Task 5b turns the m8 producer-removal guard into a tautology for the exact kind this plan rewires. `evaluateAcceptance` exists to detect that a producer was silently deleted (`ok: preMissing.length===0`, m8-coverage-gate.js:178-192, PRE includes `task_started`). Today the evidence is worktree-local, so a worktree whose `task_started` producer is gone reports `ok:false`. After Task 5b the `task_started` lookup is redirected to the shared git-common-dir corpus, which (a) is shared by every worktree of the repo and (b) per Task 1 never has `evictLRU` called on it, so it retains events forever. From then on any single historical `task_started` written by any worktree at any past time satisfies the check permanently — the guard can no longer go false for that kind regardless of whether the producer still exists. The plan reasons only about the false-negative direction ('신규 worktree에서 ok:false가 영구화된다', plan Task 5b) and never about the false-positive it creates; worse, the regression guard it adds, Task 8(11), asserts `evaluateAcceptance(...).ok === true`, i.e. it pins the weakened direction and would stay green after the producer is deleted. | plugins/mccp/scripts/lib/msw-metrics/m8-coverage-gate.js:178-192 (`PRE = ['session_start','session_end','task_started']`, `ok: preMissing.length === 0 && snapshots > 0`) vs plan Task 5b ("`evaluateAcceptance`가 ... `resolveEventsDir({ repoRoot, kind })`가 가리키는 디렉토리를 조회하도록 바꾼다") + Task 1 ("`appendEvent`가 공유 위치에 쓸 때는 `evictLRU`를 호출하지 않는다") + Task 8(11) ("fixture worktree에서 `evaluateAcceptance(...).ok === true`인지 단언한다"). |
| security | MEDIUM | `commonDirOf(root)` consumes an unvalidated filesystem path out of file contents and then mkdir/writes the durable event corpus there. Task 1 specifies: if `root/.git` is a file, read the `gitdir: <p>` line and resolve `<p>/commondir` relative to `<p>`; the result is joined with `mccp/msw-events` and `appendEvent` does `fs.mkdirSync(..., {recursive:true})` + append (msw-events.js:271-296). The plan states no containment rule at all for that resolved path (no absolute/traversal/same-volume check), so a `.git` file or `commondir` file whose content points anywhere — e.g. a directory tree extracted from an archive, or a stale/hand-edited worktree pointer — silently redirects every mccp hook write outside the repository and creates directories there. Existing precedents (codex-policy.js:66-100, receipt/pr-body.js:23-40) do the same read, but they only write scratch/seal files; this plan is the first to route the aggregated evidence corpus through it, and it is also the containment basis Task 3's migration reuses ("올바른 기준은 `commonDirOf(cwd)`가 같은 worktree만 연다") — so the same unvalidated value becomes the migration's trust predicate. | plan Task 1 ("파일이면 `gitdir: <p>` 행을 읽어 `<p>/commondir`을 `<p>` 기준으로 resolve한다. 없거나 판독 불가면 `null`") + plugins/mccp/scripts/state/msw-events.js:271-296 (mkdir recursive + appendFileSync on the resolved dir) + plan Task 3 containment rule. |
| test | HIGH | Task 3의 마이그레이션(신규 CREATE 모듈, 실제 corpus를 복사·병합하고 2단 dedupe를 수행하는 유일한 파괴적 경로)에 자동 test가 0건이다. Task 8의 회귀 항목 (1)~(11) 어디에도 마이그레이션이 없고, Files to Change의 test 파일 5건 중 이 모듈을 부르는 것이 없다. 유일한 검증은 Validation 4의 세 줄 live 실행인데, 그 출력은 기계 판정 없이 눈으로 본다 — 계획 자신이 Validation 1b·5·7에서는 '눈으로 비교하지 않는다'며 exit-code 판정으로 하드닝한 바로 그 결함이 4번에만 남았다. 게다가 R1이 지적한 '0/0 vacuous 통과'(수집 0건이어도 재실행 0건이 성립)를 배제하는 단언(수집 건수 > 0, distinct 수 일치)이 명령으로 존재하지 않는다. 규칙을 repo-root에서 common dir로 바꾼 수정이 실제로 형제 worktree를 수집하는지 반증할 수단이 없다. | plan.md:451-452 (Task 3 Validate: 'dry-run 보고 건수와 실행 후 … 일치. 재실행 시 추가 0건') vs plan.md:660-663 (Validation 4는 3개 명령만 나열, 판정 코드 없음) — 대조: plan.md:680 '눈으로 비교하지 않는다 — 값만 뽑아 유일성을 기계로 판정한다'. plan.md:337-364 Files to Change에 마이그레이션 test 파일 없음 |
| test | HIGH | 분모 granularity 정합화(지표 3, UI2)의 핵심인 producer 판정 술어 — Task 4의 'commandArgs에 PRD 경로 토큰이 있으면 prd, 아니면 milestone' — 를 반증하는 test가 없다. Task 4의 Validate는 allowlist 보존(`eventToJsonLine`)만 단언하고, Task 5/Task 8(7)의 granularity 단언은 `work_unit_kind`를 fixture에 **직접 주입**하므로 실 producer가 그 shape를 낸다는 것을 증명하지 못한다. 더구나 계획 자신이 이번 사이클에서 `prd` 라벨이 '구조적으로 0'이라고 인정하므로(Acceptance 3 주석) `prd` 분기는 test로도 라이브로도 **한 번도 실행되지 않은 채** ship된다. 오분류 방향(밀스톤을 prd로 라벨 → 분모에서 부당 제외)은 A1을 과대평가시키는 over-permissive 방향인데 그 방향을 잡는 단언이 0건이다. 실제 코드상 `work_unit`은 `deriveDecisionId`가 planPath·branch fallback에서도 파생되므로(receipt-prompt.js:393-397) 인자 축 라벨과 슬러그 축이 어긋날 수 있어 오분류는 이론이 아니다. | plan.md:456-462 (Task 4 Action/Validate) · plan.md:479-482 (Task 5 Validate: 'fixture로 prd 1건 + milestone 2건 … 주입') · plan.md:756-762 ('이번 사이클에서 그 값은 구조적으로 0') · plugins/mccp/scripts/hooks/receipt-prompt.js:393-397 |
| test | MEDIUM | DD5가 '약속된 상한이 실제로 존재하는지의 유일한 증거'라고 못박은 3초 타임아웃 검증이 Validate 산문에만 있고 Validation 블록의 어떤 명령에도, Task 8의 (1)~(11) 어떤 회귀 항목에도 없다. 즉 execFileSync timeout 배선이 누락되거나 잘못돼도 붉어지는 것이 0건이며, 그 실패 모드(스캔 stall 시 /mccp:work 진입이 멎음)는 fail-open 불변식(UI6·PRD 결정 3)이 막으려는 바로 그 방향이다. | plan.md:540-541 ('스캔이 3초를 넘도록 만든 fixture에서 … 이것이 DD5가 약속한 상한이 실제로 존재하는지의 유일한 증거다') vs plan.md:617-724 Validation 블록(해당 명령 부재) 및 plan.md:549-581 Task 8 항목 목록(해당 항목 부재) |
| invariant | HIGH | A1의 anti-gaming spike 가드가 `startupCount > 50`에서 무조건 발화하며(`_priorStartupCount`는 저장소 전체에서 **설정되는 곳이 0건**), 이 계획은 그 임계를 넘기는 조건을 구조적으로 보장한다 — 집계 경계를 저장소 전체로 올리고(DD1/DD7) 공유 위치에서 `evictLRU`를 제거해(Task 1) 분모가 단조 증가하는 corpus를 만든다. 임계 초과 시 A1은 조용히 `status:'invalid'` · `integrity_ok:false`로 뒤집히고, 그것이 Acceptance 2번('status가 computed다')과 PRD 지표 2('안정적으로 산출된다')를 사후에 무효화한다. 계획 어디에도 이 상한이 언급되지 않고 DD6은 `computeA1` 변경 0을 못박아 대응 경로도 없다. | plugins/mccp/scripts/lib/msw-metrics/index.js:114 `const unitSpikeFlag = startupCount > 50 && !model._priorStartupCount` — grep 결과 `_priorStartupCount`의 출현은 이 한 줄뿐(설정 사이트 0건). 계획: "**`appendEvent`가 공유 위치에 쓸 때는 `evictLRU`를 호출하지 않는다.**"(plan Task 1) + "### DD6 — computeA1은 변경 0이다"(plan:229) + Acceptance 2 "그 값의 status가 `computed`다"(plan:751) |
| invariant | HIGH | Task 5b가 `m8-coverage-gate`의 `evaluateAcceptance`를 kind별 `resolveEventsDir` 단일 디렉토리 조회로 바꾸는데, `resolveEventsDir`는 `MCCP_MSW_EVENTS_SHARED` 토글을 읽는다(Task 1). 따라서 DD2가 내세운 되돌림 수단(토글 off)을 쓰는 순간, 토글이 켜져 있던 기간에 공유 위치로 간 `task_started`가 게이트의 시야에서 사라져 `pre_missing`에 들어가고 `ok:false`가 된다 — R2에서 HIGH로 흡수한 '살아 있는 producer를 제거됐다고 보고하는 게이트'가 rollback 경로에서 그대로 재생산된다. reader(`session-activity`)는 두 디렉토리를 모두 읽는데(Task 2 · DD8) 게이트만 한 곳을 읽는 비대칭이 원인이며, Task 8(11)은 공유가 켜진 fixture에서 `acc.ok===true`만 단언하므로 이 rollback 상태를 붉히는 test가 0건이다. | plan Task 5b: "`evaluateAcceptance`가 `eventsDir`를 직접 만들지 않고, `PRE`/`POST`의 각 kind에 대해 `resolveEventsDir({ repoRoot, kind })`가 가리키는 디렉토리를 조회하도록 바꾼다" · plan Task 1: "공유 활성은 `MCCP_MSW_EVENTS_SHARED`가 결정한다" · plan Task 8(11): "fixture worktree에서 `evaluateAcceptance(...).ok === true`인지 단언한다" · 현행 게이트 판정 `plugins/mccp/scripts/lib/msw-metrics/m8-coverage-gate.js:192` `ok: preMissing.length === 0 && snapshots > 0` (PRE에 `task_started` 포함, :178) |
| invariant | MEDIUM | Task 5a가 `computeA2`의 분모를 `sessions.length`에서 신규 필드 `sessions_local.length`로 바꾸면서 **그 필드가 없는 model**(구 derive 캐시·기존 fixture·`session_activity` 소스를 거치지 않은 호출)에 대한 방향을 정하지 않았다. 현행 코드는 `sessions.length \|\| samples.length`로 부재를 접지만, 신규 경로는 `undefined.length` → TypeError가 되어 `computeMetrics` 전체가 죽고, 배너 CLI는 전역 try/catch + exit 0 + 빈 stdout(Task 7)이라 그 붕괴가 '배너 생략'으로 조용히 접힌다. 알 수 없는 입력이 어느 방향으로 떨어지는지가 계획에 없다. | plan Task 5a: "`msw-metrics/index.js`의 `computeA2`는 분모를 `sessions.length`가 아니라 **`sessions_local.length`** 로 읽는다" — 대응 현행 코드 `plugins/mccp/scripts/lib/msw-metrics/index.js:223` `denominator: sessions.length \|\| samples.length`. plan Task 7: "어떤 실패에도 exit 0 + 빈 stdout" |
| invariant | LOW | 공유 위치의 해소 규칙이 두 곳에 중복된다 — producer는 `msw-events.js#commonDirOf`(+토글), reader(`session-activity.js` `candidates`)는 DD2대로 토글을 읽지 않으므로 같은 오라클을 그대로 쓸 수 없다. Task 2는 reader가 어떤 함수로 공유 경로를 얻는지 명시하지 않아, 두 규칙이 드리프트하면 producer가 쓰는 곳과 reader가 읽는 곳이 어긋나 A1이 조용히 0/forward-only가 된다. Task 8(2)의 3-root 동일값 단언이 부분적으로 결속하지만 계획 본문에는 단일 소유 오라클 선언이 없다. | plan Task 2 Action: "`session-activity.js`의 `candidates`에 공유 위치를 추가하되 맨 앞이 아니라 뒤쪽(`di > 0`)에 넣는다" (해소 함수 미지정) vs plan DD2: "**읽는 쪽은 이 토글을 읽지 않는다.**" |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | 인용 검증: `msw-events.js:223/236-243/299`(discoverRepoRoot·resolveEventsDir·evictLRU 무조건 호출), `session-activity.js:87-130`(candidates·isCrossLocation=di>0·legacyKeyOf), `:154`(kind 가드 없는 sessions 채움), `:249-262`(spanOf가 session_start 요구 → B2 보존 주장 성립), `:238-242`(sealed_without_completion), `msw-metrics/index.js:202/223/237`(A2 분모), `m8-coverage-gate.js:164/178/192`(하드코딩 경로·PRE·ok) — 계획이 인용한 file:line은 전부 실제로 그렇게 적혀 있었다. 공격한 축: (1) DD7 파생 구조가 정말 도달 가능한가 → repoRoot 명시 호출자에 대해 성립, 반증 실패. (2) DD8의 KIND 경계가 B2/taxonomy/findings를 실제로 고정하는가 → `findings.js` 직접 경로 읽기와 `spanOf` 근거 모두 계획의 주장대로였다. (3) `resolveEventsDir`를 '오라클'로 부르는데 호출자가 우회 가능한가 → 실측상 reader 전원이 우회 중(finding 3). (4) 소비처 열거가 완결됐다는 주장 → `sealed_without_completion`이 누락(finding 1). (5) 토글의 producer/reader 비대칭 → Task 5b가 그 규칙을 깬다(finding 2). (6) staticLint/entrypointRegistry가 신규 파일로 붉어지는지 → 마이그레이션이 `appendEvent(`를 쓰지 않는 한 문제 없어 finding으로 올리지 않았다. (7) 마이그레이션 dedupe 2단·di>0 극성 → 반증 실패, 논증이 성립한다. |
| security | fail | 읽은 것: plan 전문(838행)·PRD 전문, `state/msw-events.js`(ALLOWED_FIELDS:55, SESSION_ID_RE:45, discoverRepoRoot:223, resolveEventsDir:236, evictLRU:119/299), `derive/sources/session-activity.js:80-190`, `msw-metrics/m8-coverage-gate.js:150-199`, `lib/codex-policy.js`·`receipt/pr-body.js`의 gitdir 선례. 공격 시도와 결과: (1) 파일명 주입 — `work_unit` 슬러그가 경로 성분이 되는가? 되지 않는다. 파일명은 `sessionId`뿐이고 `appendEvent`가 `SESSION_ID_RE`로 fail-closed 거절한다(:248). plan F12 주장은 참. (2) 절대경로 유출 — Task 7이 stderr를 repo-relative로 축약하고 공유 디렉토리는 `.git` 하위(untracked)라 커밋 산출물에 닿는 경로를 찾지 못했다. §3.12 cwd 유출 선례의 재개방 경로 없음. (3) 조상 저장소 해소 — DD7의 `root/.git` 단일 검사가 walk-up을 실제로 제거하며 Task 8(3)이 가드한다. 반증 실패. (4) 부분 상태 신뢰 — `work_unit_kind` 부재를 `milestone`으로 접는 약한 fallback이 있는지 확인했으나 DD3이 명시적으로 거부하고 unknown으로 병기한다(보수적 방향). (5) 토글 우회 — 열거 밖 값 fail-closed + Validation 7의 양방향 단언이 극성 위조를 닫는다. (6) reader dedupe 극성(`isCrossLocation = di > 0`, :130) — 공유 위치를 di>0에 두는 Task 2가 실제로 legacy 복합키 dedupe를 발동시킨다. 인용 정확. 남은 두 건만 보고한다. |
| test | fail | plan/PRD 전문을 읽고 각 Task의 Validate 줄을 Validation 블록·Task 8 회귀 항목·Files to Change의 test 파일과 대조했다. 인용 검증: session-activity.js의 `sessions`(:117,:154) · `isCrossLocation = di>0`(:130) · `legacyKeyOf`(:126), msw-events.js의 `evictLRU`(:119,:299)·`GLOBAL_MAX_BYTES`(:26)·`ALLOWED_FIELDS`(:55)·`resolveEventsDir`(:236) — 전부 계획이 적은 대로였다. msw-m8-producers.test.js:294가 `acc.ok`를 보지 않는다는 주장도 실측 확인(:294-302, Array.isArray + post_note만). DD3의 'emit 시점에 command_args를 들고 있다'도 `event.command_args`(:393) 존재로 반증 실패. A2 분모 오염(Task 8(9))·조상 격리(8(3))·경로 불변(8(4))·토글 양방향(Validation 7)은 반증 가능한 단언이 실제로 붙어 있어 공격이 실패했다. 남은 세 축 — 마이그레이션 무-test + vacuous live 판정, producer 라벨 술어의 fixture-주입 우회, 타임아웃 상한의 무-단언 — 이 위 findings다. |
| invariant | fail | DD7의 파생 구조를 `resolveEventsDir`(msw-events.js:236-243)·`discoverRepoRoot`(:223)와 대조해 walk-up 제거·`.git` 앵커·tmpdir 경로 불변 주장은 실제로 성립함을 확인(반증 실패). Task 2의 `di>0` 극성 주장을 session-activity.js:128-153의 `isCrossLocation`/`legacyKeyOf` 실코드와 대조해 정확함을 확인(반증 실패). 토글 fail-closed 접힘·Validation 7의 양방향 쌍 단언은 실제로 공허 통과를 막음(반증 실패). Task 5의 분자 필터가 `num ≤ den`을 구조 보장하는지 computeA1:172-179로 검증(성립). 그 대신 (a) computeA1의 spike 가드(:114)가 이 계획이 만드는 단조 증가 corpus에서 확정 발화하고 `_priorStartupCount` 설정 사이트가 0건임, (b) Task 5b가 게이트를 토글 종속으로 만들어 rollback이 곧 게이트 오탐이 되고 그 상태를 붉히는 test가 없음, (c) `sessions_local` 부재 시 방향 미정, (d) 공유 경로 해소 오라클 이중화를 찾음. m8 staticLint가 신규 마이그레이션 파일을 미승인 `appendEvent(` 호출자로 잡을 가능성도 추적했으나 계획이 라인 직접 복사를 명시해 트리거되지 않음(반증 실패). |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 323634,
  "halt_stage": null,
  "backlog_appended": 10,
  "backlog_skipped_nonblocking": 7,
  "granted": 4,
  "reviewed_plan_hash": "sha256:47a53275c35576ddf53a057ef977bc4d2f2b67d7b8072ff384ae2358f1c97af0",
  "plan_path": ".claude/plans/orchestrator-step-wiring-m1.plan.md",
  "recorded_at": "2026-09-02T00:52:19.219Z"
}
```
