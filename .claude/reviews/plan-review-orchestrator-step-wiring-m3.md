# Plan Review Panel — orchestrator-step-wiring-m3

**Plan**: `.claude/plans/orchestrator-step-wiring-m3.plan.md` · **Plan version**: `sha256:ce77c9caf2e31ad6882147d500b4384575b5cb6d56c3420ae5367e6aef3c44c9`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 6 blocking finding(s): security/FAIL, test/HIGH, test/HIGH, test/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | Task 9의 acceptance 두 축 중 하나(`renderer/verdict.js:127` amber)는 오늘 이미 발화 불가라, 그 항목은 '아무것도 하지 않은 것'과 '해소한 것'을 구별하지 못한다. verdict.js:127의 조건은 `fixTask && stateItem.escalate_pending`이고 `fixTask`는 `derive/sources/fix-task.js:7-9`가 `state/fix-task.js:344-347`(=`.claude/state/fix-task.md`만 읽음)로 만드는데, 이 저장소에는 `fix-task.md`가 없고 `fix-task-applied.md`만 있다(Glob 실측). 즉 amber는 escalate_pending 값과 무관하게 이미 침묵 중이다. 실제로 escalate_pending에 반응하는 운영자 표면은 `state-injector.js:145`  하나뿐이다. | plan L224-226 "`renderer/verdict.js:127`이 `fix-task pending escalate`(amber)를 더는 내지 않고" vs plugins/mccp/scripts/lib/renderer/verdict.js:126-129 (`const fixTask = sources.fix_task && sources.fix_task.item; if (fixTask && stateItem.escalate_pending)`) + plugins/mccp/scripts/state/fix-task.js:344-347 + 워크트리에 `.claude/state/fix-task.md` 부재(`fix-task-applied.md`만 존재) |
| architect | MEDIUM | Task 1이 배너 토큰을 추가하는 근거(R0 #1 흡수)가 오늘 상태에 대해 거짓이고, 그 결과 새 토큰의 발화 조건이 미지정으로 남는다. plan 자신이 startupCount는 오늘 23이라고 적으므로 `startupCount > 50` 가드는 발화하지 않고 A1은 `invalid`가 아니며, `verdict.js:93-96`의 헤드라인 경로는 지금도 A1에 대해 닫혀 있다 — '오늘 (오탐으로나마) 헤드라인에 뜨던 신호가 M3 이후 0개 표면이 된다'는 성립하지 않는다. 더 문제인 것은 DD2가 기준선 producer를 만들지 않는다고 못박으므로 가드는 **영구히** dormant이며, 토큰이 무조건 출력이면 `cli.js:332` 한 줄 배너에 상수 문자열이 매번 실리고, 51 이상에서만 출력이면 그것은 plan 본문 어디에도 적혀 있지 않다. | plan L16 "오늘 23이다" vs L104-110 "오늘 (오탐으로나마) 헤드라인에 뜨던 신호가 M3 이후 0개 표면"; plugins/mccp/scripts/lib/msw-metrics/index.js:114-115 (`startupCount > 50 && !model._priorStartupCount`), lib/renderer/verdict.js:93 (`status === 'invalid'` 가드), lib/msw-metrics/cli.js:326-332 (한 줄 배너 조립) |
| architect | LOW | Task 5가 착지하면 Task 4가 코드로 고정하려는 구분(`sessions` vs `sessions_local`)이 런타임에서 항진명제가 된다. `sessions_local`은 `observed_local` 필터인데(`:403`), `observed_local`은 비공유 디렉토리에서 엔트리가 만들어질 때 즉시 true가 되고(`:211`), Task 5는 공유 디렉토리가 엔트리를 **만들지 못하게** 하므로 남는 모든 엔트리는 항상 local이다. 즉 Task 4의 반증 test(공유 출처 세션이 분자에 안 들어감)는 producer가 더는 만들 수 없는 model을 손으로 주입해야만 red가 되며, 실제 회귀를 잡는 그물이 아니다. | plugins/mccp/scripts/derive/sources/session-activity.js:196-211 (엔트리 생성 + `if (!dirIsShared) ... observed_local = true`) 와 :400-403 (`sessions_local = sessions.filter(s => s.observed_local)`); plan Task 5 L155-158 "`sessions[sessionId]` 엔트리를 만들지 않게 한다" 와 Task 4 L146-152 |
| security | MEDIUM | Task 3의 전제가 거짓이다 — `remediation_pr`은 어떤 producer도 공유 디렉토리에 쓰지 않는다. 따라서 findings.js를 공유 corpus로 넓히면, 그 kind가 그 경로에 존재할 수 있는 유일한 출처는 손편집이거나 이 저장소의 정상 ingress를 거치지 않은 쓰기다 — 즉 C2/C3 귀속 커버리지(`with_remediation_pr`)의 신뢰 경계를 CLI 초크 포인트 밖으로 넓힌다. | plugins/mccp/scripts/state/msw-events.js:254 `A1_AXIS_KINDS = new Set(['task_started','task_completed','task_ship_sealed'])` + :419 `if (!A1_AXIS_KINDS.has(opts.kind)) return { dir: local, shared:false }` — writer는 non-A1 kind를 절대 shared로 보내지 않는다. 마이그레이션도 동일: plugins/mccp/scripts/migrations/msw-events-common-dir.js:453 `if (!evt \|\| !A1_AXIS_KINDS.has(evt.kind)) { report.skipped_non_a1++; return; }`. M1 plan 자신이 이 결정을 명시했다(.claude/plans/orchestrator-step-wiring-m1.plan.md:304 "`evidence_*`·`remediation_pr`도 worktree-local이다"). 그런데 M3 plan:143의 Validate는 "공유 디렉토리에만 있는 `remediation_pr` 레코드" fixture를 손으로 만들어 단언하므로, producer 부재를 test가 구조적으로 못 잡는다. |
| security | MEDIUM | Task 5의 격리 경계가 provenance가 아니라 '읽은 디렉토리'다. 공유 디렉토리에는 **자기 worktree가 쓴** A1 이벤트도 들어가므로, `dirIsShared`로 `sessions[sessionId]` 생성을 막으면 외래 세션뿐 아니라 자기 세션(A1 이벤트만 남긴 세션)도 B2 분모에서 사라진다. plan의 Validate는 '외래 2건이 증가시키지 않는다'와 'A1은 증가한다'만 짝으로 단언하므로 이 손실을 잡는 단언이 없다. | plugins/mccp/scripts/state/msw-events.js:418-423 — 로컬 root의 A1-axis 이벤트가 `commonDirOf(root)` 밑 shared로 라우팅된다(자기 worktree 이벤트도 포함). derive/sources/session-activity.js:114-124 주석도 "마이그레이션이 여러 worktree의 이벤트를 공유 디렉토리 하나로 모으므로"라고 적어 shared=foreign이 아님을 스스로 말한다. plan .claude/plans/orchestrator-step-wiring-m3.plan.md:155-163은 `dirIsShared`를 곧 외래로 취급한다. |
| security | MEDIUM | DD1의 부호 정정은 anti-gaming 가드를 영구 도달 불가로 만든다 — plan이 '삭제하지 않는 이유는 정책 변경이기 때문'이라고 적은 바로 그 정책 변경을 결과적으로 수행한다. 기준선 producer는 DD2가 만들지 않기로 확정했으므로, 수정 후 `unitSpikeFlag`가 발화할 수 있는 입력은 저장소 어디에도 존재하지 않는다(합성 fixture 제외). | plugins/mccp/scripts/lib/msw-metrics/index.js:114가 `model._priorStartupCount`의 유일한 등장이며 쓰기 site는 0곳(grep 전수: 나머지 매치는 plan·backlog·review 문서뿐). plan DD2(:68-71) "기준선을 persist하는 경로는 이 milestone에서 만들지 않는다" + DD1(:64-66) "가드를 삭제하지 않는 이유는 anti-gaming 축을 없애는 것이 정책 변경이기 때문". 보상은 배너의 `spike-guard=dormant` 토큰 하나뿐이며(plan:111-113) 그것은 탐지가 아니라 표기다. |
| test | HIGH | Task 5는 `derive/sources/session-activity.js`를 바꾸는데, 그 파일을 실제로 검증하는 기존 test 3종이 `## Validation` 어느 명령에도 포함되지 않는다 — M1 경계 acceptance 회귀가 그물 밖이다 | plan:246-258의 suite 목록은 `msw-events-path.test.js`만 포함하고 item 2는 `plugins/mccp/scripts/derive/tests/*.test.js`만 돈다. 그러나 session-activity를 require하는 test는 `plugins/mccp/scripts/lib/tests/session-activity.test.js` · `msw-a1-boundary.test.js` · `msw-m8-producers.test.js`(전부 lib/tests/)다. plan:300의 Risk는 'Validation 3(위치 독립성)과 work-command-body.test.js가 회귀 그물'이라 주장하지만 Validation 3은 셸 for-loop 관측이고 M1의 경계 test(`msw-a1-boundary.test.js`)는 실행되지 않는다. |
| test | HIGH | DD3/Task 5의 전제 — '공유 디렉토리 이벤트가 sessions 맵을 만들고 그것이 concurrent_pairs_count(B2 분모)를 낳는다' — 는 인용한 코드가 반증한다. 따라서 Task 5의 Validate는 실제 producer가 만들 수 없는 fixture로만 통과/실패를 가른다 | `state/msw-events.js:254` `A1_AXIS_KINDS = {task_started, task_completed, task_ship_sealed}` + `:419` 비-A1 kind는 local로만 쓰고, migration도 `migrations/msw-events-common-dir.js:453`에서 비-A1을 skip한다. `derive/sources/session-activity.js:335-348`의 `spanOf`는 `session_start` 이벤트가 있어야 span을 내고 :352-369가 그 span에서만 `concurrent_pairs_count`를 올린다. 공유 디렉토리에는 `session_start`가 구조적으로 존재할 수 없으므로 오늘 B2 분모 오염은 0이다. plan:74-76의 'DD8의 격리 주장은 소비처가 실재하는 상태로 거짓이다'는 거짓이며, plan:161-163의 fixture(공유 디렉토리 외래 세션 2건)는 writer/migration이 만들 수 없는 형태다. |
| test | MEDIUM | Task 3(findings.js 공유 corpus 조인)이 읽으려는 `remediation_pr` 레코드는 공유 디렉토리에 존재할 수 없어 dead read이고, 그 Validate 역시 손으로 심은 fixture로만 성립한다 | `derive/sources/findings.js:49`는 `o.kind !== 'remediation_pr'`만 수집한다. `remediation_pr`은 `state/msw-events.js:254`의 A1_AXIS_KINDS에 없고 `:419`가 비-A1 kind를 local dir로 고정하므로 공유 위치에 기록되는 경로가 없다. plan:143-144 'Validate: 공유 디렉토리에만 있는 remediation_pr 레코드가 finding 조인에 나타난다'는 producer가 만들 수 없는 입력을 전제한다. (같은 저장소가 이런 read를 `session-activity.js:373`에서 'dead read'라 부르며 제거한 선례가 있다.) |
| test | MEDIUM | Task 9가 지우는 `escalate_pending`은 이 사이클 자신의 receipt write가 즉시 되살릴 수 있고, acceptance는 그것을 잡을 test/순서 보장을 갖지 않는다 | `receipt/write.js:1213-1234`는 receipt write 직후 detector가 escalate면 `escalate_pending:true`를 다시 쓴다. `escalate-detector.js:57-64` → `receipt-convergence.js:31-35`는 `review_verdict='divergent'`만으로(rounds 무관) escalate를 낸다. plan:207은 M1 receipt가 `review_verdict='divergent'`였다고 기록하고 plan:432는 M3도 같은 multi-agent 경로임을 보인다 — M3 receipt가 divergent면 decision_id=…-m3로 플래그가 재점화되어 plan:321-322의 acceptance(SessionStart 주입 소멸)가 검증 시점에 뒤집힌다. Validation 블록(plan:244-288)에 이 축을 재는 명령이 없다. |
| test | MEDIUM | Task 1이 보존한다고 주장하는 anti-gaming 축('기준선 존재 시 spike 발화')은 production에서 도달 불가한 분기라, 그 test는 계약이 아니라 손으로 주입한 `model._priorStartupCount`만 검증한다 | `msw-metrics/index.js:114`가 읽는 `model._priorStartupCount`는 plan:14-15 자신이 '저장소 전체에서 쓰는 곳이 0곳'이라 실측했고 DD2(plan:68-71)가 producer를 만들지 않기로 못박는다. 따라서 plan:116-118의 두 번째 단언(기준선 10 → invalid)은 어떤 실제 derive 모델도 만들 수 없는 입력에서만 green이며, '가드를 지운 것과 구별된다'는 주장은 fixture 안에서만 참이다. |
| invariant | HIGH | Task 1 + DD2 turn the A1 anti-gaming spike guard into a permanently unreachable branch, and the paired 'guard still fires' assertion pins a code path no production writer can reach — so the test gives assurance the anti-gaming axis is intact when it structurally is not. The deferral is also unledgered: Task 8 enumerates exactly two new backlog entries ('escalation clear 경로', '공유 corpus 읽기 상한') and neither is 'baseline producer absent → spike guard permanently dormant', despite §3.16 requiring explicit deferral. | plugins/mccp/scripts/lib/msw-metrics/index.js:114 `const unitSpikeFlag = startupCount > 50 && !model._priorStartupCount`; repo-wide grep for `_priorStartupCount` returns only this read site plus plan/backlog prose (no writer anywhere). Plan DD2 (:68-71) 'DD2 — 기준선 producer는 만들지 않는다'. Task 1 Validate (:116-118) requires the second assertion via `기준선 10`, an input only a fixture can construct. Task 8 (:193-199) lists only two new backlog rows. |
| invariant | MEDIUM | The plan asserts the isolation invariant 'shared location contributes to the A1 axis only, and the reader enforces it' (DD3) and in the same plan Task 3 wires a second reader to consume the shared corpus for a non-A1 axis (C1 attribution `with_remediation_pr`). The invariant is therefore false on landing, enforced only by per-reader enumeration — the exact failure mode DD4 forbids ('좁히기는 열거가 아니라 경로다'). No task or test asserts the class-level property. | Plan :73-78 'DD3 — 공유 위치는 A1 축에만 기여한다. 그것을 reader에서 강제한다 … M3는 그 주장을 참으로 만든다' vs Task 3 :138-144 '`findings.js`가 … 공유 corpus를 본다 … local + commonDirOf 둘 다 스캔'. The consumer is the attribution axis, not A1: plugins/mccp/scripts/derive/sources/findings.js:35-55 feeds `with_remediation_pr` (:76-77, consumed by C2/C3 attribution at msw-metrics/index.js:90-97). |
| invariant | MEDIUM | Task 9's acceptance criterion for one of the two 'operator surfaces' is unfalsifiable: the amber `fix-task pending escalate` line cannot fire in this repo today because it requires a fix-task.md item, and no fix-task.md exists. The 'before/after' check therefore cannot distinguish 'the escalation was resolved' from 'this surface never fired'. The plan applies the opposite (correct) discipline to Task 1 ('앞만 있으면 가드를 지운 것과 구별되지 않는다') but not here. | plugins/mccp/scripts/lib/renderer/verdict.js:126-129 `if (fixTask && stateItem && stateItem.escalate_pending)`; plugins/mccp/scripts/derive/sources/fix-task.js:7-10 returns `item: null` when the file is absent; `.claude/state/` contains only `fix-task-applied.md` (no `fix-task.md`), while `.claude/state/STATE.md:16-17` does carry `escalate_pending: true`. Plan :224-226 and Acceptance :321-322 assert both surfaces. |
| invariant | MEDIUM | The Gate Record's second entry obtains a fresh review-round budget by re-keying the decision slug after the cap blocked the first entry (EX_BLOCK 3/1). Re-keying is functionally a fresh ledger and is not one of the audited bypasses CLAUDE.md §3.16 sanctions; the resulting receipt seals `rounds_so_far=0`, so the prior cap block leaves no trace in the ledger the ship-time audit reads — only in plan prose that the receipt does not anchor. | Plan :410 '5.2c emit-workflow-args \| **`EX_BLOCK` (exit 12)** — `3/1 for mccp-plan-codex__orchestrator-step-wiring`' and :420-428 '인자를 plan 경로로 주면 슬러그가 orchestrator-step-wiring-m3으로 갈리고 … 그 키의 원장은 0라운드다'; :432 seals `rounds_so_far=0`. CLAUDE.md §3.16: '캡에 걸렸을 때의 정당한 행동은 아래 우회 목록과 같으며(사유를 남긴다), 원장을 지우는 것은 그 목록에 없다.' |
| invariant | LOW | The Risks row for Task 3 cites a mitigation mechanism that does not exist in the code it mitigates: `findings.js` accumulates a Set of `finding_id`, so double counting is structurally impossible and there is no `event_id` dedupe to mirror. A mitigation that names the wrong mechanism cannot be checked at implement time. | Plan :303 'Task 3의 findings 공유 스캔이 중복 계상한다 \| 낮음 \| session-activity.js의 `event_id` dedupe와 같은 형태를 쓴다' vs plugins/mccp/scripts/derive/sources/findings.js:36,51 (`const ids = new Set()` … `ids.add(String(o.finding_id))`). |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | plan이 인용한 file:line을 전부 열어 대조했다 — `msw-metrics/index.js:114-115` spike 가드(부호 주장 TRUE), `:140-152` forward-only mirror(TRUE), `:216-218` localSessions fallback(TRUE), `session-activity.js:110-131`(commonDirOf·토글 미읽기·throw 금지 TRUE), `:205-211` kind 가드 부재 및 observed_local(DD3 주장 TRUE), `work-orchestrator.js:400-416`(주석 '네 필드' + worktree가 safeField를 우회 + reason만 값 가드 TRUE), `findings.js:37` 직접 path.join(TRUE), `receipt/write.js:1240-1252` clear 경로가 같은 decision_id를 요구(TRUE), `renderer/verdict.js:93-96/127`(TRUE), `state-writer.js:459-464` escalate_pending clear가 update() API로 가능한지(가능, Task 9 실행 가능). Task 5가 A1 축을 죽이는지 공격했으나 startedWorkUnits/completedWorkUnits는 sessions 맵과 독립 집합이라 성립하지 않았고, Task 3 dedupe 극성(di>0)도 인용대로였다. 깨진 것은 셋: Task 9 acceptance의 한 축이 이미 vacuous(fix-task.md 부재), Task 1 배너 근거의 오늘 상태 서술이 거짓이며 토큰 발화 조건 미지정, Task 4/5 상호작용으로 sessions_local 구분이 항진명제화. HIGH/CRITICAL은 찾지 못했다. |
| security | fail | 공격한 축: (1) Task 2의 `worktree=` 좁히기 — work-orchestrator.js:415/582에서 `path.basename`이 실제로 `safeField`를 우회함을 확인했고 plan의 서술이 정확했다(절대경로 leak은 basename이라 없음). 반증 실패. (2) Task 9의 STATE.md 편집 — plan이 `state-writer.update()` 경유를 명시하고 직접 편집을 금지해 §3.2 우회 없음. 반증 실패. (3) shared dir 해소의 traversal — `commonDirOf`는 `looksLikeGitDir` 검증 후에만 반환하고 실패는 후보 미추가로 접힌다(session-activity.js:126-131). 반증 실패. (4) `last-halt`의 cross-worktree 읽기가 남의 STATE.md를 배너로 재생하는 경로 — 이미 safeField+mask+quiet parse로 좁혀져 있고 M3가 남은 구멍(worktree)을 닫는다. 반증 실패. (5) Task 3 findings 공유 스캔의 producer 실재성 — A1_AXIS_KINDS 라우팅과 마이그레이션 skip으로 전제가 거짓임을 확인(finding 1). (6) Task 5의 shared=foreign 등식 — writer가 자기 이벤트도 shared로 보냄을 확인(finding 2). (7) DD1 부호 정정 후 가드 도달 가능성 — 기준선 쓰기 site 0곳 + DD2의 producer 미생성으로 영구 dormant 확인(finding 3). |
| test | fail | plan의 전 인용을 원본에서 대조했다: `msw-metrics/index.js:113-115`(spike 가드 부호 — 인용 정확) · `computeA2`의 `samples`가 `sessions`에서 온다는 Task 4 주장(:228 — 정확) · `work-orchestrator.js:400-418`의 `worktree=` safeField 우회와 빈 값 라벨 잔존(:414-416 — 정확) · `receipt/write.js:1240-1252` clear 경로(정확) · `env-contract/lint.js` L12 존재(정확) · `msw-metrics.test.js`가 실제로 `cli.js`를 spawn하는지(:872 — 확인, Task 1 배너 단언 가능). 반증에 실패한 축은 위 다섯을 제외한 전부다. 실제로 무너진 것은 (a) Task 5·3의 공유 corpus 오염 전제가 writer/migration의 kind 필터와 `spanOf`의 `session_start` 요구로 도달 불가라는 점, (b) Task 5가 고치는 파일의 기존 test 3종이 Validate 명령에 없다는 점, (c) escalation 해소가 같은 사이클 receipt writer와 경쟁한다는 점이다. |
| invariant | fail | Traced the A1 spike guard to its only read site and grepped repo-wide for a `_priorStartupCount` writer (zero) to test whether Task 1's sign-flip degrades a firing gate into a permanent no-op; checked whether Task 8 ledgers that deferral (it does not). Verified every cited line: index.js:114/140-152, verdict.js:93-101 and :127, state-injector.js:141-155, receipt/write.js:1240-1252, session-activity.js:110-131/196-212, findings.js:35-55, PRD milestone table. Checked the direction of the default on the escalation clear (data-only clear vs structurally unreachable code path) and looked for a third consumer keyed on fix-task.md presence — state-resumption.js:138 is gated on handoff_spawn, not escalate_pending, so no dispatch-side erosion; instead the disk check showed the verdict surface cannot fire at all, making one acceptance box unfalsifiable. Attacked the round-cap re-keying in the Gate Record against §3.16's enumerated legitimate responses. Attacked Task 5 for the A1-kills-itself case and found the plan already pairs the assertion (B2 denominator invariant AND A1 numerator increase), so I did not raise it. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 282770,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:ce77c9caf2e31ad6882147d500b4384575b5cb6d56c3420ae5367e6aef3c44c9",
  "plan_path": ".claude/plans/orchestrator-step-wiring-m3.plan.md",
  "recorded_at": "2026-09-07T08:21:48.483Z"
}
```
