# Plan Review Panel — meta-research-command

**Plan**: `.claude/plans/meta-research-command-m1.plan.md` · **Plan version**: sha256:2517deaea002817abe48cd3d6a72ea9b36f5d8c61ab38670973eddb169f8766b
**Verdict**: divergent via multi-agent
**Quorum**: 4/3 responses · 4 roles (of 4) — passed=false
**Reason**: L2 quorum not satisfied: 5 blocking finding(s): architect/HIGH, architect/FAIL, invariant/HIGH, invariant/HIGH
**Round**: R6 — R1(9) → R2(3) → R3(10) → R4(5) → R5(5) → R6(8). security·test pass.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | L3 test fixture specification is inconsistent with code specification. Code spec (line 147-150) describes 5 separate lexical screen axes; fixture spec (line 194) specifies only 4 cases, combining drive and UNC into one. This either leaves a test gap or provides inadequate coverage of two distinct attack vectors. | Line 147-150: 'lexical screen (5축) — `\0` 포함 · 절대경로 · 드라이브 문자 · UNC · `..` 포함'. Line 194: 'lexical 4: `..` · 절대경로 · 드라이브/UNC · `\0` 포함 → 전부 `REF_OUTSIDE_REPO`. existsSync 1...시점 1... Total: L3 6' |
| architect | HIGH | Missing negative test case for symlink escape check. Plan states at line 150 that assertContained catches 'symlink escape' (threat invisible to lexical screen). However, test fixture has only positive regression test (line 197: PATH_ESCAPES_GATE does not occur), no case where symlink escape should trigger the error. | Line 150: 'assertContained이 잡는 것은 lexical screen이 볼 수 없는 symlink escape 하나뿐이다'. Line 197: 'PATH_ESCAPES_GATE가 발생하지 않음을 단언한다'. Fixture spec section (lines 189-199) lists no negative test for symlink escape condition. |
| test | MEDIUM | Reference cells must have backticks stripped before path existence is checked (L3 validation) | Plan line 146: 'strip이 없으면...L3가 전건 실패한다' + line 196 positive fixtures: only 2 cases listed (plain reference + line suffix), neither explicitly testing backtick removal |
| test | LOW | Command structure test must verify Phase 0~4 headings exist in order and Phase 4 calls both register and lint functions | Plan line 187-188 and 213: require 'Phase 0~4 heading 5개가 정의된 순서대로 존재' and 'Phase 4 블록이 register/lint를 둘 다 호출' via '정적 단언' but implementation approach (regex vs parser vs other) is not detailed |
| invariant | HIGH | Task 6 ordering dependency is stated as 'invariant LOW' prerequisite but not mechanically enforced in lib code | Plan line 217: 'Task 6은 Task 2·3의 실 repo 검증보다 먼저 완료돼야 한다(invariant LOW 흡수)'. However, enforcement is only in: (1) bash comment line 242-244 (documentation), (2) test fixture pre-construction line 181 (which passes regardless), (3) runtime register exit at line 134-135 (fail-closed but not preventive). The lib code itself (`meta-research.js`) will not be written with an upfront guard to require README `## 색인` table. Current `.claude/_meta/README.md` has no `## 색인` section, confirming this dependency. |
| invariant | MEDIUM | Unit test acceptance criterion (line 276) does not prove Task 6 is required; real-repo criterion (line 278) does, creating asymmetric verification | Line 276 acceptance: unit test passes with tmp fixture that pre-constructs `## 색인` (line 181). Line 278 acceptance: real repo lint asserts `ok:true` · `exempt[]` length 5. Only the second criterion proves Task 6 completion. A developer reading just line 276 may believe unit-test-green is sufficient, but Task 6 must be done before line 278 can pass. This asymmetry is not made explicit in the acceptance checklist order. |
| invariant | MEDIUM | Task 2 register function silently succeeds even if README lacks `## 색인` section; failure only occurs at runtime during lint, leaving partially-modified README | Line 134: 'Task 2가 고정한 `## 색인` 표가 없으면 exit 1'. But register does not validate README structure upfront; it reads README (line 133), appends a row with atomic rename (line 135-136). If `## 색인` is missing, the function looks for heading with grep/regex and exits 1. However, this is discovered *during execution*, not before write. The plan describes this as fail-closed (exit 1 prevents corruption), but there is no pre-flight check that would allow graceful skip-or-error before attempting write. |
| invariant | HIGH | L4 (README index check) applies to all .md files in _meta/, but current README has no `## 색인` table, so real-repo regression test (line 198) cannot pass until Task 6 creates it | Plan line 151-152: 'L1·L4는 _meta/의 모든 .md(README 제외)에 적용'. Line 198 real repo regression: 'lint --all을 돌리고 ok:true · exempt 길이 5를 단언'. Current `.claude/_meta/README.md` grep for '색인' returns no results. All 5 existing legacy documents would fail L4 (NOT_INDEXED) until Task 6 backfills them into a new `## 색인` table. This means the test at line 198 is data-dependent on Task 6 completion, but the test itself (T0, line 179-200) is described as working with 'tmp fixture' that doesn't require Task 6. |
| invariant | MEDIUM | Lint validation (line 222) in Task 6 section asserts `exempt[]` 길이 5, but this assertion is in the 'Validation' section describing *how to verify the plan works*, not asserting that the plan's acceptance gate enforces it | Line 222: '3. `node plugins/mccp/scripts/lib/meta-research.js lint --all --json` → `ok:true` · `exempt[]` 길이 5'. This appears under Task 6 validation, but in the acceptance checklist (line 278) it's listed as 'lint --all --json → ok:true · exempt[] 길이 5 (legacy 열거, 조용한 면제 아님)'. The acceptance criteria don't explicitly order Task 6 before running this lint. However, the lint will fail (NOT_INDEXED violations on all 5 legacy docs) if Task 6's README table doesn't exist. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified path-containment.js signature (2-arg vs 3-arg calling convention); confirmed markdown-table module exists and mirrors the pattern claimed; checked archive-complete.md and impeccable-detect.js patterns cited as mirrors; extracted and cross-checked code specification (5 lexical axes) against test fixture specification (4 cases, one combining two axes); searched for all assertContained security test cases in fixture specification—found positive regression only, zero negative cases for symlink escape despite plan explicitly naming it as a catchable threat. |
| security | pass | Attacked path traversal in scaffold (--slug, --date allowlists + assertContained usage); Premises reference validation order and 3-step validation layers; README table injection via pipes in metadata; symlink-escape via realpath checks; atomic write race conditions. Verified against existing patterns in path-containment.js and pr-phase-lock.js. Test specification includes regression test for lexical-screen-first order to prevent error-code spoofing. No refutation found: strict allowlists prevent traversal characters before FS ops, realpath-anchored checks catch symlink escapes, 2-arg assertCon |
| test | pass | Examined test coverage against core claims: (1) procedure reproducibility — T0 round-trip + command structure test specified, (2) format compliance — L1/L2/L3/L4 lint checks specified with 23 negative fixtures + 2 positive, (3) index reachability — L4 lint test specified, (4) premise disclosure — L3 path validation + timestamp format checks specified, (5) receipt exclusion — schema.js unchanged verified. Inspected positive fixture count (2) against claimed coverage: backtick stripping claimed mandatory but neither fixture explicitly tests it (implicit via T0 if it fails). Inspected command str |
| invariant | fail | Attacked plan's key invariants: (1) Task 6 ordering enforcement — searched for mechanical guards in lib code paths, found only bash-script documentation and test-fixture pre-construction; (2) gate skip-predicate — verified that without `## 색인` table (not yet created), register command would fail at runtime but allow partial execution; (3) test isolation vs real-repo dependencies — confirmed T0 unit test fixture pre-constructs table while current repo's `.claude/_meta/README.md` has no `## 색인` section; (4) acceptance criteria sequencing — checked that line 276 (unit test green) is listed before |
