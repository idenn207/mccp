# Plan Review Panel — review-record-linkage-m7b

**Plan**: `.claude/plans/review-record-linkage-m7.plan.md` · **Plan version**: `sha256:c27c5a0068b89f6ef0a569df59def8614762a8c6752c1405c1de117654daeb2a`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 12 blocking finding(s): architect/HIGH, architect/HIGH, architect/HIGH, architect/FAIL — MCCP_REVIEW_SINGLE_PASS=deadline_pressure 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | 플랜의 핵심 경계 불변식(‘브랜치 = plan 파일명 = plan receipt 슬러그 = ship 슬러그’)이 이미 깨져 있는데 본문이 그 상태로 재제출됐다. 원장 실측: `mccp-plan-codex__review-record-linkage-m7.json`은 rounds index 0/1/2 = **3라운드 소진**이고 `review-single-pass.js:45` `MAX_ROUND_CAP = 3`이라 이 슬러그로는 라운드가 더 열리지 않는다. 동시에 오늘 07:03에 생성된 **다른 슬러그** 원장 `mccp-plan-codex__review-record-linkage-m7b.json`(index 0, emitted)이 실재한다. 즉 현 사이클은 (a) m7b로 진입했거나 (b) 아예 진입 불가인데, (a)이면 plan receipt는 `review-record-linkage-m7b.json`으로 떨어지고 브랜치(`review-record-linkage-m7`)에서 파생되는 ship 슬러그·F10의 `SHIP_PLAN_PATH=.claude/plans/${DECISION_SLUG}.plan.md`·F15의 `meta.plan_path` 앵커가 전부 어긋난다 — DD7이 스스로 ‘M5가 실측한 HALT’라 부른 형태의 재현이다. 플랜은 UI2·UI4·DD7·Task 0 축 2·Acceptance 5곳에서 여전히 `review-record-linkage-m7`을 단일 정체성으로 못박는다. | .claude/state/review-rounds/mccp-plan-codex__review-record-linkage-m7.json:5-24 (3 rounds) · .claude/state/review-rounds/mccp-plan-codex__review-record-linkage-m7b.json:5-12 (2026-09-08T07:03:14Z) · plugins/mccp/scripts/lib/review-single-pass.js:45 `const MAX_ROUND_CAP = 3;` · plan:24,26,190-213,384-385,557-560 |
| architect | HIGH | Task 0 축 2는 fail-closed 진입 게이트인데 그 통과 조건이 플랜 자신이 거짓이라고 선언한 명제다. 축 2는 “슬러그가 `review-record-linkage-m7`이고 `rounds_so_far`가 `0`이어야 한다. 다른 값이면 인자가 틀린 것이다 — 캡을 올려 우회하지 않는다”를 요구하는데, 같은 파일의 Cycle Outcome이 “F8 · DD7 · Task 0 축 2의 `rounds_so_far: 0`은 거짓이다. 그리고 그 결과 acceptance 도달 경로가 plan 자신의 제약(캡 상향 금지 · 원장 삭제 금지) 안에 존재하지 않는다”고 적는다. 즉 이 플랜은 자기 preflight에서 확정적으로 HALT하도록 쓰여 있고, 그 모순을 해소하는 편집(슬러그 재정의 · UI2/UI4 재판정)이 본문에 반영되지 않았다. PRD도 같은 것을 요구했다 — “다음 사이클은 게이트에 진입하기 전에 그 셋을 먼저 정해야 한다”. | plan:376-385 (Task 0 축 2) vs plan:335-336 (Cycle Outcome 3) · .claude/prds/review-record-linkage.prd.md:156-160 |
| architect | HIGH | Risks 표 1행(‘세션이 캐시 본문을 돌아 라운드만 소진한다’)의 유일한 완화인 DD10 게이트가 **소유 파일 없이** 명세돼 있다. 그 검사는 `commands/plan.md`의 5.2 블록 안(5.2b 이전)에 놓여야 하는데 `## Files to Change` 9행에 `plugins/mccp/commands/plan.md`가 없고 `## Validation` 7항목 어디에도 그 검사가 없다. 게다가 플랜 자신이 그 게이트가 양방향 모두 통과하는 상수-참임을 인정한다(캐시 본문은 `plan-path`를 purge하지도 않아 잔여 파일이 통과, 워크트리 본문은 purge와 write가 같은 블록이라 동어반복). 즉 완화가 코드도 검증도 갖지 못한 산문이며, 추상화(‘env로 위조되지 않는 관측 가능한 게이트’)가 주장한 불변식을 담지 못한다. | plan:75-85 (Files to Change — commands/plan.md 부재) · plan:500-537 (Validation — plan-path 검사 부재) · plan:328-334 (Cycle Outcome 1·2) · plan:543 (Risks 1행 mitigation) |
| architect | MEDIUM | ‘관측된 사실’ 표의 F8이 현재 원장과 불일치한다. F8은 `review-record-linkage-m7`의 `rounds_so_far:0`을 실측 사실로 싣지만 원장은 3이다. 사실 표는 이 플랜의 여러 결정(DD7 진입 인자 선택, Risks 2행의 ‘진입 전 해소’)이 서 있는 토대이므로, 재측정 없이 옮겨진 표가 하류 판단을 오염시킨다. | plan:50 (F8 `review-record-linkage-m7`은 `rounds_so_far:0`) vs .claude/state/review-rounds/mccp-plan-codex__review-record-linkage-m7.json:5-24 |
| security | MEDIUM | M7의 유일한 acceptance 오라클(`--check-live-linkage` exit 0)의 신뢰 근원이 hash-미검증·untracked 파일이다. DD8은 '네 검사 전부 HEAD 트리'로 fail-open을 닫았다고 주장하지만, HEAD에 커밋된 ship receipt의 `meta.review_record_path`·`meta.plan_review_expected` 자체가 작업 트리에만 있는 상류 plan receipt에서 carry-forward된 값이다. 코드가 그 사실을 명시한다. | plugins/mccp/scripts/lib/pr-phase-helpers/finalize-receipt.js:315-317 — "The upstream receipt is working-tree-only and hash-unverified". 그 상류 receipt는 `/mccp:receipt-write`로 수동 발행 가능하고(CLAUDE.md §3.3 복구 옵션 4), `resolution.review_source='multi-agent'`만 있으면 :324가 `--plan-review-expected=true`를 붙인다. 즉 plan.md:557의 exit 0은 '패널이 실제로 발화했다'가 아니라 '어떤 작업 트리 파일이 그렇게 말했다'만 증거한다. plan은 DD8/DD9에서 이 비대칭을 어디서도 다루지 않는다. |
| security | MEDIUM | Task 0 Validate가 홈 절대경로를 git-tracked 보고서로 흘리는 것을 산문 의무로만 막는다 — 이 저장소가 이미 절대경로 유출로 sanctioned 재봉인을 치른 축이다. | plan.md:356 `sha256sum ~/.claude/plugins/installed_plugins.json`은 셸이 `~`를 확장하므로 출력 두 번째 칼럼이 `/home/<user>/...`다. 완화는 plan.md:389-391의 "홈 경로는 `~`로 축약한다"뿐이고 기계 검사가 없다(`## Validation` 1번은 같은 명령을 축약 없이 그대로 싣는다). 대조: `linkage-audit.js:236-237`은 같은 위험을 코드 수준에서 `err.message` 미탑재로 닫았다 — plan은 그 패턴을 미러하지 않는다. |
| test | HIGH | Task 0 축 2의 통과 조건(`rounds_so_far`가 0)은 지금 결정적으로 거짓이라 이 plan의 preflight는 어떤 실행에서도 통과할 수 없다 — 즉 acceptance 체인(Task 1→4→exit 0) 전체가 도달 불가인데, Validation 블록은 여전히 `=> rounds_so_far: 0`을 기대값으로 못박고 있다 | plan:384 "슬러그가 `review-record-linkage-m7`이고 `rounds_so_far`가 `0`이어야 한다" · plan:509 "# => rounds_so_far: 0" · 실측 `.claude/state/review-rounds/mccp-plan-codex__review-record-linkage-m7.json:5-24` = rounds 3건(index 0/1/2, 전부 `emitted`). plan 자신도 이를 인정한다 — plan:335 "F8 · DD7 · Task 0 축 2의 `rounds_so_far: 0`은 거짓이다". 사실(F8:50)과 Task/Validation은 정정되지 않은 채 남았다 |
| test | HIGH | 슬러그 축의 acceptance가 실제 게이트 실행과 어긋난다 — 이번 사이클의 plan 게이트는 `review-record-linkage-m7b` 슬러그로 발화 중인데, Task 1 Validate와 Acceptance는 receipt 파일명이 `review-record-linkage-m7.json`이어야 한다고 단언한다. 그 단언은 실행 중인 구성에서 반드시 실패한다 | `.claude/state/review-rounds/mccp-plan-codex__review-record-linkage-m7b.json:4-11` (decision_id `review-record-linkage-m7b`, 2026-09-08T07:03 발화) 대 plan:418 "파일명이 `review-record-linkage-m7.json`이 아니면 축 2로 돌아간다" · plan:558 Acceptance "`mccp-plan-codex/review-record-linkage-m7.json`이 …봉인" · plan:24 UI2 |
| test | HIGH | Risks 표 1행(이 마일스톤의 지배적 실패)의 유일한 완화인 DD10 게이트는 plan 자신이 판별력 0으로 반증했는데도 Task 0·Risks에서 제거되지 않았고, 그것을 검증할 test·Validate 명령·착지 파일이 어디에도 없다 — 반증 불가한 완화가 그대로 남아 있다 | plan:328-332 "DD10의 대체 게이트는 판별력이 0이다 … 양방향 모두 통과한다" · plan:333-334 "`## Files to Change`에 `plugins/mccp/commands/plan.md`가 없고 `## Validation`에도 그 검사가 없다 — Risks 표 1행의 유일한 완화가 산문이다". 그럼에도 plan:358-370(Task 0 축 1)과 plan:543(Risks 1행)은 그 검사를 여전히 실제 게이트로 서술한다. `## Files to Change`(plan:77-85)에 `commands/plan.md` 부재 확인 |
| test | MEDIUM | Risks 표가 명명한 완화 test("강제 뷰가 `linkage-audit --json`과 갈린다 → test가 두 경로의 값 일치를 단언")가 Task 2의 필수 fixture 4종에도 Validation에도 없다 — 주장에 대응하는 test가 존재하지 않는다 | plan:549 "test가 두 경로의 값 일치를 단언" 대 plan:449-456 필수 fixture 4개(degraded·unresolved·과다승인 반증·코퍼스 혼합 반증)에 일치 단언 없음 · plan:518-523 Validation 3의 test 목록에도 없음. plan:273 스스로 test#3("Risks가 지목한 test가 Validate에 없음")을 backlog 이연으로 기록 |
| test | MEDIUM | Task 3(문서 정정)의 Validate 라인에 실행 가능한 명령이 없어 그 task의 변경을 exercise하는 검증이 부재하다 | plan:466 "**Validate**: 문서가 인용하는 file:line이 실재 (…)" — 명령 없음. plan:273-274가 test#4("Task 3 Validate에 명령 없음")를 이연으로 인정 |
| invariant | CRITICAL | The plan's decision identity is unanchored: the gate currently sealing this plan runs under decision slug `review-record-linkage-m7b`, while every anchoring assertion in the plan body names `review-record-linkage-m7`. The plan receipt will land at `mccp-plan-codex/review-record-linkage-m7b.json`, but `/mccp:pr` derives its slug from the branch (`review-record-linkage-m7`) and 2.5.9 does the chain lookup **by slug** — exactly the HALT the plan itself documents as the M5 failure. | `.claude/state/review-rounds/mccp-plan-codex__review-record-linkage-m7b.json:4` `"decision_id": "review-record-linkage-m7b"` with one `emitted` round at 2026-09-08T07:03Z (this run); branch is `review-record-linkage-m7`. Plan DD7 states the failure mode verbatim: "설령 1을 캡 상향으로 뚫어도 receipt는 `mccp-plan-codex/review-record-linkage.json`에 떨어지는데 `/mccp:pr` 2.5.9는 `review-record-linkage-m7`을 조회한다. M5 사이클이 실측한 HALT가 그 형태다." (plan:203-205). PRD:158-160 required this exact decision (UI2/UI4/DD7) be settled **before** entering the gate; the plan body was not updated. |
| invariant | HIGH | Task 0 축 2 is a fail-closed preflight whose stated pass condition is now unsatisfiable, and the plan's own fact table (F8) asserts the opposite of the ledger. A guard whose pass condition can never be met is either a guaranteed HALT (if executed) or dead prose (if skipped) — either way it stops nothing. | Plan F8 (line 50): "`review-record-linkage-m7`은 `rounds_so_far:0`"; Task 0 축 2 (plan:383-385): "슬러그가 `review-record-linkage-m7`이고 `rounds_so_far`가 `0`이어야 한다." Actual ledger `.claude/state/review-rounds/mccp-plan-codex__review-record-linkage-m7.json:5-24` holds three `emitted` rounds, and `plugins/mccp/scripts/lib/review-single-pass.js:45` `MAX_ROUND_CAP = 3` makes it terminally exhausted (PRD:151-156). The plan admits this in Cycle Outcome #3 (plan:335-336) but leaves F8, Task 0 축 2 and the Risks row unchanged. |
| invariant | HIGH | DD10's replacement gate is admitted to have zero discriminating power in both directions, yet the Risks table still names it as the sole mitigation for the milestone's stated dominant risk, and it has no landing site — `plugins/mccp/commands/plan.md` appears neither in `## Files to Change` nor in `## Validation`. This is the canonical 'gate that still looks like a gate'. | Plan Cycle Outcome #1 (plan:328-332): "DD10의 대체 게이트는 판별력이 0이다 … 양방향 모두 통과한다. DD10은 상수-참 검사를 다른 상수-참 검사로 바꾼 것이다"; #2 (plan:333-334): "`## Files to Change`에 `plugins/mccp/commands/plan.md`가 없고 `## Validation`에도 그 검사가 없다 — Risks 표 1행의 유일한 완화가 산문이다." Risks row 1 (plan:543) still reads "실제 게이트는 … `REVIEW_DIR/plan-path` 실재 확인". PRD D4 (PRD:162-168) forbids carrying it forward: "그대로 옮기는 것은 선택지가 아니다." |
| invariant | MEDIUM | The plan's acceptance is pinned to a literal receipt filename derived from the branch, but nothing in the plan pins the branch↔plan-filename↔slug coupling, so the only sanctioned budget recovery (a new decision slug, per PRD:156) silently invalidates acceptance rather than tripping a check. | Acceptance (plan:557-561) requires `mccp-pr-codex/review-record-linkage-m7.json` and `mccp-plan-codex/review-record-linkage-m7.json`; F10 (plan:52) notes `SHIP_PLAN_PATH="${PR_PLAN_PATH:-.claude/plans/${DECISION_SLUG}.plan.md}"` (`plugins/mccp/commands/pr.md:923`), and anchor resolution matches on `meta.plan_path` string equality (`finalize-receipt.js:289-303`). No Validation step asserts branch/slug/plan-filename agreement; Validation step 1 hardcodes `--args ".claude/plans/review-record-linkage-m7.plan.md"` (plan:507). |
| invariant | LOW | Task 0's Validate pins a literal sha256 of a host-local file as a pass condition, with no rollback/refresh story; and the cap escalations (1→2→3) recorded in Review Rounds have no restoration step pinned anywhere. | Plan:390-391 "완주 전 sha256이 `26925fd8b72568ea12712e023985445c97567dff129c1f6bbdd8e4a21cab16fc`와 일치"; Review Rounds (plan:288, 295-299) records raising `MCCP_GATE_ROUND_CAP` to 2 then 3, while `.claude/settings.json:12` declares `"MCCP_GATE_ROUND_CAP": "1"` and no task restores or verifies it. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | DD8의 인용(`linkage-audit.js:621-628` fail-open 서술)과 DD3가 모방한다는 `CHECK_EXIT_CODES`(`linkage-audit.js:112-118`, `STATE_EXIT_CODES`와 분리)를 열어 대조했다 — 둘 다 플랜이 주장하는 그대로였고 별도 종료코드 표를 두는 결정은 근거가 실재한다. DD4(정의를 재정의하지 않고 호출)와 Task 2의 4검사가 전역 집계를 acceptance로 쓰지 않는다는 서술도 결함을 찾지 못했다. `MAX_ROUND_CAP`(review-single-pass.js:45)을 확인해 플랜의 캡 서술이 참임을 검증했고, `.claude/state/review-rounds/` 원장을 전수로 훑어 슬러그 정체성 축을 공격했다 — 여기서 m7 3/3 소진과 오늘자 m7b 원장이 나왔고, 그것이 플랜의 단일-슬러그 경계 불변식·Task 0 축 2 preflight·Acceptance 세 곳을 동시에 무너뜨린다. Files to Change / Validation을 DD10의 착지처와 대조해 소유 파일 부재도 확인했다. |
| security | pass | 공격 시도: (1) `--decision <slug>`가 `git show`/경로 조합에 도달하는 ref·traversal 주입 — `linkage-audit.js:239-267`의 `REF_SHAPE` + `--end-of-options` 이중 가드와 `git()`의 argv 배열(shell 미경유)로 막혀 있고 plan은 그 표면을 넓히지 않는다. (2) 봉인된 `meta.review_record_path`로 임의 파일을 여는 경로 — `computeLinkage`(:383-389)가 파일을 열지 않고 스캔된 맵 조회만 하며(:310-313), carry-forward 지점도 `isRepoRelativePath` + `.claude/reviews/` prefix로 거른다(finalize-receipt.js:311). (3) 새 receipt 필드로 인한 hash/cwd 유출 재개 — plan은 receipt 필드를 하나도 추가하지 않는다. (4) DD4의 hash 동등 비교가 stale hash를 통과시키는지 — `:394-401`이 실제 동등을 강제하고 `stale_receipt_hash`로 분리 계수한다. 이 넷은 모두 랜딩하지 못했다. 랜딩한 둘은 위 findings이며 HIGH 이상은 아니라고 판단했다(단일 신뢰 사용자 위협모델 + 유출은 재봉인 없이 정정 가능). |
| test | fail | plan과 PRD 전문을 읽고, (1) F8/Task 0 축 2의 `rounds_so_far:0` 주장을 실제 원장 파일로 대조해 반증했고(3라운드 소진, 게다가 현재 발화는 m7b 슬러그), (2) Task 1/Acceptance의 receipt 파일명 단언을 실행 중 슬러그와 대조했으며, (3) DD10 게이트의 판별력 주장에 대응하는 test·Validate·착지 파일 존재 여부를 `## Files to Change`·`## Validation`에서 확인해 전부 부재임을 확인했고, (4) Risks 표가 명명한 완화 test들을 Task 2 필수 fixture 4종·Validation 목록과 대조했으며, (5) `linkage-audit.js:109-133`의 `CHECK_EXIT_CODES`/`STATE_EXIT_CODES` 분리와 기존 `linkage-audit.test.js`의 `--check-round-structure`·`--frozen-only` 커버리지를 읽어 DD3·Task 5 인용이 정확한지 확인했다(그 두 인용은 정확했고 반증하지 못했다). |
| invariant | fail | Attacked (1) decision anchoring: compared the plan's asserted slug against the live round ledgers and the branch, finding the gate is firing under `-m7b` while every acceptance/preflight names `-m7`; traced `finalize-receipt.js:281-330` (plan_path matching, slug-independent) vs `/mccp:pr` 2.5.9 slug-keyed chain lookup. (2) Skip/preflight predicates: verified F8's `rounds_so_far:0` against `.claude/state/review-rounds/mccp-plan-codex__review-record-linkage-m7.json` (3 emitted) and `review-single-pass.js:45` MAX_ROUND_CAP=3 — the fail-closed preflight is unsatisfiable. (3) DD10's `REVIEW_DIR/plan-path` gate: confirmed the plan's own Cycle Outcome and PRD D4 declare it constant-true in both directions with no landing site in Files to Change/Validation. (4) Direction of defaults in the proposed `--check-live-linkage` exit table (DD3/DD8): the HEAD-only corpus rule and `degraded`(2)/`unresolved`(3) split are correct fail-closed choices with named fixtures — I could not break these. (5) DD4 (hash equality, not existence) and the present-only/no-rehash constraints check out against §3.12 and the cited join_note. (6) Rollback: cap escalation and the pinned sha256 have no restoration/verification step. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 285001,
  "halt_stage": null,
  "backlog_appended": 12,
  "backlog_skipped_nonblocking": 7,
  "granted": 4,
  "reviewed_plan_hash": "sha256:c27c5a0068b89f6ef0a569df59def8614762a8c6752c1405c1de117654daeb2a",
  "plan_path": ".claude/plans/review-record-linkage-m7.plan.md",
  "receipt_hash": null,
  "recorded_at": "2026-09-08T07:06:58.091Z",
  "rounds": 1
}
```
