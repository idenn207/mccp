# W11 rubric — axis K M2 post-patch re-measurement

**Source rubric**: `.claude/audit/v1.0.0-fallback-ux.md` (plan v0.5 §UX Decision-Type Rubric)
**Subject row**: W4 4d — `/mccp:pr` re-invocation with orphan `pr-phase.lock` (dead PID, subphase=codex-review)
**Pre-patch state**: Type **E (5)** + NS **5** — single-row STOP_RELEASE trigger
  - Original entry: `.claude/audit/v1.0.0-receipts.md:33` ("OK (subphase off) / **FAIL (subphase on, self-trap)**")
  - BLOCKING tally: `.claude/audit/v1.0.0-release-verification-verdict.md` W-VERDICT §2 (1 row, Linux/macOS env-conditional)
  - Pre-patch rubric: `.claude/audit/v1.0.0-receipts.md:52` ("4d | E | 5 | self-trap 시 free-form debug 필요 (Bash deny 우회 경로 발견까지 사용자가 hook source 읽어야)")
**Post-patch evidence**: GitHub Actions workflow `axis-k-m2-cross-platform.yml` matrix run (TBD — fill on first PR run)
**Source commit**: `65d4c02` (axis K1+K2 ship, PR #24) + this M2 verification PR (TBD — fill on merge)

## Methodology

1. M1(axis K1) introduced `lockActive()` PID-liveness branch in `pr-phase-guard.js`. M1's unit tests (`pr-phase-guard.test.js` axes 11.1–11.5 + 12.1–12.4) verify the reclaim logic on a single Node process (Windows-host runtime).
2. M2 promotes that verification to a *cross-platform public audit record* via:
   - `axis-k-m2-reproduce.mjs` — deterministic fixture, real `pr-phase-lock` module + facade-overridden `repoRoot` (skip git-traversal). Same code path on every OS.
   - `axis-k-m2-windows-regression.mjs` — static parse of `hooks.json`, asserts no `PowerShell` substring in any PreToolUse matcher (Windows escape path preserved).
   - GitHub Actions matrix (`ubuntu-latest` + `macos-latest`) — uploads fixture stdout as artifacts (`axis-k-m2-reproduce-<os>.json` + `axis-k-m2-windows-regression-<os>.json`).
3. Re-measurement: 4d row re-scored independently per OS using the same T/NS rubric (`v1.0.0-fallback-ux.md` §UX Decision-Type Rubric).

## Re-measurement (per OS)

### ubuntu-latest

**Captured (GHA run TBD):**

- **T (UX Decision-Type)**: TBD — predicted `B (2)`. Rationale: user is not required to perceive the dead PID; the hook silently reclaims via `lockActive` → `tryReclaimStaleLock` → marker write, then `finalize-receipt` stamps `meta.pr_phase_lock_stale_reclaimed_at_hook=true` on the next PR receipt. Decision surface = post-hoc audit trail only. No free-form debug, no Bash deny escape required.
- **NS (Next-step 명료성)**: TBD — predicted `1`. Rationale: next step is `/mccp:pr` itself (re-invocation). Hook resolves lock-absent, downstream phases proceed without additional command.
- **Severity**: TBD — predicted `none` (PASS) or `LOW` (cosmetic stderr line phrasing).
- **Raw fixture stdout**: TBD (paste `axis-k-m2-reproduce-ubuntu-latest.json` body)

### macos-latest

**Captured (GHA run TBD):**

- **T (UX Decision-Type)**: TBD — predicted `B (2)`, same rationale as ubuntu (reclaim path is cross-platform, only `isPidAlive` syscall surface differs).
- **NS (Next-step 명료성)**: TBD — predicted `1`.
- **Severity**: TBD — predicted `none` or `LOW`.
- **Raw fixture stdout**: TBD (paste `axis-k-m2-reproduce-macos-latest.json` body)

### Windows-regression (static, exercised on both Linux + macOS runners)

**Captured (GHA run TBD):**

- `powershell_matched: false` (expected) — hooks.json PreToolUse matchers contain no `PowerShell` substring.
- `bash_matched: true` (expected) — hook system wiring intact.
- `pretool_count`: TBD (currently 10 in working tree).

This is OS-independent — runners parse the same hooks.json file. The static check is run on both matrix legs to keep the audit artifact symmetric.

## Delta

| Axis | Pre-patch (4d) | ubuntu (post-patch) | macos (post-patch) | Δ |
|---|---|---|---|---|
| T | 5.0 (Type E) | TBD | TBD | -TBD ✓ |
| NS | 5.0 | TBD | TBD | -TBD ✓ |
| Severity | BLOCKING | TBD | TBD | recovered |

## Verdict

- **PRD §Success Metrics row 1** (Linux/macOS reproduction PASS): TBD — gated on GHA matrix `verify` job × 2 OS green.
- **PRD §Success Metrics row 2** (W11 4d row Type ≤C/NS ≤2): TBD — gated on T/NS re-measurement above.
- **PRD §Success Metrics row 3** (Windows regression 0): TBD — gated on `windows-regression` fixture `powershell_matched:false`.
- **PRD §Success Metrics row 4** (F11 schema 무손상): TBD — gated on `pr-phase-lock-f11.test.js` 15/15 PASS on both OS (Windows baseline confirmed 15/15 on 2026-06-15T11:20Z).
- **W-VERDICT §2 BLOCKING tally**: pre-M2 = 1 (4d self-trap). post-M2 (if all above PASS) = **0** — single-row STOP_RELEASE source closed.

## Linked artifacts

- GHA run URL: TBD
- artifact `axis-k-m2-ubuntu-latest`: TBD
- artifact `axis-k-m2-macos-latest`: TBD
- Source PRD: `.claude/prds/v1-0-1-axis-k-pr-phase-guard-pid-alive.prd.md` (M2 row)
- Source plan: `.claude/plans/v1-0-1-axis-k-m2-cross-platform-verification.plan.md`
- M1 ship commit: `65d4c02` (axis K1+K2)
- Sibling unit-test axes (M1 baseline): `plugins/mccp/scripts/hooks/tests/pr-phase-guard.test.js` axes 11.1–11.5 + 12.1–12.4

## Re-measurement update procedure (run after GHA matrix green)

1. Download artifacts: `gh run download <run-id> --name axis-k-m2-ubuntu-latest` + `--name axis-k-m2-macos-latest`.
2. Paste each fixture's stdout JSON into the corresponding **Raw fixture stdout** row.
3. Score T + NS per OS using `v1.0.0-fallback-ux.md` rubric. Replace `TBD` placeholders.
4. Update **Delta** table.
5. Update **Verdict** section — flip each row to PASS/FAIL based on observed values.
6. Update **Linked artifacts** with the GHA run URL.
7. Update PRD M2 row Status `in-progress` → `complete` on PR merge (per plan acceptance).
