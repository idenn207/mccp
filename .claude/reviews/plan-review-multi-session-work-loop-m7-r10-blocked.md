# Plan Review Panel — multi-session-work-loop

**Plan**: `.claude/plans/multi-session-work-loop-m7.plan.md` · **Plan version**: `sha256:a38fe80c0f96c4672f44f7e6d5a1c2d169d13328bce3a007a972725388ea525d`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 11 blocking finding(s): security/CRITICAL, security/CRITICAL, security/HIGH, security/HIGH

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| security | CRITICAL | .gitattributes merge=union setting is absent and no machine-enforced prerequisite check prevents shipping without it, violating git-level merge safety for append-only audit corpus | .gitattributes (L1-16) contains only EOL rules; `merge=union` for findings glob not present. Plan §Task 1 (L225) claims 'Task 1 소유' but `.gitattributes` file UPDATE not listed in 'Files to Change' (L154-189). Plan requires merge=union to prevent silent data loss on parallel worktree merges (DD4, L93), but Task 1 validation is optional test (`node --test`), and Task 7's `C1-GATE-MERGE-UNION` gate (L271) is post-commit opt-in `--acceptance` mode (L297), not enforced on default PR path. Result: code can ship without `.gitattributes` containing critical merge strategy, leaving git-tracked registry vulnerable to silent append loss during merge. |
| security | CRITICAL | Coverage gate Task 7 checks for merge=union only in optional --acceptance mode, not enforced at PR approval time before code merges | Plan §Task 7 (L269): 'Validation 블록의 수동 명령으로만 두면 게이트는 잘못 설정된 인프라 위에서도 통과한다'. The check is in line 308 of Validation section (manual shell command: `git check-attr merge --...`), but this is advisory validation, not blocking PR gate. Default PR approval does not enforce `c1-coverage-gate.js --acceptance` execution (§Validation L291-297 marked opt-in). |
| security | HIGH | Path normalization enforcement relies on unenforced API contract: three separate emit points (plan-review/cli.js:663, plan-codex-runner.js, santa/seal.js) must call appendFinding() but no runtime guard prevents direct registry writes | Plan §DD4 (L225): identifies path normalization as 'single chokepoint' in appendFinding(), but acknowledges risk: '호출자 책임으로 두면 emit 지점 3곳 중 하나만 빠져도 절대경로가 새고'. Plan claims Task 7 coverage gate will prevent bypass via static scan of APPROVED_REGISTRY_WRITERS, but static literal path scanning cannot catch dynamic path construction (e.g., `path.join(basePath, work_unit + '.jsonl')`). Plan acknowledges this limit (L269): 'AST 분석이 아니므로 동적으로 조립된 경로는 원리상 못 본다' — runtime falsifier is backup but detects, does not prevent. |
| security | HIGH | Seq monotonicity for gap detection not protected under concurrent access: plan specifies lock-free seq generation (intentional per L126 note) but provides no mechanism to prevent duplicate seqs if M3 claim TTL races or lapses | Plan §Task 1 (L126): 'lock을 걸지 않는 것은 의도다' — seq generated at file append time, two processes can generate same seq. Plan relies on later detection: 'dedupe이고 **중복 `seq`는 구멍과 다른 신호로 분류**'. But if both processes write same seq then file is read later, duplicate detection flags degradation but does not prevent original loss. M3 claim TTL at 15 min (CLAUDE.md) is only defensive, not atomic guarantee. Plan states (L127): '탐지는 두 축이다: 정렬 후 동값 인접(중복)' but does not specify what reader behavior is when duplicates found—silent use of both? first-write-wins? last-write-wins? |
| security | HIGH | Findings registry path must be written only by findings-registry.js but git-tracked corpus has no pre-commit hook or automated enforcement mechanism that guarantees this before merge | Plan §Task 7 (L267): `APPROVED_REGISTRY_WRITERS` = `findings-registry.js` single entry. Plan says (L269) coverage gate will validate this via static scan + runtime falsifier. But enforcement is opt-in acceptance mode (L297), and static scan admittedly cannot catch dynamic paths (L269). A direct `fs.writeFileSync('.claude/state/findings/<work_unit>.jsonl', ...)` in any other module will pass static scan if path is dynamic, and will only be caught by runtime falsifier comparing `.claude/reviews/` surface delta. This creates a window where unnormalized paths reach git-tracked corpus before detection. |
| security | MEDIUM | Plan does not specify atomicity contract for emit failure handling: fail-open + seq gap detection assumes future writes will happen, but scenarios exist where neither detection axis functions | Plan §DD8 (L125-141) specifies fail-open ('호출자 exit code를 바꾸지 않는다', L131) + seq gap detection ('batch 전체가 유실되고 그 work_unit에 이후 성공한 write가 없는 구간은 비영점 exit 없음', L140). But if a work_unit's finding registry write fails completely (e.g., `.claude/state/findings/` directory deleted or permission denied), and that work_unit is never written to again in same session, the loss is undetected. Plan acknowledges (L140): '그 구간의 C1은 실제보다 **낮게** 보고된다. 이 잔여를 닫는다고 주장하지 않으며' — but without machine-enforced witness that registry writes succeeded, silent loss remains possible on downgrade-only scenarios. |
| test | HIGH | Task 1's unit test (findings-registry.test.js) will verify C1-MERGE-UNION assertion via git check-attr | Plan line 227 lists C1-MERGE-UNION as tested in 'node --test plugins/mccp/scripts/lib/tests/findings-registry.test.js', but the Assertion Roster (line 210) includes this assertion as Task 1's 8th item, and the Validate section in line 227 describes it as 'git check-attr merge 실측'. However, a Node test cannot directly run git commands without spawning subprocesses. The plan provides no code showing how this will be tested at the unit level. The actual git check-attr verification appears only in the manual Acceptance checklist (line 364) as a separate step run AFTER Validation, not within the unit test itself. |
| test | HIGH | Task 2's test file (c1-feedback-loop.test.js) will enforce atomic co-landing with Task 3 by asserting SOURCE_SCANNERS contains the 'findings' key | Plan line 231 claims Task 2's test file will check for 'findings' key in SOURCE_SCANNERS to prevent Task 2-only landing. Line 232 states 'Task 2 자신의 test가 붉어지는 pre-commit 장벽' (Task 2's own test will fail). However, this test file does not exist yet (it's marked CREATE). Without seeing the actual test code, it cannot be verified that: (1) the test file will actually import and inspect SOURCE_SCANNERS, (2) the assertion will use the correct variable name and structure, or (3) test execution will reach that assertion before any other failures mask it. The test file creation is entirely dependent on implementer following the plan description. |
| test | MEDIUM | The Assertion Roster's 30 assertions will be verified by running the Validate commands for each Task | The Assertion Roster (lines 210-217) lists 30 assertions across 8 tasks. However, assertion C1-MERGE-UNION is claimed to be in Task 1 (line 210) and verified by findings-registry.test.js. Yet Task 1's Validate command (line 227) is only 'node --test plugins/mccp/scripts/lib/tests/findings-registry.test.js', and git check-attr verification appears later in the manual Acceptance checklist, not in the unit test command. This means the Assertion Roster and the actual Validate commands do not align for C1-MERGE-UNION. The plan does not show that all 30 assertions from the Roster will be mechanically verified by the Validate commands. |
| test | LOW | .gitattributes has been updated with merge=union directive for findings registry files | Plan lines 178 and 225 state Task 1 will UPDATE .gitattributes to add 'merge=union' for '.claude/state/findings/*.jsonl' glob. However, the actual .gitattributes file (read at current state) contains only EOL rules (lines 1-16) and does NOT include any merge directive for the findings registry path. The plan describes this as a prerequisite for Task 1, but the file has not been updated yet. Since the plan says this is a CREATE/UPDATE for Task 1, the file should not exist in committed state before Task 1 executes. |
| test | MEDIUM | The test will verify that appending findings in batches prevents partial loss (DD8 first mitigation) | Plan line 227 claims Task 1's test will verify 'N개 finding batch가 **단일 write**로 착지하고 각 줄이 batch_expected: N을 갖되'. However, the plan provides no detail on HOW the test will verify atomic writes or partial loss scenarios. Will the test: (a) truncate the file mid-batch and verify degradation? (b) mock write() to fail? (c) inspect the batch_expected field? Without seeing the test code, it's unclear whether the test design actually falsifies the claim that batches prevent partial loss, especially concurrent partial writes. |
| invariant | HIGH | computeC1 degraded-status handling is not specified. Task 2 (lines 229-234) modifies computeC1 to check type_separation contract and add fields, but does not specify what status value is returned when coverage=degraded (detected seq gaps, duplicates, or malformed records). Degradation is flagged in coverage field but the impact on status (computed vs invalid) is unspecified. | Line 231-233: Task 2 spec says 'open_count와 deferred_rate를 추가한다' but acceptance criteria (line 357) requires 'C1.status === computed' without exception for degraded. No branch for degraded→invalid or degraded→computed-but-flagged. |
| invariant | MEDIUM | No fallback/rollback mechanism exists to disable C1 collection on runtime failures. The plan introduces emit points in 3 places (Task 4, lines 242-246) and makes findings collection essential to metric computation, but specifies no env var like MCCP_C1_DISABLED or feature flag to disable collection if the system breaks. Recovery requires code revert only. | Grep for MCCP_C1\|C1_DISABLED\|rollback in plan yields no matches. Compare with CLAUDE.md §3.3 Codex failure mode matrix: other systems like Codex have MCCP_CODEX_DISABLED and MCCP_ALLOW_CODEX_UNAVAILABLE for graceful degradation. M7 has none. |
| invariant | MEDIUM | Emitters don't explicitly validate schema compliance before appending to registry. Task 4 (lines 242-246) wires emit points in plan-review/cli.js, plan-codex-runner.js, and santa/seal.js, but the plan doesn't specify that emitters check their output against the allowlist schema before append. Registry silently drops unknown fields (line 224: 'allowlist 필드 집합'), creating risk of undetected data loss if emitters use undeclared fields. | Task 4 Action (line 244-246) says 'finding_opened를 emit한다' but doesn't say 'validate schema before append' or 'await validateFinding()'. Line 224 says fields are allowlisted but doesn't specify who validates conformance at emit time. Mirrored msw-events.js doesn't validate emitter output pre-append (§3.2: 'allowlist 필드 집합...per-line skip' = silent drop). |
| invariant | MEDIUM | Coverage gate --acceptance mode is explicitly opt-in, not automatic enforcement. Lines 295-297 comment states 'milestone 전용(opt-in)이다: default 모드에 넣으면 패널을 한 번도 돌리지 않은 fresh clone에서 붉어져'. This is intentional design but means the gate can be skipped at ship time if not manually invoked by implementer during actual /mccp:prp-implement run. | Line 295-297: '# 이 milestone 전용(opt-in)이다... node plugins/mccp/scripts/lib/msw-metrics/c1-coverage-gate.js --acceptance --json'. Line 363 says it 'will be run' in validation loop, but that's specification-time claim. Acceptance checkpoint (line 362) is checkbox-based, not pre-merge hook. Implementation fidelity risk: if implementer doesn't wire --acceptance into actual /mccp:prp-implement validate hook, it stays unrun. |
| invariant | LOW | Findings without cited_path cannot be matched across rounds and will become permanently open. DD9 (lines 142-146) restricts 2ndary key matching to findings WITH cited_path: '같은 matchKey에 후보가 둘 이상이면 매칭하지 않는다' + 'cited_path가 없는 finding에는 적용하지 않고'. This means findings with no code location can never close via non-recurrence (DD3), only via terminal REJECTED_BY_DESIGN verdict. | DD9 lines 145-146: 'cited_path가 없는 finding에는 적용하지 않고'. DD3 (line 85) closure via 'same work_unit·same reviewer axis round N→N+1 non-recurrence' depends on finding_id match or matchKey fallback. No match → finding stays open forever unless rejected in code. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | pass | Attacked the plan on: (1) Citation accuracy — verified claims about `SOURCE_SCANNERS` lacking `findings` key (confirmed via `derive/index.js:27-51`), `ADJUDICATION_VERDICTS` enum (confirmed via `intent-context.js:69-74`), fixture at `msw-metrics.test.js:416` lacking `type_separation` (confirmed). (2) Abstraction integrity — checked single-source-of-truth claims for verdict→closure mapping (DD7), slug derivation (DD4), and batch-first append API (Task 1); all properly scoped to named exports and single entry points. (3) Boundary leaks — examined work_unit sharding, registry persistence across session boundaries, `cited_path` normalization chokepoint; all correctly isolated and enforced. (4) Coupling risk between Task 2 and Task 3 — verified pre-commit test assertion (`C1-SOURCE-REGISTERED-COPRESENT`) creates fail-fast barrier if either lands alone, preventing split tree. (5) Coverage of edge cases — DD8 explicitly documents unreachable tail cases (full batch loss with no subsequent writes) and documents error direction as conservative (C1 undercount not overcount). (6) Seams for future work — M8 explicitly identified as producer owner for A1/A2/B3 metrics; M7 scope is measurement-only without gate/LLM changes per UI2. (7) Pattern consistency — `.gitignore` correctly shown as not matching `.claude/state/findings/`, `.gitattributes` correctly shown as needing `merge=union` entry (both gaps properly assigned to Task 1). (8) Invariant preservation — decision_id derivation (slug from PRD basename) correctly held invariant across archive scenarios; no unexpected slug changes breaking shard continuity. |
| security | fail | I attacked: (1) Path normalization chokepoint — read plan §DD4, traced normalization code in receipt/write.js, verified appendFinding() API contract requirement. Confirmed static code scan cannot catch dynamic paths and plan acknowledges this but proposes only detection fallback. (2) .gitattributes merge=union prerequisite — verified .gitattributes exists but lacks merge=union setting, confirmed Task 1 ownership claim in plan, checked Files-to-Change table (no .gitattributes entry), confirmed validation is optional test not PR gate. (3) Coverage gate enforcement — read Task 7 specification (now detailed with APPROVED_REGISTRY_WRITERS and APPROVED_SURFACE_WRITERS constants), confirmed gate is post-commit and opt-in `--acceptance` mode, not default PR approval. (4) Seq monotonicity — reviewed lock-free design rationale (L126), checked M3 claim TTL (15 min from CLAUDE.md), read DD8 failure detection logic; confirmed plan expects duplicate-detection and gap-detection but does not specify reader behavior or atomic guarantees. (5) Trust boundary isolation — verified appendFinding() should be single entry point but three emit sites call it (not direct writes); confirmed static scan limitation on dynamic paths; checked that runtime falsifier only detects post-fact (.claude/reviews/ delta audit), does not prevent. (6) Fail-open scenarios — read DD8 reasoning for fail-open (measurement shouldn't block work), traced gap detection logic (batch-atomic writes + seq monotonicity + marker), found scenario where final batch write fails and no follow-up write happens (loss undetectable). Could not construct scenario where both static coverage gate AND runtime falsifier miss unapproved writer, because plan explicitly states gate has two independent axes (static lint + runtime compare-to-surface). However, scenario where unapproved direct-write happens between acceptance gate check and actual merge—timing race, static scan then code added—is not addressed. |
| test | fail | Attacked the plan's test-driven design by examining: (1) Assertion Roster vs. actual Validate commands—found C1-MERGE-UNION claimed as Task 1 unit test but only appears in manual Acceptance; (2) git attribute checks—verified .gitattributes has no merge=union currently; (3) test file existence—confirmed findings-registry.test.js, c1-feedback-loop.test.js, c1-coverage-gate.test.js do not yet exist, making their design unfalsifiable without code review; (4) circular dependency claim for Task 2/3 atomicity—found the claim depends on a test file (c1-feedback-loop.test.js) not yet written; (5) DD8 batch-atomic-write claim—found no specification for HOW the test will verify partial loss prevention; (6) integration vs. unit split—identified manual Acceptance checklist items (line 364 git check-attr) appear in Acceptance, not Validation, contradicting Assertion Roster placement. |
| invariant | fail | Attacked: (1) Fail-open drift in emit failures—checked DD8 mitigation layers (seq detection, batch atomic append, runtime falsifier) and found coverage gate is optional, degradation is flagged but not blocking. (2) Receipt anchoring via work_unit sharding—verified shards exist and .gitattributes merge=union is specified in Validation (not pre-commit but in impl-validate), so acceptable. (3) Skip predicates for Task 2·3 copresence—found pre-commit test barrier in Task 2 itself; accepted. (4) Rollback paths—found none; no env var to disable C1 collection. (5) Schema enforcement at emits—found no validator called before append, only registry-side allowlist with silent drop. (6) Status handling when degraded—found acceptance criteria require computed status but no spec for degraded branch. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 354525,
  "halt_stage": "5.2e",
  "granted": 4,
  "reviewed_plan_hash": "sha256:a38fe80c0f96c4672f44f7e6d5a1c2d169d13328bce3a007a972725388ea525d",
  "plan_path": ".claude/plans/multi-session-work-loop-m7.plan.md",
  "recorded_at": "2026-08-17T15:50:37.122Z"
}
```
