# Plan Review Panel — ci-full-suite-m3o

**Plan**: `.claude/plans/ci-full-suite-m3o.plan.md` · **Plan version**: `sha256:1d955c86272f827ff0773d9945ccaeb03517f3e86441df29a7a96df9d861d0f6`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 2 blocking finding(s): architect/HIGH, architect/FAIL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | DD9의 삭제 래칫이 `judge`의 계약 밖에 있어, 이 계획이 load-bearing이라 선언한 "2단계 안 누적" 불변식에 소유자도 단언도 없다. gate.js는 순수 함수 `judge({measurement, coverage})`가 닫힌 `reasons` 열거(그 안에 `deleted_without_allowance` 포함)를 낸다고 못박는데(plan:876-889), 그 함수는 base 집합/`missing`을 인자로 받지 않으며 계획 스스로 (k)가 "순수층 밖"이라고 적는다(plan:966-970). 즉 삭제 판정과 그 사유 코드는 CLI가 계산해 사후에 합성해야 하고, 그러면 최종 `blocked`/`stage`/`reasons`의 소유자가 judge가 아니라 CLI가 되어 "gate.js가 DD3 판정 순서의 유일한 소비처"(plan:752)와 어긋난다. 그 결과 plan:893-898이 축 D B의 반증가능성 근거로 든 "2단계 안의 네 축은 전부 평가해 누적한다"는 규칙이 구현자에게는 자연스러운 단락(커버리지가 이미 막았으면 삭제 축을 평가하지 않음)으로 접히고, 그것을 붉게 만들 단언이 0건이다 — Task 2b (k)의 다섯 분기는 전부 커버리지가 통과하는 구성이고(plan:956-964), Validation 4b는 합성 green measurement를 쓰며(plan:1616-1621), (e4)는 `unexplained === 0`을 전제한다(plan:1458-1460). 커버리지 실패와 삭제가 겹치는 경우를 재는 분기가 계획 전체에 없다. | plan:876-877 `순수 함수 judge({measurement, coverage})가 ... {blocked, stage, reasons, message, coverage}를 낸다` + plan:889 `deleted_without_allowance` 포함 + plan:966-970 `judge({measurement, coverage})는 순수하지만 이 세 분기가 재는 것은 git 해소와 인자 검증이라 순수층 밖이다` + plan:893-898 `2단계 안의 네 축은 전부 평가해 reasons에 모은다 ... 단락 구현이면 앞의 것만 실려 (e4)의 단언이 래칫이 살아 있어도 거짓 red가 된다` |
| architect | MEDIUM | `exclusions_digest` 앵커를 "실제 차단력" 셋/넷에 넣은 것은, 이 계획이 R10에서 `unexplained == 0`에 대해 스스로 철회한 과대주장과 같은 형태다. 강제 workflow에서 measurement는 `--exclude-from .github/test-suite-exclusions.json`으로 생산되고(plan:1196-1197, 오라클이 값까지 리터럴로 pin) 판정 줄도 같은 리터럴 경로를 읽으므로(plan:1206-1209), 두 입력은 같은 job 안에서 같은 파일에서 나온다 — digest는 구성상 항상 일치한다. 게다가 digest는 러너가 *로드한* 목록에서 계산되므로(run.js:580 `exclusions_digest: exclusionsDigest(exclusions)`) 격리 적용이 잘못돼도 어긋나지 않는다. 즉 CI 경로에서 이 앵커는 `unexplained`와 같은 정합 검사이지 차단력이 아닌데, DD2와 Acceptance 2가 그것을 간판으로 다시 올린다. | plan:373-374 `이 milestone의 실제 차단력은 셋에서 온다 — stage 0의 attribution 불변식 · exclusions_digest 앵커링 · DD9 삭제 래칫` / plan:1818 `네 축(attribution 불변식 · 격리 파일 수 상한 · digest 앵커 · 삭제 래칫)` vs plan:1196-1209(두 인자 값이 같은 리터럴 경로로 pin됨) + scripts/test-suite/run.js:580 |
| architect | MEDIUM | DD9가 금지한 "floor 하향"을 Task 3의 도출식이 정상 운용에서 **강제**한다. floor 값은 `tracked_basis − (max_allowed_deletions + 1)`로 등식 단언되고 `max_allowed_deletions`는 `allow_deletions.length`와 등가로 pin되므로, 정당한 삭제를 위해 면제 항목을 1건 추가하는 순간 같은 diff에서 floor 값을 1 내리지 않으면 판정자 자기 test 단계가 red가 되어 저장소가 머지 불가가 된다. 그런데 DD9는 같은 문서에서 "정당한 삭제의 출구는 floor 하향이 아니라 같은 파일의 allow_deletions 배열이다"라고 못박아, DD9만 읽은 구현자·리뷰어는 요구되는 그 편집을 계약 위반으로 읽는다. | plan:695-696 `정당한 삭제의 출구는 floor 하향이 아니라 같은 파일의 allow_deletions 배열이다` vs plan:985-986 `tracked(분모 절대 하한 ... 값은 tracked_basis − (max_allowed_deletions + 1)이다)` + plan:1034 `도출 단언(순수 JSON, 트리 미판독): tracked === tracked_basis − (max_allowed_deletions + 1)` + plan:1030-1031 `max_allowed_deletions는 allow_deletions의 실제 항목 수와 등가다` |
| security | MEDIUM | DD4의 "artifact 내용은 redact.js를 이미 통과한 산출"이라는 문장은 이 계획이 새로 도입한 stage-3 경로에서 거짓이다. `redaction_ok=false`는 잔여 유출 문자열이 measurement JSON 본문에 남았다는 뜻인데(run.js:599-604 `scanResidual` hits > 0 → `redaction-incomplete`), Task 5가 업로드 단계를 마지막 + `if: always()`로 못박으므로 게이트가 3단계에서 차단해도 유출본은 **항상** 공개 artifact로 발행된다. 즉 DD3이 3축을 '차단'으로 격상한 것이 유출 발행을 하나도 줄이지 않는다. 가설이 아니다 — PRD L88이 이 저장소의 Linux 측정에서 `redaction_ok=false`를 실측했고, DD4가 `paths` 필터 제거로 노출 빈도가 넓어짐을 스스로 인정한다. | plan L533 "artifact 업로드는 유지하되(`if: always()`) 그 내용은 `scripts/test-suite/redact.js`를 이미 통과한 산출이다" vs plan L1100-1104(`if: always()` 업로드가 마지막 단계, 필수) · scripts/test-suite/run.js:599-604 · PRD L88 "그 측정을 컨테이너에 병합하는 순간 `redaction_ok=false`로 거짓이 된다" |
| security | LOW | 같은 축에서 계획이 자기와 모순된다: DD4a L647은 "artifact는 판정 전에 이미 올라가 있다"고 적지만 Task 5가 정한 단계 순서는 판정이 업로드보다 **앞**이다. 두 서술 중 어느 것이 계약인지 판정 불가이고, 실제 코드가 취할 유일한 안전 여지(차단 시 업로드 조건화)를 어느 오라클도 재지 않는다 — Task 7 오라클은 업로드 단계가 판정 **뒤**에 있고 `if: always()`임만 단언한다. | plan L647 "`redaction_ok` 차단은 발행 이후 탐지다. artifact는 판정 전에 이미 올라가 있다" vs plan L1099-1100 단계 순서 "… 전수 실행 → 판정 → 산출물 업로드(`if: always()`)" · plan L1411 오라클 "산출물 업로드(`actions/upload-artifact` 단계가 판정 **뒤**에 …)" |
| test | MEDIUM | Task 6의 artifact 개명이 '계약 변경이므로 소비처를 같은 diff에서 고친다'고 지시하면서 지목한 소비처가 실재하지 않는다 — 그리고 어떤 단언도 소비처 동기화를 재지 않는다. 즉 그 문장은 반증 불가하다. | plan L1249-1251: "개명은 계약 변경이고, 소비처를 같은 diff에서 고친다: 그 주석 본문과 `docs/ci-full-suite/*`의 `gh run download` 줄". 실측: `docs/ci-full-suite/` 안에 `gh run download`·`--name test-suite` 문자열은 0건이고, `test-suite-baseline-node` 문자열을 실제로 든 in-tree 소비처는 `.github/workflows/test-suite-baseline.yml`과 `.claude/plans/ci-full-suite-m1.plan.md`이며 후자는 `## Files to Change`에 없다. Task 7 단언 (4c)는 workflow의 `name:`/`path:`가 matrix 두 축을 담는지만 보고 소비처를 보지 않는다(plan L1429-1432). |
| test | MEDIUM | Task 1의 Validate 줄이 그 Task가 고치는 대상을 반증할 수 없다 — Linux 전용·node20 전용 red는 로컬(Windows·node24) 단독 실행에서 수리 전에도 green이므로, 그 Validate는 수리 부재를 구조적으로 통과시킨다. | plan Task 1 Validate L806-807: "각 파일을 `MCCP_CODEX_DISABLED=1 node --test --test-concurrency=2 <file>`로 단독 green 확인". 그러나 대상은 L800-801이 명시하듯 `1b santa-loop-cap.test.js DD3 symlink (Linux 전용)` · `1c dispatch-fullcycle-smoke.test.js (node20 전용)`이고, 계획 자신이 로컬을 Windows/Node 24로 전제한다(Validation 7 주석 "Windows 로컬은 약 30분", 7b "Windows 로컬에서는 통과하지 않을 수 있다"). 보완 오라클은 4개 Task 뒤의 Task 5 라이브 CI뿐이다("전체 판정은 Task 5 이후 CI가 낸다"). |
| test | LOW | `coverage.js`와 `gate.js`가 '두 번째 구현이 아니라 같은 헬퍼(inputs.js)를 공유한다'는 계약을 재는 것이 단일 합성 입력에 대한 값 일치뿐이라, 갈라진 두 구현이 그 fixture에서만 일치하면 통과한다 — 구조적 짝 단언이 없다. | plan L832-835: "같은 합성 입력에 대해 `coverage.js --assert-accounted --json`과 `gate.js --json`의 커버리지 수치와 `reasons`가 일치한다". `inputs.js`를 두 소비처가 실제로 require한다는 배선 단언은 어디에도 없다 — 이 저장소가 같은 성질에 쓰는 선례(`impeccable-guard.test.js`의 짝 단언, plan L325가 Patterns to Mirror로 인용)와 비대칭이다. |
| test | LOW | Task 5는 workflow 두 파일을 편집하는데 그 Validate 줄이 어떤 test도 돌리지 않는다 — 형태 오라클(wiring-cut.test.js 단언 1~4)은 Task 7의 Validate에만 있다. | plan Task 5 Validate L1234-1235: "PR을 열어 `test-suite` 체크가 실제로 발화하고 green임을 확인. `gh run view --json jobs`…" — `node --test scripts/tests/wiring-cut.test.js`가 없다. 반면 같은 파일을 함께 고치는 Task 6은 그 단언을 Validate에 명시한다(L1262-1263). Risks L1802가 5와 7을 한 단위로 못박아 완화하지만, Task 단위 Validate는 여전히 편집 대상을 재지 않는다. |
| invariant | MEDIUM | DD3 stage-3 blocks the merge on `redaction_ok:false`, but the plan mandates an unconditional `if: always()` artifact upload of the very JSON whose residual scan fired — no branch conditions publication on `redaction_ok`. The gate stops the merge, not the harm it names. DD4's justification for keeping the upload is factually false in exactly this case, and DD4a's ordering claim contradicts the plan's own step order. | Plan L533 (DD4): "artifact 업로드는 유지하되(`if: always()`) 그 내용은 `redact.js`를 이미 통과한 산출이다" vs `scripts/test-suite/run.js:599-604` — `out.redaction_ok = folded.redaction_ok && scan.hits.length === 0`, i.e. `redaction_ok:false` means the emitted object *still contains* residual matches. Plan L647 (DD4a) asserts "artifact는 판정 전에 이미 올라가 있다", but Task 5 fixes the order as "…판정 → 산출물 업로드(`if: always()`)" (L1099-1100) — upload is after judgment and still unconditional. Exposure frequency is widened by design (`paths` filter removed, L1096; acknowledged at L536-539). |
| invariant | MEDIUM | The DD9 deletion ratchet is path-shaped only (`missing = base_set − head_set` over `*.test.js` paths), so the failure mode DD9 exists to close — "a commit that cuts wiring silently removes the test asserting it" — survives intact by emptying the file's assertions instead of deleting the path. Coverage stays 100%, `unexplained === 0`, `tracked` unchanged, `missing` empty, suite green. The plan records residuals meticulously everywhere else but records none for this one. | Plan L657-660 states the ratchet's purpose: "배선을 끊는 커밋이 그 배선을 단언하는 test를 같이 지우면 머지 차단 게이트가 green이다"; the mechanism is L676-679 (`git ls-tree -r <base> --name-only` path-set difference). Grep of the plan for any gutting/emptying case returns 0 matches. `scripts/test-suite/enumerate.js:122` accepts a file purely by `.endsWith('.test.js')` — content is never an input to any gate axis. |
| invariant | MEDIUM | `## Validation` 4b selects the deletion target with `git ls-files '*.test.js' \| head -1`, bypassing the three selection rules the plan itself declares necessary for cut B (Task 7 (e5)) — in particular "not present in `allow_deletions`". If the alphabetically-first tracked test file is ever exempted, the ratchet legitimately passes and 4b hard-fails with "DD9 삭제 래칫이 죽었다", reproducing the "만족 불가와 안 했다를 구분할 수 없다" mode this plan absorbed three times. | Plan L1614-1625 (`DEL=$(git ls-files '*.test.js' \| head -1)` … `test "$DEL_RC" -ne 0 \|\| { echo "FAIL: 삭제했는데 게이트가 통과 — DD9 삭제 래칫이 죽었다"; exit 1; }`) vs Task 7 (e5) at L1471-1477 which requires the target to satisfy three rules including "(3) `allow_deletions`에 **없다**(있으면 면제되어 잴 것이 없다)". |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | 인용 검증: version-declaration-gate.yml L21-24·L57-60·L62-65(미러 주장 — 정확) · run.js:10-12/179-183/213-214/489-505/577-580/676-679/725(전부 소스와 일치) · enumerate.js:55-79/111-137(normalizeExclusions throw·이분 구성 — 정확). 깨보려 한 것들 중 무해로 판명된 것: (1) Validation 4b의 `git ls-files`가 pathspec 없이 전 tracked를 넘겨 합성 measurement가 비-test 파일을 담아 stage 0 `measurement_tree_mismatch`로 죽는가 → `enumerateTests`가 `.endsWith('.test.js')`로 자체 필터하므로(enumerate.js:122) 성립하지 않음. (2) 4b의 `--base-ref HEAD`가 R10이 지적한 `base_set = head_set` 접힘을 재현하는가 → `--apply-delete`가 `git rm`으로 index만 바꾸고 HEAD 커밋 트리는 그대로라 `missing`이 비지 않음, 성립하지 않음. (3) `ticket` 필드 추가가 `exclusionsDigest`를 흔들어 digest 앵커를 깨는가 → digest는 pattern+reason만 정렬 해시하므로 무영향. (4) floor 도출식이 절단 B의 트리에서 self-test를 죽이는가 → 여백이 `max_allowed_deletions + 1`이라 삭제 1건은 floor 위에 남음, 성립하지 않음. (5) `tracked` 해소 채널이 gate.js·coverage.js·run.js 셋으로 갈리는가 → inputs.js 소유 + 계획이 run.js와의 괴리를 \\"약한 검사\\"로 이미 명시(plan:1171-1176), 새 결함 아님. 남은 셋이 위 findings다. |
| security | pass | 공격한 것: (1) fork-PR 위협모델 — `permissions: contents: read` · `persist-credentials:false` · `pull_request_target` 미사용 · SHA pin · `secrets.` 미참조가 전부 Task 7 오라클 단언으로 배선돼 있어 우회 경로를 못 찾았다. (2) 셸 주입 — `${{ github.base_ref \|\| 'main' }}`을 `env:` 간접으로 받아 `"$BASE_REF"`로 읽고, R11이 \\"인용은 방어가 아니다\\"를 정확히 정정했으며 `github.base_ref`는 base 저장소 소유 값이라 attacker-controlled 경로가 없다. (3) 신뢰 경계 위조 — 판정 입력(measurement·exclusions·floor·판정 모듈)이 전부 PR 통제 트리라는 것은 DD4a가 명시 기록하고 닫지 않는 이유를 적는다(런타임 통제 지점 부재) — 새 결함이 아니라 인정된 잔여. (4) `allow_deletions` path가 glob이면 삭제 래칫 전체가 죽는 축 — R13이 리터럴 정확 일치로 못박아 이미 닫혔다. (5) `--allow-codex` CI 가드 — 플래그 파싱 지점 + `GITHUB_ACTIONS=true` Validation 실행으로 로컬 사각이 닫혔다. (6) 경로 traversal — exclusions/floor 경로가 workflow에서 리터럴 pin이라 주입면이 없다. 실제로 착지한 것은 유출 발행 축 하나뿐이고 그것도 DD4a가 절반 인정하고 있어 MEDIUM으로 남긴다. |
| test | pass | 공격한 것: (1) 계획의 최강 주장 둘 — \\"게이트가 red 스위트를 통과시키지 않는다\\"와 \\"말 없는 삭제를 래칫이 막는다\\" — 을 반증할 test가 있는지. 전자는 gate 분기 (a)(b)(m)와 축 D 절단 A의 stage=1 귀속, 후자는 (k)의 5분기·Validation 4b(트리 실제 절단 + 합성 measurement)·절단 B가 과대허용 방향을 덮는다 — 구멍을 못 찾았다. (2) 인용 검증: `run.js:179-183`(`Number(exit_code)\|\|0` 접힘) · `:205`·`:213-214`(`per_file: ok ? perFile : null`)를 소스에서 확인했고 계획 서술과 일치한다. `enumerate.js`의 `enumerateTests`/`exclusionsDigest`/`normalizeExclusions` export와 `globToRegExp`의 `**` 확장도 확인 — Validation 4b의 인라인 합성 스크립트가 실재하는 API만 부른다. (3) 버그를 고정한 기존 test 탐색: `scripts/tests/test-suite.test.js`가 `--exclude-from` CLI 경로가 아니라 `enumerateTests`를 직접 부르므로(:55-98) Task 3의 `run.js` 재배선(ticket 필수)이 기존 단언을 깨지 않음을 확인했고, `scripts/tests/`에 workflow 파일을 단언하는 test가 0건이라 DD1의 `pull_request` 트리거 제거가 기존 green을 뒤집지 않음도 확인했다. (4) Validate 줄의 경로 실재성: 새 test 파일은 전부 `## Files to Change`에 CREATE로 있고, Task 0의 옛 artifact 이름은 개명 이전 순서로 명시돼 있다. 남은 것은 위 넷이며 어느 것도 HIGH가 아니다. |
| invariant | pass | Traced every gate opening I could construct. Verified the load-bearing citations rather than trusting them: `run.js:179-183` (`Number(r.exit_code) \|\| 0` folds null to 0 — stage-0 rationale holds), `:205/:213-214` (`per_file: ok ? perFile : null` — the `null` vs `[]` correction is right), `:599-604` (redaction_ok semantics), `:576-580`/`:666-679` (measurement fields, `--list` emits post-exclusion set), `enumerate.js:55-90` (`exclusionsDigest` normalizes to pattern+reason). Attacked: (a) digest anchoring — tried to pair an exclusion-free measurement with a large list; blocked, and an absent `exclusions_digest` also falls to mismatch/fail-closed; (b) the `ticket` field being outside the digest — not an opening since `gate.js` loads and validates the file directly; (c) base-set staleness — tried to make every un-rebased PR permanently red via `origin/<base>` tip vs merge-base; refuted because `pull_request` checks out the merge ref, so head_set ⊇ base_set; (d) deletion+addition offset against the ratchet — closed by set difference; (e) `--base-ref HEAD` in 4b collapsing base_set to head_set — refuted, `git rm` diverges index from HEAD so the ratchet fires; (f) the floor/`allow_deletions` margin arithmetic (`tracked === tracked_basis − (max_allowed_deletions + 1)`) across a legitimate deletion, its cleanup, and cut B — the boundary holds at exactly one deletion; (g) stage short-circuit hiding the ratchet — reachable only via `--base-ref` absence, which the oracle and fail-closed set pin; (h) the CI-only `GITHUB_ACTIONS` guard regression — covered by Validation 3b; (i) self-test-before-judgment circularity — the plan closed it and the tree-reading assertion is reduced to one covered by both cut selection rules. Found nothing HIGH; the three MEDIUMs above are what survived. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 252414,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:1d955c86272f827ff0773d9945ccaeb03517f3e86441df29a7a96df9d861d0f6",
  "plan_path": ".claude/plans/ci-full-suite-m3o.plan.md",
  "recorded_at": "2026-09-04T04:16:30.568Z"
}
```
