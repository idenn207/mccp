# Plan Review Panel — santa-delta-review

**Plan**: `.claude/plans/santa-delta-review-m3.plan.md` · **Plan version**: `sha256:18f925384c0d698eeaf9e921b3b3ffbeee1f4a6b1d87d539ffb270722f61314b`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired

> Reason: L2 quorum not satisfied: 7 blocking finding(s): architect/FAIL, security/HIGH, security/FAIL, test/FAIL — MCCP_REVIEW_SINGLE_PASS=deadline_pressure 로 진행한다. verdict는 divergent 그대로 봉인된다.

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | MEDIUM | Task 4 claims centralization ('두 파일이 같은 판정을 쓰도록 한 자리에 둔다' — put determination in one place) but Files to Change does not specify the implementation point, risking code duplication and drift across two independent hooks. | Plan L241-242 states DD6 goal of centralization. Plan L244 cites existing gitRepoRoot in session-activity-tracker.js:358-360 as mirror. But Files to Change L58-59 lists two separate UPDATE actions (post-tool-use-failure.js and session-end-trace.js) with no new shared module or explicit reuse strategy noted. Task 4 L243 says 'two files use the same determination from one place' but does not specify where that place is or how the shared logic will be accessed. |
| security | HIGH | Task 4 reintroduces absolute path leakage into user-visible messages via git rev-parse --show-toplevel output | Plan Task 4 directs both post-tool-use-failure.js and session-end-trace.js to call `git rev-parse --show-toplevel` to obtain repoRoot. Git's --show-toplevel flag returns an absolute path (documented standard behavior). This absolute path flows through hook-trace.recordWrite() (hooks/hook-trace.js:72,93) and is returned as result.path to post-tool-use-failure.js:120, which then builds a user message containing the full absolute path at line 64: `'  trace: ' + traceLogPath` and line 71: buildAdditionalContext(). The absolute path format `/home/user/projects/repo/.claude/state/hook-trace/...` leaks the user's home directory and project location. CLAUDE.md §3.12 line 485 documents prior fix for this exact issue: 'repo-relative로 정규화한다(절대경로 leak 회피)'. |
| test | MEDIUM | Task 1 Validate section cites `node --test plugins/mccp/scripts/derive/tests/backlog-source.test.js` to measure 171→443 lines recovered, but this test file does not exist — it is listed as CREATE in Task 1 | Plan line 53 lists 'plugins/mccp/scripts/derive/tests/backlog-source.test.js \| CREATE' but Validation section line 306 references it as if it exists: 'node --test plugins/mccp/scripts/derive/tests/backlog-source.test.js'. Glob search confirms file does not exist. The Validation section presupposes successful completion of Task 1 but treats the test as a baseline check. |
| test | MEDIUM | Task 3 acceptance criterion (line 360-365) claims santa-loop-cap.test.js will show '53 pass / 0 fail' after implementation, but baseline measurement depends on repository settings containing MCCP_REVIEW_SINGLE_PASS env at measurement time | Plan DD5 line 122 states 'baseline: 25 pass / 57 fail, env만 제거하면 53 pass / 0 fail' but this baseline is only valid if .claude/settings.json currently has MCCP_REVIEW_SINGLE_PASS=deadline_pressure. If settings change or were not set when measured, the baseline becomes invalid. No prerequisite check is listed. |
| test | MEDIUM | Acceptance criteria 4a-4d (lines 360-365) describe manual verification steps ('실제 fixture 저장소의 하위 디렉토리를 event.cwd로 준 hook 호출이 저장소 루트 한 곳에만 shard를 남기고') that have no corresponding automated test or test file reference | Plan states 'Task 4: hook-trace 루트를 git toplevel에 앵커한다' and lists Validate as 'node --test plugins/mccp/scripts/hooks/tests/hook-trace-root-anchor.test.js' but the acceptance criterion at line 362 is described as manual verification that 'confirm the real fixture repo' produces the expected directory structure — this is not mechanically falsifiable without the test file. |
| test | LOW | Acceptance criteria 5-6 (lines 366-371) depend on manual inspection of notes and backlog file ('노트의 대장과 파일이 개수까지 일치한다' and '노트에 남았고') with no automated verification test cited | Plan Acceptance line 366-367 states backlog rows must have 'ABSORBED 또는 증거 있는 이연 사유 중 정확히 하나를 갖는다 — 노트의 대장과 파일이 개수까지 일치한다' but references only manual counting ('노트의 대장과 파일이'). No validation command tests this automated equivalence. |
| test | LOW | Plan claims M1·M2 outputs produce specific numerical results (68→23 unplanned count, 69→130 finding rail) but these depend on external state (actual repository commits and file contents) that may change independent of code changes | Task 5 Validate line 258 claims 'M2 plan 기준 unplanned 68 → 23' and Task 1 Validate line 214 claims 'rail이 69 → 130'. These numbers anchor to specific commits/state ('실측 anchor') but if the plan files or repository state differ from when these numbers were measured, the validation will fail. No guard ensures the inputs match the measurement baseline. |
| invariant | HIGH | Acceptance criteria (lines 357-375) mix plan-time and implementation-time checks. Items 1-6 cannot be verified when the plan review gate (`/mccp:plan`) issues the receipt, only after full execution. This creates a fail-open gate: a plan review can pass while actual implementation might fail, with no receipt evidence forcing re-validation. | Plan's Acceptance section requires (a) 'All tasks complete' and (b) 'Validation passes' and (c) specific numerical targets like 'scanBacklog returns 443 rows, invalid 0' and (d) 'santa-loop-cap.test.js: 53 pass / 0 fail'. These are only measurable AFTER implementing all 8 tasks and running full test suites. The `/mccp:plan` gate (Phase 5 in plan.md:670-932) cannot execute implementations or tests; it can only review the plan document itself. The M2 closure (lines 100-124 of .claude/milestone-closures/santa-delta-review-m2.md) explicitly shows this gap: it notes that plan-codex receipt's plan_hash becomes stale if the plan is edited after approval, yet the Acceptance criteria here assume the plan will describe things only verifiable after implementation. |
| invariant | MEDIUM | Plan's acceptance criterion for red count (lines 370-371: 'baseline(51 fail)보다 줄고 늘지 않았다') uses ambiguous phrasing that doesn't define a fail-closed threshold. The word '줄고'(reduced) AND '늘지 않았다'(not increased) could be interpreted multiple ways. Without mechanical enforcement, a plan review could pass with a vague criterion while implementation red count actually increases. | Line 370-371: '전 스위트 red 총량이 착수 전 baseline(**51 fail**)보다 **줄고 늘지 않았다**'. This phrasing is ambiguous—does it mean (a) must be < 51? (b) must be ≤ 51? (c) must match 51? The baseline section (lines 301-303) states current count is 51 fail, but the acceptance criterion doesn't specify a hard threshold. Compare to Task 3 acceptance (line 237) which uses precise targets: '25 pass / 57 fail → 53 pass / 0 fail' — but the overall acceptance doesn't replicate this specificity. |
| invariant | HIGH | Receipt anchoring gap: The M3 plan's completion depends on mechanical verification of code changes (backlog parser counting, compareCoverage expanded, env isolation, hook-trace anchoring, etc.), but the plan review receipt (`mccp-plan-codex`) will only certify that THIS PLAN was reviewed, not that those 8 tasks were successfully implemented. If implementation diverges or partially succeeds, there is no mechanical gate preventing the plan receipt from claiming approval. | Plan lines 48-71 (Files to Change section) describe 20 file modifications across 8 tasks. Each task has validation requirements (lines 298-335). However, the `/mccp:plan` gate issues ONE receipt anchored to the plan document hash. The `/mccp:prp-implement` gate will issue a SEPARATE receipt after implementation. But M3's own acceptance criteria treat the plan approval as sufficient proof that these tasks will be implemented correctly. There is no binding contract between the two receipts. M2 closure document (line 114-117) shows this risk explicitly: 'PR 미발행' (PR not yet issued) means the plan passed gate-level review but has no downstream ship receipt proving the tasks were actually completed and integrated. |
| invariant | MEDIUM | Task 7 (backlog processing, lines 272-287) requires examining '.claude/plans/codex-findings-backlog.md' and marking EVERY row from 'this cycle' with either ABSORBED or evidence-backed deferral. However, the plan does not explicitly define what 'this cycle' means or provide the backlog row list. Without a static specification, the acceptance validation (lines 366-367) becomes subjective: different reviewers might disagree on which rows belong to 'this cycle'. | Lines 272-287 (Task 7): 'santa-delta-review 사이클이 남긴 backlog 행을 하나도 빠뜨리지 않고 처리한다.' But the plan references `.claude/plans/codex-findings-backlog.md` without including the current contents or a clear timestamp/scope. The Acceptance criterion (line 366-367) says 'backlog의 사이클 행 **전건**이 ABSORBED 또는 증거 있는 이연 사유 중 정확히 하나를 갖는다' but provides no objective test of what counts as 'this cycle's rows'. If the backlog grows between plan approval and implementation, which new rows are in-scope for M3? |
| invariant | LOW | Plan does not describe what happens if any of the 8 tasks fail or remain incomplete. There is no rollback path, no partial-success guidance, and no explicit acceptance of 'good-enough' progress. The acceptance section (lines 357-375) uses 'All tasks complete' as a single atomic criterion with no fallback, making this a fail-closed gate that admits no partial credit. | Acceptance section (line 357) states '- [ ] All tasks complete' with no sub-criteria or fallback options. The Risks section (lines 340-353) describes mitigation for individual risks but does not describe what happens if, for example, Task 1 (backlog parser fix) succeeds but Task 3 (env helper) encounters issues in test-only scenarios. The plan's Design section and Acceptance both assume binary complete/incomplete, offering no degraded-but-acceptable outcome. This is unusually strict even for this project's fail-closed gates. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified all pattern citations in L39-47 against source files (detection-corpus.js, review-single-pass-gate.test.js, backlog.js, session-activity-tracker.js). Examined Task 1-8 decomposition for hidden coupling: confirmed Task 5 correctly binds two file changes to one commit (DD7); confirmed Task 3 shared helper approach in review-single-pass-gate.test.js L35 precedent. Checked Task 4's hook-trace centralization promise against Files to Change — found the architectural claim (centralization point) is stated but its location/method is not specified. Verified hook function signatures match (both have repoRootOf), gap is in the implementation boundary. Validated Task 7 backlog processing uses existing ABSORBED marking precedent, accepts manual validation. Confirmed plan correctly avoids changing MCCP_SANTA_DELTA_SCOPE default (UI10) — not in any of the files to change. |
| security | fail | Attacked Task 4's git toplevel anchoring by tracing the data flow: (1) git rev-parse call → absolute path; (2) path.join(repoRoot, TRACE_DIRNAME) preserves absoluteness; (3) recordWrite returns full path; (4) path is assigned to traceLogPath and rendered directly in buildSurface() line 64 for emission to stdout. Verified CLAUDE.md precedent showing prior absolute-path-leak concern was fixed via repo-relative normalization. Confirmed git rev-parse --show-toplevel is documented to return absolute paths. Checked Tasks 1,3,5,6 for other issues: Task 1 (backlog parser) improves accuracy without introducing vectors; Task 3 (env isolation) improves test correctness; Task 5 (two-point→three-point diff) improves accuracy; Task 6 (version, merge safety) is administrative. No other HIGH/CRITICAL findings detected. |
| test | fail | I attacked: (1) whether test files referenced in Validate section actually exist — found backlog-source.test.js is CREATE but validated against as existing; (2) whether acceptance criteria are mechanically falsifiable vs manual — found criteria 4-6 depend on manual inspection without automated tests; (3) whether validation numbers depend on assumptions about current state — found baseline measurement of '25 pass/57 fail' depends on settings.json state at measurement time; (4) whether the two-point vs three-point diff claim is true — verified correct at prp-implement.md:1041; (5) whether backtick removal is missing — verified correct at plan-conflict-detector.js:42-44; (6) whether env isolation pattern already exists — found it at review-single-pass-gate.test.js:35 but not as shared helper yet.</refutationAttempted> |
| invariant | fail | Checked: (1) Receipt anchoring and verification mechanics — found plan-review receives no mechanical proof that the 8 implementation tasks actually complete before the receipt binds; (2) Acceptance criteria verifiability at plan-review time — found 6 of 8 items require post-implementation execution/tests; (3) Fail-closed gate erosion via ambiguous thresholds — found red-count criterion lacks precise fail-closed boundary; (4) Skip predicate coverage — found Task 7 lacks objective scope definition; (5) Rollback and partial-success paths — found none described; (6) Existing receipt staleness from M2 — confirmed M2 closure noted plan-body edits would cause stale receipts. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 447105,
  "halt_stage": null,
  "backlog_appended": 7,
  "backlog_skipped_nonblocking": 9,
  "granted": 4,
  "reviewed_plan_hash": "sha256:18f925384c0d698eeaf9e921b3b3ffbeee1f4a6b1d87d539ffb270722f61314b",
  "plan_path": ".claude/plans/santa-delta-review-m3.plan.md",
  "recorded_at": "2026-08-21T07:27:55.572Z"
}
```
