# Plan Review Panel — impeccable-detection-contract

**Plan**: `.claude/plans/impeccable-detection-contract-m1.plan.md` · **Plan version**: `sha256:17f9208e368a424250543b1dd803acc702058c7e8e17d54dcad994592b69ed8e`
**Verdict**: `divergent` via `multi-agent`
**Quorum**: 4/3 responses · 4 distinct roles (of 4 fielded) · passed=false
**Layers**: L1 converged · L2 divergent · L3 not fired
**Halted at**: `5.2e`

> Reason: L2 quorum not satisfied: 12 blocking finding(s): architect/HIGH, architect/HIGH, architect/FAIL, test/CRITICAL

## Findings

| Perspective | Severity | Claim | Evidence |
|---|---|---|---|
| architect | HIGH | M1 returns new `invocation` field but has no consumer in the M1 code path—the 4 probeSkillAvailable() call sites remain unchanged and still invoke `Skill(impeccable, ...)` with bare name | Plan line 129: '4개 호출부(`plugins/mccp/commands/plan.md:1462` · ... )는 **무변경**.' Plan line 239 explicitly defers 'hokeul부 재배선' to M3. Test case line 141 shows invocation='impeccable:impeccable' for plugin but no code consumes this value in M1. |
| architect | HIGH | Plan leaves system in inconsistent state: Detection will be correct (SKILL_AVAIL=1) but invocation will be wrong (bare name), causing Skill() call to fail downstream | Plan line 215 (Risks): acknowledges 'Skill(impeccable, ...)가 plugin 본문을 못 연다' — plan's own risk statement admits the core problem persists. Acceptance criterion line 234 requires SKILL_AVAIL=1 but doesn't require Skill() invocation to work. This is exactly the 'invocation mismatch' that defeats the design. |
| architect | LOW | Citation accuracy: Plan line 43 cites 'plan.md:817' for loud stderr pattern but actual pattern is at line 815 (code block fence shifts line numbers) | Read impeccable-detect.js confirms pattern at line 815: `echo '[mccp:impeccable] silent-skip reason=...'`. Line 817 is just closing bash fence '```'. Off-by-2 drift suggests document was updated but citations were not. |
| architect | MEDIUM | Invocation name generation from plugin key is underspecified: plan says '<pluginName>:<skillDirName>' (line 104) and to read skillDirName from filesystem (line 111), but does not specify algorithm for extracting pluginName from manifest key (e.g., how to parse 'impeccable@impeccable') | Task 1 line 104-111 specifies invocation as '<pluginName>:<skillDirName>' but provides no parsing algorithm. Test case line 141 expects 'impeccable:impeccable' for key 'impeccable@impeccable' but plan text never defines which part is pluginName, leaving ambiguity for plugins with asymmetric names. |
| architect | MEDIUM | Test fixture hardcodes wrong plugin key: line 38 uses 'impeccable@anthropics' but meta-research confirms actual installed key is 'impeccable@impeccable' (E0 observation, line 43-49 of meta-research) | Meta-research 2026-08-22-impeccable-plugin-channel-migration.md line 43-49 shows actual manifest: 'impeccable keys: ["impeccable@impeccable"]'. Current test line 38: `{ 'impeccable@anthropics': ... }`. Plan Task 5 line 159-160 acknowledges this must be changed, but this is a backward regression in test accuracy that plan fixes but doesn't prevent existing green tests from hiding the defect. |
| architect | MEDIUM | Seam integrity between M1 and M3: M1 creates new data (`invocation` field, 4 new detect() fields) but defines no consumer or forwarding mechanism, leaving M3 to reverse-engineer what information to use | Plan lines 238-239 Out of Scope: 'hokeul부 재배선 — **M3**'. No M1 task updates the 4 callers. No M1 task exports the invocation value to a location M3 can find. Plan's approach is 'surface the information' but provides no contract for M3 to consume it—M3 would have to search the codebase to find where the new field lives. |
| test | CRITICAL | Validation section line 190 can run impeccable-resolve.test.js to verify the 11-case matrix | File `plugins/mccp/scripts/lib/tests/impeccable-resolve.test.js` does not exist. Plan Task 4 says it will be **created** ('신설한다'), but Validation section (lines 188-209) tries to execute it as if it already exists. Glob search returns no match for the file. |
| test | CRITICAL | Validate line 204-205 can test detect() by running it on the plan file itself | Plan says: `node plugins/mccp/scripts/lib/impeccable-detect.js detect --mode plan --plan .claude/plans/impeccable-detection-contract-m1.plan.md --json`. This circular test can only work AFTER implementation is complete. Before implementation, the plan file does not contain design artifacts (SKILL.md references, design surface paths). Testing against the plan file before those paths exist would produce false negatives that don't reflect the actual detection behavior after tasks 1-7 complete. |
| test | HIGH | Task 5 can safely change fixture key from `impeccable@anthropics` to `impeccable@impeccable` without breaking existing tests because 'prefix matching' will work with both | Plan Task 5 (lines 160-161) states: '접두어 매칭이므로 test는 붉어지지 않지만'. However, there is no test that actually validates the prefix matching code works. The new `resolveImpeccable()` function that implements prefix matching only exists in Task 1 (which has no formal test in the main Validation section). The existing 6 `probeSkillAvailable` tests at lines 169, 178, 209, 223, 235, 247 would still pass because current code at line 139 of impeccable-detect.js still probes the literal key `IMPECCABLE_PLUGIN_KEY='impeccable@anthropics'`. Changing the fixture key does NOT test whether new prefix matching code correctly matches `/^impeccable@/`. |
| test | MEDIUM | Acceptance checkpoint 3 (line 234) can verify that `/mccp:plan` gate runs without MCCP_IMPECCABLE_SKILL set | Acceptance requires: '`MCCP_IMPECCABLE_SKILL` **미설정** 상태에서 `/mccp:plan` 게이트가 `SKILL_AVAIL=1`로 진입'. This is Task 8 (라이브 완주). But Tasks 1-7 must complete before Task 8 can run. Therefore Acceptance criteria cannot be verified during plan review — only after full implementation. This means none of Tasks 1-7 can be individually validated for correctness before code changes are applied. |
| test | MEDIUM | The existing 6 probeSkillAvailable tests remain valid after fixture change from `impeccable@anthropics` to `impeccable@impeccable` | Plan says existing tests (line 153) will not be deleted ('삭제하지 않는다'). But if fixture key is changed at Task 5, and legacy key `impeccable@anthropics` test case is moved to Task 4's new file, there is an ordering dependency: if Task 5 executes before Task 4, the fixture will be changed but the legacy test case won't exist yet to verify the legacy key still works. The plan does not document the required task execution order. |
| test | MEDIUM | Task 1's Validate command can test resolveImpeccable() function | Plan Task 1 Validate (line 118) states: `node -e "console.log(JSON.stringify(require('./plugins/mccp/scripts/lib/impeccable-detect.js').resolveImpeccable({}),...`. This inline test assumes `resolveImpeccable()` exists as an exported function in impeccable-detect.js. However, there is no specification in the plan that says it MUST be exported from that module. If it's defined but not exported, this test fails without revealing the actual problem (function exists but isn't exported). |
| invariant | CRITICAL | Task 8 acceptance validation is not mechanically enforced by the gate | Plan lines 225-234 specify 4 checkboxes and require Task 8 observations (command·output·decision) be recorded in `.claude/notes/impeccable-detection-contract-m1.md` for M1 to be complete. However, the `/mccp:plan` receipt machinery does not read this markdown file or validate these checkboxes. The gate only enforces Codex receipt convergence, not completion of documented acceptance criteria. If Task 8.2 shows path mismatch (detection broken), line 184 says to record this as UNMET, but there is no code branch that blocks approval if UNMET is recorded. |
| invariant | HIGH | Test fixture regression: existing tests will fail silently with new code but there is no atomic gate preventing partial commits | Task 1 (line 111) requires plugin source to have actual `installPath` on disk AND `skills/<name>/SKILL.md` file present. Current test at impeccable-detect.test.js:38 uses `installPath: '/fake'` which does not exist on disk. When Task 1 code runs, resolveImpeccable() will return `available:false` for this fixture. Existing test boolean regression at line 153 says preserve 6 tests for boolean contract, but fixture update (Task 4) comes after code creation (Task 1). First commit of Task 1 will have broken tests but there is no gate preventing that commit from merging independently of Task 4. |
| invariant | HIGH | Invocation field validation gap allows available=true with invocation=null | Plan line 90 shows return type with `invocation: 'impeccable' \| 'impeccable:impeccable' \| null`. While logic should prevent this combination, there is no explicit code guard documented. Test matrix at lines 135-151 enumerates 11 cases but does not include the failure case of `available=true, invocation=null`. If this combination somehow occurs, callers like `Skill(impeccable, ...)` at plan.md:731 would fail with `unknown_skill` but this is undocumented as a possible return state. |
| invariant | HIGH | Task 0 tool execution failure lacks specific fail-closed HALT | Task 0(b) (line 75) asks to execute cache binaries `context.mjs` and `doctor.mjs` to check permissions. Line 216 lists this as risk with 'likelihood=중, impact=높음'. Mitigation says 'MVP 범위에 권한 축을 추가하고 조용히 진행하지 않는다' (add scope, don't silently proceed). But there is no specific exit code, checkpoint, or mechanical gate documented that would halt M1 if Task 0(b) observes permission denial. The mitigation is "add scope" (scope creep), not "fail-closed reject". |
| invariant | MEDIUM | Version drift risk spans two sync checkpoints but only one is verified by test | Plan §3.7 and Task 7 (line 174-178) require 4-surface version sync: plugin.json · renderer/html.js · renderer/markdown.js · CHANGELOG.md. But per §3.7, only i18n-surface.test.js verifies 3 surfaces (footer 2 + derived 1), leaving CHANGELOG.md unmonitored. §3.7 documents 4 prior real incidents of version drift from parallel branches. Task 7 validation says 'patch 자리를 선점할 수 있다' but doesn't specify what happens if the number changes between base merge (Task 7 start) and `/mccp:pr` entry (Task 7 end). |
| invariant | MEDIUM | Test creates permanent debt for unverified literal via fixture preservation | Plan Task 5 (line 160) preserves test case for legacy key `impeccable@anthropics` saying it preserves backward compatibility. But PRD line 80 lists this literal's origin as Open Question: '`impeccable@anthropics`라는 리터럴은 어디서 왔는가'. The plan assumes this literal is real enough to test without evidence it ever existed or was used. This creates a permanent test obligation without a known use case. |

## Refutation attempted

| Perspective | Verdict | What was attacked |
|---|---|---|
| architect | fail | Verified plan's core claims: (1) Plugin skills register as `plugin:skill` namespace per multi-file evidence (mccp uses `Skill(mccp:*)` invocation, v0.2-s9 tests show `Skill(mccp:impeccable)` disambiguation); (2) Actual installed key is `impeccable@impeccable` not `impeccable@anthropics` per meta-research observation; (3) Current code looks for wrong key and wrong path, so detection fails; (4) Plan's resolveImpeccable() addresses detection but probeSkillAvailable() remains a thin wrapper returning only boolean; (5) Verified all 4 call sites (plan.md:1462, plan.md:1862, prp-implement.md:224, codex-runner.js:277) remain hardcoded to bare `Skill(impeccable, ...)`. Checked whether new detect() fields or resolveImpeccable() return value are consumed—found no consumer in M1 code paths. Searched for alternative mechanisms to pass invocation name to callers—none exist. Examined plan's Out of Scope section: explicitly defers caller rewiring to M3. Found test fixture encodes literal wrong key. Located meta-research: corroborates all structural issues. Identified seam gap: M1 creates data M3 must use but doesn't establish contract or location for handoff. |
| security | pass | 1. Path leakage to receipts: Confirmed plan does NOT propose stamping new `impeccable_path` field to receipts (schema.js not in Files to Change, new fields don't appear in receipt schema). Path data remains diagnostic-only, not durable. 2. Plugin-only escalation: Confirmed plan acknowledges plugin calling-convention mismatch via PRD Risk 1 + documented handoff to M3. Validation (Task 8, criterion 2) mechanically catches this via path-mismatch test. 3. Malicious plugin manifest: Confirmed upstream responsibility (Claude's plugin manager writes installed_plugins.json); plan reads passively, parsing SKILL.md frontmatter only for version enum with non-string → null, never executes. 4. Symlink attacks: File reads check `skills/<name>/SKILL.md` existence and parse as YAML frontmatter; symlink-following does not grant file-escape (attestation still scoped to skills/ subtree). 5. Frontmatter parsing hardness: Plan limits read to 8KB; parsing failures return null (no throw). 6. Directory name injection (skillDirName): Plan does not explain extraction logic (unspecified but likely glob + basename); no downstream code execution on name (only used as Skill invocation reference, validated by Skill system). 7. Version string format: No format validation but returned as JSON data only, not used in shell/SQL contexts; injection risk is LOW. 8. Trust boundary clarity: Root cause is impeccable plugin registered as `impeccable:impeccable` but mccp calls bare `impeccable` — plan returns both sources, Skill() resolution determines actual use, caller invariant preserved. |
| test | fail | Attacked the claim that Validate lines reference testable artifacts by checking file existence (found `impeccable-resolve.test.js` missing). Attacked the claim that circular validation (running detect on the plan file) works pre-implementation by analyzing what design artifacts must exist. Attacked the claim that fixture key change won't break tests by examining the prefix matching code and finding it's untested. Attacked the Acceptance criteria by noting Task 8 dependencies on Tasks 1-7. Examined fixture interdependencies between Task 4 and Task 5. Checked module export assumptions in Task 1 Validate. |
| invariant | fail | Attacked the plan's gating and acceptance criteria by (1) examining the Acceptance section (lines 225-234) and verifying that markdown checkboxes are not mechanically enforced by receipt machinery; (2) tracing the test fixture path through Task 1 (SKILL.md requirement) and Task 4 (test updates) to expose regression window before atomic commit; (3) checking return type definition for invocation field nullability and tracing call sites (plan.md:731) to confirm lack of guard clause; (4) reading the Risk mitigation for tool execution (line 216) and confirming it says \\"add scope\\" not \\"halt\\"; (5) checking test coverage in renderer/tests/i18n-surface.test.js against the 4 version sync surfaces and finding CHANGELOG.md unmonitored; (6) examining PRD Open Questions to establish that `impeccable@anthropics` origin is unresolved. Did not find evidence of mechanical gates that would enforce acceptance criteria, atomicity of test fixes, invocation validation, Task 0 halt, comprehensive version drift detection, or justification for legacy literal preservation. |

## Measurement

<!-- Written by plan-review/cli.js record on EVERY exit path, pass or halt.
     Machine-readable; do not hand-edit. A null field means the axis was
     not observed, never that it was zero. -->

```json
{
  "verdict": "divergent",
  "source": "multi-agent",
  "layers": {
    "l1": "converged",
    "l2": "divergent",
    "l3": "not fired"
  },
  "quorum": {
    "responded": 4,
    "required": 3,
    "roles": 4,
    "of": 4,
    "passed": false
  },
  "wall_clock_ms": 466027,
  "halt_stage": "5.2e",
  "backlog_appended": null,
  "backlog_skipped_nonblocking": null,
  "granted": 4,
  "reviewed_plan_hash": "sha256:17f9208e368a424250543b1dd803acc702058c7e8e17d54dcad994592b69ed8e",
  "plan_path": ".claude/plans/impeccable-detection-contract-m1.plan.md",
  "recorded_at": "2026-08-22T07:51:24.927Z"
}
```
