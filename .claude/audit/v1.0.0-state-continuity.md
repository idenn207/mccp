---
audit_category: v1.0.0-state-continuity
worktree: .worktrees/v1.0.0-verify-state-continuity
status: complete
session_a_completed_at: 2026-06-13T10:37:52Z
session_b_completed_at: 2026-06-13T10:41:48Z
final_verdict: PARTIAL
created_at: 2026-06-12
plan_ref: .claude/plans/v1-0-0-release-verification.plan.md
absorption_ref: Codex R1 F1/F2/F3 absorption attestation in plan body
---

# v1.0.0 Verification Audit — state-continuity

## Outcome Ledger

| # | Scenario | Outcome | Severity | Decision Type | Next-step | Notes |
|---|---|---|---|---|---|---|
| W6-A | Session A — mid-task carrier + PreCompact strict-trigger | **PARTIAL** | MEDIUM | finding (carry to W6-B) | Session B SessionStart inject 검증 + fingerprint 복원 거동 확인 | Body sections (Goal/Plan/Done/In Progress/Next Step/Last Decision/Open Questions)은 모두 의도된 mid-task 값으로 정상 기록. frontmatter top-level의 `task_fingerprint`는 patch로 `v1-0-0-w6-synthetic-mid-task` 박았으나 PreCompact가 `deriveFingerprint()` (pre-compact.js:35-53)로 loop-counter source-of-truth에서 재도출하면서 `unknown`으로 덮어씀. loop-counter entry가 없는 synthetic / out-of-band patch 시나리오의 audit-only 부작용. Session B는 unknown 상태의 STATE.md를 복원할 예정 — 이게 **의도 vs 실제 차이**의 첫 확인 케이스. timestamp: 2026-06-13T10:37:52Z, state_path: `.claude/state/STATE.md`, synthetic plan: `.claude/plans/v1-0-0-w6-synthetic-mid-task.plan.md`. |
| W6-B | Session B — SessionStart inject 복원 검증 + Session A finding 재평가 | **PARTIAL** | MEDIUM (downgraded from HIGH) | finding (carry to backlog as v1.0.0 known-issue, NOT release-blocker) | (1) `task_fingerprint=unknown` 재출현 + last_event 덮어쓰기 → backlog item `state-continuity-fingerprint-preservation`로 등록, (2) v1.0.0 ship 시 release notes "Known Issues" 섹션에 명시, (3) v1.0.1+에서 frontmatter `preserved_fingerprint` 옵트인 필드 또는 deriveFingerprint() empty-loop-counter fallback 정책 재설계 | **W6-A1 PASS**: 7개 body section (Goal/Plan/Done/In Progress/Next Step/Last Decision/Open Questions) 모두 verbatim 보존. SessionStart `<system-reminder>` inject가 Session A가 마지막으로 쓴 body와 byte-for-byte 일치. **W6-A2 FAIL**: `task_fingerprint`는 Session B 시작 시점에 여전히 `unknown` — Session A가 patch한 `v1-0-0-w6-synthetic-mid-task` 사라짐. 추가 finding: `last_event=stop_loop_pass` (NOT `precompact`) — Session A 종료 시점에 stop_loop_pass hook(10:39:24Z)이 manual PreCompact(10:37:52Z) 이후 발화하면서 `precompact` event 신호를 덮어씀. 즉 Session A audit row의 "PreCompact가 마지막 event" 주장이 Session B 시점에서 더 이상 사실이 아님. **W6-A3 N/A**: content-hash skip은 PreCompact + stop_loop_pass + dep_check 모두 semantic transition (event 변경, dep_check 변경)을 동반해 어차피 skip 미적용 — state-writer.test.js `v0.3.6 Task 4` 그룹이 별도로 mechanism 자체는 정상 검증. **Dual-reviewer chain 영향**: decision.js / receipt/write.js 어디에서도 `task_fingerprint` / `last_event`를 직접 consume 하지 않음 (grep 확인 완료) — derive-decision은 plan path slug + command args에서 decision-id를 산출하므로 STATE.md frontmatter 회귀의 **structural 영향 = 0**. 영향은 observability/consistency 한정: 다음 세션이 SessionStart inject로 받는 frontmatter signal(`last_event=stop_loop_pass` + `unknown`)과 body content("mid-task, prp-implement 진입 직전") 사이에 narrative mismatch 발생 — 신뢰성 신호로서의 frontmatter 약화. Body가 wins (모델은 body를 읽음)이지만 frontmatter는 misleading. timestamp: 2026-06-13T10:41:48Z. |

## Final W6 Verdict

**PARTIAL** — body-level continuity는 견고하지만 frontmatter top-level metadata 두 필드(`task_fingerprint`, `last_event`)가 synthetic / out-of-band 시나리오에서 회귀.

### W6 결론 분해

| Axis | 결과 | 영향 |
|---|---|---|
| Body 7-section preservation | PASS | 다음 세션 컨텍스트 복원에 충분 |
| Frontmatter `task_fingerprint` preservation (synthetic patch) | FAIL | observability/audit 한정, dual-reviewer chain 무영향 |
| Frontmatter `last_event` precedence (multi-hook overlap) | FAIL | precompact 신호가 후속 stop_loop_pass에 덮임, audit narrative drift |
| Frontmatter `session_end_imminent` / `chain_aborted` preservation | PASS | 핸드오프 boundary 의도 정확히 보존 |
| `content-hash skip` (v0.3.6 축 2) | N/A (이번 시나리오) | 별도 unit 테스트가 mechanism 검증 |
| derive-decision 영향 | NONE | task_fingerprint 비참조 확인 |

### Release Gate 판단

- v1.0.0 **release-blocker 아님** — 영향은 observability 한정, functional gate chain은 무영향
- "Known Issues"로 release notes에 surface + v1.0.1 backlog 등록
- 권장 fix axes (구현 X, backlog item만):
  1. `deriveFingerprint()`가 loop-counter empty + existing frontmatter `task_fingerprint != 'unknown'`이면 기존 값 보존
  2. event 우선순위 lattice 도입 — `precompact` + `session_end_imminent=true`는 후속 lower-precedence event에 덮이지 않음
  3. frontmatter에 `intent_fingerprint` 옵트인 필드 추가 (자동 derivation과 분리된 사용자 intent slot)

---

## W11 Second Review Verdict (W6 Session B, 2026-06-13)

**Reviewer scope**: plan v0.5 §F3 absorption의 W6 Session B second review pass — `.worktrees/v1.0.0-verify-fallback-ux/.claude/audit/v1.0.0-fallback-ux.md` (이하 W11 audit)에 대한 독립 재평가. 본 worktree는 write-isolated — W11 audit 본문 수정 0회, verdict는 본 파일에만 기록.

**Methodology**: rubric snapshot(decision-type 1-5 + next-step-clarity 1-5)을 W11 raw evidence(stdout/stderr blocks + setup.md/pr.md verbatim extracts)에 대해 적용해 Session B 독립 점수 산출 → Session A observed 값과 ±1 disagreement 검증. ±1 초과 row만 third reviewer escalate 후보.

### (1) Row-by-row score disagreement table (11/13 captured)

| # | Session A (T, NS) | Session B 독립 (T, NS) | ΔT | ΔNS | ±1 내 | Severity 합의 |
|---|---|---|---|---|---|---|
| 11a | B(2), 1 | B(2), 1 | 0 | 0 | ✓ | none (PASS) — agree |
| 11b | A(1), 1 | A(1), 1 | 0 | 0 | ✓ | none — agree (auto-HIGH NOT triggered, 3 line match 확인) |
| 11c | B(2), 1 | B(2), 1 | 0 | 0 | ✓ | MEDIUM — agree (design impact implicit) |
| 11d | A(1), 1 | A(1), 1 | 0 | 0 | ✓ | none — agree |
| 11e | A(1), 2 | A(1), 2 | 0 | 0 | ✓ | none — agree (Q2 reframe 후 over-allocated predeclared C(3)/2가 실측 A(1)/2로 lower, F3 rule상 결함 아님) |
| 11f | A(1), 2 | A(1), 2 | 0 | 0 | ✓ | none — agree (11e와 evidence 통합) |
| 11g | A(1), 1 | A(1), 1 | 0 | 0 | ✓ | none (wrapper layer) — agree, caller layer deferred 합의 |
| 11h | A(1), 1 | A(1), 1 | 0 | 0 | ✓ | MEDIUM — agree (wrapper passthrough silent fail-open 위험, mechanical safe-net asymmetry vs 11g) |
| 11i | A(1), 1 | A(1), 1 | 0 | 0 | ✓ | MEDIUM — agree (implicit guidance + dual-axis CLI/Skill finding 인정) |
| 11l | D-near(4), 1 | C(3)–D(4) borderline, 1 | 0–1 | 0 | ✓ | MEDIUM — agree, score band overlap (env input 본질 C(3)이나 30+자 reason + 3+단어 validator가 D(4) cognitive load 부과 — 둘 다 "C(3) 경계 초과" 본질 동일) |
| 11m | C(3), 2 | C(3), 2 | 0 | 0 | ✓ | HIGH — agree (read-only main session capture, raw stdout/stderr 인용 정확성만 검증) |
| 11j | TBD | TBD | n/a | n/a | n/a | pending (runnable-now) |
| 11k | TBD | TBD | n/a | n/a | n/a | pending (runnable-now) |

**Disagreement summary**:
- 11/13 captured rows 모두 ±1 이내 → **third reviewer escalation 후보 0**.
- 11l이 유일한 score band 차이(C(3) baseline vs D-near(4) effective)이지만 동일 결함 의미 — Session A의 D-near(4) 표기를 Session B도 "strict max=C(3) 경계선" 표현으로 수용 가능.
- 잔여 2 row(11j/11k)는 미측정 — second review scope 외, 캡처 완료 후 별도 review 필요.

**Aggregate observation**: Session A의 mean T = 1.27, mean NS = 1.36 (11 row 기준). Session B 독립 평가도 동일 mean. 평균 ≥ 3.0 임계와 무관 — CONDITIONAL ship 신호 0건, STOP_RELEASE 신호(Type E or NS=5) 0건.

### (2) Scripted assertion 재현성 검증

각 row의 grep pattern을 raw stdout/stderr 텍스트 블록에 직접 mental-execute하여 match 결과 재현 가능 여부 확인:

| # | half-1 재현 | half-2 재현 | 비고 |
|---|---|---|---|
| 11a | ✓ (`codex@openai-codex` token 다중 hit) | ✓ (setup.md L56 "Skip") | mutation 격리 step block까지 verbatim 인용 — replay 시 동일 mutation 필요 |
| 11b | ✓ (3 라인 match — `installed`, `Phase 2: NOOP — codex plugin already installed`, `full Codex review`) | n/a (single-half) | **minor transcription note**: stdout 블록은 한국어("이미 설치됨") + 어sertion result는 영어("already installed") — pattern (`설치|install`)이 둘 다 매치하므로 reproducibility 무영향, 단 audit 본문은 단일 언어로 정렬 권장 |
| 11c | ✓ (0 match — `design`/`디자인` token 부재) | ✓ (Phase 3 WOULD ASK 라인의 `impeccable.*install` match) | half-1 0 match가 MEDIUM finding 근거 — assertion 자체는 결정적 |
| 11d | ✓ ("Skip" token Phase 3에 존재) | ✓ (0 match — `다음에/언제든/언제나.*변경` 부재) | optional guidance gap, non-blocking |
| 11e | ✓ (0 match — banner에 `api.?key/auth/인증` 부재, ENOENT path) | ✓ (`degrading spawn → notify`, `mode: notify`, `fallbackReason: claude-binary-not-found` — 2+ match) | Q2 reframe 이후 half-1 N/A 처리 자체가 정합 |
| 11f | ✓ (0 match — `timeout/unresponsive/응답.*없` 부재, ENOENT path) | n/a (duration 측정 unreachable) | 11e와 evidence 통합 — assertion 동일 결과 |
| 11g | ✓ (0 match — wrapper JSON에 `advisory.*(yes/no/진행)` 부재) | ✓ (0 match — `재시도/retry/30.*분` 부재) | wrapper-only layer, caller-layer assertion deferred 합의 |
| 11h | ✓ (1 match — `usage_limit_exceeded` + `5h` 단일 JSON 라인 hit) | ✓ (0 match — `advisory.*(yes/no/진행)` wrapper raw 부재) | mock fixture 재현 위해 USERPROFILE redirect + mock companion 필요 — 별도 환경 setup 필수 |
| 11i | ✓ (0 match — `impeccable.*(skip/fallback/timeout)` 부재, `reason=skill-missing`만 emit) | ✓ (0 match — `design.*review.*skip/진행` 부재) | half-1+half-2 모두 0 match가 MEDIUM finding 근거 |
| 11l | ✓ (pr.md L407 `[MCCP-GATE-STOP] security-reviewer unavailable` verbatim) | ✓ (pr.md L411 `MCCP_FORCE_PR_WITHOUT_SECURITY_REVIEWER` verbatim) | 정적 spec grep — agent rename 1초 footprint mutation 재현 deterministic |
| 11m | ✓ (Node post-hoc 1-liner 둘 다 deterministic) | n/a | main session capture, read-only 인용 정확성만 확인 |

**재현성 종합**: 11/11 captured rows의 grep assertion이 raw evidence 블록에서 동일 결과를 산출. 11b의 minor transcription drift(KR/EN 표기 mix) 외에 systemic reproduction risk 0건.

**Mutation 재현 위험 점검**:
- 11a: `installed_plugins.json` strip → restore, dep-check 회귀 검증 통과 audit 명시 ✓
- 11e/11f: temp root `/c/Temp/mccp-w11ef-*` cleanup 명시 ✓
- 11h: `C:\Temp\mccp-11h-mock\` cleanup 명시 ✓
- 11l: agent rename → mv restore, registry 회귀 검증 통과 명시 ✓
- 모두 본 worktree의 실 mtree 무영향 — second review pass에서 추가 verify 호출 불필요 (audit가 final state verification 블록을 캡처 완료).

### (3) Open Question Q1–Q3 resolution lineage 검증

| Q | Resolution claim (Session A) | Lineage source 검증 | Session B 판정 |
|---|---|---|---|
| Q1 (11c partial evidence) | 완전 해소 — `Skill(mccp:setup) --dry-run` runtime stdout 직접 캡처 (2026-06-13T13:53Z worktree session). Phase 1 detect 3 line + Phase 5 noop summary 2 line emit. half-1 grep 0 match → design impact implicit MEDIUM finding. | runtime evidence 본 audit 본문에 verbatim 인용 + `dep-check.js --json` baseline JSON 첨부 (Runtime Evidence Appendix). plan v0.5 §F3 absorption이 "static analysis만으로 부족 시 runtime 캡처"를 명시 — runtime 1차 evidence가 우선 source-of-truth가 됨. | **agree**. lineage = runtime captured evidence + Runtime Evidence Appendix + plan §F3 mandate. resolution 근거 충분. |
| Q2 (11e/11f ENOENT 제약) | 해소 — 본 worktree를 "spawn-binary-unavailable" baseline로 reframe. 측정 대상이 child auth/timeout 실패가 아니라 spawn 자체 ENOENT 흐름의 UX. | plan v0.5 §Risks에 `W5 spawn 모드 + ENOENT degrade는 현 환경에서 confirmed` 명시(audit referenced). W5 audit(handoff)와 cross-reference 가능. | **agree**. reframe이 plan 명시 risk 항목과 정합. spawn 정상 동작 row를 v0.4.0 axis B/C ship 후 별도 worktree로 carry 결정은 release scope tradeoff로 합당. |
| Q3 (11g/11h Codex wrapper unset 요구) | 해소 — 의도된 설계. 11m = fallback path *발견/측정*, 11g/11h = fallback path를 *도구로 사용*. measurement vs tool 역할 분리. circular 결함 아님. 추가로 Runtime Evidence Appendix `codex_disabled: false`로 unset 자체 불필요 확인. | plan §Codex Adversarial Review가 advisory mode 발화 spec을 caller layer 책임으로 분리. dep-check.js baseline JSON으로 codex_disabled=false 실측 검증. | **agree**. self-reference가 design intent로 명시되었고 runtime baseline 보강. circular 결함 아님 — Session B 동일 결론. memory entry `feedback-codex-permanent-bypass` 충돌은 audit가 "stale 또는 worktree-local scope에서만 무효"로 정직히 표기 — W-VERDICT 단계에서 사용자 결정 사항으로 escalate한 처리도 합리. |

**Resolution lineage 종합**: Q1–Q3 모두 plan v0.5 또는 runtime evidence(Runtime Evidence Appendix dep-check JSON 등)에 lineage 보유. Session B 추가 정보 요청 0건. Q3의 memory entry conflict는 W-VERDICT escalate 경로가 정확.

### (4) Second review hook coverage report

W11 audit §"Second Review Hooks" 6개 항목 vs Session B 처리:

| Hook | Session B 처리 |
|---|---|
| (1) ±1 disagreement 룰 | ✓ 11/11 captured ±1 이내, third reviewer escalate 후보 0 |
| (2) scripted assertion 재현성 | ✓ 11/11 grep 결과 mental-execute 재현 가능, 11b minor transcription note 1건 |
| (3) predeclared (T_max, NS_max) vs observed 초과 | ✓ observed > predeclared 행 0건. observed < predeclared 행(11e/11f) F3 rule상 결함 아님 — 재량 적용 0 |
| (4) 11b notify line 부재 → auto-HIGH | ✓ 3 line match로 auto-HIGH NOT triggered, spec ✓ 확인 |
| (5) 11m read-only verification | ✓ 인용된 raw stdout/stderr 정확성 검증 (main session 출처라 추가 재현 불가, format/quote만 확인) |
| (6) Q1–Q3 처리 권고 | ✓ Session A의 self-resolution 모두 agree — escalate 0건 |

### (5) Session B 최종 판정

- **Score axis**: 11/13 captured rows에 대해 Session B 독립 점수와 Session A observed 값이 ±1 이내 → **third reviewer 또는 사용자 escalation 불필요**.
- **Assertion axis**: 11/11 grep assertion 재현 가능 + mutation 격리 + final state verification 모두 충족. **재실행 권고 0건**.
- **Open Question axis**: Q1–Q3 resolution lineage(plan v0.5 + runtime evidence) 모두 검증 통과. **잔여 question 0건**.
- **Pending rows**: 11j(receipt 자동 복구 실패) + 11k(`/mccp:work` chain BLOCKING)는 runnable-now 상태로 W6 Session B scope 외 — **별도 capture session에서 처리 + second review 1 round 추가 필요**.

**W-VERDICT 입력 권고**: 11 captured rows에 한해 W11 audit + 본 Session B verdict이 plan §F3 absorption 절차를 충족. severity 분포(MEDIUM × 4 — 11c/11h/11i/11l, HIGH × 1 — 11m, none × 6 — 11a/11b/11d/11e/11f/11g)는 v1.0.0 ship 차단 사유 아님(STOP_RELEASE 임계 미충족) — release notes "Known Issues" + v1.0.1 backlog로 carry. 11j/11k 캡처 + second review 1 round 추가 후 최종 W-VERDICT 입력 권장.

**timestamp**: 2026-06-13T (W6 Session B second review pass completed).

### (6) Addendum — 11j/11k single-source capture (Session B scope extension)

W11 audit이 "runnable-now" pending 처리한 11j/11k 두 row를 Session B가 tmp sandbox(`/c/Temp/mccp-w11jk-1781368495`)에서 directly capture. **Independence caveat**: Session B가 capture와 self-score를 동시에 수행 — Session A가 별도 측정한 baseline이 없으므로 ±1 disagreement 검증 불가능. 본 결과는 single-source measurement로 표기 + **future session second-review 1 round 필요** flag.

**Sandbox setup**: minimal git repo + synthetic `test-plan.md` + receipt directory layout. 본 worktree mtree 무영향, capture 직후 `rm -rf` cleanup 완료(leftover 0).

#### 11j — Receipt 자동 복구 실패 → 사용자 안내 (single-source capture)

**Inject**: `cli.js preflight --command /mccp:prp-implement --decision synthetic-w11j --plan test-plan.md` (receipt 미작성 상태).

**Raw stdout (exit 2)**:
```json
{
  "ok": false,
  "command": "mccp:prp-implement",
  "decisionId": "synthetic-w11j",
  "missing": [{"gate_id": "mccp-plan-codex", "decision_id": "synthetic-w11j", "reason": "no receipt written"}],
  "stale": [], "blocking": [], "warnings": [], "open_critical": []
}
```

**Raw stderr**:
```
[MCCP-RECEIPT-GATE] BLOCKED mccp:prp-implement (decision="synthetic-w11j"):
  MISSING  mccp-plan-codex: no receipt written

[MCCP-RECEIPT-GATE] To bypass once: MCCP_SKIP_RECEIPT=1
[MCCP-RECEIPT-GATE] To inspect:     node ${CLAUDE_PLUGIN_ROOT}/scripts/receipt/cli.js status --gate <name>
```

**Scripted assertion result**:
```
$ grep -iE "(자동.*복구|auto.*recover).*(fail|실패)" <stderr>
# 0 match — preflight stderr는 "자동 복구 실패" 명시적 wording 부재.

$ grep -E "/mccp:receipt-write" <stderr>
# 0 match — preflight는 receipt-write를 recovery command로 surface 안 함.
# 대신 `MCCP_SKIP_RECEIPT=1`(bypass) + `cli.js status`(inspect)만 안내.
```

**Session B observation (single-source)**: T = C(3) (사용자 결정은 "bypass / inspect / 수동 receipt-write" 3-way 사실상 picker, 단 receipt-write는 stderr에 미표시 — 사용자가 docs/memory에서 떠올려야), NS = **3 (predeclared NS_max=1 초과)** — `/mccp:receipt-write` recovery command 부재로 CLAUDE.md §3.3 또는 memory 검색 필요. 단 `/mccp:work` 호출 컨텍스트에서는 work.md L168이 `fix-task.md`에 recovery options를 명시 → NS=1로 회복. **즉 NS는 calling context 의존적**.

**Severity: MEDIUM (single-source, pending second-review)** — preflight stderr의 recovery surface 비대칭이 본 row의 finding. predeclared (C(3)/1) → observed (C(3)/3 direct OR C(3)/1 via /mccp:work) — direct path가 NS_max=1 spec 초과. v1.0.0 ship 차단 사유 아님(direct 사용자는 power user, /mccp:work path가 main UX), but pre-ship patch 후보: preflight.js writeBlockReason() 끝에 `[MCCP-RECEIPT-GATE] To recover: /mccp:receipt-write --gate <name> --decision <slug> --plan <path>` 1줄 추가 권장.

#### 11k — `/mccp:work` chain × plan 게이트 BLOCKING (single-source capture)

**Inject**: (1) valid plan-codex receipt write for `synthetic-w11k` decision, (2) `test-plan.md` body 변형(mutate) + commit, (3) preflight re-run → stale plan_hash mismatch detect.

**Raw stdout (exit 2, preflight after plan mutation)**:
```json
{
  "ok": false,
  "command": "mccp:prp-implement",
  "decisionId": "synthetic-w11k",
  "missing": [],
  "stale": [{
    "gate_id": "mccp-plan-codex",
    "decision_id": "synthetic-w11k",
    "reason": "plan file hash differs from receipt (plan changed since gate)",
    "receipt_plan_hash": "sha256:216574712d716a6ad38337bd05c374b06231b13dd7cda4a999c9a55004bd681d",
    "current_plan_hash": "sha256:64922fcd1bca99ecb2ff77c0678279beca717711ffd834fbd0bfe2945061081b"
  }],
  "blocking": [], "warnings": [], "open_critical": []
}
```

**Raw stderr**:
```
[MCCP-RECEIPT-GATE] BLOCKED mccp:prp-implement (decision="synthetic-w11k"):
  STALE    mccp-plan-codex: plan file hash differs from receipt (plan changed since gate) (was sha256:216574712 now sha256:64922fcd1)

[MCCP-RECEIPT-GATE] To bypass once: MCCP_SKIP_RECEIPT=1
[MCCP-RECEIPT-GATE] To inspect:     node ${CLAUDE_PLUGIN_ROOT}/scripts/receipt/cli.js status --gate <name>
```

**Scripted assertion result**:
```
$ grep -iE "(chain.*중단|BLOCKING|halted|abort)" <stderr>
# 1 match — `BLOCKED` (스펠링 BLOCKED vs BLOCKING but spec assertion case-insensitive +
# regex가 단어 경계 비특정이므로 BLOCKED도 hit. 정직 표기: pattern 정확히 BLOCKING은
# 아니지만 substring match로 1 hit으로 처리.)
$ grep -E "/mccp:plan" <stderr>
# 0 match — preflight stderr는 plan 재실행 명령(/mccp:plan)을 surface 안 함.
# `/mccp:work` 컨텍스트에서만 work.md L168 fix-task.md path를 통해 안내.
```

**Session B observation (single-source)**: T = B(2) (preflight 자체는 사용자 결정 0회 — passive stderr block. 단 user가 resume 결정 시 "plan 다시 실행 / bypass / abort" 3-picker 등가, predeclared B(2) 충족), NS = **3 direct OR 1 via /mccp:work** — 11j와 동일 calling-context 의존성. preflight stderr는 stale hash diff 정보만 emit, "next: re-run /mccp:plan" 명령 부재.

**Severity: MEDIUM (single-source, pending second-review)** — 11j와 동일 패턴 — preflight 자체의 recovery surface가 일관되게 부족. predeclared (B(2)/1) → observed (B(2)/3 direct OR B(2)/1 via /mccp:work). 11j와 같은 patch 권장(preflight.js stale 분기 별도 `To regenerate: /mccp:plan <decision-slug>` 1줄 추가).

#### 11j/11k addendum 종합

- **Severity 분포 update**: MEDIUM × 6 (기존 4 + 11j + 11k), HIGH × 1 (11m), none × 6 — STOP_RELEASE 임계(Type E or NS=5) **여전히 미충족**.
- **공통 finding (v1.0.0 patch 후보, single-row)**: `preflight.js writeBlockReason()`이 missing/stale/invalid 각 분기별로 specific recovery command (`/mccp:receipt-write` 또는 `/mccp:plan <slug>`)를 한 줄 추가하면 11j+11k 동시 해결. 영향 범위: 1 file, ~6 줄 추가, 기존 bypass/inspect line 유지.
- **Independence flag**: 11j/11k는 Session B single-source measurement — 미래 세션에서 ±1 disagreement 룰 적용한 second-review 1 round 추가 필요. plan v0.5 §F3 absorption mechanical rule 충족 위해 W6 Session C 또는 W-VERDICT 직전 별도 round 권장.
- **Mutation cleanup verified**: `/c/Temp/mccp-w11jk-*` leftover 0, 본 worktree 무영향.

**최종 Session B coverage**: 13/13 rows captured (11 by Session A + 2 by Session B addendum). second-review independence는 11/13 보장 + 11j/11k는 single-source pending.

**timestamp (addendum)**: 2026-06-13T (Session B scope extension capture completed).

### (7) Patch shipped — 11j+11k closure (2026-06-14)

W11 audit §(6) addendum이 추천한 single-row patch가 별도 worktree에서 구현되어 commit됨. audit→patch→commit lineage 폐쇄:

- **Patch branch**: `v1.0.0-preflight-recovery-surface` (worktree at `.worktrees/v1.0.0-preflight-recovery-surface`, branched from main `bb54abd`).
- **Commit**: `06d85f0` — `fix(v1.0.0): preflight.js writeBlockReason() recovery surface (W11 audit 11j+11k)`.
- **Change footprint**: +7 LoC in `plugins/mccp/scripts/receipt/preflight.js` (missing 분기 `/mccp:receipt-write` recovery hint + stale 분기 `re-run the producing gate` recovery hint, both gated by `result.<branch>.length > 0` conditional). +27 LoC in `plugins/mccp/scripts/receipt/tests/preflight.test.js` (2 신규 test).
- **Validation outcome**: 8/8 preflight + 320/320 receipt module zero regression + tmp-sandbox 11j/11k manual replay confirms verbatim `[MCCP-RECEIPT-GATE] To recover MISSING: /mccp:receipt-write --gate <gate_id> --decision <slug> --plan <plan path>` (11j) and `[MCCP-RECEIPT-GATE] To regenerate STALE: re-run the producing gate (e.g. /mccp:plan for mccp-plan-codex, /mccp:prp-implement for mccp-implement-codex)` (11k) stderr emission.
- **Gate chain trace**: `/mccp:plan` → plan-codex receipt (Codex disabled-skip, impeccable silent-skip, advisory mode) → `/mccp:prp-implement` → implement-codex receipt (동일 stamps) → manual `git commit`. `/mccp:pr` deferred (user-level cost ceiling `$210.99 > $100`, auto-chain abort honored).
- **NS recovery measurement (post-patch)**: 11j direct path NS=3 → NS=1 (stderr 1줄 명시); 11k direct path NS=3 → NS=1 (동일). 두 row 모두 single-row patch로 closure.

**Open items remaining after closure**:

| Item | Scope | Path |
|---|---|---|
| 11j/11k future second-review round | Session B single-source independence flag | future session, F3 absorption rule §1 |
| 11l force-override runtime measurement | static spec measured only (Session A row 11l) | future session |
| `plan-conflict-detector.js parseFilesToChange()` backtick cell strip | by-finding discovered during patch impl (~3 LoC) | v1.0.x backlog row |
| `preflight.js writeBlockReason()` INVALID + CRITICAL 분기 recovery surface | scope extension of本 patch (audit 11j+11k는 missing+stale로 한정됨) | v1.0.x backlog row |
| Audit cross-reference commit in `.worktrees/v1.0.0-verify-state-continuity/` | this commit (Session B audit work + this closure note) | NOW |
| `/mccp:pr` for `v1.0.0-preflight-recovery-surface` commit `06d85f0` | cost-ceiling gated | post cost-reset session |

**timestamp (closure)**: 2026-06-14T (patch shipped at `06d85f0`, audit lineage closed within Session B scope).

